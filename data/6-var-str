[{:lociüÕ:projects[I"activemodel_proj:ETI"activerecord_proj;TI"backup_proj;TI"bundler_proj;TI"cancan_proj;TI"capistrano_proj;TI"compass_proj;TI"cucumber_prok;TI"devise_proj;TI"diaspora_proj;TI"haml_proj;TI"homebrew_proj;TI"jekyll_proj;TI"mongoid_proj;TI"nokogiri_proj;TI"omni_proj;TI"paperclip_proj;TI"redmine_proj;TI"rescue_proj;TI"rspec_proj;TI"sinatra_proj;TI"SiriProxy_proj;TI"state_machine_proj;TI"twitter_proj;TI"whenever_proj;T:options[	I"
--var;TI"
--str;TI"
--fun;TI"--fargs;T[≈[I"end;FI"end;FI"end;FI"end;FI"end;FI"end;Fiõ[õ[	I":data//activerecord_proj/active_record/aggregations.rb;TicI"Ø          else
            var.each { |arglist| self[var.first] = var.send(var.last) }
            @var[var] = var.freeze
          end
        end
      end
    end
  end
end;Fi[	I"Vdata//activerecord_proj/active_record/associations/has_one_through_association.rb;TiI"ª              var.update(var)
            else
              owner.new_record? ? (var.build(var)) : (var.create(var))
            end
          end
        end
      end
    end
  end
end;Fi[	@3iI"ù            else
              owner.new_record? ? (var.build(var)) : (var.create(var))
            end
          end
        end
      end
    end
  end
end;Fi[	I"[data//activerecord_proj/active_record/associations/preloader/collection_association.rb;TiI"±            var.loaded!
            var.target.concat(var)
            var.each { |arglist| var.set_inverse_instance(var) }
          end
        end
      end
    end
  end
end;Fi[	I"Ydata//activerecord_proj/active_record/associations/preloader/singular_association.rb;TiI"Ø            var = var.association(reflection.name)
            var.target = var
            var.set_inverse_instance(var)
          end
        end
      end
    end
  end
end;Fi[	I"Mdata//activerecord_proj/active_record/attribute_methods/serialization.rb;TiÉI"}            @var[var].serialized_value
          else
            super
          end
        end
      end
    end
  end
end;Fi[	I"Ydata//activerecord_proj/active_record/connection_adapters/postgresql/array_parser.rb;Ti]I"«        def function(arglist)
          unless (var.length == 0) then
            var.! and (var == "STRING") ? (var.push(nil)) : (var.push(var))
          end
        end
      end
    end
  end
end;Fi[	I"Qdata//activerecord_proj/active_record/connection_adapters/postgresql/cast.rb;TiãI"n            var
          else
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	I"bdata//activerecord_proj/active_record/connection_adapters/postgresql/referential_integrity.rb;TiI"#          ensure
            if supports_disable_referential_integrity? then
              execute(tables.collect { |arglist| "STRING" }.join("STRING")) rescue execute(tables.collect { |arglist| "STRING" }.join("STRING"))
            end
          end
        end
      end
    end
  end
end;Fi[	@GiI"            if supports_disable_referential_integrity? then
              execute(tables.collect { |arglist| "STRING" }.join("STRING")) rescue execute(tables.collect { |arglist| "STRING" }.join("STRING"))
            end
          end
        end
      end
    end
  end
end;Fi[	I"5data//activerecord_proj/active_record/railtie.rb;TiEI"ﬁ                self.connection.schema_cache = var
              else
                warn("STRING")
              end
            end
          end
        end
      end
    end
    
    initializer("STRING") do |arglist|;Fi[	I"=data//activerecord_proj/active_record/scoping/default.rb;TiqI"                    var.merge(unscoped { |arglist| var.call })
                  else
                    var.merge(var)
                  end
                end
              end
            end
          end
        end
        
        def function(arglist);Fi[	@OiÜI"á            yield)
          ensure
            self.ignore_default_scope = false
          end
        end
      end
    end
  end
end;Fi[	I";data//activerecord_proj/active_record/scoping/named.rb;TiòI"‡            var = var.respond_to?(:call) ? (unscoped { |arglist| var.call(*var) }) : (var)
            var = all.merge(var)
            var ? (var.extending(var)) : (var)
          end
        end
      end
    end
  end
end;Fi[	I",data//backup_proj/backup/storage/ftp.rb;TidI"õ            var.mkdir(var.join("STRING"))
          rescue Net::FTPPermError
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	I"-data//backup_proj/backup/storage/sftp.rb;TiVI"•            var.mkdir!(var.join("STRING"))
          rescue Net::SFTP::StatusException
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	I"2data//backup_proj/backup/syncer/cloud/base.rb;Ti¿I"§              var if var.unpack("STRING").!.! rescue @var = true
                "STRING"
            end.join
          end
        end
      end
    end
  end
end;Fi[	I"(data//bundler_proj/bundler/graph.rb;TiåI"ï          rescue ArgumentError => var
            $stderr.puts("STRING")
            raise(var)
          end
        end
      end
    end
  end
end;Fi[	I"8data//bundler_proj/bundler/source/path/installer.rb;TiI"À          if Bundler.requires_sudo? then
            Bundler.mkdir_p("STRING")
            spec.executables.each { |arglist| Bundler.sudo("STRING") }
          end
        end
      end
    end
  end
end;Fi[	I".data//bundler_proj/bundler/source/path.rb;TiπI"≤            var = "STRING" if var.inspect =~ /@(.*:\d+)/
            var = "STRING"
            raise(InstallHookError, var)
          end
        end
      end
    end
  end
end;Fi[	I"@data//bundler_proj/bundler/vendor/thor/actions/directory.rb;Ti^I"¿            var = base.template(var, var[(0..-4)], config, &@var)
          else
            var = base.copy_file(var, var, config, &@var)
          end
        end
      end
    end
  end
end;Fi[	I"Gdata//bundler_proj/bundler/vendor/thor/actions/inject_into_file.rb;TiYI"÷          if (var or var.include?(replacement).!) then
            var.gsub!(var, var)
            File.open(destination, "STRING") { |arglist| var.write(var) }
          end
        end
      end
    end
  end
end;Fi[	I"?data//cancan_proj/cancan/model_adapters/mongoid_adapter.rb;Ti&I"ﬁ                var.or(var.conditions)
              else
                var.base_behavior.! ? (var.excludes(var.conditions)) : (var)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@oi'I"ﬁ              else
                var.base_behavior.! ? (var.excludes(var.conditions)) : (var)
              end
            end
          end
        end
      end
    end
  end
end
module Mongoid::Document::ClassMethods;Fi[	@oi(I"˚                var.base_behavior.! ? (var.excludes(var.conditions)) : (var)
              end
            end
          end
        end
      end
    end
  end
end
module Mongoid::Document::ClassMethods
  include(CanCan::ModelAdditions::ClassMethods);Fi[	I"%data//cancan_proj/cancan/rule.rb;Ti~I"„                end
              else
                var.kind_of?(Enumerable) ? (var.include?(var)) : ((var == var))
              end
            end
          end
        end
      end
    end
    
    def function(arglist);Fi[	I"/data//capistrano_proj/capistrano/cli/ui.rb;Ti#I"ø            var.default = "STRING"
            var.validate = /(y(es)?)|(no?)|(a(bort)?|\n)/i
            var.responses[:not_valid] = var
          end
        end
      end
    end
  end
end;Fi[	I"Ldata//capistrano_proj/capistrano/configuration/actions/file_transfer.rb;Ti,I"Ò          execute_on_servers(var) do |arglist|
            var = var.map { |arglist| sessions[var] }
            Transfer.process(var, var, var, var, var.merge(:logger => (logger)), &var)
          end
        end
      end
    end
  end
end;Fi[	I"Idata//capistrano_proj/capistrano/configuration/actions/invocation.rb;TiI"w            exit(-1)
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	I">data//capistrano_proj/capistrano/configuration/loading.rb;TifI"#                  instance_eval(&var[:proc])
                else
                  raise(ArgumentError, "STRING")
                end
              end
            end
          end
        end
      end
      
      # Require another file. This is identical to the standard require method,;Fi[	I"Cdata//capistrano_proj/capistrano/recipes/deploy/scm/accurev.rb;Ti~I"Æ          
          def function(arglist)
            (stream == var.stream) and (transaction_id == var.transaction_id)
          end
        end
      end
    end
  end
end;Fi[	I"Adata//capistrano_proj/capistrano/recipes/deploy/scm/darcs.rb;TiWI"n            nil
          else
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	I"Ddata//capistrano_proj/capistrano/recipes/deploy/scm/perforce.rb;TiÇI"n            "STRING"
          else
            var
          end
        end
      end
    end
  end
end;Fi[	I"7data//capistrano_proj/capistrano/recipes/deploy.rb;TiuI"ƒ                :perforce
              else
                File.exist?("STRING") ? (:subversion) : (:none)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@âivI"ˆ              else
                File.exist?("STRING") ? (:subversion) : (:none)
              end
            end
          end
        end
      end
    end
  end
end
# Auxiliary helper method for the `deploy:check' task. Lets you set up your;Fi[	@âiwI"˜                File.exist?("STRING") ? (:subversion) : (:none)
              end
            end
          end
        end
      end
    end
  end
end
# Auxiliary helper method for the `deploy:check' task. Lets you set up your
# own dependencies.;Fi[	I"8data//compass_proj/compass/configuration/helpers.rb;Ti1I"˜                    Compass::AppIntegration.lookup(var).configuration
                  else
                    raise("STRING")
                  end
                end
              end
            end
          end
        end
      end
      ;Fi[	@êi2I"Œ                  else
                    raise("STRING")
                  end
                end
              end
            end
          end
        end
      end
      
      # Support for testing.;Fi[	I"Edata//compass_proj/compass/sass_extensions/sprites/row_fitter.rb;TiPI"                        var = true
                        (var << var)
                        throw(:done)
                      end
                    end
                  end
                end
              end
            end
          end while var
        end;Fi[	I"Idata//compass_proj/compass/sass_extensions/sprites/sprite_methods.rb;TiÄI"¯        def function(arglist)
          if options[:compass] and (options[:compass][:logger] and options[:quiet].!) then
            options[:compass][:logger].record(var, relativize(var), *var)
          end
        end
      end
    end
  end
end;Fi[	I":data//cucumber_prok/cucumber/formatter/interceptor.rb;Ti>I"}            return $stdout
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	I"Hdata//cucumber_prok/cucumber/wire_support/wire_protocol/requests.rb;TipI"á          
          def function(arglist)
            super(request_params(var))
          end
        end
      end
    end
  end
end;Fi[	I"7data//devise_proj/devise/models/authenticatable.rb;Ti≈I"∫          loop do |arglist|
            var = Devise.friendly_token
            break var unless to_adapter.find_first(var => (var))
          end
        end
      end
    end
  end
end;Fi[	I"8data//diaspora_proj/controllers/users_controller.rb;TiKI"˙                    flash[:notice] = I18n.t("STRING")
                  else
                    flash[:error] = I18n.t("STRING")
                  end
                end
              end
            end
          end
        end
      end
    else;Fi[	@§iLI"Ò                  else
                    flash[:error] = I18n.t("STRING")
                  end
                end
              end
            end
          end
        end
      end
    else
      if var = params[:reorder_aspects] then;Fi[	I"#data//homebrew_proj/cleaner.rb;Ti:I"◊              var.unlink unless @var.skip_clean?(var)
            else
              clean_file_permissions(var) if var.symlink?.!
            end
          end
        end
      end
    end
  end
end
class Pathname;Fi[	@©i;I"«            else
              clean_file_permissions(var) if var.symlink?.!
            end
          end
        end
      end
    end
  end
end
class Pathname
  alias_method(:orig_unlink, :unlink);Fi[	I",data//homebrew_proj/extend/fileutils.rb;TiCI"            end
          else
            File.chmod(var.mode, var)
          end
        end
      end
    end
  end
end;Fi[	I"data//homebrew_proj/keg.rb;TiÒI"Â            unless resolve_any_conflicts(var) then
              make_relative_symlink(var, var, var)
              Find.prune
            end
          end
        end
      end
    end
  end
end
require("keg_fix_install_names");Fi[	@±iÚI"≤              make_relative_symlink(var, var, var)
              Find.prune
            end
          end
        end
      end
    end
  end
end
require("keg_fix_install_names");Fi[	I"1data//homebrew_proj/keg_fix_install_names.rb;TiI"Á                  system(MacOS.locate("STRING"), "STRING", var, var, var)
                else
                  opoo("STRING")
                end
              end
            end
          end
        end
      end
    end
  end;Fi[	@∂iI"†                else
                  opoo("STRING")
                end
              end
            end
          end
        end
      end
    end
  end
  ;Fi[	@∂iI"ï                  opoo("STRING")
                end
              end
            end
          end
        end
      end
    end
  end
  
  private;Fi[	I"1data//jekyll_proj/jekyll/migrators/drupal.rb;Ti2I"®              var.puts("STRING")
              var.puts("STRING")
              var.puts("STRING")
            end
          end
        end
      end
    end
  end
end;Fi[	@Ωi3I"á              var.puts("STRING")
              var.puts("STRING")
            end
          end
        end
      end
    end
  end
end;Fi[	I"Adata//mongoid_proj/lib/mongoid/atomic/paths/embedded/many.rb;Ti'I"¥            var = parent.atomic_position
            var = document.new_record? ? ("STRING") : ("STRING")
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	I"@data//mongoid_proj/lib/mongoid/atomic/paths/embedded/one.rb;Ti&I"ì          def function(arglist)
            var = parent.atomic_position
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	I"?data//mongoid_proj/lib/mongoid/config/validators/option.rb;TiI"À        def function(arglist)
          unless Config.settings.keys.include?(var.to_sym) then
            raise(Errors::InvalidConfigOption.new(var))
          end
        end
      end
    end
  end
end;Fi[	I"8data//mongoid_proj/lib/mongoid/criterion/scoping.rb;TiëI"ƒ        var.each do |arglist|
          send(var).reject! do |arglist|
            var.send(var).has_key?(var) and (var.send(var)[var] == var)
          end
        end
      end
    end
  end
end;Fi[	I"6data//mongoid_proj/lib/mongoid/extensions/date.rb;Ti?I"*          unless var.blank? then
            var = var.__mongoize_time__
            ::Time.utc(var.year, var.month, var.day)
          end
        end
      end
    end
  end
end
::Date.__send__(:include, Mongoid::Extensions::Date)
::Date.__send__(:extend, Mongoid::Extensions::Date::ClassMethods);Fi[	I"6data//mongoid_proj/lib/mongoid/extensions/time.rb;TiII"%            ::Time.at(var.to_i, var).utc)
          rescue ArgumentError
            raise(Errors::InvalidTime.new(var))
          end
        end
      end
    end
  end
end
::Time.__send__(:include, Mongoid::Extensions::Time)
::Time.__send__(:extend, Mongoid::Extensions::Time::ClassMethods);Fi[	I">data//mongoid_proj/lib/mongoid/fields/validators/macro.rb;TiCI"          var.keys.each do |arglist|
            if OPTIONS.include?(var).! and Fields.options.include?(var).! then
              raise(Errors::InvalidFieldOption.new(var, var, var, OPTIONS))
            end
          end
        end
      end
    end
  end
end;Fi[	@‘iDI"‡            if OPTIONS.include?(var).! and Fields.options.include?(var).! then
              raise(Errors::InvalidFieldOption.new(var, var, var, OPTIONS))
            end
          end
        end
      end
    end
  end
end;Fi[	I"Adata//mongoid_proj/lib/mongoid/indexes/validators/options.rb;TiLI"⁄            next if (var == :options)
            unless VALID_TYPES.include?(var) then
              raise(Errors::InvalidIndex.new(var, var, var))
            end
          end
        end
      end
    end
  end
end;Fi[	@ŸiMI"¥            unless VALID_TYPES.include?(var) then
              raise(Errors::InvalidIndex.new(var, var, var))
            end
          end
        end
      end
    end
  end
end;Fi[	I"8data//mongoid_proj/lib/mongoid/nested_attributes.rb;Ti4I"Ê          re_define_method(var) do |arglist|
            _assigning do |arglist|
              var.nested_builder(var, var).build(self, mass_assignment_options)
            end
          end
        end
      end
    end
  end
end;Fi[	@ﬁi5I"π            _assigning do |arglist|
              var.nested_builder(var, var).build(self, mass_assignment_options)
            end
          end
        end
      end
    end
  end
end;Fi[	I"=data//mongoid_proj/lib/mongoid/persistence/atomic/bit.rb;TiI"            end
            execute("STRING")
            document[field]
          end
        end
      end
    end
  end
end;Fi[	I"Cdata//mongoid_proj/lib/mongoid/persistence/atomic/operation.rb;TiâI"ù            var = document.send(field).concat(value.__array__)
            execute(var)
            var
          end
        end
      end
    end
  end
end;Fi[	I"=data//mongoid_proj/lib/mongoid/persistence/atomic/pop.rb;TiI"´              (value > 0) ? (var.pop) : (var.shift)
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	@ÈiI"|              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	I">data//mongoid_proj/lib/mongoid/persistence/atomic/pull.rb;TiI"ó              var.delete(value)
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	@ÓiI"|              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	I"Bdata//mongoid_proj/lib/mongoid/persistence/atomic/pull_all.rb;TiI"µ              var.delete_if { |arglist| value.include?(var) }
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	@ÛiI"|              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	I"@data//mongoid_proj/lib/mongoid/persistence/atomic/rename.rb;TiI"ô            execute("STRING")
            document.remove_change(value)
            document[value]
          end
        end
      end
    end
  end
end;Fi[	I">data//mongoid_proj/lib/mongoid/persistence/atomic/sets.rb;TiI"ì            document[field] = value
            execute("STRING")
            document[field]
          end
        end
      end
    end
  end
end;Fi[	I"?data//mongoid_proj/lib/mongoid/persistence/atomic/unset.rb;TiI"∏          prepare do |arglist|
            fields.each { |arglist| document.attributes.delete(var) }
            execute("STRING")
          end
        end
      end
    end
  end
end;Fi[	I"Mdata//mongoid_proj/lib/mongoid/persistence/operations/embedded/insert.rb;TiI"–                parent.insert
              else
                collection.find(parent.atomic_selector).update(inserts)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@iI"≤              else
                collection.find(parent.atomic_selector).update(inserts)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@iI"ü                collection.find(parent.atomic_selector).update(inserts)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	I"Mdata//mongoid_proj/lib/mongoid/persistence/operations/embedded/remove.rb;TiI"              parent.remove_child(var) if notifying_parent?
              if parent.persisted? then
                collection.find(parent.atomic_selector).update(deletes)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@iI"«              if parent.persisted? then
                collection.find(parent.atomic_selector).update(deletes)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@iI"ü                collection.find(parent.atomic_selector).update(deletes)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	I"Ddata//mongoid_proj/lib/mongoid/persistence/operations/insert.rb;TiI"§          prepare do |arglist|
            collection.insert(var.as_document)
            IdentityMap.set(var)
          end
        end
      end
    end
  end
end;Fi[	I"Ddata//mongoid_proj/lib/mongoid/persistence/operations/update.rb;TiI"˝              collection.find(selector).update(updates)
              conflicts.each_pair do |arglist|
                collection.find(selector).update(var => (var))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@iI"≈              conflicts.each_pair do |arglist|
                collection.find(selector).update(var => (var))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@iI"ñ                collection.find(selector).update(var => (var))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	I"Ddata//mongoid_proj/lib/mongoid/persistence/operations/upsert.rb;TiI"¿        def function(arglist)
          prepare do |arglist|
            collection.find(selector).update(document.as_document, [:upsert])
          end
        end
      end
    end
  end
end;Fi[	I"Edata//mongoid_proj/lib/mongoid/relations/bindings/embedded/in.rb;Ti8I"ˇ                target.do_or_do_not(metadata.inverse(target)).delete(base)
              else
                target.do_or_do_not(metadata.inverse_setter(target), nil)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@i9I"¥              else
                target.do_or_do_not(metadata.inverse_setter(target), nil)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@i:I"°                target.do_or_do_not(metadata.inverse_setter(target), nil)
              end
            end
          end
        end
      end
    end
  end
end;Fi[	I"Gdata//mongoid_proj/lib/mongoid/relations/bindings/embedded/many.rb;Ti-I"Ã          # @since 2.0.0.rc.1
          def function(arglist)
            binding { |arglist| var.do_or_do_not(metadata.inverse_setter(target), nil) }
          end
        end
      end
    end
  end
end;Fi[	I"Fdata//mongoid_proj/lib/mongoid/relations/bindings/embedded/one.rb;Ti1I"Œ          def function(arglist)
            binding do |arglist|
              target.do_or_do_not(metadata.inverse_setter(target), nil)
            end
          end
        end
      end
    end
  end
end;Fi[	@&i2I"Æ            binding do |arglist|
              target.do_or_do_not(metadata.inverse_setter(target), nil)
            end
          end
        end
      end
    end
  end
end;Fi[	I"Gdata//mongoid_proj/lib/mongoid/relations/bindings/referenced/in.rb;Ti>I"·                  target.__send__(var).delete(base)
                else
                  target.__send__("STRING", nil)
                end
              end
            end
          end
        end
      end
    end
  end;Fi[	@+i?I"±                else
                  target.__send__("STRING", nil)
                end
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@+i@I"ú                  target.__send__("STRING", nil)
                end
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@+iAI"p                end
              end
            end
          end
        end
      end
    end
  end
end;Fi[	I"Idata//mongoid_proj/lib/mongoid/relations/bindings/referenced/many.rb;Ti!I"∏          # @since 2.0.0.rc.1
          def function(arglist)
            binding { |arglist| unbind_from_relational_parent(var) }
          end
        end
      end
    end
  end
end;Fi[	I"Qdata//mongoid_proj/lib/mongoid/relations/bindings/referenced/many_to_many.rb;Ti/I"Ã              end
              base.synced[metadata.foreign_key] = true
              var.synced[metadata.inverse_foreign_key] = true
            end
          end
        end
      end
    end
  end
end;Fi[	@7i0I"∫              base.synced[metadata.foreign_key] = true
              var.synced[metadata.inverse_foreign_key] = true
            end
          end
        end
      end
    end
  end
end;Fi[	I"Hdata//mongoid_proj/lib/mongoid/relations/bindings/referenced/one.rb;Ti#I"ª          # @since 2.0.0.rc.1
          def function(arglist)
            binding { |arglist| unbind_from_relational_parent(target) }
          end
        end
      end
    end
  end
end;Fi[	I"Edata//mongoid_proj/lib/mongoid/relations/builders/embedded/in.rb;TiI"Æ              Factory.from_db(klass, object)
            else
              Factory.build(klass, object)
            end
          end
        end
      end
    end
  end
end;Fi[	@?iI"Å            else
              Factory.build(klass, object)
            end
          end
        end
      end
    end
  end
end;Fi[	I"Gdata//mongoid_proj/lib/mongoid/relations/builders/embedded/many.rb;Ti!I"l              end
            end
            var
          end
        end
      end
    end
  end
end;Fi[	I"Fdata//mongoid_proj/lib/mongoid/relations/builders/embedded/one.rb;TiI"Æ              Factory.from_db(klass, object)
            else
              Factory.build(klass, object)
            end
          end
        end
      end
    end
  end
end;Fi[	@GiI"Å            else
              Factory.build(klass, object)
            end
          end
        end
      end
    end
  end
end;Fi[	I"Pdata//mongoid_proj/lib/mongoid/relations/builders/nested_attributes/many.rb;Ti®I"≤              var.assign_attributes(var, var)
            else
              var.update_attributes(var, var)
            end
          end
        end
      end
    end
  end
end;Fi[	@Li©I"Ñ            else
              var.update_attributes(var, var)
            end
          end
        end
      end
    end
  end
end;Fi[	I"Odata//mongoid_proj/lib/mongoid/relations/builders/nested_attributes/one.rb;Ti~I"´          # @since 2.0.0
          def function(arglist)
            existing and (destroyable?.! and acceptable_id?)
          end
        end
      end
    end
  end
end;Fi[	I"Gdata//mongoid_proj/lib/mongoid/relations/builders/referenced/in.rb;TiI"‘            return object unless query?
            var = var ? (var.constantize) : (metadata.klass)
            metadata.criteria(object, var).from_map_or_db
          end
        end
      end
    end
  end
end;Fi[	I"Idata//mongoid_proj/lib/mongoid/relations/builders/referenced/many.rb;TiI"˘            return [] if object.is_a?(Array)
            var = metadata.criteria(Conversions.flag(object, metadata), base.class)
            (IdentityMap.get_many(var.klass, var.selector) or var)
          end
        end
      end
    end
  end
end;Fi[	I"Qdata//mongoid_proj/lib/mongoid/relations/builders/referenced/many_to_many.rb;Ti%I"∏          # @since 2.0.0.rc.1
          def function(arglist)
            (object.nil? or object.first.is_a?(Mongoid::Document).!)
          end
        end
      end
    end
  end
end;Fi[	I"Hdata//mongoid_proj/lib/mongoid/relations/builders/referenced/one.rb;TiI"Â            return object unless query?
            return nil if base.new_record?
            metadata.criteria(Conversions.flag(object, metadata), base.class).from_map_or_db
          end
        end
      end
    end
  end
end;Fi[	I"Adata//mongoid_proj/lib/mongoid/relations/cascading/delete.rb;Ti(I"∂              relation.clear
            else
              ::Array.wrap(relation).each { |arglist| var.delete }
            end
          end
        end
      end
    end
  end
end;Fi[	@`i)I"ô            else
              ::Array.wrap(relation).each { |arglist| var.delete }
            end
          end
        end
      end
    end
  end
end;Fi[	I"Bdata//mongoid_proj/lib/mongoid/relations/cascading/destroy.rb;Ti'I"´              relation.each { |arglist| var.destroy }
            else
              relation.destroy
            end
          end
        end
      end
    end
  end
end;Fi[	@ei(I"z            else
              relation.destroy
            end
          end
        end
      end
    end
  end
end;Fi[	I"Cdata//mongoid_proj/lib/mongoid/relations/cascading/restrict.rb;Ti#I"√        def function(arglist)
          unless relation.blank? then
            raise(Errors::DeleteRestriction.new(document, metadata.name))
          end
        end
      end
    end
  end
end;Fi[	I";data//mongoid_proj/lib/mongoid/relations/constraint.rb;Ti+I"ó            var.map! { |arglist| var.mongoize(var) }
          else
            var.mongoize(var)
          end
        end
      end
    end
  end
end;Fi[	I"Cdata//mongoid_proj/lib/mongoid/relations/embedded/batchable.rb;TiBI"ë            var.freeze
            var.destroyed = true
            IdentityMap.remove(var)
          end
        end
      end
    end
  end
end;Fi[	I"8data//mongoid_proj/lib/mongoid/relations/options.rb;TiI"¬        var.keys.each do |arglist|
          if var.include?(var).! then
            raise(Errors::InvalidOptions.new(var[:name], var, var))
          end
        end
      end
    end
  end
end;Fi[	I"<data//nokogiri_proj/nokogiri/html/sax/parser_context.rb;TiI"x            super
          else
            memory(var, var)
          end
        end
      end
    end
  end
end;Fi[	I"0data//nokogiri_proj/nokogiri/xml/pp/node.rb;Ti1I"v              end
            end
            var.breakable
          end
        end
      end
    end
  end
end;Fi[	I";data//nokogiri_proj/nokogiri/xml/sax/parser_context.rb;TiI"à            io(var, Parser::ENCODINGS[var])
          else
            memory(var)
          end
        end
      end
    end
  end
end;Fi[	I"1data//paperclip_proj/paperclip/storage/s3.rb;TiI"«            var[$1.downcase] = var
          else
            var[var.to_s.downcase.sub(/^x-amz-/, "STRING").tr("STRING", "STRING").to_sym] = var
          end
        end
      end
    end
  end
end;Fi[	I"Zdata//redmine_proj/plugins/acts_as_activity_provider/lib/acts_as_activity_provider.rb;Ti:I"É              end
            end
            var.all(var[:find_options].dup)
          end
        end
      end
    end
  end
end;Fi[	I"Ldata//redmine_proj/plugins/acts_as_searchable/lib/acts_as_searchable.rb;TiZI"™            var = var.where("STRING", var[:offset]) if var[:offset]
            var = var.all
            [var, var]
          end
        end
      end
    end
  end
end;Fi[	I"2data//redmine_proj/plugins/rfpdf/lib/tcpdf.rb;Ti”I"                var.gsub!(/[\t\r\n\f]/, "STRING")
                var.gsub!(/&nbsp;/, "STRING")
                Write(@var, unhtmlentities(var), "STRING", var)
              end
            end
          end
        end
      end
    end
    Ln(@var) if var
  end;Fi[	I">data//redmine_proj/redmine/core_ext/string/conversions.rb;TiI"ó          # Object#to_a removed in ruby1.9
          def function(arglist)
            [self.dup]
          end
        end
      end
    end
  end
end;Fi[	I"-data//redmine_proj/redmine/export/pdf.rb;Ti´I"w            return var
          else
            return nil
          end
        end
      end
    end
  end
end;Fi[	I"6data//redmine_proj/redmine/views/my_page/block.rb;TiI"∑            var = File.basename(var).split("STRING").first.gsub(/^_/, "STRING")
            var[var] = var.to_sym
            var
          end
        end
      end
    end
  end
end;Fi[	I"Ddata//redmine_proj/redmine/wiki_formatting/textile/formatter.rb;TiqI"∆                var = ("STRING" + Redmine::SyntaxHighlighting.highlight_by_language($2, $1))
              end
              var
            end
          end
        end
      end
    end
  end
end;Fi[	@îirI"n              end
              var
            end
          end
        end
      end
    end
  end
end;Fi[	I"Adata//redmine_proj/redmine/wiki_formatting/textile/helper.rb;TiI"”              ((javascript_include_tag("STRING") + javascript_include_tag("STRING")) + stylesheet_link_tag("STRING"))
            end
            @var = true
          end
        end
      end
    end
  end
end;Fi[	I"-data//rspec_proj/spec/example/subject.rb;Ti{I"§            self.__should_not_for_example_group__(var)
          else
            subject.should_not(var, var)
          end
        end
      end
    end
  end
end;Fi[	I"6data//rspec_proj/spec/extensions/instance_exec.rb;Ti"I"À            return send(:__instance_exec, *var)
          ensure
            var.module_eval { |arglist| remove_method(:__instance_exec) } rescue nil
          end
        end
      end
    end
  end
end;Fi[	I")data//rspec_proj/spec/mocks/proxy.rb;Ti|I"                  @var.superclass.send(var, *var, &var)
                else
                  @var.__send__(:method_missing, var, *var, &var)
                end
              end
            end
          end
        end
      end
      
      def function(arglist);Fi[	I"4data//rspec_proj/spec/runner/differs/default.rb;TiKI"          
          def function(arglist)
            @var.context_lines
          end
        end
      end
    end
  end
end;Fi[	I"Pdata//state_machine_proj/state_machine/integrations/data_mapper/observer.rb;Ti¢I"˚                var.state_machines.values
              end
              var.each { |arglist| var.send(var, *var, &var) }
            end
          end
        end
      end
    end
  end
end
DataMapper::Observer::ClassMethods.class_eval do |arglist|;Fi[	@®i£I"              end
              var.each { |arglist| var.send(var, *var, &var) }
            end
          end
        end
      end
    end
  end
end
DataMapper::Observer::ClassMethods.class_eval do |arglist|
  include(StateMachine::Integrations::DataMapper::Observer);Fi[	I"Adata//state_machine_proj/state_machine/yard/handlers/base.rb;TiI"è          else
            var = extract_node_name(var)
            var ? ([var]) : (var)
          end
        end
      end
    end
  end
end;Fi[	I"Bdata//state_machine_proj/state_machine/yard/handlers/event.rb;TiI"Ê            var.each do |arglist|
              owner.event(var) do |arglist|
                var.parse_block(var.last.last, :owner => (self))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@∞iI"ƒ              owner.event(var) do |arglist|
                var.parse_block(var.last.last, :owner => (self))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@∞iI"ò                var.parse_block(var.last.last, :owner => (self))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	I"Ddata//state_machine_proj/state_machine/yard/handlers/machine.rb;Ti∆I"œ            end
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING"]
          end
        end
      end
    end
  end
end;Fi[	I"Bdata//state_machine_proj/state_machine/yard/handlers/state.rb;TiI"Ê            var.each do |arglist|
              owner.state(var) do |arglist|
                var.parse_block(var.last.last, :owner => (self))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@∫iI"ƒ              owner.state(var) do |arglist|
                var.parse_block(var.last.last, :owner => (self))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	@∫iI"ò                var.parse_block(var.last.last, :owner => (self))
              end
            end
          end
        end
      end
    end
  end
end;Fi[	I"Gdata//state_machine_proj/state_machine/yard/handlers/transition.rb;Ti+I"r            end
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	I"5data//twitter_proj/twitter/error/client_error.rb;TiI"€            if var[:errors] then
              var = Array(var[:errors]).first
              var.kind_of?(Hash) ? (var[:message].chomp) : (var.chomp)
            end
          end
        end
      end
    end
  end
end;Fi[	@ƒiI"∫              var = Array(var[:errors]).first
              var.kind_of?(Hash) ? (var[:message].chomp) : (var.chomp)
            end
          end
        end
      end
    end
  end
end;Fi[	I"6data//twitter_proj/twitter/response/parse_json.rb;TiI"∆        if respond_to?(:parse) then
          unless [204, 301, 302, 304].include?(var[:status]) then
            var[:body] = parse(var[:body])
          end
        end
      end
    end
  end
end;Fi[	I"7data//whenever_proj/whenever/capistrano/support.rb;Ti I"≈            var = var.empty? ? ("STRING") : ("STRING")
            var = "STRING"
            run(var, whenever_options.merge(:hosts => (var)))
          end
        end
      end
    end
  end
end;Fi[@(I" ;FI"def function(arglist);FI""STRING";FI"end;FI" ;Fi}[}[	I"Tdata//activerecord_proj/active_record/associations/join_dependency/join_base.rb;TiI"6      class JoinBase < JoinPart
        def function(arglist)
          (var.class == self.class) and (var.active_record == active_record)
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          Arel::Table.new(table_name, arel_engine);Fi[	I"\data//activerecord_proj/active_record/associations/preloader/has_and_belongs_to_many.rb;TiI"˚        
        def function(arglist)
          reflection.active_record_primary_key
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          join_table[reflection.foreign_key];Fi[	I"^data//activerecord_proj/active_record/connection_adapters/abstract/database_statements.rb;Ti-I"           quote(var, var[var])
        end
        execute("STRING", "STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@‹i1I"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@‹i5I"Ÿ      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      # Sanitizes the given LIMIT parameter in order to prevent SQL injection.
      #;Fi[	I"Rdata//activerecord_proj/active_record/connection_adapters/abstract/quoting.rb;TiiI"ˆ      # Quotes the table name. Defaults to column name quoting.
      def function(arglist)
        quote_column_name(var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@„imI"ƒ      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        if var.acts_like?(:time) then;Fi[	I"Rdata//activerecord_proj/active_record/connection_adapters/abstract_adapter.rb;TiI"Û      
      def function(arglist)
        var[var].lower.eq(var.lower(var))
      end
      
      def function(arglist)
        "STRING"
      end
      
      # Check the connection back in to the connection pool
      def function(arglist);Fi[	I"Xdata//activerecord_proj/active_record/connection_adapters/abstract_mysql_adapter.rb;Ti8I"Û          var.table(var.source)
          var.wheres = var.constraints
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      # SCHEMA STATEMENTS ========================================
      #:nodoc:;Fi[	@ÎiuI"Ì        var = "STRING"
        add_column_options!(var, var)
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var.map { |arglist| remove_column_sql(var, var) };Fi[	I"Qdata//activerecord_proj/active_record/connection_adapters/sqlite3_adapter.rb;TiÀI"      
      def function(arglist)
        @var.class.quote(var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      # Quote date/time values for use in SQL input. Includes microseconds
      # if the value is a Time responding to usec.;Fi[	I"Hdata//activerecord_proj/active_record/tasks/mysql_database_tasks.rb;TilI""            defined? Mysql ? (Mysql::Error) : (StandardError)
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        configuration_without_database.merge("STRING" => "STRING", "STRING" => (root_password));Fi[	I",data//bundler_proj/bundler/dep_proxy.rb;Ti!I"â    
    def function(arglist)
      @var.requirement
    end
    
    def function(arglist)
      "STRING"
    end
    
    private
    ;Fi[	I"(data//bundler_proj/bundler/index.rb;Ti I"˚      @var = {}
      @var = Hash.new { |arglist| var[var] = [] }
      var.specs.each { |arglist| @var[var] = var.dup }
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      each { |arglist| return false };Fi[	I"+data//bundler_proj/bundler/resolver.rb;TiQI"ò      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      ;Fi[	I"-data//bundler_proj/bundler/source/git.rb;TiñI"©        else
          Bundler.cache.join("STRING", var)
        end)
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      ;Fi[	@fi1I"Ó        (var << "STRING")
        (var << "STRING") unless (@var == DEFAULT_GLOB)
        (var << "STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        self.class.hash;Fi[	@Äi{I"          
          def function(arglist)
            (@var == "STRING")
          end
          
          def function(arglist)
            "STRING"
          end
          
          def function(arglist)
            (stream == var.stream) and (transaction_id == var.transaction_id);Fi[	I"1data//capistrano_proj/capistrano/transfer.rb;TiFI"ﬂ    
    def function(arglist)
      transfers.any? { |arglist| var.active? }
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      from.responds_to?(:read) ? ("STRING") : (from);Fi[	I"Udata//compass_proj/compass/app_integration/stand_alone/configuration_defaults.rb;TiI"„      module ConfigurationDefaults
        def function(arglist)
          :stand_alone
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@	iI"≈        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@	iI"≈        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@	iI"≈        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	I"+data//compass_proj/compass/compiler.rb;Ti3I"    
    def function(arglist)
      @var ||= sass_files.map { |arglist| corresponding_css_file(var) }
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      css_files.map { |arglist| File.dirname(var) }.uniq.sort.sort_by do |arglist|;Fi[	I"9data//compass_proj/compass/configuration/defaults.rb;TiI"≥      
      def function(arglist)
        :stand_alone
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@iI"Õ      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        File.join(top_level.css_dir, "STRING");Fi[	@òi*I"¯              raise(Sass::SyntaxError, "STRING")
            end
          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        # The on-the-disk filename of the sprite
        def function(arglist);Fi[	I"0data//cucumber_prok/cucumber/ast/feature.rb;TiLI"        var = @var.index(var)
        var = @var[(var + 1)]
        var.call(var) if var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        if Cucumber::WINDOWS and (var and ENV["STRING"].!) then;Fi[	@iWI"‘          var = var.gsub(/\//, "STRING")
        end
        @var = var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var = name.split(/\n/)[0];Fi[	I"6data//cucumber_prok/cucumber/ast/outline_table.rb;Ti¢I"·        
        def function(arglist)
          @var.backtrace_line(name, line)
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          @var.language;Fi[	I"-data//cucumber_prok/cucumber/ast/step.rb;TiqI"ª          var[delimited(var)] = var
          var
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var = @var;Fi[	I".data//cucumber_prok/cucumber/ast/table.rb;TiBI"±        
        def function(arglist)
          @var[0].line
        end
        
        def function(arglist)
          "STRING"
        end
        
        private
        ;Fi[	I"2data//cucumber_prok/cucumber/core_ext/proc.rb;TiI"Ø  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  if Proc.new { |arglist| }.to_s.=~(PROC_PATTERN) then
    def function(arglist);Fi[	I"3data//cucumber_prok/cucumber/formatter/html.rb;TiºI"ﬂ      
      def function(arglist)
        File.read((File.dirname("STRING") + "STRING"))
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        (@var << "STRING");Fi[	I";data//cucumber_prok/cucumber/py_support/py_language.rb;TiI"¥      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = [];Fi[	I"6data//diaspora_proj/helpers/application_helper.rb;TiI"ç  
  def function(arglist)
    raw_bookmarklet
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    bookmarklet;Fi[	I"?data//diaspora_proj/models/notifications/also_commented.rb;TiI"√class Notifications::AlsoCommented < Notification
  def function(arglist)
    Jobs::Mail::AlsoCommented
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING";Fi[	@6iI"§  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    Post.where(:id => (self.target_id)).first;Fi[	I"@data//diaspora_proj/models/notifications/comment_on_post.rb;TiI"√class Notifications::CommentOnPost < Notification
  def function(arglist)
    Jobs::Mail::CommentOnPost
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING";Fi[	@;iI"§  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    Post.where(:id => (self.target_id)).first;Fi[	I"6data//diaspora_proj/models/notifications/liked.rb;TiI"≥class Notifications::Liked < Notification
  def function(arglist)
    Jobs::Mail::Liked
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING";Fi[	@@iI"å  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    var = self.target;Fi[	I":data//diaspora_proj/models/notifications/mentioned.rb;TiI"ªclass Notifications::Mentioned < Notification
  def function(arglist)
    Jobs::Mail::Mentioned
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING";Fi[	@EiI"ú  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    Mention.find(self.target_id).post;Fi[	I"@data//diaspora_proj/models/notifications/private_message.rb;TiI"class Notifications::PrivateMessage < Notification
  def function(arglist)
    Jobs::Mail::PrivateMessage
  end
  
  def function(arglist)
    "STRING"
  end
  
  def self.make_notification(arglist)
    var = var.new(:target => (var), :recipient_id => (var.id));Fi[	I"9data//diaspora_proj/models/notifications/reshared.rb;TiI"πclass Notifications::Reshared < Notification
  def function(arglist)
    Jobs::Mail::Reshared
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING";Fi[	@MiI"Ü  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    self.target;Fi[	I"@data//diaspora_proj/models/notifications/started_sharing.rb;TiI"”class Notifications::StartedSharing < Notification
  def function(arglist)
    Jobs::Mail::StartedSharing
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    super(var.sender, var);Fi[	I")data//diaspora_proj/models/person.rb;Ti¬I"ê      var = @var["STRING"]
    end
    var
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    if self.owner then;Fi[	I"'data//diaspora_proj/models/post.rb;Ti<I"Å  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    [];Fi[	I"(data//homebrew_proj/cmd/--config.rb;TiZI"°  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    `uname -m`.chomp;Fi[	I"0data//homebrew_proj/compat/compatibility.rb;TiII"∫    var = FailsWithLLVM.new(var, var)
    @var ||= CompilerFailures.new
    (@var << var)
  end
  
  def function(arglist)
    "STRING"
  end
  
  class << self
    def function(arglist);Ti[	I")data//homebrew_proj/macos/xquartz.rb;Ti%I"È        MacOS.pkgutil_info(FORGE_PKG_ID) =~ /version: (\d\.\d\.\d).+$/ and $1
      end
    end)
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    ([FORGE_BUNDLE_ID, APPLE_BUNDLE_ID].find do |arglist|;Fi[	I"4data//jekyll_proj/jekyll/converters/markdown.rb;TiVI"    def function(arglist)
      var = (("STRING" + @var["STRING"].gsub("STRING", "STRING")) + "STRING")
      var.=~(Regexp.new(var, Regexp::IGNORECASE))
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      setup;Fi[	I"3data//jekyll_proj/jekyll/converters/textile.rb;TiI"    def function(arglist)
      var = (("STRING" + @var["STRING"].gsub("STRING", "STRING")) + "STRING")
      var.=~(Regexp.new(var, Regexp::IGNORECASE))
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      setup;Fi[	I"/data//jekyll_proj/jekyll/tags/highlight.rb;Ti0I"      var = (var["STRING"] + var) if var["STRING"]
      var = (var + var["STRING"]) if var["STRING"]
      var
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      var = var.sub(/<pre>/, (("STRING" + var) + "STRING"));Fi[	I"Adata//mongoid_proj/lib/rails/generators/mongoid_generator.rb;Ti"I"∆      
      def self.build(arglist)
        var ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@mi&I"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@mi*I"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@mi.I"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	I"6data//nokogiri_proj/nokogiri/css/xpath_visitor.rb;TiwI"7            (var.value.first + "STRING")
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      { "STRING" => "STRING", "STRING" => "STRING", "STRING" => "STRING", "STRING" => "STRING", "STRING" => "STRING" }.each do |arglist|
        class_eval("STRING");Fi[	I"-data//nokogiri_proj/nokogiri/xml/node.rb;TiiI"‰          raise(ArgumentError, "STRING")
        end
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        [:name, :namespace, :attribute_nodes, :children];Fi[	I")data//omni_proj/omniauth/strategy.rb;Ti{I"D      self.class.args.each { |arglist| options[var] = var.shift }
      raise(ArgumentError, "STRING") unless var.empty?
      yield(options) if block_given?
    end
    
    def function(arglist)
      "STRING"
    end
    
    # Direct access to the OmniAuth logger, automatically prefixed
    # with this strategy's name.;Fi[	I"Edata//paperclip_proj/generators/paperclip/paperclip_generator.rb;TiI"í  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    migration_name.camelize;Fi[	I"Cdata//paperclip_proj/paperclip/io_adapters/abstract_adapter.rb;TiI"œ    
    def function(arglist)
      @var.read(var, var)
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      @var = var.gsub(OS_RESTRICTED_CHARACTERS, "STRING");Fi[	I">data//paperclip_proj/paperclip/io_adapters/nil_adapter.rb;Ti	I"Ω  class NilAdapter < AbstractAdapter
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING";Fi[	@ÖiI"í    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      0;Fi[	I"Jdata//paperclip_proj/paperclip/matchers/have_attached_file_matcher.rb;TiI"          @var = var
          @var = @var.class unless Class.===(@var)
          responds? and (has_column? and included?)
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@äi I"≈        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@äi$I"Ø        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        ;Fi[	I"Xdata//paperclip_proj/paperclip/matchers/validate_attachment_content_type_matcher.rb;Ti2I"ı            (var << "STRING") if @var.present? and @var.present?
            (var << rejected_types_and_failures)
          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        ;Fi[	@ëiHI"˝              @var.any? ? ((var << "STRING")) : ((var << "STRING"))
            end
          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          @var.send("STRING", var);Fi[	I"Tdata//paperclip_proj/paperclip/matchers/validate_attachment_presence_matcher.rb;TiI"
          @var = var
          @var = var.new if (var.class == Class)
          error_when_not_valid? and no_error_when_valid?
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@ñiI"≈        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@ñi#I"Ø        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        ;Fi[	I"Pdata//paperclip_proj/paperclip/matchers/validate_attachment_size_matcher.rb;Ti.I"1          @var = var
          @var = @var.new if (@var.class == Class)
          lower_than_low? and (higher_than_low? and (lower_than_high? and higher_than_high?))
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@ùi2I"≈        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@ùi6I"Ø        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        ;Fi[	I"ddata//redmine_proj/plugins/open_id_authentication/lib/open_id_authentication/mem_cache_store.rb;Ti8I"ô    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING";Fi[	@§i<I"¨    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      Digest::SHA1.hexdigest(var);Fi[	I")data//redmine_proj/redmine/themes.rb;Ti;I"       
      def function(arglist)
        @var ||= assets("STRING", "STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@©i?I"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@©iCI"ó      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      ;Fi[	@ôiI"Ú          heads_for_wiki_formatter
          var = "STRING"
          javascript_tag("STRING")
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          unless @var then;Fi[	I")data//redmine_proj/SVG/Graph/Plot.rb;Ti∫I"∑          end
          var = (var + 1))
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        return "STRING";Fi[	I"'data//rescue_proj/resque/worker.rb;TiÁI"    # Is this worker the same as another worker?
    def function(arglist)
      (to_s == var.to_s)
    end
    
    def function(arglist)
      "STRING"
    end
    
    # The string representation is the same as the id for this worker
    # instance. Can be used with `Worker.find`.;Fi[	I" data//rescue_proj/resque.rb;Ti∏I"µ  # Register an after_perform proc.
  def function(arglist)
    register_hook(:after_perform, var)
  end
  
  def function(arglist)
    "STRING"
  end
  
  attr_accessor(:inline)
  ;Fi[	I")data//rspec_proj/spec/matchers/be.rb;TiI"ª      def function(arglist)
        @var = var
        @var.!.!
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ªiI"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ªi!I"¸      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      [:==, :<, :<=, :>=, :>, :===].each do |arglist|
        define_method(var) { |arglist| BeComparedTo.new(var, var) };Fi[	@ªiKI"“      def function(arglist)
        @var = var
        @var.__send__(@var, @var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var = "STRING";Fi[	@ªinI"µ        rescue NameError
          raise(var)
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ªirI"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ªivI"ó      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      ;Fi[	@ªiôI"ƒ      def function(arglist)
        @var = var
        @var.equal?(@var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ªiùI"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	I"-data//rspec_proj/spec/matchers/change.rb;Ti1I"∂      
      def function(arglist)
        (@var - @var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = var;Fi[	I"*data//rspec_proj/spec/matchers/has.rb;TiI"“      
      def function(arglist)
        var.__send__(predicate(@var), *@var, &@var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@—iI"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@—iI"ó      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      ;Fi[	I"+data//rspec_proj/spec/matchers/have.rb;Ti'I"        return (@var >= @var) if (@var == :at_least)
        return (@var <= @var) if (@var == :at_most)
        return (@var == @var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ÿi+I"¡      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        if (@var == :exactly) then;Fi[	@ÿi;I"‡            return "STRING" if (@var == :at_least)
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        (@var.respond_to?(var) or super);Fi[	I"2data//rspec_proj/spec/matchers/match_array.rb;TiI"Î        var = (var + "STRING") unless @var.empty?
        var = (var + "STRING") unless @var.empty?
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ﬂiI"ó      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      ;Fi[	I"6data//rspec_proj/spec/matchers/raise_exception.rb;Ti;I"…      
      def function(arglist)
        @var ? (@var.message) : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@‰i?I"ó      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      ;Fi[	I"1data//rspec_proj/spec/matchers/respond_to.rb;TiI"Û          (@var << var) unless var.respond_to?(var) and matches_arity?(var, var)
        end
        return @var.empty?
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ÈiI"Ø      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	@ÈiI"±      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = var;Fi[	I".data//rspec_proj/spec/matchers/satisfy.rb;TiI"ø        @var = var if var
        @var = var
        @var.call(var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING";Fi[	I"3data//rspec_proj/spec/matchers/throw_symbol.rb;Ti,I"≠      
      def function(arglist)
        @var ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      ;Fi[	I"(data//rspec_proj/spec/mocks/mock.rb;TiI"ˆ      # target.
      def function(arglist)
        (var == __mock_proxy)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        inspect.gsub("STRING", "STRING").gsub("STRING", "STRING");Fi[	@¢i∏I"Ì            target_metaclass.protected_method_defined?(var) ? ("STRING") : ("STRING")
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var.clear;Fi[	I"2data//rspec_proj/spec/runner/configuration.rb;TiΩI"‰      
      def function(arglist)
        (var[:type] or var[:behaviour_type])
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var, var = args_and_options(*var);Fi[	I"=data//rspec_proj/spec/runner/formatter/html_formatter.rb;TiI"‹          @var.puts("STRING")
          @var.puts("STRING")
          @var.flush
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@˛iÉI"≈        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	@˛iáI"≈        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING";Fi[	I"1data//whenever_proj/whenever/command_line.rb;TikI"⁄    def function(arglist)
      var = var.split($/, -1)[(@var[:cut]..-1)].join($/)
      var.gsub!(/\s+$/, $/)
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING";Fi[	@ioI"ô    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING";Fi[I" ;FI"def function(arglist);FI""STRING";FI"end;FI" ;FI"def function(arglist);Fil[l[	@÷iI""        def function(arglist)
          (var.class == self.class) and (var.active_record == active_record)
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          Arel::Table.new(table_name, arel_engine)
        end;Fi[	@ŸiI"˛        def function(arglist)
          reflection.active_record_primary_key
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          join_table[reflection.foreign_key]
        end;Fi[	@‹i.I"µ        end
        execute("STRING", "STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@‹i2I"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@„ijI"¿      def function(arglist)
        quote_column_name(var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@„inI"      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        if var.acts_like?(:time) then
          var = (ActiveRecord::Base.default_timezone == :utc) ? (:getutc) : (:getlocal);Fi[	@ÎivI"‡        add_column_options!(var, var)
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var.map { |arglist| remove_column_sql(var, var) }
      end;Fi[	@ÛimI"Ó          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        configuration_without_database.merge("STRING" => "STRING", "STRING" => (root_password))
      end;Fi[	@˘i!I"ˆ      @var = Hash.new { |arglist| var[var] = [] }
      var.specs.each { |arglist| @var[var] = var.dup }
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      each { |arglist| return false }
      true;Fi[	@fi2I"ﬁ        (var << "STRING") unless (@var == DEFAULT_GLOB)
        (var << "STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        self.class.hash
      end;Fi[	@Äi|I"!          def function(arglist)
            (@var == "STRING")
          end
          
          def function(arglist)
            "STRING"
          end
          
          def function(arglist)
            (stream == var.stream) and (transaction_id == var.transaction_id)
          end;Fi[	@iGI"‚    def function(arglist)
      transfers.any? { |arglist| var.active? }
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      from.responds_to?(:read) ? ("STRING") : (from)
    end;Fi[	@	iI"Ã        def function(arglist)
          :stand_alone
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@	iI"»        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@	iI"»        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@	iI"»        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@i4I"$    def function(arglist)
      @var ||= sass_files.map { |arglist| corresponding_css_file(var) }
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      css_files.map { |arglist| File.dirname(var) }.uniq.sort.sort_by do |arglist|
        var.length;Fi[	@iI"∂      def function(arglist)
        :stand_alone
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@iI"–      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        File.join(top_level.css_dir, "STRING")
      end;Fi[	@iMI"        var = @var[(var + 1)]
        var.call(var) if var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        if Cucumber::WINDOWS and (var and ENV["STRING"].!) then
          var = var.gsub(/\//, "STRING");Fi[	@iXI"ˆ        end
        @var = var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var = name.split(/\n/)[0]
        var =~ /#{language.keywords("STRING")}STRING/ ? ($1.strip) : (var);Fi[	@!i£I"‰        def function(arglist)
          @var.backtrace_line(name, line)
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          @var.language
        end;Fi[	@$irI"µ          var
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var = @var
        var.each do |arglist|;Fi[	@*i	I"ó  PROC_PATTERN = /[\d\w]+@(.+):(\d+).*>/
  
  PWD = Dir.pwd
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end;Fi[	@-iΩI"‚      def function(arglist)
        File.read((File.dirname("STRING") + "STRING"))
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        (@var << "STRING")
      end;Fi[	@0iI"‡      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = []
        add_to_python_path(File.dirname("STRING"));Fi[	@3iI"ê  def function(arglist)
    raw_bookmarklet
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    bookmarklet
  end;Fi[	@6i	I"ó  def function(arglist)
    Jobs::Mail::AlsoCommented
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end;Fi[	@6iI"ß  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    Post.where(:id => (self.target_id)).first
  end;Fi[	@;i	I"ó  def function(arglist)
    Jobs::Mail::CommentOnPost
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end;Fi[	@;iI"ß  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    Post.where(:id => (self.target_id)).first
  end;Fi[	@@i	I"è  def function(arglist)
    Jobs::Mail::Liked
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end;Fi[	@@iI"±  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    var = self.target
    var = var.target if var.is_a?(Like);Fi[	@Ei	I"ì  def function(arglist)
    Jobs::Mail::Mentioned
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end;Fi[	@EiI"ü  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    Mention.find(self.target_id).post
  end;Fi[	@Mi	I"í  def function(arglist)
    Jobs::Mail::Reshared
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end;Fi[	@MiI"â  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    self.target
  end;Fi[	@Ri	I"¶  def function(arglist)
    Jobs::Mail::StartedSharing
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    super(var.sender, var)
  end;Fi[	@Ui√I"ï    end
    var
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    if self.owner then
      var = self.owner.username;Fi[	@Xi=I"Ñ  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    []
  end;Fi[	I"4data//diaspora_proj/models/services/facebook.rb;TiI":  OVERRIDE_FIELDS_ON_FB_UPDATE = [:contact_id, :person_id, :request_id, :invitation_id, :photo_url, :name, :username]
  
  MAX_CHARACTERS = 420
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    Rails.logger.debug("STRING")
    post_to_facebook("STRING", create_post_params(var).to_param);Fi[	I"2data//diaspora_proj/models/services/tumblr.rb;TiI"µ  include(ActionView::Helpers::TagHelper)
  
  MAX_CHARACTERS = 1000
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    AppConfig.services.tumblr.key
  end;Fi[	I"3data//diaspora_proj/models/services/twitter.rb;Ti
I"¬  MAX_CHARACTERS = 140
  
  SHORTENED_URL_LENGTH = 21
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    Rails.logger.debug("STRING")
    var = public_message(var, var);Fi[	I"5data//diaspora_proj/uploaders/processed_image.rb;TiI"€class ProcessedImage < CarrierWave::Uploader::Base
  include(CarrierWave::MiniMagick)
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    ["STRING", "STRING", "STRING", "STRING", "STRING"]
  end;Fi[	I"7data//diaspora_proj/uploaders/unprocessed_image.rb;TiI"”class UnprocessedImage < CarrierWave::Uploader::Base
  include(CarrierWave::MiniMagick)
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    ["STRING", "STRING", "STRING", "STRING"]
  end;Fi[	@[i[I"§  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    `uname -m`.chomp
  end;Fi[	@ai&I"ø      end
    end)
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    ([FORGE_BUNDLE_ID, APPLE_BUNDLE_ID].find do |arglist|
      MacOS.app_with_bundle_id(var);Fi[	@diWI"      var = (("STRING" + @var["STRING"].gsub("STRING", "STRING")) + "STRING")
      var.=~(Regexp.new(var, Regexp::IGNORECASE))
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      setup
      case @var["STRING"];Fi[	@giI"&      var = (("STRING" + @var["STRING"].gsub("STRING", "STRING")) + "STRING")
      var.=~(Regexp.new(var, Regexp::IGNORECASE))
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      setup
      return RedCloth.new(var).to_html if @var["STRING"].nil?;Fi[	@ji1I"˛      var = (var + var["STRING"]) if var["STRING"]
      var
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      var = var.sub(/<pre>/, (("STRING" + var) + "STRING"))
      var = var.sub(/<\/pre>/, "STRING");Fi[	@mi#I"…      def self.build(arglist)
        var ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@mi'I"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@mi+I"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@mi/I"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	I"1data//nokogiri_proj/nokogiri/xml/document.rb;TiﬁI"§      end
      
      private
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        [:name, :children]
      end;Fi[	@yijI"≈        end
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        [:name, :namespace, :attribute_nodes, :children]
      end;Fi[	I" data//omni_proj/omniauth.rb;TidI"Ø  
  module Utils
    module_function
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      var = var.dup
      var.keys.each do |arglist|;Fi[	@iI"u  end
  
  protected
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end;Fi[	@iI"ï  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    migration_name.camelize
  end;Fi[	@ÇiI"“    def function(arglist)
      @var.read(var, var)
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      @var = var.gsub(OS_RESTRICTED_CHARACTERS, "STRING")
    end;Fi[	@Öi
I"†    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end;Fi[	@ÖiI"ï    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      0
    end;Fi[	@äiI"˛          @var = @var.class unless Class.===(@var)
          responds? and (has_column? and included?)
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@äi!I"»        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@ëiII"œ            end
          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          @var.send("STRING", var)
          @var.valid?;Fi[	@ñiI"          @var = var.new if (var.class == Class)
          error_when_not_valid? and no_error_when_valid?
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@ñi I"»        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@ùi/I"(          @var = @var.new if (@var.class == Class)
          lower_than_low? and (higher_than_low? and (lower_than_high? and higher_than_high?))
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@ùi3I"»        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@§i5I"Ñ    end
    
    private
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end;Fi[	@§i9I"ú    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end;Fi[	@§i=I"Ø    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      Digest::SHA1.hexdigest(var)
    end;Fi[	@©i<I"Õ      def function(arglist)
        @var ||= assets("STRING", "STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@©i@I"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ôiI"          var = "STRING"
          javascript_tag("STRING")
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          unless @var then
            content_for(:header_tags) do |arglist|;Fi[	@≤iªI"≥          var = (var + 1))
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        return "STRING"
      end;Fi[	@ªiI"©        @var = var
        @var.!.!
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ªiI"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ªiLI"        @var = var
        @var.__send__(@var, @var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var = "STRING"
        raise((var << ([:===, :==].include?(@var) ? ("STRING") : ("STRING"))));Fi[	@ªioI"¶          raise(var)
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ªisI"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ªiöI"≤        @var = var
        @var.equal?(@var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ªiûI"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@Œi2I"º      def function(arglist)
        (@var - @var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = var
        self;Fi[	@—iI"’      def function(arglist)
        var.__send__(predicate(@var), *@var, &@var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@—iI"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ÿi(I"◊        return (@var <= @var) if (@var == :at_most)
        return (@var == @var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ÿi,I"‘      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        if (@var == :exactly) then
          return "STRING";Fi[	@ÿi<I"∑          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        (@var.respond_to?(var) or super)
      end;Fi[	@ﬂiI"√        var = (var + "STRING") unless @var.empty?
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@‰i<I"Ã      def function(arglist)
        @var ? (@var.message) : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ÈiI"¨        end
        return @var.empty?
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ÈiI"≤      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ÈiI"∑      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = var
        self;Fi[	@iI"Ø        @var = var
        @var.call(var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@ˆi I"      def function(arglist)
        (var == __mock_proxy)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        inspect.gsub("STRING", "STRING").gsub("STRING", "STRING")
      end;Fi[	@¢iπI"°          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var.clear
      end;Fi[	@˚iæI"      def function(arglist)
        (var[:type] or var[:behaviour_type])
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        var, var = args_and_options(*var)
        return [scope_from(*var), var];Fi[	@˛iÄI"           @var.puts("STRING")
          @var.flush
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@˛iÑI"»        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@˛iàI"»        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end;Fi[	@ilI"»      var = var.split($/, -1)[(@var[:cut]..-1)].join($/)
      var.gsub!(/\s+$/, $/)
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end;Fi[	@ipI"ú    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end;Fi[@(@)I" ;FI"private;FI" ;FI"def function(arglist);FiH[H[	I"5data//activemodel_proj/active_model/callbacks.rb;Ti4I"ı      var.each do |arglist|
        define_callbacks(var, var)
        var.each { |arglist| send("STRING", self, var) }
      end
    end
    
    private
    
    def function(arglist)
      var.class_eval("STRING", "STRING", (126 + 1))
    end;Fi[	I";data//activemodel_proj/active_model/serializers/xml.rb;Ti`I"Ÿ            add_extra_behavior
            add_procs
            yield(@var) if block_given?
          end
        end
        
        private
        
        def function(arglist)
          # do nothing
        end;Fi[	I"Adata//activemodel_proj/active_model/validations/clusivity.rb;TiI"c      def function(arglist)
        unless (delimiter.respond_to?(:include?) or (delimiter.respond_to?(:call) or delimiter.respond_to?(:to_sym))) then
          raise(ArgumentError, ERROR_MESSAGE)
        end
      end
      
      private
      
      def function(arglist)
        var = if delimiter.respond_to?(:call) then
          delimiter.call(var);Fi[	I">data//activemodel_proj/active_model/validations/length.rb;Ti1I"3          var = options[MESSAGES[var]]
          var[:message] ||= var if var
          var.errors.add(var, MESSAGES[var], var)
        end
      end
      
      private
      
      def function(arglist)
        (if options[:tokenizer] and var.kind_of?(String) then
          options[:tokenizer].call(var);Fi[	I"Hdata//activerecord_proj/active_record/associations/alias_tracker.rb;Ti I"4          var = connection.table_alias_for(var)
          aliases[var] += 1
          (aliases[var] > 1) ? ("STRING") : (var)
        end
      end
      
      private
      
      def function(arglist)
        return 0 if Arel::Table.===(table_joins)
        var = connection.quote_table_name(var).downcase;Fi[	I"Xdata//activerecord_proj/active_record/associations/preloader/through_association.rb;TiI"R          var.each do |arglist|
            var.map! { |arglist| var.send(source_reflection.name) }.flatten!
            var.compact!
          end
        end
        
        private
        
        def function(arglist)
          Preloader.new(owners, through_reflection.name, through_scope).run
          Hash[owners.map do |arglist|;Fi[	I"Bdata//activerecord_proj/active_record/attribute_assignment.rb;TirI"!          read_time
        else
          (var == Date) ? (read_date) : (read_other(var))
        end
      end
      
      private
      
      def function(arglist)
        if object.class.send(:create_time_zone_conversion_attribute?, name, column) then
          Time.zone.local(*var);Fi[	I"Tdata//activerecord_proj/active_record/attribute_methods/time_zone_conversion.rb;Ti+I"m            generated_attribute_methods.module_eval(var, "STRING", var)
          else
            super
          end
        end
        
        private
        
        def function(arglist)
          time_zone_aware_attributes and (self.skip_time_zone_conversion_for_attributes.include?(var.to_sym).! and [:datetime, :timestamp].include?(var.type))
        end;Fi[	@i2I"C        
        def function(arglist)
          time_zone_aware_attributes and (self.skip_time_zone_conversion_for_attributes.include?(var.to_sym).! and [:datetime, :timestamp].include?(var.type))
        end
      end
      
      private
      
      def function(arglist)
        var.change(:usec => 0) if var
      end;Fi[	I"Zdata//activerecord_proj/active_record/connection_adapters/abstract/connection_pool.rb;TiEI"        def function(arglist)
          synchronize do |arglist|
            var ? ((no_wait_poll or wait_poll(var))) : (no_wait_poll)
          end
        end
        
        private
        
        def function(arglist)
          @var.synchronize(&var)
        end;Fi[	@i¿I"–            break unless (var <= Base))
          end
          class_to_pool[var.name] = var)
        end
      end
      
      private
      
      def function(arglist)
        @var[Process.pid]
      end;Fi[	I"Zdata//activerecord_proj/active_record/connection_adapters/connection_specification.rb;Ti&I";            resolve_hash_connection(config)
          else
            # do nothing
          end
        end
        
        private
        
        def function(arglist)
          var = configurations.fetch(var) { |arglist| connection_url_to_hash(var) }
          raise(AdapterNotSpecified, "STRING") unless var;Fi[	I"Odata//activerecord_proj/active_record/connection_adapters/mysql_adapter.rb;TiëI"ÿ          exec_query("STRING")
        rescue Mysql::Error
          # do nothing
        end
      end
      
      private
      
      def function(arglist)
        var = {}
        log(var, var, var) do |arglist|;Fi[	I"9data//activerecord_proj/active_record/query_cache.rb;Ti&I"      rescue Exception => var
        restore_query_cache_settings(var, var)
        raise(var)
      end
    end
    
    private
    
    def function(arglist)
      ActiveRecord::Base.connection_id = var
      ActiveRecord::Base.connection.clear_query_cache;Fi[	I">data//activerecord_proj/active_record/relation/batches.rb;TiMI"ã        else
          raise("STRING")
        end
      end
    end
    
    private
    
    def function(arglist)
      "STRING"
    end;Fi[	I"3data//activerecord_proj/active_record/store.rb;Ti,I"Ò      if var.!=(var[var]) then
        send(:"#{var}STRING")
        var[var] = var
      end
    end
    
    private
    
    def function(arglist)
      var = send(var)
      unless var.is_a?(ActiveSupport::HashWithIndifferentAccess) then;Fi[	I"-data//bundler_proj/bundler/definition.rb;TiI"”          # do nothing
        end
        raise(RubyVersionMismatch, var)
      end
    end
    
    private
    
    def function(arglist)
      @var.! and (@var.! and (@var.! and (@var.! and @var.!)))
    end;Fi[	@˘i~I"û        @var.uniq!
      else
        raise(ArgumentError, "STRING")
      end
    end
    
    private
    
    def function(arglist)
      @var[var]
    end;Fi[	I"+data//bundler_proj/bundler/injector.rb;Ti!I"‘        return @var)
      ensure
        Bundler.settings[:frozen] = "STRING" if var
      end
    end
    
    private
    
    def function(arglist)
      @var.map { |arglist| "STRING" }.join("STRING")
    end;Fi[	I",data//bundler_proj/bundler/installer.rb;TiîI"Ì          Bundler.ui.warn("STRING"))
        end
        Bundler.ui.warn("STRING")
      end
    end
    
    private
    
    def function(arglist)
      var = Bundler.bin_path
      var = File.read(File.expand_path("STRING", "STRING"));Fi[	I"*data//bundler_proj/bundler/runtime.rb;Ti≥I"        var.unshift("STRING")
        Bundler::WINDOWS ? (var.unshift("STRING")) : (var.unshift("STRING"))
        ENV["STRING"] = var.join("STRING")
      end
    end
    
    private
    
    def function(arglist)
      var = Dir["STRING"]
      var = var.delete_if do |arglist|;Fi[	I"Edata//cancan_proj/cancan/model_adapters/active_record_adapter.rb;TixI"O          else
            @var.scoped(:conditions => (conditions), :joins => (joins))
          end
        end
      end
      
      private
      
      def function(arglist)
        var = @var.map(&:conditions).compact
        if defined? ActiveRecord::Relation and var.any? { |arglist| var.kind_of?(ActiveRecord::Relation) } then;Fi[	@ÉiJI"Á            return var[/hash='(.*?)'/, 1]
          else
            return var
          end
        end
        
        private
        
        def function(arglist)
          case variable(:scm_verbose)
          when nil then;Fi[	I"Mdata//capistrano_proj/capistrano/recipes/deploy/strategy/remote_cache.rb;TiI"J          super.check do |arglist|
            var.remote.command("STRING") unless copy_exclude.empty?
            var.remote.writable(shared_path)
          end
        end
        
        private
        
        def function(arglist)
          File.join(shared_path, (configuration[:repository_cache] or "STRING"))
        end;Fi[	I"8data//compass_proj/compass/commands/project_base.rb;TiFI"T          raise(Compass::FilesystemConflict.new("STRING"))
        else
          raise(Compass::Error.new("STRING")) if File.directory?(project_directory).!
        end
      end
      
      private
      
      def function(arglist)
        if var[:project_name] then
          File.basename(strip_trailing_separator(var[:project_name]));Fi[	I"@data//compass_proj/compass/sass_extensions/sprites/image.rb;TiåI"6          if [hover?, target?, active?].any? then
            PARENT.match(name)
            base.image_for($1)
          end
        end
        
        private
        
        def function(arglist)
          @var ||= Compass::SassExtensions::Functions::ImageSize::ImageProperties.new(file).size
        end;Fi[	I";data//cucumber_prok/cucumber/core_ext/instance_exec.rb;Ti1I"Ω      else
        instance_exec(*var, &var)
      end
    end
  end
  
  private
  
  def function(arglist)
    var = var.arity
    Cucumber::RUBY_1_9 ? (var) : ((var == -1) ? (0) : (var));Fi[	I"1data//cucumber_prok/cucumber/feature_file.rb;TiPI"Æ          end
          raise(var)
        end
      end
    end
    
    private
    
    def function(arglist)
      var = DEFAULT_ENCODING
      var.each_line do |arglist|;Fi[	I"9data//diaspora_proj/controllers/blocks_controller.rb;Ti!I"5    respond_with do |arglist|
      var.html { |arglist| redirect_to(:back, var) }
      var.json { |arglist| render(:nothing => (true), :status => 204) }
    end
  end
  
  private
  
  def function(arglist)
    if var = current_user.contact_for(var) then
      current_user.disconnect(var, :force => (true));Fi[	I";data//diaspora_proj/controllers/comments_controller.rb;Ti@I"4        render(:json => (CommentPresenter.as_collection(@var)), :status => 200)
      end
      var.mobile { |arglist| render(:layout => (false)) }
    end
  end
  
  private
  
  def function(arglist)
    if user_signed_in? then
      @var = current_user.find_visible_shareable_by_id(Post, params[:post_id]);Fi[	I">data//diaspora_proj/controllers/invitations_controller.rb;TiHI"⁄    unless AppConfig.settings.invitations.open? then
      flash[:error] = I18n.t("STRING")
      redirect_to(:back)
    end
  end
  
  private
  
  def function(arglist)
    User.email_regexp.match(var).present?
  end;Fi[	I"8data//diaspora_proj/controllers/likes_controller.rb;Ti+I"s    respond_to do |arglist|
      var.all { |arglist| render(:layout => (false)) }
      var.json { |arglist| render(:json => (@var.as_api_response(:backbone))) }
    end
  end
  
  private
  
  def function(arglist)
    @var ||= if params[:post_id] then
      (current_user.find_visible_shareable_by_id(Post, params[:post_id]) or raise(ActiveRecord::RecordNotFound.new));Fi[	I"9data//diaspora_proj/controllers/people_controller.rb;Ti•I"–        flash[:error] = I18n.t("STRING", :name => (search_query))
        redirect_to(:back)
      end
    end
  end
  
  private
  
  def function(arglist)
    var = var.map { |arglist| var.id }
    var = {};Fi[	I"9data//diaspora_proj/controllers/photos_controller.rb;Ti{I"       end
    else
      redirect_to(person_photos_path(current_user.person))
    end
  end
  
  private
  
  def function(arglist)
    if request.params[:qqfile].is_a?(String).! then
      var[:qqfile];Fi[	I";data//diaspora_proj/controllers/profiles_controller.rb;Ti9I"          redirect_to(edit_profile_path)
        end
      end
    end
  end
  
  private
  
  def function(arglist)
    unless (@var[:tag_string].nil? or (@var[:tag_string] == I18n.t("STRING"))) then
      @var[:tag_string].split("STRING").each do |arglist|;Fi[	I"Bdata//diaspora_proj/controllers/status_messages_controller.rb;TiBI"L        var.mobile { |arglist| redirect_to(stream_path) }
        var.json { |arglist| render(:nothing => (true), :status => 403) }
      end
    end
  end
  
  private
  
  def function(arglist)
    if (params[:status_message][:public] or (params[:status_message][:aspect_ids].first == "STRING")) then
      current_user.aspect_ids;Fi[	I"7data//diaspora_proj/controllers/tags_controller.rb;Ti,I"„          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  
  private
  
  def function(arglist)
    TagFollowing.user_is_following?(current_user, params[:name])
  end;Fi[	I"4data//diaspora_proj/helpers/analytics_helper.rb;TiI"‚  def function(arglist)
    include_analytics("STRING") do |arglist|
      javascript_tag { |arglist| "STRING".html_safe }
    end
  end
  
  private
  
  def function(arglist)
    return unless configured?(var)
    yield(var);Fi[	I"*data//diaspora_proj/models/contact.rb;TiPI"Î      else
        AspectMembership.exists?(:contact_id => (self.id), :aspect_id => (var.id))
      end
    end
  end
  
  private
  
  def function(arglist)
    (errors[:base] << "STRING") if person_id and person.closed_account?
  end;Fi[	@Ui7I"    if self.url then
      self.url = ("STRING" + self.url) unless self.url.match(/https?:\/\//)
      self.url = (self.url + "STRING") if self.url[-1, 1].!=("STRING")
    end
  end
  
  private
  
  def function(arglist)
    Webfinger.new(self.diaspora_handle).fetch
    self.reload;Fi[	I"*data//diaspora_proj/models/profile.rb;TiªI"€    if @var then
      errors.add(:birthday)
      @var = nil
    end
  end
  
  private
  
  def function(arglist)
    ((self.attributes.keys - Profile.protected_attributes.to_a) - ["STRING", "STRING", "STRING"])
  end;Fi[	I"3data//diaspora_proj/models/share_visibility.rb;TiI"    else
      var = var.map { |arglist| [var, var.id, var.class.base_class.to_s] }
      ShareVisibility.import([:contact_id, :shareable_id, :shareable_type], var)
    end
  end
  
  private
  
  def function(arglist)
    (errors[:base] << "STRING") if shareable.public?
  end;Fi[	I"$data//haml_proj/haml/options.rb;Ti„I"»      self.class.buffer_option_keys.inject({}) do |arglist|
        var[var] = send(var)
        var
      end
    end
    
    private
    
    def function(arglist)
      self.class.defaults
    end;Fi[	I"$data//homebrew_proj/cmd/info.rb;TiwI"ƒ      else
        raise("STRING")
      end
    end
  end
  
  private
  
  def function(arglist)
    ((var[(0..6)] == "STRING") or ((var[(0..7)] == "STRING") or (var[(0..5)] == "STRING")))
  end;Fi[	I"$data//homebrew_proj/cmd/list.rb;TiI"˘          ARGV.kegs.each { |arglist| PrettyListing.new(var) }
        end
      end
    end
  end
  
  private
  
  def function(arglist)
    var = HOMEBREW_PREFIX.children.select { |arglist| var.directory? }.map do |arglist|
      var.basename.to_s;Fi[	I"&data//homebrew_proj/cmd/update.rb;Ti+I"è    else
      puts("STRING")
      var.dump
    end
  end
  
  private
  
  def function(arglist)
    begin
      if Dir["STRING"].empty? then;Fi[	I"-data//homebrew_proj/download_strategy.rb;TinI"ƒ      safe_system("STRING", "STRING", @var)
    else
      FileUtils.cp(@var, File.basename(@var))
    end
  end
  
  private
  
  def function(arglist)
    var = Dir["STRING"]
    case var.length;Fi[	@riwI"≈        Find.prune
        FileUtil.rm_r(var, :force => (true))
      end
    end
  end
  
  private
  
  def function(arglist)
    var = var.sub(/^cvs:\/\//, "STRING").split(/:/)
    var = var.pop;Fi[	I"'data//homebrew_proj/extend/ARGV.rb;Ti®I"¿      yield)
    ensure
      replace(var)
    end
  end
  
  private
  
  def function(arglist)
    @var ||= named.map { |arglist| var.include?("STRING") ? (var) : (var.downcase) }.uniq
  end;Fi[	I"#data//homebrew_proj/version.rb;Ti£I"ß      @var
    else
      @var.is_a?(Symbol) ? (detect_from_symbol) : (raise("STRING"))
    end
  end
  
  private
  
  def function(arglist)
    raise("STRING")
  end;Fi[	I"6data//jekyll_proj/jekyll/generators/pagination.rb;Ti0I"∑        else
          var.pager = var
        end
      end
    end
    
    private
    
    def function(arglist)
      var = var.config["STRING"]
      var.sub("STRING", var.to_s);Fi[	@yi+I"Ñ        document.canonicalize(var, var, var) do |arglist|
          var = var.is_a?(XML::Node) ? (var) : (var)
          ((var == var) or var.ancestors.include?(var))
        end
      end
      
      private
      
      def function(arglist)
        var = (var == :next) ? (:add_next_sibling_node) : (:add_previous_sibling_node)
        var = (var == :next) ? (:reverse_each) : (:each);Fi[	I"/data//paperclip_proj/paperclip/geometry.rb;Ti{I"…        end
      else
        scale_to(var)
      end
    end
    
    private
    
    def function(arglist)
      if (var.horizontal? or var.square?) then
        [("STRING" % var.width), var.width];Fi[	I">data//paperclip_proj/paperclip/geometry_parser_factory.rb;TiI"    def function(arglist)
      if match then
        Geometry.new(:height => (@var), :width => (@var), :modifier => (@var), :orientation => (@var))
      end
    end
    
    private
    
    def function(arglist)
      if var = (@var and @var.match(FORMAT)) then
        @var = var[1];Fi[	I"2data//paperclip_proj/paperclip/storage/fog.rb;Ti|I"˘        rescue ::Fog::Errors::Error => var
          warn("STRING")
          false
        end
      end
      
      private
      
      def function(arglist)
        if @var[:fog_host].respond_to?(:call) then
          @var[:fog_host].call(self);Fi[	@iÌI"∆        rescue AWS::Errors::Base => var
          warn("STRING")
          false
        end
      end
      
      private
      
      def function(arglist)
        case var
        when File then;Fi[	I"Kdata//paperclip_proj/paperclip/validators/attachment_size_validator.rb;Ti"I"9      def function(arglist)
        unless (AVAILABLE_CHECKS + [:in]).any? { |arglist| options.has_key?(var) } then
          raise(ArgumentError, "STRING")
        end
      end
      
      private
      
      def function(arglist)
        if var = var[:in] then
          if var[:in].respond_to?(:call).! then;Fi[	I"Tdata//redmine_proj/plugins/open_id_authentication/lib/open_id_authentication.rb;TijI"€      complete_open_id_authentication(&var)
    else
      begin_open_id_authentication(var, var, &var)
    end
  end
  
  private
  
  def function(arglist)
    (params[:openid_identifier] or params[:openid_url])
  end;Fi[	I"Sdata//redmine_proj/plugins/open_id_authentication/test/mem_cache_store_test.rb;TiLI",      assert_nonce(var, false, var, "STRING")
      var = OpenID::Nonce.mk_nonce(3600)
      assert_nonce(var, false, var, "STRING")
    end
  end
  
  private
  
  def function(arglist)
    var = OpenID::CryptUtil.random_string(20, nil)
    var = OpenID::CryptUtil.random_string(128, ALLOWED_HANDLE);Fi[	I"9data//redmine_proj/plugins/rfpdf/lib/fpdf/chinese.rb;TiÍI"Ÿ    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|;Fi[	I":data//redmine_proj/plugins/rfpdf/lib/fpdf/japanese.rb;TiÛI"Ÿ    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|;Fi[	I"8data//redmine_proj/plugins/rfpdf/lib/fpdf/korean.rb;TiŸI"Ÿ    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|;Fi[	I"7data//redmine_proj/plugins/rfpdf/lib/rfpdf/fpdf.rb;TiI"…      return @var
    else
      open(var, "STRING") { |arglist| var.write(@var) }
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    unless (@var.nil? or (@var == "STRING")) then;Fi[	I"*data//redmine_proj/SVG/Graph/Graph.rb;Ti&I"Á            # do nothing
          end
          var.attributes["STRING"] = "STRING"
        end
      end
      
      private
      
      def function(arglist)
        if (var < var) then
          var = partition(var, var, var);Fi[	I"Cdata//rspec_proj/spec/runner/formatter/no_op_method_missing.rb;TiI"Ú            true
          else
            private_methods.any? { |arglist| [var.to_s, var.to_sym].include?(var) }.!
          end
        end
        
        private
        
        def function(arglist)
          # do nothing
        end;Fi[	I"-data//rspec_proj/spec/runner/reporter.rb;TihI"◊        
        def function(arglist)
          @var.is_a?(Spec::Expectations::ExpectationNotMetError)
        end
      end
      
      private
      
      def function(arglist)
        @var.formatters
      end;Fi[	I"1data//sinatra_proj/sinatra/showexceptions.rb;Ti%I"O        end
        var["STRING"] = var
        [500, { "STRING" => (var), "STRING" => (Rack::Utils.bytesize(var.join).to_s) }, var]
      end
    end
    
    private
    
    def function(arglist)
      (Request.new(var).preferred_type("STRING", "STRING") == "STRING").! and [/curl/].index { |arglist| var.=~(var["STRING"]) }
    end;Fi[I"def function(arglist);FI""STRING";FI"end;FI" ;F@I""STRING";Fi6[6[	@‹i/I"∞        execute("STRING", "STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@‹i3I"ù        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@„ikI"´        quote_column_name(var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@	iI"∑          :stand_alone
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@	iI"≥          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@	iI"≥          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@	iI"¥          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end;Fi[	@iI"°        :stand_alone
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@*i
I"v  
  PWD = Dir.pwd
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  ;Fi[	@6i
I"Ç    Jobs::Mail::AlsoCommented
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  ;Fi[	@;i
I"Ç    Jobs::Mail::CommentOnPost
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  ;Fi[	@@i
I"    Jobs::Mail::Liked
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  ;Fi[	@Ei
I"~    Jobs::Mail::Mentioned
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  ;Fi[	@Mi
I"}    Jobs::Mail::Reshared
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  ;Fi[	@mi$I"≤        var ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@mi(I"ù        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@mi,I"ù        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@mi0I"û        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end;Fi[	@iI"r  
  protected
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  ;Fi[	@ÖiI"ã      # do nothing
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    ;Fi[	@äiI"‘          responds? and (has_column? and included?)
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@äi"I"≥          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@ñiI"Ÿ          error_when_not_valid? and no_error_when_valid?
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@ñi!I"≥          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@ùi0I"˛          lower_than_low? and (higher_than_low? and (lower_than_high? and higher_than_high?))
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@ùi4I"≥          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@§i6I"Å    
    private
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    ;Fi[	@§i:I"á      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    ;Fi[	@©i=I"∏        @var ||= assets("STRING", "STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@©iAI"ù        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ªiI"ù        @var.!.!
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ªiI"ù        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ªipI"ò        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ªitI"ù        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ªiõI"¶        @var.equal?(@var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ªiüI"û        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end;Fi[	@—iI"¿        var.__send__(predicate(@var), *@var, &@var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@—iI"ù        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ÿi)I"™        return (@var == @var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ﬂiI"ò        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@‰i=I"∑        @var ? (@var.message) : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ÈiI"ß        return @var.empty?
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ÈiI"ù        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@iI"§        @var.call(var)
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end;Fi[	@˛iÅI"µ          @var.flush
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@˛iÖI"≥          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@˛iâI"¥          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end;Fi[	@imI"î      var.gsub!(/\s+$/, $/)
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    ;Fi[	@iqI"à      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
  end;Fi[I""STRING";FI"end;FI" ;F@I""STRING";FI"end;Fi6[6[	@‹i0I"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@‹i4I"€      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      # Sanitizes the given LIMIT parameter in order to prevent SQL injection.;Fi[	@„ilI"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@	iI"æ        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@	iI"æ        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@	iI"æ        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@	iI"©        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end
    end;Fi[	@iI"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@*iI"•  PWD = Dir.pwd
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  if Proc.new { |arglist| }.to_s.=~(PROC_PATTERN) then;Fi[	@6iI"|  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist);Fi[	@;iI"|  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist);Fi[	@@iI"|  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist);Fi[	@EiI"|  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist);Fi[	@MiI"|  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist);Fi[	@mi%I"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@mi)I"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@mi-I"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@mi1I"ì      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end;Fi[	@iI"Ç  protected
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist);Fi[	@ÖiI"í    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist);Fi[	@äiI"æ        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@äi#I"≤        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected;Fi[	@ñiI"æ        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@ñi"I"≤        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected;Fi[	@ùi1I"æ        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@ùi5I"≤        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected;Fi[	@§i7I"ñ    private
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist);Fi[	@§i;I"í    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist);Fi[	@©i>I"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@©iBI"ö      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private;Fi[	@ªiI"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@ªi I"¬      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      [:==, :<, :<=, :>=, :>, :===].each do |arglist|;Fi[	@ªiqI"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@ªiuI"ö      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private;Fi[	@ªiúI"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@ªi†I"í      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
    ;Fi[	@—iI"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@—iI"ö      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private;Fi[	@ÿi*I"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@ﬂiI"ö      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private;Fi[	@‰i>I"ö      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private;Fi[	@ÈiI"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@ÈiI"®      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@iI"í      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
    ;Fi[	@˛iÇI"æ        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@˛iÜI"æ        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@˛iäI"©        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end
    end;Fi[	@inI"í    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist);Fi[	@irI"}    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[@(@œ@–I"# do nothing;FI"end;FI" ;Fi2[2[	I"]data//activerecord_proj/active_record/associations/belongs_to_polymorphic_association.rb;TiI"⁄      
      def function(arglist)
        reflection.polymorphic_inverse_of(var.class)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var = super;Fi[	@‹iI",      # done if the transaction block raises an exception or returns false.
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      # Set the sequence to the max value of the table's column.
      def function(arglist);Fi[	@Ëi˙I"„        var = "STRING"
        ActiveSupport::Deprecation.warn(var)
        @var.joinable = var
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing;Fi[	@Ëi˛I"ª      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing;Fi[	@ËiI"≤      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var;Fi[	I";data//activerecord_proj/active_record/null_relation.rb;Ti9I"ì    
    def function(arglist)
      0
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false;Fi[	I"8data//activerecord_proj/active_record/reflection.rb;Ti‚I"‹            raise(InverseOfAssociationNotFoundError.new(self))
          end
        end
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing;Fi[	@ìiÊI"      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      # A chain of reflections from this one back to the owner. For more see the explanation in
      # ThroughReflection.;Fi[	I"%data//bundler_proj/bundler/ui.rb;Ti
I"´  class UI
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing;Fi[	@òiI"•    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing;Fi[	@òiI"•    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing;Fi[	@òiI"•    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing;Fi[	@òiI"û    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false;Fi[	I"Hdata//compass_proj/compass/app_integration/stand_alone/installer.rb;Ti,I"G          unless (config_files_exist? or @var.generate_config?.!) then
            write_configuration_files
          end
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          if var[:create] and manifest.welcome_message_options[:replace].! then;Fi[	I"=data//compass_proj/compass/exec/command_option_parser.rb;TiI"     
    def function(arglist)
      OptionParser.new { |arglist| self.set_options(var) }
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      opts.to_s;Fi[	I"<data//compass_proj/compass/installers/bare_installer.rb;TiI"      def function(arglist)
        directory(targetize("STRING"))
        directory(targetize(Compass.configuration.sass_dir))
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var ||= targetize("STRING");Fi[	I")data//compass_proj/compass/logger.rb;TiTI"¨  class NullLogger
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      yield;Fi[	I"2data//compass_proj/compass/sprite_importer.rb;Ti I"›        return self.class.sass_engine(var, self.class.sprite_name(var), self, var)
      end
      nil
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      self.class.name;Fi[	I"(data//compass_proj/compass/stats.rb;TiI"        end
        self.mixin_def_count += 1 if var.is_a?(Sass::Tree::MixinDefNode)
        self.mixin_count += 1 if var.is_a?(Sass::Tree::MixinNode)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing;Fi[	@≤i I"ª      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        return false;Fi[	I"9data//cucumber_prok/cucumber/ast/scenario_outline.rb;TiAI"%        var.visit_steps(@var)
        skip_invoke! if @var and @var.failed?
        var.visit_examples_array(@var) unless @var.empty?
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        @var.each { |arglist| var.skip_invoke! };Fi[	I"6data//cucumber_prok/cucumber/formatter/console.rb;TiÑI"        else
          @var.puts(format_string("STRING", :passed))
        end
      end
      
      def function(arglist)
        # do nothing
      end
      
      #define @delayed_messages = [] in your Formatter if you want to
      #activate this feature;Fi[	I"4data//cucumber_prok/cucumber/formatter/junit.rb;TiEI"      def function(arglist)
        @var = (var.nil? or (var == "STRING")) ? ("STRING") : (var.split("STRING")[0])
        @var = "STRING"
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        return if (@var or @var);Fi[	I";data//cucumber_prok/cucumber/js_support/js_language.rb;TitI"Â      
      def function(arglist)
        var.each { |arglist| load_code_file("STRING") }
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        @var = var.language;Fi[	@¿i|I"À      
      def function(arglist)
        @var = var.language
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        @var.map do |arglist|;Fi[	I";data//cucumber_prok/cucumber/parser/gherkin_builder.rb;Ti;I"9        var = Ast::Step.new(var.line, var.keyword, var.name, Ast::MultilineArgument.from((var.doc_string or var.rows)))
        var.gherkin_statement(var)
        @var.add_step(var)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing;Fi[	@≈i?I"ü      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      private
      ;Fi[	@0iI"∏      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        "STRING";Fi[	@0i"I"        RubyPython.start
        @var = import((File.dirname("STRING") + "STRING"))
        @var.each { |arglist| import(var) }
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        @var.step_matches(var, var);Fi[	I"9data//cucumber_prok/cucumber/runtime/support_code.rb;TiI"        
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          @var.invoke(var.name, Ast::MultilineArgument.from((var.doc_string or var.rows)));Fi[	I"Adata//cucumber_prok/cucumber/wire_support/request_handler.rb;TiI"≠      
      def function(arglist)
        raise(@var.exception(var))
      end
      
      def function(arglist)
        # do nothing
      end
      
      private
      ;Fi[	I"?data//cucumber_prok/cucumber/wire_support/wire_language.rb;TiI"»      
      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var = Configuration.new(var);Fi[	@°i,I"•      
      def function(arglist)
        :inactive
      end
      
      def function(arglist)
        # do nothing
      end
      
      var = ["STRING"]
      ;Fi[	I"@data//devise_proj/devise/models/database_authenticatable.rb;TiOI"        var = update_attributes(var, *var)
        clean_up_passwords
        var
      end
      
      def function(arglist)
        # do nothing
      end
      
      # A reliable way to expose the salt regardless of the implementation.
      def function(arglist);Fi[	I"3data//devise_proj/devise/models/recoverable.rb;TiOI"(      def function(arglist)
        self.reset_password_token = nil
        self.reset_password_sent_at = nil
      end
      
      def function(arglist)
        # do nothing
      end
      
      module ClassMethods
        # Attempt to find a user by its email. If a record is found, send new;Fi[	@Xi8I"é  
  def function(arglist)
    self.class.name
  end
  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    "STRING";Fi[	I"%data//haml_proj/haml/compiler.rb;TizI"õ          end
        end
      end
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      var = @var.value;Fi[	I"8data//omni_proj/omniauth/test/strategy_test_case.rb;TiI"            [404, { "STRING" => "STRING" }, [(var or var.key?("STRING").to_s)]]
          end)
        end.to_app
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        last_request.env["STRING"];Fi[	@ÖiI"ï    
    def function(arglist)
      true
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      true;Fi[	I"0data//paperclip_proj/paperclip/processor.rb;TiI"≤      @var = var
      @var = var
      @var = var
    end
    
    def function(arglist)
      # do nothing
    end
    
    def self.make(arglist)
      new(var, var, var).make;Fi[	@üiˇI"Å  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    # do nothing
  end
  
  attr_reader :function
  ;Fi[	I"6data//rspec_proj/spec/runner/backtrace_tweaker.rb;TiI"¬      
      def function(arglist)
        var.gsub!("STRING", "STRING")
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        [];Fi[	I"Odata//rspec_proj/spec/runner/formatter/failing_example_groups_formatter.rb;TiI"œ            @var.flush
            @var = nil
          end
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing;Fi[	I"Idata//rspec_proj/spec/runner/formatter/failing_examples_formatter.rb;TiI"‰        def function(arglist)
          @var.puts("STRING")
          @var.flush
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing;Fi[	@˛ipI"˛            var = (((example_number.to_f / @var.to_f) * 1000).to_i / 10.0)
          end
          var
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          if dry_run? then;Fi[@(@œ@–I"0assert_version_detected("STRING", "STRING");FI"end;FI" ;Fi/[/[	I".data//homebrew_proj/test/test_versions.rb;TiNI"  
  def function(arglist)
    assert_raises(RuntimeError) { |arglist| var = TestBadVersion.new }
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iRI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iVI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iZI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i^I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛ibI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛ifI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛ijI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛inI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛irI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛ivI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛izI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i~I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i}I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iÅI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iÖI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iâI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iíI"  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iñI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iöI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iûI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i¢I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i¶I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i™I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iÆI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i≤I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i∂I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i∫I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iæI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i¬I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i∆I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iŒI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i“I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i÷I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i⁄I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iﬁI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛i‚I"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iÊI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iÍI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iÓI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING");Fi[	@˛iÚI"8  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  # def test_version_ghc_style
  #   assert_version_detected '7.0.4', 'http://www.haskell.org/ghc/dist/7.0.4/ghc-7.0.4-x86_64-apple-darwin.tar.bz2';Fi[@
@@˘I"end;FI" ;FI"def function(arglist);Fi.[.[	@˛iOI"  def function(arglist)
    assert_raises(RuntimeError) { |arglist| var = TestBadVersion.new }
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iSI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iWI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i[I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i_I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛icI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛igI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛ikI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛ioI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛isI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iwI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i{I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i~I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iÇI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iÜI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iäI"  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@˛iìI"    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛ióI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iõI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iüI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i£I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛ißI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i´I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iØI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i≥I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i∑I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iªI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iøI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i√I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i«I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iÀI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iœI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i”I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i◊I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i€I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iﬂI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛i„I"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iÁI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iÎI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iÔI"Ô  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end;Fi[@Æ@˘I"end;FI" ;FI"def function(arglist);FI"0assert_version_detected("STRING", "STRING");Fi.[.[	@˛iPI"Ò    assert_raises(RuntimeError) { |arglist| var = TestBadVersion.new }
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iTI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iXI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i\I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i`I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛idI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛ihI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛ilI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛ipI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛itI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛ixI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i|I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i{I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iÉI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iáI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iãI"    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iîI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iòI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iúI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i†I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i§I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i®I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i¨I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i∞I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i¥I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i∏I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iºI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i¿I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iƒI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i»I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iÃI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i–I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i‘I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iÿI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i‹I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i‡I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛i‰I"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iËI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iÏI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[	@˛iI"⁄    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  ;Fi[@˘I"end;FI" ;FI"def function(arglist);F@≠I"end;Fi.[.[	@˛iQI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iUI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iYI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i]I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iaI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛ieI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iiI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛imI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iqI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iuI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iyI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i}I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i|I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iÄI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iÑI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iàI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iëI"Ï  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iïI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iôI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iùI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i°I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i•I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i©I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i≠I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i±I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iµI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iπI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iΩI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i¡I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i≈I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i…I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iÕI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i—I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i’I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iŸI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i›I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛i·I"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iÂI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iÈI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iÌI"¬  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist);Fi[	@˛iÒI"…  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  def function(arglist)
    assert_version_detected("STRING", "STRING")
  end
  
  # def test_version_ghc_style;Fi[@(@)@*@+I" ;FI"def function(arglist);Fi,[,[	I"\data//activerecord_proj/active_record/connection_adapters/abstract/schema_statements.rb;TiI"&            if (var < var) then
              execute("STRING")
              (var << var)
            end
          end
        end
      end
      
      def function(arglist)
        if var = native_database_types[var.to_sym] then
          var = (var.is_a?(Hash) ? (var[:name]) : (var)).dup;Fi[	@Di_I"œ                (var..var)
              else
                var
              end
            end
          end
        end
        
        def function(arglist)
          if var.nil? then
            nil;Fi[	I"6data//activerecord_proj/active_record/fixtures.rb;Ti·I"˘        FixtureSet::File.open(var) do |arglist|
          var.each do |arglist|
            fixtures[var] = ActiveRecord::Fixture.new(var, model_class)
          end
        end
      end
    end
    
    def function(arglist)
      "STRING"
    end;Fi[	I":data//activerecord_proj/active_record/sanitization.rb;TiîI"G              quote_bound_value(var[var])
            else
              raise(PreparedStatementInvalid, "STRING")
            end
          end
        end
      end
      
      def function(arglist)
        if var.respond_to?(:map) and var.acts_like?(:string).! then
          if var.respond_to?(:empty?) and var.empty? then;Fi[	@OisI"ﬁ                    var.merge(var)
                  end
                end
              end
            end
          end
        end
        
        def function(arglist)
          Thread.current["STRING"]
        end;Fi[	@¸i‰I"Ï              debug { |arglist| "STRING" }
              throw(var)
            end
          end
        end
      end
    end
    
    def function(arglist)
      var.required_by.replace(var.required_by)
      (var.required_by << var);Fi[	@&i?I"ø              end
              raise(var) if var.nil?
            end
          end
        end
      end
    end
    
    def function(arglist)
      if var.empty? then
        dependencies;Fi[	@vi/I"«            else
              @var.empty? ? (true) : (@var)
            end
          end
        end
      end
    end
    
    def function(arglist)
      conditions_empty? and @var.nil?.!
    end;Fi[	@viÄI")                var.kind_of?(Enumerable) ? (var.include?(var)) : ((var == var))
              end
            end
          end
        end
      end
    end
    
    def function(arglist)
      var, var = var.first
      matches_conditions_hash?(var, (@var[var.class.name.downcase.to_sym] or {}));Fi[	I"0data//capistrano_proj/capistrano/command.rb;TiRI"È                configuration.send(var, *var, &var)
              else
                super
              end
            end
          end
        end
        
        def function(arglist)
          @var = var
          @var = var;Fi[	I"4data//compass_proj/compass/commands/registry.rb;TiI"            raise(Compass::Error, "STRING")
          else
            raise(Compass::Error, "STRING")
          end
        end
      end
    end
    
    def function(arglist)
      var = /^#{Regexp.escape(var)}/
      @var.keys.detect { |arglist| var.to_s.=~(var) };Fi[	I"5data//compass_proj/compass/configuration/data.rb;TiÄI"              if inherited_data.respond_to?(:asset_cache_buster) then
                inherited_data.asset_cache_buster
              end
            end
          end
        end
      end
      
      def function(arglist)
        @var ||= []
        (@var << [var, var]);Fi[	I"Rdata//compass_proj/compass/sass_extensions/functions/cross_browser_support.rb;TiAI"§          var.send(:"to_#{var}")
        else
          var
        end
      end
    end
  end
  
  def function(arglist)
    CSS2FallbackValue.new(var, var)
  end;Fi[	I"8data//cucumber_prok/cucumber/ast/feature_element.rb;Ti/I"j              (((Ast::Step::INDENT + @var.unpack("STRING").length) + "STRING".length) + var.unpack("STRING").length)
            else
              ((Ast::Step::INDENT + Ast::Step::INDENT) + var.unpack("STRING").length)
            end
          end
        end
      end
      
      def function(arglist)
        var.detect { |arglist| var.=~(name) }
      end;Fi[	@!i~I")                var.invoke(var.step_mother, var.configuration)
                @var ||= var.reported_exception
                var.visit_step_result(var)
              end
            end
          end
        end
        
        def function(arglist)
          @var.accept_hook?(var)
        end;Fi[	@-iEI"            @var.div(:id => "STRING") do |arglist|
              @var.p("STRING", :id => "STRING")
              @var.p("STRING", :id => "STRING")
            end
          end
        end
      end
      
      def function(arglist)
        print_stats(var)
        (@var << "STRING");Fi[	I">data//diaspora_proj/controllers/application_controller.rb;TihI"ç            @var = var
            break
          end
        end
      end
    end
  end
  
  def function(arglist)
    (@var or nil)
  end;Fi[	I":data//diaspora_proj/controllers/aspects_controller.rb;TiI"ÿ        var.html do |arglist|
          flash[:error] = I18n.t("STRING")
          redirect_to(:back)
        end
      end
    end
  end
  
  def function(arglist)
    @var = Aspect.new
    @var = params[:person_id];Fi[	I"0data//diaspora_proj/helpers/posts_helper.rb;TiI"          if var.respond_to?(:photos) and var.photos.present? then
            I18n.t("STRING", :count => (var.photos.size), :author => (var.author_name))
          end
        end
      end
    end
  end
  
  def function(arglist)
    var[:width] ||= 516
    var[:height] ||= 315;Fi[	I"1data//diaspora_proj/helpers/stream_helper.rb;TiI"õ          else
            raise("STRING")
          end
        end
      end
    end
  end
  
  def function(arglist)
    var.instance_of?(Reshare)
  end;Fi[	I"/data//diaspora_proj/models/notification.rb;Ti%I"ö          var
        else
          nil
        end
      end
    end
  end
  
  def function(arglist)
    super(var.merge(:methods => :note_html))
  end;Fi[	@·iwI"          if Parser::MID_BLOCK_KEYWORDS.include?(var) then
            @var = @var.parent.value[:dont_indent_next_line]
            @var = @var.parent.value[:dont_tab_up_next_text]
          end
        end
      end
    end
    
    def function(arglist)
      # do nothing
    end;Fi[	I"'data//homebrew_proj/cmd/cleanup.rb;TiKI"ñ            puts("STRING")
            rm(var)
          end
        end
      end
    end
  end
  
  def function(arglist)
    system("STRING")
  end;Fi[	@liJI"Ï          var.symlink? ? (puts("STRING")) : (print_dir(var))
        else
          puts(var) if FORMULA_META_FILES.should_list?(var.basename.to_s)
        end
      end
    end
  end
  
  def function(arglist)
    var = []
    var = [];Fi[	I"&data//homebrew_proj/cmd/search.rb;Ti4I"˝            puts("STRING")
            GitHub.find_pull_requests(var) { |arglist| puts(var) }
          end
        end
      end
    end
  end
  
  def function(arglist)
    (return [] if (HOMEBREW_LIBRARY / "STRING").directory?
    require("open-uri");Fi[	I"+data//homebrew_proj/extend/pathname.rb;TiI"ó          else
            raise("STRING".undent)
          end
        end
      end
    end
  end
  
  def function(arglist)
    join(var.to_s)
  end;Fi[	I"-data//homebrew_proj/formula_installer.rb;TiI"»          opoo("STRING")
          puts("STRING")
          @var = true
        end
      end
    end
  end
  
  def function(arglist)
    if (f.prefix + "STRING").directory? then
      opoo("STRING");Fi[	I"!data//homebrew_proj/macos.rb;TiI"           :snowleopard
        else
          (version == :leopard) ? (:leopard) : (nil)
        end
      end
    end
  end
  
  def function(arglist)
    (@var ||= {}).fetch(var.to_s) do |arglist|
      @var[var.to_s] = if File.executable?("STRING") then;Fi[	@çiI"Ù            unless var.send(CHECKS[var], var) then
              var = options[:in] ? (:in_between) : (var)
              var.errors.add(var, var, filtered_options(var).merge(:min => (min_value_in_human_size(var)), :max => (max_value_in_human_size(var)), :count => (human_size(var))))
            end
          end
        end
      end
      
      def function(arglist)
        unless (AVAILABLE_CHECKS + [:in]).any? { |arglist| options.has_key?(var) } then
          raise(ArgumentError, "STRING");Fi[	@ñi4I"Ÿ          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]));Fi[	@ôi=I"Ÿ          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]));Fi[	@úi#I"Ÿ          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]));Fi[	@üiêI"          var = ("STRING" + var.downcase)
          self.Error(("STRING" + var)) unless self.respond_to?(var)
          self.send(var, var)
        end
      end
    end
  end
  
  def function(arglist)
    var = @var ? ("STRING") : ("STRING")
    @var.each do |arglist|;Fi[	@üiI"Ÿ        else
          (var = freadshort(var)
          var.seek((var - 2), IO::SEEK_CUR))
        end
      end
    end
  end
  
  def function(arglist)
    while true do
      while (var = freadbyte(var)).!=(255) do;Fi[	@àiÇI"Ì            var = ("STRING" + var.downcase)
            self.respond_to?(var).! ? (Error(("STRING" + var))) : (self.send(var, var))
          end
        end
      end
    end
  end
  
  def function(arglist)
    newobj
    out("STRING");Fi[	I"6data//redmine_proj/redmine/helpers/time_report.rb;TiNI"Ø              var = (var + 1.day)
            else
              # do nothing
            end
          end
        end
      end
      
      def function(arglist)
        @var = { "STRING" => ({ :sql => "STRING", :klass => (Project), :label => :label_project }), "STRING" => ({ :sql => "STRING", :klass => (IssueStatus), :label => :field_status }), "STRING" => ({ :sql => "STRING", :klass => (Version), :label => :label_version }), "STRING" => ({ :sql => "STRING", :klass => (IssueCategory), :label => :field_category }), "STRING" => ({ :sql => "STRING", :klass => (User), :label => :label_user }), "STRING" => ({ :sql => "STRING", :klass => (Tracker), :label => :label_tracker }), "STRING" => ({ :sql => "STRING", :klass => (TimeEntryActivity), :label => :label_activity }), "STRING" => ({ :sql => "STRING", :klass => (Issue), :label => :label_issue }) }
        var = @var.nil? ? (IssueCustomField.for_all) : (@var.all_issue_custom_fields);Fi[	I";data//redmine_proj/redmine/views/builders/structure.rb;Ti9I"Î                @var.last[var].merge!(var)
              else
                @var.last[var] = var
              end
            end
          end
        end
        
        def function(arglist)
          raise("STRING")
        end;Fi[	@¢i~I"                  @var.__send__(:method_missing, var, *var, &var)
                end
              end
            end
          end
        end
      end
      
      def function(arglist)
        @var.raise_unexpected_message_args_error(var, *var)
      end;Fi[	I"(data//whenever_proj/whenever/job.rb;Ti1I"¬            escape_double_quotes(var)
          else
            var
          end
        end
      end
    end
    
    def function(arglist)
      var.gsub(/'/) { |arglist| "STRING" }
    end;Fi[@
@I"# do nothing;FI"end;FI" ;FI"def function(arglist);Fi,[,[	@idI"        end
        
        private
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          serializable_collection.each do |arglist|
            var = ActiveSupport::XmlMini.rename_key(var.name, options);Fi[	@ÖiI"      def function(arglist)
        reflection.polymorphic_inverse_of(var.class)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var = super
        var and [var.to_s, owner[reflection.foreign_type].to_s];Fi[	@Ëi˚I"÷        ActiveSupport::Deprecation.warn(var)
        @var.joinable = var
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end;Fi[	@ËiˇI"æ      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end;Fi[	@ËiI"µ      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var
      end;Fi[	@êi:I"ñ    def function(arglist)
      0
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end;Fi[	@ìi„I"ß          end
        end
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end;Fi[	I"5data//bundler_proj/bundler/lazy_specification.rb;TiFI"æ    end
    
    private
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      raise("STRING") unless @var
      return super unless respond_to?(var);Fi[	@òiI"®    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end;Fi[	@òiI"®    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end;Fi[	@òiI"®    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end;Fi[	@òiI"®    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end;Fi[	@òiI"°    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end;Fi[	@£i-I"            write_configuration_files
          end
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          if var[:create] and manifest.welcome_message_options[:replace].! then
            puts("STRING");Fi[	@¶iI"Õ    def function(arglist)
      OptionParser.new { |arglist| self.set_options(var) }
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      opts.to_s
    end;Fi[	@©iI"        directory(targetize("STRING"))
        directory(targetize(Compass.configuration.sass_dir))
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var ||= targetize("STRING")
        write_file(var, config_contents);Fi[	@¨iUI"°    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      yield
    end;Fi[	@Øi!I"í      end
      nil
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      self.class.name
    end;Fi[	@≤iI"        self.mixin_def_count += 1 if var.is_a?(Sass::Tree::MixinDefNode)
        self.mixin_count += 1 if var.is_a?(Sass::Tree::MixinNode)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end;Fi[	@≤i!I"‘      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        return false
        var = var.send(:import);Fi[	@∑iBI"N        skip_invoke! if @var and @var.failed?
        var.visit_examples_array(@var) unless @var.empty?
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        @var.each { |arglist| var.skip_invoke! }
        @var.next_feature_element(self) { |arglist| var.skip_invoke! };Fi[	@ΩiFI"        @var = (var.nil? or (var == "STRING")) ? ("STRING") : (var.split("STRING")[0])
        @var = "STRING"
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        return if (@var or @var)
        var = (Time.now - @var);Fi[	@¿iuI"Ë      def function(arglist)
        var.each { |arglist| load_code_file("STRING") }
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        @var = var.language
      end;Fi[	@¿i}I"Ù      def function(arglist)
        @var = var.language
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        @var.map do |arglist|
          if var = var.arguments_from(var) then;Fi[	@≈i<I"À        var.gherkin_statement(var)
        @var.add_step(var)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end;Fi[	@0iI"ª      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@0i#I"        @var = import((File.dirname("STRING") + "STRING"))
        @var.each { |arglist| import(var) }
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        @var.step_matches(var, var)
      end;Fi[	@ŒiI"        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          @var.invoke(var.name, Ast::MultilineArgument.from((var.doc_string or var.rows)))
        end;Fi[	@‘iI"Á      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var = Configuration.new(var)
        (@var << Connection.new(var));Fi[	@Xi9I"ë  def function(arglist)
    self.class.name
  end
  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    "STRING"
  end;Fi[	@·i{I"¡        end
      end
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      var = @var.value
      rstrip_buffer! if var[:nuke_outer_whitespace];Fi[	@‰iI"Ω          end)
        end.to_app
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        last_request.env["STRING"]
      end;Fi[	@ÖiI"ò    def function(arglist)
      true
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      true
    end;Fi[	@üi¸I"ê  end
  
  alias_method(:add_page, :AddPage)
  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    # do nothing
  end;Fi[	I"?data//rspec_proj/spec/adapters/mock_frameworks/flexmock.rb;TiI"ÿ  module Adapters
    module MockFramework
      include(FlexMock::MockContainer)
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        flexmock_verify
      end;Fi[	@ÓiI"≈      def function(arglist)
        var.gsub!("STRING", "STRING")
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        []
      end;Fi[	@ÒiI"ƒ            @var = nil
          end
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end;Fi[	@ÙiI"“          @var.puts("STRING")
          @var.flush
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end;Fi[	@˛iqI"Œ          end
          var
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          if dry_run? then
            var = "STRING";Fi[I"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi([([	@ñiXI"–    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ñiYI"◊    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ñiZI"À    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end;Fi[	@ñi[I"´    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@ôiYI"–    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ôiZI"◊    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ôi[I"À    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end;Fi[	@ôi\I"´    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@úiCI"–    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@úiDI"◊    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@úiEI"À    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end;Fi[	@úiFI"´    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@ài†I"–    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ài°I"◊    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ài¢I"À    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end;Fi[	@ài£I"™    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
  ;Fi[	@àia
I"æ    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àib
I"æ    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àic
I"æ    newobj
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àid
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àie
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àif
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àig
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àih
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àii
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àij
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àik
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àil
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àim
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àin
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àio
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àip
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àiq
I"≈    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àir
I"æ    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    newobj;Fi[	@àis
I"æ    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    newobj
    out("STRING");Fi[I"var;FI"end;FI"end;FI"end;FI"end;FI"end;Fi![![	I"Pdata//activerecord_proj/active_record/associations/preloader/association.rb;TimI"√          if options[:as] then
            var.where!(klass.table_name => ({ reflection.type => (model.base_class.sti_name) }))
          end
          var
        end
      end
    end
  end
end;Fi[	@¸i3I"Ω            var.order!(reflection_scope.values[:order])
            var.references!(reflection_scope.values[:references])
          end
          var
        end
      end
    end
  end
end;Fi[	@iII"∆            var = Hash[var.query.split("STRING").map { |arglist| var.split("STRING") }].symbolize_keys
            var.merge!(var)
          end
          var
        end
      end
    end
  end
end;Fi[	I"Tdata//activerecord_proj/active_record/connection_adapters/postgresql/quoting.rb;TiíI"ﬂ          var = super
          var = "STRING" if var.acts_like?(:time) and var.respond_to?(:usec)
          var = (var.sub(/^-/, "STRING") + "STRING") if (var.year < 0)
          var
        end
      end
    end
  end
end;Fi[	I"Idata//activerecord_proj/active_record/railties/controller_runtime.rb;Ti,I"∞        def function(arglist)
          var, var = super, var[:db_runtime]
          (var << ("STRING" % var.to_f)) if var
          var
        end
      end
    end
  end
end;Fi[	I"4data//capistrano_proj/capistrano/cli/options.rb;Ti¥I"p        when "STRING" then
          nil
        else
          var
        end
      end
    end
  end
end;Fi[	I"Fdata//capistrano_proj/capistrano/configuration/actions/inspect.rb;Ti.I"s              # do nothing
            end
          end
          var
        end
      end
    end
  end
end;Fi[	I"@data//capistrano_proj/capistrano/recipes/deploy/scm/base.rb;Ti±I"®        def function(arglist)
          var = variable(:scm_arguments)
          var = var[var] if var.is_a?(Hash)
          var
        end
      end
    end
  end
end;Fi[	@ÜiÅI"Ñ          when /^\d+/ then
            "STRING"
          else
            var
          end
        end
      end
    end
  end
end;Fi[	I"<data//compass_proj/compass/configuration/inheritance.rb;TiÀI"s            end
            var[var] = var
          end
          var
        end
      end
    end
  end
end;Fi[	@≈iNI"Ç            attr_accessor(:line)
          end
          var.line = var.line
          var
        end
      end
    end
  end
end;Fi[	I";data//cucumber_prok/cucumber/rb_support/rb_language.rb;Ti§I"l            raise(var)
          end
        else
          var
        end
      end
    end
  end
end;Fi[	I">data//homebrew_proj/vendor/multi_json/adapters/ok_json.rb;Ti(I"z            var.merge!(var => (var))
          end
        else
          var
        end
      end
    end
  end
end;Fi[	I"7data//mongoid_proj/lib/mongoid/contextual/eager.rb;TiñI"À          return nil unless var
          var = Factory.from_db(klass, var, criteria.object_id)
          eager_load_one(var) if eager_loadable?(var)
          var
        end
      end
    end
  end
end;Fi[	@ÊiàI"’            document[field] = [] unless document[field]
            var = document.send(field).concat(value.__array__)
            execute(var)
            var
          end
        end
      end
    end
  end
end;Fi[	@ÈiI"‘              var = document.send(field)
              (value > 0) ? (var.pop) : (var.shift)
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	@ÓiI"¿              var = document.send(field)
              var.delete(value)
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	@ÛiI"ﬁ              var = document.send(field)
              var.delete_if { |arglist| value.include?(var) }
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end
end;Fi[	@Di I"õ                var.push(Factory.build(klass, var))
              end
            end
            var
          end
        end
      end
    end
  end
end;Fi[	I"<data//mongoid_proj/lib/mongoid/relations/conversions.rb;TiI"ê          var
        else
          var.unconvertable_to_bson = true if var.is_a?(String)
          var
        end
      end
    end
  end
end;Fi[	I".data//mongoid_proj/lib/mongoid/scoping.rb;Ti1I"          var.remove_scoping(var)
          var.to_proc
        else
          var
        end
      end
    end
  end
end;Fi[	I"5data//omni_proj/omniauth/strategies/developer.rb;TiI"™      info do |arglist|
        options.fields.inject({}) do |arglist|
          var[var] = request.params[var.to_s]
          var
        end
      end
    end
  end
end;Fi[	I"Sdata//redmine_proj/plugins/awesome_nested_set/lib/awesome_nested_set/helper.rb;Ti'I"s              end
            end.compact)
          end
          var
        end
      end
    end
  end
end;Ti[	I"6data//redmine_proj/redmine/views/builders/json.rb;TiI"ã            var = "STRING"
            response.content_type = "STRING"
          end
          var
        end
      end
    end
  end
end;Fi[	@ëiI"          @@additional_blocks ||= Dir.glob("STRING").inject({}) do |arglist|
            var = File.basename(var).split("STRING").first.gsub(/^_/, "STRING")
            var[var] = var.to_sym
            var
          end
        end
      end
    end
  end
end;Fi[	@îipI"              if var.match(/<code\s+class="(\w+)">\s?(.+)/m) then
                var = ("STRING" + Redmine::SyntaxHighlighting.highlight_by_language($2, $1))
              end
              var
            end
          end
        end
      end
    end
  end
end;Fi[	I"Ldata//state_machine_proj/state_machine/integrations/mongoid/versions.rb;Ti#I"Ω            var = read(var, :state)
            var.changes[self.attribute.to_s] = [(var == :event) ? (var) : (var), var]
          end
          var
        end
      end
    end
  end
end;Fi[	I".data//twitter_proj/twitter/media/photo.rb;TiI"√      def function(arglist)
        @var ||= Array(@var[:sizes]).inject({}) do |arglist|
          var[var] = Twitter::Size.fetch_or_new(var)
          var
        end
      end
    end
  end
end;Fi[@(@)@*@+@,I" ;Fi![![	I"?data//activerecord_proj/active_record/nested_attributes.rb;TiÅI"˛              send(var, var.except(*UNASSIGNABLE_KEYS))
            else
              raise(ArgumentError, "STRING")
            end
          end
        end
      end
    end
    
    # Assigns the given attributes to the collection association.
    #;Fi[	@LiFI"‰              else
                warn("STRING")
              end
            end
          end
        end
      end
    end
    
    initializer("STRING") do |arglist|
      ActiveSupport.on_load(:active_record) do |arglist|;Fi[	I"Adata//activerecord_proj/active_record/relation/delegation.rb;Ti<I"È              arel.send(var, *var, &var)
            else
              super
            end
          end
        end
      end
    end
    
    module ClassMethods
      @@subclasses = ThreadSafe::Cache.new(:initial_capacity => 2);Fi[	@OirI"È                  else
                    var.merge(var)
                  end
                end
              end
            end
          end
        end
        
        def function(arglist)
          Thread.current["STRING"];Fi[	@]iïI"!                  var.destroy
                else
                  MUTEX.synchronize { |arglist| Logger.message("STRING") }
                end
              end
            end
          end
        end
        
        class LocalFile
          attr_reader(:path, :relative_path, :md5);Fi[	@¸i„I"Û            if var.include?(var) then
              debug { |arglist| "STRING" }
              throw(var)
            end
          end
        end
      end
    end
    
    def function(arglist)
      var.required_by.replace(var.required_by);Fi[	I"/data//bundler_proj/bundler/ruby_version.rb;Ti+I"◊            [:engine_version, engine_version, var.engine_version]
          else
            nil
          end
        end
      end
    end
  end
  
  class SystemRubyVersion < RubyVersion
    def function(arglist);Fi[	I"7data//bundler_proj/bundler/rubygems_integration.rb;Ti°I"’            @var = RubygemsIntegration::Legacy.new
          else
            @var = RubygemsIntegration::Ancient.new
          end
        end
      end
    end
  end
  
  class << self
    attr_reader(:rubygems);Fi[	@&i>I"≈                raise(var)
              end
              raise(var) if var.nil?
            end
          end
        end
      end
    end
    
    def function(arglist)
      if var.empty? then;Fi[	I"3data//bundler_proj/bundler/vendor/thor/util.rb;TiZI"%              ENV["STRING"]
            else
              File.expand_path("STRING") rescue File::ALT_SEPARATOR ? ("STRING") : ("STRING")
            end
          end
        end
      end
    end
    
    def self.thor_root(arglist)
      File.join(user_home, "STRING").gsub(/\\/, "STRING");Fi[	@vi.I"Î              matches_conditions_hash?(var)
            else
              @var.empty? ? (true) : (@var)
            end
          end
        end
      end
    end
    
    def function(arglist)
      conditions_empty? and @var.nil?.!;Fi[	@viI"Í              else
                var.kind_of?(Enumerable) ? (var.include?(var)) : ((var == var))
              end
            end
          end
        end
      end
    end
    
    def function(arglist)
      var, var = var.first;Fi[	@}igI"J                else
                  raise(ArgumentError, "STRING")
                end
              end
            end
          end
        end
      end
      
      # Require another file. This is identical to the standard require method,
      # with the exception that it sets the receiver as the "current" configuration;Fi[	@yiI"            else
              if inherited_data.respond_to?(:asset_cache_buster) then
                inherited_data.asset_cache_buster
              end
            end
          end
        end
      end
      
      def function(arglist)
        @var ||= [];Fi[	@êi3I"”                    raise("STRING")
                  end
                end
              end
            end
          end
        end
      end
      
      # Support for testing.
      def function(arglist);Fi[	I"6data//cucumber_prok/cucumber/formatter/unicode.rb;TiDI"Û              rescue Iconv::IllegalSequence => var
                STDERR.cucumber_puts("STRING")
                cucumber_puts(*var)
              end
            end
          end
        end
      end
      
      Kernel.extend(self)
      ;Fi[	@ÜigI"¶          unless var.nil? then
            @var = var
            break
          end
        end
      end
    end
  end
  
  def function(arglist)
    (@var or nil);Fi[	@åiI"        else
          if var.respond_to?(:photos) and var.photos.present? then
            I18n.t("STRING", :count => (var.photos.size), :author => (var.author_name))
          end
        end
      end
    end
  end
  
  def function(arglist)
    var[:width] ||= 516;Fi[	@èiI"”            stream_path(:max_time => (time_for_scroll(@var)))
          else
            raise("STRING")
          end
        end
      end
    end
  end
  
  def function(arglist)
    var.instance_of?(Reshare);Fi[	@·iI"‡              "STRING"
            else
              "STRING"
            end
          end
        end
      end
    end
    
    # Evaluates `text` in the context of the scope object, but
    # does not output the result.;Fi[	@óiJI"ü          else
            puts("STRING")
            rm(var)
          end
        end
      end
    end
  end
  
  def function(arglist)
    system("STRING");Fi[	@úi3I"          if ($found == 0) and blacklisted?(var).! then
            puts("STRING")
            GitHub.find_pull_requests(var) { |arglist| puts(var) }
          end
        end
      end
    end
  end
  
  def function(arglist)
    (return [] if (HOMEBREW_LIBRARY / "STRING").directory?;Fi[	@üiI"¥            raise("STRING".undent)
          else
            raise("STRING".undent)
          end
        end
      end
    end
  end
  
  def function(arglist)
    join(var.to_s);Fi[	@∂iI"|                end
              end
            end
          end
        end
      end
    end
  end
  
  private
  ;Fi[	I"%data//jekyll_proj/jekyll/site.rb;Ti~I"@              (pages << Page.new(self, self.source, var, var))
            else
              (static_files << StaticFile.new(self, self.source, var, var))
            end
          end
        end
      end
    end
    
    # Read all the files in <source>/<dir>/_posts and create a new Post
    # object with each one.;Fi[	@+i!I"ä                    target.__send__(var).push(base) unless Mongoid.using_identity_map?
                  else
                    target.do_or_do_not(metadata.inverse_setter(target), base)
                  end
                end
              end
            end
          end
          
          # Unbinds the base object and the inverse, caused by setting the
          # reference to nil.;Fi[	@àiÅI"Í          else
            var = ("STRING" + var.downcase)
            self.respond_to?(var).! ? (Error(("STRING" + var))) : (self.send(var, var))
          end
        end
      end
    end
  end
  
  def function(arglist)
    newobj;Fi[	@¢i}I"                else
                  @var.__send__(:method_missing, var, *var, &var)
                end
              end
            end
          end
        end
      end
      
      def function(arglist)
        @var.raise_unexpected_message_args_error(var, *var);Fi[@
@I"@var = var;FI"end;FI" ;FI"def function(arglist);Fi![![	I"Pdata//activerecord_proj/active_record/connection_adapters/postgresql/oid.rb;TiDI"0        
        class Array < Type
          attr_reader(:subtype)
          
          def function(arglist)
            @var = var
          end
          
          def function(arglist)
            if String.===(var) then
              ConnectionAdapters::PostgreSQLColumn.string_to_array(var, @var);Fi[	@iI"        ActiveRecord::Base.connected? ? (connection.uncached(&var)) : (yield)
      end
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      begin
        (var = ActiveRecord::Base.connection.query_cache_enabled;Fi[	@ÛiI"(      ACCESS_DENIED_ERROR = 1045
      
      delegate(:connection, :establish_connection, :to => (ActiveRecord::Base))
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        begin
          (establish_connection(configuration_without_database);Fi[	I"Mdata//activerecord_proj/active_record/tasks/postgresql_database_tasks.rb;TiI"J      DEFAULT_ENCODING = (ENV["STRING"] or "STRING")
      
      delegate(:connection, :establish_connection, :clear_active_connections!, :to => (ActiveRecord::Base))
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        begin
          (establish_master_connection unless var;Fi[	@ iI"Ó      var = new(var)
      var.inject(Bundler.default_gemfile, Bundler.default_lockfile)
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      begin
        (if Bundler.settings[:frozen] then;Fi[	I"5data//cucumber_prok/cucumber/formatter/pretty.rb;TirI"≤        @var = 6
        @var = 6
      end
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = nil
        @var = 4;Fi[	I"7data//cucumber_prok/cucumber/formatter/progress.rb;Ti0I"≤        progress(var)
        @var = var
      end
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = nil
      end;Fi[	@ŒiI"—      
      class StepInvoker
        include(Gherkin::Rubify)
        
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          # do nothing
        end;Fi[	I"<data//cucumber_prok/cucumber/wire_support/connection.rb;TiI"ƒ      end
      
      include(WireProtocol)
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        var = WirePacket.new(var, var)
        begin;Fi[	I"#data//homebrew_proj/caveats.rb;TiI"éclass Caveats
  attr_reader(:f)
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    var = []
    (var << f.caveats);Fi[	I"1data//omni_proj/omniauth/failure_endpoint.rb;TiI"‹    def self.call(arglist)
      new(var).call
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      raise_out! if (ENV["STRING"].to_s == "STRING")
      redirect_to_failure;Fi[	I"<data//paperclip_proj/paperclip/content_type_detector.rb;Ti
I"ƒ    EMPTY_TYPE = "STRING"
    
    SENSIBLE_DEFAULT = "STRING"
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      (if blank? then
        SENSIBLE_DEFAULT;Fi[	I"Idata//paperclip_proj/paperclip/file_command_content_type_detector.rb;TiI"”module Paperclip
  class FileCommandContentTypeDetector
    SENSIBLE_DEFAULT = "STRING"
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      type_from_file_command
    end;Fi[	@ÖiI"6module Paperclip
  class GeometryParser
    FORMAT = /\b(\d*)x?(\d*)\b(?:,(\d?))?([\>\<\#\@\%^!])?/i
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      if match then
        Geometry.new(:height => (@var), :width => (@var), :modifier => (@var), :orientation => (@var));Fi[	I"-data//paperclip_proj/paperclip/logger.rb;TiI"Œ    def function(arglist)
      @var ||= (options[:logger] or ::Logger.new(STDOUT))
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      options[:log]
    end;Fi[	@üiÑI"¢    @var = var
    @var = var if (@var > 0) and (@var < var)
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end;Fi[	@üiàI"ï  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
    @var = var;Fi[	@üiüI"ü      raise(("STRING" + var)) if var.!=("STRING")
    end
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end;Fi[	@üi£I"å  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end;Fi[	@üißI"å  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end;Fi[	@üi´I"å  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end;Fi[	@üiØI"å  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end;Fi[	@üi≥I"å  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end;Fi[	@üi∑I"ô  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    raise(("STRING" + var))
  end;Fi[	@ªiI"À  module Matchers
    class Be
      include(Spec::Matchers::Pretty)
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = var
        @var.!.!;Fi[	@ﬂi	I"˜  module Matchers
    class MatchArray
      include(Spec::Matchers::Pretty)
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = var
        @var = difference_between_arrays(@var, @var);Fi[	I"7data//rspec_proj/spec/mocks/message_expectation.rb;TiVI"≈      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        if @var then
          @var.clear;Fi[	I"Bdata//rspec_proj/spec/runner/formatter/base_text_formatter.rb;TiI"⁄          end
          @var = []
        end
        
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          (@var << ["STRING", var, var.location])
        end;Fi[@(@œ@–@—@“I"end;Fi [ [	@ci‰I"√          end
        end
      end
    end
    
    def function(arglist)
      "STRING"
    end
  end
  
  Fixtures = ActiveSupport::Deprecation::DeprecatedConstantProxy.new("STRING", "STRING");Fi[	I"(data//backup_proj/backup/package.rb;Ti)I"é      else
        chunk_suffixes.map { |arglist| "STRING" }
      end
    end
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	I"&data//bundler_proj/bundler/cli.rb;Ti·I"û    
    def function(arglist)
      "STRING" if `which more` rescue "STRING".empty?.!
    end
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@si_I"„        
        def function(arglist)
          Evaluator.new(configuration, condition, var).result
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      def function(arglist);Fi[	@	iI"£        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end
    end
  end;Fi[	@¿i2I"„      def function(arglist)
        var = eval_js("STRING")
        var.to_a[(1..-1)].map { |arglist| JsArg.new(var) } if var
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    class JsHook;Fi[	I"'data//devise_proj/devise/models.rb;Ti
I"À    class MissingAttribute < StandardError
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    def self.config(arglist);Fi[	I"Cdata//devise_proj/generators/active_record/devise_generator.rb;Ti$I"&        var = (var.size - 1)
        var = (var.split("STRING").map { |arglist| (("STRING" * var) + var) }.join("STRING") << "STRING")
        inject_into_class(model_path, var.last, var) if model_exists?
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I";data//devise_proj/generators/devise/views_generator.rb;TifI"ÿ      
      def function(arglist)
        view_directory(:markerb, target_path)
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    class ViewsGenerator < Rails::Generators::Base;Fi[	I"=data//devise_proj/generators/mongoid/devise_generator.rb;TiI"Œ        if model_exists? then
          inject_into_file(model_path, model_contents, :after => "STRING")
        end
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I"Adata//diaspora_proj/models/notifications/request_accepted.rb;TiI"•class Notifications::RequestAccepted < Notification
  def function(arglist)
    Jobs::Mail::RequestAcceptance
  end
  
  def function(arglist)
    "STRING"
  end
end;Fi[	@`i¬I"¥  
  def function(arglist)
    ((self.attributes.keys - Profile.protected_attributes.to_a) - ["STRING", "STRING", "STRING"])
  end
  
  def function(arglist)
    "STRING"
  end
end;Fi[	@bi%I"{  
  def function(arglist)
    super(var, MAX_CHARACTERS, var)
  end
  
  def function(arglist)
    "STRING"
  end
end;Fi[	@mi2I"ç      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I"4data//nokogiri_proj/nokogiri/xml/entity_decl.rb;TiI"°      
      def self.new(arglist)
        var.create_entity(var, *var)
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	@ªiTI"Ò      def function(arglist)
        var = "STRING"
        raise((var << ([:===, :==].include?(@var) ? ("STRING") : ("STRING"))))
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    class BePredicate < Be;Fi[	@ªi°I"ô      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:;Fi[	@ŒiNI"°      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:;Fi[	@ÿiSI"ò        @var = var
        @var = var
        self
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:;Fi[	@iI"ô      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:;Fi[	I"5data//rspec_proj/spec/matchers/simple_matcher.rb;Ti$I"˛      
      def function(arglist)
        (@var or @var.nil? ? (explanation) : ("STRING"))
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # simple_matcher makes it easy for you to create your own custom matchers;Fi[	I"5data//rspec_proj/spec/mocks/argument_matchers.rb;TiI"≤        
        def function(arglist)
          true
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      class RegexpMatcher;Fi[	@x	iEI"”          rescue NoMethodError => var
            return false
          end
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      class HashNotIncludingMatcher;Fi[	@x	iZI"À          rescue NoMethodError => var
            return false
          end
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      class DuckTypeMatcher;Fi[	@˛iãI"£        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end
    end
  end;Fi[	@isI"z    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	I"7data//whenever_proj/whenever/output_redirection.rb;Ti9I"Ö        else
          "STRING"
        end
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[@(@)@*I" ;FI"private;FI" ;Fi[[	I"Ndata//activerecord_proj/active_record/associations/has_one_association.rb;Ti8I"b            target.update_columns(reflection.foreign_key => (nil))
          else
            # do nothing
          end
        end
      end
      
      private
      
      # The reason that the save param for replace is false, if for create (not just build),
      # is because the setting of the foreign keys is actually handled by the scoping when;Fi[	@i0I"g          var = (Time.now - @var)
          connections.dup.each do |arglist|
            remove(var) if var.in_use? and ((var > var.last_use) and var.active?.!)
          end
        end
      end
      
      private
      
      # Acquire a connection by one of 1) immediately removing one
      # from the queue of available connections, 2) creating a new;Fi[	@DitI"˘              end
            else
              var
            end
          end
        end
        
        private
        
        HstorePair = (var = /"[^"\\]*(?:\\.[^"\\]*)*"/
        var = /(?:\\.|[^\s,])[^\s=,\\]*(?:\\.[^\s=,\\]*|=[^,>])*/;Fi[	@iLI"æ          var = var.where(table[primary_key].gt(var)).to_a
        else
          raise("STRING")
        end
      end
    end
    
    private
    
    def function(arglist)
      "STRING";Fi[	I".data//backup_proj/backup/database/riak.rb;Ti.I"˘          @var.compressor.compress_with do |arglist|
            run("STRING")
            FileUtils.rm_f(var)
          end
        end
      end
      
      private
      
      ##
      # Builds the full riak-admin string based on all attributes;Fi[	@)iwI"Ô            end
          else
            @var.scoped(:conditions => (conditions), :joins => (joins))
          end
        end
      end
      
      private
      
      def function(arglist)
        var = @var.map(&:conditions).compact;Fi[	@7i0I"å        end
      else
        instance_exec(*var, &var)
      end
    end
  end
  
  private
  
  def function(arglist)
    var = var.arity;Fi[	@:iOI"°            exit(1)
          end
          raise(var)
        end
      end
    end
    
    private
    
    def function(arglist)
      var = DEFAULT_ENCODING;Fi[	I",data//cucumber_prok/cucumber/runtime.rb;TiçI"+        FileUtils.mkdir_p(@var.dotcucumber) if File.directory?(@var.dotcucumber).!
        File.open(File.join(@var.dotcucumber, "STRING"), "STRING") do |arglist|
          var.write(JSON.pretty_generate(var))
        end
      end
    end
    
    private
    
    #:nodoc
    def function(arglist);Fi[	@Ii§I"Œ      else
        flash[:error] = I18n.t("STRING", :name => (search_query))
        redirect_to(:back)
      end
    end
  end
  
  private
  
  def function(arglist)
    var = var.map { |arglist| var.id };Fi[	@Oi8I"’        else
          redirect_to(edit_profile_path)
        end
      end
    end
  end
  
  private
  
  def function(arglist)
    unless (@var[:tag_string].nil? or (@var[:tag_string] == I18n.t("STRING"))) then;Fi[	@RiAI"`        var.html { |arglist| redirect_to(:back) }
        var.mobile { |arglist| redirect_to(stream_path) }
        var.json { |arglist| render(:nothing => (true), :status => 403) }
      end
    end
  end
  
  private
  
  def function(arglist)
    if (params[:status_message][:public] or (params[:status_message][:aspect_ids].first == "STRING")) then;Fi[	@Ui+I"        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  
  private
  
  def function(arglist)
    TagFollowing.user_is_following?(current_user, params[:name]);Fi[	@[iOI"        aspects.detect { |arglist| (var.id == var.id) }
      else
        AspectMembership.exists?(:contact_id => (self.id), :aspect_id => (var.id))
      end
    end
  end
  
  private
  
  def function(arglist)
    (errors[:base] << "STRING") if person_id and person.closed_account?;Fi[	@©iI"Ó      if var.children.empty? and var.skip_clean?(var).! then
        puts("STRING") if ARGV.verbose?
        var.rmdir
      end
    end
  end
  
  private
  
  # Set permissions for executables and non-executables
  def function(arglist);Fi[	@iivI"ı        var.children.each { |arglist| puts("STRING") }
      else
        raise("STRING")
      end
    end
  end
  
  private
  
  def function(arglist)
    ((var[(0..6)] == "STRING") or ((var[(0..7)] == "STRING") or (var[(0..5)] == "STRING")));Fi[	@liI"Ó        else
          ARGV.kegs.each { |arglist| PrettyListing.new(var) }
        end
      end
    end
  end
  
  private
  
  def function(arglist)
    var = HOMEBREW_PREFIX.children.select { |arglist| var.directory? }.map do |arglist|;Fi[	@rivI"ˇ      if FileTest.directory?(var) and (File.basename(var) == "STRING") then
        Find.prune
        FileUtil.rm_r(var, :force => (true))
      end
    end
  end
  
  private
  
  def function(arglist)
    var = var.sub(/^cvs:\/\//, "STRING").split(/:/);Fi[	@∂iI"≠            end
          end
        end
      end
    end
  end
  
  private
  
  OTOOL_RX = /\t(.*) \(compatibility version (\d+\.)*\d+, current version (\d+\.)*\d+\)/
  ;Fi[	@}i/I"≤          (var.pages << var)
        else
          var.pager = var
        end
      end
    end
    
    private
    
    def function(arglist)
      var = var.config["STRING"];Fi[	I"1data//jekyll_proj/jekyll/migrators/tumblr.rb;Ti&I"U        var[:name] = truncate_post_name(var[:name]) if (var[:name].size > 255)
        File.open("STRING", "STRING") do |arglist|
          var.puts(((var[:header].to_yaml + "STRING") + var[:content]))
        end
      end
    end
    
    private
    
    def self.truncate_post_name(arglist)
      var = var.match(/^(.+)\.(.+)$/).captures;Fi[	I":data//mongoid_proj/lib/mongoid/matchers/strategies.rb;Ti6I"            Matchers::And.new(var, var)
          else
            Default.new(extract_attribute(var, var))
          end
        end
      end
      
      private
      
      # Extract the attribute from the key, being smarter about dot notation.
      #;Fi[	I";data//mongoid_proj/lib/mongoid/validations/presence.rb;Ti!I"˜            end
          else
            var.errors.add(var, :blank, options) if not_present?(var)
          end
        end
      end
      
      private
      
      # Returns true if the relation is blank or the foreign key is blank.
      #;Fi[	I"=data//mongoid_proj/lib/mongoid/validations/uniqueness.rb;Ti,I"”            validate_embedded(var, var, var)
          else
            validate_root(var, var, var)
          end
        end
      end
      
      private
      
      # Add the error to the document.
      #;Fi[	@©iRI"È          end
        else
          Dir.glob("STRING").collect { |arglist| File.basename(var) }
        end
      end
    end
    
    private
    
    def self.scan_themes(arglist)
      var = Dir.glob("STRING").select do |arglist|;Fi[	I".data//rspec_proj/spec/matchers/matcher.rb;TiTI"Ô          define_method(var) do |arglist|
            var.call(*var)
            self
          end
        end
      end
      
      private
      
      # :nodoc:
      # Our home-grown instance_exec in ruby 1.8.6 results in any methods;Fi[@Æ@Ø@∞I"end;FI"end;FI"end;Fi[[	@î	i6I"ö      
      ##
      # Builds the full riak-admin string based on all attributes
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I"2data//backup_proj/backup/notifier/campfire.rb;Ti}I"Æ        
        ##
        # Returns the url for the specified room (in JSON format)
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I".data//backup_proj/backup/storage/rsync.rb;Ti{I"º      ##
      # RSync options
      # -z = Compresses the bytes that will be transferred to reduce bandwidth usage
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I"9data//backup_proj/backup/syncer/cloud/cloud_files.rb;Ti@I"≠        
        ##
        # This is the provider that Fog uses for the Cloud Files
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"0data//backup_proj/backup/syncer/cloud/s3.rb;Ti7I"≠        
        ##
        # This is the provider that Fog uses for the Cloud Files
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"2data//backup_proj/backup/syncer/rsync/base.rb;Ti)I"ß        
        ##
        # Returns Rsync syntax for invoking "archive" mode
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"@data//capistrano_proj/capistrano/recipes/deploy/scm/none.rb;TiI"™        end
        
        # log: There's no log, so it just echos from and to.
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"Gdata//capistrano_proj/capistrano/recipes/deploy/strategy/remote.rb;Ti)I"Î        
        # Returns the command which will write the identifier of the
        # revision being deployed to the REVISION file on each host.
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@	iI"Ä          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"2data//compass_proj/compass/installers/base.rb;Ti§I"	      end
      
      # Emits an HTML fragment that can be used to link to the compiled css files
      def function(arglist)
        "STRING"
      end
    end
  end
end
require("compass/installers/bare_installer")
require("compass/installers/manifest_installer");Fi[	@V	i&I"ü        inject_into_class(model_path, var.last, var) if model_exists?
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	@\	iI"j        end
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I"Bdata//mongoid_proj/lib/mongoid/contextual/aggregable/mongo.rb;TiìI"Æ        # @return [ String ] The reduce JS function.
        #
        # @since 3.0.0
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I";data//mongoid_proj/lib/mongoid/criterion/inspection.rb;TiI"ó      # @return [ String ] The inspection string.
      #
      # @since 1.0.0
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	@mi4I"ã        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end
module Rails
  module Generators;Fi[	I"=data//nokogiri_proj/nokogiri/html/element_description.rb;TiI"}      
      ###
      # Inspection information
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I"7data//nokogiri_proj/nokogiri/xml/attribute_decl.rb;TiI"ä      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I".data//nokogiri_proj/nokogiri/xml/cdata.rb;Ti
I"†    class CDATA < Nokogiri::XML::Text
      ###
      # Get the name of this CDATA node
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I"5data//nokogiri_proj/nokogiri/xml/element_decl.rb;TiI"ä      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	@h	iI"~        var.create_entity(var, *var)
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I":data//nokogiri_proj/nokogiri/xml/pp/character_data.rb;TiI"        end
        
        # :nodoc:
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@˛içI"Ä          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"Kdata//state_machine_proj/state_machine/integrations/sequel/versions.rb;TiCI"ï        def function(arglist)
          "STRING"
        end
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@É	i;I"j        end
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[@(@)I"var;FI"end;FI" ;FI"def function(arglist);Fi[[	I"Ldata//activerecord_proj/active_record/associations/association_scope.rb;TiUI"            var = eval_scope(var.klass, var)
            var.includes!(var.includes_values)
            var.where_values += var.where_values
          end
        end
        var
      end
      
      def function(arglist)
        reflection.name
      end;Fi[	I"Wdata//activerecord_proj/active_record/associations/has_many_through_association.rb;TidI"e            (var.send(var.name) << build_through_record(var))
          else
            var.send("STRING", build_through_record(var)) if (var.macro == :has_one)
          end
        end
        var
      end
      
      def function(arglist)
        ((through_reflection.macro == :belongs_to) and owner[through_reflection.foreign_key].blank?).!
      end;Fi[	I"Jdata//activerecord_proj/active_record/associations/join_dependency.rb;Ti¥I"            set_target_and_inverse(var, var, var)
          else
            raise(ConfigurationError, "STRING")
          end
        end
        var
      end
      
      def function(arglist)
        var = var.association(var.reflection.name)
        var.target = var;Fi[	I"Fdata//activerecord_proj/active_record/associations/join_helper.rb;TiI"ß          (var << alias_tracker.aliased_table_for(table_name_for(var), table_alias_for(var, var.!=(self.reflection))))
          if (var.source_macro == :has_and_belongs_to_many) then
            (var << alias_tracker.aliased_table_for((var.source_reflection or var).join_table, table_alias_for(var, true)))
          end
        end
        var
      end
      
      def function(arglist)
        var.table_name
      end;Fi[	@iQI"6        if var.respond_to?(:unlock!) and @var[:sources].include?(var.name) then
          var.unlock!
          var = true
        end
      end
      var
    end
    
    def function(arglist)
      (@var + @var).each do |arglist|
        var.source = @var.find { |arglist| (var.source == var) } if var.source;Fi[	@iÉI"9        next unless var.respond_to?(:unlock!)
        unless var.any? { |arglist| (var.source == var) } then
          var.unlock! if var.empty?.! and var.any? { |arglist| (var.source == var) }
        end
      end
      var
    end
    
    def function(arglist)
      var and (var.source == var.source)
    end;Fi[	@iûI"          if (var or (var == generic(Gem::Platform.local))) then
            (var << DepProxy.new(var, var))
          end
        end
      end
      var
    end
    
    def function(arglist)
      @var.sort_by do |arglist|
        [var.is_a?(Source::Rubygems) ? (1) : (0), var.to_s];Fi[	@˘i4I"q        var.search(var, var).each do |arglist|
          (var << var) unless var.include?([var.name, var.version, var.platform])
          (var << [var.name, var.version, var.platform])
        end
      end
      var
    end
    
    def function(arglist)
      case var
      when Gem::Specification, RemoteSpecification, LazySpecification, EndpointSpecification then;Fi[	@fiáI".            raise(PathError, "STRING")
          else
            raise(PathError, "STRING")
          end
        end
        var
      end
      
      def function(arglist)
        if path.to_s.match(/^#{Regexp.escape(Bundler.root.to_s)}/) then
          return path.relative_path_from(Bundler.root);Fi[	I">data//compass_proj/compass/configuration/serialization.rb;Ti;I"&            (var << var)
          else
            (var << serialize_property(var, var)) unless var.nil?
          end
        end
        var
      end
      
      def function(arglist)
        if var.respond_to?(:serialize_to_config) then
          (var.serialize_to_config(var) + "STRING");Fi[	I"Mdata//compass_proj/compass/sass_extensions/functions/gradient_support.rb;Ti’I"È          Sass::Script::Number.new(50, ["STRING"])
        else
          var
        end
      end
      var
    end
    
    def function(arglist)
      Sass::Script::List.new(var.map do |arglist|
        if ColorStop.===(var) then;Fi[	@:i\I"ﬁ        if ENCODING_PATTERN.=~(var) then
          var = $1
          break
        end
      end
      var
    end
    
    def function(arglist)
      if var.respond_to?(:encode) then
        var.encode(DEFAULT_ENCODING);Fi[	I"1data//cucumber_prok/cucumber/formatter/io.rb;TiI"ˇ          unless var.closed? then
            var.flush
            var.close
          end
        end
        var
      end
      
      def function(arglist)
        raise("STRING") unless String.===(var)
        raise("STRING") if File.directory?(var);Fi[	I"1data//diaspora_proj/models/status_message.rb;TiVI"        var ? (ERB::Util.h(var.name)) : (ERB::Util.h($~[1]))
      else
        var ? (person_link(var, :class => "STRING")) : (ERB::Util.h($~[1]))
      end
    end
    var
  end
  
  def function(arglist)
    if self.persisted? then
      create_mentions if self.mentions.empty?;Fi[	I"#data//haml_proj/haml/parser.rb;Ti—I"∫          var["STRING"] = var
        else
          # do nothing
        end
      end
      var
    end
    
    def function(arglist)
      var = {}
      var = StringScanner.new(var);Fi[	I"'data//homebrew_proj/cmd/missing.rb;TiI"      unless var.empty? then
        yield(var.name, var) if block_given?
        var[var.name] = var
      end
    end
    var
  end
  
  def function(arglist)
    return unless HOMEBREW_CELLAR.exist?
    var = ARGV.named.empty? ? (installed_brews) : (ARGV.formulae);Fi[	I"/data//nokogiri_proj/nokogiri/css/parser.rb;TiÛI"Ú            var = Node.new(:AN_PLUS_B, var)
          else
            var = var
          end
        end
        var
      end
      
      def function(arglist)
        if (var[1] == "STRING") then
          var = Node.new(:AN_PLUS_B, var);Fi[	@<
iI"¸            var = Node.new(:AN_PLUS_B, var)
          else
            raise(Racc::ParseError, "STRING")
          end
        end
        var
      end
      
      def function(arglist)
        if (var[1] == "STRING") then
          (var << "STRING");Fi[	I",data//nokogiri_proj/nokogiri/version.rb;Ti8I"Ê        if jruby? then
          var["STRING"] = Nokogiri::XERCES_VERSION
          var["STRING"] = Nokogiri::NEKO_VERSION
        end
      end
      var
    end
    
    def function(arglist)
      begin
        require("psych");Fi[	I"$data//redmine_proj/redcloth3.rb;TiI"µ          end
        end
        var
      end
    end
    var
  end
  
  def function(arglist)
    var.gsub!(/<redpre#(\d+)>/) { |arglist| @var[$1.to_i] } unless @var.empty?
  end;Fi[	@éi2I"÷                end
              end
            end
          end
        end
        var
      end
      
      def function(arglist)
        var.SetFontStyle("STRING", 8)
        var.SetFillColor(230, 230, 230);Fi[	I"0data//redmine_proj/redmine/helpers/gantt.rb;TiúI",        if relations[var.id] then
          relations[var.id].each do |arglist|
            ((var[var.relation_type] ||= []) << var.issue_to_id)
          end
        end
        var
      end
      
      def function(arglist)
        var = "STRING"
        if var[:bar_start] and var[:bar_end] then;Fi[	I",data//rspec_proj/spec/runner/options.rb;TiÈI"”            end
          else
            File.file?(var) ? ((var << var)) : (raise("STRING"))
          end
        end
        var
      end
      
      def function(arglist)
        (@var == true)
      end;Fi[	I")data//SiriProxy_proj/siri_objects.rb;Ti)I"Ê        end
      else
        var["STRING"][var] = properties[var].to_hash rescue properties[var]
      end
    end
    var
  end
  
  def function(arglist)
    self.extend(SiriRootObject)
    self.ref_id = (var or random_ref_id);Fi[@(@œ@–I"	true;FI"end;FI" ;Fi[[	I"Ydata//activerecord_proj/active_record/associations/builder/collection_association.rb;TiI"‡      var = super
      CALLBACKS.each { |arglist| define_callback(var) }
      var
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      [:finder_sql, :counter_sql].each do |arglist|;Fi[	I"Wdata//activerecord_proj/active_record/associations/builder/singular_association.rb;Ti	I"˘  class SingularAssociation < Association
    def function(arglist)
      (super + [:remote, :dependent, :counter_cache, :primary_key, :inverse_of])
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      super;Fi[	I"Vdata//activerecord_proj/active_record/connection_adapters/abstract/transaction.rb;TiI"       
      def function(arglist)
        RealTransaction.new(connection, self, var)
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        false;Fi[	@ÎiâI"!      # Returns true, since this connection adapter supports migrations.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      
      # Returns true, since this connection adapter supports savepoints.
      def function(arglist);Fi[	@ÎiíI"H      # Returns true, since this connection adapter supports savepoints.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      
      # Technically MySQL allows to create indexes with the sort order syntax
      # but at the moment (5.5) it doesn't yet implement them;Fi[	I"Pdata//activerecord_proj/active_record/connection_adapters/mysql2_adapter.rb;Ti$I"˛        super
        @var = BindSubstitution.new(self)
        configure_connection
      end
      
      def function(arglist)
        true
      end
      
      # HELPER METHODS ===========================================
      def function(arglist);Fi[	@iÜI"∫      # Returns true.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      
      # Returns true
      def function(arglist);Fi[	@i™I"≥      # Returns true
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      
      #:nodoc:
      def function(arglist);Fi[	@êiI"ã    
    def function(arglist)
      0
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      false;Fi[	@si)I"À        
        def function(arglist)
          @var = true
        end
        
        def function(arglist)
          true
        end
        
        def function(arglist)
          command.inspect;Fi[	@i&I"      
      def function(arglist)
        (top_level.environment == :development)
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        if var = top_level.project_path and var = top_level.sass_dir then;Fi[	@iãI"“      
      def function(arglist)
        http_root_relative(top_level.http_javascripts_dir)
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        :scss;Fi[	@|iI"†    
    def function(arglist)
      (var == "STRING")
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      css2_value;Fi[	@+
i@I"7    
    def function(arglist)
      GRADIENT_ASPECTS.include?(var)
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      var.is_a?(Sass::Script::Number) and ((var.numerator_units.size == 1) and ((var.numerator_units.first == "STRING") and var.denominator_units.empty?));Fi[	I"4data//cucumber_prok/cucumber/formatter/debug.rb;TiI"ÿ      def function(arglist)
        @var = var
        @var = 0
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        @var = (@var - 2) if var.to_s =~ /^after/;Fi[	@°i$I"¨      
      def function(arglist)
        :invalid
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        :inactive;Fi[	I"3data//devise_proj/devise/models/validatable.rb;Ti'I"B      # or confirmation are being set somewhere.
      def function(arglist)
        (persisted?.! or (password.nil?.! or password_confirmation.nil?.!))
      end
      
      def function(arglist)
        true
      end
      
      module ClassMethods
        Devise::Models.config(self, :email_regexp, :password_length);Fi[	I"8data//devise_proj/devise/strategies/rememberable.rb;Ti"I"3        if var.respond_to?(:extend_remember_period=) then
          var.extend_remember_period = mapping.to.extend_remember_period
        end
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        mapping.to.rememberable_options.fetch(:key, "STRING");Fi[	I"(data//diaspora_proj/models/photo.rb;Ti~I"“  
  def function(arglist)
    Resque.enqueue(Jobs::ProcessPhoto, self.id)
  end
  
  def function(arglist)
    true
  end
  
  scope(:on_statuses, lambda { |arglist| where(:status_message_guid => (var)) })
end;Fi[	I"7data//diaspora_proj/models/relayable_retraction.rb;TiI"ù  
  def function(arglist)
    self.sender_handle
  end
  
  def function(arglist)
    true
  end
  
  def function(arglist)
    Rails.logger.debug("STRING");Fi[	@ÖiI"í    
    def function(arglist)
      0
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      # do nothing;Fi[	@x	iI"÷      class AnyArgMatcher
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          true
        end
        
        def function(arglist)
          "STRING";Fi[@@I"end;FI"end;FI"end;FI"end;Fi[[	@DiäI"Ü          when "STRING" then
            var
          else
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	@Œ	i~I"•        ##
        # Returns the url for the specified room (in JSON format)
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@‘	iAI"§        ##
        # This is the provider that Fog uses for the Cloud Files
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@◊	i8I"§        ##
        # This is the provider that Fog uses for the Cloud Files
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@⁄	i*I"û        ##
        # Returns Rsync syntax for invoking "archive" mode
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"?data//capistrano_proj/capistrano/recipes/deploy/scm/cvs.rb;TiÄI"ï          var = var.split(/\//)
          var = var.pop
          var = var.join("STRING")
          "STRING"
        end
      end
    end
  end
end;Fi[	@ÉiVI"É          when false then
            nil
          else
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	@›	iI"û        
        # log: There's no log, so it just echos from and to.
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@‡	i*I"‚        # Returns the command which will write the identifier of the
        # revision being deployed to the REVISION file on each host.
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@	iI"r        end
        
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"9data//compass_proj/compass/configuration/comments.rb;TiI"ß        if (top_level.preferred_syntax == :scss) and top_level.sass_dir then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[	@·iI"›            when "STRING" then
              "STRING"
            else
              "STRING"
            end
          end
        end
      end
    end
    
    # Evaluates `text` in the context of the scope object, but;Fi[	I"-data//jekyll_proj/jekyll/tags/include.rb;TiI"·          var = Liquid::Template.parse(var)
          var.stack { |arglist| var.render(var) }
        else
          "STRING"
        end
      end
    end
  end
end
Liquid::Template.register_tag("STRING", Jekyll::IncludeTag);Fi[	@¬i&I"‘          def function(arglist)
            var = parent.atomic_position
            var = document.new_record? ? ("STRING") : ("STRING")
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	@≈i%I"¨          # @since 2.1.0
          def function(arglist)
            var = parent.atomic_position
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	@Ï	iîI"~        #
        # @since 3.0.0
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I"@data//mongoid_proj/lib/mongoid/errors/document_not_found.rb;TiTI"v        when Hash then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[	@
iI"x        
        # :nodoc:
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@˛iéI"r        end
        
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@
iDI"|          "STRING"
        end
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	I">data//twitter_proj/twitter/request/multipart_with_file.rb;Ti!I"{        when /\.png$/i then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[@(@œ@–I"@var = var;FI"end;FI" ;Fi[[	@iI"ﬁ      def function(arglist)
        ActiveRecord::Base.connected? ? (connection.uncached(&var)) : (yield)
      end
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      begin;Fi[	I"9data//bundler_proj/bundler/endpoint_specification.rb;Ti@I"œ      if @var and File.exists?(local_specification_path) then
        eval(File.read(local_specification_path))
      end
    end
    
    def function(arglist)
      @var = var
    end
    
    private
    ;Fi[	@ i
I"‡    def self.inject(arglist)
      var = new(var)
      var.inject(Bundler.default_gemfile, Bundler.default_lockfile)
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      begin;Fi[	@!iâI"Ì        
        def function(arglist)
          (@var or @var)
        end
        
        def function(arglist)
          @var = var
        end
        
        # Returns true if one or more steps failed
        def function(arglist);Fi[	@
	iqI"¥        @var.flush
        @var = 6
        @var = 6
      end
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = nil;Fi[	@	i/I"ƒ      def function(arglist)
        progress(var)
        @var = var
      end
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = nil;Fi[	@	iI"«    
    def self.call(arglist)
      new(var).call
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      raise_out! if (ENV["STRING"].to_s == "STRING");Fi[	@#	iI"”    #:nodoc:
    def function(arglist)
      @var ||= (options[:logger] or ::Logger.new(STDOUT))
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      options[:log];Fi[	@êi/I"ê    
    def self.[](arglist)
      new(var)
    end
    
    def function(arglist)
      @var = var
    end
    
    attr_reader :function
    ;Fi[	@üiÉI"¥  def function(arglist)
    @var = var
    @var = var if (@var > 0) and (@var < var)
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var;Fi[	@üiáI"â  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var;Fi[	@üiûI"¢    else
      raise(("STRING" + var)) if var.!=("STRING")
    end
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var;Fi[	@üi¢I"â  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var;Fi[	@üi¶I"â  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var;Fi[	@üi™I"â  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var;Fi[	@üiÆI"â  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var;Fi[	@üi≤I"â  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var;Fi[	@üi∂I"ñ  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    raise(("STRING" + var));Fi[	@<	iUI"‘      # instance and not the class.
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        if @var then;Fi[	@?	iI"Â            @var = var
          end
          @var = []
        end
        
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          (@var << ["STRING", var, var.location]);Fi[@(I"var;FI"end;FI"end;FI"end;FI"end;Fi[[	@milI"ˇ          var.includes!((var[:includes] or var[:includes]))
          if options[:as] then
            var.where!(klass.table_name => ({ reflection.type => (model.base_class.sti_name) }))
          end
          var
        end
      end
    end
  end
end;Fi[	@¸i2I"Õ            end
            var.order!(reflection_scope.values[:order])
            var.references!(reflection_scope.values[:references])
          end
          var
        end
      end
    end
  end
end;Fi[	@iHI"‚          if var.query then
            var = Hash[var.query.split("STRING").map { |arglist| var.split("STRING") }].symbolize_keys
            var.merge!(var)
          end
          var
        end
      end
    end
  end
end;Fi[	@Ûi}I"ˇ        var.concat(["STRING", configuration["STRING"]]) if configuration["STRING"]
        configuration.slice("STRING", "STRING", "STRING").each do |arglist|
          var.concat(["STRING", var]) if var
        end
        var
      end
    end
  end
end;Fi[	@¸ibI"v              (var << "STRING")
            end
          end
        end
        var
      end
    end
  end
end;Fi[	I":data//bundler_proj/bundler/vendor/thor/shell/basic.rb;TiRI"ª          var = var.include?(var) ? (var) : (nil)
          var = var.map(&:inspect).join("STRING")
          say("STRING") unless var)
        end
        var
      end
    end
  end
end;Fi[	@)i∞I"¯        var = []
        var.each do |arglist|
          (var << (var.empty? ? (var) : ({ var => (clean_joins(var)) })))
        end
        var
      end
    end
  end
end
ActiveRecord::Base.class_eval { |arglist| include(CanCan::ModelAdditions) };Fi[	I"Cdata//cancan_proj/cancan/model_adapters/data_mapper_adapter.rb;Ti"I"È        end
        var.each do |arglist|
          var = (var - @var.all(:conditions => (var.conditions)))
        end
        var
      end
    end
  end
end
DataMapper::Model.append_extensions(CanCan::ModelAdditions::ClassMethods);Fi[	@}i-I"            else
              # do nothing
            end
          end
          var
        end
      end
    end
  end
end;Fi[	@Öi I"'              (var << { :raw => (var.send("STRING") rescue nil), :value => (var.send("STRING") rescue nil), :default => (var.send("STRING") rescue nil), :resolved => (var.send(var)) })
            end
            var[var] = var
          end
          var
        end
      end
    end
  end
end;Fi[	@$i|I"è        var.each do |arglist|
          var ||= "STRING"
          var = var.gsub(var, var)
        end
        var
      end
    end
  end
end;Fi[	@DiI"Æ              else
                var.push(Factory.build(klass, var))
              end
            end
            var
          end
        end
      end
    end
  end
end;Fi[	I"2data//nokogiri_proj/nokogiri/css/tokenizer.rb;TiuI"è          end
        else
          raise(ScanError, (("STRING" + state.to_s) + "STRING"))
        end
        var
      end
    end
  end
end;Fi[	@¶i&I"ì                [yield(var), var.id]
              end
            end.compact)
          end
          var
        end
      end
    end
  end
end;Ti[	@©iI"¨          if jsonp.present? then
            var = "STRING"
            response.content_type = "STRING"
          end
          var
        end
      end
    end
  end
end;Fi[	@îioI"&              var = @var[$1.to_i]
              if var.match(/<code\s+class="(\w+)">\s?(.+)/m) then
                var = ("STRING" + Redmine::SyntaxHighlighting.highlight_by_language($2, $1))
              end
              var
            end
          end
        end
      end
    end
  end;Fi[	I"-data//rspec_proj/spec/matchers/pretty.rb;Ti"I"°          else
            (var < (var.length - 1)) ? ((var << "STRING")) : ((var << "STRING"))
          end
        end
        var
      end
    end
  end
end;Fi[	@∞i"I"          if ((var == :state) or ((var == :event) and var)) and var.send("STRING").! then
            var = read(var, :state)
            var.changes[self.attribute.to_s] = [(var == :event) ? (var) : (var), var]
          end
          var
        end
      end
    end
  end
end;Fi[I"#   # => [;FI"A#   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;TI";#   #       #<Pet id: 2, name: "Spook", person_id: 1>,;TI">#   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;TI"#   #    ];TI"#;Ti[[	I"Kdata//activerecord_proj/active_record/associations/collection_proxy.rb;Ti*I"J      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.select(:name)
      #   # => [;Ti[	@&icI"≈      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.find(1) # => #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>
      #   person.pets.find(4) # => ActiveRecord::RecordNotFound: Couldn't find Pet with id=4;Ti[	@&i|I"n      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.first # => #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>
      #;Ti[	@&iöI"k      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.last # => #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #;Ti[	@&i	I"ì      #
      #   person.id # => 1
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.concat([Pet.new(name: 'Brain'), Pet.new(name: 'Benny')])
      #   person.pets.size # => 5;Ti[	@&i>I"[      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [;Ti[	@&iEI"h      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.size # => 0
      #   person.pets      # => [];Ti[	@&i_I"[      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [;Ti[	@&ifI"k      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1, 2, 3)
      #   # => ActiveRecord::RecordNotFound;Ti[	@&ixI"[      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [;Ti[	@&iI"k      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1, 2, 3)
      #   # => ActiveRecord::RecordNotFound;Ti[	@&iïI"S      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.destroy_all
      #;Ti[	@&iµI"î      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>];Ti[	@&i—I"q      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete(Pet.find(1), Pet.find(3))
      #   # => [;Ti[	@&iÌI"î      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>];Ti[	@&i	I"å      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete("1")
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>];Ti[	@&i%I"ï      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.destroy(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>];Ti[	@&iïI"¸      #   # executes something like SELECT COUNT(*) FROM "pets" WHERE "pets"."person_id" = 1
      #
      #   person.pets # This will execute a SELECT * FROM query
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.size # => 3
      #   # Because the collection is already loaded, this will behave like;Ti[@(@)@ÁI"protected;FI" ;FI"def function(arglist);Fi[[	I"=data//activemodel_proj/active_model/attribute_methods.rb;Ti~I"Ê        else
          match_attribute_method?(var.to_s).nil?.!
        end
      end
    end
    
    protected
    
    def function(arglist)
      respond_to_without_attributes?(:attributes) and attributes.include?(var)
    end;Fi[	I"Ddata//activemodel_proj/active_model/validations/numericality.rb;Ti0I"              var.errors.add(var, var, filtered_options(var).merge(:count => (var)))
            end)
          end
        end
      end
      
      protected
      
      def function(arglist)
        case var
        when /\A0[xX]/ then;Fi[	@
iSI"´          end
        else
          # do nothing
        end
      end
      
      protected
      
      def function(arglist)
        var = []
        var = var.parent;Fi[	@æi)I"Û            return if method_defined?(var)
            super
          end
        end
      end
      
      protected
      
      def function(arglist)
        if @var.respond_to?(var) then
          self.class.delegate_to_scoped_klass(var);Fi[	I"Edata//activerecord_proj/active_record/relation/finder_methods.rb;TiÆI"ı        connection.select_value(var, "STRING", var.bind_values))
      rescue ThrowResult
        false
      end
    end
    
    protected
    
    def function(arglist)
      begin
        (var = construct_join_dependency_for_association_find;Fi[	@iI"‡        end
        self.stored_attributes[var] ||= []
        self.stored_attributes[var] |= var
      end
    end
    
    protected
    
    def function(arglist)
      var = initialize_store_attribute(var)
      var[var];Fi[	I">data//bundler_proj/bundler/vendor/thor/parser/argument.rb;Ti*I"…        default.empty?.!
      else
        default
      end
    end
    
    protected
    
    def function(arglist)
      if required? and default.nil?.! then
        raise(ArgumentError, "STRING");Fi[	I"4data//cancan_proj/cancan/controller_resource.rb;TiEI"            true if [var[:only]].flatten.include?(@var[:action].to_sym)
          end
        end
      end
    end
    
    protected
    
    def function(arglist)
      if parent?.! and new_actions.include?(@var[:action].to_sym) then
        build_resource;Fi[	I">data//capistrano_proj/capistrano/configuration/servers.rb;Ti?I"™          var = var.select { |arglist| var.all? { |arglist| (var.options[var] == var) } }
          var = var.reject { |arglist| var.any? { |arglist| (var.options[var] == var) } }
          var[:skip_hostfilter] ? (var.uniq) : (filter_server_list(var.uniq))
        end
      end
      
      protected
      
      def function(arglist)
        return var unless (ENV["STRING"] or ENV["STRING"])
        if ENV["STRING"] then;Fi[	I"6data//compass_proj/compass/exec/sub_command_ui.rb;TiI"Œ          ::Compass::Exec::Helpers.report_error(var, (@var or {}))
        end
        return 1
      end
    end
    
    protected
    
    def function(arglist)
      begin
        ($command = args.shift;Fi[	I"Bdata//compass_proj/compass/sass_extensions/functions/lists.rb;Ti]I"™      else
        var
      end
    end
  end
  
  protected
  
  def function(arglist)
    raise(ArgumentError.new("STRING")) unless var.is_a?(Sass::Script::List)
  end;Fi[	I"0data//devise_proj/devise/models/lockable.rb;Ti`I"≈          else
            super
          end
        end
      end
      
      protected
      
      def function(arglist)
        (self.failed_attempts > self.class.maximum_attempts)
      end;Fi[	I"Adata//mongoid_proj/lib/mongoid/multi_parameter_attributes.rb;TiEI"Í        super(var, var, var)
      else
        super
      end
    end
    
    protected
    
    def function(arglist)
      return nil if var.all? { |arglist| var.nil? }
      var = var.collect { |arglist| var.nil? ? (1) : (var) };Fi[	I"5data//rspec_proj/spec/example/example_matcher.rb;TiI"ï      def function(arglist)
        var.any? do |arglist|
          (matches_literal_example?(var) or matches_example_not_considering_modules?(var))
        end
      end
      
      protected
      
      def function(arglist)
        var =~ /(^#{example_group_regex}STRING#{example_regexp}STRING#{example_group_regex}STRING#{example_group_with_before_all_regexp}STRING#{example_regexp}STRING/
      end;Fi[	@i-I"¶          puts("STRING")
          exit(0)
        end
      end
    end
    
    protected
    
    def function(arglist)
      File.expand_path(@var[:file])
    end;Fi[	I")data//whenever_proj/whenever/cron.rb;Ti6I"a          parse_as_string
        else
          parse_time
        end
      end
      
      protected
      
      def function(arglist)
        var = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
        @var.is_a?(String) and var.any? { |arglist| @var.downcase.index(var) };Fi[	@É	iI"Ô          "STRING"
        else
          "STRING"
        end
      end
      
      protected
      
      def function(arglist)
        return unless @var.has_key?(:standard)
        @var[:standard].nil? ? ("STRING") : (@var[:standard]);Fi[@(@)@ÁI"def function(arglist);FI""STRING";FI"end;Fi[[	@Îi7I"Ò        else
          var.table(var.source)
          var.wheres = var.constraints
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      # SCHEMA STATEMENTS ========================================;Fi[	@ci„I"≤            fixtures[var] = ActiveRecord::Fixture.new(var, model_class)
          end
        end
      end
    end
    
    def function(arglist)
      "STRING"
    end
  end
  ;Fi[	@ÛikI"—          else
            defined? Mysql ? (Mysql::Error) : (StandardError)
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@G	i(I"°        [basename]
      else
        chunk_suffixes.map { |arglist| "STRING" }
      end
    end
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@òi)I"            unless var =~ /\A#{Sass::SCSS::RX::IDENT}STRING/ then
              raise(Sass::SyntaxError, "STRING")
            end
          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        # The on-the-disk filename of the sprite;Fi[	@$ipI"Ã        var.inject({}) do |arglist|
          var[delimited(var)] = var
          var
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@\	iI"Ê      def function(arglist)
        if model_exists? then
          inject_into_file(model_path, model_contents, :after => "STRING")
        end
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end;Fi[	@vivI")          else
            (var.value.first + "STRING")
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      { "STRING" => "STRING", "STRING" => "STRING", "STRING" => "STRING", "STRING" => "STRING", "STRING" => "STRING" }.each do |arglist|;Fi[	@ëi1I"            (var << accepted_types_and_failures)
            (var << "STRING") if @var.present? and @var.present?
            (var << rejected_types_and_failures)
          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected;Fi[	@ëiGI"             "STRING".tap do |arglist|
              @var.any? ? ((var << "STRING")) : ((var << "STRING"))
            end
          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist);Fi[	@≤iπI"Ø            end
          end
          var = (var + 1))
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@ªimI"          return @var = var.__send__(present_tense_predicate, *@var, &@var)
        rescue NameError
          raise(var)
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@ÿi:I"∆          else
            return "STRING" if (@var == :at_least)
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@x	iDI"¡            true)
          rescue NoMethodError => var
            return false
          end
        end
        
        def function(arglist)
          "STRING"
        end
      end
      ;Fi[	@x	iYI"¡            true)
          rescue NoMethodError => var
            return false
          end
        end
        
        def function(arglist)
          "STRING"
        end
      end
      ;Fi[	@¢i∑I"È          else
            target_metaclass.protected_method_defined?(var) ? ("STRING") : ("STRING")
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@É	i8I"î          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end;Fi[@(@œI"J# Turn the object from the ruby type we deal with to a Mongo friendly;FI"# type.;FI"#;FI"$# @example Mongoize the object.;Fi[[	I"7data//mongoid_proj/lib/mongoid/extensions/array.rb;TiPI"      # @since 3.0.0
      def function(arglist)
        (first.resizable? or (size > 1))
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #;Fi[	@´iäI"-          else
            var.blank? ? ([]) : (var.convert(Array(var)))
          end
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Array.mongoize([ 1, 2, 3 ])
        #;Fi[	I"=data//mongoid_proj/lib/mongoid/extensions/big_decimal.rb;TiI"¯      # @since 3.0.3
      def function(arglist)
        to_f
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #;Fi[	@ŒiI"      # @since 3.0.0
      def function(arglist)
        ::Time.configured.local(year, month, day)
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   date.mongoize
      #;Fi[	@Œi.I"C        # @since 3.0.0
        def function(arglist)
          ::Date.new(var.year, var.month, var.day) if var
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Date.mongoize("2012-1-1")
        #;Fi[	I";data//mongoid_proj/lib/mongoid/extensions/date_time.rb;TiI"          var = to_time
          var.respond_to?(:getlocal) ? (var.getlocal) : (var)
        end
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   date_time.mongoize
      #;Fi[	@∑i4I"@        # @since 3.0.0
        def function(arglist)
          ::Time.demongoize(var).try(:to_datetime)
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   DateTime.mongoize("2012-1-1")
        #;Fi[	I"6data//mongoid_proj/lib/mongoid/extensions/hash.rb;TilI"Ê          var = var
        end
        var
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #;Fi[	I"8data//mongoid_proj/lib/mongoid/extensions/object.rb;Ti~I"„        else
          false
        end
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #;Fi[	@øi˘I"        # @since 3.0.0
        def function(arglist)
          var
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Object.mongoize("123.11")
        #;Fi[	I"7data//mongoid_proj/lib/mongoid/extensions/range.rb;TiI"˜      # @since 3.0.0
      def function(arglist)
        to_a
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   range.mongoize
      #;Fi[	@ƒi:I"M        # @since 3.0.0
        def function(arglist)
          var.nil? ? (nil) : (::Range.new(var["STRING"], var["STRING"]))
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Range.mongoize(1..3)
        #;Fi[	I"5data//mongoid_proj/lib/mongoid/extensions/set.rb;Ti"I"        # @since 3.0.0
        def function(arglist)
          ::Set.new(var)
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Set.mongoize(1..3)
        #;Fi[	I"8data//mongoid_proj/lib/mongoid/extensions/string.rb;Ti±I""        # @since 3.0.0
        def function(arglist)
          var.try(:to_s)
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   String.mongoize("123.11")
        #;Fi[	I"8data//mongoid_proj/lib/mongoid/extensions/symbol.rb;Ti!I"$        # @since 3.0.0
        def function(arglist)
          var.try(:to_sym)
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Symbol.mongoize("123.11")
        #;Fi[	@—i4I"&            var = var.in_time_zone(Mongoid.time_zone)
          end
          var
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Time.mongoize("2012-1-1")
        #;Fi[	I"@data//mongoid_proj/lib/mongoid/extensions/time_with_zone.rb;Ti#I"K        def function(arglist)
          return nil if var.blank?
          ::Time.demongoize(var).in_time_zone
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   TimeWithZone.mongoize("2012-1-1")
        #;Fi[@
@I"	true;FI"end;FI" ;FI"def function(arglist);Fi[[	@W
iI"      CALLBACKS.each { |arglist| define_callback(var) }
      var
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      [:finder_sql, :counter_sql].each do |arglist|
        ActiveSupport::Deprecation.warn("STRING") if options.include?(var);Fi[	@Z
i
I"˚    def function(arglist)
      (super + [:remote, :dependent, :counter_cache, :primary_key, :inverse_of])
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      super
      define_constructors if constructable?;Fi[	@]
iI"Õ      def function(arglist)
        RealTransaction.new(connection, self, var)
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        false
      end;Fi[	@êiI"é    def function(arglist)
      0
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      false
    end;Fi[	@si*I"Œ        def function(arglist)
          @var = true
        end
        
        def function(arglist)
          true
        end
        
        def function(arglist)
          command.inspect
        end;Fi[	@i'I"#      def function(arglist)
        (top_level.environment == :development)
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        if var = top_level.project_path and var = top_level.sass_dir then
          Compass.projectize(var, var);Fi[	@iåI"’      def function(arglist)
        http_root_relative(top_level.http_javascripts_dir)
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        :scss
      end;Fi[	@|iI"£    def function(arglist)
      (var == "STRING")
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      css2_value
    end;Fi[	@+
iAI":    def function(arglist)
      GRADIENT_ASPECTS.include?(var)
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      var.is_a?(Sass::Script::Number) and ((var.numerator_units.size == 1) and ((var.numerator_units.first == "STRING") and var.denominator_units.empty?))
    end;Fi[	@w
iI"œ        @var = var
        @var = 0
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        @var = (@var - 2) if var.to_s =~ /^after/
        print(var);Fi[	@°i%I"Ø      def function(arglist)
        :invalid
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        :inactive
      end;Fi[	@
i#I"          var.extend_remember_period = mapping.to.extend_remember_period
        end
      end
      
      def function(arglist)
        true
      end
      
      def function(arglist)
        mapping.to.rememberable_options.fetch(:key, "STRING")
      end;Fi[	@Ö
i I"Í  def function(arglist)
    self.sender_handle
  end
  
  def function(arglist)
    true
  end
  
  def function(arglist)
    Rails.logger.debug("STRING")
    if (self.parent_author_signature.nil?.! or self.parent.author.remote?) then;Fi[	I"4data//jekyll_proj/jekyll/converters/identity.rb;Ti
I"å    safe(true)
    
    priority(:lowest)
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      var
    end;Fi[	@ÖiI"ï    def function(arglist)
      0
    end
    
    def function(arglist)
      true
    end
    
    def function(arglist)
      # do nothing
    end;Fi[	@x	iI"»        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          true
        end
        
        def function(arglist)
          "STRING"
        end;Fi[@e@fI" ;FI"private;FI" ;FI"def function(arglist);Fi[[	I"^data//activerecord_proj/active_record/associations/has_and_belongs_to_many_association.rb;TiI";          var = join_table.compile_insert(join_table[reflection.foreign_key] => (owner.id), join_table[reflection.association_foreign_key] => (var.id))
          owner.connection.insert(var)
        end
        var
      end
      
      private
      
      def function(arglist)
        load_target.size
      end;Fi[	@
i:I"⁄        end
        save_through_record(var)
        update_counter(1)
        var
      end
      
      private
      
      def function(arglist)
        @var ||= owner.association(through_reflection.name)
      end;Fi[	I"Odata//activerecord_proj/active_record/associations/singular_association.rb;Ti"I"˛        var = build_record(var)
        yield(var) if block_given?
        set_new_record(var)
        var
      end
      
      private
      
      def function(arglist)
        scope.scope_for_create.stringify_keys.except(klass.primary_key)
      end;Fi[	I"]data//activerecord_proj/active_record/connection_adapters/abstract/schema_definitions.rb;TiI"E        var[:null] = null unless null.nil?
        var[:default] = default unless default.nil?
        add_column_options!(var, var) unless (type.to_sym == :primary_key)
        var
      end
      
      private
      
      def function(arglist)
        base.add_column_options!(var, var.merge(:column => (self)))
      end;Fi[	@ci¬I"§        end
        var
      end
      var
    end
    
    private
    
    def function(arglist)
      @var ||= (model_class and model_class.primary_key)
    end;Fi[	I"Ddata//activerecord_proj/active_record/relation/query_methods.rb;Ti¨I"       var.distinct(uniq_value)
      var.from(build_from) if from_value
      var.lock(lock_value) if lock_value
      var
    end
    
    private
    
    def function(arglist)
      var = var.reject { |arglist| var.blank? }
      return [] if var.empty?;Fi[	I";data//activerecord_proj/active_record/schema_dumper.rb;TiI"±      header(var)
      tables(var)
      trailer(var)
      var
    end
    
    private
    
    def function(arglist)
      @var = var
      @var = @var.native_database_types;Fi[	I"&data//bundler_proj/bundler/env.rb;Ti#I"Ú      ((var << read_file("STRING")) << "STRING")
      ((var << "STRING") << "STRING")
      ((var << read_file("STRING")) << "STRING")
      var
    end
    
    private
    
    def function(arglist)
      begin
        File.read(var).strip;Fi[	@viSI"          var[var] = var unless [Array, Range, Hash].include?(var.class)
        end
      end
      var
    end
    
    private
    
    def function(arglist)
      var = (var.kind_of?(Hash) ? (var.values.first) : (var)).class
      ((var == Class) or (var == Module));Fi[	I"Idata//capistrano_proj/capistrano/recipes/deploy/remote_dependency.rb;Ti^I"Œ      def function(arglist)
        var = @var.dup
        (var << "STRING") if @var
        var
      end
      
      private
      
      def function(arglist)
        begin
          (return unless @var;Fi[	I"Edata//capistrano_proj/capistrano/recipes/deploy/strategy/base.rb;TiBI",            var = Benchmark.realtime { |arglist| var = super }
          end
          logger.trace("STRING")
          var
        end
        
        private
        
        def function(arglist)
          @var ||= (configuration.logger or Capistrano::Logger.new(:output => (STDOUT)))
        end;Fi[	I"*data//diaspora_proj/models/reshare.rb;TiOI"∫    while var.is_a?(Reshare) do
      var = var.root
    end
    var
  end
  
  private
  
  def function(arglist)
    var = Webfinger.new(@var).fetch
    var.save! unless var.persisted?;Fi[	@oiaI"ó        (var[var] << var.to_s)
      end
    end
    var
  end
  
  private
  
  def function(arglist)
    `git rev-parse -q --verify HEAD`.chomp
  end;Fi[	@ÀiàI"
        if klass.default_scopable? and (unscoped?.! and scoped?.!) then
          var.apply_default_scope
        end
        var
      end
      
      private
      
      def function(arglist)
        var.each do |arglist|
          send(var).reject! do |arglist|;Fi[	I"3data//redmine_proj/redmine/activity/fetcher.rb;TiHI"˘        end
        var.sort! { |arglist| (var.event_datetime <=> var.event_datetime) }
        var = var.slice(0, var[:limit]) if var[:limit]
        var
      end
      
      private
      
      def function(arglist)
        @@var[var]
      end;Fi[	I"?data//state_machine_proj/state_machine/event_collection.rb;TiI"≠          false
        end
      end)
      var
    end
    
    private
    
    def function(arglist)
      var and var[:on] ? ([fetch(var.delete(:on))]) : (self)
    end;Fi[I"#   person.pets;TI"#   # => [;T@@ @!I"#   #    ];Ti[[	@&i)I"T      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.select(:name);Ti[	@&ibI"É      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.find(1) # => #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>;Ti[	@&i{I"Å      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.first # => #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>;Ti[	@&iôI"~      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.last # => #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&iI"ì      #   person.pets.size # => 3
      #
      #   person.id # => 1
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.concat([Pet.new(name: 'Brain'), Pet.new(name: 'Benny')]);Ti[	@&i=I"X      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all;Ti[	@&i^I"X      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all;Ti[	@&iwI"X      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all;Ti[	@&iîI"Y      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.destroy_all;Ti[	@&i¥I"a      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete(Pet.find(1));Ti[	@&i–I"n      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete(Pet.find(1), Pet.find(3));Ti[	@&iÏI"a      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete(Pet.find(1));Ti[	@&iI"Y      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete("1");Ti[	@&i$I"b      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.destroy(Pet.find(1));Ti[	@&i|I"i      #   end
      #
      #   person.pets.count # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      def function(arglist)
        @var.count(var, var);Ti[	@&i∞I"°      #
      #   # Because the collection is loaded, you can
      #   # call the collection with no additional queries:
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      def function(arglist)
        @var.length;Ti[I"	else;FI""STRING";FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@’iI"Á    def function(arglist)
      if ((platform == Gem::Platform::RUBY) or platform.nil?) then
        "STRING"
      else
        "STRING"
      end
    end
    
    def function(arglist)
      (identifier == var.identifier)
    end;Fi[	@i|I"˚      def function(arglist)
        if var = top_level.fonts_dir_without_default then
          var
        else
          "STRING"
        end
      end
      
      def function(arglist)
        http_root_relative(top_level.http_fonts_dir)
      end;Fi[	@(
iCI"*      def function(arglist)
        if var.respond_to?(:serialize_to_config) then
          (var.serialize_to_config(var) + "STRING")
        else
          "STRING"
        end
      end
      
      def function(arglist)
        $stderr.puts("STRING") if (http_images_path == :relative)
      end;Fi[	@¨i8I"    def function(arglist)
      if Compass.configuration.color_output and (var and COLORS.has_key?(var.to_sym)) then
        defined? $boring and $boring ? ("STRING") : ("STRING")
      else
        "STRING"
      end
    end
    
    def function(arglist)
      print(var)
    end;Fi[	@-iI"            var = []
            var.join("STRING")
            var[(var..var)].join("STRING")
          else
            "STRING"
          end
        end
        
        def function(arglist)
          var = []
          var.split("STRING").each_with_index do |arglist|;Fi[	@3i	I"  def function(arglist)
    if AppConfig.settings.pod_name.present? then
      AppConfig.settings.pod_name
    else
      "STRING"
    end
  end
  
  def function(arglist)
    AppConfig.version.number.present? ? (AppConfig.version.number) : ("STRING")
  end;Fi[	@|ihI"    def function(arglist)
      if options[:request_path].is_a?(String) then
        options[:request_path]
      else
        "STRING"
      end
    end
    
    def function(arglist)
      if options[:callback_path].is_a?(String) then
        options[:callback_path];Fi[	@àiãI"      def function(arglist)
        if @var[:fog_directory].to_s.=~(Fog::AWS_BUCKET_SUBDOMAIN_RESTRICTON_REGEX) then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        case var
        when File then;Fi[	@I
iI"¿          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      
      def function(arglist)
        case var[:format]
        when :html then;Fi[	@I
i4I"„          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      
      def function(arglist)
        while @var.any? and var.is_descendant_of?(@var.last).! do
          @var.pop;Fi[	@iI"“          "STRING"
        when 2 then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        var = "STRING"
        var.each_with_index do |arglist|;Fi[	@‰iMI"…          @var
        when Regexp then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        @var.nil? ? ("STRING") : ("STRING")
      end;Fi[	I"@data//rspec_proj/spec/runner/formatter/snippet_extractor.rb;Ti0I"            var = []
            var.join("STRING")
            var[(var..var)].join("STRING")
          else
            "STRING"
          end
        end
        
        def function(arglist)
          var = []
          var.split("STRING").each_with_index do |arglist|;Fi[	@L
i–I"C      def function(arglist)
        if ([/mswin/, /java/].detect { |arglist| var.=~(RUBY_PLATFORM) } or (Spec::Ruby.version.to_f == 1.9)) then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        @var ||= [["STRING", @var]]
        require(which_heckle_runner);Fi[	@É	i6I"≤          "STRING"
        when stdout then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        "STRING"
      end;Fi[@ÆI"# do nothing;FI"end;FI" ;F@ƒI"# do nothing;Fi[[	@‹iI"!      
      # Rolls back the transaction (and turns on auto-committing). Must be
      # done if the transaction block raises an exception or returns false.
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      ;Fi[	@Ëi¸I"∞        @var.joinable = var
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      ;Fi[	@Ëi I"©        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      ;Fi[	@ìi‰I"†        end
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      ;Fi[	@òiI"≤require("rubygems/user_interaction")
module Bundler
  class UI
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    ;Fi[	@òiI"ì      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    ;Fi[	@òiI"ì      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    ;Fi[	@òiI"ì      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    ;Fi[	@òiI"ì      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    ;Fi[	@¨iRI"è  end
  
  class NullLogger
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    ;Fi[	@≤iI"÷        self.mixin_count += 1 if var.is_a?(Sass::Tree::MixinNode)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      ;Fi[	@≈i=I"Ø        @var.add_step(var)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      ;Fi[	@üi˝I"ç  
  alias_method(:add_page, :AddPage)
  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    # do nothing
  end
  ;Fi[	@ÒiI"∑          end
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end;Fi[	@ÙiI"æ          @var.flush
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end;Fi[@
@@@I"end;FI"end;Fi[[	@iQI"]    end
    
    private
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@G	i*I"É        chunk_suffixes.map { |arglist| "STRING" }
      end
    end
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@J	i‚I"ô    def function(arglist)
      "STRING" if `which more` rescue "STRING".empty?.!
    end
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@»
iGI"]    end
    
    private
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@	iI"û        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@V	i%I"	        var = (var.split("STRING").map { |arglist| (("STRING" * var) + var) }.join("STRING") << "STRING")
        inject_into_class(model_path, var.last, var) if model_exists?
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	@\	iI"∞          inject_into_file(model_path, model_contents, :after => "STRING")
        end
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	@mi3I"ì      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end
module Rails;Fi[	@˜	iI"µ      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	@˝	iI"µ      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	@h	iI"ö      def self.new(arglist)
        var.create_entity(var, *var)
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[	I"%data//omni_proj/omniauth/form.rb;TiYI"_    end
    
    protected
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@˛iåI"û        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
      end
    end
  end
end;Fi[	@itI"u    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@É	i:I"}          "STRING"
        end
      end
      
      def function(arglist)
        "STRING"
      end
    end
  end
end;Fi[@TI"# do nothing;FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@πiÎI"Î        (method(var).arity == 0) ? (send(var)) : (send(var, var))
      when Proc then
        var.call(var)
      else
        # do nothing
      end
    end
    
    def function(arglist)
      raise(RecordNotFound, "STRING")
    end;Fi[	@ìiBI"“          else
            Associations::HasOneAssociation
          end
        else
          # do nothing
        end
      end
      
      def function(arglist)
        options.key?(:polymorphic)
      end;Fi[	I"6data//activerecord_proj/active_record/relation.rb;Ti›I"ª        (var.to_sql == to_sql)
      when Array then
        (to_a == var)
      else
        # do nothing
      end
    end
    
    def function(arglist)
      var.pp(self.to_a)
    end;Ti[	I"\data//activerecord_proj/rails/generators/active_record/migration/migration_generator.rb;Ti I"E            @var = attributes.map(&:plural_name)
            set_index_names
          end
        else
          # do nothing
        end
      end
      
      def function(arglist)
        attributes.each_with_index do |arglist|
          var.index_name = [var, attributes[(var - 1)]].map { |arglist| index_name_for(var) };Fi[	@-i%I"Ì        case var
        when /^image\/(png|gif|jpg|jpeg)/ then
          embed_image(var, var)
        else
          # do nothing
        end
      end
      
      def function(arglist)
        var = "STRING"
        @var = (@var + 1);Fi[	I"'data//homebrew_proj/cmd/install.rb;Ti I"ı    case Hardware.cpu_type
    when :ppc, :dunno then
      abort("STRING".undent)
    else
      # do nothing
    end
  end
  
  def function(arglist)
    if HOMEBREW_CELLAR.exist? and HOMEBREW_CELLAR.writable_real?.! then
      raise("STRING");Fi[	I"%data//homebrew_proj/compilers.rb;Ti;I"«      MacOS.llvm_build_version.to_i
    when :gcc then
      MacOS.gcc_42_build_version.to_i
    else
      # do nothing
    end
  end
  
  def function(arglist)
    (@var.to_sym == var.to_sym)
  end;Fi[	@riI"÷    when Hash then
      @var = @var.keys.first
      @var = @var.values.first
    else
      # do nothing
    end
  end
  
  def function(arglist)
    var.each_with_index do |arglist|
      if var.is_a?(Hash) then;Fi[	@rizI"Ï      raise("STRING")
    when 1 then
      Dir.chdir(var.first) rescue nil
    else
      # do nothing
    end
  end
  
  def function(arglist)
    var = /https?:\/\/(www\.)?github\.com\/.*\/(zip|tar)ball\//
    if var.match(@var) then;Fi[	I"#data//homebrew_proj/patches.rb;TiYI"≠    when :bzip2 then
      @var = (@var + "STRING")
      FileUtils.mv(@var, @var)
    else
      # do nothing
    end
  end
  
  def function(arglist)
    @var.nil?.!
  end;Fi[	I"(data//homebrew_proj/requirements.rb;Ti(I"Ó      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when :rbx then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    else
      # do nothing
    end
  end
  
  def function(arglist)
    case @var
    when :chicken then;Fi[	@I
i‹I"        case var[:format]
        when :pdf then
          var[:pdf].Line(15, var[:top], PDF::TotalWidth, var[:top])
        else
          # do nothing
        end
      end
      
      def function(arglist)
        case var[:format]
        when :html then;Fi[	@I
iÔI"        when :pdf then
          pdf_new_page?(var)
          pdf_subject(var, var.name)
        else
          # do nothing
        end
      end
      
      def function(arglist)
        if var.is_a?(Project) and (var.start_date and var.due_date) then
          var[:zoom] ||= 1;Fi[	@I
iI"&        when :pdf then
          pdf_new_page?(var)
          pdf_subject(var, var.to_s_with_project)
        else
          # do nothing
        end
      end
      
      def function(arglist)
        if var.is_a?(Version) and (var.due_date and var.start_date) then
          var[:zoom] ||= 1;Fi[	@<	i5I"®          1
        when :twice then
          2
        else
          # do nothing
        end
      end
      
      def function(arglist)
        @var = 0
      end;Fi[@ÆI"@var = var;FI"@var = var;FI"end;FI" ;FI"def function(arglist);Fi[[	@i{I"      class Reaper
        attr_reader(:pool, :frequency)
        
        def function(arglist)
          @var = var
          @var = var
        end
        
        def function(arglist)
          return unless frequency
          Thread.new(frequency, pool) do |arglist|;Fi[	@iI"¸      class Resolver
        attr_reader(:config, :klass, :configurations)
        
        def function(arglist)
          @var = var
          @var = var
        end
        
        def function(arglist)
          case config
          when nil then;Fi[	I"Pdata//activerecord_proj/active_record/connection_adapters/statement_pool.rb;Ti
I"‘    class StatementPool
      include(Enumerable)
      
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end;Fi[	@ci¯I"¬    
    attr_reader(:model_class, :fixture)
    
    def function(arglist)
      @var = var
      @var = var
    end
    
    def function(arglist)
      model_class.name if model_class
    end;Fi[	I"@data//cancan_proj/cancan/model_adapters/abstract_adapter.rb;Ti)I"Ÿ        raise(NotImplemented, "STRING")
      end
      
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        raise(NotImplemented, "STRING")
      end;Fi[	@ÄitI"˜            InternalRevision.new(var, var)
          end
          
          def function(arglist)
            @var = var
            @var = var
          end
          
          def function(arglist)
            (@var == "STRING")
          end;Fi[	I">data//compass_proj/compass/installers/template_context.rb;TiI"2        new(*var).send(:get_binding)
      end
      
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        (config.http_stylesheets_path or (config.default_for(:http_stylesheets_path) or config.http_root_relative(config.css_dir)))
      end;Fi[	I"Hdata//cucumber_prok/cucumber/formatter/gherkin_formatter_adapter.rb;Ti
I"˙module Cucumber
  module Formatter
    class GherkinFormatterAdapter
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        @var.uri(var.file)
        @var.feature(var.gherkin_statement);Fi[	I"=data//diaspora_proj/presenters/extreme_post_presenter.rb;TiI"Ãclass ExtremePostPresenter
  def function(arglist)
    @var = var
    @var = var
  end
  
  def function(arglist)
    var = PostPresenter.new(@var, @var)
    var = PostInteractionPresenter.new(@var, @var);Fi[	I"8data//diaspora_proj/presenters/o_embed_presenter.rb;TiI"¶  
  include(ActionView::Helpers::TextHelper)
  
  def function(arglist)
    @var = var
    @var = var
  end
  
  def function(arglist)
    as_json(var).to_json
  end;Fi[	I"7data//diaspora_proj/presenters/person_presenter.rb;TiI"class PersonPresenter
  def function(arglist)
    @var = var
    @var = var
  end
  
  def function(arglist)
    var = @var.as_api_response(:backbone).merge(:is_own_profile => (is_own_profile))
    if (is_own_profile or person_is_following_current_user) then;Fi[	I"5data//diaspora_proj/presenters/post_presenter.rb;TiDI"_  end
end
class PostInteractionPresenter
  def function(arglist)
    @var = var
    @var = var
  end
  
  def function(arglist)
    { :likes => (as_api(@var.likes)), :reshares => (PostPresenter.collection_json(@var.reshares, @var)), :comments => (CommentPresenter.as_collection(@var.comments)), :participations => (as_api(@var.participations)) }
  end;Fi[	I"0data//nokogiri_proj/nokogiri/xml/builder.rb;TiÅI"…      end
      
      class NodeBuilder
        def function(arglist)
          @var = var
          @var = var
        end
        
        def function(arglist)
          @var[var] = var
        end;Fi[	I"4data//paperclip_proj/paperclip/url_generator.rb;TiI"require("uri")
module Paperclip
  class UrlGenerator
    def function(arglist)
      @var = var
      @var = var
    end
    
    def function(arglist)
      escape_url_as_needed(timestamp_as_needed(@var[:interpolator].interpolate(most_appropriate_url, @var, var), var), var)
    end;Fi[	@tiI"module Spec
  module Example
    class ExampleMatcher
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        var.any? do |arglist|
          (matches_literal_example?(var) or matches_example_not_considering_modules?(var));Fi[I"# do nothing;FI"end;FI" ;F@ƒI"# do nothing;FI"end;Fi[[	@‹iI"[      # Rolls back the transaction (and turns on auto-committing). Must be
      # done if the transaction block raises an exception or returns false.
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      # Set the sequence to the max value of the table's column.;Fi[	@Ëi˝I"∞      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist);Fi[	@ËiI"∞      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist);Fi[	@ìiÂI"Ù      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      # A chain of reflections from this one back to the owner. For more see the explanation in;Fi[	@òi	I"ßmodule Bundler
  class UI
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist);Fi[	@òiI"ö    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist);Fi[	@òiI"ö    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist);Fi[	@òiI"ö    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist);Fi[	@òiI"ö    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist);Fi[	@¨iSI"£  
  class NullLogger
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist);Fi[	@≤iI"∞      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist);Fi[	@≈i>I"¢      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      private;Fi[	@üi˛I"¢  alias_method(:add_page, :AddPage)
  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    # do nothing
  end
  
  attr_reader :function;Fi[	@ÒiI"±        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end
    end;Fi[	@ÙiI"±        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end
    end;Fi[@
@@@@üI" ;Fi[[	@ciÂI"∏        end
      end
    end
    
    def function(arglist)
      "STRING"
    end
  end
  
  Fixtures = ActiveSupport::Deprecation::DeprecatedConstantProxy.new("STRING", "STRING")
  ;Fi[	@si`I"Ì        def function(arglist)
          Evaluator.new(configuration, condition, var).result
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      def function(arglist)
        @var = var;Fi[	@¿i3I"„        var = eval_js("STRING")
        var.to_a[(1..-1)].map { |arglist| JsArg.new(var) } if var
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    class JsHook
      def function(arglist);Fi[	@S	iI"≥      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    def self.config(arglist)
      class << var;Fi[	@Y	igI"Ê      def function(arglist)
        view_directory(:markerb, target_path)
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    class ViewsGenerator < Rails::Generators::Base
      desc("STRING");Fi[	@ªiUI"Ò        var = "STRING"
        raise((var << ([:===, :==].include?(@var) ? ("STRING") : ("STRING"))))
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    class BePredicate < Be
      def function(arglist);Fi[	@ªi¢I"©      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:
    #   should be_true;Fi[	@ŒiOI"∂        @var = var
        self
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:
    #   should change(receiver, message, &block);Fi[	@ÿiTI"Ω        @var = var
        self
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:
    #   should have(number).named_collection__or__sugar;Fi[	@iI"¨      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:
    #   should satisfy {};Fi[	@u	i%I"@      def function(arglist)
        (@var or @var.nil? ? (explanation) : ("STRING"))
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # simple_matcher makes it easy for you to create your own custom matchers
    # in just a few lines of code when you don't need all the power of a;Fi[	@x	iI"«        def function(arglist)
          true
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      class RegexpMatcher
        def function(arglist);Fi[	@x	iFI"À            return false
          end
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      class HashNotIncludingMatcher
        def function(arglist);Fi[	@x	i[I"√            return false
          end
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      class DuckTypeMatcher
        def function(arglist);Fi[@Æ@ÌI"end;FI" ;FI"def function(arglist);FI"@var = var;Fi[[	@äiI"      end
      
      class HaveAttachedFileMatcher
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          @var = var
          @var = @var.class unless Class.===(@var)
          responds? and (has_column? and included?);Fi[	@ñiI"#      end
      
      class ValidateAttachmentPresenceMatcher
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          @var = var
          @var = var.new if (var.class == Class)
          error_when_not_valid? and no_error_when_valid?;Fi[	@ùiI"–      end
      
      class ValidateAttachmentSizeMatcher
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          @var = var
          self
        end;Fi[	@üiÖI"ñ    @var = var if (@var > 0) and (@var < var)
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  ;Fi[	@üiâI"ï    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
    @var = var
    @var = (@var - var);Fi[	@üi†I"u    end
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  ;Fi[	@üi§I"|    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  ;Fi[	@üi®I"|    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  ;Fi[	@üi¨I"|    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  ;Fi[	@üi∞I"|    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  ;Fi[	@üi¥I"|    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  ;Fi[	@ªiI"√    class Be
      include(Spec::Matchers::Pretty)
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = var
        @var.!.!
      end;Fi[	@ﬂi
I"    class MatchArray
      include(Spec::Matchers::Pretty)
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = var
        @var = difference_between_arrays(@var, @var)
        @var = difference_between_arrays(@var, @var);Fi[	@<	iSI"4      # raise an instance of it, creating it with +new+. If the exception
      # class initializer requires any parameters, you must pass in an
      # instance and not the class.
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = var
      end
      ;Fi[@(@œ@–I"execute("STRING");FI"end;FI" ;Fi[[	@^i§I"	      #   remove_index :accounts, name: :by_branch_party
      def function(arglist)
        remove_index!(var, index_name_for_remove(var, var))
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      # Rename an index.
      #;Fi[	@Îi I"ø        rescue
          # do nothing
        end
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING");Fi[	@Îi$I"       
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING");Fi[	@Îi(I"<      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      # In the simple case, MySQL allows us to place JOINs directly into the UPDATE
      # query. However, this does not allow for LIMIT, OFFSET and ORDER. To support;Fi[	@ÎiµI"˙      #   rename_table('octopuses', 'octopi')
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        var = column_for(var, var);Fi[	@ÎiƒI"!        var = column_for(var, var)
        execute("STRING") unless (var or var.nil?)
        change_column(var, var, var.sql_type, :null => (var))
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING");Fi[	@Îi»I"Ô      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      # Maps logical Rails types to MySQL-specific data types.
      def function(arglist);Fi[	I"`data//activerecord_proj/active_record/connection_adapters/postgresql/database_statements.rb;Ti±I"!          var = "STRING"
          ActiveSupport::Deprecation.warn(var)
          (@var.transaction_status == PGconn::PQTRANS_IDLE)
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING");Fi[	@ïiµI"‡        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING");Fi[	I"^data//activerecord_proj/active_record/connection_adapters/postgresql/schema_statements.rb;Ti	I"Ó        def function(arglist)
          clear_cache!
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING");Fi[	@öiI"—        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          63;Fi[	@i*I"“      
      def function(arglist)
        exec_query(var, var).rows
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING");Fi[	@i.I"       
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING");Fi[	@i2I"ø      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      #:nodoc:
      def function(arglist);Fi[@(@œ@–I"2broadcast(var) { |arglist| var.accept(self) };FI"end;FI" ;Fi[[	I"4data//cucumber_prok/cucumber/ast/tree_walker.rb;TiI"      #:nodoc:
      def function(arglist)
        @var, @var, @var = var, var, var
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) };Fi[	@™iI"      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) };Fi[	@™iI"ˇ      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var);Fi[	@™i I"‡      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var);Fi[	@™i1I"G      # +feature_element+ is either Scenario or ScenarioOutline
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var, var, var);Fi[	@™i9I"      
      def function(arglist)
        broadcast(var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) };Fi[	@™i=I"      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var);Fi[	@™iEI"Ù      
      def function(arglist)
        broadcast(var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var, var, var);Fi[	@™iMI"      
      def function(arglist)
        broadcast(var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) };Fi[	@™iQI"/      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var, var, var, var, var, var, var) do |arglist|;Fi[	@™iaI"˛      
      def function(arglist)
        broadcast(var, var, var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var);Fi[	@™imI"ˇ      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) };Fi[	@™iqI"      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var);Fi[@TI"
super;FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@>ijI"\        def function(arglist)
          if var and var = self.class.serialized_attributes[var.name] then
            Attribute.new(var, var, :unserialized)
          else
            super
          end
        end
        
        def function(arglist)
          self.class.serialized_attributes.include?(var) ? (var.!=(var)) : (super)
        end;Fi[	@>ivI"M        def function(arglist)
          if self.class.serialized_attributes.include?(var) then
            super.unserialized_value
          else
            super
          end
        end
        
        def function(arglist)
          super.dup.tap do |arglist|
            self.class.serialized_attributes.each_key do |arglist|;Fi[	@Îi=I"Ÿ            :integer
          when /bit/i then
            :binary
          else
            super
          end
        end
        
        def function(arglist)
          case var
          when /blob|text/i then;Fi[	@Îi¯I"˙          else
            raise(ActiveRecordError, "STRING")
          end
        else
          super
        end
      end
      
      def function(arglist)
        var[:first] ? ((var << "STRING")) : ((var << "STRING") if var[:after])
      end;Fi[	@ÎiSI"˚          RecordNotUnique.new(var, var)
        when 1452 then
          InvalidForeignKey.new(var, var)
        else
          super
        end
      end
      
      def function(arglist)
        var = "STRING"
        add_column_options!(var, var);Fi[	@DiI"-            (-1.0 / 0.0)
          when / BC$/ then
            super(("STRING" + var.sub(/ BC$/, "STRING")))
          else
            super
          end
        end
        
        def function(arglist)
          Hash.===(var) ? (var.map { |arglist| "STRING" }.join("STRING")) : (var)
        end;Fi[	@tiMI"’            else
              super
            end
          else
            super
          end
        end
        
        def function(arglist)
          return super(var, var) unless var
          case var;Fi[	@iƒI"L        if var.kind_of?(String) and (var and ((var.type == :binary) and var.class.respond_to?(:string_to_binary))) then
          var = var.class.string_to_binary(var).unpack("STRING")[0]
          "STRING"
        else
          super
        end
      end
      
      def function(arglist)
        @var.class.quote(var)
      end;Fi[	I"9data//activerecord_proj/active_record/inheritance.rb;TiI"9      def function(arglist)
        if using_single_table_inheritance?(var) then
          find_sti_class(var[inheritance_column])
        else
          super
        end
      end
      
      def function(arglist)
        var[inheritance_column].present? and columns_hash.include?(inheritance_column)
      end;Fi[	@wiI"	          var = ActiveRecord::LogSubscriber.reset_runtime
          self.db_runtime = (var + var)
          (var - var)
        else
          super
        end
      end
      
      def function(arglist)
        super
        if ActiveRecord::Base.connected? then;Fi[	I":data//bundler_proj/bundler/vendor/thor/shell/color.rb;TitI"5          var = File.binread(var).to_s.split("STRING")
          var = var.to_s.split("STRING")
          Diff::LCS.sdiff(var, var).each { |arglist| output_diff_line(var) }
        else
          super
        end
      end
      
      def function(arglist)
        case var.action
        when "STRING" then;Fi[	I"9data//bundler_proj/bundler/vendor/thor/shell/html.rb;TibI"5          var = File.binread(var).to_s.split("STRING")
          var = var.to_s.split("STRING")
          Diff::LCS.sdiff(var, var).each { |arglist| output_diff_line(var) }
        else
          super
        end
      end
      
      def function(arglist)
        case var.action
        when "STRING" then;Fi[	I"5data//redmine_proj/redmine/views/builders/xml.rb;TiI"B        def function(arglist)
          if (var.size == 1) and var.first.is_a?(::Time) then
            __send__(var, var.first.xmlschema, &var)
          else
            super
          end
        end
        
        def function(arglist)
          __send__(var, (var or {}).merge(:type => "STRING"), &var)
        end;Fi[I"when "STRING" then;FI""STRING";FI"when "STRING" then;FI""STRING";FI"when "STRING" then;FI""STRING";Fi[[	I"Fdata//compass_proj/compass/sass_extensions/functions/constants.rb;TiI"8      var.value.map! do |arglist|
        if var.is_a?(Sass::Script::String) then
          var = case var.value
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÚiI"          var = case var.value
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÚiI"            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else
            (Compass::Util.compass_warn("STRING");Fi[	@ÚiDI"    def function(arglist)
      var = var.value.split(/ +/).map do |arglist|
        case var
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING";Fi[	@ÚiFI"ˆ        case var
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING";Fi[	@ÚiHI"Â          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        else
          var;Fi[	@·i˜I"4              "STRING"
            else
              case @var.value[:type]
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING";Fi[	@·i˘I"F              case @var.value[:type]
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING";Fi[	@·i˚I",                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              else
                "STRING";Fi[	@vigI"            "STRING"
          when "STRING", "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@viiI"            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@vikI"            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@vimI"            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else
            (var.value.first + "STRING");Fi[@(@œ@–I"
false;FI"end;FI" ;Fi[[	@]
iI"•      
      def function(arglist)
        true
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        false;Fi[	@]
iI"”      
      def function(arglist)
        false
      end
      
      def function(arglist)
        false
      end
      
      # This is a noop when there are no open transactions
      def function(arglist);Fi[	@]
ikI"€            var.logger.error(var) if var.respond_to?(:logger) and var.logger
          end
        end
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        true;Fi[	@Ëi}I"#      # SQL Server, and others support this. MySQL and others do not.
      def function(arglist)
        false
      end
      
      def function(arglist)
        false
      end
      
      # Does this adapter support savepoints? PostgreSQL and MySQL do,
      # SQLite < 3.6.8 does not.;Fi[	@êi!I"è    
    def function(arglist)
      true
    end
    
    def function(arglist)
      false
    end
    
    def function(arglist)
      false;Fi[	@êi%I"ú    
    def function(arglist)
      false
    end
    
    def function(arglist)
      false
    end
    
    def function(arglist)
      @var ||= "STRING";Fi[	@ìiI"	      # ThroughReflection.
      def function(arglist)
        [self]
      end
      
      def function(arglist)
        false
      end
      
      # An array of arrays of scopes. Each item in the outside array corresponds to a reflection
      # in the #chain.;Fi[	@òiI"†    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end
    
    class Shell < UI
      attr_writer(:shell);Fi[	I"3data//bundler_proj/bundler/vendor/thor/task.rb;TiI"    def function(arglist)
      super(var)
      self.options = var.options.dup if var.options
    end
    
    def function(arglist)
      false
    end
    
    # By default, a task invokes a method in the thor class. You can change this
    # implementation to create custom tasks.;Fi[	@Â	i5I"¯      # This could print out a message or something.
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        "STRING" if options[:pattern_name];Fi[	@$iI"∞      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        :skipped;Fi[	I"/data//diaspora_proj/models/conversation.rb;Ti.I"‹  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist)
    false
  end
  
  def function(arglist)
    self.participants.map { |arglist| var.diaspora_handle }.join("STRING");Fi[	@XiyI"Ê  # @return Returns true if this Post will accept updates (i.e. updates to the caption of a photo).
  def function(arglist)
    false
  end
  
  def function(arglist)
    false
  end
  
  def function(arglist)
    I18n.t("STRING");Fi[@
@@•I"end;FI" ;FI"def function(arglist);Fi[[	@™iI"      def function(arglist)
        @var, @var, @var = var, var, var
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end;Fi[	@™iI"!      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end;Fi[	@™iI"      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var)
      end;Fi[	@™i!I"„      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var)
      end;Fi[	@™i2I"      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var, var, var)
      end;Fi[	@™i:I"      def function(arglist)
        broadcast(var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end;Fi[	@™i>I"      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end;Fi[	@™iFI"˜      def function(arglist)
        broadcast(var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var, var, var)
      end;Fi[	@™iNI"      def function(arglist)
        broadcast(var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end;Fi[	@™iRI"`      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var, var, var, var, var, var, var) do |arglist|
          visit_step_name(var, var, var, var, var, var);Fi[	@™ibI"      def function(arglist)
        broadcast(var, var, var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end;Fi[	@™inI"      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end;Fi[	@™irI"      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end;Fi[@T@¬@√@ƒI"end;FI"end;Fi[[	I"Ddata//activerecord_proj/active_record/associations/preloader.rb;TiÜI"õ          HasAndBelongsToMany
        when :belongs_to then
          BelongsTo
        else
          # do nothing
        end
      end
    end
  end
end;Fi[	@ïiOI"c                var.gsub!(/[^-\d.]/, "STRING")
              when /^-?\D+[\d.]+,\d{2}$/ then
                var.gsub!(/[^-\d,]/, "STRING").sub!(/,/, "STRING")
              else
                # do nothing
              end
            end
          end
        end
        
        # Queries the database and returns the results in an Array-like object;Fi[	I":data//activerecord_proj/active_record/transactions.rb;Ti»I"¶        destroyed?
      when :update then
        (transaction_record_state(:new_record) or destroyed?).!
      else
        # do nothing
      end
    end
  end
end;Fi[	I"6data//bundler_proj/bundler/vendor/thor/actions.rb;TiI"¡      when Hash then
        [:force, :skip, "STRING", "STRING"].each { |arglist| var.delete(var) }
        var.merge!(var => (true))
      else
        # do nothing
      end
    end
  end
end;Fi[	@_iGI"y        "STRING"
      when :array then
        "STRING"
      else
        # do nothing
      end
    end
  end
end;Fi[	@iI"°            false
          when "STRING" then
            exit(-1)
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	@iΩI"z        var.abort!
      when :scp then
        var.close
      else
        # do nothing
      end
    end
  end
end;Fi[	@õi<I"º          when :stdout then
            $stdout = self.new($stdout)
            return $stdout
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	I"$data//homebrew_proj/cmd/edit.rb;Ti+I"â      case File.basename(var)
      when "STRING", "STRING" then
        true
      else
        # do nothing
      end
    end
  end
end;Fi[	@disI"Ä        var
      when "STRING" then
        Maruku.new(var).to_html
      else
        # do nothing
      end
    end
  end
end;Fi[	@∂iLI"‰            when "STRING" then
              (@var << "STRING")
              var = (var + 1.day)
            else
              # do nothing
            end
          end
        end
      end
      
      def function(arglist);Fi[	@¡i)I"†            else
              AllMatcher.instance
            end
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[@Æ@Ø@∞@±I"private;FI" ;Fi[[	@ˆi#I"ö      @var.requirement
    end
    
    def function(arglist)
      "STRING"
    end
    
    private
    
    def function(arglist)
      @var.send(*var);Fi[	@¸iSI"¨        @var[var]
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        @var ||= (var = {};Fi[	@ˇiòI"†        end)
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        @var = true;Fi[	@'iDI"√          @var[0].line
        end
        
        def function(arglist)
          "STRING"
        end
        
        private
        
        def function(arglist)
          @var.index(self);Fi[	I"<data//mongoid_proj/lib/mongoid/contextual/map_reduce.rb;TiÿI"Á      # @return [ String ] The inspection string.
      #
      # @since 3.1.0
      def function(arglist)
        "STRING"
      end
      
      private
      
      # Apply criteria specific options - query, sort, limit.
      #;Fi[	@©iEI"§        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        if var then;Fi[	@ªixI"®        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        "STRING".to_sym;Fi[	@—iI"®        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        "STRING".to_sym;Fi[	@ﬂi!I"⁄        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        var.all? { |arglist| var.respond_to?(:<=>) } ? (var.sort) : (var);Fi[	@‰iAI"¢        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        case @var;Fi[	@Ûi.I"“        @var ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        @var.nil? ? ("STRING") : ("STRING");Fi[	I"9data//state_machine_proj/state_machine/transition.rb;Ti'I"“    # 
    #   transition = StateMachine::Transition.new(object, machine, :ignite, :parked, :idling)
    #   transition   # => #<StateMachine::Transition attribute=:state event=:ignite from="parked" from_name=:parked to="idling" to_name=:idling>
    def function(arglist)
      "STRING"
    end
    
    private
    
    # Runs a block that may get paused.  If the block doesn't pause, then
    # execution will continue as normal.  If the block gets paused, then it;Fi[@TI"var;FI"end;FI"end;FI"end;FI" ;Fi[[	I"2data//activemodel_proj/active_model/errors.rb;TilI"œ        generate_message(var, var, var.except(*CALLBACKS_OPTIONS))
      when Proc then
        var.call
      else
        var
      end
    end
  end
  
  class StrictValidationFailed < StandardError
  end;Fi[	@Di)I"2                var = var.gsub(/^"(.*)"$/, "STRING").gsub(/\\(.)/, "STRING")
                [var, var]
              end]
            else
              var
            end
          end
        end
        
        def function(arglist)
          Hash.===(var) ? (ActiveSupport::JSON.encode(var)) : (var);Fi[	@DirI"™                  nil
                end
              end
            else
              var
            end
          end
        end
        
        private
        ;Fi[	@˛iLI"$          def function(arglist)
            if String.===(var) then
              ConnectionAdapters::PostgreSQLColumn.string_to_array(var, @var)
            else
              var
            end
          end
        end
        
        class Integer < Type
          def function(arglist);Fi[	I"2data//activerecord_proj/active_record/core.rb;Ti`I"√        var = Relation.new(self, arel_table)
        if finder_needs_type_condition? then
          var.where(type_condition).create_with(inheritance_column.to_sym => (sti_name))
        else
          var
        end
      end
    end
    
    # New objects can be instantiated as either empty (pass no construction parameter) or pre-set with
    # attributes but not yet saved (pass a hash with key names matching the associated table column names).;Fi[	@iI"          var.each_with_object({}) do |arglist|
            var[var] = (var == :asc) ? (:desc) : (:asc)
          end
        else
          var
        end
      end
    end
    
    def function(arglist)
      var.is_a?(Array) and var.all? { |arglist| var.is_a?(String) };Fi[	I"9data//bundler_proj/bundler/vendor/thor/invocation.rb;TiI"        case var
        when Symbol, String then
          Thor::Util.find_class_and_task_by_namespace(var.to_s, var.!)
        else
          var
        end
      end
    end
    
    # Make initializer aware of invocations and the initialization args.
    def function(arglist);Ti[	@i≠I"·          var = StringIO.new(var.read)
          var.pos = var.pos = var
          var
        else
          var
        end
      end
    end
    
    def function(arglist)
      raise(var) if var.message.include?("STRING");Fi[	I"9data//compass_proj/compass/configuration/adapters.rb;Ti*I"b        (additional_import_paths or []).map do |arglist|
          if var.is_a?(String) and (project_path and absolute_path?(var).!) then
            File.join(project_path, var)
          else
            var
          end
        end
      end
      
      def function(arglist)
        ((var.index(File::SEPARATOR) == 0) or (var.index("STRING") == 1));Fi[	@kiZI"≠    else
      if defined? Sass::Script::List and var.is_a?(Sass::Script::List) then
        var.value.first
      else
        var
      end
    end
  end
  
  protected
  ;Fi[	@ŒiîI"            var.select do |arglist|
              (var.args.inject(0) { |arglist| (var + var.to_s.length) } == var)
            end
          else
            var
          end
        end
      end
      
      def function(arglist)
        if var = programming_language_for(var) then;Fi[@@I"end;FI"end;FI"end;FI"end;Fi[[	@QiáI"}        when :belongs_to then
          BelongsTo
        else
          # do nothing
        end
      end
    end
  end
end;Fi[	@WicI"´          begin
            var.mkdir(var.join("STRING"))
          rescue Net::FTPPermError
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	@ZiUI"µ          begin
            var.mkdir!(var.join("STRING"))
          rescue Net::SFTP::StatusException
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	@iI"è          when "STRING" then
            exit(-1)
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	@õi=I"†            $stdout = self.new($stdout)
            return $stdout
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[	I"5data//nokogiri_proj/nokogiri/xml/sax/document.rb;TimI"¬        # +name+ is the target of the instruction
        # +content+ is the value of the instruction
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	I"=data//rspec_proj/spec/runner/formatter/base_formatter.rb;TiêI"‘        
        # This method is invoked at the very end. Allows the formatter to clean up, like closing open streams.
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@ÒiI"v        end
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@ÙiI"v        end
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@•iI"z        private
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@¡i*I"è              AllMatcher.instance
            end
          else
            # do nothing
          end
        end
      end
    end
  end
end;Fi[@(@œ@–I"@var = true;FI"end;FI" ;Fi[[	@fiI"œ        @var = var["STRING"]
        @var = var["STRING"]
        @var = @var
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = true;Fi[	@fi"I"Â      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = true
      end
      
      def self.from_lock(arglist)
        new(var.merge("STRING" => (var.delete("STRING"))));Fi[	I"2data//bundler_proj/bundler/source/rubygems.rb;TiI"        @var = false
        @var = false
        @var = ([Bundler.app_cache] + Bundler.rubygems.gem_path.map { |arglist| File.expand_path("STRING") })
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = true;Fi[	@”iI"∫      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        Rubygems.hash;Fi[	@òi=I"∑      
      def function(arglist)
        tell_me(var, :red, var)
      end
      
      def function(arglist)
        @var = true
      end
      
      attr_reader :function
      ;Fi[	@òiGI"’      
      def function(arglist)
        @var.!.! and @var.!
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        tell_me(var, nil, var) if debug?;Fi[	I"0data//compass_proj/compass/commands/base.rb;TiI"ö      
      def function(arglist)
        @var.!
      end
      
      def function(arglist)
        @var = true
      end
      
      protected
      ;Fi[	I"+data//cucumber_prok/cucumber/errors.rb;TiI"¶    def function(arglist)
      super("STRING")
      @var = var
    end
    
    def function(arglist)
      @var = true
    end
    
    attr_reader :function
  end;Fi[	@Ωi2I"        write_file(feature_result_filename(var.file), @var.target!)
        Interceptor::Pipe.unwrap!(:stdout)
        Interceptor::Pipe.unwrap!(:stderr)
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = false;Fi[	@	i=I"À        return unless @var
        var ||= @var
        progress(var) unless table_header_cell?(var)
      end
      
      def function(arglist)
        @var = true
      end
      
      private
      ;Fi[	@˚iüI"ƒ      # :nodoc:
      def function(arglist)
        @var ||= []
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var ||= false;Fi[@@@I"private;TI" ;TI"def function(arglist);Ti[[	@Õi˝I"    def function(arglist)
      var = to_a.take([limit_value, 11].compact.min).map!(&:inspect)
      var[10] = "STRING" if (var.size == 11)
      "STRING"
    end
    
    private
    
    def function(arglist)
      var = with_default_scope
      if var.equal?(self) then;Ti[	@ˆi$I"ã    end
    
    def function(arglist)
      "STRING"
    end
    
    private
    
    def function(arglist)
      @var.send(*var)
    end;Fi[	@¸iTI"∏      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        @var ||= (var = {}
        ALL.each do |arglist|;Fi[	@ˇiôI"≠      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        @var = true
        @var = @var = nil;Fi[	@'iEI"∏        end
        
        def function(arglist)
          "STRING"
        end
        
        private
        
        def function(arglist)
          @var.index(self)
        end;Fi[	@©iFI"≈      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        if var then
          Dir.glob("STRING").collect do |arglist|;Fi[	@ªiyI"°      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        "STRING".to_sym
      end;Fi[	@—iI"°      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        "STRING".to_sym
      end;Fi[	@ﬂi"I"”      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        var.all? { |arglist| var.respond_to?(:<=>) } ? (var.sort) : (var)
      end;Fi[	@‰iBI"ß      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        case @var
        when nil then;Fi[	@Ûi/I"µ      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist)
        @var.nil? ? ("STRING") : ("STRING")
      end;Fi[@I"	else;FI""STRING";FI"end;FI"end;FI"end;Fi[[	@õi:I"Ó          else
            if table_exists? then
              var = (columns.map { |arglist| "STRING" } * "STRING")
              "STRING"
            else
              "STRING"
            end
          end
        end
      end
      ;Fi[	I"9data//activerecord_proj/active_record/integration.rb;Ti7I"¥        "STRING"
      when var = self[:updated_at] then
        var = var.utc.to_s(cache_timestamp_format)
        "STRING"
      else
        "STRING"
      end
    end
  end
end;Fi[	@ß
iI"       
      def function(arglist)
        if (top_level.preferred_syntax == :scss) and top_level.sass_dir then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[	@·i I"˙              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              else
                "STRING"
              end
            end
          end
        else
          if @var.html4? then;Fi[	@·iI"œ            when "STRING" then
              "STRING"
            when "STRING" then
              "STRING"
            else
              "STRING"
            end
          end
        end
      end
    end;Fi[	I"'data//homebrew_proj/macos/xcode.rb;TiqI"¶        when 40 then
          "STRING"
        when 41 then
          "STRING"
        else
          "STRING"
        end
      end
  end
  
  def function(arglist);Fi[	I"(data//jekyll_proj/jekyll/filters.rb;Ti|I"Ä      when 1 then
        var[0].to_s
      when 2 then
        "STRING"
      else
        "STRING"
      end
    end
  end
end;Fi[	@µ
iRI"û      def function(arglist)
        case var
        when Hash then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[	@D
iQI"´            end
          end
        end
        "STRING"
      else
        "STRING"
      end
    end
  end
  
  ALLOWED_TAGS = ["STRING", "STRING", "STRING", "STRING"];Fi[	@´i3I"        "STRING"
      else
        if ((defined? Gem and var.filename.include?(Gem.dir)) or var.filename =~ /\/bin\/(\w+)$/) then
          "STRING"
        else
          "STRING"
        end
      end
    end
    
    TEMPLATE = "STRING";Fi[	@æ
iI"•        when /\.gif$/i then
          "STRING"
        when /\.png$/i then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[I"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");Fi[[	I"Mdata//redmine_proj/generators/redmine_plugin/redmine_plugin_generator.rb;TiI"  end
  
  def function(arglist)
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"(  
  def function(arglist)
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"C  def function(arglist)
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"I    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"I    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"I    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"I    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"I    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"I    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@+iI"L    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    template("STRING", "STRING");Fi[	@+iI"O    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    template("STRING", "STRING")
    template("STRING", "STRING");Fi[@@@@@I" ;Fi[[	@„i,I"-        when Symbol then
          "STRING"
        when Class then
          "STRING"
        else
          "STRING"
        end
      end
      
      # Cast a +value+ to a type that the database understands. For example,
      # SQLite does not understand dates, so this method will convert a Date;Fi[	@’iI"‰    
    def function(arglist)
      if ((platform == Gem::Platform::RUBY) or platform.nil?) then
        "STRING"
      else
        "STRING"
      end
    end
    
    def function(arglist)
      (identifier == var.identifier);Fi[	I"7data//bundler_proj/bundler/remote_specification.rb;TiI"7    
    def function(arglist)
      if ((platform == Gem::Platform::RUBY) or platform.nil?) then
        "STRING"
      else
        "STRING"
      end
    end
    
    # Because Rubyforge cannot be trusted to provide valid specifications
    # once the remote gem is downloaded, the backend specification will;Fi[	I"Edata//capistrano_proj/capistrano/recipes/deploy/scm/mercurial.rb;TicI"Ù          when nil then
            nil
          when false then
            "STRING"
          else
            "STRING"
          end
        end
        
        # honor Cap 2.1+'s :scm_prefer_prompt if present
        def function(arglist);Fi[	I"%data//jekyll_proj/jekyll/page.rb;TiBI"    # Returns the template String.
    def function(arglist)
      if (self.site.permalink_style == :pretty) and (index?.! and html?) then
        "STRING"
      else
        "STRING"
      end
    end
    
    # The generated relative url of this page. e.g. /about.html.
    #;Fi[	@àiäI"¯      
      def function(arglist)
        if @var[:fog_directory].to_s.=~(Fog::AWS_BUCKET_SUBDOMAIN_RESTRICTON_REGEX) then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        case var;Fi[	@iI"Ω        when 1 then
          "STRING"
        when 2 then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        var = "STRING";Fi[	@‰iLI"’        when nil then
          @var
        when Regexp then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        @var.nil? ? ("STRING") : ("STRING");Fi[	@L
iœI"%      
      def function(arglist)
        if ([/mswin/, /java/].detect { |arglist| var.=~(RUBY_PLATFORM) } or (Spec::Ruby.version.to_f == 1.9)) then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        @var ||= [["STRING", @var]];Fi[	@É	iI"µ        when Hash then
          redirect_from_hash
        when NilClass then
          "STRING"
        else
          "STRING"
        end
      end
      
      protected
      ;Fi[	@É	i5I"œ        when (stdout == "STRING") then
          "STRING"
        when stdout then
          "STRING"
        else
          "STRING"
        end
      end
      
      def function(arglist)
        "STRING";Fi[@T@U@V@WI"end;FI"end;Fi[[	@DiâI"ô          case var
          when "STRING" then
            var
          else
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	@õi;I"?            if table_exists? then
              var = (columns.map { |arglist| "STRING" } * "STRING")
              "STRING"
            else
              "STRING"
            end
          end
        end
      end
      
      # Overwrite the default class equality method to provide support for association proxies.;Fi[	@i8I"£      when var = self[:updated_at] then
        var = var.utc.to_s(cache_timestamp_format)
        "STRING"
      else
        "STRING"
      end
    end
  end
end;Fi[	@ÉiUI"ò            "STRING"
          when false then
            nil
          else
            "STRING"
          end
        end
      end
    end
  end
end;Fi[	@ß
iI"√      def function(arglist)
        if (top_level.preferred_syntax == :scss) and top_level.sass_dir then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[	@·iI"µ              "STRING"
            when "STRING" then
              "STRING"
            else
              "STRING"
            end
          end
        end
      end
    end
    ;Fi[	@i}I"s        var[0].to_s
      when 2 then
        "STRING"
      else
        "STRING"
      end
    end
  end
end;Fi[	@¨
iI"          var = File.read(@var)
          var = Liquid::Template.parse(var)
          var.stack { |arglist| var.render(var) }
        else
          "STRING"
        end
      end
    end
  end
end
Liquid::Template.register_tag("STRING", Jekyll::IncludeTag);Fi[	@µ
iSI"Ç        case var
        when Hash then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[	I"=data//rspec_proj/spec/matchers/generated_descriptions.rb;TiI"¥    def self.last_description(arglist)
      if last_matcher.respond_to?(:description) then
        last_matcher.description
      else
        "STRING"
      end
    end
  end
end;Fi[	@æ
i I"â          "STRING"
        when /\.png$/i then
          "STRING"
        else
          "STRING"
        end
      end
    end
  end
end;Fi[I"execute("STRING");FI"end;FI" ;FI"def function(arglist);FI"execute("STRING");FI"end;Fi[[	@Îi#I"∫      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist);Fi[	@Îi'I"Ú      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      # In the simple case, MySQL allows us to place JOINs directly into the UPDATE;Fi[	@Îi¥I"Ë      # Example:
      #   rename_table('octopuses', 'octopi')
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist);Fi[	@Îi«I"›      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      # Maps logical Rails types to MySQL-specific data types.;Fi[	@ïi¥I"–        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist);Fi[	@ïi∏I"ª        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
      end
    end;Fi[	@öiI"˘        # Renames a column in a table.
        def function(arglist)
          clear_cache!
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist);Fi[	@öiI"–        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist);Fi[	@i-I"∫      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist);Fi[	@i1I"≠      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      #:nodoc:;Fi[@(@Ô
@
I" ;FI"private;FI" ;Fi[[	I"9data//activemodel_proj/active_model/serialization.rb;Ti6I"®        else
          var.serializable_hash(var)
        end
      end
      var
    end
    
    private
    
    (alias :read_attribute_for_serialization :send)
    ;Fi[	@iI">        else
          var = join_table.compile_insert(join_table[reflection.foreign_key] => (owner.id), join_table[reflection.association_foreign_key] => (var.id))
          owner.connection.insert(var)
        end
        var
      end
      
      private
      
      def function(arglist)
        load_target.size;Fi[	I"Ndata//activerecord_proj/active_record/associations/through_association.rb;TiI"°        var = super
        chain[(1..-1)].each do |arglist|
          var = var.merge(var.klass.all.with_default_scope.except(:select, :create_with, :includes, :preload, :joins, :eager_load))
        end
        var
      end
      
      private
      
      # Construct attributes for :through pointing to owner and associate. This is used by the
      # methods which create and delete records on the association.;Fi[	I":data//activerecord_proj/active_record/associations.rb;Ti©I":        var = self.class.reflect_on_association(var)
        var = var.association_class.new(self, var)
        association_instance_set(var, var)
      end
      var
    end
    
    private
    
    # Returns the specified association instance if it responds to :loaded?, nil otherwise.
    def function(arglist);Fi[	@ci¡I"™          end
        end
        var
      end
      var
    end
    
    private
    
    def function(arglist)
      @var ||= (model_class and model_class.primary_key);Fi[	@viRI"        @var.each do |arglist|
          var[var] = var unless [Array, Range, Hash].include?(var.class)
        end
      end
      var
    end
    
    private
    
    def function(arglist)
      var = (var.kind_of?(Hash) ? (var.values.first) : (var)).class;Fi[	@"iNI"•    var = self
    while var.is_a?(Reshare) do
      var = var.root
    end
    var
  end
  
  private
  
  def function(arglist)
    var = Webfinger.new(@var).fetch;Fi[	@oi`I"ﬁ        var = Pathname.pwd.join(var).relative_path_from(HOMEBREW_REPOSITORY)
        (var[var] << var.to_s)
      end
    end
    var
  end
  
  private
  
  def function(arglist)
    `git rev-parse -q --verify HEAD`.chomp;Fi[	@ÀiáI"ı        var = clone
        if klass.default_scopable? and (unscoped?.! and scoped?.!) then
          var.apply_default_scope
        end
        var
      end
      
      private
      
      def function(arglist)
        var.each do |arglist|;Fi[	I",data//mongoid_proj/lib/mongoid/dirty.rb;TiíI"À            var[var] = var unless atomic_unsets.include?(var)
          end
        end
      end
      var
    end
    
    private
    
    # Get the old and new value for the provided attribute.
    #;Fi[@
@@¯I"@var = var;FI"end;FI" ;Fi[[	@iI"Ê      
      class Reaper
        attr_reader(:pool, :frequency)
        
        def function(arglist)
          @var = var
          @var = var
        end
        
        def function(arglist)
          return unless frequency;Fi[	@iI"Î      
      class Resolver
        attr_reader(:config, :klass, :configurations)
        
        def function(arglist)
          @var = var
          @var = var
        end
        
        def function(arglist)
          case config;Fi[	@¯i	I"Ê  module ConnectionAdapters
    class StatementPool
      include(Enumerable)
      
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        raise(NotImplementedError);Fi[	@ci˜I"¬    end
    
    attr_reader(:model_class, :fixture)
    
    def function(arglist)
      @var = var
      @var = var
    end
    
    def function(arglist)
      model_class.name if model_class;Fi[	I")data//backup_proj/backup/splitter.rb;TiI"€module Backup
  class Splitter
    include(Backup::CLI::Helpers)
    
    def function(arglist)
      @var = var
      @var = var
    end
    
    ##
    # This is called as part of the procedure used to build the final;Fi[	@˝i(I"˙      def self.matches_condition?(arglist)
        raise(NotImplemented, "STRING")
      end
      
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        raise(NotImplemented, "STRING");Fi[	@ÄisI"            var = (var[3] or "STRING")
            InternalRevision.new(var, var)
          end
          
          def function(arglist)
            @var = var
            @var = var
          end
          
          def function(arglist)
            (@var == "STRING");Fi[	@iI"D      def self.ctx(arglist)
        new(*var).send(:get_binding)
      end
      
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        (config.http_stylesheets_path or (config.default_for(:http_stylesheets_path) or config.http_root_relative(config.css_dir)));Fi[	@i
I"∑  include(PostsHelper)
  
  include(ActionView::Helpers::TextHelper)
  
  def function(arglist)
    @var = var
    @var = var
  end
  
  def function(arglist)
    as_json(var).to_json;Fi[	@iI"require(Rails.root.join("lib", "template_picker"))
class PostPresenter
  attr_accessor(:post, :current_user)
  
  def function(arglist)
    @var = var
    @var = var
  end
  
  def self.collection_json(arglist)
    var.map { |arglist| PostPresenter.new(var, var) };Fi[@
@I"@var = false;FI"end;FI" ;FI"def function(arglist);Fi[[	I"6data//compass_proj/compass/installers/manifest.rb;TikI"∂      attr_reader :function
      
      protected
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        @var = false
      end;Fi[	@“ioI"√      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        begin
          (@var = var;Fi[	@Ωi7I"ˇ      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        raise(UnNamedFeatureError.new(@var.file)) if var.empty?
        var = var.split(/\r?\n/);Fi[	@ΩiYI"…        @var = true
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        return unless @var
        @var = Time.now;Fi[	@	iI"“        @var.puts
        print_summary(var)
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        progress(:failed) if @var
        @var = false;Fi[	@	i'I"Œ        progress(:failed) if @var
        @var = false
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        progress(var)
        @var = var;Fi[	I"4data//cucumber_prok/cucumber/formatter/rerun.rb;Ti"I"      def function(arglist)
        @var.close
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        if (@var or var.failed?) and Ast::ScenarioOutline.===(var).! then
          (@var << var.line);Fi[	@ﬂi<I"ª        @var = true
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        return unless @var
      end;Fi[	@6
i±I"      @var = nil
      @var = nil
    end
    
    def function(arglist)
      @var = false
    end
    
    def function(arglist)
      @var.pop if ["STRING", "STRING", "STRING"].include?(var.value[:keyword])
      return unless (var.value[:keyword] == "STRING");Fi[	@L
iI"Õ      end
      
      attr_reader :function
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        Spec::Runner.configuration.mock_framework
      end;Fi[@(@œ@–@¡
I"	self;FI"end;Fi[[	I"Hdata//capistrano_proj/capistrano/recipes/deploy/local_dependency.rb;TiI"¡        @var ||= "STRING"
        @var = find_in_path(var)
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      attr_reader :function;Fi[	@iRI"©          @var = false
        end
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      attr_reader :function;Fi[	@ùiI"Ê      class ValidateAttachmentSizeMatcher
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist);Fi[	@ùiI"À        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist);Fi[	@Œi5I"≠      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist);Fi[	@Œi:I"µ      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist);Fi[	@Œi?I"µ      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist);Fi[	@ŒiDI"µ      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist);Fi[	@ŒiII"µ      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist);Fi[	@Èi I"≠      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist);Fi[@@ÏI""STRING";F@ÓI""STRING";FI"when "STRING" then;Fi[[	@ÚiI"3        if var.is_a?(Sass::Script::String) then
          var = case var.value
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÚiI"          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else;Fi[	@ÚiEI"      var = var.value.split(/ +/).map do |arglist|
        case var
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then;Fi[	@ÚiGI"Ú        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        else;Fi[	@·i¯I">            else
              case @var.value[:type]
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then;Fi[	@·i˙I"4              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              else;Fi[	@vifI"*          when "STRING", "STRING" then
            "STRING"
          when "STRING", "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@vihI"           when "STRING", "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@vijI"          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@vilI"          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else;Fi[I"(instance_eval(&var) if block_given?;FI"end;FI" ;FI"private;FI" ;FI"##;Fi[[	@Œ	iI"      # Campfire account's room id
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	I"1data//backup_proj/backup/notifier/hipchat.rb;Ti/I"        @var ||= "STRING"
        @var ||= "STRING"
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	I".data//backup_proj/backup/notifier/mail.rb;TinI"      # Example: '/tmp/test-mails'
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	I"/data//backup_proj/backup/notifier/prowl.rb;TiI":      # Create a Prowl account and request an API key on prowlapp.com.
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	I"2data//backup_proj/backup/notifier/pushover.rb;TiI"      # The priority of the notification
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	I"1data//backup_proj/backup/notifier/twitter.rb;TiI"      # OAuth credentials
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	I"3data//backup_proj/backup/storage/cloudfiles.rb;TiI"        super(var, var)
        @var ||= false
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # This is the provider that Fog uses for the Cloud Files Storage
      def function(arglist);Fi[	I"0data//backup_proj/backup/storage/dropbox.rb;Ti$I"=        super(var, var)
        @var ||= "STRING"
        @var ||= :app_folder
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # The initial connection to Dropbox will provide the user with an
      # authorization url. The user must open this URL and confirm that the;Fi[	I"1data//backup_proj/backup/storage/ninefold.rb;TiI"      def function(arglist)
        super(var, var)
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # This is the provider that Fog uses for the Ninefold storage
      def function(arglist);Fi[	I"+data//backup_proj/backup/storage/s3.rb;TiI"      def function(arglist)
        super(var, var)
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # This is the provider that Fog uses for the S3 Storage
      def function(arglist);Fi[@
@@ÇI"end;FI" ;F@|i[[	@Îi!I"∫          # do nothing
        end
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end;Fi[	@Îi%I"Õ      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end;Fi[	@Îi∂I"      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        var = column_for(var, var)
        change_column(var, var, var.sql_type, :default => (var));Fi[	@Îi≈I"        execute("STRING") unless (var or var.nil?)
        change_column(var, var, var.sql_type, :null => (var))
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end;Fi[	@ïi≤I"          ActiveSupport::Deprecation.warn(var)
          (@var.transaction_status == PGconn::PQTRANS_IDLE)
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end;Fi[	@ïi∂I"„        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end;Fi[	@öi
I"‹          clear_cache!
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end;Fi[	@öiI"‘        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          63
        end;Fi[	@i+I"’      def function(arglist)
        exec_query(var, var).rows
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end;Fi[	@i/I"Õ      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end;Fi[@
@@@@I"private;Fi[[	@ˆi"I"û    def function(arglist)
      @var.requirement
    end
    
    def function(arglist)
      "STRING"
    end
    
    private
    
    def function(arglist);Fi[	@¸iRI"≠      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist);Fi[	@ˇióI"∏          Bundler.cache.join("STRING", var)
        end)
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist);Fi[	@'iCI"∆        def function(arglist)
          @var[0].line
        end
        
        def function(arglist)
          "STRING"
        end
        
        private
        
        def function(arglist);Fi[	@©iDI"¨      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist);Fi[	@ªiwI"¨      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist);Fi[	@—iI"¨      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist);Fi[	@ﬂi I"¨      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist);Fi[	@‰i@I"¨      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist);Fi[	@Ûi-I"¬      def function(arglist)
        @var ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        "STRING"
      end
      
      private
      
      def function(arglist);Fi[@
@@¯I"	self;FI"end;FI" ;Fi[[	@ÏiI"Æ        @var = find_in_path(var)
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      attr_reader :function
      ;Fi[	@iSI"ô        end
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      attr_reader :function
      ;Fi[	@ùiI"—        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist)
          @var = var;Fi[	@ùiI"ÿ          @var = var
          self
        end
        
        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist)
          @var, @var = var.first, var.last;Fi[	@Œi6I"π      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var;Fi[	@Œi;I"¨        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var;Fi[	@Œi@I"¨        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var;Fi[	@ŒiEI"¨        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var;Fi[	@ŒiJI"™        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        "STRING";Fi[	@Èi!I"≥      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        self;Fi[I"var = -1;FI"var = var;FI"var = 0;FI"if (var == 1) then;FI"@var = @var;FI"!var = ((@var - @var) - @var);Fi[[	@ñiΩI"O      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@ñi€I"b          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@ôi∏I"E      if (var == 10) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@ôi„I"b          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@úi¨I"O      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@úi I"b          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@üiöI"Q      if (var == "STRING"[0]) then
        self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@üiªI">          self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
          var = (var + 1)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@àiæI"<      if (var == "STRING"[0]) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (var - (3 * @var))
        end;Fi[	@ài‹I")          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var + 1)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (var - (3 * @var))
        end;Fi[@Æ@ÌI"	self;FI"end;FI" ;FI"def function(arglist);Fi[[	@ùiI"¬          @var = var
        end
        
        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist)
          @var = var
          self;Fi[	@ùi I"“          self
        end
        
        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist)
          @var, @var = var.first, var.last
          self;Fi[	I"3data//rspec_proj/spec/example/example_proxy.rb;Ti I"3      
      # Convenience method for example group - updates the value of
      # <tt>description</tt> and returns self.
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        (var.description == description).&((var.location == location))
      end;Fi[	@Œi7I"™        "STRING"
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self;Fi[	@Œi<I"¶        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self;Fi[	@ŒiAI"¶        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self;Fi[	@ŒiFI"¶        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self;Fi[	@ŒiKI"°        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@Èi"I"°        "STRING"
      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        self
      end;Fi[I")#   matcher.matches?({ :key => 10 });FI"#;FI"1# @param [ Hash ] value The values to check.;FI"#;FI"1# @return [ true, false ] If a value exists.;FI"def function(arglist);Fi[[	I"6data//mongoid_proj/lib/mongoid/matchers/exists.rb;TiI"2      # non-existence.
      #
      # @example Does anything exist?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        @var.nil?.!=(var.values.first)
      end;Fi[	I"2data//mongoid_proj/lib/mongoid/matchers/gt.rb;TiI"O      # Return true if the attribute is greater than the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        determine(var, :>)
      end;Fi[	I"3data//mongoid_proj/lib/mongoid/matchers/gte.rb;TiI"\      # Return true if the attribute is greater than or equal to the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        determine(var, :>=)
      end;Fi[	I"2data//mongoid_proj/lib/mongoid/matchers/in.rb;TiI"ê      # Return true if the attribute is in the values.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        var = Array.wrap(@var)
        var.values.first.any? { |arglist| var.any? { |arglist| var.===(var) } };Fi[	I"2data//mongoid_proj/lib/mongoid/matchers/lt.rb;TiI"L      # Return true if the attribute is less than the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        determine(var, :<)
      end;Fi[	I"3data//mongoid_proj/lib/mongoid/matchers/lte.rb;TiI"Y      # Return true if the attribute is less than or equal to the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        determine(var, :<=)
      end;Fi[	I"2data//mongoid_proj/lib/mongoid/matchers/ne.rb;TiI"Z      # Return true if the attribute and first value are not equal.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        @var.!=(var.values.first)
      end;Fi[	I"3data//mongoid_proj/lib/mongoid/matchers/nin.rb;TiI"      # Return true if the attribute is not in the value list.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        Array.wrap(@var).none? { |arglist| var.values.first.include?(var) }
      end;Fi[	I"4data//mongoid_proj/lib/mongoid/matchers/size.rb;TiI"c      # Return true if the attribute size is equal to the first value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        (@var.size == var.values.first)
      end;Fi[I"# @return [ nil ] Nil.;FI"#;FI"# @since 3.0.0;FI"def function(arglist);FI"Jquery.update_all("STRING" => ({ database_field_name(var) => (var) }));FI"end;Fi[[	I"8data//mongoid_proj/lib/mongoid/contextual/atomic.rb;TiI"Ç      # @param [ String, Symbol ] field The name of the field to add to.
      # @param [ Object ] value The single value to add.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $bit operation on the matching documents.;Fi[	@Ái I"É      # @param [ Hash ] value The bitwise operations to perform. Keys may be
      #   "and" or "or" and must have numeric values.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $inc operation on the matching documents.;Fi[	@Ái/I"}      # @param [ String, Symbol ] field The field to increment.
      # @param [ Integer ] value The amount to increment by.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $pop operation on the matching documents.;Fi[	@ÁiCI"]      # @param [ Integer ] value 1 to pop from the end, -1 to pop from the
      #   front.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $pull operation on the matching documents.;Fi[	@ÁiTI"~      # @param [ String, Symbol ] field The field to pull from.
      # @param [ Object ] value The single value to pull.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $pullAll operation on the matching documents.;Fi[	@ÁicI"}      # @param [ String, Symbol ] field The field to pull from.
      # @param [ Array<Object> ] values The values to pull.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $push operation on the matching documents.;Fi[	@ÁirI"u      # @param [ String, Symbol ] field The field to push to.
      # @param [ Object ] value The value to push.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $pushAll operation on the matching documents.;Fi[	@Ái|I"}      # @param [ String, Symbol ] field The field to push to.
      # @param [ Array<Object> ] values The values to push.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $rename of fields on the matching documents.;Fi[	@ÁiöI"t      # @param [ String, Symbol ] field The name of the field.
      # @param [ Object ] value The value to set.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $unset of a field on the matching documents.;Fi[I" #   person.pets.size # => 3;TI"#   person.pets;TI"#   # => [;TI"A#   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;TI";#   #       #<Pet id: 2, name: "Spook", person_id: 1>,;TI">#   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[[	@&i<I"z      #     has_many :pets # dependent: :nullify option by default
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&i]I"g      #     has_many :pets, dependent: :destroy
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&ivI"j      #     has_many :pets, dependent: :delete_all
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&iìI"R      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&i≥I"z      #     has_many :pets # dependent: :nullify option by default
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&iœI"g      #     has_many :pets, dependent: :destroy
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&iÎI"j      #     has_many :pets, dependent: :delete_all
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&iI"R      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&i#I"R      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[@
I"#alias_type("STRING", "STRING");FI" ;FI"#alias_type("STRING", "STRING");FI" ;FI"#alias_type("STRING", "STRING");Fi[[	@˛i I"        end
        
        register_type("STRING", OID::Integer.new)
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        register_type("STRING", OID::Decimal.new);Fi[	@˛i‘I"-        register_type("STRING", OID::Decimal.new)
        
        register_type("STRING", OID::Identity.new)
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING");Fi[	@˛i÷I""        register_type("STRING", OID::Identity.new)
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING");Fi[	@˛iÿI"        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING");Fi[	@˛i⁄I"        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING");Fi[	@˛i‹I"        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING");Fi[	@˛iﬁI"        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING");Fi[	@˛i‡I"        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING");Fi[	@˛i‚I""        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        register_type("STRING", OID::Identity.new);Fi[@
@I"@var[var];FI"end;FI" ;FI"def function(arglist);Fi[[	@˛i™I"Ï          def function(arglist)
            @var[var] = var
          end
          
          def function(arglist)
            @var[var]
          end
          
          def function(arglist)
            @var.key?(var)
          end;Fi[	@˘i}I"∫    end
    
    private
    
    def function(arglist)
      @var[var]
    end
    
    def function(arglist)
      @var[(var or false)] ||= {}
      @var[(var or false)][var] ||= begin;Fi[	@¸iNI"¿      def function(arglist)
        @var ||= first.source
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        "STRING"
      end;Fi[	@i|I"Õ      def function(arglist)
        (@var.nil? or @var.active?)
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var[var] = var
      end;Fi[	I"1data//cucumber_prok/cucumber/ast/features.rb;TiI"ª      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var.each(&var)
      end;Fi[	@'i;I"‘        def function(arglist)
          self[var].value
        end
        
        def function(arglist)
          @var[var]
        end
        
        def function(arglist)
          @var[0].line
        end;Fi[	I"0data//cucumber_prok/cucumber/cli/options.rb;Ti.I"∆        @var = default_options
        @var = @var = nil
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var[var] = var
      end;Fi[	@
	i›I"ˆ        var[(1..-1)].each { |arglist| @var.puts("STRING") }
        @var.flush
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        print_stats(var, @var)
        print_snippets(@var);Fi[	@iâI"        def function(arglist)
          @var[var] = var
        end
        
        def function(arglist)
          @var[var]
        end
        
        def function(arglist)
          var = var.last.is_a?(Hash) ? (var.pop) : ({})
          case var.to_s;Fi[@(@œ@–I"@var = false;FI"end;FI" ;Fi[[	@Ëi^I"      def function(arglist)
        var.connection = self
        @var = var
      end
      
      def function(arglist)
        @var = false
      end
      
      # Returns the human-readable name of the adapter. Use mixed case - one
      # can always use downcase if needed.;Fi[	@“inI"¥      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        begin;Fi[	@Ωi6I"Â      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        raise(UnNamedFeatureError.new(@var.file)) if var.empty?;Fi[	@ΩiXI"Õ      def function(arglist)
        @var = true
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        return unless @var;Fi[	@	iI"œ        @var.puts
        @var.puts
        print_summary(var)
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        progress(:failed) if @var;Fi[	@	i&I"◊      def function(arglist)
        progress(:failed) if @var
        @var = false
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        progress(var);Fi[	@ﬂi!I"Ó      
      def function(arglist)
        @var.close
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        if (@var or var.failed?) and Ast::ScenarioOutline.===(var).! then;Fi[	@ﬂi;I"Õ      def function(arglist)
        @var = true
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist)
        return unless @var;Fi[	@6
i∞I"‰      @var = false
      @var = nil
      @var = nil
    end
    
    def function(arglist)
      @var = false
    end
    
    def function(arglist)
      @var.pop if ["STRING", "STRING", "STRING"].include?(var.value[:keyword]);Fi[I"#;FI"$# @example Do the values match?;F@ºI"#;F@æI"#;Fi[[	I"3data//mongoid_proj/lib/mongoid/matchers/all.rb;Ti	I"b  module Matchers
    class All < Default
      # Return true if the attribute and first value in the hash are equal.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If the values match.
      def function(arglist);Fi[	@«i	I"S  module Matchers
    class Gt < Default
      # Return true if the attribute is greater than the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist);Fi[	@ i	I"`  module Matchers
    class Gte < Default
      # Return true if the attribute is greater than or equal to the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist);Fi[	@Õi	I"J  module Matchers
    class In < Default
      # Return true if the attribute is in the values.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist);Fi[	@–i	I"P  module Matchers
    class Lt < Default
      # Return true if the attribute is less than the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist);Fi[	@”i	I"]  module Matchers
    class Lte < Default
      # Return true if the attribute is less than or equal to the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist);Fi[	@÷i	I"W  module Matchers
    class Ne < Default
      # Return true if the attribute and first value are not equal.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist);Fi[	@Ÿi	I"S  module Matchers
    class Nin < Default
      # Return true if the attribute is not in the value list.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist);Fi[	@‹i	I"\  module Matchers
    class Size < Default
      # Return true if the attribute size is equal to the first value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist);Fi[@^I"# @since 3.0.0;FI"def function(arglist);F@„I"end;FI" ;Fi[[	@ÁiI"A      # @param [ Object ] value The single value to add.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $bit operation on the matching documents.
      #;Fi[	@Ái!I">      #   "and" or "or" and must have numeric values.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $inc operation on the matching documents.
      #;Fi[	@Ái0I"E      # @param [ Integer ] value The amount to increment by.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $pop operation on the matching documents.
      #;Fi[	@ÁiDI"      #   front.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $pull operation on the matching documents.
      #;Fi[	@ÁiUI"F      # @param [ Object ] value The single value to pull.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $pullAll operation on the matching documents.
      #;Fi[	@ÁidI"E      # @param [ Array<Object> ] values The values to pull.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $push operation on the matching documents.
      #;Fi[	@ÁisI"?      # @param [ Object ] value The value to push.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $pushAll operation on the matching documents.
      #;Fi[	@Ái}I"G      # @param [ Array<Object> ] values The values to push.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $rename of fields on the matching documents.
      #;Fi[	@ÁiõI"=      # @param [ Object ] value The value to set.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      
      # Perform an atomic $unset of a field on the matching documents.
      #;Fi[@(@œ@–I"@var[var];FI"end;FI" ;Fi[[	@˛i©I"È          
          def function(arglist)
            @var[var] = var
          end
          
          def function(arglist)
            @var[var]
          end
          
          def function(arglist)
            @var.key?(var);Fi[	@¸iMI"Ω      
      def function(arglist)
        @var ||= first.source
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        "STRING";Fi[	@i{I"       
      def function(arglist)
        (@var.nil? or @var.active?)
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var[var] = var;Fi[	@;iI"∏      
      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var.each(&var);Fi[	@'i:I"—        
        def function(arglist)
          self[var].value
        end
        
        def function(arglist)
          @var[var]
        end
        
        def function(arglist)
          @var[0].line;Fi[	@@i-I"Œ        @var = []
        @var = default_options
        @var = @var = nil
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var[var] = var;Fi[	@
	i‹I"Î        @var.puts
        var[(1..-1)].each { |arglist| @var.puts("STRING") }
        @var.flush
      end
      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        print_stats(var, @var);Fi[	@iàI"Ú        
        def function(arglist)
          @var[var] = var
        end
        
        def function(arglist)
          @var[var]
        end
        
        def function(arglist)
          var = var.last.is_a?(Hash) ? (var.pop) : ({});Fi[	I";data//rspec_proj/spec/example/example_group_factory.rb;Ti@I"Ã          var = @var
          @var = Hash.new(var)
          @var.merge!(var) if var
        end
        
        def function(arglist)
          @var[var]
        end
        
        protected
        ;Fi[@
@@¯@≤I"@var = var;FI"@var = var;Fi[[	@mi
I"6    class Preloader
      class Association
        attr_reader(:owners, :reflection, :preload_scope, :model, :klass)
        
        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var
          @var = (var.first and var.first.class)
          @var = nil;Fi[	@»
iI"     attr_reader(:name, :version, :platform, :dependencies)
    
    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
    end
    ;Fi[	@`i	I"∫module Bundler
  class Graph
    GRAPH_NAME = :Gemfile
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
      @var = var
      @var = [];Fi[	@IiI"ª    attr_reader(:name, :version, :platform)
    
    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
    end
    ;Fi[	I"7data//bundler_proj/bundler/source/git/git_proxy.rb;TiI"        attr_accessor(:path, :uri, :ref)
        
        attr_writer(:revision)
        
        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var
          @var = (var or Proc.new { |arglist| true })
        end;Fi[	@i!I"æ    attr_reader(:logger)
    
    attr_reader(:transfers)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
      @var = var
      @var = var;Fi[	I"Edata//compass_proj/compass/sass_extensions/sprites/sprite_map.rb;Ti"I"S            return var.gsub("STRING", "STRING") if var.include?(var)
          end
        end
        
        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var
          @var["STRING"] ||= Sass::Script::Bool.new(true)
          @var["STRING"] ||= Sass::Script::String.new("STRING");Fi[	I".data//cucumber_prok/cucumber/rake/task.rb;Ti'I"¯      
      class ForkedCucumberRunner
        include(::Rake::DSL) if defined? ::Rake::DSL
        
        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var
          @var = var
        end;Fi[	I",data//diaspora_proj/mailers/notifier.rb;Ti$I"8      var.html
    end
  end
  
  def function(arglist)
    @var = var
    @var = var
    @var = var
    @var = var
    var = { :to => (var), :from => (AppConfig.mail.sender_address), :subject => (I18n.t("STRING", :name => (@var.name))), :host => (AppConfig.pod_uri.host) }
    I18n.with_locale(var) do |arglist|;Fi[@(@)@Á@Ë@ÈI"##;Fi[[	I"(data//backup_proj/backup/archive.rb;TiUI"
        Logger.message("STRING")
      else
        raise(Errors::Archive::PipelineError, ("STRING" + var.error_messages))
      end
    end
    
    private
    
    ##
    # Returns a "tar-ready" string of all the specified paths combined
    def function(arglist);Fi[	I"1data//backup_proj/backup/database/mongodb.rb;TiKI"’        ensure
          (unlock_database if @var
          package! unless var)
        end
      end
      
      private
      
      ##
      # Builds and runs the mongodump command
      def function(arglist);Fi[	I"/data//backup_proj/backup/database/mysql.rb;TiGI"          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages))
        end
      end
      
      private
      
      ##
      # Builds the full mysqldump string based on all attributes
      def function(arglist);Fi[	@î	i/I"‡            run("STRING")
            FileUtils.rm_f(var)
          end
        end
      end
      
      private
      
      ##
      # Builds the full riak-admin string based on all attributes
      def function(arglist);Fi[	I".data//backup_proj/backup/encryptor/gpg.rb;TidI"”          yield("STRING", "STRING"))
        ensure
          cleanup
        end
      end
      
      private
      
      ##
      # Remove any temporary directories and reset all instance variables.
      #;Fi[	I".data//backup_proj/backup/notifier/base.rb;Ti?I"–        if var then
          log!
          notify!(var)
        end
      end
      
      private
      
      ##
      # Return the notifier name, with Backup namespace removed
      def function(arglist);Fi[	@]iRI"h              Parallel.each(all_file_names, { :in_processes => (var) }, &var)
            else
              raise(Errors::Syncer::Cloud::ConfigurationError, "STRING")
            end
          end
          
          private
          
          ##
          # Gathers all the relative paths to the local files
          # and merges them with the , removing;Fi[	I"2data//backup_proj/backup/syncer/rsync/pull.rb;TiI"‚            end)
          ensure
            remove_password_file!
          end
        end
        
        private
        
        ##
        # Return expanded @path, since this path is local
        def function(arglist);Fi[	I"2data//backup_proj/backup/syncer/rsync/push.rb;Ti5I"˜            run(("STRING" + "STRING")))
          ensure
            remove_password_file!
          end
        end
        
        private
        
        ##
        # Return @path with any preceeding "~/" removed
        def function(arglist);Fi[@^I" #   person.pets.size # => 3;TI"#   person.pets;TI"#   # => [;T@˝@˛i[[	@&i;I"û      #   class Person < ActiveRecord::Base
      #     has_many :pets # dependent: :nullify option by default
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&i\I"ã      #   class Person < ActiveRecord::Base
      #     has_many :pets, dependent: :destroy
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&iuI"é      #   class Person < ActiveRecord::Base
      #     has_many :pets, dependent: :delete_all
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&iíI"v      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&i≤I"û      #   class Person < ActiveRecord::Base
      #     has_many :pets # dependent: :nullify option by default
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&iŒI"ã      #   class Person < ActiveRecord::Base
      #     has_many :pets, dependent: :destroy
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&iÍI"é      #   class Person < ActiveRecord::Base
      #     has_many :pets, dependent: :delete_all
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&iI"v      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&i"I"v      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[@I" ;F@I" ;F@I" ;Fi[[	@˛iÀI"        
        register_type("STRING", OID::Integer.new)
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        register_type("STRING", OID::Decimal.new)
        ;Fi[	@˛i’I"        
        register_type("STRING", OID::Identity.new)
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        ;Fi[	@˛i◊I"¯        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        ;Fi[	@˛iŸI"¯        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        ;Fi[	@˛i€I"¯        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        ;Fi[	@˛i›I"¯        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        ;Fi[	@˛iﬂI"¯        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        ;Fi[	@˛i·I"¯        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        ;Fi[	@˛i„I"        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        alias_type("STRING", "STRING")
        
        register_type("STRING", OID::Identity.new)
        ;Fi[@^@ﬂI"#;FI"# @since 3.0.0;F@‚@„i[[	@ÁiI"F      #
      # @param [ String, Symbol ] field The name of the field to add to.
      # @param [ Object ] value The single value to add.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[	@ÁiI"å      # @param [ String, Symbol ] field The name of the field to operate on.
      # @param [ Hash ] value The bitwise operations to perform. Keys may be
      #   "and" or "or" and must have numeric values.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[	@Ái.I"A      #
      # @param [ String, Symbol ] field The field to increment.
      # @param [ Integer ] value The amount to increment by.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[	@ÁiBI"(      #   from.
      # @param [ Integer ] value 1 to pop from the end, -1 to pop from the
      #   front.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[	@ÁiSI">      #
      # @param [ String, Symbol ] field The field to pull from.
      # @param [ Object ] value The single value to pull.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[	@ÁibI"@      #
      # @param [ String, Symbol ] field The field to pull from.
      # @param [ Array<Object> ] values The values to pull.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[	@ÁiqI"5      #
      # @param [ String, Symbol ] field The field to push to.
      # @param [ Object ] value The value to push.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[	@Ái{I">      #
      # @param [ String, Symbol ] field The field to push to.
      # @param [ Array<Object> ] values The values to push.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[	@ÁiôI"5      #
      # @param [ String, Symbol ] field The name of the field.
      # @param [ Object ] value The value to set.
      #
      # @return [ nil ] Nil.
      #
      # @since 3.0.0
      def function(arglist)
        query.update_all("STRING" => ({ database_field_name(var) => (var) }))
      end
      ;Fi[@T@ã@å@çI" ;FI"def function(arglist);Fi[[	@^i.I"Ì            end
          end
          var
        else
          var
        end
      end
      
      def function(arglist)
        (var << "STRING") if options_include_default?(var)
        (var << "STRING") if (var[:null] == false);Fi[	@i›I"Œ      when Relation then
        var ||= "STRING"
        var.arel.as(var.to_s)
      else
        var
      end
    end
    
    def function(arglist)
      var = var.group_by do |arglist|
        case var;Fi[	I"=data//bundler_proj/bundler/vendor/thor/parser/options.rb;Ti@I"ü        shift
        @var = false
        super
      else
        var
      end
    end
    
    def function(arglist)
      @var = var.dup
      @var = true;Fi[	@1iZI"Û          else
            File.join(var, var[:project_name])
          end
        else
          var
        end
      end
      
      def function(arglist)
        ((var.index(File::SEPARATOR) == 0) or (var.index("STRING") == 1))
      end;Fi[	I"Ddata//compass_proj/compass/sass_extensions/functions/sprites.rb;Ti»I"%      Sass::Script::String.new(reversed_color_names[var.rgb])
    when Sass::Script::Bool then
      Sass::Script::String.new(var.to_s)
    else
      var
    end
  end
  
  def function(arglist)
    unless var.is_a?(Compass::SassExtensions::Sprites::SpriteMap) then
      missing_sprite!(var);Fi[	I"-data//devise_proj/devise/test_helpers.rb;TiNI"’        end
      when Hash then
        _process_unauthenticated(var, var)
      else
        var
      end
    end
    
    def function(arglist)
      var[:action] ||= :unauthenticated
      var = var["STRING"];Fi[	@ziI"√      "STRING"
    when /\d+/ then
      var.to_i
    else
      var
    end
  end
  
  def function(arglist)
    return unless var.is_a?(VersionElement)
    return -1 if string? and var.numeric?;Fi[	@i(I"      if var[:timestamp] and timestamp_possible? then
        var = var.match(/\?.+=/) ? ("STRING") : ("STRING")
        "STRING"
      else
        var
      end
    end
    
    def function(arglist)
      @var.respond_to?(:updated_at) and @var.updated_at.present?
    end;Fi[	@çi6I"Á      def function(arglist)
        if var.is_a?(Range) then
          [:less_than, :less_than_or_equal_to].include?(var) ? (var.max) : (var.min)
        else
          var
        end
      end
      
      def function(arglist)
        var = I18n.translate(:"number.human.storage_units.format", :locale => (options[:locale]), :raise => (true))
        var = I18n.translate(:"number.human.storage_units.units.byte", :locale => (options[:locale]), :count => (var.to_i), :raise => (true));Fi[@T@ã@å@ç@éI"end;Fi[[	I"Kdata//activemodel_proj/active_model/forbidden_attributes_protection.rb;TiI"¡    def function(arglist)
      if var.respond_to?(:permitted?) and var.permitted?.! then
        raise(ActiveModel::ForbiddenAttributesError)
      else
        var
      end
    end
  end
end;Fi[	@Di]I"F                var = ("STRING" == var[1]) ? ((var[2].to_i + 1)) : (var[2].to_i)
                var = ("STRING" == var[5]) ? ((var[4].to_i - 1)) : (var[4].to_i)
                (var..var)
              else
                var
              end
            end
          end
        end
        
        def function(arglist);Fi[	@zi≥I"{          false
        when "STRING" then
          nil
        else
          var
        end
      end
    end
  end
end;Fi[	@ÜiÄI"ô            "STRING"
          when /^\d+/ then
            "STRING"
          else
            var
          end
        end
      end
    end
  end
end;Fi[	@|i?I"        if var.respond_to?(:supports?) and (var.supports?(var) and var.respond_to?(:"to_#{var}")) then
          var.options = options
          var.send(:"to_#{var}")
        else
          var
        end
      end
    end
  end
  
  def function(arglist);Fi[	@äi£I"§            var.backtrace.push(var.backtrace_line("STRING"))
            raise(var)
          end
        else
          var
        end
      end
    end
  end
end;Fi[	@çi'I"û            var = modify_keys(var, &var)
            var.merge!(var => (var))
          end
        else
          var
        end
      end
    end
  end
end;Fi[	@†i0I"•          var = default_scoping.try(:call)
          var.remove_scoping(var)
          var.to_proc
        else
          var
        end
      end
    end
  end
end;Fi[	@æi/I"◊        else
          if var.all? { |arglist| (var == "STRING") } then
            escape_double_quotes(var)
          else
            var
          end
        end
      end
    end
    
    def function(arglist);Fi[I"#   end;FI"#;F@›I"#   person.pets;TI"#   # => [;T@˝i[[	@&i:I"ï      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets # dependent: :nullify option by default
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&i[I"Ç      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets, dependent: :destroy
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&itI"Ö      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets, dependent: :delete_all
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&iëI"m      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&i±I"ï      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets # dependent: :nullify option by default
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&iÕI"Ç      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets, dependent: :destroy
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&iÈI"Ö      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets, dependent: :delete_all
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&iI"m      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&i!I"m      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[@Æ@yI"end;FI" ;F@|@}i[[	@Îi"I"™        end
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      ;Fi[	@Îi&I"∏        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      ;Fi[	@Îi≥I"‘      #
      # Example:
      #   rename_table('octopuses', 'octopi')
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      ;Fi[	@Îi∆I"‹        change_column(var, var, var.sql_type, :null => (var))
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      ;Fi[	@ïi≥I"Ó          (@var.transaction_status == PGconn::PQTRANS_IDLE)
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        ;Fi[	@ïi∑I"œ          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
      end;Fi[	@öiI"Œ          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        
        def function(arglist)
          execute("STRING")
        end
        ;Fi[	@i,I"¿        exec_query(var, var).rows
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      ;Fi[	@i0I"∏        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      
      def function(arglist)
        execute("STRING")
      end
      ;Fi[I"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";Fi[[	@üi*I"    @var = 0
    @var = []
    @var = {}
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@üi+I")    @var = []
    @var = {}
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@üi,I"9    @var = {}
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@üi-I"I    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@üi.I"I    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@üi/I"I    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@üi0I"I    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@üi1I"I    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    if (var == "STRING") then;Fi[	@üi2I":    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    if (var == "STRING") then
      @var = 1;Fi[I"@var = var;FI"end;FI" ;FI"def function(arglist);FI"@var = var;FI"end;Fi[[	@	i.I"∏      
      def function(arglist)
        progress(var)
        @var = var
      end
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist);Fi[	@üiÜI"Ä  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist);Fi[	@üi°I"Ä  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist);Fi[	@üi•I"Ä  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist);Fi[	@üi©I"Ä  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist);Fi[	@üi≠I"Ä  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist);Fi[	@üi±I"Ä  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist);Fi[	@üiµI"Ä  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist);Fi[	@<	iTI"      # class initializer requires any parameters, you must pass in an
      # instance and not the class.
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist);Fi[@(@œ@–I"raise(NotImplementedError);FI"end;FI" ;Fi[[	I"Ndata//activerecord_proj/active_record/associations/builder/association.rb;Ti5I"Ó      @var = model.create_reflection(macro, name, scope, options, model)
      super
      @var
    end
    
    def function(arglist)
      raise(NotImplementedError)
    end
    
    def function(arglist)
      Association.valid_options;Fi[	I"Tdata//activerecord_proj/active_record/associations/join_dependency/join_part.rb;TiI"!        
        def function(arglist)
          Arel::Nodes::TableAlias.new(table, aliased_table_name)
        end
        
        def function(arglist)
          raise(NotImplementedError)
        end
        
        # An Arel::Table for the active_record
        def function(arglist);Fi[	@miI"        
        def function(arglist)
          preload unless owners.first.association(reflection.name).loaded?
        end
        
        def function(arglist)
          raise(NotImplementedError)
        end
        
        def function(arglist)
          @var ||= build_scope;Fi[	@¯iI"·      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError);Fi[	@¯iI"Â      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError);Fi[	@¯iI"Â      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError);Fi[	@¯iI"Â      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError);Fi[	@¯iI"Â      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError);Fi[	@¯i!I"Â      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError);Fi[@(@œI"##;FI"I# Removes the transferred archive file(s) from the storage location.;FI"6# Any error raised will be rescued during Cycling;FI"B# and a warning will be logged, containing the error message.;Fi[[	@3i7I"Ä            connection.put_object(container, File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var);Fi[	@6iYI"s            connection.put_file(File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var);Fi[	@WiCI"~            var.put(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var);Fi[	I".data//backup_proj/backup/storage/local.rb;Ti"I"É          var = File.join(var, var)
          FileUtils.send(transfer_method, var, var)
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var);Fi[	@9i?I"u            var.files.create(:key => (var), :body => (var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var);Fi[	@<i:I"}            connection.put_object(bucket, File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var);Fi[	I",data//backup_proj/backup/storage/scp.rb;Ti7I"Ü            var.scp.upload!(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var);Fi[	@Zi5I"Ç            var.upload!(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var);Fi[@(@)@ÁI"##;F@Ã@Õi[[	@3i6I"§          File.open(File.join(local_path, var), "STRING") do |arglist|
            connection.put_object(container, File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist);Fi[	@6iXI"ó          File.open(File.join(local_path, var), "STRING") do |arglist|
            connection.put_file(File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist);Fi[	@WiBI"ç            Logger.message(("STRING" + "STRING"))
            var.put(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist);Fi[	@◊i!I"ã          var = File.join(local_path, var)
          var = File.join(var, var)
          FileUtils.send(transfer_method, var, var)
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist);Fi[	@9i>I"ô          File.open(File.join(local_path, var), "STRING") do |arglist|
            var.files.create(:key => (var), :body => (var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist);Fi[	@<i9I"°          File.open(File.join(local_path, var), "STRING") do |arglist|
            connection.put_object(bucket, File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist);Fi[	@ﬁi6I"ï            Logger.message(("STRING" + "STRING"))
            var.scp.upload!(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist);Fi[	@Zi4I"ë            Logger.message(("STRING" + "STRING"))
            var.upload!(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist);Fi[@(@œI"desc("STRING", "STRING");FI" ;FI"long_desc("STRING");FI" ;Fi[[	@J	i9I"∂      else
        super
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :string, :banner => "STRING")
    ;Fi[	@J	iWI"˘        puts("STRING")
        FileUtils.cp(File.expand_path("STRING", "STRING"), "STRING")
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :string, :banner => "STRING")
    ;Fi[	@J	iI"Ã          Bundler.ui.info("STRING")
        end
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :array, :banner => "STRING")
    ;Fi[	@J	i€I"Û        Bundler.ui.warn("STRING") if Bundler.definition.no_sources?
        raise(var)
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :array, :banner => "STRING")
    ;Fi[	@J	i˙I"G      Bundler.load.cache if Bundler.root.join("STRING").exist?
      clean if Bundler.settings[:clean] and Bundler.settings[:path]
      Bundler.ui.confirm(("STRING" + "STRING"))
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    ;Fi[	@J	i/I"      else
        var.generate_bundler_executable_stubs(var, :force => (options[:force]), :binstubs_cmd => (true))
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    ;Fi[	@J	iûI"∫        Bundler.ui.error("STRING")
        exit(128)
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    def function(arglist)
      var = var.shift;Fi[	@J	iI"A        var = (var.license or var.licenses)
        var.empty? ? (Bundler.ui.warn("STRING")) : (Bundler.ui.info("STRING"))
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option(:file, :type => :string, :default => "STRING", :aliases => "STRING", :banner => "STRING")
    ;Fi[I"
super;FI"end;FI"end;FI"end;FI"end;FI"end;Fi[[	@>iÇI"æ          if self.class.serialized_attributes.include?(var) then
            @var[var].serialized_value
          else
            super
          end
        end
      end
    end
  end
end;Fi[	@i
I"¢        when /column(s)? .* (is|are) not unique/ then
          RecordNotUnique.new(var, var)
        else
          super
        end
      end
    end
  end
end;Fi[	@æi;I"€              self.class.delegate(var, :to => :arel)
              arel.send(var, *var, &var)
            else
              super
            end
          end
        end
      end
    end
    
    module ClassMethods;Fi[	I"6data//backup_proj/backup/configuration/helpers.rb;TiiI"µ          self.class.log_deprecation_warning(var, var)
          var[:action].call(self, var[0]) if var[:action]
        else
          super
        end
      end
    end
  end
end;Fi[	I">data//mongoid_proj/lib/mongoid/validations/localizable.rb;TiI"∑        if var.try(:localized?) and var.blank?.! then
          var.values.each { |arglist| super(var, var, var) }
        else
          super
        end
      end
    end
  end
end;Fi[	I"Qdata//state_machine_proj/state_machine/integrations/active_model/versions.rb;TiI"∑        end
        def function(arglist)
          owner_class.set_callback(:validation, :after, "STRING", :prepend => (true))
          super
        end
      end
    end
  end
end;Fi[	I"Rdata//state_machine_proj/state_machine/integrations/active_record/versions.rb;TikI"¿          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end
    end
  end
end;Fi[	I"Qdata//state_machine_proj/state_machine/integrations/mongo_mapper/versions.rb;TiSI"¿          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end
    end
  end
end;Fi[I"
begin;FI"((Threaded.begin_execution("STRING");FI"yield);FI"ensure;FI"&Threaded.exit_execution("STRING");FI"end;Fi[[	I":data//mongoid_proj/lib/mongoid/relations/accessors.rb;TièI"›      #
      # @since 3.0.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      ;Fi[	@†i≈I"›      #
      # @since 3.0.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      ;Fi[	I"9data//mongoid_proj/lib/mongoid/threaded/lifecycle.rb;TiI"›      #
      # @since 2.2.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      ;Fi[	@6i8I"›      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      ;Fi[	@6iWI"›      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      ;Fi[	@6i}I"›      #
      # @since 2.3.2
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      ;Fi[	@6iúI"›      #
      # @since 2.3.4
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      ;Fi[	@6i∞I"Û        #
        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield)
          ensure
            Threaded.exit_execution("STRING")
          end
        end
        ;Fi[I"homepage("STRING");FI" ;FI"url("STRING");FI" ;FI"sha1("STRING");FI" ;Fi[[	I")data//homebrew_proj/test/testball.rb;Ti~I"ã  end
end
class ExplicitVersionSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  version("STRING")
  ;Fi[	@KiíI"ñ  end
end
class OldBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle do |arglist|
    url("STRING");Fi[	@Ki¢I"à  end
end
class AncientBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle("STRING")
  ;Fi[	@Ki›I"Ø  end
end
class SnowLeopardBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle { |arglist| sha1("STRING" => :snowleopard) }
  ;Fi[	@KiÍI"°  end
end
class LionBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle { |arglist| sha1("STRING" => :lion) }
  ;Fi[	@Ki˜I"´  end
end
class AllCatsBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle do |arglist|
    sha1("STRING" => :snowleopard);Fi[	@KiI"ó  end
end
class RevisedBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle do |arglist|
    version(1);Fi[	@KiI"¬class CustomVersionScheme < Version
end
class CustomVersionSchemeTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  version("STRING" => (CustomVersionScheme))
  ;Fi[I"if (var == 1) then;FI"@var = @var;F@åI"/var = (((var - (2 * @var)) * 1000) / @var);FI"end;FI"var = (var + 1);Fi[[	@ñi¿I"˙        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end;Fi[	@ñiﬁI"        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + (var ? (1) : (2)));Fi[	@ôiªI"˙        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end;Fi[	@ôiÊI"        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + var);Fi[	@úiØI"˙        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end;Fi[	@úiÕI"        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + (var ? (1) : (2)));Fi[	@üiùI"˙        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end;Fi[	@üiæI"        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + 1);Fi[@áI"var = 0;FI"var = 0;FI"var = 0;FI"var = 1;FI"while (var < var) do;Fi[[	@ñinI"        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128);Fi[	@ñi≤I"    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128);Fi[	@ôicI"¸        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var;Fi[	@ôi≠I"    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var;Fi[	@úi]I"        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128);Fi[	@úi°I"    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128);Fi[	@üiêI"¯    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then;Fi[	@ài¥I"Ã      @var = (@var + GetStringWidth(var))
      return
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then;Fi[I"var = 0;F@äI"@var = @var;F@å@^I"end;Fi[[	@ñiøI"        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next;Fi[	@ñi›I"˙        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else;Fi[	@ôi∫I"        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next;Fi[	@ôiÂI"˙        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else;Fi[	@úiÆI"        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next;Fi[	@úiÃI"˙        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else;Fi[	@üiúI"        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next;Fi[	@üiΩI"˙        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else;Fi[@(@œ@–I"(@var << "STRING");FI"end;FI" ;Fi[[	@-iTI"ÿ      def function(arglist)
        @var = []
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING");Fi[	@-iXI"Õ      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING");Fi[	@-i\I"       
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        @var.text!(var);Fi[	@-i´I"Ÿ      def function(arglist)
        (@var << "STRING")
        @var = nil
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING");Fi[	@-iØI"œ      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        @var.h4 do |arglist|;Fi[	@-iªI"Á          @var.text!("STRING")
          @var.span(var, :class => "STRING")
        end
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING");Fi[	@-iøI"Ã      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        @var = var.dom_id;Fi[	@-i¿I"º      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        var = 100.0;Fi[@ÆI"
begin;F@*I"yield);FI"ensure;F@-i[[	@1iéI"
      # @return [ Object ] The result of the yield.
      #
      # @since 3.0.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end;Fi[	@†iƒI"
      # @return [ Object ] The result of the block.
      #
      # @since 3.0.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end;Fi[	@6iI"      # @return [ Object ] The yielded value.
      #
      # @since 2.2.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end;Fi[	@6i7I"      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end;Fi[	@6iVI"      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end;Fi[	@6i|I"      # @return [ Object ] The return value of the block.
      #
      # @since 2.3.2
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end;Fi[	@6iõI"      # @return [ Object ] The return value of the block.
      #
      # @since 2.3.4
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end;Fi[	@6iØI"&        # @return [ Object ] The return value of the block.
        #
        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield)
          ensure
            Threaded.exit_execution("STRING")
          end
        end;Fi[I"yield);FI"ensure;F@-I"end;FI"end;FI" ;Fi[[	@1iëI"      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      module ClassMethods
        # Adds the existence check for relations.;Fi[	@†i«I"’      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      private
      ;Fi[	@6iI"˘      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in assigning mode?
      #;Fi[	@6i:I"˜      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in binding mode?
      #;Fi[	@6iYI"¯      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in building mode?
      #;Fi[	@6iI"˜      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in loading mode?
      #;Fi[	@6iûI"      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      module ClassMethods
        # Execute a block in creating mode.;Fi[	@6i≤I"        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield)
          ensure
            Threaded.exit_execution("STRING")
          end
        end
        
        # Is the current thread in loading revision mode?
        #;Fi[@(@œ@–I"(@var << var);FI"end;FI" ;Fi[[	@i"I"‘          var.init
          var.feature = self
        end
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        return if Cucumber.wants_to_quit;Fi[	@iI"·      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        var.each { |arglist| var.feature_element = self };Fi[	@'iI"œ        
        def function(arglist)
          @var = []
        end
        
        def function(arglist)
          (@var << var)
        end
        
        def function(arglist)
          # do nothing;Fi[	@-i1I"Í        build_cell(@var, var, var)
        set_scenario_color(var)
        @var = (@var + 1)
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        return if @var.empty?;Fi[	@0iI"π      
      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        # do nothing;Fi[	@πi4I"ø    def function(arglist)
      @var = true
      (@var << "STRING")
    end
    
    def function(arglist)
      (@var << var)
    end
    
    def function(arglist)
      (@var << "STRING");Fi[	I"/data//rspec_proj/spec/mocks/order_group.rb;TiI"⁄      def function(arglist)
        @var = var
        @var = Array.new
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        return (@var.first == var);Fi[	@L
i-I"¡        @var = []
        @var = false
        @var = nil
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        line_number.!.!;Fi[@
I"##;F@Ã@Õ@ŒI"def function(arglist);Fi[[	@3i8I"i          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        transferred_files_for(var) do |arglist|;Fi[	@6iZI"J          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = [];Fi[	@WiDI"Y          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        connection do |arglist|;Fi[	@◊i#I"p          FileUtils.send(transfer_method, var, var)
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = [];Fi[	@9i@I"b          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        if var = directory_for(var) then;Fi[	@<i;I"W          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        connection.sync_clock;Fi[	@ﬁi8I"J          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = [];Fi[	@Zi6I"Y          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        connection do |arglist|;Fi[I"var = var;FI"var = 0;F@äI"@var = @var;F@å@^i[[	@ñiæI"C        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1);Fi[	@ñi‹I"0          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1);Fi[	@ôiπI"C        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1);Fi[	@ôi‰I"0          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1);Fi[	@úi≠I"C        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1);Fi[	@úiÀI"0          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1);Fi[	@üiõI"F        self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1);Fi[	@üiºI"	          var = (var + 1)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1);Fi[@
@@≤I"end;FI" ;FI"def function(arglist);Fi[[	@∑i6I"≠      super
      @var
    end
    
    def function(arglist)
      raise(NotImplementedError)
    end
    
    def function(arglist)
      Association.valid_options
    end;Fi[	@miI"        def function(arglist)
          preload unless owners.first.association(reflection.name).loaded?
        end
        
        def function(arglist)
          raise(NotImplementedError)
        end
        
        def function(arglist)
          @var ||= build_scope
        end;Fi[	@¯iI"œ        @var = var
        @var = var
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end;Fi[	@¯iI"Ë      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end;Fi[	@¯iI"Ë      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end;Fi[	@¯iI"Ë      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end;Fi[	@¯iI"Ë      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end;Fi[	@¯i"I"Ë      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end;Fi[@
@I"
false;FI"end;FI" ;FI"def function(arglist);Fi[[	@]
iI"®      def function(arglist)
        true
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        false
      end;Fi[	@]
ilI"ò          end
        end
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        true
      end;Fi[	@êi"I"í    def function(arglist)
      true
    end
    
    def function(arglist)
      false
    end
    
    def function(arglist)
      false
    end;Fi[	@êi&I"ü    def function(arglist)
      false
    end
    
    def function(arglist)
      false
    end
    
    def function(arglist)
      @var ||= "STRING"
    end;Fi[	@Â	i6I"Õ      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        "STRING" if options[:pattern_name]
      end;Fi[	@$iI"≥      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        :skipped
      end;Fi[	@)i/I"ﬂ  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist)
    false
  end
  
  def function(arglist)
    self.participants.map { |arglist| var.diaspora_handle }.join("STRING")
  end;Fi[	@XizI"à  def function(arglist)
    false
  end
  
  def function(arglist)
    false
  end
  
  def function(arglist)
    I18n.t("STRING")
  end;Fi[@_@ºI"#;F@æI"#;FI"1# @return [ true, false ] If a value exists.;Fi[[	@«i
I"\    class Gt < Default
      # Return true if the attribute is greater than the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        determine(var, :>);Fi[	@ i
I"j    class Gte < Default
      # Return true if the attribute is greater than or equal to the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        determine(var, :>=);Fi[	@Õi
I"W    class In < Default
      # Return true if the attribute is in the values.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        var = Array.wrap(@var);Fi[	@–i
I"Y    class Lt < Default
      # Return true if the attribute is less than the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        determine(var, :<);Fi[	@”i
I"g    class Lte < Default
      # Return true if the attribute is less than or equal to the value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        determine(var, :<=);Fi[	@÷i
I"g    class Ne < Default
      # Return true if the attribute and first value are not equal.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        @var.!=(var.values.first);Fi[	@Ÿi
I"ç    class Nin < Default
      # Return true if the attribute is not in the value list.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        Array.wrap(@var).none? { |arglist| var.values.first.include?(var) };Fi[	@‹i
I"r    class Size < Default
      # Return true if the attribute size is equal to the first value.
      #
      # @example Do the values match?
      #   matcher.matches?({ :key => 10 })
      #
      # @param [ Hash ] value The values to check.
      #
      # @return [ true, false ] If a value exists.
      def function(arglist)
        (@var.size == var.values.first);Fi[I"#       event :ignite do;FI",#         transition :parked => :idling;FI"#       end;FI"#     end;FI"#   end;FI"#;Fi[[	@,iI"    #         transition :idling => :parked
    #       end
    #       
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   
    #   events = Vehicle.state_machine(:state).events
    #   ;Fi[	@,iDI"˚    #         transition :idling => :parked
    #       end
    #       
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   
    #   events = Vehicle.state_machine.events
    #   ;Fi[	@,idI"g    # 
    #   class Vehicle < ActiveRecord::Base
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                       # => #<Vehicle id: nil, state: "parked">
    #   events = Vehicle.state_machine.events;Fi[	@®iI"I        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver
        #     include DataMapper::Observer;Fi[	@®iYI"I        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver
        #     include DataMapper::Observer;Fi[	@®i}I"U        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver
        #     after_transition_failure do |transition|;Fi[	@àiÂI">    # 
    #   class Vehicle
    #     state_machine do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new
    #   transition = StateMachine::Transition.new(vehicle, Vehicle.state_machine, :ignite, :parked, :idling);Fi[	@ài˛I"Å    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new     # => #<Vehicle:0xb7b7f568 @state="parked">
    #   transition = StateMachine::Transition.new(vehicle, Vehicle.state_machine, :ignite, :parked, :idling);Fi[@
@I"(@var << var);FI"end;FI" ;FI"def function(arglist);Fi[[	@i#I"Œ          var.feature = self
        end
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        return if Cucumber.wants_to_quit
        init;Fi[	@iI"‰      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        var.each { |arglist| var.feature_element = self }
      end;Fi[	@'iI"“        def function(arglist)
          @var = []
        end
        
        def function(arglist)
          (@var << var)
        end
        
        def function(arglist)
          # do nothing
        end;Fi[	@-i2I"Ê        set_scenario_color(var)
        @var = (@var + 1)
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        return if @var.empty?
        @var.each do |arglist|;Fi[	@0iI"º      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        # do nothing
      end;Fi[	@πi5I"ƒ      @var = true
      (@var << "STRING")
    end
    
    def function(arglist)
      (@var << var)
    end
    
    def function(arglist)
      (@var << "STRING")
      self.instance_eval(&var);Fi[	@ÒiI"»        @var = var
        @var = Array.new
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        return (@var.first == var)
      end;Fi[	@L
i.I"π        @var = false
        @var = nil
      end
      
      def function(arglist)
        (@var << var)
      end
      
      def function(arglist)
        line_number.!.!
      end;Fi[@(@œI"private;FI" ;F@ÍI"
begin;Fi[[	@i$I"‹      ((var << "STRING") << "STRING")
      ((var << read_file("STRING")) << "STRING")
      var
    end
    
    private
    
    def function(arglist)
      begin
        File.read(var).strip
      rescue Errno::ENOENT;Fi[	@i_I"        var = @var.dup
        (var << "STRING") if @var
        var
      end
      
      private
      
      def function(arglist)
        begin
          (return unless @var
          configuration.invoke_command(var, var) do |arglist|;Fi[	I"-data//cucumber_prok/cucumber/cli/main.rb;Ti;I"        @var.parse!(@var)
        Cucumber.logger = @var.log
        @var
      end
      
      private
      
      def function(arglist)
        begin
          (return false unless configuration.drb?
          @var = DRbClient.run(@var, @var, @var, configuration.drb_port);Fi[	I";data//cucumber_prok/cucumber/runtime/user_interface.rb;Ti3I"Ú      #
      def function(arglist)
        @var.embed(var, var, var)
      end
      
      private
      
      def function(arglist)
        begin
          Timeout.timeout(var) { |arglist| STDIN.gets }
        rescue Timeout::Error => var;Fi[	@oi,I"§      puts("STRING")
      var.dump
    end
  end
  
  private
  
  def function(arglist)
    begin
      if Dir["STRING"].empty? then
        safe_system("STRING");Fi[	I"@data//paperclip_proj/paperclip/geometry_detector_factory.rb;TiI"E    def function(arglist)
      var = GeometryParser.new(geometry_string.strip).make
      (var or raise(Errors::NotIdentifiedByImageMagickError.new))
    end
    
    private
    
    def function(arglist)
      begin
        silence_stream(STDERR) do |arglist|
          Paperclip.run("STRING", "STRING", :file => "STRING");Fi[	I"9data//redmine_proj/redmine/core_ext/active_record.rb;Ti	I"  module FinderMethods
    def function(arglist)
      find_ids_with_associations
    end
    
    private
    
    def function(arglist)
      begin
        (var = construct_join_dependency_for_association_find
        var = construct_relation_for_association_find_ids(var);Fi[	I")data//twitter_proj/twitter/client.rb;TiZI"'    # Perform an HTTP PUT request
    def function(arglist)
      request(:put, var, var)
    end
    
    private
    
    def function(arglist)
      begin
        connection.send(var.to_sym, var, var) do |arglist|
          var.headers[:authorization] = auth_header(var.to_sym, var, var).to_s;Fi[@(@œI"module ClassMethods;FI"I# Convert the object from its mongo friendly ruby type to this type.;FI"#;FI"&# @example Demongoize the object.;Fi[[	@∞iI"      # @since 3.0.0
      def function(arglist)
        to_s
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #
        # @example Demongoize the object.
        #   Object.demongoize(object)
        #;Fi[	@ŒiI"(      # @since 3.0.0
      def function(arglist)
        ::Date.mongoize(self)
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #
        # @example Demongoize the object.
        #   Date.demongoize(object)
        #;Fi[	@∑i%I"0      # @since 3.0.0
      def function(arglist)
        ::DateTime.mongoize(self)
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #
        # @example Demongoize the object.
        #   DateTime.demongoize(object)
        #;Fi[	@ƒi+I",      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #
        # @example Demongoize the object.
        #   Range.demongoize({ "min" => 1, "max" => 5 })
        #;Fi[	@…iI":      # @since 3.0.0
      def function(arglist)
        ::Set.mongoize(self)
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #
        # @example Demongoize the object.
        #   Set.demongoize({ "min" => 1, "max" => 5 })
        #;Fi[	@Ãi¢I"        else
          self
        end
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #
        # @example Demongoize the object.
        #   String.demongoize(object)
        #;Fi[	@œiI"%      # @since 2.3.1
      def function(arglist)
        to_s.mongoid_id?
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #
        # @example Demongoize the object.
        #   Symbol.demongoize(object)
        #;Fi[	@‘iI"G      # @since 3.0.0
      def function(arglist)
        ::ActiveSupport::TimeWithZone.mongoize(self)
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #
        # @example Demongoize the object.
        #   TimeWithZone.demongoize(object)
        #;Fi[@
@@üI"end;FI" ;FI"def function(arglist);Fi[[	@-iUI"∆        @var = []
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end;Fi[	@-iYI"–      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end;Fi[	@-i]I"”      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        @var.text!(var)
        @var.br;Fi[	@-i¨I"«        (@var << "STRING")
        @var = nil
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end;Fi[	@-i∞I"ı      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        @var.h4 do |arglist|
          @var.span(var, :class => "STRING");Fi[	@-iºI"“          @var.span(var, :class => "STRING")
        end
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end;Fi[	@-i¿I"ﬂ      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        @var = var.dom_id
        @var = (@var + 1);Fi[	@-i¡I"       def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        var = 100.0
        var = (((@var.to_f / @var.to_f) * 1000).to_i / 10.0) if @var.!=(0);Fi[@*I"yield);FI"ensure;F@-I"end;FI"end;Fi[[	@1iêI"Ô      # @since 3.0.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      module ClassMethods;Fi[	@†i∆I"„      # @since 3.0.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      private;Fi[	@6iI"      # @since 2.2.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in assigning mode?;Fi[	@6i9I"      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in binding mode?;Fi[	@6iXI"      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in building mode?;Fi[	@6i~I"      # @since 2.3.2
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in loading mode?;Fi[	@6iùI"Ô      # @since 2.3.4
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      module ClassMethods;Fi[	@6i±I"#        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield)
          ensure
            Threaded.exit_execution("STRING")
          end
        end
        
        # Is the current thread in loading revision mode?;Fi[I"def self.included(arglist);FI"var.extend(ClassMethods);FI"end;FI" ;FI"module ClassMethods;FI"def function(arglist);Fi[[	@+
i0I"¿  end
  
  module Gradient
    def self.included(arglist)
      var.extend(ClassMethods)
    end
    
    module ClassMethods
      def function(arglist)
        class_eval("STRING")
      end;Fi[	@ÇiI"amodule Redmine
  module Acts
    module ActivityProvider
      def self.included(arglist)
        var.extend(ClassMethods)
      end
      
      module ClassMethods
        def function(arglist)
          unless self.included_modules.include?(Redmine::Acts::ActivityProvider::InstanceMethods) then
            cattr_accessor(:activity_provider_options);Fi[	I"Ldata//redmine_proj/plugins/acts_as_attachable/lib/acts_as_attachable.rb;TiI"module Redmine
  module Acts
    module Attachable
      def self.included(arglist)
        var.extend(ClassMethods)
      end
      
      module ClassMethods
        def function(arglist)
          cattr_accessor(:attachable_options)
          self.attachable_options = {};Fi[	I"Pdata//redmine_proj/plugins/acts_as_customizable/lib/acts_as_customizable.rb;TiI"1module Redmine
  module Acts
    module Customizable
      def self.included(arglist)
        var.extend(ClassMethods)
      end
      
      module ClassMethods
        def function(arglist)
          if self.included_modules.include?(Redmine::Acts::Customizable::InstanceMethods) then
            return;Fi[	I"Bdata//redmine_proj/plugins/acts_as_event/lib/acts_as_event.rb;TiI"#module Redmine
  module Acts
    module Event
      def self.included(arglist)
        var.extend(ClassMethods)
      end
      
      module ClassMethods
        def function(arglist)
          if self.included_modules.include?(Redmine::Acts::Event::InstanceMethods) then
            return;Fi[	I"Jdata//redmine_proj/plugins/acts_as_watchable/lib/acts_as_watchable.rb;TiI"+module Redmine
  module Acts
    module Watchable
      def self.included(arglist)
        var.extend(ClassMethods)
      end
      
      module ClassMethods
        def function(arglist)
          if self.included_modules.include?(Redmine::Acts::Watchable::InstanceMethods) then
            return;Fi[	I"Ddata//redmine_proj/plugins/rfpdf/lib/rfpdf/action_controller.rb;Ti	I"&  module ActionController
    DEFAULT_RFPDF_OPTIONS = { :inline => (true) }
    
    def self.included(arglist)
      var.extend(ClassMethods)
    end
    
    module ClassMethods
      def function(arglist)
        var = breakdown_rfpdf_options(var)
        write_inheritable_hash(:rfpdf, var);Fi[	I"3data//redmine_proj/redmine/subclass_factory.rb;TiI"∆module Redmine
  module SubclassFactory
    def self.included(arglist)
      var.extend(ClassMethods)
    end
    
    module ClassMethods
      def function(arglist)
        var = nil
        begin;Fi[I"##;F@Ã@Õ@Œ@˜I"var = remote_path_for(var);Fi[[	@3i9I"ã        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        transferred_files_for(var) do |arglist|
          Logger.message(("STRING" + "STRING"));Fi[	@6i[I"å        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |arglist| (var << ("STRING" + "STRING")) };Fi[	@WiEI"}        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        connection do |arglist|
          transferred_files_for(var) do |arglist|;Fi[	@◊i$I"        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |arglist| (var << "STRING") };Fi[	@9iAI"g        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        if var = directory_for(var) then
          var = [];Fi[	@<i<I"y        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        connection.sync_clock
        transferred_files_for(var) do |arglist|;Fi[	@ﬁi9I"å        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |arglist| (var << ("STRING" + "STRING")) };Fi[	@Zi7I"}        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        connection do |arglist|
          transferred_files_for(var) do |arglist|;Fi[I"	self;FI"end;FI"end;FI"end;FI"end;FI"end;Fi[[	@1i¸I"¥          re_define_method(var) do |arglist|
            send(var.setter, var.klass.find(var.reject(&:blank?)))
          end
          self
        end
      end
    end
  end
end;Fi[	I"9data//mongoid_proj/lib/mongoid/relations/builders.rb;TiTI"ê            save if new_record? and var.stores_foreign_key?
            var
          end
          self
        end
      end
    end
  end
end;Fi[	I":data//mongoid_proj/lib/mongoid/relations/cascading.rb;Ti3I"©        # @since 2.0.0.rc.1
        def function(arglist)
          cascades.push(var.name.to_s) if var.dependent?
          self
        end
      end
    end
  end
end;Fi[	I"<data//mongoid_proj/lib/mongoid/relations/polymorphic.rb;Ti I"ë              field(var.inverse_of_field, :type => (Symbol))
            end
          end
          self
        end
      end
    end
  end
end;Fi[	I"@data//mongoid_proj/lib/mongoid/relations/synchronization.rb;TiåI"ø        # @since 2.2.1
        def function(arglist)
          set_callback(:destroy, :after) { |arglist| var.remove_inverse_keys(var) }
          self
        end
      end
    end
  end
end;Fi[	I":data//mongoid_proj/lib/mongoid/relations/touchable.rb;TiI"¶        # @since 3.0.0
        def function(arglist)
          self.touchables.push(var.name) if var.touchable?
          self
        end
      end
    end
  end
end;Fi[	I":data//mongoid_proj/lib/mongoid/timestamps/timeless.rb;Ti.I"é        # @since 2.3.0
        def function(arglist)
          Threaded.timeless = true
          self
        end
      end
    end
  end
end;Fi[	@i§I"~            @var.parent = var
            return var
          end
          self
        end
      end
    end
  end
end;Fi[@(@)@Á@ˆI" ;FI"long_desc("STRING");Fi[[	@J	i8I"Ω        end
      else
        super
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :string, :banner => "STRING");Fi[	@J	iVI"ˇ      else
        puts("STRING")
        FileUtils.cp(File.expand_path("STRING", "STRING"), "STRING")
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :string, :banner => "STRING");Fi[	@J	i~I"˛          Bundler.load.lock unless options[:"dry-run"]
          Bundler.ui.info("STRING")
        end
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :array, :banner => "STRING");Fi[	@J	i⁄I"<        Bundler.ui.warn("STRING") if var[:local] and Bundler.app_cache.exist?
        Bundler.ui.warn("STRING") if Bundler.definition.no_sources?
        raise(var)
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :array, :banner => "STRING");Fi[	@J	i.I"/        Bundler.ui.warn("STRING")
      else
        var.generate_bundler_executable_stubs(var, :force => (options[:force]), :binstubs_cmd => (true))
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING");Fi[	@J	iùI"ø      rescue ArgumentError
        Bundler.ui.error("STRING")
        exit(128)
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    def function(arglist);Fi[	@J	iI"S        var = var.name
        var = (var.license or var.licenses)
        var.empty? ? (Bundler.ui.warn("STRING")) : (Bundler.ui.info("STRING"))
      end
    end
    
    desc("STRING", "STRING")
    
    long_desc("STRING")
    
    method_option(:file, :type => :string, :default => "STRING", :aliases => "STRING", :banner => "STRING");Fi[@T@≈@∆@«I"end;FI"end;Fi[[	@>iÅI"‹        def function(arglist)
          if self.class.serialized_attributes.include?(var) then
            @var[var].serialized_value
          else
            super
          end
        end
      end
    end
  end
end;Fi[	@i	I"ª        case var.message
        when /column(s)? .* (is|are) not unique/ then
          RecordNotUnique.new(var, var)
        else
          super
        end
      end
    end
  end
end;Fi[	@æi:I"Ì            if arel.respond_to?(var) then
              self.class.delegate(var, :to => :arel)
              arel.send(var, *var, &var)
            else
              super
            end
          end
        end
      end
    end
    ;Fi[	@ihI"…        if var then
          self.class.log_deprecation_warning(var, var)
          var[:action].call(self, var[0]) if var[:action]
        else
          super
        end
      end
    end
  end
end;Fi[	@Ii4I"∏    def function(arglist)
      if Gem::Specification.new.respond_to?(var) then
        _remote_specification.send(var, *var, &var)
      else
        super
      end
    end
  end
end;Fi[	@siPI"            else
              if configuration.respond_to?(var) then
                configuration.send(var, *var, &var)
              else
                super
              end
            end
          end
        end
        
        def function(arglist);Fi[	@iI"⁄        var = var.fields[var.to_s]
        if var.try(:localized?) and var.blank?.! then
          var.values.each { |arglist| super(var, var, var) }
        else
          super
        end
      end
    end
  end
end;Fi[@(@œ@–I"@var ||= [];FI"end;FI" ;Fi[[	I"/data//bundler_proj/bundler/rubygems_ext.rb;Ti/I"Û    # RubyGems 1.8+ used only.
    def function(arglist)
      full_gem_path
    end
    
    def function(arglist)
      @var ||= []
    end
    
    def function(arglist)
      if @var and File.exist?(File.join(full_gem_path, "STRING")) then;Fi[	I"(data//cancan_proj/cancan/ability.rb;TiI"<        var = (var + aliases_for_action(var)) if var.include?(var)
      end
      var
    end
    
    def function(arglist)
      @var ||= []
    end
    
    # Returns an array of Rule instances which match the action and subject
    # This does not take into consideration any hash conditions or block statements;Fi[	I"Fdata//cucumber_prok/cucumber/language_support/language_methods.rb;TiSI"      
      def function(arglist)
        @var ||= Hash.new { |arglist| var[var] = [] }
      end
      
      def function(arglist)
        @var ||= []
      end
      
      def function(arglist)
        hooks_for(:around, var).reverse.inject(var) do |arglist|;Fi[	@Ùi#I"I        
        def function(arglist)
          respond_to?(:visible?) ? (visible?(var)) : (true) and var.allowed_to?(self.class.attachable_options[:delete_permission], self.project)
        end
        
        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          @var ||= [];Fi[	@Ùi'I"⁄        
        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          if var.is_a?(Hash) then;Fi[	@§iI"        def function(arglist)
          @var = nil
          default(ExampleGroup)
        end
        
        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          example_group_creation_listeners.each do |arglist|;Fi[	@<	i‹I"…      
      def function(arglist)
        (@var == @var)
      end
      
      def function(arglist)
        @var ||= []
      end
      
      def function(arglist)
        (similar_messages << var);Fi[@(@œ@–I"@var.empty?;FI"end;FI" ;Fi[[	I")data//backup_proj/backup/pipeline.rb;Ti8I"¯      rescue Exception => var
        raise(Errors::Pipeline::ExecutionError.wrap(var))
      end
    end
    
    def function(arglist)
      @var.empty?
    end
    
    ##
    # Returns a multi-line String, reporting all STDERR messages received;Fi[	I"?data//bundler_proj/bundler/vendor/thor/parser/arguments.rb;Ti7I"π    def function(arglist)
      var =~ /^--(no|skip)-([-\w]+)$/
      $2
    end
    
    def function(arglist)
      @var.empty?
    end
    
    def function(arglist)
      @var.first;Fi[	I"8data//cucumber_prok/cucumber/ast/step_collection.rb;Ti,I"      def function(arglist)
        var = (@var.index(var) or -1)
        @var[(var - 1)]
      end
      
      def function(arglist)
        @var.empty?
      end
      
      def function(arglist)
        var = (@var + [var]).map { |arglist| var.text_length };Fi[	@ŒidI"X      def function(arglist)
        load_programming_language("STRING") if unknown_programming_language?
        @var.map { |arglist| var.snippet_text(var, var, var) }.join("STRING")
      end
      
      def function(arglist)
        @var.empty?
      end
      
      def function(arglist)
        @var.each { |arglist| var.send(var, *var) };Fi[	I"%data//homebrew_proj/checksums.rb;TiI"è  def function(arglist)
    @var = var
    @var = var.to_s
  end
  
  def function(arglist)
    @var.empty?
  end
  
  attr_reader :function
  ;Fi[	I"+data//homebrew_proj/formula_support.rb;Ti•I"©  
  def function(arglist)
    any? { |arglist| (var.name == var) }
  end
  
  def function(arglist)
    @var.empty?
  end
  
  def function(arglist)
    @var.each(&var);Fi[	@ﬂiI"§  
  def function(arglist)
    @var.each(&var)
  end
  
  def function(arglist)
    @var.empty?
  end
  
  def function(arglist)
    return unless external_patches?;Fi[@^@w@xI"	self;FI"end;FI" ;Fi[[	I"/data//mongoid_proj/lib/mongoid/criteria.rb;TiHI"∫    #   criteria.to_criteria
    #
    # @return [ Criteria ] self.
    #
    # @since 3.0.0
    def function(arglist)
      self
    end
    
    # Convert the criteria to a proc.
    #;Fi[	@øiI"Ë      #   object.__evolve_object_id__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      (alias :__mongoize_object_id__ :__evolve_object_id__)
      ;Fi[	@øiI"”      #   object.__find_args__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      # Mongoize a plain object into a time.
      #;Fi[	@øi(I"Ÿ      #   object.__mongoize_time__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      # Try to form a setter from this object.
      #;Fi[	@øi@I"‹      #   object.__sortable__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      # Conversion of an object to an $inc-able value.
      #;Fi[	@øiÇI"…      #   object.mongoize
      #
      # @return [ Object ] The object.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      # Is the object multi args.
      #;Fi[	I";data//mongoid_proj/lib/mongoid/extensions/object_id.rb;TiI"˙      #   object_id.__evolve_object_id__
      #
      # @return [ Moped::BSON::ObjectId ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      (alias :__mongoize_object_id__ :__evolve_object_id__)
      ;Fi[@(@œ@–I"0;FI"end;FI" ;Fi[[	@êiI"Ö    
    def function(arglist)
      []
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0;Fi[	@êiI"Ñ    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0;Fi[	@êiI"Ñ    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0;Fi[	@êiI"á    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      true;Fi[	@êi1I"Ö    
    def function(arglist)
      {}
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0;Fi[	@êi5I"è    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      # do nothing;Fi[	@ÖiI"é    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      true;Fi[@(@œ@–@Ä@ÅI"end;Fi[[	I".data//bundler_proj/bundler/environment.rb;Ti&I"î    
    def function(arglist)
      @var.lock(Bundler.default_lockfile)
    end
    
    def function(arglist)
      # do nothing
    end
  end
end;Fi[	I"1data//cucumber_prok/cucumber/ast/scenario.rb;TiI"À        
        def function(arglist)
          StepCollection.new(var)
        end
        
        def function(arglist)
          # do nothing
        end
      end
      
      def function(arglist);Fi[	@'iI"ø        
        def function(arglist)
          (@var << var)
        end
        
        def function(arglist)
          # do nothing
        end
      end
      
      include(Enumerable);Fi[	@ŒiI"        
        def function(arglist)
          @var.invoke(var.name, Ast::MultilineArgument.from((var.doc_string or var.rows)))
        end
        
        def function(arglist)
          # do nothing
        end
      end
      
      include(Constantize);Fi[	I"*data//diaspora_proj/models/service.rb;Ti%I"    var = truncate(var, :length => ((var - var)))
    var = "STRING"
    return var
  end
  
  def function(arglist)
    # do nothing
  end
end
require(Rails.root.join("app", "models", "services", "facebook"))
require(Rails.root.join("app", "models", "services", "twitter"));Fi[	@ÒiI"´        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end;Fi[	@ÙiI"´        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end;Fi[@(@)@*@Ü	I"protected;FI" ;Fi[[	@Pi}I"Ó          false
        else
          match_attribute_method?(var.to_s).nil?.!
        end
      end
    end
    
    protected
    
    def function(arglist)
      respond_to_without_attributes?(:attributes) and attributes.include?(var);Fi[	@Si/I"            unless var.send(CHECKS[var], var) then
              var.errors.add(var, var, filtered_options(var).merge(:count => (var)))
            end)
          end
        end
      end
      
      protected
      
      def function(arglist)
        case var;Fi[	@æi(I"Ë          @var.synchronize do |arglist|
            return if method_defined?(var)
            super
          end
        end
      end
      
      protected
      
      def function(arglist)
        if @var.respond_to?(var) then;Fi[	@biDI"˚          else
            true if [var[:only]].flatten.include?(@var[:action].to_sym)
          end
        end
      end
    end
    
    protected
    
    def function(arglist)
      if parent?.! and new_actions.include?(@var[:action].to_sym) then;Fi[	@ki\I"º        var.value.first
      else
        var
      end
    end
  end
  
  protected
  
  def function(arglist)
    raise(ArgumentError.new("STRING")) unless var.is_a?(Sass::Script::List);Fi[	@ni_I"œ            :locked
          else
            super
          end
        end
      end
      
      protected
      
      def function(arglist)
        (self.failed_attempts > self.class.maximum_attempts);Fi[	@i,I"∑          puts("STRING")
          puts("STRING")
          exit(0)
        end
      end
    end
    
    protected
    
    def function(arglist)
      File.expand_path(@var[:file]);Fi[@^@w@xI"@var = var;FI"end;FI" ;Fi[[	I"8data//mongoid_proj/lib/mongoid/contextual/memory.rb;Ti,I"ﬁ      # @param [ Integer ] value The limit.
      #
      # @return [ Integer ] The limit.
      #
      # @since 3.0.0
      def function(arglist)
        @var = var
      end
      
      # Get the skiping value.
      #;Fi[	@›iHI"›      # @param [ Integer ] value The skip.
      #
      # @return [ Integer ] The skip.
      #
      # @since 3.0.0
      def function(arglist)
        @var = var
      end
      
      # Apply criteria options.
      #;Fi[	@èieI"    # @param [ Array<Document> ] docs The embedded documents.
    #
    # @return [ Array<Document> ] The embedded documents.
    #
    # @since 3.0.0
    def function(arglist)
      @var = var
    end
    
    # Is the criteria for embedded documents?
    #;Fi[	@èiI"R    # @param [ Array<Metadata> ] The inclusions.
    #
    # @return [ Array<Metadata> ] The new inclusions.
    #
    # @since 3.0.0
    def function(arglist)
      @var = var
    end
    
    # Merges another object with this +Criteria+ and returns a new criteria.
    # The other object may be a +Criteria+ or a +Hash+. This is used to;Fi[	I"/data//mongoid_proj/lib/mongoid/loggable.rb;TiI"ª    # @param [ Logger ] The logger to set.
    #
    # @return [ Logger ] The new logger.
    #
    # @since 3.0.0
    def function(arglist)
      @var = var
    end
    
    private
    ;Fi[	@pióI".        # @param [ true, false ] value The flag.
        #
        # @return [ true, false ] The flag.
        #
        # @since 3.0.0
        def function(arglist)
          @var = var
        end
        
        # Normalize the documents, in case they were provided as an array of
        # hashes.;Fi[	@piŒI"        # @param [ String ] value The path.
        #
        # @return [ String ] The path.
        #
        # @since 3.0.0
        def function(arglist)
          @var = var
        end
        
        # Get the selector for executing atomic operations on the collection.
        #;Fi[@òI"@var = var;FI"@var = var;FI"end;FI" ;FI"def function(arglist);Fi[[	@ˇieI"R      attr_reader(:object, :name, :values, :column)
      
      def function(arglist)
        @var = var
        @var = var
        @var = var
      end
      
      def function(arglist)
        return if values.values.compact.empty?
        @var = (object.class.reflect_on_aggregation(name.to_sym) or object.column_for_attribute(name));Fi[	@si=I"'          attr_reader(:configuration, :condition, :server)
          
          def function(arglist)
            @var = var
            @var = var
            @var = var
          end
          
          def function(arglist)
            configuration.roles[var].include?(server)
          end;Fi[	@'iBI"Ë        var = ensure_array_of_array(rubify(var))
        var = var.transpose
        create_cell_matrix(var)
        @var = var
        @var = var
        @var = var
      end
      
      def function(arglist)
        dup
      end;Fi[	@ªi+I"˛        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var
          @var = var
        end
        
        def function(arglist)
          [("STRING" % @var.join(File::PATH_SEPARATOR))]
        end;Fi[	I"7data//cucumber_prok/cucumber/rb_support/rb_hook.rb;TiI"      attr_reader(:tag_expressions)
      
      def function(arglist)
        @var = var
        @var = var
        @var = var
      end
      
      def function(arglist)
        @var.current_world.cucumber_instance_exec(false, var, *[var, var].compact, &@var)
      end;Fi[	@‚iôI"Ô  
  # The user can chose to force installation even in the face of conflicts.
  def function(arglist)
    @var = var
    @var = var
    @var = var
  end
  
  def function(arglist)
    var = "STRING"
    (var << "STRING") if @var[:because];Fi[	@Èi
I"√    attr_accessor(:file, :options, :attachment)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
    end
    
    def function(arglist)
      # do nothing
    end;Fi[@(@)@*@Ü	@~I""STRING";Fi[[	@ci‚I"œ          var.each do |arglist|
            fixtures[var] = ActiveRecord::Fixture.new(var, model_class)
          end
        end
      end
    end
    
    def function(arglist)
      "STRING"
    end
  end;Fi[	@ÛijI"œ            Mysql2::Error
          else
            defined? Mysql ? (Mysql::Error) : (StandardError)
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@òi(I"          for var in sprite_names do
            unless var =~ /\A#{Sass::SCSS::RX::IDENT}STRING/ then
              raise(Sass::SyntaxError, "STRING")
            end
          end
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@viuI"µ            "STRING"
          else
            (var.value.first + "STRING")
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@ëiFI"          if @var.present? then
            "STRING".tap do |arglist|
              @var.any? ? ((var << "STRING")) : ((var << "STRING"))
            end
          end
        end
        
        def function(arglist)
          "STRING"
        end
        ;Fi[	@ÿi9I"∆            return "STRING"
          else
            return "STRING" if (@var == :at_least)
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@¢i∂I"‚            "STRING"
          else
            target_metaclass.protected_method_defined?(var) ? ("STRING") : ("STRING")
          end
        end
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[@
@I"@var = true;FI"end;FI" ;FI"def function(arglist);Fi[[	@fiI"º        @var = var["STRING"]
        @var = @var
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = true
      end;Fi[	@”iI"        @var = false
        @var = ([Bundler.app_cache] + Bundler.rubygems.gem_path.map { |arglist| File.expand_path("STRING") })
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = true
      end;Fi[	@”iI"Ω      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        Rubygems.hash
      end;Fi[	@òiHI"ÿ      def function(arglist)
        @var.!.! and @var.!
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        tell_me(var, nil, var) if debug?
      end;Fi[	@si&I"Ω        end
        
        attr_reader :function
        
        def function(arglist)
          @var = true
        end
        
        def function(arglist)
          true
        end;Fi[	@Ωi3I"‚        Interceptor::Pipe.unwrap!(:stdout)
        Interceptor::Pipe.unwrap!(:stderr)
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = false
      end;Fi[	@˚i†I"æ      def function(arglist)
        @var ||= []
      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var ||= false
      end;Fi[@(@œ@–I"@var = nil;FI"end;FI" ;Fi[[	I"Fdata//activerecord_proj/active_record/associations/association.rb;TiTI"      # actually gets built.
      def function(arglist)
        @var ||= AssociationScope.new(self).scope if klass
      end
      
      def function(arglist)
        @var = nil
      end
      
      # Set the inverse association, if possible
      def function(arglist);Fi[	@ìiœI"·      
      def function(arglist)
        @var ||= klass.connection.columns(var)
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        check_validity_of_inverse!;Fi[	@-ieI"—      def function(arglist)
        @var.text!(var)
        @var.br
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        @var.text!(@var) if @var;Fi[	@
	i©I"Ó      def function(arglist)
        return if (@var[:no_multiline] or @var)
        @var = var
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        return if (@var.! or @var);Fi[	@	i3I"Ω      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        return unless @var;Fi[	@gi,I"◊      def function(arglist)
        (hooks[var.to_sym] << var)
        var
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        transforms.unshift(var);Fi[	@äi~I"ß      
      def function(arglist)
        begin_rb_scenario(var)
      end
      
      def function(arglist)
        @var = nil
      end
      
      private
      ;Fi[@^I"*#   class Person < ActiveRecord::Base;FI"#     has_many :pets;TI"#   end;TI"#;TI"#   person.pets;Ti[[	@&i$I"A      # Works in two ways.
      #
      # *First:* Specify a subset of fields to be selected from the result set.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&i]I"≠      # Finds an object in the collection responding to the +id+. Uses the same
      # rules as <tt>ActiveRecord::Base.find</tt>. Returns <tt>ActiveRecord::RecordNotFound</tt>
      # error if the object can not be found.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&i{I"ï      # Returns the first record, or the first +n+ records, from the collection.
      # If the collection is empty, the first form returns +nil+, and the second
      # form returns an empty array.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&iîI"ì      # Returns the last record, or the last +n+ records, from the collection.
      # If the collection is empty, the first form returns +nil+, and the second
      # form returns an empty array.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&iI"W      
      # Replace this collection with +other_array+. This will perform a diff
      # and delete/add only records that have changed.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [#<Pet id: 1, name: "Gorby", group: "cats", person_id: 1>]
      #;Ti[	@&i@I"r      # contain the same number of elements and if each element is equal
      # to the corresponding element in the other array, otherwise returns
      # +false+.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #      #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&iZI"c      
      # Returns a new array of objects from the collection. If the collection
      # hasn't been loaded, it fetches the records from the database.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 4, name: "Benny", person_id: 1>,;Ti[@˘I"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");Fi[[	@˛i˝I"  #   assert_version_detected '7.0.4', 'http://www.haskell.org/ghc/dist/7.0.4/ghc-7.0.4-i386-apple-darwin.tar.bz2'
  # end
  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@˛i˛I"œ  # end
  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@˛iˇI"˜  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@˛i I"    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@˛iI"    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@˛iI"Â    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end;Fi[	@˛iI"π    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end
end;Fi[@(@)I"return var;FI"end;FI" ;FI"def function(arglist);Fi[[	@ÎiAI"!            var.each { |arglist| var[var] += "STRING" }
          else
            # do nothing
          end
        end
        return var
      end
      
      def function(arglist)
        var = Hash[var.map { |arglist| [var, "STRING"] }]
        var = add_index_length(var, var, var);Fi[	@-iìI"E              var = var.instance_variable_get("STRING")
              var = (var + var.size) unless var.nil?
            end
          end
        end
        return var
      end
      
      def function(arglist)
        var = var.format_args(lambda { |arglist| "STRING" })
        @var.div(:class => "STRING") do |arglist|;Fi[	I"(data//homebrew_proj/cmd/versions.rb;Ti#I"˙      unless (var.include?(var) or var.nil?) then
        yield(var, var) if block_given?
        (var << var)
      end
    end
    return var
  end
  
  def function(arglist)
    (Pathname.pwd == HOMEBREW_REPOSITORY) ? ("STRING") : ("STRING")
  end;Fi[	@üi I"◊        var.each { |arglist| (var << install_p(var, var)) }
      else
        (var << install_p(var))
      end
    end
    return var
  end
  
  def function(arglist)
    if var then
      var = File.basename(var);Fi[	@üiEI"ı        var.each { |arglist| (var << install_symlink_p(var, var)) }
      else
        (var << install_symlink_p(var))
      end
    end
    return var
  end
  
  def function(arglist)
    if var.nil? then
      var = (self + File.basename(var));Fi[	I"data//redmine_proj/diff.rb;Ti.I"À        while var do
          var[var[1]] = var[2]
          var = var[0]
        end
      end
      return var
    end
    
    def function(arglist)
      var = Diff.lcs(var, var)
      var = var = 0;Fi[	I")data//redmine_proj/SVG/Graph/Line.rb;TiBI"Ó            var = @var[-1][:data].min
          else
            var = @var.collect { |arglist| var[:data].min }.min
          end
        end
        return var
      end
      
      def function(arglist)
        @var[:fields]
      end;Fi[@@I" ;FI"def function(arglist);FI"@var = var;FI"	self;Fi[[	@ÏiI"¡      def function(arglist)
        @var ||= "STRING"
        @var = find_in_path(var)
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      ;Fi[	@iQI"ß          @var = "STRING"
          @var = false
        end
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      ;Fi[	@ùiI"∂        
        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist)
          @var = var
          self
        end
        ;Fi[	@Œi9I"†      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      ;Fi[	@Œi>I"†      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      ;Fi[	@ŒiCI"†      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      ;Fi[	@ŒiHI"†      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end
      ;Fi[I"end.to_yaml;FI"/File.open("STRING", "STRING") do |arglist|;FI"var.puts(var);FI"var.puts("STRING");FI"var.puts(var);FI"end;Fi[[	@Ωi"I"¨        var = ((var.strftime("STRING") + var) + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
        if var then
          var = var["STRING", "STRING"].all;Fi[	I"1data//jekyll_proj/jekyll/migrators/joomla.rb;TiI"∂        var = ("STRING" % [var.year, var.month, var.day, var])
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:id]), "STRING" => (var[:alias]), "STRING" => (var) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end;Fi[	I"3data//jekyll_proj/jekyll/migrators/mephisto.rb;Ti!I"ù        var = ([var.year, var.month, var.day, var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:entry_id]) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end;Fi[	I"-data//jekyll_proj/jekyll/migrators/mt.rb;TiI"≈        var = (([var.year, var.month, var.day, var].join("STRING") + "STRING") + self.suffix(var))
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:entry_id]), "STRING" => (var) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end;Fi[	I"4data//jekyll_proj/jekyll/migrators/posterous.rb;Ti.I"ß          var = ("STRING" % [var.year, var.month, var.day, var])
          var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |arglist|
            (var.nil? or (var == "STRING"))
          end.to_yaml
          File.open("STRING", "STRING") do |arglist|
            var.puts(var)
            var.puts("STRING")
            var.puts(var)
          end
        end
        var = (var + 1);Fi[	I"6data//jekyll_proj/jekyll/migrators/textpattern.rb;TiI"ß        var = ([var.strftime("STRING"), var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:Keywords].split("STRING")) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end;Fi[	I"4data//jekyll_proj/jekyll/migrators/wordpress.rb;Ti\I"      end
      var = { "STRING" => (var[:type].to_s), "STRING" => (var[:status].to_s), "STRING" => ((var[:status].to_s == "STRING")), "STRING" => (var.to_s), "STRING" => (var[:author].to_s), "STRING" => (var[:author_login].to_s), "STRING" => (var[:author_email].to_s), "STRING" => (var[:author_url].to_s), "STRING" => (var), "STRING" => (var), "STRING" => (var[:id]), "STRING" => (var[:guid].to_s), "STRING" => (var), "STRING" => (var[:categories] ? (var) : (nil)), "STRING" => (var[:tags] ? (var) : (nil)), "STRING" => (var[:comments] ? (var) : (nil)) }.delete_if do |arglist|
        (var.nil? or (var == "STRING"))
      end.to_yaml
      File.open("STRING", "STRING") do |arglist|
        var.puts(var)
        var.puts("STRING")
        var.puts(var)
      end
    end
    ;Fi[@
@I"0;FI"end;FI" ;FI"def function(arglist);Fi[[	@êiI"à    def function(arglist)
      []
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end;Fi[	@êiI"á    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end;Fi[	@êiI"á    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end;Fi[	@êiI"ä    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      true
    end;Fi[	@êi2I"à    def function(arglist)
      {}
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end;Fi[	@êi6I"í    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      # do nothing
    end;Fi[	@ÖiI"ë    def function(arglist)
      "STRING"
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      true
    end;Fi[@
@I"@var ||= [];FI"end;FI" ;FI"def function(arglist);Fi[[	@ai0I"$    def function(arglist)
      full_gem_path
    end
    
    def function(arglist)
      @var ||= []
    end
    
    def function(arglist)
      if @var and File.exist?(File.join(full_gem_path, "STRING")) then
        var = Dir.chdir(full_gem_path) { |arglist| `git rev-parse HEAD`.strip };Fi[	@giTI"      def function(arglist)
        @var ||= Hash.new { |arglist| var[var] = [] }
      end
      
      def function(arglist)
        @var ||= []
      end
      
      def function(arglist)
        hooks_for(:around, var).reverse.inject(var) do |arglist|
          proc do |arglist|;Fi[	@Ùi$I"L        def function(arglist)
          respond_to?(:visible?) ? (visible?(var)) : (true) and var.allowed_to?(self.class.attachable_options[:delete_permission], self.project)
        end
        
        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          @var ||= []
        end;Fi[	@Ùi(I"ˆ        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          if var.is_a?(Hash) then
            var = var.stringify_keys;Fi[	@§iI"          @var = nil
          default(ExampleGroup)
        end
        
        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          example_group_creation_listeners.each do |arglist|
            var.register_example_group(var);Fi[	I":data//rspec_proj/spec/example/shared_example_group.rb;Ti I"‹        end
        
        private
        
        def function(arglist)
          @var ||= []
        end
        
        def function(arglist)
          var = find(var.description)
          return false unless var;Fi[	@<	i›I"Ã      def function(arglist)
        (@var == @var)
      end
      
      def function(arglist)
        @var ||= []
      end
      
      def function(arglist)
        (similar_messages << var)
      end;Fi[@@@¨I" ;FI"private;FI" ;Fi[[	@i%I"          when Hash then
            resolve_hash_connection(config)
          else
            # do nothing
          end
        end
        
        private
        
        def function(arglist)
          var = configurations.fetch(var) { |arglist| connection_url_to_hash(var) };Fi[	@iêI"æ        begin
          exec_query("STRING")
        rescue Mysql::Error
          # do nothing
        end
      end
      
      private
      
      def function(arglist)
        var = {};Fi[	@ú
iLI"Ì          when /\(yes\/no\)/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        
        # Constructs the CVSROOT command-line option
        def function(arglist);Fi[	I"?data//capistrano_proj/capistrano/recipes/deploy/scm/git.rb;TiäI""          when /accept \(t\)emporarily/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        
        # If verbose output is requested, return nil, otherwise return the
        # command-line switch for "quiet" ("-q").;Fi[	@LiJI"ﬂ          when /yes\/no/i then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        
        # Fine grained mercurial commands
        def function(arglist);Fi[	@ÜiLI"7          when /Client \'[\w\-\_\.]+\' unknown.*/i then
            raise(Capistrano::Error, "STRING")
          else
            # do nothing
          end
        end
        
        private
        
        # Builds the set of authentication switches that perforce understands.
        def function(arglist);Fi[	I"Fdata//capistrano_proj/capistrano/recipes/deploy/scm/subversion.rb;TiUI"<          when /accept \(t\)emporarily/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        
        # If a username is configured for the SCM, return the command-line
        # switches for that. Note that we don't need to return the password;Fi[I"$(var.nil? or (var == "STRING"));FI"end.to_yaml;F@íI"var.puts(var);F@îI"var.puts(var);Fi[[	@Ωi!I">        var = var.strip.downcase.gsub(/(&|&amp;)/, "STRING").gsub(/[\s\.\/\\]/, "STRING").gsub(/[^\w-]/, "STRING").gsub(/[-_]{2,}/, "STRING").gsub(/^[-_]/, "STRING").gsub(/[-_]$/, "STRING")
        var = ((var.strftime("STRING") + var) + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
        if var then;Fi[	@õiI"         var = var[:content]
        var = ("STRING" % [var.year, var.month, var.day, var])
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:id]), "STRING" => (var[:alias]), "STRING" => (var) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end;Fi[	@ûi I"Æ        var = var[:body]
        var = ([var.year, var.month, var.day, var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:entry_id]) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end;Fi[	@°iI"Û        var = ((var + "STRING") + var) if var.!=(nil)
        var = (([var.year, var.month, var.day, var].join("STRING") + "STRING") + self.suffix(var))
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:entry_id]), "STRING" => (var) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end;Fi[	@§i-I"Ø          var = var["STRING"].!
          var = ("STRING" % [var.year, var.month, var.day, var])
          var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |arglist|
            (var.nil? or (var == "STRING"))
          end.to_yaml
          File.open("STRING", "STRING") do |arglist|
            var.puts(var)
            var.puts("STRING")
            var.puts(var)
          end
        end;Fi[	@ßiI"∏        var = var[:Body]
        var = ([var.strftime("STRING"), var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:Keywords].split("STRING")) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end;Fi[	@™i[I"H        var.sort! { |arglist| (var["STRING"] <=> var["STRING"]) }
      end
      var = { "STRING" => (var[:type].to_s), "STRING" => (var[:status].to_s), "STRING" => ((var[:status].to_s == "STRING")), "STRING" => (var.to_s), "STRING" => (var[:author].to_s), "STRING" => (var[:author_login].to_s), "STRING" => (var[:author_email].to_s), "STRING" => (var[:author_url].to_s), "STRING" => (var), "STRING" => (var), "STRING" => (var[:id]), "STRING" => (var[:guid].to_s), "STRING" => (var), "STRING" => (var[:categories] ? (var) : (nil)), "STRING" => (var[:tags] ? (var) : (nil)), "STRING" => (var[:comments] ? (var) : (nil)) }.delete_if do |arglist|
        (var.nil? or (var == "STRING"))
      end.to_yaml
      File.open("STRING", "STRING") do |arglist|
        var.puts(var)
        var.puts("STRING")
        var.puts(var)
      end
    end;Fi[@(@)@*@Ü	I"##;F@Ãi[[	@3i5I"´          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |arglist|
            connection.put_object(container, File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@6iWI"û          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |arglist|
            connection.put_file(File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@WiAI"§          files_to_transfer_for(@var) do |arglist|
            Logger.message(("STRING" + "STRING"))
            var.put(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@9i=I"†          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |arglist|
            var.files.create(:key => (var), :body => (var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@<i8I"µ          Logger.message(("STRING" + "STRING"))
          File.open(File.join(local_path, var), "STRING") do |arglist|
            connection.put_object(bucket, File.join(var, var), var)
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@ﬁi5I"¨          files_to_transfer_for(@var) do |arglist|
            Logger.message(("STRING" + "STRING"))
            var.scp.upload!(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@Zi3I"®          files_to_transfer_for(@var) do |arglist|
            Logger.message(("STRING" + "STRING"))
            var.upload!(File.join(local_path, var), File.join(var, var))
          end
        end
      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[@(@)@*I"var;FI"end;FI" ;Fi[[	@iùI"        var.gem_platforms(@var).each do |arglist|
          if (var or (var == generic(Gem::Platform.local))) then
            (var << DepProxy.new(var, var))
          end
        end
      end
      var
    end
    
    def function(arglist)
      @var.sort_by do |arglist|;Fi[	@+
i>I"9        else
          if (var.value.last == var.value.last) and (var.value.first.value == 0) then
            var.value[0] = Sass::Script::Number.new(var.value)
          end
        end
      end
      var
    end
    
    # returns the end position of the gradient from the color stop
    def function(arglist);Fi[	@ØiêI"⁄            var.add_atomic_changes(self, var, var, var, var, var)
          else
            var[var] = var unless atomic_unsets.include?(var)
          end
        end
      end
      var
    end
    
    private
    ;Fi[	I"0data//mongoid_proj/lib/mongoid/hierarchy.rb;Ti,I"◊              var.push(var)
              var.concat(var._children) unless var.versioned?
            end
          end
        end
      end
      var
    end
    
    # Marks all children as being persisted.
    #;Fi[	@éi1I"»                  var = 0
                end
              end
            end
          end
        end
        var
      end
      
      def function(arglist)
        var.SetFontStyle("STRING", 8);Fi[	I"Adata//state_machine_proj/state_machine/machine_collection.rb;Ti+I"q        each_value do |arglist|
          if var.dynamic_initial_state? then
            var.initialize_state(var, :force => ((var[:dynamic] == :force)), :to => (var[:to]))
          end
        end
      end
      var
    end
    
    # Runs one or more events in parallel on the given object.  See
    # StateMachine::InstanceMethods#fire_events for more information.;Fi[@^I"# `:warning`;FI"D# : The backup completed successfully, but warnings were logged;FI"C# : Notification will be sent, including a copy of the current;FI"6# : backup log, if `on_warning` was set to `true`;FI"#;Fi[[	@Œ	i#I"•      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@$i;I"•      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@'izI"•      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@*i!I"•      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@-i+I"•      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@0iI"•      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[@^I"# `:success`;FI"+# : The backup completed successfully.;FI"D# : Notification will be sent if `on_success` was set to `true`;FI"#;FI"# `:warning`;Fi[[	@Œ	iI"¶      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@$i7I"¶      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@'ivI"¶      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@*iI"¶      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@-i'I"¶      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@0iI"¶      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[@(@œI"module InstanceMethods;FI"def self.included(arglist);FI"var.extend(ClassMethods);FI"end;Fi[[	@ÇiI",          var[:author_key] = "STRING" if var[:author_key].is_a?(Symbol)
          self.activity_provider_options[var] = var
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        module ClassMethods;Fi[	@ÙiI"(          send(:include, Redmine::Acts::Attachable::InstanceMethods)
          before_save(:attach_saved_attachments)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        def function(arglist);Fi[	@˜iI"          validate(:validate_custom_field_values)
          after_save(:save_custom_field_values)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        def function(arglist);Fi[	@˙iI"O          self.event_options = var.merge(var)
          send(:include, Redmine::Acts::Event::InstanceMethods)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        ["STRING", "STRING", "STRING", "STRING", "STRING"].each do |arglist|;Fi[	@Öi&I"Y          searchable_options[:search_custom_fields] = reflect_on_association(:custom_values).nil?.!
          send(:include, Redmine::Acts::Searchable::InstanceMethods)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        module ClassMethods;Fi[	@˝iI"V          send(:include, Redmine::Acts::Watchable::InstanceMethods)
          alias_method_chain(:watcher_user_ids=, :uniq_ids)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        # Returns an array of users that are proposed as watchers;Fi[@<@=I"#   end;TI"#;TI"#   person.pets;TI"#   # => [;Ti[[	@&i%I"c      #
      # *First:* Specify a subset of fields to be selected from the result set.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@&i^I"ö      # rules as <tt>ActiveRecord::Base.find</tt>. Returns <tt>ActiveRecord::RecordNotFound</tt>
      # error if the object can not be found.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@&i|I"Å      # If the collection is empty, the first form returns +nil+, and the second
      # form returns an empty array.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@&iïI"Å      # If the collection is empty, the first form returns +nil+, and the second
      # form returns an empty array.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@&iAI"d      # to the corresponding element in the other array, otherwise returns
      # +false+.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #      #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #      #<Pet id: 2, name: "Spook", person_id: 1>;Ti[	@&i[I"ô      # Returns a new array of objects from the collection. If the collection
      # hasn't been loaded, it fetches the records from the database.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 4, name: "Benny", person_id: 1>,
      #   #       #<Pet id: 5, name: "Brain", person_id: 1>,;Ti[I"@var ||= "STRING";FI"try("STRING", var);FI"	self;FI"end;FI" ;FI"def function(arglist);Fi[[	@iI"–      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var);Fi[	@iI"–      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var);Fi[	@i I"–      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var);Fi[	@i&I"Ô      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        var = configuration.fetch(:gem_command, "STRING");Fi[	@i3I"–      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var);Fi[	@i9I"˜      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        var = Regexp.new(Regexp.escape(var.to_s)) unless var.is_a?(Regexp)
        var = {};Fi[@%@&I"#;FI"# `:failure`;FI"%# : The backup operation failed.;FI"H# : Notification will be sent, including the Exception which caused;Fi[[	@Œ	i&I"˛      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@$i>I"˛      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@'i}I"˛      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@*i$I"˛      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@-i.I"˛      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@0i"I"˛      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[@^I"# `:failure`;F@É@ÑI"F# : the failure, the Exception's backtrace, a copy of the current;FI"K# : backup log and other information if `on_failure` was set to `true`;Fi[[	@Œ	i(I"      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist);Fi[	@$i@I"      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist);Fi[	@'iI"      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist);Fi[	@*i&I"      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist);Fi[	@-i0I"      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist);Fi[	@0i$I"      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist);Fi[@$@%@&I"#;FI"# `:failure`;F@Éi[[	@Œ	i%I"˜      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@$i=I"˜      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@'i|I"˜      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@*i#I"˜      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@-i-I"˜      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@0i!I"˜      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[I"1# @param [ String ] name The attribute name.;FI"7# @param [ String ] meth The name of the accessor.;FI"#;FI"# @since 3.0.0;FI"def function(arglist);FI"/generated_methods.module_eval do |arglist|;Fi[[	@ØiI"ò      # @example Create the accessor.
      #   Model.create_dirty_change_accessor("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_change(var) }
        end;Fi[	@ØiI"î      # @example Create the check.
      #   Model.create_dirty_change_check("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_changed?(var) }
        end;Fi[	@Øi)I"©      # @example Create the check.
      #   Model.create_dirty_default_change_check("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_changed_from_default?(var) }
        end;Fi[	@Øi8I"ù      # @example Create the accessor.
      #   Model.create_dirty_previous_value_accessor("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_was(var) }
        end;Fi[	@ØiGI"ñ      # @example Create the flag.
      #   Model.create_dirty_change_flag("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_will_change!(var) }
        end;Fi[	@ØiVI"ã      # @example Create the reset.
      #   Model.create_dirty_reset("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| reset_attribute!(var) }
        end;Fi[@≤I"end;FI" ;F@!I"raise(NotImplementedError);FI"end;Fi[[	@¯iI"Ã      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist);Fi[	@¯iI"Ã      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist);Fi[	@¯iI"Ã      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist);Fi[	@¯iI"Ã      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist);Fi[	@¯i I"Ã      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist);Fi[	@¯i$I"∑      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
    end
  end;Fi[@P@QI"7# @param [ Symbol ] name The name of the relation.;FI"4# @param [ Hash ] options The relation options.;FI"D# @param [ Proc ] block Optional block for defining extensions.;FI"def function(arglist);Fi[[	I"7data//mongoid_proj/lib/mongoid/relations/macros.rb;Ti/I"…        #   class Address
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          if ancestors.include?(Mongoid::Versioning) then
            raise(Errors::VersioningNotOnRoot.new(self));Fi[	@‡iMI"¿        #   class Address
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          var = characterize(var, Embedded::Many, var, &var)
          self.cyclic = true if var[:cyclic];Fi[	@‡iiI"º        #   class Name
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          var = characterize(var, Embedded::One, var, &var)
          self.cyclic = true if var[:cyclic];Fi[	@‡iÄI"…        #   class Person
        #     include Mongoid::Document
        #     has_one :game
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          var = reference_one_to_one(var, var, Referenced::In, &var)
          aliased_fields[var.to_s] = var.foreign_key;Fi[	@‡iôI"¨        #   class Game
        #     include Mongoid::Document
        #     belongs_to :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          var = characterize(var, Referenced::Many, var, &var)
          relate(var, var);Fi[	@‡i”I"õ        #   class Person
        #     include Mongoid::Document
        #     has_one :game
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          reference_one_to_one(var, var, Referenced::One, &var)
        end;Fi[@(@œ@ç@éI"##;FI"7# Notify the user of the backup operation results.;Fi[[	@Œ	iI"˚      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@$i0I"         @var ||= "STRING"
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@'ioI"˚      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@*iI"˚      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@-i I"˚      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@0iI"˚      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[@
@I"@var = [];FI"end;FI" ;FI"def function(arglist);Fi[[	@;iI"ª      include(Enumerable)
      
      attr_reader(:duration)
      
      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        @var[var]
      end;Fi[	@'iI"«      
      class Builder
        attr_reader(:rows)
        
        def function(arglist)
          @var = []
        end
        
        def function(arglist)
          (@var << var)
        end;Fi[	@0i
I"Ÿ  module PySupport
    class PyLanguage
      include(LanguageSupport::LanguageMethods)
      
      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        (@var << var)
      end;Fi[	@‘iI"‹  module WireSupport
    class WireLanguage
      include(LanguageSupport::LanguageMethods)
      
      def function(arglist)
        @var = []
      end
      
      def function(arglist)
        # do nothing
      end;Fi[	I""data//homebrew_proj/debrew.rb;TiI"ª  attr_accessor(:prompt)
  
  attr_accessor(:entries)
  
  def function(arglist)
    @var = []
  end
  
  def function(arglist)
    (entries << { :name => (var), :action => (var) })
  end;Fi[	I";data//paperclip_proj/paperclip/io_adapters/registry.rb;TiI"´    end
    
    attr_reader(:registered_handlers)
    
    def function(arglist)
      @var = []
    end
    
    def function(arglist)
      (@var << [var, var])
    end;Fi[@T@¬@√@ƒ@≈I"private;Fi[[	@i$I"˘            resolve_string_connection(config.to_s)
          when Hash then
            resolve_hash_connection(config)
          else
            # do nothing
          end
        end
        
        private
        
        def function(arglist);Fi[	@ú
iKI"‰            "STRING"
          when /\(yes\/no\)/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        
        # Constructs the CVSROOT command-line option;Fi[	@·iâI"            "STRING"
          when /accept \(t\)emporarily/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        
        # If verbose output is requested, return nil, otherwise return the;Fi[	@LiII"÷            "STRING"
          when /yes\/no/i then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        
        # Fine grained mercurial commands;Fi[	@ÜiKI"H            raise(Capistrano::Error, "STRING")
          when /Client \'[\w\-\_\.]+\' unknown.*/i then
            raise(Capistrano::Error, "STRING")
          else
            # do nothing
          end
        end
        
        private
        
        # Builds the set of authentication switches that perforce understands.;Fi[	@ËiTI"            raise(Capistrano::Error, "STRING")
          when /accept \(t\)emporarily/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        
        # If a username is configured for the SCM, return the command-line;Fi[@ÓI"/# `status` indicates one of the following:;FI"#;FI"# `:success`;F@7@8i[[	@Œ	iI"0      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@$i5I"0      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@'itI"0      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@*iI"0      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@-i%I"0      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@0iI"0      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[@(@œ@–I"__raise("STRING");FI"end;FI" ;Fi[[	I"3data//rspec_proj/spec/mocks/error_generator.rb;TiI"ÿ      
      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        var = format_args(*var.expected_args);Fi[	@6iI"        var = format_args(*var.expected_args)
        var = var.empty? ? ("STRING") : (format_args(*var))
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING");Fi[	@6i I"       
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING");Fi[	@6i$I"       
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING");Fi[	@6i(I"       
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING");Fi[	@6i,I"©      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      private
      ;Fi[@&I"#;FI"# `:failure`;F@É@Ñ@îi[[	@Œ	i'I"˛      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@$i?I"˛      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@'i~I"˛      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@*i%I"˛      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@-i/I"˛      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@0i#I"˛      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[I"# `:warning`;F@$@%@&I"#;FI"# `:failure`;Fi[[	@Œ	i$I"‹      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@$i<I"‹      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@'i{I"‹      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@*i"I"‹      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@-i,I"‹      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@0i I"‹      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[I"# `:failure`;F@É@Ñ@î@ïI"#;Fi[[	@Œ	i)I"ÿ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var;Fi[	@$iAI"›      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var, var = case var;Fi[	@'i{I"›      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var, var = case var;Fi[	@*i'I"ÿ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var;Fi[	@-i1I"ÿ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var;Fi[	@0i%I"ÿ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var;Fi[@Æ@Ì@ÓI"@var = var;FI"end;FI" ;Fi[[	@ˇidI"    class MultiparameterAttribute
      attr_reader(:object, :name, :values, :column)
      
      def function(arglist)
        @var = var
        @var = var
        @var = var
      end
      
      def function(arglist)
        return if values.values.compact.empty?;Fi[	@Œ	iaI"L        ##
        # Instantiates a new Campfire::Room object and sets all the
        # necessary arguments (@room_id, @subdomain, @api_token)
        def function(arglist)
          @var = var
          @var = var
          @var = var
        end
        
        ##
        # Wrapper method for the #send_message (private) method;Fi[	@si<I"1        class Evaluator
          attr_reader(:configuration, :condition, :server)
          
          def function(arglist)
            @var = var
            @var = var
            @var = var
          end
          
          def function(arglist)
            configuration.roles[var].include?(server);Fi[	@¸i
I"    class RbHook
      attr_reader(:tag_expressions)
      
      def function(arglist)
        @var = var
        @var = var
        @var = var
      end
      
      def function(arglist)
        @var.current_world.cucumber_instance_exec(false, var, *[var, var].compact, &@var);Fi[	@‚iòI"ﬁ  fatal(ARGV.force?.!)
  
  # The user can chose to force installation even in the face of conflicts.
  def function(arglist)
    @var = var
    @var = var
    @var = var
  end
  
  def function(arglist)
    var = "STRING";Fi[	@Èi	I"Õ  class Processor
    attr_accessor(:file, :options, :attachment)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
    end
    
    def function(arglist)
      # do nothing;Fi[@^@¥@µI"#;FI"# @since 3.0.0;F@∏i[[	@Øi
I"î      #
      # @example Create the accessor.
      #   Model.create_dirty_change_accessor("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_change(var) };Fi[	@ØiI"ê      #
      # @example Create the check.
      #   Model.create_dirty_change_check("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_changed?(var) };Fi[	@Øi(I"•      #
      # @example Create the check.
      #   Model.create_dirty_default_change_check("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_changed_from_default?(var) };Fi[	@Øi7I"ô      #
      # @example Create the accessor.
      #   Model.create_dirty_previous_value_accessor("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_was(var) };Fi[	@ØiFI"í      #
      # @example Create the flag.
      #   Model.create_dirty_change_flag("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| attribute_will_change!(var) };Fi[	@ØiUI"á      #
      # @example Create the reset.
      #   Model.create_dirty_reset("name", "alias")
      #
      # @param [ String ] name The attribute name.
      # @param [ String ] meth The name of the accessor.
      #
      # @since 3.0.0
      def function(arglist)
        generated_methods.module_eval do |arglist|
          re_define_method("STRING") { |arglist| reset_attribute!(var) };Fi[@8I"#;FI"# `:warning`;F@$@%@&i[[	@Œ	i"I"Ü      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@$i:I"Ü      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@'iyI"Ü      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@*i I"Ü      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@-i*I"Ü      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@0iI"Ü      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[@Æ@Ì@`@a@bI"
begin;Fi[[	@iI"‰      end
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      begin
        (var = ActiveRecord::Base.connection.query_cache_enabled
        var = ActiveRecord::Base.connection_id;Fi[	@ÛiI"W      
      delegate(:connection, :establish_connection, :to => (ActiveRecord::Base))
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        begin
          (establish_connection(configuration_without_database)
          connection.create_database(configuration["STRING"], creation_options);Fi[	@	iI"Ä      
      delegate(:connection, :establish_connection, :clear_active_connections!, :to => (ActiveRecord::Base))
      
      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        begin
          (establish_master_connection unless var
          connection.create_database(configuration["STRING"], configuration.merge("STRING" => (encoding)));Fi[	@ iI"#      var.inject(Bundler.default_gemfile, Bundler.default_lockfile)
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist)
      begin
        (if Bundler.settings[:frozen] then
          Bundler.definition.ensure_equivalent_gemfile_and_lockfile(true);Fi[	@x	i8I"      end
      
      class HashIncludingMatcher
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          begin
            (@var.each do |arglist|
              return false unless var.has_key?(var) and (var == var[var]);Fi[	@x	iMI"      end
      
      class HashNotIncludingMatcher
        def function(arglist)
          @var = var
        end
        
        def function(arglist)
          begin
            (@var.each do |arglist|
              return false if var.has_key?(var) and (var == var[var]);Fi[@7@8I"#;FI"# `:warning`;F@$@%i[[	@Œ	i!I"§      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@$i9I"§      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@'ixI"§      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@*iI"§      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@-i)I"§      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@0iI"§      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[@É@Ñ@î@ïI"#;FI"def function(arglist);Fi[[	@Œ	i*I"Æ      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then;Fi[	@$iBI"≥      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var, var = case var
        when :success then;Fi[	@'i|I"≥      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var, var = case var
        when :success then;Fi[	@*i(I"Æ      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then;Fi[	@-i2I"Æ      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then;Fi[	@0i&I"Æ      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then;Fi[@Æ@•I"end;FI" ;F@0I"2broadcast(var) { |arglist| var.accept(self) };Fi[[	@™iI"ˇ        @var, @var, @var = var, var, var
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      ;Fi[	@™iI"        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      ;Fi[	@™i/I"      end
      
      # +feature_element+ is either Scenario or ScenarioOutline
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      ;Fi[	@™i;I"¸        broadcast(var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      ;Fi[	@™iOI"¸        broadcast(var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      ;Fi[	@™ioI"Ì        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      ;Fi[@(@œ@–I"@var ||= "STRING";F@nI"	self;Fi[[	@iI"π        @var = var
        @var = true
        @var = nil
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[	@iI"¡        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[	@iI"¡        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[	@i#I"¡        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[	@i0I"·        var = configuration.fetch(:gem_command, "STRING")
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[	@i6I"¡        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[I" assert_equal("STRING", var);FI"end;FI" ;FI"def function(arglist);FI"var = "STRING";FI")var.extend(StringInreplaceExtension);Fi[[	I"/data//homebrew_proj/test/test_inreplace.rb;TiI"2    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@˝iI"2    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@˝iI"2    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@˝i!I"2    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@˝i(I"(    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.remove_make_var!("STRING")
    assert_equal("STRING", var);Fi[	@˝i/I"*    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.remove_make_var!("STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.remove_make_var!(["STRING", "STRING"])
    assert_equal("STRING", var);Fi[@
@I"@var ||= "STRING";F@nI"	self;FI"end;Fi[[	@iI"¬        @var = true
        @var = nil
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist);Fi[	@iI"√        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist);Fi[	@iI"√        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist);Fi[	@i$I"√        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist);Fi[	@i1I"√        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist);Fi[	@i7I"√        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist);Fi[I"#@var = (HOMEBREW_CACHE + @var);FI"end;FI" ;FI"attr_reader :function;FI" ;FI"def function(arglist);Fi[[	@riµI"A    @@svn ||= "STRING"
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (@var + "STRING") if ARGV.include?("STRING")
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var.sub!(/^svn\+/, "STRING") if @var =~ /^svn\+http:\/\//
    ohai("STRING");Fi[	@riI"˜    super
    @@git ||= "STRING"
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var.!=(:revision) and host_supports_depth?
  end;Fi[	@ri\I"Ù  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    ohai("STRING")
    var, var = split_url(@var);Fi[	@riáI"  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var ||= ["STRING", "STRING", "STRING"].find { |arglist| File.executable?(var) }
  end;Fi[	@riØI"  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var ||= ["STRING", "STRING"].find { |arglist| File.executable?(var) }
  end;Fi[	@riÕI"  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var ||= ["STRING", "STRING"].find { |arglist| File.executable?(var) }
  end;Fi[@
I"private;FI" ;FI"##;F@Ó@ i[[	@Œ	iI"Ú        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@$i1I"˘        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@'ipI"Ú        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@*iI"Ú        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@-i!I"Ú        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@0iI"Ú        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[I"# `:success`;F@7@8I"#;FI"# `:warning`;F@$i[[	@Œ	i I"’      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@$i8I"’      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@'iwI"’      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@*iI"’      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@-i(I"’      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@0iI"’      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[@
@ˆ@Ó@ I"#;FI"# `:success`;Fi[[	@Œ	iI"&      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@$i3I"&      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@'irI"&      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@*iI"&      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@-i#I"&      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@0iI"&      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[@äI"var = 0;FI"var = 0;FI"var = 1;F@wI"?var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[[	@ñioI"‰      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then;Fi[	@ñi≥I"    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then;Fi[	@ôidI"“      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var
      if (var == 10) then;Fi[	@ôiÆI"¸    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var
      if (var == 10) then;Fi[	@úi^I"‰      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then;Fi[	@úi¢I"    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then;Fi[@(@)@Á@I@J@Ki[[	@ÇiI"4          var[:find_options] ||= {}
          var[:author_key] = "STRING" if var[:author_key].is_a?(Symbol)
          self.activity_provider_options[var] = var
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        ;Fi[	@ÙiI"u          has_many(:attachments, var.merge(:as => :container, :order => "STRING", :dependent => :destroy))
          send(:include, Redmine::Acts::Attachable::InstanceMethods)
          before_save(:attach_saved_attachments)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        ;Fi[	@˜iI"=          send(:include, Redmine::Acts::Customizable::InstanceMethods)
          validate(:validate_custom_field_values)
          after_save(:save_custom_field_values)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        ;Fi[	@˙iI"+          cattr_accessor(:event_options)
          self.event_options = var.merge(var)
          send(:include, Redmine::Acts::Event::InstanceMethods)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        ;Fi[	@Öi%I"é          searchable_options[:order_column] ||= searchable_options[:date_column]
          searchable_options[:search_custom_fields] = reflect_on_association(:custom_values).nil?.!
          send(:include, Redmine::Acts::Searchable::InstanceMethods)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        ;Fi[	@˝iI""          end
          send(:include, Redmine::Acts::Watchable::InstanceMethods)
          alias_method_chain(:watcher_user_ids=, :uniq_ids)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        ;Fi[@@Ó@ I"#;FI"# `:success`;F@7i[[	@Œ	iI"$      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@$i4I"$      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@'isI"$      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@*iI"$      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@-i$I"$      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@0iI"$      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[I"/File.open("STRING", "STRING") do |arglist|;FI"var.puts(var);F@îI"var.puts(var);FI"end;FI"end;Fi[[	@õiI"}        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:id]), "STRING" => (var[:alias]), "STRING" => (var) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end;Fi[	@ûi"I"U        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:entry_id]) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end;Fi[	@°iI"g        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:entry_id]), "STRING" => (var) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
    ;Fi[	@§i/I"ö          var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |arglist|
            (var.nil? or (var == "STRING"))
          end.to_yaml
          File.open("STRING", "STRING") do |arglist|
            var.puts(var)
            var.puts("STRING")
            var.puts(var)
          end
        end
        var = (var + 1)
        var = JSON.parse(self.fetch("STRING").body);Fi[	@ßiI"e        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:Keywords].split("STRING")) }.delete_if do |arglist|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end;Fi[	@™i]I"&      var = { "STRING" => (var[:type].to_s), "STRING" => (var[:status].to_s), "STRING" => ((var[:status].to_s == "STRING")), "STRING" => (var.to_s), "STRING" => (var[:author].to_s), "STRING" => (var[:author_login].to_s), "STRING" => (var[:author_email].to_s), "STRING" => (var[:author_url].to_s), "STRING" => (var), "STRING" => (var), "STRING" => (var[:id]), "STRING" => (var[:guid].to_s), "STRING" => (var), "STRING" => (var[:categories] ? (var) : (nil)), "STRING" => (var[:tags] ? (var) : (nil)), "STRING" => (var[:comments] ? (var) : (nil)) }.delete_if do |arglist|
        (var.nil? or (var == "STRING"))
      end.to_yaml
      File.open("STRING", "STRING") do |arglist|
        var.puts(var)
        var.puts("STRING")
        var.puts(var)
      end
    end
    
    def self.clean_entities(arglist);Fi[@ I"#;FI"# `:success`;F@7@8I"#;Fi[[	@Œ	iI"h      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@$i6I"h      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@'iuI"h      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@*iI"h      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@-i&I"h      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@0iI"h      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[I"if (@var == "STRING") then;FI"out("STRING");FI"	else;FI"if (@var == "STRING") then;FI"out("STRING");FI"	else;Fi[[	@üiÊI"Ù  def function(arglist)
    out("STRING")
    out("STRING")
    if (@var == "STRING") then
      out("STRING")
    else
      if (@var == "STRING") then
        out("STRING")
      else
        if (@var == "STRING") then
          out("STRING");Fi[	@üiÈI""    if (@var == "STRING") then
      out("STRING")
    else
      if (@var == "STRING") then
        out("STRING")
      else
        if (@var == "STRING") then
          out("STRING")
        else
          out((("STRING" + (@var / 100)) + "STRING")) if @var.kind_of?(String).!
        end;Fi[	@üiÛI"—        end
      end
    end
    if (@var == "STRING") then
      out("STRING")
    else
      if (@var == "STRING") then
        out("STRING")
      else
        out("STRING") if (@var == "STRING")
      end;Fi[	@ài	I"Ù  def function(arglist)
    out("STRING")
    out("STRING")
    if (@var == "STRING") then
      out("STRING")
    else
      if (@var == "STRING") then
        out("STRING")
      else
        if (@var == "STRING") then
          out("STRING");Fi[	@ài	I"    if (@var == "STRING") then
      out("STRING")
    else
      if (@var == "STRING") then
        out("STRING")
      else
        if (@var == "STRING") then
          out("STRING")
        else
          out((("STRING" + (@var / 100)) + "STRING")) if @var.is_a?(String).!
        end;Fi[	@ài	I"—        end
      end
    end
    if (@var == "STRING") then
      out("STRING")
    else
      if (@var == "STRING") then
        out("STRING")
      else
        out("STRING") if (@var == "STRING")
      end;Fi[@
@I@J@KI"end;FI" ;Fi[[	@ÇiI"C          self.activity_provider_options[var] = var
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        module ClassMethods
          # Returns events of type event_type visible by user that occured between from and to;Fi[	@ÙiI"r          before_save(:attach_saved_attachments)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        def function(arglist)
          respond_to?(:visible?) ? (visible?(var)) : (true) and var.allowed_to?(self.class.attachable_options[:view_permission], self.project);Fi[	@˜iI"          after_save(:save_custom_field_values)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        def function(arglist)
          CustomField.where("STRING").sorted.all;Fi[	@˙iI":          send(:include, Redmine::Acts::Event::InstanceMethods)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        ["STRING", "STRING", "STRING", "STRING", "STRING"].each do |arglist|
          var = "STRING";Fi[	@Öi'I")          send(:include, Redmine::Acts::Searchable::InstanceMethods)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        module ClassMethods
          # Searches the model for the given tokens;Fi[	@˝iI"0          alias_method_chain(:watcher_user_ids=, :uniq_ids)
        end
      end
      
      module InstanceMethods
        def self.included(arglist)
          var.extend(ClassMethods)
        end
        
        # Returns an array of users that are proposed as watchers
        def function(arglist);Fi[@•I"end;FI" ;F@0@÷I"end;Fi[[	@™iI"Ú      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist);Fi[	@™iI"Ú      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist);Fi[	@™i0I"(      
      # +feature_element+ is either Scenario or ScenarioOutline
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist);Fi[	@™i<I"Ú      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist);Fi[	@™iPI"Ú      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist);Fi[	@™ipI"Ú      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist);Fi[@nI"	self;FI"end;FI" ;F@rI"@var ||= "STRING";Fi[[	@iI"”      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self;Fi[	@iI"”      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self;Fi[	@i!I"”      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self;Fi[	@i'I"       
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        var = configuration.fetch(:gem_command, "STRING")
        try("STRING", var);Fi[	@i.I"      def function(arglist)
        @var ||= "STRING"
        var = configuration.fetch(:gem_command, "STRING")
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self;Fi[	@i4I"”      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self;Fi[@
@I"@var, @var = var, var;FI"end;FI" ;FI"def function(arglist);Fi[[	@i
I"ˇ  module ConnectionAdapters
    class ConnectionSpecification
      attr_reader(:config, :adapter_method)
      
      def function(arglist)
        @var, @var = var, var
      end
      
      def function(arglist)
        @var = var.config.dup
      end;Fi[	I"Idata//activerecord_proj/active_record/tasks/sqlite_database_tasks.rb;Ti	I"c  module Tasks
    class SQLiteDatabaseTasks
      delegate(:connection, :establish_connection, :to => (ActiveRecord::Base))
      
      def function(arglist)
        @var, @var = var, var
      end
      
      def function(arglist)
        raise(DatabaseAlreadyExists) if File.exist?(configuration["STRING"])
        establish_connection(configuration);Fi[	@'i"I"2        include(Gherkin::Formatter::Escaping)
        
        attr_reader(:exception)
        
        def function(arglist)
          @var, @var = var, var
        end
        
        def function(arglist)
          return if Cucumber.wants_to_quit
          each { |arglist| var.visit_table_cell(var) };Fi[	I"-data//cucumber_prok/cucumber/ast/tags.rb;Ti
I"  module Ast
    class Tags
      attr_reader(:tags)
      
      def function(arglist)
        @var, @var = var, var
      end
      
      def function(arglist)
        return if Cucumber.wants_to_quit
        @var.each { |arglist| var.visit_tag_name(var.name) };Fi[	I":data//cucumber_prok/cucumber/step_definition_light.rb;TiI"module Cucumber
  class StepDefinitionLight
    attr_reader(:regexp_source, :file_colon_line)
    
    def function(arglist)
      @var, @var = var, var
    end
    
    def function(arglist)
      (regexp_source == var.regexp_source) and (file_colon_line == var.file_colon_line)
    end;Fi[	I"=data//cucumber_prok/cucumber/wire_support/wire_packet.rb;TiI"÷      end
      
      attr_reader(:message, :params)
      
      def function(arglist)
        @var, @var = var, var
      end
      
      def function(arglist)
        var = [@var]
        (var << @var) if @var;Fi[I"private;FI" ;FI"##;F@Ó@ I"#;Fi[[	@Œ	iI"        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@$i2I"        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@'iqI"        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@*iI"        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@-i"I"        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@0iI"        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[@Æ@{@|I"end;FI"end;FI"end;Fi[[	I"?data//nokogiri_proj/nokogiri/xml/processing_instruction.rb;TiI"êmodule Nokogiri
  module XML
    class ProcessingInstruction < Node
      def function(arglist)
        # do nothing
      end
    end
  end
end;Fi[	@ºilI"˙        # Called when processing instructions are found
        # +name+ is the target of the instruction
        # +content+ is the value of the instruction
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@øièI"‡        end
        
        # This method is invoked at the very end. Allows the formatter to clean up, like closing open streams.
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@ÒiI"à          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@ÙiI"à          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@•iI"~        
        private
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[@Æ@≤I"end;FI" ;F@!@ i[[	@¯iI"√        @var = var
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      ;Fi[	@¯iI"”        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      ;Fi[	@¯iI"”        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      ;Fi[	@¯iI"”        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      ;Fi[	@¯iI"”        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      ;Fi[	@¯i#I"‘        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
      
      def function(arglist)
        raise(NotImplementedError)
      end
    end;Fi[@ÆI"@var ||= "STRING";F@nI"	self;FI"end;FI" ;Fi[[	@iI"»        @var = nil
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING";Fi[	@iI"¬        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING";Fi[	@iI"¬        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING";Fi[	@i%I"¬        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING";Fi[	@i2I"¬        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING";Fi[	@i8I"Û        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        var = Regexp.new(Regexp.escape(var.to_s)) unless var.is_a?(Regexp);Fi[@
@I"return "STRING";FI"end;FI"end;FI"end;Fi
[
[	I",data//redmine_proj/SVG/Graph/BarBase.rb;Ti2I"        end
        return var
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@¢iêI"Ø        @var = ((width - @var) - @var)
        @var = ((height - @var) - @var)
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@ziãI"á          var = (var - 1))
        end
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	I"(data//redmine_proj/SVG/Graph/Pie.rb;Ti«I"†        var = (10 ** var.to_f)
        ((var * var).to_i / var)
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@≤iøI"ç      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[@ÆI"var = "STRING";FI")var.extend(StringInreplaceExtension);FI"-var.change_make_var!("STRING", "STRING");FI" assert_equal("STRING", var);FI"end;Fi
[
[	@˝iI"require("testing_env")
require("extend/string")
class InreplaceTest < Test::Unit::TestCase
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist);Fi[	@˝iI"Í    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist);Fi[	@˝iI"Í    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist);Fi[	@˝iI"Í    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist);Fi[	@˝i$I"Í    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist);Fi[@
@@¯@≤@ßI"end;Fi
[
[	@ˇicI"„    
    class MultiparameterAttribute
      attr_reader(:object, :name, :values, :column)
      
      def function(arglist)
        @var = var
        @var = var
        @var = var
      end
      
      def function(arglist);Fi[	@si;I"        
        class Evaluator
          attr_reader(:configuration, :condition, :server)
          
          def function(arglist)
            @var = var
            @var = var
            @var = var
          end
          
          def function(arglist);Fi[	@¸i	I"–  module RbSupport
    class RbHook
      attr_reader(:tag_expressions)
      
      def function(arglist)
        @var = var
        @var = var
        @var = var
      end
      
      def function(arglist);Fi[	@qi
I"    module Errors
      class AttributeAssignmentError < Mongoid::Errors::MongoidError
        attr_reader(:exception, :attribute)
        
        def function(arglist)
          @var = var
          @var = var
          @var = var
        end
      end
      ;Fi[	@ÈiI"Àmodule Paperclip
  class Processor
    attr_accessor(:file, :options, :attachment)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
    end
    
    def function(arglist);Fi[@(I"var = -1;FI"var = var;FI"var = 0;FI"if (var == 1) then;FI"@var = @var;Fi
[
[	@ñi⁄I"c        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@ôi‚I"c        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@úi…I"c        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@üi∫I"?        else
          self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
          var = (var + 1)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@ài€I"*        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var + 1)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (var - (3 * @var));Fi[I"(@var << "STRING");FI"end;FI" ;FI"def function(arglist);F@üI"end;Fi
[
[	@-iSI"ƒ      
      def function(arglist)
        @var = []
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist);Fi[	@-iWI"º      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist);Fi[	@-i[I"º      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist);Fi[	@-iÆI"º      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist);Fi[	@-iæI"º      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist);Fi[@
I"6source_root(File.expand_path("STRING", "STRING"));FI" ;FI"desc("STRING");FI" ;FI"
hide!;Fi
[
[	@Y	i,I"    
    class SharedViewsGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      
      # Override copy_views to just copy mailer and shared.;Fi[	@Y	i;I"’    
    class FormForGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
    end
    ;Fi[	@Y	iEI"€    
    class SimpleFormForGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
    end
    ;Fi[	@Y	iOI"Á    
    class ErbGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      
      def function(arglist);Fi[	@Y	i]I"Î    
    class MarkerbGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      
      def function(arglist);Fi[I"include(ViewPathTemplates);FI" ;FI"6source_root(File.expand_path("STRING", "STRING"));FI" ;FI"desc("STRING");FI" ;Fi
[
[	@Y	i+I"€    end
    
    class SharedViewsGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      ;Fi[	@Y	i:I"ÿ    end
    
    class FormForGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
    end;Fi[	@Y	iDI"ﬁ    end
    
    class SimpleFormForGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
    end;Fi[	@Y	iNI"”    end
    
    class ErbGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      ;Fi[	@Y	i\I"◊    end
    
    class MarkerbGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      ;Fi[I"# @author Nicola Asuni;FI"# @since 1.5.2;FI"#;FI"def function(arglist);FI"return @var;FI"end;Fi
[
[	@àiûI"‘  #
  	# Returns the image scale.
  	# @return float image scale.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_image_scale, :GetImageScale);Fi[	@ài™I"◊  #
  	# Returns the page width in units.
  	# @return int page width.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_page_width, :GetPageWidth);Fi[	@ài∂I"€  #
  	# Returns the page height in units.
  	# @return int page height.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_page_height, :GetPageHeight);Fi[	@ài¬I"‡  #
  	# Returns the page break margin.
  	# @return int page break margin.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_break_margin, :GetBreakMargin);Fi[	@àiŒI"ˆ  #
  	# Returns the scale factor (number of points in user unit).
  	# @return int scale factor.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_scale_factor, :GetScaleFactor);Fi[I"# @since 1.5.2;FI"#;FI"def function(arglist);FI"return @var;FI"end;FI" ;Fi
[
[	@àiüI"”  	# Returns the image scale.
  	# @return float image scale.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_image_scale, :GetImageScale)
  ;Fi[	@ài´I"÷  	# Returns the page width in units.
  	# @return int page width.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_page_width, :GetPageWidth)
  ;Fi[	@ài∑I"⁄  	# Returns the page height in units.
  	# @return int page height.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_page_height, :GetPageHeight)
  ;Fi[	@ài√I"ﬂ  	# Returns the page break margin.
  	# @return int page break margin.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_break_margin, :GetBreakMargin)
  ;Fi[	@àiœI"ı  	# Returns the scale factor (number of points in user unit).
  	# @return int scale factor.
  	# @author Nicola Asuni
  	# @since 1.5.2
  	#
  def function(arglist)
    return @var
  end
  
  alias_method(:get_scale_factor, :GetScaleFactor)
  ;Fi[@e@f@g@hI" ;FI"def function(arglist);Fi
[
[	@Di*I"Ò                [var, var]
              end]
            else
              var
            end
          end
        end
        
        def function(arglist)
          Hash.===(var) ? (ActiveSupport::JSON.encode(var)) : (var)
        end;Fi[	@iI"È            var[var] = (var == :asc) ? (:desc) : (:asc)
          end
        else
          var
        end
      end
    end
    
    def function(arglist)
      var.is_a?(Array) and var.all? { |arglist| var.is_a?(String) }
    end;Fi[	@iÆI"ﬂ          var.pos = var.pos = var
          var
        else
          var
        end
      end
    end
    
    def function(arglist)
      raise(var) if var.message.include?("STRING")
      var = session_map[var.session];Fi[	@•i+I"3          if var.is_a?(String) and (project_path and absolute_path?(var).!) then
            File.join(project_path, var)
          else
            var
          end
        end
      end
      
      def function(arglist)
        ((var.index(File::SEPARATOR) == 0) or (var.index("STRING") == 1))
      end;Fi[	@ŒiïI"              (var.args.inject(0) { |arglist| (var + var.to_s.length) } == var)
            end
          else
            var
          end
        end
      end
      
      def function(arglist)
        if var = programming_language_for(var) then
          log.debug("STRING");Fi[@(@œ@–I"super("STRING");FI"end;FI"end;Fi
[
[	@KiwI"‚    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end
class ExplicitVersionSpecTestBall < Formula
  homepage("STRING");Fi[	@KiãI"¥    version("STRING")
    url("STRING")
    sha1("STRING")
  end
  
  def function(arglist)
    super("STRING")
  end
end
class OldBottleSpecTestBall < Formula
  homepage("STRING");Fi[	@KiõI"∏  bottle do |arglist|
    url("STRING")
    sha1("STRING")
  end
  
  def function(arglist)
    super("STRING")
  end
end
class AncientBottleSpecTestBall < Formula
  homepage("STRING");Fi[	@KiI"‡    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end
class RevisedBottleSpecTestBall < Formula
  homepage("STRING");Fi[	@KiI"…    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end
class CustomVersionScheme < Version
end;Fi[@
@I"@var = nil;FI"end;FI" ;FI"def function(arglist);Fi
[
[	@ìi–I"0      def function(arglist)
        @var ||= klass.connection.columns(var)
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        check_validity_of_inverse!
        if has_and_belongs_to_many? and (association_foreign_key == foreign_key) then;Fi[	@-ifI"Õ        @var.text!(var)
        @var.br
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        @var.text!(@var) if @var
        @var = "STRING";Fi[	@
	i™I"„        return if (@var[:no_multiline] or @var)
        @var = var
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        return if (@var.! or @var)
        @var = 0;Fi[	@	i4I"À      def function(arglist)
        @var = var
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        return unless @var
        var ||= @var;Fi[	@gi-I"«        (hooks[var.to_sym] << var)
        var
      end
      
      def function(arglist)
        @var = nil
      end
      
      def function(arglist)
        transforms.unshift(var)
        var;Fi[@(@œ@–I"[];FI"end;FI" ;Fi
[
[	@êi	I"û  module NullRelation
    def function(arglist)
      @var = []
    end
    
    def function(arglist)
      []
    end
    
    def function(arglist)
      0;Fi[	@∏iI"ﬁ      class EmptyBackground
        def function(arglist)
          false
        end
        
        def function(arglist)
          []
        end
        
        def function(arglist)
          StepCollection.new(var);Fi[	@Xi@I"|  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    []
  end
  
  def function(arglist)
    [];Fi[	@XiDI"¨  
  def function(arglist)
    []
  end
  
  def function(arglist)
    []
  end
  
  def self.excluding_blocks(arglist)
    var = var.blocks.map { |arglist| var.person_id };Fi[	@ÓiI"¡      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        []
      end
      
      def function(arglist)
        return if var.backtrace.nil?;Fi[@(@œ@–I"@var[var] = var;FI"end;FI" ;Fi
[
[	@˛i•I"Ô        class TypeMap
          def function(arglist)
            @var = {}
          end
          
          def function(arglist)
            @var[var] = var
          end
          
          def function(arglist)
            @var[var];Fi[	@iI"∫      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var[var] = var
      end
      
      def function(arglist)
        @var.abort!;Fi[	@@i1I"¬      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var[var] = var
      end
      
      def function(arglist)
        return @var if @var;Fi[	@iÑI"€        def function(arglist)
          @var = var
          @var = var
        end
        
        def function(arglist)
          @var[var] = var
        end
        
        def function(arglist)
          @var[var];Fi[	@ƒ	ipI"      
      def function(arglist)
        var ? (cache(var, &var)) : (call_cached(var))
      end
      
      def function(arglist)
        @var[var] = var
      end
      
      def function(arglist)
        (@var[var].arity == 1) ? (@var[var].call(@var)) : (@var[var].call);Fi[I"B@var = "STRING" unless (var.to_s.empty? or (var == "STRING"));FI"#@var = (HOMEBREW_CACHE + @var);FI"end;FI" ;FI"attr_reader :function;FI" ;Fi
[
[	@riI"	  def function(arglist)
    super
    @@git ||= "STRING"
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var.!=(:revision) and host_supports_depth?;Fi[	@ri[I"
class CVSDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    ohai("STRING");Fi[	@riÜI"Rclass MercurialDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var ||= ["STRING", "STRING", "STRING"].find { |arglist| File.executable?(var) };Fi[	@riÆI"Eclass BazaarDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var ||= ["STRING", "STRING"].find { |arglist| File.executable?(var) };Fi[	@riÃI"Eclass FossilDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist)
    @var ||= ["STRING", "STRING"].find { |arglist| File.executable?(var) };Fi[I"super(var);FI"(instance_eval(&var) if block_given?;FI"end;FI" ;FI"private;FI" ;Fi
[
[	@Œ	iI"Ó      ##
      # Campfire account's room id
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.;Fi[	@'imI"      # Default location is '$HOME/Backup/emails'
      # Example: '/tmp/test-mails'
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.;Fi[	@*iI"      # API-Key
      # Create a Prowl account and request an API key on prowlapp.com.
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.;Fi[	@-iI"Ù      ##
      # The priority of the notification
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.;Fi[	@0iI"Â      ##
      # OAuth credentials
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.;Fi[@e@f@˛I"protected;FI" ;FI"def function(arglist);Fi
[
[	@i%I"q            var[:encoding] = "STRING" if (type == :binary)
            var[:type] = (type == :string) ? (nil) : (type)
            var[:nil] = true if value.nil?
            var
          end
          
          protected
          
          def function(arglist)
            return if value.nil?
            var = ActiveSupport::XmlMini::TYPE_NAMES[value.class.name];Fi[	@ iÓI"Ô      # Thor::Shell::Basic class.
      #
      def function(arglist)
        var
      end
      
      protected
      
      def function(arglist)
        return var unless var.is_a?(Symbol)
        self.class.const_get(var.to_s.upcase);Fi[	I"@data//capistrano_proj/capistrano/configuration/execution.rb;TijI"A        trigger(var[:before], var) if var[:before]
        var = execute_task(var)
        trigger(var[:after], var) if var[:after]
        var
      end
      
      protected
      
      def function(arglist)
        return if Thread.current[:rollback_requests].nil?
        rollback_requests.reverse.each do |arglist|;Fi[	@°i∑I"#              var.errors.add(var, (var.present? ? (var) : (:blank)))
            end
          end
          var
        end
        
        protected
        
        def function(arglist)
          @var ||= Devise::ParamFilter.new(case_insensitive_keys, strip_whitespace_keys)
        end;Fi[	I"9data//rspec_proj/spec/runner/example_group_runner.rb;TiI"        var = true
        example_groups.each { |arglist| var = var.&(var.run(@var)) }
        finish
        var
      end
      
      protected
      
      def function(arglist)
        reporter.start(number_of_examples)
        example_groups.reverse! if reverse;Fi[@ÆI"super(var);F@OI"end;FI" ;FI"private;Fi
[
[	@Œ	iI"⁄      # Campfire account's subdomain
      ##
      # Campfire account's room id
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##;Fi[	@'ilI"6      # Folder where mail will be kept when using the `:file` `delivery_method` option.
      # Default location is '$HOME/Backup/emails'
      # Example: '/tmp/test-mails'
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##;Fi[	@*iI"È      ##
      # API-Key
      # Create a Prowl account and request an API key on prowlapp.com.
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##;Fi[	@-iI"’      # The message title
      ##
      # The priority of the notification
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##;Fi[	@0iI"’      # Twitter consumer key credentials
      ##
      # OAuth credentials
      def function(arglist)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##;Fi[@
@@1I"end;FI" ;FI"def function(arglist);Fi
[
[	@6iI"      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        var = format_args(*var.expected_args)
        var = var.empty? ? ("STRING") : (format_args(*var));Fi[	@6iI"Ì        var = var.empty? ? ("STRING") : (format_args(*var))
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end;Fi[	@6i!I"Õ      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end;Fi[	@6i%I"Õ      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end;Fi[	@6i)I"Õ      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end;Fi[@1I"end;FI" ;F@ÉI"__raise("STRING");FI"end;Fi
[
[	@6iI"      def function(arglist)
        var = format_args(*var.expected_args)
        var = var.empty? ? ("STRING") : (format_args(*var))
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist);Fi[	@6iI"∫      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist);Fi[	@6i#I"∫      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist);Fi[	@6i'I"∫      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist);Fi[	@6i+I"¨      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      private;Fi[I"/#     state_machine :initial => :parked do;FI"#       event :ignite do;FI",#         transition :parked => :idling;FI"#       end;FI"#     end;FI"#   end;Fi
[
[	@,icI"K    # == Examples
    # 
    #   class Vehicle < ActiveRecord::Base
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                       # => #<Vehicle id: nil, state: "parked">;Fi[	@®iI"A        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver;Fi[	@®iXI"A        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver;Fi[	@®i|I"A        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver;Fi[	@ài˝I"%    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new     # => #<Vehicle:0xb7b7f568 @state="parked">;Fi[@k@l@mI"end;FI" ;FI"def function(arglist);Fi
[
[	@˝i
I"(class InreplaceTest < Test::Unit::TestCase
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@˝iI"   
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@˝iI"   
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@˝iI"   
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@˝i&I"   
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[@äI"if var then;F@\I"var = "STRING";FI"var = "STRING";FI"var = "STRING";Fi
[
[	@ñiaI"    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING";Fi[	@ôiVI"    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING";Fi[	@úiPI"    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING";Fi[	@ài;I"ˇ    var = (var - (3 * @var))
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        if var.is_a?(String) then;Fi[	@àiÙI"    SetX(var)
    SetY(var)
    var = ((@var - var) - @var) if (var == 0)
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        if var.is_a?(String) then;Fi[@TI"#raise(ArgumentError, "STRING");FI"end;FI"end;FI"end;FI"end;Fi
[
[	@πiRI"|            self.nested_attributes_options = var
            var = var.collection? ? (:collection) : (:one_to_one)
            generated_feature_methods.module_eval("STRING", "STRING", (284 + 1))
          else
            raise(ArgumentError, "STRING")
          end
        end
      end
    end
    
    # Returns ActiveRecord::AutosaveAssociation::marked_for_destruction? It's;Fi[	@πiI"ˆ            var = "STRING"
            if respond_to?(var) then
              send(var, var.except(*UNASSIGNABLE_KEYS))
            else
              raise(ArgumentError, "STRING")
            end
          end
        end
      end
    end
    ;Fi[	@}idI"+                if var[:proc] then
                  remember_load(var) unless var[:reloading]
                  instance_eval(&var[:proc])
                else
                  raise(ArgumentError, "STRING")
                end
              end
            end
          end
        end
      end;Fi[	I";data//state_machine_proj/state_machine/eval_helpers.rb;TiLI"È        var.is_a?(Proc) ? (var.call(*var)) : (var.call(*var, &var))
      when String then
        eval(var, var.instance_eval { |arglist| binding }, &var)
      else
        raise(ArgumentError, "STRING")
      end
    end
  end
end;Fi[	I"*data//twitter_proj/twitter/factory.rb;TiI"°          $1.upcase
        end
        var.const_get(var.to_sym).fetch_or_new(var)
      else
        raise(ArgumentError, "STRING")
      end
    end
  end
end;Fi[@l@mI"end;FI" ;FI"def function(arglist);FI"var = "STRING";Fi
[
[	@˝iI"*  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@˝iI"*  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@˝iI"*  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@˝i I"*  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@˝i'I"   def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.remove_make_var!("STRING");Fi[@(I"	else;FI"var;FI"end;FI"end;FI"end;Fi
[
[	@DiqI"∂                else
                  nil
                end
              end
            else
              var
            end
          end
        end
        
        private;Fi[	@iI"‰        when Hash then
          var.each_with_object({}) do |arglist|
            var[var] = (var == :asc) ? (:desc) : (:asc)
          end
        else
          var
        end
      end
    end
    
    def function(arglist);Fi[	@äi¢I"ƒ            var.backtrace.clear
            var.backtrace.push(var.backtrace_line("STRING"))
            raise(var)
          end
        else
          var
        end
      end
    end
  end
end;Fi[	@ŒiìI"˝            end.min
            var.select do |arglist|
              (var.args.inject(0) { |arglist| (var + var.to_s.length) } == var)
            end
          else
            var
          end
        end
      end
      
      def function(arglist);Fi[	@çi&I"æ            var = var.call(var)
            var = modify_keys(var, &var)
            var.merge!(var => (var))
          end
        else
          var
        end
      end
    end
  end
end;Fi[@(@ÙI"
super;FI"end;FI"end;FI" ;Fi
[
[	@Îi˜I"            "STRING"
          else
            raise(ActiveRecordError, "STRING")
          end
        else
          super
        end
      end
      
      def function(arglist)
        var[:first] ? ((var << "STRING")) : ((var << "STRING") if var[:after]);Fi[	@tiLI"‘              end
            else
              super
            end
          else
            super
          end
        end
        
        def function(arglist)
          return super(var, var) unless var;Fi[	@öiEI"              "STRING"
            else
              raise(ActiveRecordError, "STRING")
            end
          else
            super
          end
        end
        
        # Returns a SELECT DISTINCT clause for a given set of columns and a given ORDER BY clause.
        #;Fi[	@J	i5I"Æ          Kernel.exec("STRING")
        else
          puts(File.read("STRING"))
        end
      else
        super
      end
    end
    
    desc("STRING", "STRING")
    ;Fi[	I"Ddata//state_machine_proj/state_machine/transition_collection.rb;Ti»I"        end
        if skip_after and success? then
          each { |arglist| var.machine.write(object, :event_transition, var) }
        end
      else
        super
      end
    end
    
    # Tracks that before callbacks have now completed
    def function(arglist);Fi[I"assert(var.i386?.!);FI"assert(var.x86_64?.!);FI"assert(var.ppc7400?.!);FI"assert(var.ppc64?.!);FI"assert(var.dylib?.!);FI"%assert(var.mach_o_executable?.!);Fi
[
[	I"*data//homebrew_proj/test/test_mach.rb;TiEI"9  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?);Fi[	@ioI"=  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!);Fi[	@iéI"@    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs);Fi[	@iûI"@    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs);Fi[	@iÆI"B    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert_equal([], var.archs);Fi[@(@œ@–I"teardown_db;FI"end;FI" ;Fi
[
[	I">data//redmine_proj/plugins/acts_as_list/test/list_test.rb;Ti2I"2  def function(arglist)
    setup_db
    (1..4).each { |arglist| ListMixin.create!(:pos => (var), :parent_id => 5) }
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    assert_equal([1, 2, 3, 4], ListMixin.find(:all, :conditions => "STRING", :order => "STRING").map(&:id));Fi[	@.iºI"J    (1..4).each do |arglist|
      (((var % 2) == 1) ? (ListMixinSub1) : (ListMixinSub2)).create!(:pos => (var), :parent_id => 5000)
    end
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    assert_equal([1, 2, 3, 4], ListMixin.find(:all, :conditions => "STRING", :order => "STRING").map(&:id));Fi[	I"Fdata//redmine_proj/plugins/acts_as_tree/test/acts_as_tree_test.rb;Ti?I"Ô    @var = TreeMixin.create!(:parent_id => (@var.id))
    @var = TreeMixin.create!
    @var = TreeMixin.create!
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    assert_equal(@var.children, [@var, @var]);Fi[	@3ióI"è    @var = RecursivelyCascadedTreeMixin.create!(:parent_id => (@var.id))
    @var = RecursivelyCascadedTreeMixin.create!(:parent_id => (@var.id))
    @var = RecursivelyCascadedTreeMixin.create!(:parent_id => (@var.id))
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    var = TreeMixin.find(:all, :include => :children, :conditions => "STRING", :order => "STRING");Fi[	@3iΩI"Ó    setup_db
    @var = TreeMixinWithoutOrder.create!
    @var = TreeMixinWithoutOrder.create!
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    assert([@var, @var].include?(TreeMixinWithoutOrder.root));Fi[@e@f@˛I"def function(arglist);FI""STRING";FI"end;Fi
[
[	@ÎitI"Í        var = select_one("STRING", "STRING")["STRING"]
        var = "STRING"
        add_column_options!(var, var)
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@Ui¡I"ã    rescue => var
      var = @var["STRING"]
    end
    var
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist);Fi[	@ji/I"+      var = add_code_tags(Pygments.highlight(var, :lexer => (@var), :options => (@var)), @var)
      var = (var["STRING"] + var) if var["STRING"]
      var = (var + var["STRING"]) if var["STRING"]
      var
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist);Fi[	@yihI"        if (var.is_a?(Document) or (var.is_a?(XML::Attr) or var.is_a?(XML::Node).!)) then
          raise(ArgumentError, "STRING")
        end
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@ﬂiI"˘        var = (var + "STRING")
        var = (var + "STRING") unless @var.empty?
        var = (var + "STRING") unless @var.empty?
        var
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[@(@œ@–I"return "STRING";FI"end;FI"end;Fi
[
[	@^i1I"ü          var = min_scale_value
        end
        return var
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@¢ièI"Ã        calculate_top_margin
        @var = ((width - @var) - @var)
        @var = ((height - @var) - @var)
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@ziäI"ü          var = var.dup
          var = (var - 1))
        end
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@ei∆I"º      def function(arglist)
        var = (10 ** var.to_f)
        ((var * var).to_i / var)
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@≤iæI"î      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[@(@)I"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;Fi
[
[	@ñilI"+        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@ôiaI"+        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@úi[I"+        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@üiGI"        var = "STRING" unless var.index("STRING").nil?
        var = (var + "STRING") unless var.index("STRING").nil?
        var = var.index("STRING").nil?.! ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 0
    var = 1;Fi[	@àiHI"‡          (var << "STRING") if var.include?("STRING")
          var = var.include?("STRING") ? ((var + "STRING")) : (var)
        end
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 0
    var = 1;Fi[@@I" ;FI"private;FI" ;FI"def function(arglist);Fi
[
[	I"5data//activemodel_proj/active_model/validator.rb;TiOI"º  class BlockValidator < EachValidator
    def function(arglist)
      @var = var
      super
    end
    
    private
    
    def function(arglist)
      @var.call(var, var, var)
    end;Fi[	I"7data//activerecord_proj/active_record/timestamp.rb;TiI"ÿ    
    def function(arglist)
      clear_timestamp_attributes
      super
    end
    
    private
    
    def function(arglist)
      if self.record_timestamps then
        var = current_time_from_proper_timezone;Fi[	I"0data//backup_proj/backup/compressor/lzma.rb;Ti!I"<      # Yields to the block the compressor command and filename extension.
      def function(arglist)
        Backup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"))
        super
      end
      
      private
      
      def function(arglist)
        ("STRING" if @var or "STRING" if @var)
      end;Fi[	I"2data//backup_proj/backup/compressor/pbzip2.rb;Ti'I"W      # Yields to the block the compressor command and filename extension.
      def function(arglist)
        Backup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"))
        super
      end
      
      private
      
      def function(arglist)
        var = ("STRING" if @var or "STRING" if @var)
        var = "STRING" if @var;Fi[	I"@data//diaspora_proj/controllers/registrations_controller.rb;TiI"∆  end
  
  def function(arglist)
    super
  end
  
  private
  
  def function(arglist)
    return true if AppConfig.settings.enable_registrations?
    return true if invite and invite.can_be_used?;Fi[@(@œI"attr_reader :function;FI" ;FI"private;FI" ;Fi
[
[	I"Qdata//activerecord_proj/active_record/associations/belongs_to_association.rb;TiI"⁄      def function(arglist)
        super
        @var = false
      end
      
      attr_reader :function
      
      private
      
      def function(arglist)
        loaded?.! and (foreign_key_present? and klass);Fi[	I"Pdata//activerecord_proj/active_record/attribute_methods/before_type_cast.rb;TiI"      #   task.read_attribute_before_type_cast('completed_on') # => "2012-10-21"
      def function(arglist)
        @var[var]
      end
      
      attr_reader :function
      
      private
      
      # Handle *_before_type_cast for method_missing.
      def function(arglist);Fi[	@]i≤I";            @var = var.slice((4..-36))
            @var = var.slice((-32..-1))
            @var = @var.sub((@var + "STRING"), "STRING")
          end
          
          attr_reader :function
          
          private
          
          ##
          # Sanitize string and replace any invalid UTF-8 characters.;Fi[	@zi.I"™      end
      check_requirement!
      @var
    end
    
    attr_reader :function
    
    private
    
    def function(arglist)
      var =~ /^--(no|skip)-([-\w]+)$/;Fi[	@ÏiI"      def function(arglist)
        @var = var
        self
      end
      
      attr_reader :function
      
      private
      
      # Searches the path, looking for the given utility. If an executable
      # file is found that matches the parameter, this returns true.;Fi[@ÍI"var.print("STRING");FI"when "STRING" then;FI"var.print("STRING");FI"when "STRING" then;FI"var.print("STRING");Fi
[
[	I";data//homebrew_proj/vendor/multi_json/vendor/okjson.rb;TiÖI"L      var = var.class.method_defined?(:encoding)
      while (var < var.length) do
        case var[var]
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@ñiáI"2        case var[var]
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@ñiâI":          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@ñiãI":          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@ñiçI"(          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        else
          (var = var[var];Fi[@(@)@
@
I"end;FI"end;Fi
[
[	@¸iaI"Ç            else
              (var << "STRING")
            end
          end
        end
        var
      end
    end
  end
end;Fi[	@}i,I"ú              warn("STRING")
            else
              # do nothing
            end
          end
          var
        end
      end
    end
  end
end;Fi[	I"-data//compass_proj/compass/frameworks.rb;TiÄI"ï            end
            (var << "STRING")
          end
        end
      end
      var
    end
  end
end
Compass::Frameworks.discover(:defaults);Fi[	@DiI"‡                var.push(Factory.from_db(klass, var))
              else
                var.push(Factory.build(klass, var))
              end
            end
            var
          end
        end
      end
    end
  end;Fi[	@i!I"ø            (var << "STRING")
          else
            (var < (var.length - 1)) ? ((var << "STRING")) : ((var << "STRING"))
          end
        end
        var
      end
    end
  end
end;Fi[I"var = (var + 1);FI"var = -1;FI"var = var;FI"var = 0;F@äI"@var = @var;Fi
[
[	@ñiºI"[      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@ôi∑I"I      var = var
      if (var == 10) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@úi´I"[      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@üiôI"Z      var = var[var]
      if (var == "STRING"[0]) then
        self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@àiΩI"E      var = var[var]
      if (var == "STRING"[0]) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (var - (3 * @var));Fi[@
@I"@var.empty?;FI"end;FI" ;FI"def function(arglist);Fi
[
[	@zi8I"ß      var =~ /^--(no|skip)-([-\w]+)$/
      $2
    end
    
    def function(arglist)
      @var.empty?
    end
    
    def function(arglist)
      @var.first
    end;Fi[	@}i-I"˙        var = (@var.index(var) or -1)
        @var[(var - 1)]
      end
      
      def function(arglist)
        @var.empty?
      end
      
      def function(arglist)
        var = (@var + [var]).map { |arglist| var.text_length }
        var.max;Fi[	@ŒieI"F        load_programming_language("STRING") if unknown_programming_language?
        @var.map { |arglist| var.snippet_text(var, var, var) }.join("STRING")
      end
      
      def function(arglist)
        @var.empty?
      end
      
      def function(arglist)
        @var.each { |arglist| var.send(var, *var) }
      end;Fi[	@Öi¶I"¨  def function(arglist)
    any? { |arglist| (var.name == var) }
  end
  
  def function(arglist)
    @var.empty?
  end
  
  def function(arglist)
    @var.each(&var)
  end;Fi[	@ﬂiI"ø  def function(arglist)
    @var.each(&var)
  end
  
  def function(arglist)
    @var.empty?
  end
  
  def function(arglist)
    return unless external_patches?
    curl(*external_curl_args);Fi[@(@)@
@
@°I" ;Fi
[
[	I"=data//activerecord_proj/active_record/relation/merger.rb;Ti I"È            Hash.===(var) ? (var.joins!(var)) : (var.joins!(*var))
          else
            var.send("STRING", var)
          end
        end
        var
      end
    end
    
    class Merger
      attr_reader(:relation, :values);Fi[	@ziÑI"*        if @var.is_a?(Hash) and var = @var[var] then
          if var.enum and var.enum.include?(var).! then
            raise(MalformattedArgumentError, "STRING")
          end
        end
        var
      end
    end
    
    # Raises an error if @non_assigned_required array is not empty.
    #;Fi[	@+
i)I"ﬂ          (var << stop.times(Sass::Script::Number.new(100, ["STRING"])).inspect)
        else
          (var << stop.inspect)
        end
      end
      var
    end
  end
  
  module Gradient
    def self.included(arglist);Fi[	I"9data//mongoid_proj/lib/mongoid/relations/metadata.rb;TiWI"ç          var.class.relations.values.each do |arglist|
            if (var.as == name) and (var.class_name == inverse_class_name) then
              var.push(var.name)
            end
          end
          var
        end
      end
      
      # For polymorphic children, we need to figure out the inverse from the
      # actual instance on the other side, since we cannot know the exact class;Fi[	@ﬂi0I"        var.each do |arglist|
          if var = var.index(var) then
            var.delete_at(var)
          end
        end
        var
      end
    end
    
    OperatorMatcher.register(Array, "STRING", Spec::Matchers::MatchArray)
  end;Fi[@@@}@~@ÌI"try("STRING", var);Fi
[
[	@iI"÷      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end;Fi[	@iI"÷      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end;Fi[	@i"I"÷      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end;Fi[	@i/I"Ù        @var ||= "STRING"
        var = configuration.fetch(:gem_command, "STRING")
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end;Fi[	@i5I"÷      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist)
        @var ||= "STRING"
        try("STRING", var)
        self
      end;Fi[@(@œI"J# Defines an initialization hook into the owner class for setting the;FI"J# initial state of the machine *before* any attributes are set on the;FI"# object;FI"def function(arglist);Fi
[
[	I"Idata//state_machine_proj/state_machine/integrations/active_record.rb;Ti&I"¿        if owner_class.connected? and (owner_class.table_exists? and var = owner_class.columns_hash[attribute.to_s]) then
          var.default
        end
      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function(arglist)
        define_static_state_initializer
        define_dynamic_state_initializer;Fi[	I"Gdata//state_machine_proj/state_machine/integrations/data_mapper.rb;TiPI"¶      # Pluralizes the name using the built-in inflector
      def function(arglist)
        ::DataMapper::Inflector.pluralize(var.to_s)
      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function(arglist)
        define_helper(:instance, "STRING", "STRING", (426 + 1))
      end;Fi[	I"Hdata//state_machine_proj/state_machine/integrations/mongo_mapper.rb;Ti$I"´      # Gets the Mongoid key for this machine's attribute (if it exists)
      def function(arglist)
        owner_class.keys[attribute.to_s]
      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function(arglist)
        define_helper(:instance, "STRING", "STRING", (325 + 1))
      end;Fi[	I"Cdata//state_machine_proj/state_machine/integrations/mongoid.rb;Ti(I"ß      # Gets the field for this machine's attribute (if it exists)
      def function(arglist)
        owner_class.fields[attribute.to_s]
      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function(arglist)
        define_helper(:instance, "STRING", "STRING", (386 + 1))
      end;Fi[	I"Bdata//state_machine_proj/state_machine/integrations/sequel.rb;TiDI"ç      # Loads the built-in inflector
      def function(arglist)
        require("sequel/extensions/inflector")
      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function(arglist)
        define_helper(:instance, "STRING", "STRING", (344 + 1))
      end;Fi[@ò@Ì@ÓI"@var = var;FI"end;FI" ;Fi
[
[	@»
iI"©    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
    end
    
    attr_reader :function
    ;Fi[	@IiI"    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
    end
    
    # Needed before installs, since the arch matters then and quick
    # specs don't bother to include the arch in the platform string;Fi[	@ªi*I"˚        
        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var
          @var = var
        end
        
        def function(arglist)
          [("STRING" % @var.join(File::PATH_SEPARATOR))];Fi[	I",data//jekyll_proj/jekyll/static_file.rb;TiI"    # dir  - The String path between <source> and the file.
    # name - The String filename of the file.
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
    end
    
    # Returns source file path.
    def function(arglist);Fi[	I"-data//rescue_proj/resque/failure/base.rb;TiI"e      # The string name of the queue from which the failed job was pulled
      # The payload object associated with the failed job
      def function(arglist)
        @var = var
        @var = var
        @var = var
        @var = var
      end
      
      # When a job fails, a new instance of your Failure backend is created
      # and #save is called.;Fi[@^I"# @since 2.4.0;FI"def function(arglist);FI"@var ||= {};FI"end;FI" ;Fi
[
[	I"-data//mongoid_proj/lib/mongoid/atomic.rb;Ti3I"È    #   person.atomic_array_pushes
    #
    # @return [ Hash ] The array pushes.
    #
    # @since 2.4.0
    def function(arglist)
      @var ||= {}
    end
    
    # For array fields these are the pulls that need to happen.
    #;Fi[	@i?I"Ì    #   person.atomic_array_pulls
    #
    # @return [ Hash ] The array pulls.
    #
    # @since 2.4.0
    def function(arglist)
      @var ||= {}
    end
    
    # For array fields these are the unique adds that need to happen.
    #;Fi[	@iKI"9    #   person.atomic_array_add_to_sets
    #
    # @return [ Hash ] The array add_to_sets.
    #
    # @since 2.4.0
    def function(arglist)
      @var ||= {}
    end
    
    # Get all the atomic updates that need to happen for the current
    # +Document+. This includes all changes that need to happen in the;Fi[	@Øi3I"Ë    #   model.changed_attributes
    #
    # @return [ Hash<String, Object> ] The attribute changes.
    #
    # @since 2.4.0
    def function(arglist)
      @var ||= {}
    end
    
    # Get all the changes for the document.
    #;Fi[	@ØilI"'    #   model.previous_changes
    #
    # @return [ Hash<String, Array<Object, Object> ] The previous changes.
    #
    # @since 2.4.0
    def function(arglist)
      @var ||= {}
    end
    
    # Remove a change from the dirty attributes hash. Used by the single field
    # atomic updators.;Fi[@
@I"teardown_db;FI"end;FI" ;FI"def function(arglist);Fi
[
[	@.i3I";    setup_db
    (1..4).each { |arglist| ListMixin.create!(:pos => (var), :parent_id => 5) }
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    assert_equal([1, 2, 3, 4], ListMixin.find(:all, :conditions => "STRING", :order => "STRING").map(&:id))
    ListMixin.find(2).move_lower;Fi[	@.iΩI"N      (((var % 2) == 1) ? (ListMixinSub1) : (ListMixinSub2)).create!(:pos => (var), :parent_id => 5000)
    end
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    assert_equal([1, 2, 3, 4], ListMixin.find(:all, :conditions => "STRING", :order => "STRING").map(&:id))
    ListMixin.find(2).move_lower;Fi[	@3i@I"·    @var = TreeMixin.create!
    @var = TreeMixin.create!
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    assert_equal(@var.children, [@var, @var])
    assert_equal(@var.children, [@var]);Fi[	@3iòI"p    @var = RecursivelyCascadedTreeMixin.create!(:parent_id => (@var.id))
    @var = RecursivelyCascadedTreeMixin.create!(:parent_id => (@var.id))
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    var = TreeMixin.find(:all, :include => :children, :conditions => "STRING", :order => "STRING")
    assert_equal([@var, @var, @var], var);Fi[	@3iæI"Á    @var = TreeMixinWithoutOrder.create!
    @var = TreeMixinWithoutOrder.create!
  end
  
  def function(arglist)
    teardown_db
  end
  
  def function(arglist)
    assert([@var, @var].include?(TreeMixinWithoutOrder.root))
  end;Fi[@(@œ@ç@éI"attr_reader :function;FI" ;Fi
[
[	@ÛiLI"        var.concat(["STRING", "STRING"])
        var.concat(["STRING", "STRING"])
        Kernel.system(*var)
      end
      
      private
      
      attr_reader :function
      
      def function(arglist)
        configuration.merge("STRING" => (nil));Fi[	@	i@I"Î      def function(arglist)
        set_psql_env
        Kernel.system("STRING")
      end
      
      private
      
      attr_reader :function
      
      def function(arglist)
        (configuration["STRING"] or DEFAULT_ENCODING);Fi[	@i)I"Ÿ      def function(arglist)
        var = configuration["STRING"]
        `sqlite3 #{var}STRING#{var}STRING`
      end
      
      private
      
      attr_reader :function
      
      attr_reader :function
    end;Fi[	@iRI"Ã    
    def function(arglist)
      to.responds_to?(:read) ? ("STRING") : (to)
    end
    
    private
    
    attr_reader :function
    
    def function(arglist)
      logger.info("STRING") if logger;Fi[	I"4data//state_machine_proj/state_machine/state.rb;Ti·I"5    def function(arglist)
      var = [[:name, name], [:value, @var], [:initial, initial?], [:context, methods.keys]]
      "STRING"
    end
    
    private
    
    attr_reader :function
    
    # Adds a predicate method to the owner class so long as a name has
    # actually been configured for the state;Fi[I"var = "STRING";F@k@l@mI"end;FI" ;Fi
[
[	@˝i	I"require("extend/string")
class InreplaceTest < Test::Unit::TestCase
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING";Fi[	@˝iI"›  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING";Fi[	@˝iI"›  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING";Fi[	@˝iI"›  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING";Fi[	@˝i%I"›  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING";Fi[@(@)@Á@~I"# do nothing;FI"end;Fi
[
[	@ìi·I"˙          if has_inverse? and inverse_of.nil? then
            raise(InverseOfAssociationNotFoundError.new(self))
          end
        end
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist);Fi[	@£i+I"        def function(arglist)
          unless (config_files_exist? or @var.generate_config?.!) then
            write_configuration_files
          end
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist);Fi[	@∫iÉI"          print_elements(var, :passed, "STRING")
        else
          @var.puts(format_string("STRING", :passed))
        end
      end
      
      def function(arglist)
        # do nothing
      end
      
      #define @delayed_messages = [] in your Formatter if you want to;Fi[	@·iyI"¡            @var = @var.parent.value[:dont_tab_up_next_text]
          end
        end
      end
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist);Fi[	@ÒiI"ˇ            @var.puts(@var.description.gsub(/ \(druby.*\)/, "STRING"))
            @var.flush
            @var = nil
          end
        end
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist);Fi[@(@œI"protected;FI" ;F@I"
begin;Fi
[
[	@ËiI"V      # Check the connection back in to the connection pool
      def function(arglist)
        pool.checkin(self)
      end
      
      protected
      
      def function(arglist)
        begin
          @var.instrument("STRING", :sql => (var), :name => (var), :connection_id => (object_id), :binds => (var)) do |arglist|
            yield;Fi[	@ZiØI"Ô      rescue ThrowResult
        false
      end
    end
    
    protected
    
    def function(arglist)
      begin
        (var = construct_join_dependency_for_association_find
        var = construct_relation_for_association_find(var);Fi[	@hiI"µ        end
        return 1
      end
    end
    
    protected
    
    def function(arglist)
      begin
        ($command = args.shift
        var = Compass::Commands[$command];Fi[	@±i´I"      var.directory? ? (var.rmdir) : (var.delete if var.exist?)
    end
    var.make_relative_symlink(self)
  end
  
  protected
  
  def function(arglist)
    begin
      if var.symlink? and var.directory? then
        var = (var.parent + var.readlink).cleanpath;Fi[	@<	içI"˛      def function(arglist)
        (var << var) unless var.nil?
        (@var.arity == 0) ? (@var.call) : (@var.call(*var))
      end
      
      protected
      
      def function(arglist)
        begin
          @var.call(*var)
        rescue => var;Fi[@òI"	self;FI"end;FI" ;F@¶I"@var = var;Fi
[
[	@ùiI"π        end
        
        def function(arglist)
          @var = var
          self
        end
        
        def function(arglist)
          @var = var
          self
        end;Fi[	@Œi8I"£      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end;Fi[	@Œi=I"£      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end;Fi[	@ŒiBI"£      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end;Fi[	@ŒiGI"£      end
      
      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        @var = var
        self
      end;Fi[I"if var then;F@\I"var = "STRING";FI"var = "STRING";FI"var = "STRING";FI"	else;Fi
[
[	@ñibI".    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?;Fi[	@ôiWI".    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?;Fi[	@úiQI".    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?;Fi[	@ài<I"˚    var = var.gsub("STRING", "STRING")
    var = var.length
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        if var.is_a?(String) then
          var = "STRING";Fi[	@àiıI"˚    SetY(var)
    var = ((@var - var) - @var) if (var == 0)
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        if var.is_a?(String) then
          var = "STRING";Fi[@ÆI"Jif (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then;FI",@var = sprintf("STRING", (var / 255.0));FI"	else;FI"J@var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0));FI"end;Fi
[
[	@üiI",  
  attr_reader :function
  
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    out(@var) if (@var > 0)
  end;Fi[	@üiI"F    out(@var) if (@var > 0)
  end
  
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    @var = @var.!=(@var)
    out(@var) if (@var > 0);Fi[	@üiI"0    out(@var) if (@var > 0)
  end
  
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    @var = @var.!=(@var)
  end;Fi[	@ài∂I"q  	# @since 1.3
  	# @see SetFillColor(), SetTextColor(), Line(), Rect(), Cell(), MultiCell()
  	#
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    out(@var) if (@var > 0)
  end;Fi[	@ài I"|  	# @since 1.3
  	# @see SetDrawColor(), SetTextColor(), Rect(), Cell(), MultiCell()
  	#
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    @var = @var.!=(@var)
    out(@var) if (@var > 0);Fi[@^@w@xI"	true;FI"end;FI" ;Fi
[
[	@´iwI"˝      #   object.resizable?
      #
      # @return [ true ] true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Convert the provided object to a propery array of foreign keys.;Fi[	I"7data//mongoid_proj/lib/mongoid/extensions/float.rb;TiI"      #   object.numeric?
      #
      # @return [ true ] Always true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Turn the object from the ruby type we deal with to a Mongo friendly;Fi[	@ºi|I"-      #   {}.resizable?
      #
      # @return [ true ] true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      # Convert this hash to a criteria. Will iterate over each keys in the
      # hash which must correspond to method on a criteria object. The hash;Fi[	I"9data//mongoid_proj/lib/mongoid/extensions/integer.rb;TiI"Ó      #   object.numeric?
      #
      # @return [ true ] Always true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      # Is the object not to be converted to bson on criteria creation?
      #;Fi[	@ƒi'I"ˇ      #   range.resizable?
      #
      # @return [ true ] True.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.;Fi[@ÆI"return "STRING";FI"end;FI"end;FI"end;FI"end;Fi
[
[	@^i3I"x        return var
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@¢iëI"à        @var = ((height - @var) - @var)
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@ziåI"q        end
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@ei»I"Å        ((var * var).to_i / var)
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[	@≤i¿I"v        "STRING"
      end
      
      def function(arglist)
        return "STRING"
      end
    end
  end
end;Fi[@
@@-I"end;FI" ;FI"def function(arglist);Fi
[
[	@˛i¶I"Á          def function(arglist)
            @var = {}
          end
          
          def function(arglist)
            @var[var] = var
          end
          
          def function(arglist)
            @var[var]
          end;Fi[	@iÄI"Ω      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var[var] = var
      end
      
      def function(arglist)
        @var.abort!
      end;Fi[	@@i2I"◊      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var[var] = var
      end
      
      def function(arglist)
        return @var if @var
        @var = (var = false;Fi[	@iÖI"…          @var = var
          @var = var
        end
        
        def function(arglist)
          @var[var] = var
        end
        
        def function(arglist)
          @var[var]
        end;Fi[	@ƒ	iqI"      def function(arglist)
        var ? (cache(var, &var)) : (call_cached(var))
      end
      
      def function(arglist)
        @var[var] = var
      end
      
      def function(arglist)
        (@var[var].arity == 1) ? (@var[var].call(@var)) : (@var[var].call)
      end;Fi[I""next if send(var).to_s.empty?;FI""STRING";FI"end.compact.join("STRING");FI"end;FI" ;FI"##;Fi	[	[	@«iâI"l      # to perform the database dumping process
      def function(arglist)
        ["STRING", "STRING"].map do |arglist|
          next if send(var).to_s.empty?
          "STRING"
        end.compact.join("STRING")
      end
      
      ##
      # Builds the MongoDB connectivity options syntax to connect the user
      # to perform the database dumping process;Fi[	@«iìI"D      # to perform the database dumping process
      def function(arglist)
        ["STRING", "STRING"].map do |arglist|
          next if send(var).to_s.empty?
          "STRING"
        end.compact.join("STRING")
      end
      
      ##
      # Returns the mongodump syntax for enabling ipv6
      def function(arglist);Fi[	@ igI"]      # to perform the database dumping process
      def function(arglist)
        ["STRING", "STRING", "STRING"].map do |arglist|
          next if send(var).to_s.empty?
          "STRING"
        end.compact.join("STRING")
      end
      
      ##
      # Builds a MySQL compatible string for the additional options
      # specified by the user;Fi[	I"/data//backup_proj/backup/database/redis.rb;TieI"]      # to perform the database dumping process
      def function(arglist)
        ["STRING", "STRING", "STRING"].map do |arglist|
          next if send(var).to_s.empty?
          "STRING"
        end.compact.join("STRING")
      end
      
      ##
      # Builds a Redis compatible string for the
      # additional options specified by the user;Fi[I"(attr_accessor(:username, :password);FI" ;FI"attr_accessor(:ip, :port);FI" ;FI"attr_accessor(:path);FI" ;Fi	[	[	@Wi	I"◊module Backup
  module Storage
    class FTP < Base
      attr_accessor(:username, :password)
      
      attr_accessor(:ip, :port)
      
      attr_accessor(:path)
      
      attr_accessor(:passive_mode)
      ;Fi[	@—	iI"ﬁ    class RSync < Base
      include(Backup::CLI::Helpers)
      
      attr_accessor(:username, :password)
      
      attr_accessor(:ip, :port)
      
      attr_accessor(:path)
      
      attr_accessor(:local)
      ;Fi[	@ﬁi
I"—module Backup
  module Storage
    class SCP < Base
      attr_accessor(:username, :password)
      
      attr_accessor(:ip, :port)
      
      attr_accessor(:path)
      
      ##
      # Server credentials;Fi[	@Zi
I"“module Backup
  module Storage
    class SFTP < Base
      attr_accessor(:username, :password)
      
      attr_accessor(:ip, :port)
      
      attr_accessor(:path)
      
      ##
      # Server credentials;Fi[I"puts(var);FI"@var = true;FI"end;FI"end;FI" ;FI"def function(arglist);Fi	[	[	@¢i1I"%      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    return unless f.lib.directory?
    var = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	@¢iAI"       opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    return unless f.bin.directory?
    var = f.bin.children.select { |arglist| (var.directory? or var.executable?.!) };Fi[	@¢iMI"      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    return unless f.sbin.directory?
    var = f.sbin.children.select { |arglist| (var.directory? or var.executable?.!) };Fi[	@¢iYI"∞      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    check_jars
    check_non_libraries;Fi[@
@I"flag?("STRING");FI"end;FI" ;FI"def function(arglist);Fi	[	[	@wiCI"◊  def function(arglist)
    (at((@var + 1)) or raise(UsageError))
  end
  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    (flag?("STRING") or (ENV["STRING"] or ENV["STRING"]))
  end;Fi[	@wiOI"Æ  def function(arglist)
    (flag?("STRING") or ENV["STRING"])
  end
  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    flag?("STRING")
  end;Fi[	@wiSI"õ  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    flag?("STRING")
  end;Fi[	@wiWI"µ  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    (include?("STRING") or switch?("STRING"))
  end;Fi[I"puts("STRING");FI"puts(var);FI"@var = true;FI"end;FI"end;FI" ;Fi	[	[	@¢i0I"¡      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    return unless f.lib.directory?;Fi[	@¢i@I"«    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    return unless f.bin.directory?;Fi[	@¢iLI"»    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    return unless f.sbin.directory?;Fi[	@¢iXI"≥    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    check_jars;Fi[@I"puts("STRING");FI"puts(var);FI"@var = true;FI"end;FI"end;Fi	[	[	@¢i/I"≥      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist);Fi[	@¢i?I"¨    end
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist);Fi[	@¢iKI"¯    var = f.bin.children.select { |arglist| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist);Fi[	@¢iWI"˘    var = f.sbin.children.select { |arglist| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist);Fi[@
@I"@var.each(&var);FI"end;FI" ;FI"def function(arglist);Fi	[	[	@;iI"—      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var.each(&var)
      end
      
      def function(arglist)
        var.features = self
        (@var << var);Fi[	@}i$I"Ù      def function(arglist)
        StepCollection.new((@var + var))
      end
      
      def function(arglist)
        @var.each(&var)
      end
      
      def function(arglist)
        var = (@var.index(var) or -1)
        @var[(var - 1)];Fi[	@Öi™I"¢  def function(arglist)
    @var.empty?
  end
  
  def function(arglist)
    @var.each(&var)
  end
  
  def function(arglist)
    map { |arglist| var.flag }
  end;Fi[	@ﬂiI"£  def function(arglist)
    external_curl_args.empty?.!
  end
  
  def function(arglist)
    @var.each(&var)
  end
  
  def function(arglist)
    @var.empty?
  end;Fi[@I"'@var = path.sub(/^\~\//, "STRING");FI"end;FI" ;FI"private;FI" ;Fi	[	[	@Wi I"         @var ||= 21
        @var ||= "STRING"
        @var ||= false
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server;Fi[	@—	i"I"        @var ||= 22
        @var ||= "STRING"
        @var ||= false
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # This is the remote path to where the backup files will be stored;Fi[	@ﬁiI"        super(var, var)
        @var ||= 22
        @var ||= "STRING"
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server;Fi[	@ZiI"        super(var, var)
        @var ||= 22
        @var ||= "STRING"
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server;Fi[@
@I"Avar = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);FI"var;FI"end;FI" ;Fi	[	[	@<
i√I"0        var = Node.new(:FUNCTION, [var.first.strip])
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);Fi[	@<
i»I"@        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);Fi[	@<
iÕI"@        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);Fi[	@<
i“I"        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = [var.first, var.last];Fi[@
@I"@var ||= var;FI"end;FI" ;FI"def function(arglist);Fi	[	[	I"1data//cucumber_prok/cucumber/ast/examples.rb;TiI"˜      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        return if Cucumber.wants_to_quit
        var.visit_comment(@var) unless @var.empty?;Fi[	@iI"Ÿ      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        @var.feature = self if @var
        @var.init if @var;Fi[	@iI"Ò      attr_accessor(:feature)
      
      attr_reader(:gherkin_statement, :raw_steps, :title, :description)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        (@var << var)
      end;Fi[	@$iI"≥      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        false
      end;Fi[I"var.print("STRING");F@ê@ë@í@ìI"when "STRING" then;Fi	[	[	@ñiÜI"6      while (var < var.length) do
        case var[var]
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@ñiàI"7        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@ñiäI"7        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@ñiåI")        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        else;Fi[@
@I"+source_tags.map { |arglist| var.name };FI"end;FI" ;FI"def function(arglist);Fi	[	[	@i>I"ﬂ      def function(arglist)
        @var.tags
      end
      
      def function(arglist)
        source_tags.map { |arglist| var.name }
      end
      
      def function(arglist)
        @var.accept_hook?(var)
      end;Fi[	@iHI"@      def function(arglist)
        Gherkin::TagExpression.new(var.tag_expressions).eval(source_tags)
      end
      
      def function(arglist)
        source_tags.map { |arglist| var.name }
      end
      
      def function(arglist)
        (@var.tags.to_a + (@var ? (@var.source_tags.to_a) : ([]))).uniq
      end;Fi[	@!i(I"      def function(arglist)
        @var.source_tags
      end
      
      def function(arglist)
        source_tags.map { |arglist| var.name }
      end
      
      def function(arglist)
        init
        example_rows.each { |arglist| var.skip_invoke! };Fi[	@!iRI"‚          super
          @var = nil
        end
        
        def function(arglist)
          source_tags.map { |arglist| var.name }
        end
        
        def function(arglist)
          @var.source_tags
        end;Fi[@
@I"	to_s;FI"end;FI" ;FI"def function(arglist);Fi	[	[	@|iI"©      self.value = var
      self.css2_value = var
    end
    
    def function(arglist)
      to_s
    end
    
    def function(arglist)
      value.to_s(var)
    end;Fi[	@+
i9I"¨        class_eval("STRING")
      end
    end
    
    def function(arglist)
      to_s
    end
    
    def function(arglist)
      GRADIENT_ASPECTS.include?(var)
    end;Fi[	I"Qdata//compass_proj/compass/sass_extensions/monkey_patches/browser_support.rb;Ti%I"ö    def function(arglist)
      args
    end
    
    def function(arglist)
      to_s
    end
    
    def function(arglist)
      var = "STRING"
    end;Fi[	I"1data//mongoid_proj/lib/mongoid/extensions.rb;TiI"πend
class Moped::BSON::ObjectId
  undef :as_json
  
  def function(arglist)
    to_s
  end
  
  def function(arglist)
    ActiveSupport::XmlMini.to_tag(var[:root], self.to_s, var)
  end;Fi[I"if (var == "STRING") then;FI"var = "STRING";FI"	else;FI"5if ((var == "STRING") or (var == "STRING")) then;FI"var = "STRING";FI"	else;Fi	[	[	@üi^I"    var = (var - (var * var))
    var = var
    out(sprintf("STRING", var, var, var, var, var, var))
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end;Fi[	@üipI"÷  end
  
  def function(arglist)
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end;Fi[	@àilI"    var = (var - (var * var))
    var = var
    out(sprintf("STRING", var, var, var, var, var, var))
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end;Fi[	@àiÜI"  	# @see SetLineWidth(), SetDrawColor(), SetFillColor()
  	#
  def function(arglist)
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end;Fi[@
@I"var = @var["STRING"];FI"!var = ((@var - @var) - @var);FI"/var = (((var - (2 * @var)) * 1000) / @var);FI"'var = var.gsub("STRING", "STRING");Fi	[	[	@ñi´I"˙      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1;Fi[	@ôi¶I"˙      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1;Fi[	@úiöI"˙      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1;Fi[	@üiâI"    self.Cell(var, var, var[(var..var)], var, 2, var, var)
    @var = @var
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1;Fi[@
@I"@var ||= {};FI"end;FI" ;FI"def function(arglist);Fi	[	[	@iI"        def function(arglist)
          defaults.reset!
        end
        
        def function(arglist)
          @var ||= {}
        end
        
        def function(arglist)
          var = ("STRING" + "STRING")
          (var << "STRING") if var[:message];Fi[	@giHI"†      end
      
      private
      
      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        @var ||= {}
      end;Fi[	@giLI"›      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        @var ||= Hash.new { |arglist| var[var] = [] }
      end;Fi[	@6iI"∑        @var = var
        @var = var
      end
      
      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        __raise("STRING")
      end;Fi[I"yield(var);FI"end;FI"end;FI" ;FI"##;FI"A# Transfers the archived file to the specified remote server;Fi	[	[	@Wi4I"X        Net::FTP.send(:const_set, :FTP_PORT, port)
        Net::FTP.open(ip, username, password) do |arglist|
          var.passive = true if passive_mode
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var);Fi[	@—	i4I"U      # Establishes a connection to the remote server
      def function(arglist)
        Net::SSH.start(ip, username, :password => (password), :port => (port)) do |arglist|
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        begin;Fi[	@ﬁi(I"s      # Net::SCP will use this connection to transfer backups
      def function(arglist)
        Net::SSH.start(ip, username, :password => (password), :port => (port)) do |arglist|
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var);Fi[	@Zi&I"l      # Establishes a connection to the remote server
      def function(arglist)
        Net::SFTP.start(ip, username, :password => (password), :port => (port)) do |arglist|
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var);Fi[@ÍI"var["STRING"] = "STRING";FI"when "STRING" then;FI"var["STRING"] = "STRING";FI"when "STRING" then;FI"var["STRING"] = "STRING";Fi	[	[	I"?data//redmine_proj/plugins/rfpdf/lib/core/image_science.rb;TiI",      var[0] = var.width
      var[1] = var.height
      case var[2]
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING";Fi[	@ÿiI"3      case var[2]
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING";Fi[	@ÿiI"(        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      when "STRING" then
        var["STRING"] = "STRING"
      else
        # do nothing;Fi[	@∂i-I"^            case @var
            when "STRING" then
              var["STRING"] = var["STRING"]
            when "STRING" then
              var["STRING"] = "STRING"
            when "STRING" then
              var["STRING"] = "STRING"
            when "STRING" then
              var["STRING"] = "STRING"
            else
              # do nothing;Fi[@
@I"[];FI"end;FI" ;FI"def function(arglist);Fi	[	[	@êi
I"ê    def function(arglist)
      @var = []
    end
    
    def function(arglist)
      []
    end
    
    def function(arglist)
      0
    end;Fi[	@∏iI"Œ        def function(arglist)
          false
        end
        
        def function(arglist)
          []
        end
        
        def function(arglist)
          StepCollection.new(var)
        end;Fi[	@XiAI"  def function(arglist)
    "STRING"
  end
  
  def function(arglist)
    []
  end
  
  def function(arglist)
    []
  end;Fi[	@ÓiI"Î      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        []
      end
      
      def function(arglist)
        return if var.backtrace.nil?
        var = var.backtrace.collect do |arglist|;Fi[@
@@@@I"protected;Fi	[	[	@äi%I"ƒ        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist);Fi[	@ëi3I"“            (var << rejected_types_and_failures)
          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist);Fi[	@ñi$I"ƒ        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist);Fi[	@ùi7I"ƒ        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist);Fi[@ÍI"%["STRING", var[0, 1], var[0, 1]];FI"when "STRING" then;FI"%["STRING", var[0, 1], var[0, 1]];FI"when "STRING" then;FI"%["STRING", var[0, 1], var[0, 1]];Fi	[	[	@ñiÉI"K    # it is the lexeme.
    def function(arglist)
      case var[0]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]];Fi[	@ñiÖI"[      case var[0]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]];Fi[	@ñiáI"r        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]];Fi[	@ñiâI"^        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        nulltok(var);Fi[@Ñ@î@ïI"#;F@≈I"var = case var;Fi	[	[	@Œ	i+I"â      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING";Fi[	@*i)I"â      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING";Fi[	@-i3I"â      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING";Fi[	@0i'I"â      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING";Fi[@
@I"colour(var, "STRING");FI"end;FI" ;FI"def function(arglist);Fi	[	[	@?	i~I"—            false
          end
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end;Fi[	@?	i}I"Ô        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end;Fi[	@?	iÅI"Ô        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end;Fi[	@?	iÖI"˘        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          Spec.deprecate("STRING")
          red(var);Fi[@
@I"var = "STRING";F@˙I"-var.change_make_var!("STRING", "STRING");FI" assert_equal("STRING", var);Fi	[	[	@˝iI"ˇ    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  ;Fi[	@˝iI"ˇ    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  ;Fi[	@˝iI"ˇ    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  ;Fi[	@˝i#I"ˇ    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  ;Fi[@
@/@0@ÍI""STRING";FI"end;Fi	[	[	@iOI"s        end
      end
    end
    
    private
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@»
iEI"É    def function(arglist)
      @var = var
    end
    
    private
    
    def function(arglist)
      "STRING"
    end
  end
end;Fi[	@Éi‹I"Ã        raise("STRING") unless Nokogiri.jruby?
        return toJavaDocument
      end
      
      private
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@§i3I"œ      @var.write(nonce_key(var, var), var, :expires_in => (OpenID::Nonce.skew))
      return true
    end
    
    private
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist);Fi[@î@ïI"#;F@≈I"var = case var;FI"when :success then;Fi	[	[	@Œ	i,I"ú      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then;Fi[	@*i*I"ú      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then;Fi[	@-i4I"ú      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :failure then;Fi[	@0i(I"ú      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then;Fi[I"0;FI"end;FI" ;FI"def function(arglist);FI"0;FI"end;Fi	[	[	@êiI"Ñ    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist);Fi[	@êiI"Ñ    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist);Fi[	@êiI"Ñ    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist);Fi[	@êi4I"Ñ    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist);Fi[@ïI"#;F@≈I"var = case var;F@>I""STRING";Fi	[	[	@Œ	i-I"ú      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING";Fi[	@*i+I"ú      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING";Fi[	@-i5I"ú      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :failure then
          "STRING";Fi[	@0i)I"ú      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING";Fi[@?I"end;FI" ;FI"private;FI" ;FI"##;Fi	[	[	@Wi!I"Ù        @var ||= "STRING"
        @var ||= false
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server
      #;Fi[	@—	i#I"        @var ||= "STRING"
        @var ||= false
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # This is the remote path to where the backup files will be stored
      #;Fi[	@ﬁiI"        @var ||= 22
        @var ||= "STRING"
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server
      # and yields the Net::SSH connection.;Fi[	@ZiI"        @var ||= 22
        @var ||= "STRING"
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server
      def function(arglist);Fi[@
@@¡@¬I"end;FI" ;Fi	[	[	@∏iI"Ó        def function(arglist)
          StepCollection.new(var)
        end
        
        def function(arglist)
          # do nothing
        end
      end
      
      def function(arglist)
        @var = (var or EmptyBackground.new);Fi[	@'iI"Ω        def function(arglist)
          (@var << var)
        end
        
        def function(arglist)
          # do nothing
        end
      end
      
      include(Enumerable)
      ;Fi[	@¿i[I"¿      end
      
      attr_reader :function
      
      def function(arglist)
        # do nothing
      end
    end
    
    class JsLanguage
      include(LanguageSupport::LanguageMethods);Fi[	@ŒiI"        def function(arglist)
          @var.invoke(var.name, Ast::MultilineArgument.from((var.doc_string or var.rows)))
        end
        
        def function(arglist)
          # do nothing
        end
      end
      
      include(Constantize)
      ;Fi[@
@@¡@¬@uI"end;Fi	[	[	@µi'I"è    def function(arglist)
      @var.lock(Bundler.default_lockfile)
    end
    
    def function(arglist)
      # do nothing
    end
  end
end;Fi[	@ÒiI"¶        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@ÙiI"¶        def function(arglist)
          # do nothing
        end
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[	@•iI"ä        end
        
        private
        
        def function(arglist)
          # do nothing
        end
      end
    end
  end
end;Fi[I"Dmethod_option("STRING", :type => :boolean, :banner => "STRING");FI" ;FI"Dmethod_option("STRING", :type => :boolean, :banner => "STRING");FI" ;FI"Dmethod_option("STRING", :type => :boolean, :banner => "STRING");FI" ;Fi	[	[	@J	iÑI"p    
    method_option("STRING", :type => :string, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    ;Fi[	@J	iÜI"ã    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :string, :lazy_default => "STRING", :banner => "STRING")
    ;Fi[	@J	iíI"Ö    
    method_option("STRING", :type => :string, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :array, :lazy_default => ([]), :banner => "STRING")
    ;Fi[	@J	i„I"d    
    method_option("STRING", :type => :array, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    def function(arglist)
      var = Array(options[:source]);Fi[I"@var = @var;F@å@^I"end;FI"var = (var + 1);FI"	next;Fi	[	[	@ñi¡I"        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      var = var if (var.! or (var.chr == "STRING"));Fi[	@ôiºI"        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      if (var < 128) then;Fi[	@úi∞I"        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      var = var if (var.! or (var.chr == "STRING"));Fi[	@üiûI"        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      if (var == "STRING"[0]) then;Fi[@
I"module ClassMethods;FI"end;FI"end;FI"end;FI"end;Fi	[	[	@Ùi[I"ø        def function(arglist)
          saved_attachments.each { |arglist| (self.attachments << var) }
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˜iÖI"É          @var = nil
          @var = true
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˙i>I"™          var.reject! { |arglist| visible?(var).! }
          var.collect(&:mail)
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˝iRI"¢        def function(arglist)
          notified_watchers.collect(&:mail)
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[@
I"# Returns true;FI"def function(arglist);FI"	true;FI"end;FI" ;Fi	[	[	@irI"˜      def function(arglist)
        "STRING"
      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      # Returns true if SQLite version is '3.6.8' or greater, false otherwise.
      def function(arglist);Fi[	@iãI"ª      def function(arglist)
        true
      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      def function(arglist)
        @var.!=(false);Fi[	@i°I"ø      def function(arglist)
        @var.clear
      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      # Returns true
      def function(arglist);Fi[	@i¶I"±      def function(arglist)
        true
      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      def function(arglist)
        true;Fi[@
@åI" ;F@éI" ;F@êi	[	[	@J	iÉI"≠    method_option("STRING", :type => :array, :banner => "STRING")
    
    method_option("STRING", :type => :string, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING");Fi[	@J	iÖI"…    method_option("STRING", :type => :string, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :string, :lazy_default => "STRING", :banner => "STRING");Fi[	@J	iëI"√    method_option("STRING", :type => :string, :banner => "STRING")
    
    method_option("STRING", :type => :string, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :array, :lazy_default => ([]), :banner => "STRING");Fi[	@J	i‚I"X    long_desc("STRING")
    
    method_option("STRING", :type => :array, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    def function(arglist);Fi[@ú@å@^@ù@ûI"	else;Fi	[	[	@ñiﬂI"        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + (var ? (1) : (2)))
      end;Fi[	@ôiÁI"        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + var)
        var = var if (var >= 128);Fi[	@úiŒI"        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + (var ? (1) : (2)))
      end;Fi[	@üiøI"ˇ        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + 1)
      end;Fi[@äI"var = 1;F@w@dI"var = (var < 128);FI""if (var.chr == "STRING") then;Fi	[	[	@ñiqI"*    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1);Fi[	@ñiµI"2    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1);Fi[	@úi`I"*    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1);Fi[	@úi§I"2    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1);Fi[I"var = var.length;FI"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;FI"var = 1;Fi	[	[	@ñi±I""    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@ôi¨I""    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@úi†I""    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@üièI"ˆ    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var];Fi[@ä@aI"var = 1;F@wI"var = var[var];FI"!if (var == "STRING"[0]) then;Fi	[	[	@üiLI"«    var = -1
    var = 0
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        if (@var > 0) then
          @var = 0;Fi[	@üiíI"    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
        var = (var + 1);Fi[	@àiMI"«    var = -1
    var = 0
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        if (@var > 0) then
          @var = 0;Fi[	@ài∂I"ı    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1);Fi[I"@var.puts("STRING");FI"@var.puts("STRING");FI"@var.flush;FI"end;FI" ;FI"def function(arglist);Fi	[	[	@˛i,I"            @var.puts("STRING")
          end
          @var.puts("STRING")
          @var.puts("STRING")
          @var.puts("STRING")
          @var.flush
        end
        
        def function(arglist)
          @var.puts("STRING")
          @var.puts("STRING");Fi[	@˛i2I"ﬁ        end
        
        def function(arglist)
          @var.puts("STRING")
          @var.puts("STRING")
          @var.flush
        end
        
        def function(arglist)
          @var = (@var + 1)
        end;Fi[	@˛iOI"y          @var.puts("STRING") unless var.exception.nil?
          @var.puts("STRING") unless var.exception.nil?
          @var.puts(var) unless (var == "STRING")
          @var.puts("STRING")
          @var.puts("STRING")
          @var.flush
        end
        
        def function(arglist)
          @var.puts("STRING") unless @var
          @var.puts("STRING") unless @var;Fi[	@˛i|I"¸          @var.puts("STRING")
          @var.puts("STRING")
          @var.puts("STRING")
          @var.puts("STRING")
          @var.puts("STRING")
          @var.flush
        end
        
        def function(arglist)
          "STRING"
        end;Fi[@ä@a@¸@w@dI"var = (var < 128);Fi	[	[	@ñipI"    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var);Fi[	@ñi¥I"/    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);Fi[	@úi_I"    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var);Fi[	@úi£I"/    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);Fi[@(@àI"var = 0;FI"var = 0;FI"var = 0;FI"var = 1;Fi	[	[	@ñimI"0        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@ôibI"0        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@úi\I"0        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@ài≥I"÷    if (var == 1) and (var == "STRING") then
      @var = (@var + GetStringWidth(var))
      return
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var];Fi[@
I"acts_as_api;FI" ;FI"+api_accessible(:backbone) do |arglist|;FI"var.add(:id);FI"var.add(:guid);Fi	[	[	I"'data//diaspora_proj/models/like.rb;TiI"Ê  after_destroy { |arglist| self.parent.update_likes_counter }
  
  xml_attr(:positive)
  
  acts_as_api
  
  api_accessible(:backbone) do |arglist|
    var.add(:id)
    var.add(:guid)
    var.add(:author)
    var.add(:created_at);Fi[	I"0data//diaspora_proj/models/participation.rb;TiI"∏      { :target => (@var) }
    end
  end
  
  acts_as_api
  
  api_accessible(:backbone) do |arglist|
    var.add(:id)
    var.add(:guid)
    var.add(:author)
    var.add(:created_at);Fi[	@UiI"¯  include(Encryptor::Public)
  
  include(Diaspora::Guid)
  
  acts_as_api
  
  api_accessible(:backbone) do |arglist|
    var.add(:id)
    var.add(:guid)
    var.add(:name)
    var.add(lambda { |arglist| var.diaspora_handle }, :as => :diaspora_id);Fi[	@Ç
iI"—  include(Diaspora::Commentable)
  
  include(Diaspora::Shareable)
  
  acts_as_api
  
  api_accessible(:backbone) do |arglist|
    var.add(:id)
    var.add(:guid)
    var.add(:created_at)
    var.add(:author);Fi[@ä@a@b@c@wI"var = var[var];Fi	[	[	@üiKI"º    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        if (@var > 0) then;Fi[	@üiëI"    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var);Fi[	@àiLI"º    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        if (@var > 0) then;Fi[	@àiµI"Í      return
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);Fi[I"var = @var["STRING"];F@¶@ß@®I"var = var.length;FI"var = -1;Fi	[	[	@ñi≠I"Í  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0;Fi[	@ôi®I"Í  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0;Fi[	@úiúI"Í  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0;Fi[	@üiãI"Í  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0;Fi[@
@•I"# type.;FI"#;F@®I"#   object.mongoize;Fi	[	[	@´iQI"%      def function(arglist)
        (first.resizable? or (size > 1))
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #
      # @return [ Array ] The object.;Fi[	@∞iI"
      def function(arglist)
        to_f
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #
      # @return [ Object ] The object.;Fi[	@ºimI"˜        end
        var
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #
      # @return [ Hash ] The object.;Fi[	@øiI"˝          false
        end
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #
      # @return [ Object ] The object.;Fi[@
I"J# Initialize the new cascade strategy, which will set up the relation;FI"# and the metadata.;FI"#;FI"(# @example Instantiate the strategy;FI")#   Strategy.new(document, metadata);Fi	[	[	@`i
I"É    module Cascading
      class Delete
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.;Fi[	@ei
I"Ñ    module Cascading
      class Destroy
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.;Fi[	I"Bdata//mongoid_proj/lib/mongoid/relations/cascading/nullify.rb;Ti
I"Ñ    module Cascading
      class Nullify
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.;Fi[	@ji
I"Ö    module Cascading
      class Restrict
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.;Fi[I"@var ||= false;FI"(instance_eval(&var) if block_given?;FI"@var = "STRING";FI"@var = "STRING";FI"end;FI" ;Fi	[	[	I"1data//backup_proj/backup/compressor/bzip2.rb;TiI"      # Creates a new instance of Backup::Compressor::Bzip2
      def function(arglist)
        load_defaults!
        @var ||= false
        instance_eval(&var) if block_given?
        @var = "STRING"
        @var = "STRING"
      end
      
      private
      ;Fi[	I"0data//backup_proj/backup/compressor/gzip.rb;TiI"      # Creates a new instance of Backup::Compressor::Gzip
      def function(arglist)
        load_defaults!
        @var ||= false
        instance_eval(&var) if block_given?
        @var = "STRING"
        @var = "STRING"
      end
      
      private
      ;Fi[	@tiI"!      def function(arglist)
        load_defaults!
        @var ||= false
        @var ||= false
        instance_eval(&var) if block_given?
        @var = "STRING"
        @var = "STRING"
      end
      
      ##
      # Yields to the block the compressor command and filename extension.;Fi[	@wiI"        load_defaults!
        @var ||= false
        @var ||= false
        @var ||= false
        instance_eval(&var) if block_given?
        @var = "STRING"
        @var = "STRING"
      end
      
      ##
      # Yields to the block the compressor command and filename extension.;Fi[I"'var = var.gsub("STRING", "STRING");FI"var = var.length;FI"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;Fi	[	[	@ñi∞I"˙    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@ôi´I"˙    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@úiüI"˙    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@üiéI"˙    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[@=I"#   end;TI"#;TI"#   person.pets;TI"#   # => [;T@i	[	[	@&i&I"õ      # *First:* Specify a subset of fields to be selected from the result set.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&i_I"y      # error if the object can not be found.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&i}I"p      # form returns an empty array.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&iñI"p      # form returns an empty array.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[I"5if ((var == "STRING") or (var == "STRING")) then;FI"var = "STRING";FI"	else;FI"var = "STRING";FI"end;FI"end;Fi	[	[	@üiaI"Û    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end
    out(sprintf("STRING"))
    out(sprintf("STRING"));Fi[	@üisI"%    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end
    out(sprintf("STRING", (var * @var), ((@var - var) * @var), (var * @var), (-var * @var), var))
  end;Fi[	@àioI"Û    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end
    out(sprintf("STRING"))
    out(sprintf("STRING"));Fi[	@àiâI"%    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end
    out(sprintf("STRING", (var * @var), ((@var - var) * @var), (var * @var), (-var * @var), var))
  end;Fi[@
I" @var = { :action => :save };FI" ;FI")def self.matching_ancestors(arglist);FI"["STRING"];FI"end;Fi	[	[	@ÛiI"      include(ActiveModel)
      
      require("state_machine/integrations/active_record/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      def self.extended(arglist);Fi[	@˘iI"Ú      include(ActiveModel)
      
      require("state_machine/integrations/mongo_mapper/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      protected;Fi[	@¸iI"˛      include(ActiveModel)
      
      require("state_machine/integrations/mongoid/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      def self.extended(arglist);Fi[	@ˇiI"#      include(Base)
      
      require("state_machine/integrations/sequel/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      # Forces the change in state to be recognized regardless of whether the;Fi[@@Ï@@Ó@I"	else;Fi	[	[	@ÚiI"          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else
            (Compass::Util.compass_warn("STRING")
            var.value);Fi[	@ÚiII"ﬁ        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        else
          var
        end;Fi[	@·i¸I"%              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              else
                "STRING"
              end;Fi[	@vinI"          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else
            (var.value.first + "STRING")
          end;Fi[@(I"	self;FI"end;FI"end;FI"end;FI"end;Fi	[	[	@1i˚I"Õ          var = "STRING"
          re_define_method(var) do |arglist|
            send(var.setter, var.klass.find(var.reject(&:blank?)))
          end
          self
        end
      end
    end
  end
end;Fi[	@$iSI"•            var.save
            save if new_record? and var.stores_foreign_key?
            var
          end
          self
        end
      end
    end
  end
end;Fi[	@*iI"               field(var.inverse_type, :type => (String))
              field(var.inverse_of_field, :type => (Symbol))
            end
          end
          self
        end
      end
    end
  end
end;Fi[	@i£I"§            var = @var.instance_eval(&var)
            @var.parent = var
            return var
          end
          self
        end
      end
    end
  end
end;Fi[@@I"# do nothing;FI"end;FI"end;FI" ;Fi	[	[	@ú
iJI"‘          when /\bpassword.*:/i then
            "STRING"
          when /\(yes\/no\)/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        ;Fi[	@·iàI"             end
            "STRING"
          when /accept \(t\)emporarily/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        ;Fi[	@LiHI"º            end
            "STRING"
          when /yes\/no/i then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        ;Fi[	@ËiSI"          when /The entry \'(.+?)\' is no longer a directory/ then
            raise(Capistrano::Error, "STRING")
          when /accept \(t\)emporarily/ then
            "STRING"
          else
            # do nothing
          end
        end
        
        private
        ;Fi[@@@· @‚ I"var = ("STRING" % var);FI"send_message(var);Fi	[	[	@Œ	i6I"’        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      ;Fi[	@*i4I"’        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      ;Fi[	@-i>I"’        when :failure then
          "STRING"
        when :warning then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      ;Fi[	@0i2I"’        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      ;Fi[@(@ÙI"# do nothing;FI"end;FI"end;FI" ;Fi	[	[	@
iPI"Ÿ              end
            end
            remove_duplicate_results!(var.klass, var, var[var]) unless var.empty?
          end
        else
          # do nothing
        end
      end
      
      protected
      ;Fi[	@ìiAI"˚            Associations::HasOneThroughAssociation
          else
            Associations::HasOneAssociation
          end
        else
          # do nothing
        end
      end
      
      def function(arglist)
        options.key?(:polymorphic);Fi[	@–iI"            @var = "STRING"
            @var = attributes.map(&:plural_name)
            set_index_names
          end
        else
          # do nothing
        end
      end
      
      def function(arglist)
        attributes.each_with_index do |arglist|;Fi[	@ài9I"≠          SetLineWidth(0.1)
        end
        Line((@var + var), @var, (@var + var), @var)
      end
    else
      # do nothing
    end
  end
  
  #
  	# Sets font style.;Fi[I"module ClassMethods;FI"end;FI"end;FI"end;FI"end;FI"end;Fi	[	[	@Ùi\I"°          saved_attachments.each { |arglist| (self.attachments << var) }
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˜iÜI"s          @var = true
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˙i?I"{          var.collect(&:mail)
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˝iSI"Ñ          notified_watchers.collect(&:mail)
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[@^I"#   class Person;FI"0#     include ActiveModel::AttributeMethods;FI"#;FI"#     attr_accessor :name;FI",#     attribute_method_suffix '_short?';Fi	[	[	@PiGI"T      #
      # An <tt>attribute#{suffix}</tt> instance method must exist and accept at
      # least the +attr+ argument.
      #
      #   class Person
      #     include ActiveModel::AttributeMethods
      #
      #     attr_accessor :name
      #     attribute_method_suffix '_short?'
      #     define_attribute_methods :name
      #;Fi[	@PiÅI"      end
      
      # Allows you to make aliases for attributes.
      #
      #   class Person
      #     include ActiveModel::AttributeMethods
      #
      #     attr_accessor :name
      #     attribute_method_suffix '_short?'
      #     define_attribute_methods :name
      #;Fi[	@Pi√I"º      # To use, pass an attribute name (as string or symbol), be sure to declare
      # +define_attribute_method+ after you define any prefix, suffix or affix
      # method, or they will not hook in.
      #
      #   class Person
      #     include ActiveModel::AttributeMethods
      #
      #     attr_accessor :name
      #     attribute_method_suffix '_short?'
      #
      #     # Call to define_attribute_method must appear after the;Fi[	@PiÍI"8      end
      
      # Removes all the previously dynamically defined methods from the class.
      #
      #   class Person
      #     include ActiveModel::AttributeMethods
      #
      #     attr_accessor :name
      #     attribute_method_suffix '_short?'
      #     define_attribute_method :name
      #;Fi[@(@Ô
@
@ï@
I"var = {};Fi	[	[	@viHI"      var = {}
      if var.kind_of?(Hash) then
        var.map { |arglist| var[var] = associations_hash(var) if var.kind_of?(Hash) }
      end
      var
    end
    
    def function(arglist)
      var = {}
      if @var.kind_of?(Hash) then
        @var.each do |arglist|;Fi[	@ÖiæI"          var = [self]
          while var.last.inherited_data do
            (var << var.last.inherited_data)
          end
          var
        end
        
        def function(arglist)
          var = {}
          ATTRIBUTES.each do |arglist|
            var = [];Fi[	@6
i“I"≤        else
          # do nothing
        end
      end
      var
    end
    
    def function(arglist)
      var = {}
      var = StringScanner.new(var)
      var.scan(/\s+/);Fi[	@9
iI"I    HOMEBREW_CELLAR.subdirs.each do |arglist|
      var = Formula.factory(var.basename.to_s) rescue nil
      (var << var) if var and (var.rack.exist? and (var.rack.subdirs.length > 0))
    end
    var
  end
  
  def function(arglist)
    var = {}
    var.each do |arglist|
      var = var.recursive_deps.uniq.reject do |arglist|;Fi[@@@I"protected;FI" ;FI"def function(arglist);Fi	[	[	@äi'I"        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist)
          var = @var.instance_methods.map(&:to_s)
          var.include?("STRING") and (var.include?("STRING") and var.include?("STRING"));Fi[	@ëi5I"Ÿ        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist)
          if @var.present? then
            "STRING".tap do |arglist|;Fi[	@ñi&I"œ        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist)
          @var.send(@var).assign(nil)
          @var.valid?;Fi[	@ùi9I"ª        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist)
          class << var
            self;Fi[@^I"#     private;FI"#;FI"%#     def attribute_short?(attr);FI""#       send(attr).length < 5;FI"#     end;Fi	[	[	@PiNI"      #     attr_accessor :name
      #     attribute_method_suffix '_short?'
      #     define_attribute_methods :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #;Fi[	@PiäI"Ù      #     define_attribute_methods :name
      #
      #     alias_attribute :nickname, :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #;Fi[	@PiŒI"/      #     # attribute_method_prefix, attribute_method_suffix or
      #     # attribute_method_affix declares.
      #     define_attribute_method :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #;Fi[	@PiÒI"      #     attr_accessor :name
      #     attribute_method_suffix '_short?'
      #     define_attribute_method :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #;Fi[@^@B!@C!I"#     end;FI"#   end;FI"#;Fi	[	[	@PiPI"˚      #     define_attribute_methods :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob';Fi[	@PiåI"˝      #     alias_attribute :nickname, :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob';Fi[	@Pi–I"˙      #     define_attribute_method :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob';Fi[	@PiÛI"˙      #     define_attribute_method :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob';Fi[@@@@@I"#assert(var.text_executable?.!);Fi	[	[	@iFI"X    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match(/Mach-O (64-bit )?bundle/, file(var));Fi[	@iTI"N    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match(/Mach-O bundle/, file(var));Fi[	@ipI"F    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!)
    assert((var.arch == :dunno));Fi[	@iØI"<    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert_equal([], var.archs)
    assert((var.arch == :dunno));Fi[@^@<@=@>@?I" #   person.pets.size # => 3;Ti	[	[	@&iéI"O      # Deletes the records of the collection directly from the database.
      # This will _always_ remove the records ignoring the +:dependent+
      # option.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [;Ti[	@&iI"8      #
      # You can pass +Fixnum+ or +String+ values, it finds the records
      # responding to the +id+ and executes delete on them.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [;Ti[	@&iI"í      # Destroys the +records+ supplied and removes them from the collection.
      # This method will _always_ remove record from the database ignoring
      # the +:dependent+ option. Returns an array with the removed records.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [;Ti[	@&iåI"Ã      # If the collection has been already loaded +size+ and +length+ are
      # equivalent. If not and you are going to need the records anyway
      # +length+ will take one less query. Otherwise +size+ is more efficient.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   # executes something like SELECT COUNT(*) FROM "pets" WHERE "pets"."person_id" = 1
      #;Ti[I"assert(var.universal?.!);FI"assert(var.i386?.!);FI"assert(var.x86_64?.!);FI"assert(var.ppc7400?.!);FI"assert(var.ppc64?.!);FI"assert(var.dylib?.!);Fi	[	[	@inI"  
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	@içI"8  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?);Fi[	@iùI"8  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?);Fi[	@i≠I":  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[@^I"#   person.pets;TI"#   # => [;T@@ @!i	[	[	@&i(I"\      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&iaI"\      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&iI"\      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&iòI"\      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[I"var = read_file("STRING");FI"/assert(var.include?("STRING").!, "STRING");FI"-assert(var.include?("STRING"), "STRING");FI"end;FI"end;FI"end;Fi	[	[	I".data//homebrew_proj/test/test_patching.rb;Ti#I"  def function(arglist)
    shutup do |arglist|
      DefaultPatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist);Fi[	@ñ!i-I"  def function(arglist)
    shutup do |arglist|
      ListPatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist);Fi[	@ñ!i7I"  def function(arglist)
    shutup do |arglist|
      P0PatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist);Fi[	@ñ!iAI"  def function(arglist)
    shutup do |arglist|
      P1PatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
end;Fi[@(@)I"module Homebrew;FI"extend(self);FI" ;FI"def function(arglist);Fi	[	[	I"$data//homebrew_proj/cmd/deps.rb;Ti
I"  var.deps.each do |arglist|
    puts((("STRING" * var) + var.to_s))
    recursive_deps_tree(Formula.factory(var), (var + 1))
  end
end
module Homebrew
  extend(self)
  
  def function(arglist)
    if ARGV.include?("STRING") then
      Formula.each { |arglist| puts("STRING") };Fi[	I"#data//homebrew_proj/cmd/irb.rb;TiI"ªclass String
  def function(arglist)
    Formula.factory(self)
  end
end
module Homebrew
  extend(self)
  
  def function(arglist)
    if ARGV.include?("STRING") then
      puts("STRING");Fi[	I"'data//homebrew_proj/cmd/options.rb;TiI"—      raise(FormulaUnspecifiedError) if ARGV.named.empty?
      ARGV.formulae
    end
  end
end
module Homebrew
  extend(self)
  
  def function(arglist)
    ff.each do |arglist|
      next if var.build.empty?;Fi[	I"'data//homebrew_proj/cmd/upgrade.rb;Ti	I"Ûclass Fixnum
  def function(arglist)
    (self > 1) ? ("STRING") : ("STRING")
  end
end
module Homebrew
  extend(self)
  
  def function(arglist)
    if Process.uid.zero? and File.stat(HOMEBREW_BREW_FILE).uid.zero?.! then
      abort("STRING");Fi[@˙@˚@¸@˝@˛I"when "STRING" then;Fi	[	[	@ñiÑI"L    def function(arglist)
      case var[0]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then;Fi[	@ñiÜI"b      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then;Fi[	@ñiàI"b      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then;Fi[	@ñiäI"N      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        ["STRING", var[0, 1], var[0, 1]]
      when "STRING" then
        nulltok(var)
      when "STRING" then;Fi[@(@)@
@
@
I"private;Fi	[	[	@öi5I"Â          var.to_ary.map { |arglist| var.serializable_hash(var) }
        else
          var.serializable_hash(var)
        end
      end
      var
    end
    
    private
    
    (alias :read_attribute_for_serialization :send);Fi[	@viQI"·      if @var.kind_of?(Hash) then
        @var.each do |arglist|
          var[var] = var unless [Array, Range, Hash].include?(var.class)
        end
      end
      var
    end
    
    private
    
    def function(arglist);Fi[	@oi_I"ø        end
        var = Pathname.pwd.join(var).relative_path_from(HOMEBREW_REPOSITORY)
        (var[var] << var.to_s)
      end
    end
    var
  end
  
  private
  
  def function(arglist);Fi[	@ØiëI"‘          else
            var[var] = var unless atomic_unsets.include?(var)
          end
        end
      end
      var
    end
    
    private
    
    # Get the old and new value for the provided attribute.;Fi[I"var.puts(var);F@îI"var.puts(var);FI"end;FI"end;FI"end;Fi	[	[	@õiI"Â          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	@ûi#I"Â          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	@°iI"˝          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
    
    def self.suffix(arglist);Fi[	@ßiI"Â          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[I"# @return [ true ] true.;FI"#;FI"# @since 3.0.0;FI"def function(arglist);FI"	true;FI"end;Fi	[	[	@´ivI"›      # @example Is the object resizable?
      #   object.resizable?
      #
      # @return [ true ] true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods;Fi[	@´i†I"ﬂ        # @example Is the object resizable?
        #   Array.resizable?
        #
        # @return [ true ] true.
        #
        # @since 3.0.0
        def function(arglist)
          true
        end
      end
    end;Fi[	@ºi{I"	      # @example Is the hash resizable?
      #   {}.resizable?
      #
      # @return [ true ] true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      # Convert this hash to a criteria. Will iterate over each keys in the;Fi[	@ºiΩI"⁄        # @example Is the hash resizable?
        #   {}.resizable?
        #
        # @return [ true ] true.
        #
        # @since 3.0.0
        def function(arglist)
          true
        end
      end
    end;Fi[@(@)@*@+@ZI"module ClassMethods;Fi	[	[	@æi=I"«            else
              super
            end
          end
        end
      end
    end
    
    module ClassMethods
      @@subclasses = ThreadSafe::Cache.new(:initial_capacity => 2)
      ;Fi[	I"Bdata//activerecord_proj/active_record/validations/presence.rb;TiI"ã          var = Array(var.send(var))
          if var.present? and var.all? { |arglist| var.marked_for_destruction? } then
            var.errors.add(var, :blank, options)
          end
        end
      end
    end
    
    module ClassMethods
      # Validates that the specified attributes are not blank (as defined by
      # Object#blank?), and, if the attribute is an association, that the;Fi[	@'i I"]            if var = relations[var] then
              var = var.cascade_strategy
              var.new(self, var).cascade if var
            end
          end
        end
      end
      
      module ClassMethods
        # Attempt to add the cascading information for the document to know how
        # to handle associated documents on a removal.;Fi[	I"0data//mongoid_proj/lib/mongoid/relations.rb;TièI"C        if instance_variable_defined?("STRING") then
          if (_parent.nil? or instance_variable_get("STRING").!=(_parent)) then
            remove_instance_variable("STRING")
          end
        end
      end
    end
    
    module ClassMethods
      # This is convenience for librarys still on the old API.
      #;Fi[@mI"(instance_eval(&var) if block_given?;FI"end;FI" ;FI"private;FI" ;Fi	[	[	@$i.I"ı        @var ||= []
        @var ||= "STRING"
        @var ||= "STRING"
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.;Fi[	@3iI"      def function(arglist)
        super(var, var)
        @var ||= false
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # This is the provider that Fog uses for the Cloud Files Storage;Fi[	@9iI"!      # Creates a new instance of the storage object
      def function(arglist)
        super(var, var)
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # This is the provider that Fog uses for the Ninefold storage;Fi[	@<iI"      # Creates a new instance of the storage object
      def function(arglist)
        super(var, var)
        @var ||= "STRING"
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # This is the provider that Fog uses for the S3 Storage;Fi[@•I"# type.;FI"#;F@®@] I"#;Fi	[	[	@´iRI"        (first.resizable? or (size > 1))
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #
      # @return [ Array ] The object.
      #;Fi[	@∞iI"ˆ        to_f
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #
      # @return [ Object ] The object.
      #;Fi[	@ºinI"Û        var
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #
      # @return [ Hash ] The object.
      #;Fi[	@øi{I"ı        end
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize
      #
      # @return [ Object ] The object.
      #;Fi[I"# @return [ Object ] self.;FI"#;FI"# @since 3.0.0;FI"def function(arglist);FI"	self;FI"end;Fi	[	[	@øiI"      # @example Evolve the object.
      #   object.__evolve_object_id__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      (alias :__mongoize_object_id__ :__evolve_object_id__);Fi[	@øiI"¯      # @example Convert the object to args.
      #   object.__find_args__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      # Mongoize a plain object into a time.;Fi[	@øi'I"˜      # @example Mongoize the object.
      #   object.__mongoize_time__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      # Try to form a setter from this object.;Fi[	@øi?I"      # @example Get the object as sort criteria.
      #   object.__sortable__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      
      # Conversion of an object to an $inc-able value.;Fi[I"var = @var;FI"@var.each do |arglist|;FI"newobj;FI"'out((("STRING" + var) + "STRING"));FI"out("STRING");FI"end;Fi	[	[	@ñiI"Õ  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj;Fi[	@ôi˘I"Õ  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj;Fi[	@úiﬂI"Õ  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj;Fi[	@ài1I"◊  	# @access protected
  	#
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each do |arglist|
      newobj;Fi[I"=# @param [ Hash ] options The mongo persistence options.;FI"#;FI"<# @return [ Array<Object> ] The new value of the field.;FI"#;FI"# @since 2.0.0;FI"def function(arglist);Fi	[	[	I"9data//mongoid_proj/lib/mongoid/persistence/atomic.rb;Ti!I"É      #
      # @param [ Symbol ] field The name of the field.
      # @param [ Object, Array<Object> ] value The value or values to add.
      # @param [ Hash ] options The mongo persistence options.
      #
      # @return [ Array<Object> ] The new value of the field.
      #
      # @since 2.0.0
      def function(arglist)
        AddToSet.new(self, var, var, var).persist
      end;Fi[	@7"iJI"l      #
      # @param [ Symbol ] field The name of the field.
      # @param [ Numeric ] value The value to increment.
      # @param [ Hash ] options The mongo persistence options.
      #
      # @return [ Array<Object> ] The new value of the field.
      #
      # @since 2.0.0
      def function(arglist)
        Inc.new(self, var, var, var).persist
      end;Fi[	@7"i~I"r      #
      # @param [ Symbol ] field The name of the field.
      # @param [ Array<Object> ] value The values to pull.
      # @param [ Hash ] options The mongo persistence options.
      #
      # @return [ Array<Object> ] The new value of the field.
      #
      # @since 2.0.0
      def function(arglist)
        PullAll.new(self, var, var, var).persist
      end;Fi[	@7"ièI"g      #
      # @param [ Symbol ] field The name of the field.
      # @param [ Object ] value The value to push.
      # @param [ Hash ] options The mongo persistence options.
      #
      # @return [ Array<Object> ] The new value of the field.
      #
      # @since 2.0.0
      def function(arglist)
        Push.new(self, var, var, var).persist
      end;Fi[@^@w@xI"counts["STRING"];FI"end;FI" ;Fi	[	[	@yi1I"      #   map_reduce.emitted
      #
      # @return [ Integer ] The number of emitted documents.
      #
      # @since 3.0.0
      def function(arglist)
        counts["STRING"]
      end
      
      # Provide a finalize js function for the map/reduce.
      #;Fi[	@yi]I"Û      #   map_reduce.input
      #
      # @return [ Integer ] The number of input documents.
      #
      # @since 3.0.0
      def function(arglist)
        counts["STRING"]
      end
      
      # Sets the map/reduce to use jsMode.
      #;Fi[	@yiãI"      #   map_reduce.output
      #
      # @return [ Integer ] The number of output documents.
      #
      # @since 3.0.0
      def function(arglist)
        counts["STRING"]
      end
      
      # Get the raw output from the map/reduce operation.
      #;Fi[	@yiÆI"      #   map_reduce.reduced
      #
      # @return [ Integer ] The number of reduced documents.
      #
      # @since 3.0.0
      def function(arglist)
        counts["STRING"]
      end
      
      # Adds a javascript object to the global scope of the map/reduce.
      #;Fi[@/"@0"@1"@2"I"# @since 2.1.0;FI"def function(arglist);Fi	[	[	@7"i^I"v      #
      # @param [ Symbol ] field The name of the field.
      # @param [ Integer ] value Whether to pop the first or last.
      # @param [ Hash ] options The mongo persistence options.
      #
      # @return [ Array<Object> ] The new value of the field.
      #
      # @since 2.1.0
      def function(arglist)
        Pop.new(self, var, var, var).persist
      end;Fi[	@7"iqI"g      #
      # @param [ Symbol ] field The name of the field.
      # @param [ Object ] value The value to pull.
      # @param [ Hash ] options The mongo persistence options.
      #
      # @return [ Array<Object> ] The new value of the field.
      #
      # @since 2.1.0
      def function(arglist)
        Pull.new(self, var, var, var).persist
      end;Fi[	@7"i†I"r      #
      # @param [ Symbol ] field The name of the field.
      # @param [ Array<Object> ] value The values to push.
      # @param [ Hash ] options The mongo persistence options.
      #
      # @return [ Array<Object> ] The new value of the field.
      #
      # @since 2.1.0
      def function(arglist)
        PushAll.new(self, var, var, var).persist
      end;Fi[	@7"i¬I"g      #
      # @param [ Symbol ] field The name of the field.
      # @param [ Integer ] value The value to set.
      # @param [ Hash ] options The mongo persistence options.
      #
      # @return [ Array<Object> ] The new value of the field.
      #
      # @since 2.1.0
      def function(arglist)
        Sets.new(self, var, var, var).persist
      end;Fi[I"<# @param [ Metadata ] metadata The relation's metadata.;FI"#;FI"-# @return [ Strategy ] The new strategy.;FI"def function(arglist);FI"@var, @var = var, var;FI"@var = var.send(var.name);Fi	[	[	@`iI"m        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        ;Fi[	@eiI"m        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        ;Fi[	@s iI"m        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        ;Fi[	@jiI"m        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        ;Fi[I"J# Binds the base object to the inverse of the relation. This is so we;FI"E# are referenced to the actual objects themselves on both sides.;FI"#;FI"G# This case sets the metadata on the inverse object as well as the;FI"# document itself.;FI"#;Fi	[	[	@i
I"º    module Bindings
      module Embedded
        class In < Binding
          # Binds the base object to the inverse of the relation. This is so we
          # are referenced to the actual objects themselves on both sides.
          #
          # This case sets the metadata on the inverse object as well as the
          # document itself.
          #
          # @example Bind the documents.
          #   name.person.bind(:continue => true);Fi[	@&i
I"º    module Bindings
      module Embedded
        class One < Binding
          # Binds the base object to the inverse of the relation. This is so we
          # are referenced to the actual objects themselves on both sides.
          #
          # This case sets the metadata on the inverse object as well as the
          # document itself.
          #
          # @example Bind the document.
          #   person.name.bind(:continue => true);Fi[	@+i
I"æ    module Bindings
      module Referenced
        class In < Binding
          # Binds the base object to the inverse of the relation. This is so we
          # are referenced to the actual objects themselves on both sides.
          #
          # This case sets the metadata on the inverse object as well as the
          # document itself.
          #
          # @example Bind the documents.
          #   game.person.bind(:continue => true);Fi[	@<i
I"æ    module Bindings
      module Referenced
        class One < Binding
          # Binds the base object to the inverse of the relation. This is so we
          # are referenced to the actual objects themselves on both sides.
          #
          # This case sets the metadata on the inverse object as well as the
          # document itself.
          #
          # @example Bind the document.
          #   person.game.bind(:continue => true);Fi[I"#raise(ArgumentError, "STRING");FI"end;FI"end;FI"end;FI" ;FI"def function(arglist);Fi	[	[	@ˆiI"j        var.each do |arglist|
          var = options[var]
          unless ((var.is_a?(Integer) and (var >= 0)) or (var == Float::INFINITY)) then
            raise(ArgumentError, "STRING")
          end
        end
      end
      
      def function(arglist)
        var = tokenize(var)
        var = var.respond_to?(:length) ? (var.length) : (var.to_s.length);Fi[	@icI"3        when :scp then
          prepare_scp_transfer(var, var, var)
        else
          raise(ArgumentError, "STRING")
        end
      end
    end
    
    def function(arglist)
      var = (callback or Proc.new { |arglist| logger.trace("STRING") if logger and (var == 0) })
      var = case direction;Fi[	@iüI"        when :down then
          var.download(var, var, var, &var)
        else
          raise(ArgumentError, "STRING")
        end
      end
    end
    
    def function(arglist)
      if var.is_a?(String) then
        var.gsub(/\$CAPISTRANO:HOST\$/, var.xserver.host);Fi[	@àiúI"          if var.respond_to?(:call) then
            var.call(self)
          else
            raise(ArgumentError, "STRING")
          end
        end
      end
      
      def function(arglist)
        @var ||= ::Fog::Storage.new(fog_credentials)
      end;Fi[I"3attr_accessor(:document, :relation, :metadata);FI" ;F@h @i I"#;F@k i	[	[	@`i	I"Q  module Relations
    module Cascading
      class Delete
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #;Fi[	@ei	I"R  module Relations
    module Cascading
      class Destroy
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #;Fi[	@s i	I"R  module Relations
    module Cascading
      class Nullify
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #;Fi[	@ji	I"S  module Relations
    module Cascading
      class Restrict
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #;Fi[@(@)@Á@„@∆I"def function(arglist);Fi	[	[	@Wi5I"E        Net::FTP.open(ip, username, password) do |arglist|
          var.passive = true if passive_mode
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|;Fi[	@—	i5I"L      def function(arglist)
        Net::SSH.start(ip, username, :password => (password), :port => (port)) do |arglist|
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        begin
          (write_password_file! unless local;Fi[	@ﬁi)I"U      def function(arglist)
        Net::SSH.start(ip, username, :password => (password), :port => (port)) do |arglist|
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|;Fi[	@Zi'I"V      def function(arglist)
        Net::SFTP.start(ip, username, :password => (password), :port => (port)) do |arglist|
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|;Fi[@(@)@ÁI"#:nodoc:;FI"def function(arglist);FI"
begin;Fi	[	[	@ÎiI"∆          begin_db_transaction)
        rescue
          # do nothing
        end
      end
      
      #:nodoc:
      def function(arglist)
        begin
          execute("STRING")
        rescue;Fi[	@ÎiI"¬          execute("STRING")
        rescue
          # do nothing
        end
      end
      
      #:nodoc:
      def function(arglist)
        begin
          execute("STRING")
        rescue;Fi[	@'i I"          raise("STRING") if (var.length > 1)
          var[0].value = var
          @var[var] = @var.delete(var) if @var.has_key?(var)
        end
      end
      
      #:nodoc:
      def function(arglist)
        begin
          require("diff/lcs")
        rescue LoadError => var;Fi[	@¢iGI"⁄          @var.delete(var)
        else
          raise(MockExpectationError, "STRING")
        end
      end
      
      #:nodoc:
      def function(arglist)
        begin
          verify_expectations
        ensure;Fi[@^I"# @since 2.1.0;FI"def function(arglist);FI""Threaded.executing?("STRING");FI"end;FI" ;Fi	[	[	@6i'I"       #   proxy._assigning?
      #
      # @return [ true, false ] If the thread is assigning.
      #
      # @since 2.1.0
      def function(arglist)
        Threaded.executing?("STRING")
      end
      
      # Execute a block in binding mode.
      #;Fi[	@6iFI"¸      #   proxy.binding?
      #
      # @return [ true, false ] If the thread is binding.
      #
      # @since 2.1.0
      def function(arglist)
        Threaded.executing?("STRING")
      end
      
      # Execute a block in building mode.
      #;Fi[	@6ieI"      #   proxy._building?
      #
      # @return [ true, false ] If the thread is building.
      #
      # @since 2.1.0
      def function(arglist)
        Threaded.executing?("STRING")
      end
      
      # Is the current thread in creating mode?
      #;Fi[	@6iqI"˝      #   proxy.creating?
      #
      # @return [ true, false ] If the thread is creating.
      #
      # @since 2.1.0
      def function(arglist)
        Threaded.executing?("STRING")
      end
      
      # Execute a block in loading mode.
      #;Fi[@(@)@ÁI"attr_reader :function;FI" ;FI"def function(arglist);Fi	[	[	@‹i›I"π            rollback_transaction
            raise
          end
        end
      end
      
      attr_reader :function
      
      def function(arglist)
        @var.open?
      end;Fi[	@)i4I"
      var.each do |arglist|
        @var[var.switch_name] = var
        var.aliases.each { |arglist| @var[var.to_s] ||= var.switch_name }
      end
    end
    
    attr_reader :function
    
    def function(arglist)
      return super unless @var
      var = super;Fi[	@@i—I"“            @var[:env_vars][$1] = $2
            true
          end
        end
      end
      
      attr_reader :function
      
      def function(arglist)
        if @var then
          @var.puts("STRING");Fi[	@ÖipI"´      (MacOS.version < :mountain_lion)
    else
      true
    end
  end
  
  attr_reader :function
  
  def function(arglist)
    case @var
    when :provided_by_osx then;Fi[@h @i I"#;F@k @l I"#;Fi	[	[	@`iI"Æ      class Delete
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.;Fi[	@eiI"Ø      class Destroy
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.;Fi[	@s iI"Ø      class Nullify
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.;Fi[	@jiI"∞      class Restrict
        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.;Fi[@òI"@var = (var + var);FI"	else;FI"@var = var if (var == 2);FI"end;FI"end;Fi	[	[	@ñiúI"®      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  ;Fi[	@ôióI"®      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  ;Fi[	@úiãI"®      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  ;Fi[	@àiéI"©    else
      if (var == 0) then
        @var = var
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  ;Fi[@i I"#;F@k @l I"#;FI"A# @param [ Document ] document The document to cascade from.;Fi	[	[	@`iI"•        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #;Fi[	@eiI"•        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #;Fi[	@s iI"•        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #;Fi[	@jiI"•        attr_accessor(:document, :relation, :metadata)
        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #;Fi[@k @l I"#;F@Á"I"<# @param [ Metadata ] metadata The relation's metadata.;FI"#;Fi	[	[	@`iI"¥        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist);Fi[	@eiI"¥        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist);Fi[	@s iI"¥        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist);Fi[	@jiI"¥        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist);Fi[@^@÷!I"#;FI"# @since 3.0.0;F@Ÿ!I"	true;Fi	[	[	@´iuI"À      #
      # @example Is the object resizable?
      #   object.resizable?
      #
      # @return [ true ] true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      ;Fi[	@´iüI"·        #
        # @example Is the object resizable?
        #   Array.resizable?
        #
        # @return [ true ] true.
        #
        # @since 3.0.0
        def function(arglist)
          true
        end
      end;Fi[	@ºiI"≈      #
      # @example Is the hash resizable?
      #   {}.resizable?
      #
      # @return [ true ] true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      ;Fi[	@ºiºI"‹        #
        # @example Is the hash resizable?
        #   {}.resizable?
        #
        # @return [ true ] true.
        #
        # @since 3.0.0
        def function(arglist)
          true
        end
      end;Fi[@l I"#;F@Á"@Û"I"#;F@["i	[	[	@`iI"Ü        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var;Fi[	@eiI"Ü        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var;Fi[	@s iI"Ü        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var;Fi[	@jiI"Ü        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var;Fi[@Á"@Û"I"#;F@["@\"@]"i	[	[	@`iI"ê        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end;Fi[	@eiI"ê        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end;Fi[	@s iI"ê        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end;Fi[	@jiI"ê        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end;Fi[@(@)@Á@~I"@var = var;FI"@var = var;Fi	[	[	@siTI"Õ                super
              end
            end
          end
        end
        
        def function(arglist)
          @var = var
          @var = var
          super(var, var, var)
        end;Fi[	@∏i I"D          Compass.configuration.sprite_load_path.each do |arglist|
            var = File.expand_path(var)
            return var.gsub("STRING", "STRING") if var.include?(var)
          end
        end
        
        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var;Fi[	@ñiI"“      class << self
        def function(arglist)
          new(var).execute!
        end
      end
      
      def function(arglist)
        @var = var
        @var = var
        @var = var
        @var = nil;Fi[	@æi"I"ù    mail(var) do |arglist|
      var.text
      var.html
    end
  end
  
  def function(arglist)
    @var = var
    @var = var
    @var = var
    @var = var;Fi[@^@"I"#;FI"# @since 3.0.0;F@"I"	self;Fi	[	[	@øiI"—      #
      # @example Evolve the object.
      #   object.__evolve_object_id__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      ;Fi[	@øiI"”      #
      # @example Convert the object to args.
      #   object.__find_args__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      ;Fi[	@øi&I"–      #
      # @example Mongoize the object.
      #   object.__mongoize_time__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      ;Fi[	@øi>I"◊      #
      # @example Get the object as sort criteria.
      #   object.__sortable__
      #
      # @return [ Object ] self.
      #
      # @since 3.0.0
      def function(arglist)
        self
      end
      ;Fi[@["@\"@]"@^"I"end;FI" ;Fi	[	[	@`iI"”        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all;Fi[	@eiI"”        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all;Fi[	@s iI"±        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # This cascade does not delete the referenced relations, but instead
        # sets the foreign key values to nil.;Fi[	@jiI"”        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all;Fi[I"undef_method(:namespace);FI" ;FI")undef_method(:namespace_definitions);FI" ;FI"2undef_method(:line) if method_defined?(:line);FI" ;Fi	[	[	@˜	iI"Í      
      undef_method(:content)
      
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        "STRING";Fi[	I",data//nokogiri_proj/nokogiri/xml/dtd.rb;TiI"Ò      
      undef_method(:content)
      
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        attributes.keys;Fi[	@˝	iI"module Nokogiri
  module XML
    class ElementDecl < Nokogiri::XML::Node
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        "STRING";Fi[	@h	iI"      
      undef_method(:attributes)
      
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def self.new(arglist)
        var.create_entity(var, *var);Fi[@@@Ô @ I"end;FI" ;Fi	[	[	@Œ	i8I"Û        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      ##
      # Creates a new Campfire::Interface object and passes in the;Fi[	@*i6I"        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      def function(arglist)
        var = Prowler.new(:application => (application), :api_key => (api_key));Fi[	@-i@I"Ô        when :warning then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      # Push a message via the Pushover API
      def function(arglist);Fi[	@0i4I"Ï        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      def function(arglist)
        ::Twitter.configure do |arglist|;Fi[@^@k @l I"#;F@Á"@Û"i	[	[	@`iI"ü        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.;Fi[	@eiI"ü        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.;Fi[	@s iI"ü        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.;Fi[	@jiI"ü        
        # Initialize the new cascade strategy, which will set up the relation
        # and the metadata.
        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.;Fi[@^@Á"@Û"I"#;F@["@\"i	[	[	@`iI"é        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name);Fi[	@eiI"é        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name);Fi[	@s iI"é        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name);Fi[	@jiI"é        #
        # @example Instantiate the strategy
        #   Strategy.new(document, metadata)
        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name);Fi[@^@["@\"@]"@^"I"end;Fi	[	[	@`iI"ê        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.;Fi[	@eiI"ê        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.;Fi[	@s iI"ç        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # This cascade does not delete the referenced relations, but instead;Fi[	@jiI"ê        #
        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.;Fi[I"-var = Category.create(:name => "STRING");FI"-var = Category.create(:name => "STRING");FI"-var = Category.create(:name => "STRING");FI"var.move_to_child_of(var);FI"var.move_to_child_of(var);FI"&var = Category.roots.last.to_text;Fi	[	[	I"Rdata//redmine_proj/plugins/awesome_nested_set/spec/awesome_nested_set_spec.rb;Ti?I"q    (categories(:child_2_1).right.should == 9)
  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!;Fi[	@é#iJI"r    (Category.roots.last.to_text.should == var)
  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!;Fi[	I"Rdata//redmine_proj/plugins/awesome_nested_set/test/awesome_nested_set_test.rb;TiãI"°  
  #rebuild swaps the position of the 2 children when added using move_to_child twice onto same parent
  def function(arglist)
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!;Fi[	@ì#iòI"à  
  # doing move_to_child twice onto same parent from the furthest right first
  def function(arglist)
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!;Fi[@(@œI"E# Unbinds the base object and the inverse, caused by setting the;FI"# reference to nil.;FI"#;FI"$# @example Unbind the document.;Fi	[	[	@i$I"x                target.do_or_do_not(metadata.inverse_setter(target), base)
              end
            end
          end
          
          # Unbinds the base object and the inverse, caused by setting the
          # reference to nil.
          #
          # @example Unbind the document.
          #   name.person.unbind(:continue => true)
          #   name.person = nil;Fi[	@&iI"Ö            binding do |arglist|
              target.do_or_do_not(metadata.inverse_setter(target), base)
            end
          end
          
          # Unbinds the base object and the inverse, caused by setting the
          # reference to nil.
          #
          # @example Unbind the document.
          #   person.name.unbind(:continue => true)
          #   person.name = nil;Fi[	@+i%I"A                end
              end
            end
          end
          
          # Unbinds the base object and the inverse, caused by setting the
          # reference to nil.
          #
          # @example Unbind the document.
          #   game.person.unbind(:continue => true)
          #   game.person = nil;Fi[	@<iI"è          # @since 2.0.0.rc.1
          def function(arglist)
            binding { |arglist| bind_from_relational_parent(target) }
          end
          
          # Unbinds the base object and the inverse, caused by setting the
          # reference to nil.
          #
          # @example Unbind the document.
          #   person.game.unbind(:continue => true)
          #   person.game = nil;Fi[@Ü#@á#@â#@ä#@ã#I""Category.update_all("STRING");Fi	[	[	@é#i@I"r  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!
    (Category.roots.last.to_text.should == var);Fi[	@é#iKI"r  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!
    (Category.roots.last.to_text.should == var);Fi[	@ì#iåI"—  #rebuild swaps the position of the 2 children when added using move_to_child twice onto same parent
  def function(arglist)
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!
    assert_equal(Category.roots.last.to_text, var);Fi[	@ì#iôI"∏  # doing move_to_child twice onto same parent from the furthest right first
  def function(arglist)
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!
    assert_equal(Category.roots.last.to_text, var);Fi[@Ü#@â#@ä#@ã#@¶#I"Category.rebuild!;Fi	[	[	@é#iAI"r  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!
    (Category.roots.last.to_text.should == var)
  end;Fi[	@é#iLI"r  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!
    (Category.roots.last.to_text.should == var)
  end;Fi[	@ì#içI"q  def function(arglist)
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!
    assert_equal(Category.roots.last.to_text, var)
  end;Fi[	@ì#iöI"q  def function(arglist)
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING")
    Category.rebuild!
    assert_equal(Category.roots.last.to_text, var)
  end;Fi[I"raise("STRING");FI"end;FI"end;FI"end;FI"end;FI"end;Fi	[	[	@êi0I"                  if var.is_a?(Symbol) then
                    Compass::AppIntegration.lookup(var).configuration
                  else
                    raise("STRING")
                  end
                end
              end
            end
          end
        end
      end;Fi[	@èiI"Ú          if controller.instance_of?(StreamsController) then
            stream_path(:max_time => (time_for_scroll(@var)))
          else
            raise("STRING")
          end
        end
      end
    end
  end
  
  def function(arglist);Fi[	@πi?I"y        end
        
        def function(arglist)
          raise("STRING")
        end
      end
    end
  end
end;Fi[	I"?data//rspec_proj/spec/runner/class_and_arguments_parser.rb;TiI"í          var = ($2 == "STRING") ? (nil) : ($2)
          [$1, var]
        else
          raise("STRING")
        end
      end
    end
  end
end;Fi[I"newobj;FI"out("STRING");FI"$out(("STRING" + var["STRING"]));FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi	[	[	@ñiUI"€    end
    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ôiVI"ÿ    out((var + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@úi@I"€    end
    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àiùI"ÿ    out((var + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[@@I"newobj;FI"out("STRING");FI"out("STRING");FI"$out(("STRING" + var["STRING"]));Fi	[	[	@ñi>I"ﬁ    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((((("STRING" + textstring("STRING")) + "STRING") + textstring(var["STRING"]["STRING"])) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[	@ôiGI"¨    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[	@úi-I"¨    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[	@àiéI"¨    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[@(@œI"J# Determines what the response should be for a particular bit of text;FI"H# from the SCM. Password prompts, connection requests, passphrases,;FI"# etc. are handled here.;FI"def function(arglist);Fi	[	[	@ú
i?I"}            var[/^date: (.*?);/, 1]
          end.sort.last + "STRING")
          return var
        end
        
        # Determines what the response should be for a particular bit of text
        # from the SCM. Password prompts, connection requests, passphrases,
        # etc. are handled here.
        def function(arglist)
          logger.info("STRING")
          case var;Fi[	@·iwI"}        
        def function(arglist)
          (variable(:git) or super)
        end
        
        # Determines what the response should be for a particular bit of text
        # from the SCM. Password prompts, connection requests, passphrases,
        # etc. are handled here.
        def function(arglist)
          var = var[:channel][:host]
          logger.info("STRING");Fi[	@Üi<I"≠        # Increments the given revision number and returns it.
        def function(arglist)
          (var.to_i + 1)
        end
        
        # Determines what the response should be for a particular bit of text
        # from the SCM. Password prompts, connection requests, passphrases,
        # etc. are handled here.
        def function(arglist)
          case var
          when /\(P4PASSWD\) invalid or unset\./i then;Fi[	@ËiAI"®        # Increments the given revision number and returns it.
        def function(arglist)
          (var.to_i + 1)
        end
        
        # Determines what the response should be for a particular bit of text
        # from the SCM. Password prompts, connection requests, passphrases,
        # etc. are handled here.
        def function(arglist)
          var = var[:channel][:host]
          logger.info("STRING");Fi[@@@@@I"end;Fi	[	[	@ñi\I"ô    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@ôi]I"ô    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@úiGI"ô    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@ài§I"ú    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
  
  #;Fi[@^I"/#     state_machine :initial => :parked do;FI"#       event :ignite do;FI",#         transition :parked => :idling;FI"#       end;FI"#     end;Fi	[	[	@®iI".        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   ;Fi[	@®iWI".        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   ;Fi[	@®i{I".        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   ;Fi[	I"<data//state_machine_proj/state_machine/macro_methods.rb;TiåI"D    #   class Vehicle
    #     include DataMapper::Resource
    #     property :id, Serial
    #     
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #     
    #     state_machine :alarm_state, :initial => :active do;Fi[I"
false;FI"end;FI" ;F@7I"
false;FI"end;Fi	[	[	@]
iI"¡      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        false
      end
      
      # This is a noop when there are no open transactions;Fi[	@Ëi|I"T      # CREATE TABLE or ALTER TABLE get rolled back by a transaction? PostgreSQL,
      # SQL Server, and others support this. MySQL and others do not.
      def function(arglist)
        false
      end
      
      def function(arglist)
        false
      end
      
      # Does this adapter support savepoints? PostgreSQL and MySQL do,;Fi[	@êi$I"å    end
    
    def function(arglist)
      false
    end
    
    def function(arglist)
      false
    end
    
    def function(arglist);Fi[	@XixI"‘  
  # @return Returns true if this Post will accept updates (i.e. updates to the caption of a photo).
  def function(arglist)
    false
  end
  
  def function(arglist)
    false
  end
  
  def function(arglist);Fi[I"	true;FI"end;FI"end;FI"end;FI"end;FI"end;Fi	[	[	@'i|I"n        end
        
        def function(arglist)
          true
        end
      end
    end
  end
end;Fi[	@´i§I"        #
        # @since 3.0.0
        def function(arglist)
          true
        end
      end
    end
  end
end
::Array.__send__(:include, Mongoid::Extensions::Array)
::Array.__send__(:extend, Mongoid::Extensions::Array::ClassMethods);Fi[	@ºi¡I"Ï        #
        # @since 3.0.0
        def function(arglist)
          true
        end
      end
    end
  end
end
::Hash.__send__(:include, Mongoid::Extensions::Hash)
::Hash.__send__(:extend, Mongoid::Extensions::Hash::ClassMethods);Fi[	I".data//rspec_proj/spec/matchers/include.rb;Ti&I"à              return false unless var.include?(var)
            end
          end
          true
        end
      end
    end
  end
end;Fi[@^I"%# @return [Array<Twitter::User>];FI"def function(arglist);FI"S@var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) };FI"end;FI"end;Fi	[	[	I"0data//twitter_proj/twitter/action/follow.rb;TiI"       end
      
      # A collection containing the followed user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	I";data//twitter_proj/twitter/action/list_member_added.rb;TiI"      end
      
      # A collection of users who were added to a list
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	I"1data//twitter_proj/twitter/action/mention.rb;Ti&I"      end
      
      # A collection containing the mentioned user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	I"1data//twitter_proj/twitter/action/retweet.rb;TiI"      end
      
      # A collection containing the retweeted user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[@#$@$$I" ;F@⁄I"	true;FI"end;Fi	[	[	@ÎiàI"      
      # Returns true, since this connection adapter supports migrations.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      
      # Returns true, since this connection adapter supports savepoints.;Fi[	@ÎiëI"      
      # Returns true, since this connection adapter supports savepoints.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      
      # Technically MySQL allows to create indexes with the sort order syntax;Fi[	@iÖI"•      
      # Returns true.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      
      # Returns true;Fi[	@i©I"û      
      # Returns true
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      
      #:nodoc:;Fi[@I"B@var = "STRING" unless (var.to_s.empty? or (var == "STRING"));FI"#@var = (HOMEBREW_CACHE + @var);FI"end;FI" ;FI"attr_reader :function;Fi	[	[	@riZI"˚end
class CVSDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist);Fi[	@riÖI"end
class MercurialDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist);Fi[	@ri≠I"˛end
class BazaarDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist);Fi[	@riÀI"˛end
class FossilDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  
  def function(arglist);Fi[I"#     private;FI"#;F@B!@C!I"#     end;FI"#   end;Fi	[	[	@PiOI"      #     attribute_method_suffix '_short?'
      #     define_attribute_methods :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new;Fi[	@PiãI"Á      #
      #     alias_attribute :nickname, :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new;Fi[	@PiœI"      #     # attribute_method_affix declares.
      #     define_attribute_method :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new;Fi[	@PiÚI"
      #     attribute_method_suffix '_short?'
      #     define_attribute_method :name
      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new;Fi[I"#     end;FI"#   end;FI"#;FI"K#   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">;FI".#   vehicle.state           # => "parked";FI"#;Fi	[	[	@$i€I"ù    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # 
    # In the above example, no +initialize+ method is defined.  As a result,
    # the default behavior of initializing the state machine attributes is used.;Fi[	@$iˇI"Ü    #     def initialize(attributes = {})
    #       ...
    #       super()
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # 
    # Because of the way the inclusion of modules works in Ruby, calling
    # <tt>super()</tt> will not only call the superclass's +initialize+, but;Fi[	@$iI"¢    #     def initialize(attributes = {})
    #       ...
    #       initialize_state_machines
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # 
    # You may also need to call the +initialize_state_machines+ helper manually
    # in cases where you want to change how static / dynamic initial states get;Fi[	@$i+I"ù    #       initialize_state_machines(:static => :force) do
    #         ...
    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # 
    # The above example is also noteworthy because it demonstrates how to avoid
    # initialization issues when +nil+ is a valid state.  Without passing in;Fi[@P@QI"#   person.pets;TI"#   # => [;T@@ i	[	[	@&i'I"\      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&i`I"\      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&i~I"\      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&ióI"\      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[@(@œI"# Returns true;F@∫I"	true;FI"end;Fi	[	[	@iqI"Í      #:nodoc:
      def function(arglist)
        "STRING"
      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      # Returns true if SQLite version is '3.6.8' or greater, false otherwise.;Fi[	@iäI"´      
      def function(arglist)
        true
      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      def function(arglist);Fi[	@i†I"—      # Clears the prepared statements cache.
      def function(arglist)
        @var.clear
      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      # Returns true;Fi[	@i•I"π      # Returns true
      def function(arglist)
        true
      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      def function(arglist);Fi[@I"newobj;FI"out("STRING");F@œ#I"out("STRING");FI"out("STRING");Fi	[	[	@ñiTI"”      end
    end
    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ôiUI"Œ    end
    out((var + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@úi?I"Ê      var = (var + "STRING")
    end
    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àiúI"Œ    end
    out((var + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[@(@œ@}@~I"attr_reader :function;FI" ;Fi	[	[	I"9data//activerecord_proj/active_record/persistence.rb;TiGI"       def function(arglist)
        self
      end
    end
    
    attr_reader :function
    
    attr_reader :function
    
    # Returns true if the record is persisted, i.e. it's not a new record and it was
    # not destroyed, otherwise returns false.;Fi[	@“idI"ﬂ      # Enumerates over the manifest files
      def function(arglist)
        @var.each { |arglist| yield(var) }
      end
      
      attr_reader :function
      
      attr_reader :function
      
      protected
      ;Fi[	I"1data//paperclip_proj/paperclip/attachment.rb;TiùI"Q    def function(arglist)
      var = @var[:processors]
      var.respond_to?(:call) ? (var.call(instance)) : (var)
    end
    
    attr_reader :function
    
    attr_reader :function
    
    # Saves the file, if there are no errors. If there are, it flushes them to
    # the instance's errors and returns false, cancelling the save.;Fi[	@˛iI"–          @var = 0
          @var = 0
          @var = nil
        end
        
        attr_reader :function
        
        attr_reader :function
        
        def function(arglist)
          @var = var;Fi[@(@œ@–@I"end;FI" ;Fi	[	[	@?	i}I"‰          rescue NoMethodError
            false
          end
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING");Fi[	@?	i|I"Ï        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING");Fi[	@?	iÄI"Ï        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING");Fi[	@?	iÑI"Ô        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          Spec.deprecate("STRING");Fi[@e@f@˛@:@NI"var;Fi	[	[	@<
i¡I"Ú      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip])
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      ;Fi[	@<
i∆I"      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      ;Fi[	@<
iÀI"      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      ;Fi[	@<
i–I"      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      ;Fi[@I"Cout(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]));FI"$out(("STRING" + var["STRING"]));FI"3out((("STRING" + (@var + 1).to_s) + "STRING"));FI"out("STRING");FI"out("STRING");Fi	[	[	@ñi:I"  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING");Fi[	@ôiCI"  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING");Fi[	@úi)I"  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING");Fi[	@àiäI"!  def function(arglist)
    newobj
    out("STRING")
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING");Fi[@Æ@Ì@Ó@tI"@var = var;FI"@var = var;Fi	[	[	@ìidI"u      # <tt>composed_of :balance, class_name: 'Money'</tt> returns <tt>{ class_name: "Money" }</tt>
      # <tt>has_many :clients</tt> returns +{}+
      # :nodoc:
      def function(arglist)
        @var = var
        @var = var
        @var = var
        @var = var
        @var = var
        @var = var.pluralize_table_names ? (var.to_s.pluralize) : (var.to_s)
      end;Fi[	@`i
I"‚  class Graph
    GRAPH_NAME = :Gemfile
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
      @var = var
      @var = []
      @var = Hash.new { |arglist| var[var] = Set.new };Fi[	@i"I"…    
    attr_reader(:transfers)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
      @var = var
      @var = var
      @var = var.fetch(:via, :sftp);Fi[	@ªi(I"˙      class ForkedCucumberRunner
        include(::Rake::DSL) if defined? ::Rake::DSL
        
        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var
          @var = var
        end
        ;Fi[@Æ@Ì@Ó@t@‹$I"end;Fi	[	[	@»
iI"©    
    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
    end
    
    attr_reader :function;Fi[	@IiI"”    
    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
    end
    
    # Needed before installs, since the arch matters then and quick;Fi[	@iI""    # base - The String path to the <source>.
    # dir  - The String path between <source> and the file.
    # name - The String filename of the file.
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var
    end
    
    # Returns source file path.;Fi[	@iI"{      # The worker object who detected the failure
      # The string name of the queue from which the failed job was pulled
      # The payload object associated with the failed job
      def function(arglist)
        @var = var
        @var = var
        @var = var
        @var = var
      end
      
      # When a job fails, a new instance of your Failure backend is created;Fi[@(@œ@–@•@¶@ßi	[	[	@ñi™I"ˆ    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length;Fi[	@ôi•I"ˆ    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length;Fi[	@úiôI"ˆ    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length;Fi[	@üiàI"W    var = (var + "STRING") if var.!=(0) and var.index("STRING").nil?.!
    self.Cell(var, var, var[(var..var)], var, 2, var, var)
    @var = @var
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length;Fi[@@‡#I"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi	[	[	@ñiVI"Â    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ôiWI"–    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@úiAI"Â    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àiûI"–    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[I"/var = (((var - (2 * @var)) * 1000) / @var);FI"'var = var.gsub("STRING", "STRING");FI"var = var.length;FI"var = -1;FI"var = 0;FI"var = 0;Fi	[	[	@ñiØI"˘  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[	@ôi™I"˘  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[	@úiûI"˘  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[	@üiçI"˘  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[@\I"var = "STRING";FI"var = "STRING";FI"var = "STRING";FI"	else;FI"var = "STRING";Fi	[	[	@ñicI"]    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?;Fi[	@ôiXI"]    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?;Fi[	@úiRI"]    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?;Fi[	@üi>I"]    var = (var - 1) if (var > 0) and (var[(var - 1)].chr == "STRING")
    var = 0
    if var.!=(0) then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.index("STRING").nil?
        var = (var + "STRING") unless var.index("STRING").nil?;Fi[@(@œ@–@NI"var;FI"end;Fi	[	[	@<
i¬I"      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip])
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist);Fi[	@<
i«I"      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist);Fi[	@<
iÃI"      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist);Fi[	@<
i—I"      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist);Fi[@á@à@âI"var = 0;FI"var = (var + 1);FI"$var = var if var and (var == 2);Fi	[	[	@üiYI"6        var = (var == 0) ? (0) : ((var - 1))
        self.Cell(var, var, var[(var..var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        if (var == "STRING"[0]) then;Fi[	@üivI""            self.Cell(var, var, var[(var..var)], var, 2, var, var)
            var = (var + 1)
          end
          var = -1
          var = var
          var = 0
          var = 0
          var = (var + 1)
          var = var if var and (var == 2)
        else
          var = (var + 1);Fi[	@ài[I"        var = (var == 0) ? (1) : (var)
        Cell(var, var, var[(var..var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end;Fi[	@àiyI"          Cell(var, var, var[(var..var)], var, 2, var, var)
          var = (var + 1)
        end
        var = -1
        var = var
        var = 0
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + 1);Fi[@(@œ@–I"include?("STRING");FI"end;FI" ;Fi	[	[	@wibI"Õ  
  def function(arglist)
    (include?("STRING") or ENV["STRING"])
  end
  
  def function(arglist)
    include?("STRING")
  end
  
  def function(arglist)
    var = find { |arglist| var =~ /--json=.+/ };Fi[	@wikI"€  def function(arglist)
    var = find { |arglist| var =~ /--json=.+/ }
    var.split("STRING").last if var
  end
  
  def function(arglist)
    include?("STRING")
  end
  
  def function(arglist)
    include?("STRING");Fi[	@wioI"Æ  
  def function(arglist)
    include?("STRING")
  end
  
  def function(arglist)
    include?("STRING")
  end
  
  def function(arglist)
    (build_head? or build_devel?).!;Fi[	@wiwI"Í  
  def function(arglist)
    (build_head? or build_devel?).!
  end
  
  def function(arglist)
    include?("STRING")
  end
  
  # Request a 32-bit only build.
  # This is needed for some use-cases though we prefer to build Universal;Fi[@(@œ@–I"flag?("STRING");FI"end;FI" ;Fi	[	[	@wiBI"‘  
  def function(arglist)
    (at((@var + 1)) or raise(UsageError))
  end
  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    (flag?("STRING") or (ENV["STRING"] or ENV["STRING"]));Fi[	@wiNI"´  
  def function(arglist)
    (flag?("STRING") or ENV["STRING"])
  end
  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    flag?("STRING");Fi[	@wiRI"ò  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    flag?("STRING");Fi[	@wiVI"≤  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    flag?("STRING")
  end
  
  def function(arglist)
    (include?("STRING") or switch?("STRING"));Fi[I"<Cell(var, var, var[var, (var - var)], var, 2, var, var);FI"if (var == 1) then;FI"@var = @var;FI"	else;FI"if (var == 0) then;FI"@var = var;Fi	[	[	@ñióI"      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else;Fi[	@ôiíI"      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else;Fi[	@úiÜI"      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else;Fi[	@àiàI")      out("STRING")
    end
    (var << "STRING") if var.is_a?(String) and var.include?("STRING")
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = var
        @var = (var + var);Fi[@÷"I"	else;F@ÿ"I"end;FI"end;FI"end;Fi	[	[	@ñiùI"Æ    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist);Fi[	@ôiòI"Æ    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist);Fi[	@úiåI"Æ    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist);Fi[	@àièI"»      if (var == 0) then
        @var = var
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  alias_method(:multi_cell, :MultiCell);Fi[@¶@ß@®I"var = var.length;FI"var = -1;FI"var = 0;Fi	[	[	@ñiÆI"  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0;Fi[	@ôi©I"  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0;Fi[	@úiùI"  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0;Fi[	@üiåI"  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0;Fi[@TI"5if ((var == "STRING") or (var == "STRING")) then;FI"var = "STRING";FI"	else;FI"var = "STRING";FI"end;Fi	[	[	@üi`I"    out(sprintf("STRING", var, var, var, var, var, var))
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end
    out(sprintf("STRING"));Fi[	@üirI"7  def function(arglist)
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end
    out(sprintf("STRING", (var * @var), ((@var - var) * @var), (var * @var), (-var * @var), var));Fi[	@àinI"    out(sprintf("STRING", var, var, var, var, var, var))
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end
    out(sprintf("STRING"));Fi[	@àiàI"7  def function(arglist)
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end
    out(sprintf("STRING", (var * @var), ((@var - var) * @var), (var * @var), (-var * @var), var));Fi[@¶@ßI"end;FI"var = (var + 1);FI"	next;FI"end;Fi	[	[	@ñi¬I"D        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      var = var if (var.! or (var.chr == "STRING"))
      var = (var + (var ? (var[var.chr]) : (1000) or 0));Fi[	@ôiΩI"        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      if (var < 128) then
        var = (var + (var[var.chr] or 0));Fi[	@úi±I"D        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      var = var if (var.! or (var.chr == "STRING"))
      var = (var + (var ? (var[var.chr]) : (1000) or 0));Fi[	@üiüI"        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      if (var == "STRING"[0]) then
        var = var;Fi[@T@ÿ"I"end;FI"end;FI"end;FI" ;Fi	[	[	@ñiûI"Œ      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then;Fi[	@ôiôI"Œ      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then;Fi[	@úiçI"Œ      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then;Fi[	@àiêI"≤        @var = var
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  alias_method(:multi_cell, :MultiCell)
  ;Fi[@(@œ@–I"broadcast(var, var);FI"end;FI" ;Fi	[	[	@™i(I"Ô      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      
      # +feature_element+ is either Scenario or ScenarioOutline
      def function(arglist);Fi[	@™iAI"      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) };Fi[	@™ieI"Â      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      
      def function(arglist)
        broadcast(var);Fi[	@™iuI"      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      
      # Print +messages+. This method can be called from within StepDefinitions.
      def function(arglist);Fi[@TI"/render(:nothing => (true), :status => 422);FI"end;FI"end;FI" ;FI"def function(arglist);Fi	[	[	@@iI"I          render(:partial => "STRING", :locals => ({ :post => (@var.post), :comment => (@var) }))
        end
      end
    else
      render(:nothing => (true), :status => 422)
    end
  end
  
  def function(arglist)
    @var = Comment.find(params[:id])
    if (current_user.owns?(@var) or current_user.owns?(@var.parent)) then;Fi[	@FiI"3          render(:json => (@var.as_api_response(:backbone)), :status => 201)
        end
      end
    else
      render(:nothing => (true), :status => 422)
    end
  end
  
  def function(arglist)
    @var = Like.find_by_id_and_author_id!(params[:id], current_user.person.id)
    current_user.retract(@var);Fi[	@IiI"C    if params[:diaspora_handle] then
      Webfinger.in_background(params[:diaspora_handle], :single_aspect_form => (true))
      render(:nothing => (true))
    else
      render(:nothing => (true), :status => 422)
    end
  end
  
  def function(arglist)
    @var = Person.find_by_guid(params[:person_id])
    if @var then;Fi[	@LiII"˙      else
        render(:nothing => (true), :status => 422)
      end
    else
      render(:nothing => (true), :status => 422)
    end
  end
  
  def function(arglist)
    var = current_user.photos.where(:id => (params[:id])).first
    if var then;Fi[@ÆI"
super;F@T$@U$I"end;FI" ;Fi	[	[	@riYI"È  end
end
class CVSDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  ;Fi[	@riÑI"Ô  end
end
class MercurialDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  ;Fi[	@ri¨I"Ï  end
end
class BazaarDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  ;Fi[	@ri I"Ï  end
end
class FossilDownloadStrategy < AbstractDownloadStrategy
  def function(arglist)
    super
    @var = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @var = (HOMEBREW_CACHE + @var)
  end
  
  attr_reader :function
  ;Fi[@ÆI"
false;FI"end;FI" ;F@7I"
false;Fi	[	[	@]
iI"ì        true
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist)
        false
      end
      ;Fi[	@Ëi{I"]      # Does this adapter support DDL rollbacks in transactions? That is, would
      # CREATE TABLE or ALTER TABLE get rolled back by a transaction? PostgreSQL,
      # SQL Server, and others support this. MySQL and others do not.
      def function(arglist)
        false
      end
      
      def function(arglist)
        false
      end
      ;Fi[	@êi#I"}      true
    end
    
    def function(arglist)
      false
    end
    
    def function(arglist)
      false
    end
    ;Fi[	@XiwI"¬  end
  
  # @return Returns true if this Post will accept updates (i.e. updates to the caption of a photo).
  def function(arglist)
    false
  end
  
  def function(arglist)
    false
  end
  ;Fi[@TI"raise("STRING");FI"end;FI"end;FI"end;FI"end;Fi	[	[	@êi/I"'                else
                  if var.is_a?(Symbol) then
                    Compass::AppIntegration.lookup(var).configuration
                  else
                    raise("STRING")
                  end
                end
              end
            end
          end
        end;Fi[	@èiI"Á        else
          if controller.instance_of?(StreamsController) then
            stream_path(:max_time => (time_for_scroll(@var)))
          else
            raise("STRING")
          end
        end
      end
    end
  end
  ;Fi[	@Ôi#I"¨      var = self.converters.select { |arglist| (var.class == var) }
      if var = var.first then
        var
      else
        raise("STRING")
      end
    end
  end
end;Fi[	@ #iI"œ        if var =~ /([a-zA-Z_]+(?:::[a-zA-Z_]+)*):?(.*)/ then
          var = ($2 == "STRING") ? (nil) : ($2)
          [$1, var]
        else
          raise("STRING")
        end
      end
    end
  end
end;Fi[I"DCell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);FI"var = (var + 1);FI"var = -1;FI"var = var;FI"var = 0;F@äi	[	[	@ñiªI"g      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var);Fi[	@ôi∂I"U      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var
      if (var == 10) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var);Fi[	@úi™I"g      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var);Fi[	@àiºI";    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var);Fi[@ÆI"	true;FI"end;FI" ;F@⁄I"	true;Fi	[	[	@ÎiáI"Õ      end
      
      # Returns true, since this connection adapter supports migrations.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      ;Fi[	@ÎiêI"Õ      end
      
      # Returns true, since this connection adapter supports savepoints.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      ;Fi[	@iÑI"ö      end
      
      # Returns true.
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      ;Fi[	@i®I"ô      end
      
      # Returns true
      def function(arglist)
        true
      end
      
      def function(arglist)
        true
      end
      ;Fi[@TI"nil;FI"end;FI"end;FI"end;FI"end;Fi	[	[	@«i)I"„        else
          if engine_version.!=(var.engine_version) and @var then
            [:engine_version, engine_version, var.engine_version]
          else
            nil
          end
        end
      end
    end
  end
  ;Fi[	I"Gdata//cucumber_prok/cucumber/rb_support/regexp_argument_matcher.rb;TiI"¨            var = var.offset(var)[0]
            Gherkin::Formatter::Argument.new(var, var)
          end
        else
          nil
        end
      end
    end
  end
end;Fi[	@Œi±I"            (@var << var)
            nil
          end
        else
          nil
        end
      end
    end
  end
end;Fi[	@íi#I"¢        if var then
          var.email_the_user(var, var)
          var
        else
          nil
        end
      end
    end
  end
  
  def function(arglist);Fi[I"$out(("STRING" + var["STRING"]));FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi	[	[	@ñiWI"–    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@ôiXI"–    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@úiBI"–    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@àiüI"–    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[@&&@œ$I"out("STRING");FI"out("STRING");FI"newobj;FI"out("STRING");Fi	[	[	@ñi<I":  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]));Fi[	@ôiEI":  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]));Fi[	@úi+I":  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]));Fi[	@àiåI"4    out("STRING")
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]));Fi[@(@œ@–I"@var.each(&var);FI"end;FI" ;Fi	[	[	@;iI"¬      
      def function(arglist)
        @var[var]
      end
      
      def function(arglist)
        @var.each(&var)
      end
      
      def function(arglist)
        var.features = self;Fi[	@}i#I"$      # Duplicates this instance and adds +step_invocations+ to the end
      def function(arglist)
        StepCollection.new((@var + var))
      end
      
      def function(arglist)
        @var.each(&var)
      end
      
      def function(arglist)
        var = (@var.index(var) or -1);Fi[	@Öi©I"ü  
  def function(arglist)
    @var.empty?
  end
  
  def function(arglist)
    @var.each(&var)
  end
  
  def function(arglist)
    map { |arglist| var.flag };Fi[	@ﬂiI"†  
  def function(arglist)
    external_curl_args.empty?.!
  end
  
  def function(arglist)
    @var.each(&var)
  end
  
  def function(arglist)
    @var.empty?;Fi[@ÆI"var = @var;T@ "I"newobj;F@""I"out("STRING");Fi	[	[	@ñiÔI"√  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|;Fi[	@ôi¯I"√  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|;Fi[	@úiﬁI"√  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|;Fi[	@ài0I"ÿ  	# putfonts
  	# @access protected
  	#
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each do |arglist|;Fi[@Õ$@Œ$@œ$I"out("STRING");FI"out("STRING");FI"newobj;Fi	[	[	@ñi;I"  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING");Fi[	@ôiDI"  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING");Fi[	@úi*I"  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING");Fi[	@àiãI"    newobj
    out("STRING")
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING");Fi[@(@œ@–@wI"end;FI" ;Fi	[	[	@i=I"‹      
      def function(arglist)
        @var.tags
      end
      
      def function(arglist)
        source_tags.map { |arglist| var.name }
      end
      
      def function(arglist)
        @var.accept_hook?(var);Fi[	@iGI"=      
      def function(arglist)
        Gherkin::TagExpression.new(var.tag_expressions).eval(source_tags)
      end
      
      def function(arglist)
        source_tags.map { |arglist| var.name }
      end
      
      def function(arglist)
        (@var.tags.to_a + (@var ? (@var.source_tags.to_a) : ([]))).uniq;Fi[	@!i'I"—      
      def function(arglist)
        @var.source_tags
      end
      
      def function(arglist)
        source_tags.map { |arglist| var.name }
      end
      
      def function(arglist)
        init;Fi[	@!iQI"Ù        def function(arglist)
          super
          @var = nil
        end
        
        def function(arglist)
          source_tags.map { |arglist| var.name }
        end
        
        def function(arglist)
          @var.source_tags;Fi[@Æ@Ø@∞@±I"protected;FI" ;Fi	[	[	@äi&I"ÿ          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist)
          var = @var.instance_methods.map(&:to_s);Fi[	@ëi4I"¡          end
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist)
          if @var.present? then;Fi[	@ñi%I"Ã          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist)
          @var.send(@var).assign(nil);Fi[	@ùi8I"Ω          "STRING"
        end
        
        def function(arglist)
          "STRING"
        end
        
        protected
        
        def function(arglist)
          class << var;Fi[@(@œ@–I"	to_s;FI"end;FI" ;Fi	[	[	@|iI"ª    def function(arglist)
      self.value = var
      self.css2_value = var
    end
    
    def function(arglist)
      to_s
    end
    
    def function(arglist)
      value.to_s(var);Fi[	@+
iI"        raise(Sass::SyntaxError, "STRING")
      end
      self.color, self.stop = var, var
    end
    
    def function(arglist)
      to_s
    end
    
    def self.color_to_s(arglist)
      var.is_a?(Sass::Script::String) ? (var.value.dup) : (var.inspect.dup);Fi[	@+
i8I"¿      def function(arglist)
        class_eval("STRING")
      end
    end
    
    def function(arglist)
      to_s
    end
    
    def function(arglist)
      GRADIENT_ASPECTS.include?(var);Fi[	@èi$I"ó    
    def function(arglist)
      args
    end
    
    def function(arglist)
      to_s
    end
    
    def function(arglist)
      var = "STRING";Fi[@Æ@Ø@∞@«	I" ;FI"# :call-seq:;Fi	[	[	@ªi£I"•        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:
    #   should be_true
    #   should be_false;Fi[	@ŒiPI"ﬁ        self
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:
    #   should change(receiver, message, &block)
    #   should change(receiver, message, &block).by(value);Fi[	@ÿiUI"Ê        self
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:
    #   should have(number).named_collection__or__sugar
    #   should_not have(number).named_collection__or__sugar;Fi[	@iI"Æ        "STRING"
      end
      
      def function(arglist)
        "STRING"
      end
    end
    
    # :call-seq:
    #   should satisfy {}
    #   should_not satisfy {};Fi[@œ$I"out("STRING");FI"out("STRING");FI"newobj;FI"out("STRING");FI"out("STRING");Fi	[	[	@ñi=I"Ω    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((((("STRING" + textstring("STRING")) + "STRING") + textstring(var["STRING"]["STRING"])) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));Fi[	@ôiFI"ã    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));Fi[	@úi,I"ã    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));Fi[	@àiçI"ã    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));Fi[@(@œ@–I"@var ||= {};FI"end;FI" ;Fi	[	[	@iI"˛        # Used only within the specs
        def function(arglist)
          defaults.reset!
        end
        
        def function(arglist)
          @var ||= {}
        end
        
        def function(arglist)
          var = ("STRING" + "STRING");Fi[	@giKI"⁄      
      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        @var ||= Hash.new { |arglist| var[var] = [] };Fi[	I"'data//redmine_proj/redmine/i18n.rb;TiíI"ö          var = ::I18n.load_path.select { |arglist| (File.basename(var, "STRING") == var) }
          load_translations(var)
          translations[var] ||= {}
        end
        
        def function(arglist)
          @var ||= {}
        end
        
        # Looks up a translation from the translations hash. Returns nil if
        # eiher key is nil, or locale, scope or key do not exist as a key in the;Fi[	@6iI"ﬁ        @var = (var[:__declared_as] or "STRING")
        @var = var
        @var = var
      end
      
      def function(arglist)
        @var ||= {}
      end
      
      def function(arglist)
        __raise("STRING");Fi[@ÆI"0;FI"end;FI" ;F@LI"0;Fi	[	[	@êiI"x      []
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    ;Fi[	@êiI"w      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    ;Fi[	@êiI"w      0
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    ;Fi[	@êi3I"x      {}
    end
    
    def function(arglist)
      0
    end
    
    def function(arglist)
      0
    end
    ;Fi[@(@œ@–I"if var.nil? then;FI"nil;FI"	else;Fi	[	[	@DiI"B        
        def function(arglist)
          Hash.===(var) ? (var.map { |arglist| "STRING" }.join("STRING")) : (var)
        end
        
        def function(arglist)
          if var.nil? then
            nil
          else
            if String.===(var) then
              Hash[var.scan(HstorePair).map do |arglist|;Fi[	@DiPI"        
        def function(arglist)
          parse_pg_array(var).map { |arglist| var.type_cast(var) }
        end
        
        def function(arglist)
          if var.nil? then
            nil
          else
            if ("STRING" == var) then
              (nil..nil);Fi[	@DibI"              end
            end
          end
        end
        
        def function(arglist)
          if var.nil? then
            nil
          else
            if Range.===(var) then
              if [var.first, var.last].all? { |arglist| Integer.===(var) } then;Fi[	@êiI"˙        var.inherit_from!(configuration)
        var.on_top!
        @var = var
      end
      
      def function(arglist)
        if var.nil? then
          nil
        else
          if var.is_a?(Compass::Configuration::Data) then
            var;Fi[@(@œ@–I"var;FI"end;FI" ;Fi	[	[	@ËiI"«      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var
      end
      
      def function(arglist)
        var[var].lower.eq(var.lower(var));Fi[	@Îi I"Â      
      def function(arglist)
        var.case_sensitive? ? (super) : (var[var].eq(var))
      end
      
      def function(arglist)
        var
      end
      
      def function(arglist)
        @var.fetch(:strict, true);Fi[	@ûi+I":          def function(arglist)
            var = { :step_keyword => (var), :step_name => (var), :multiline_arg_class => (var) }
            super(var)
          end
          
          def function(arglist)
            var
          end
          
          alias :handle_snippet_text :handle_success
        end;Fi[	@˜iI"ã    
    def function(arglist)
      true
    end
    
    def function(arglist)
      var
    end
    
    def function(arglist)
      var;Fi[@(@œ@–I"var = "STRING";F@˙@$i	[	[	@˝iI"%    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@˝iI"%    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@˝iI"%    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@˝i"I"%    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[@ÆI",self.class.to_s.sub("STRING", "STRING");FI"end;FI" ;FI"##;FI";# Logs a message to the console and log file to inform;Fi	[	[	I"0data//backup_proj/backup/compressor/base.rb;TiI"G      
      ##
      # Return the compressor name, with Backup namespace removed
      def function(arglist)
        self.class.to_s.sub("STRING", "STRING")
      end
      
      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is using the compressor
      def function(arglist);Fi[	I".data//backup_proj/backup/database/base.rb;Ti'I"E      
      ##
      # Return the database name, with Backup namespace removed
      def function(arglist)
        self.class.to_s.sub("STRING", "STRING")
      end
      
      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is dumping the database
      def function(arglist);Fi[	I"/data//backup_proj/backup/encryptor/base.rb;TiI"H      
      ##
      # Return the encryptor name, with Backup namespace removed
      def function(arglist)
        self.class.to_s.sub("STRING", "STRING")
      end
      
      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is encrypting the archive
      def function(arglist);Fi[	@“iFI"L      
      ##
      # Return the notifier name, with Backup namespace removed
      def function(arglist)
        self.class.to_s.sub("STRING", "STRING")
      end
      
      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is notifying about the process
      def function(arglist);Fi[@T@U@V@W@^I" ;Fi	[	[	I"Kdata//activerecord_proj/active_record/attribute_methods/primary_key.rb;Ti[I"'          else
            if ActiveRecord::Base.!=(self) and table_exists? then
              connection.schema_cache.primary_keys[table_name]
            else
              "STRING"
            end
          end
        end
        
        # Sets the name of the primary key column.
        #;Fi[	@irI"™          "STRING"
        when 41 then
          "STRING"
        else
          "STRING"
        end
      end
  end
  
  def function(arglist)
    (version.to_f < 4.3);Fi[	@D
iRI"û          end
        end
        "STRING"
      else
        "STRING"
      end
    end
  end
  
  ALLOWED_TAGS = ["STRING", "STRING", "STRING", "STRING"]
  ;Fi[	@´i4I"Â      else
        if ((defined? Gem and var.filename.include?(Gem.dir)) or var.filename =~ /\/bin\/(\w+)$/) then
          "STRING"
        else
          "STRING"
        end
      end
    end
    
    TEMPLATE = "STRING"
  end;Fi[@∏ I" ;F@∫ I"["STRING"];FI"end;FI" ;Fi	[	[	@ÛiI"      
      require("state_machine/integrations/active_record/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      def self.extended(arglist)
        require("active_record/version");Fi[	@˘iI"ﬁ      
      require("state_machine/integrations/mongo_mapper/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      protected
      ;Fi[	@¸iI"      
      require("state_machine/integrations/mongoid/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      def self.extended(arglist)
        require("mongoid/version");Fi[	@ˇiI"4      
      require("state_machine/integrations/sequel/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      # Forces the change in state to be recognized regardless of whether the
      # state value actually changed;Fi[@(@œ@–@R
@S
I"end;Fi	[	[	@]
ioI"≠      
      def function(arglist)
        false
      end
      
      def function(arglist)
        true
      end
    end
    
    class RealTransaction < OpenTransaction;Fi[	@'iyI"µ      class SurplusCell < Cell
        def function(arglist)
          :comment
        end
        
        def function(arglist)
          true
        end
      end
    end
  end;Fi[	I"3data//diaspora_proj/models/account_deletion.rb;Ti+I"Ü  
  def function(arglist)
    Postzord::Dispatcher.build(person.owner, self).post
  end
  
  def function(arglist)
    true
  end
end;Fi[	@ÖiI"∏    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      true
    end
  end
end
Paperclip.io_adapters.register(Paperclip::NilAdapter) do |arglist|;Fi[@T@¬@√@Ô @ I"end;Fi	[	[	@Œ	i7I"√          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      ##;Fi[	@*i5I"÷          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      def function(arglist);Fi[	@-i?I"Ê          "STRING"
        when :warning then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      # Push a message via the Pushover API;Fi[	@0i3I"÷          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      def function(arglist);Fi[@Æ@üI"end;FI" ;F@øI"(@var << "STRING");Fi	[	[	@-iVI"ª        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      ;Fi[	@-iZI"ª        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      ;Fi[	@-i≠I"≥        @var = nil
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      ;Fi[	@-iΩI"¨        end
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      
      def function(arglist)
        (@var << "STRING")
      end
      ;Fi[@T@¬@√@ƒ@≈I"#;Fi	[	[	@ài¢I"∏        var = (var + var)
      end
      @var = (@var + var)
    else
      # do nothing
    end
  end
  
  #
  	# Calculate closing tags.
  	# @param string :tag tag name (in upcase);Fi[	@ài≤I"›      @var[@var] = @var if (@var[@var] < @var)
      @var = (@var + 1)
      @var.push([])
    else
      # do nothing
    end
  end
  
  #
  	# Convert to accessible file path
  	# @param string :attrname image file name;Fi[	@àiçI"”      SetFontSize((@var + var))
      SetStyle("STRING", true)
      @var = (@var * @@var)
    else
      # do nothing
    end
  end
  
  #
  	# Process closing tags.
  	# @param string :tag tag name (in upcase);Fi[	@ài:I"¡        end
        Line((@var + var), @var, (@var + var), @var)
      end
    else
      # do nothing
    end
  end
  
  #
  	# Sets font style.
  	# @param string :tag tag name (in lowercase);Fi[I"&# @return [Array<Twitter::Tweet>];FI";# @param options [Hash] A customizable set of options.;FI"{# @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;FI"|# @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;FI"w# @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;FI"¥# @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi	[	[	I"0data//twitter_proj/twitter/api/timelines.rb;TiI"Ÿ      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @example Return the 20 most recent mentions (statuses containing @username) for the authenticating user
      #   Twitter.mentions;Fi[	@O'i]I"Z      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[	@O'irI"…      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :include_rts Specifies that the timeline should include native retweets in addition to regular tweets. Note: If you're using the trim_user parameter in conjunction with include_rts, the retweets will no longer contain a full user object.;Fi[	@O'iÑI"Z      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[@4$@5$@6$I"end;FI"end;FI"end;Fi	[	[	@;$iI"ˆ      
      # A collection containing the followed user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	@>$i I"˚      
      # A collection of users who were added to a list
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	@A$i'I"˜      
      # A collection containing the mentioned user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	@D$iI"˜      
      # A collection containing the retweeted user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[I"# @rate_limited Yes;FI",# @authentication Requires user context;FI"g# @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;FI"&# @return [Array<Twitter::Tweet>];F@H'@I'i	[	[	@O'iI"(      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/mentions_timeline
      # @note This method can only return up to 800 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@O'iZI"&      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@O'ioI"8      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@O'iÅI"8      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[@T@¬@√@ƒ@MI" ;Fi	[	[	@ã	i6I"‚            target.destroy
          when :nullify then
            target.update_columns(reflection.foreign_key => (nil))
          else
            # do nothing
          end
        end
      end
      
      private
      ;Fi[	I"%data//homebrew_proj/cmd/audit.rb;TigI"“        problem("STRING")
      when "STRING", "STRING" then
        problem("STRING".undent)
      else
        # do nothing
      end
    end
  end
  
  def function(arglist)
    f.conflicts.each do |arglist|;Fi[	@x'iØI"*        problem("STRING") unless var.url =~ /[a-fA-F0-9]{40}/
      when /macports\/trunk/ then
        problem("STRING")
      else
        # do nothing
      end
    end
  end
  
  def function(arglist)
    if text =~ /<(Formula|AmazonWebServicesFormula|ScriptFileFormula|GithubGistFormula)/ then;Fi[	@üi∫I"»        :zip
      when "STRING" then
        :p7zip
      else
        # do nothing
      end
    end
  end
  
  def function(arglist)
    /^#!\s*\S+/.===(open("STRING") { |arglist| var.read(1024) });Fi[@(@œ@ç@é@ÍI"var = @var;Fi	[	[	@ñiÎI"ﬁ    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj;Fi[	@ôiÙI"ﬁ    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj;Fi[	@úi⁄I"ﬁ    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj;Fi[	@üiI"˜    else
      open(var, "STRING") { |arglist| var.write(@var) }
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    unless (@var.nil? or (@var == "STRING")) then
      1.upto(var) { |arglist| @var[var].gsub!(@var, var.to_s) };Fi[@TI"
false;FI"end;FI"end;FI" ;FI"def function(arglist);Fi	[	[	I"Qdata//activerecord_proj/active_record/associations/collection_association.rb;TiI".            load_target if options[:finder_sql]
            loaded? ? (target.include?(var)) : (scope.exists?(var))
          end
        else
          false
        end
      end
      
      def function(arglist)
        @var = merge_target_lists(find_target, target) if find_target?
        loaded!;Fi[	@)içI"‹      case peek
      when LONG_RE, SHORT_RE, EQ_RE, SHORT_NUM, SHORT_SQ_RE then
        true
      else
        false
      end
    end
    
    def function(arglist)
      peek and (parsing_options?.! or super)
    end;Fi[	I"'data//diaspora_proj/models/user.rb;TiãI"    var = var.class.base_class.name
    if self.hidden_shareables.has_key?(var) then
      self.hidden_shareables[var].include?(var.id.to_s)
    else
      false
    end
  end
  
  def function(arglist)
    var = var.id.to_s
    var = var.class.base_class.to_s;Fi[	@üiÈI"›  def function(arglist)
    if ((@var + var) > @var) and (@var.! and self.AcceptPageBreak) then
      true
    else
      false
    end
  end
  
  def function(arglist)
    if self.BreakThePage?(var) then
      var = @var;Fi[@ÆI"6super(compose_message("STRING", :klass => (var)));FI"end;FI"end;FI"end;FI"end;Fi	[	[	I":data//mongoid_proj/lib/mongoid/errors/invalid_path.rb;TiI"¬      # @param [ Class ] klass The document class.
      #
      # @since 3.0.14
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[	I"9data//mongoid_proj/lib/mongoid/errors/no_metadata.rb;TiI"¡      # @param [ Class ] klass The document class.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[	I"7data//mongoid_proj/lib/mongoid/errors/no_parent.rb;TiI"—      # @param [ Class ] klass The class of the embedded document.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[	I"Ddata//mongoid_proj/lib/mongoid/errors/versioning_not_on_root.rb;TiI"¡      # @param [ Class ] klass The embedded class.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[@(@œ@ç@é@ÍI""STRING";Fi	[	[	@iNI"Ñ          raise("STRING")
        end
      end
    end
    
    private
    
    def function(arglist)
      "STRING"
    end
  end;Fi[	@»
iDI"Ñ    
    def function(arglist)
      @var = var
    end
    
    private
    
    def function(arglist)
      "STRING"
    end
  end;Fi[	@Éi€I"Ã      def function(arglist)
        raise("STRING") unless Nokogiri.jruby?
        return toJavaDocument
      end
      
      private
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@§i2I"¸      return false if ((var - Time.now.to_i).abs > OpenID::Nonce.skew)
      @var.write(nonce_key(var, var), var, :expires_in => (OpenID::Nonce.skew))
      return true
    end
    
    private
    
    def function(arglist)
      "STRING"
    end
    ;Fi[@Æ@NI"var;FI"end;FI" ;FI"def function(arglist);Fi	[	[	@<
iƒI"        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var;Fi[	@<
i…I"        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var;Fi[	@<
iŒI"        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var;Fi[	@<
i”I"Ê        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var;Fi[@(@œ@ç@é@ÍI"case var;Fi	[	[	@QiCI"Û      
      def function(arglist)
        associations.each { |arglist| preload(var) } unless records.empty?
      end
      
      private
      
      def function(arglist)
        case var
        when Hash then
          preload_hash(var);Fi[	I"Xdata//activerecord_proj/active_record/connection_adapters/abstract/schema_dumper.rb;Ti&I"      # Lists the valid migration options
      def function(arglist)
        [:name, :limit, :precision, :scale, :default, :null]
      end
      
      private
      
      def function(arglist)
        case var
        when BigDecimal then
          var.to_s;Fi[	@iÓI"◊          warn("STRING")
          false
        end
      end
      
      private
      
      def function(arglist)
        case var
        when File then
          YAML.load(ERB.new(File.read(var.path)).result);Fi[	@æ
iI"≥          end
        end
        @var.call(var)
      end
      
      private
      
      def function(arglist)
        case var
        when /\.jpe?g/i then
          "STRING";Fi[@f'@g'@h'@H'@I'@J'i	[	[	@O'iI"€      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/mentions_timeline
      # @note This method can only return up to 800 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@O'i[I"Ÿ      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@O'ipI"Î      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@O'iÇI"Î      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[@Æ@•@¶@ß@®I"var = var.length;Fi	[	[	@ñi¨I"Ê    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0;Fi[	@ôißI"Ê    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0;Fi[	@úiõI"Ê    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0;Fi[	@üiäI"Ó    @var = @var
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0;Fi[@g'@h'@H'@I'@J'@K'i	[	[	@O'iI"˙      # @note This method can only return up to 800 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @example Return the 20 most recent mentions (statuses containing @username) for the authenticating user;Fi[	@O'i\I"Ê      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi[	@O'iqI"¯      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi[	@O'iÉI"¯      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi[@(@œ@ßI"end;FI"end;FI"end;Fi	[	[	@ÙiZI"»        
        def function(arglist)
          saved_attachments.each { |arglist| (self.attachments << var) }
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˜iÑI"°        def function(arglist)
          @var = nil
          @var = true
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˙i=I"—          var = project.notified_users
          var.reject! { |arglist| visible?(var).! }
          var.collect(&:mail)
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[	@˝iQI"⁄        # Returns an array of watchers' email addresses
        def function(arglist)
          notified_watchers.collect(&:mail)
        end
        
        module ClassMethods
        end
      end
    end
  end
end;Fi[@DI"	else;F@òI"var = "STRING";FI"	else;FI"var = "STRING";Fi	[	[	@üi_I"    var = var
    out(sprintf("STRING", var, var, var, var, var, var))
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end;Fi[	@üiqI"ÿ  
  def function(arglist)
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end;Fi[	@àimI"    var = var
    out(sprintf("STRING", var, var, var, var, var, var))
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end;Fi[	@àiáI"⁄  	#
  def function(arglist)
    if (var == "STRING") then
      var = "STRING"
    else
      if ((var == "STRING") or (var == "STRING")) then
        var = "STRING"
      else
        var = "STRING"
      end
    end;Fi[I";# @param options [Hash] A customizable set of options.;FI"{# @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;FI"|# @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;FI"w# @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;F@L'I"S# @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi	[	[	@O'iGI"∏      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @example Return the 20 most recent retweets posted by @sferik;Fi[	@O'i^I"ñ      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @example Return the 20 most recent retweets posted by the authenticating user;Fi[	@O'isI"a      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :include_rts Specifies that the timeline should include native retweets in addition to regular tweets. Note: If you're using the trim_user parameter in conjunction with include_rts, the retweets will no longer contain a full user object.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[	@O'iÖI"ƒ      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @option options [Boolean, String, Integer] :include_entities The tweet entities node will be disincluded when set to false.;Fi[@Æ@1I"end;FI" ;F@ÉI"__raise("STRING");Fi	[	[	@6iI"∏        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      ;Fi[	@6i"I"∏        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      ;Fi[	@6i&I"∏        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      ;Fi[	@6i*I"∏        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      
      def function(arglist)
        __raise("STRING")
      end
      ;Fi[@C!I"#     end;FI"#   end;FI"#;FI"#   person = Person.new;FI"#   person.name = 'Bob';Fi	[	[	@PiRI"      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob'
      #   person.name          # => "Bob"
      #   person.name_short?   # => true;Fi[	@PiéI"       #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob'
      #   person.name            # => "Bob"
      #   person.nickname        # => "Bob";Fi[	@Pi“I"      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob'
      #   person.name        # => "Bob"
      #   person.name_short? # => true;Fi[	@PiıI"˜      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob'
      #   person.name_short? # => true
      #;Fi[@B!@C!I"#     end;FI"#   end;FI"#;F@,(i	[	[	@PiQI"˙      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob'
      #   person.name          # => "Bob";Fi[	@PiçI"¸      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob'
      #   person.name            # => "Bob";Fi[	@Pi—I"¯      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob'
      #   person.name        # => "Bob";Fi[	@PiÙI"˜      #
      #     private
      #
      #     def attribute_short?(attr)
      #       send(attr).length < 5
      #     end
      #   end
      #
      #   person = Person.new
      #   person.name = 'Bob'
      #   person.name_short? # => true;Fi[@Æ@6$I"end;FI"end;FI"end;FI"end;Fi	[	[	@;$iI"Ô      # A collection containing the followed user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	@>$i!I"Ù      # A collection of users who were added to a list
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	@A$i(I"      # A collection containing the mentioned user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[	@D$iI"      # A collection containing the retweeted user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:targets]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
    end
  end
end;Fi[@ä @ã I"Bvar = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING");FI"var = 0;FI"if var then;F@\i[[	@ñi^I"W    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING";Fi[	@ôiSI"W    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING";Fi[	@úiMI"W    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING";Fi[@ÆI"Rvar.class.state_machines.transitions(var, action).perform { |arglist| yield };FI"end;FI" ;FI"G# Creates a scope for finding records *with* a particular state or;FI"# states for the attribute;Fi[[	@ÛiDI"ï      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist)
        create_scope(var, lambda { |arglist| ["STRING", var] });Fi[	@˘i=I"É      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist)
        define_scope(var, lambda do |arglist|;Fi[	@¸iAI"Æ      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist)
        define_scope(var, lambda { |arglist| { attribute => ({ "STRING" => (var) }) } });Fi[@ÆI"#if (action_hook == :save) then;FI"Gowner_class.set_callback(:save, :around, self, :prepend => (true));FI"	else;FI"
super;FI"end;Fi[[	@Ûi;I"$      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      ;Fi[	@˘i4I"$      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      ;Fi[	@¸i8I"$      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      ;Fi[@ÆI"9@var = Array(@var[:target_objects]).map do |arglist|;FI"%Twitter::Tweet.fetch_or_new(var);FI"end;FI"end;FI" ;Fi[[	@A$iI">      # A collection of tweets that mention a user
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      
      # A collection containing the mentioned user
      #;Fi[	I"/data//twitter_proj/twitter/action/reply.rb;TiI"E      # A collection of tweets that reply to a user
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      
      # A collection that contains the replied-to tweets
      #;Fi[	@D$iI",      # A collection of retweets
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      
      # A collection containing the retweeted user
      #;Fi[@ÆI"+var = Twitter::API::Arguments.new(var);FI"&merge_list!(var.options, var.pop);FI"Lunless (var.options[:owner_id] or var.options[:owner_screen_name]) then;FI"8merge_owner!(var.options, (var.pop or screen_name));FI"end;Fi[[	I",data//twitter_proj/twitter/api/lists.rb;TiHI"      #     Twitter.list_timeline('sferik', 8863586)
      #     Twitter.list_timeline(7505382, 'presidents')
      #     Twitter.list_timeline(7505382, 8863586)
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options[:owner_id] or var.options[:owner_screen_name]) then
          merge_owner!(var.options, (var.pop or screen_name))
        end
        objects_from_response(Twitter::Tweet, :get, "STRING", var.options)
      end;Fi[	@ç(iI"ª      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options[:owner_id] or var.options[:owner_screen_name]) then
          merge_owner!(var.options, (var.pop or screen_name))
        end
        object_from_response(Twitter::List, var, var, var.options)
      end;Fi[	@ç(iI"º        object_from_response(Twitter::List, var, var, var.options)
      end
      
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options[:owner_id] or var.options[:owner_screen_name]) then
          merge_owner!(var.options, (var.pop or screen_name))
        end
        cursor_from_response(:users, Twitter::User, var, var, var.options, var)
      end;Fi[@Æ@k(I"Jowner_class.set_callback(:save, :after, "STRING", :prepend => (true));FI"end;FI"
super;FI"end;Fi[[	@#igI"\        def self.active?(arglist)
          (::ActiveRecord::VERSION::MAJOR == 3) and (::ActiveRecord::VERSION::MINOR == 0)
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end
    end;Fi[	@&iOI"T        def self.active?(arglist)
          defined? ::MongoMapper::Version and ::MongoMapper::Version =~ /^0\.9\./
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end
    end;Fi[	@∞iI">        def function(arglist)
          define_helper(:instance, "STRING", "STRING", (26 + 1))
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end
      ;Fi[@Æ@I"end;FI" ;F@I"colour(var, "STRING");Fi[[	@?	iI"»          end
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        ;Fi[	@?	i~I"⁄          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        ;Fi[	@?	iÇI"⁄          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        ;Fi[@DI"
begin;FI"!var = (var + var.iconv(var));FI""rescue Iconv::IllegalSequence;FI"var = (var + $!.success);FI"6var = ("STRING" + $!.failed[1, $!.failed.length]);Fi[[	I"/data//redmine_proj/redmine/codeset_util.rb;TiI"ê          var = var.iconv(var)) rescue var = var.gsub(/[^\r\n\t\x20-\x7e]/, "STRING")
        else
          var = Iconv.new("STRING", "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue;Fi[	@≤(i=I"ã          var = var.iconv(var)) rescue var = var.gsub(/[^\r\n\t\x20-\x7e]/, "STRING")
        else
          var = Iconv.new("STRING", var)
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue;Fi[	@≤(i{I"ã          var = var.iconv(var)) rescue var = var.gsub(/[^\r\n\t\x20-\x7e]/, "STRING")
        else
          var = Iconv.new(var, "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue;Fi[@DI"(var << "STRING");FI"(var << "STRING");FI"(var << "STRING");FI"](var << view.content_tag(:div, "STRING".html_safe, :style => (var), :class => "STRING"));FI"end;Fi[[	@I
i≥I"˙          (var << view.content_tag(:div, "STRING".html_safe, var))
          if var[:bar_late_end] then
            var = ((var[:bar_late_end] - var[:bar_start]) - 2)
            var = "STRING"
            (var << "STRING")
            (var << "STRING")
            (var << "STRING")
            (var << view.content_tag(:div, "STRING".html_safe, :style => (var), :class => "STRING"))
          end
          if var[:bar_progress_end] then
            var = ((var[:bar_progress_end] - var[:bar_start]) - 2);Fi[	@I
i∆I"f        end
        if var[:markers] then
          if var[:start] then
            var = "STRING"
            (var << "STRING")
            (var << "STRING")
            (var << "STRING")
            (var << view.content_tag(:div, "STRING".html_safe, :style => (var), :class => "STRING"))
          end
          if var[:end] then
            var = "STRING";Fi[	@I
iÕI"û            (var << view.content_tag(:div, "STRING".html_safe, :style => (var), :class => "STRING"))
          end
          if var[:end] then
            var = "STRING"
            (var << "STRING")
            (var << "STRING")
            (var << "STRING")
            (var << view.content_tag(:div, "STRING".html_safe, :style => (var), :class => "STRING"))
          end
        end
        if var[:label] then;Fi[@D@ˇ'I"var = "STRING";FI"8var = "STRING" unless var.to_s.index("STRING").nil?;FI"@var = (var + "STRING") unless var.to_s.index("STRING").nil?;FI"@var = var.to_s.index("STRING") ? ((var + "STRING")) : (var);Fi[[	@ñifI"U      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end;Fi[	@ôi[I"U      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end;Fi[	@úiUI"U      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end;Fi[@ÆI"var = max_value;FI"var = min_value;FI"var = (var - var);FI"-var = (var == 0) ? (10) : ((var / 20.0));FI"var = ((var + var) - var);Fi[[	I"(data//redmine_proj/SVG/Graph/Bar.rb;TiI"M        @var[:fields]
      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers;Fi[	I"2data//redmine_proj/SVG/Graph/BarHorizontal.rb;TiI"D      
      protected
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers;Fi[	@ziQI"Z        @var = var if (var > @var)
      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers;Fi[@DI"var = "STRING";FI"var = "STRING";FI"	else;FI"var = "STRING";F@«(i[[	@ñidI"_    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var);Fi[	@ôiYI"_    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var);Fi[	@úiSI"_    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var);Fi[@D@·(I"	else;FI"var = "STRING";F@«(@»(i[[	@ñieI"]    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end;Fi[	@ôiZI"]    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end;Fi[	@úiTI"]    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end;Fi[@ÆI"out("STRING");F@Õ$@Œ$@œ$I"out("STRING");Fi[[	@ñi9I"    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj;Fi[	@ôiBI"    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj;Fi[	@úi(I"    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj;Fi[@D@«(@»(@…(I"end;FI"end;Fi[[	@ñihI">        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0;Fi[	@ôi]I">        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0;Fi[	@úiWI">        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0;Fi[@DI"G32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) };FI"out((var + "STRING"));FI"out("STRING");FI"newobj;FI"var = ("STRING" + var);Fi[[	@ñi%I"ò        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var));Fi[	@ôi.I"ò        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var));Fi[	@úiI"ò        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var));Fi[@ÆI"2AddCIDFont(var, "STRING", var, var, var, var);FI"?AddCIDFont(var, "STRING", (var + "STRING"), var, var, var);FI"?AddCIDFont(var, "STRING", (var + "STRING"), var, var, var);FI"?AddCIDFont(var, "STRING", (var + "STRING"), var, var, var);FI"end;Fi[[	@ñiI"⁄    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -130, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist);Fi[	@ôiI"⁄    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -120, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist);Fi[	@úiI"⁄    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -130, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist);Fi[@ÆI"%var = Node.new(:COMBINATOR, var);FI"var;FI"end;FI" ;FI"def function(arglist);Fi[[	@<
i(I"Ì      
      # reduce 49 omitted
      # reduce 50 omitted
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var;Fi[	@<
i-I"œ        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var;Fi[	@<
i2I"œ        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var;Fi[@ÆI"Gsuper(compose_message("STRING", :name => (var), :config => (var)));FI"end;FI"end;FI"end;FI"end;Fi[[	I"Idata//mongoid_proj/lib/mongoid/errors/mixed_session_configuration.rb;TiI"Ÿ      # @param [ Hash ] config The configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[	I"Adata//mongoid_proj/lib/mongoid/errors/no_session_database.rb;TiI"ﬁ      # @param [ Hash ] config The hash configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[	I">data//mongoid_proj/lib/mongoid/errors/no_session_hosts.rb;TiI"ﬁ      # @param [ Hash ] config The hash configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[@TI"var = "STRING";F@«(@»(@…(I"end;Fi[[	@ñigI"I        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1;Fi[	@ôi\I"I        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1;Fi[	@úiVI"I        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1;Fi[@ÆI")super(compose_message("STRING", {}));FI"end;FI"end;FI"end;FI"end;Fi[[	I":data//mongoid_proj/lib/mongoid/errors/invalid_find.rb;TiI"õ      #   InvalidFind.new
      #
      # @since 2.2.0
      def function(arglist)
        super(compose_message("STRING", {}))
      end
    end
  end
end;Fi[	I"<data//mongoid_proj/lib/mongoid/errors/no_environment.rb;TiI"ù      #   NoEnvironment.new
      #
      # @since 2.4.0
      def function(arglist)
        super(compose_message("STRING", {}))
      end
    end
  end
end;Fi[	I"@data//mongoid_proj/lib/mongoid/errors/no_sessions_config.rb;TiI"†      #   NoSessionsConfig.new
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", {}))
      end
    end
  end
end;Fi[@TI"	true;FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@
isI"          inverse_updates_counter_cache?(through_reflection).!
        when :nullify then
          false
        else
          true
        end
      end
      
      def function(arglist)
        ensure_not_nested
        var = load_target if (var == :all);Fi[	@Ç
i?I"6  def function(arglist)
    if self.status_message_guid and self.status_message.text_and_photos_blank? then
      self.status_message.destroy
    else
      true
    end
  end
  
  def function(arglist)
    var = StatusMessage.find_by_guid(self.status_message_guid)
    if self.status_message_guid and var then;Fi[	@ài4I"»          else
            @var[:fog_public]
          end
        else
          true
        end
      end
      
      def function(arglist)
        for var, var in @var do
          (log("STRING");Fi[@ÆI"'var = (HOMEBREW_PREFIX / "STRING");FI"var.write("STRING");FI"assert(var.universal?.!);FI"assert(var.i386?.!);FI"assert(var.x86_64?.!);Fi[[	@iäI"
    (HOMEBREW_PREFIX / "STRING").unlink
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@iöI"    assert_match(/text executable/, file(var))
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@i™I"    assert_match(/text executable/, file(var))
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[@ÆI"!var = Pathname.new("STRING");FI"assert(var.universal?);FI"assert(var.i386?.!);F@@i[[	@i
I"-  `/usr/bin/file -h '#{var}STRING`.chomp
end
class MachOPathnameTests < Test::Unit::TestCase
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?);Fi[	@i4I"*    assert_match(/Mach-O 64-bit dynamically linked shared library/, file(var))
  end
  
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@iBI"    assert_match(/Mach-O (64-bit )?executable/, file(var))
  end
  
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[@ÆI"*return image_url if (var == "STRING");FI"4if (var.nil? or var.match(/^https?:\/\//)) then;FI"super(var);FI"	else;FI"%super(absolutify_local_url(var));Fi[[	@`ilI"    self.attributes.merge(var) { |arglist| var.blank? ? (var) : (var) }
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end;Fi[	@`iuI"’    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end;Fi[	@`i~I"’    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end;Fi[@ÆI" self.author.diaspora_handle;FI"end;FI" ;FI"def function(arglist);FI"+self.author = Webfinger.new(var).fetch;Fi[[	I"*data//diaspora_proj/models/comment.rb;Ti7I"  after_destroy { |arglist| self.parent.update_comments_counter }
  
  #should be in relayable (pending on fixing Message)
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  ;Fi[	@)i(I"Ω    (self.participants - [self.author])
  end
  
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  ;Fi[	I"*data//diaspora_proj/models/message.rb;Ti/I"   
  #sign comment as commenter
  #sign comment as post owner
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  ;Fi[@Æ@üI"	self;FI"end;FI" ;FI"def function(arglist);Fi[[	@πiI"ü      var
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist)
      (@var << "STRING")
      self;Fi[	@πi I"π      self
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist)
      label_field(var, var)
      input_field("STRING", var);Fi[	@πiAI"ª      self
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist)
      return self if @var
      (@var << "STRING") unless @var;Fi[@T@¬@√I"	else;FI""STRING";FI"end;Fi[[	@I
iI"3            image_task(var, var, :label => (var), :markers => (true), :height => 3)
          when :pdf then
            pdf_task(var, var, :label => (var), :markers => (true), :height => 0.8)
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      ;Fi[	@I
i1I"3            image_task(var, var, :label => (var), :markers => (true), :height => 3)
          when :pdf then
            pdf_task(var, var, :label => (var), :markers => (true), :height => 0.8)
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      ;Fi[	@I
ilI"Ì            image_task(var, var, :label => (var))
          when :pdf then
            pdf_task(var, var, :label => (var))
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      ;Fi[@Æ@•@‘@’@0I"broadcast(var, var);Fi[[	@™i?I"Ú        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      ;Fi[	@™icI"Ï        broadcast(var, var, var, var, var, var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      ;Fi[	@™isI"Ú        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      ;Fi[@T@¬@√I"var;FI"end;FI" ;Fi[[	@õi-I"Ï          when :stderr then
            var = $stderr
            $stderr = var.unwrap!
          else
            # do nothing
          end
          var
        end
        
        def self.wrap(arglist)
          validate_pipe(var);Fi[	@ç(iJI"        when Twitter::List then
          var[:list_id] = var.id
          merge_owner!(var, var.user)
        else
          # do nothing
        end
        var
      end
      
      # Take an owner and merge it into the hash with the correct key
      #;Fi[	I",data//twitter_proj/twitter/api/utils.rb;TiëI"N          var[[var, "STRING"].compact.join("STRING").to_sym] = var
        when Twitter::User then
          var[[var, "STRING"].compact.join("STRING").to_sym] = var.id
        else
          # do nothing
        end
        var
      end
      
      # Take a multiple users and merge them into the hash with the correct keys
      #;Fi[@ÆI"var = case var;F@>I""STRING";FI"when :warning then;FI""STRING";Fi[[	@Œ	i/I"Y      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING";Fi[	@*i-I"Y      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING";Fi[	@0i+I"Y      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING";Fi[@T@—@“@”@‘I" ;Fi[[	@ibI"G          prepare_sftp_transfer(var, var, var)
        when :scp then
          prepare_scp_transfer(var, var, var)
        else
          raise(ArgumentError, "STRING")
        end
      end
    end
    
    def function(arglist)
      var = (callback or Proc.new { |arglist| logger.trace("STRING") if logger and (var == 0) });Fi[	@iûI"           var.upload(var, var, var, &var)
        when :down then
          var.download(var, var, var, &var)
        else
          raise(ArgumentError, "STRING")
        end
      end
    end
    
    def function(arglist)
      if var.is_a?(String) then;Fi[	@àiõI"        else
          if var.respond_to?(:call) then
            var.call(self)
          else
            raise(ArgumentError, "STRING")
          end
        end
      end
      
      def function(arglist)
        @var ||= ::Fog::Storage.new(fog_credentials);Fi[@ÆI"Logger.message("STRING");FI"end;FI"end;FI"end;FI"end;Fi[[	@ˆ&i.I"’      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is dumping the database
      def function(arglist)
        Logger.message("STRING")
      end
    end
  end
end;Fi[	@˘&iI"◊      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is encrypting the archive
      def function(arglist)
        Logger.message("STRING")
      end
    end
  end
end;Fi[	@“iMI"‹      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is notifying about the process
      def function(arglist)
        Logger.message("STRING")
      end
    end
  end
end;Fi[@ÆI"Thread.current["STRING"];FI"end;FI" ;FI"def function(arglist);FI"#Thread.current["STRING"] = var;Fi[[	I"Adata//activerecord_proj/active_record/connection_handling.rb;Ti8I"∏      retrieve_connection
    end
    
    def function(arglist)
      Thread.current["STRING"]
    end
    
    def function(arglist)
      Thread.current["STRING"] = var
    end
    ;Fi[	@OixI"‘          end
        end
        
        def function(arglist)
          Thread.current["STRING"]
        end
        
        def function(arglist)
          Thread.current["STRING"] = var
        end
        ;Fi[	I"5data//activerecord_proj/active_record/scoping.rb;TiI"Œ    
    module ClassMethods
      #:nodoc:
      def function(arglist)
        Thread.current["STRING"]
      end
      
      def function(arglist)
        Thread.current["STRING"] = var
      end
    end;Fi[@ÆI"var = 0;FI"var = @var["STRING"];FI"var = var.length;FI"var = 0;FI"while (var < var) do;Fi[[	@ñi>I"‚    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then;Fi[	@ôi.I"‚    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then;Fi[	@úi-I"‚    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then;Fi[@Æ@Ø@∞@«	@ê&I"def function(arglist);Fi[[	@siaI"·          Evaluator.new(configuration, condition, var).result
        end
        
        def function(arglist)
          "STRING"
        end
      end
      
      def function(arglist)
        @var = var
        @var = [];Fi[	I"<data//cucumber_prok/cucumber/rb_support/rb_transform.rb;Ti	I"+  module RbSupport
    class RbTransform
      class MissingProc < StandardError
        def function(arglist)
          "STRING"
        end
      end
      
      def function(arglist)
        raise(MissingProc) if (var.nil? or (var.arity < 1))
        @var, @var, @var = var, Regexp.new(var), var;Fi[	I",data//rspec_proj/spec/example/errors.rb;TiI"Â    
    class NoDescriptionError < ArgumentError
      class << self
        def function(arglist)
          "STRING"
        end
      end
      
      def function(arglist)
        super(self.class.message(var, var))
      end;Fi[@T@≈@∆@«@»I":# Runs state events around the machine's :save action;Fi[[	@Ûi>I"w      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield };Fi[	@˘i7I"w      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield };Fi[	@¸i;I"w      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield };Fi[@Æ@Q&I"var = @var;FI"var = @var["STRING"];FI"/var = ((@var - @var) - @var) if (var == 0);F@%i[[	@ñiXI"˘    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length;Fi[	@ôiMI"˘    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length;Fi[	@úiGI"˘    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length;Fi[@T@≈@∆@«@JI" ;Fi[[	@ïi0I"            if var then
              super
              last_insert_id_value((var or default_sequence_name(var, var)))
            else
              super
            end
          end
        end
        
        def function(arglist)
          super.insert;Fi[	@ni]I"’        else
          if lock_strategy_enabled?(:failed_attempts) and attempts_exceeded? then
            :locked
          else
            super
          end
        end
      end
      
      protected
      ;Fi[	I"/data//paperclip_proj/paperclip/tempfile.rb;TiI"Á        end
        var = Time.now.strftime("STRING")
        var = "STRING"
      else
        super
      end
    end
  end
  
  module TempfileEncoding
    # This overrides Tempfile#binmode to make sure that the extenal encoding;Fi[@TI"
raise;FI"end;FI"end;FI"end;FI" ;Fi[[	@ÎiÌI"æ        rescue ActiveRecord::StatementInvalid => var
          if var.message.split("STRING").first =~ /Packets out of order/ then
            raise(ActiveRecord::StatementInvalid, "STRING")
          else
            raise
          end
        end
      end
      
      # MysqlAdapter has to free a result after using it, so we use this method to write
      # stuff in an abstract way without concerning ourselves about whether it needs to be;Fi[	@	iI")        rescue ActiveRecord::StatementInvalid => var
          if /database .* already exists/.===(var.message) then
            raise(DatabaseAlreadyExists)
          else
            raise
          end
        end
      end
      
      def function(arglist)
        establish_master_connection;Fi[	@J	i(I"¯        if var.message =~ /GraphViz not installed or dot not in PATH/ then
          Bundler.ui.error(var.message)
          Bundler.ui.warn("STRING")
        else
          raise
        end
      end
    end
    
    desc("STRING", "STRING")
    ;Fi[@Æ@&@	&I"end;FI"end;FI"end;Fi[[	@'i{I"|          :comment
        end
        
        def function(arglist)
          true
        end
      end
    end
  end
end;Fi[	@´i£I"Õ        # @return [ true ] true.
        #
        # @since 3.0.0
        def function(arglist)
          true
        end
      end
    end
  end
end
::Array.__send__(:include, Mongoid::Extensions::Array);Fi[	@ºi¿I"À        # @return [ true ] true.
        #
        # @since 3.0.0
        def function(arglist)
          true
        end
      end
    end
  end
end
::Hash.__send__(:include, Mongoid::Extensions::Hash);Fi[@T@Í%@Î%@Ï%@Ì%I" ;Fi[[	@iJI"∆        break if (var < var)
        if var then
          var = var.where(table[primary_key].gt(var)).to_a
        else
          raise("STRING")
        end
      end
    end
    
    private
    ;Fi[	I"4data//devise_proj/devise/models/rememberable.rb;Ti6I"
        else
          if respond_to?(:authenticatable_salt) and var = authenticatable_salt then
            var
          else
            raise("STRING")
          end
        end
      end
      
      def function(arglist)
        self.class.rememberable_options;Fi[	@iitI"µ      if var.directory? then
        ohai("STRING")
        var.children.each { |arglist| puts("STRING") }
      else
        raise("STRING")
      end
    end
  end
  
  private
  ;Fi[@Æ@‹%@›%I"end;FI"end;FI"end;Fi[[	@i4I"g        end
      end
      
      def function(arglist)
        false
      end
    end
  end
end;Fi[	@
i•I"õ      end
      
      # NOTE - not sure that we can actually cope with inverses here
      def function(arglist)
        false
      end
    end
  end
end;Fi[	@1icI"†        ((var.index(File::SEPARATOR) == 0) or (var.index("STRING") == 1))
      end
      
      def function(arglist)
        false
      end
    end
  end
end;Fi[@T@Í%@Î%@Ï%I" ;FI"def function(arglist);Fi[[	@˘iAI"Ï        specs_by_name(var)
      when Gem::Dependency then
        search_by_dependency(var, var)
      else
        raise("STRING")
      end
    end
    
    def function(arglist)
      sources.map { |arglist| var.class }.uniq
    end;Fi[	@i!I"        when Ast::ScenarioOutline then
          @var = true
          @var.scenario_outline(var.gherkin_statement)
        else
          raise("STRING")
        end
      end
      
      def function(arglist)
        @var.step(var.gherkin_statement)
        if @var then;Fi[	@¥&iI"Ù            ::I18n.t(var.first, :count => (var.last))
          end
        end
      else
        raise("STRING")
      end
    end
    
    def function(arglist)
      var = "STRING".to_sym
      ::I18n.t(var, :default => (var.to_s.humanize));Fi[@TI"[];FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@©i#I"          Dir.glob(File.join(templates_directory, "STRING")).map do |arglist|
            File.basename(var)
          end
        else
          []
        end
      end
      
      def function(arglist)
        File.join(templates_directory, var.to_s, "STRING")
      end;Fi[	I"Cdata//diaspora_proj/helpers/interim_stream_hackiness_helper.rb;Ti#I"Ì  def function(arglist)
    if defined? @var and (params[:controller] == "STRING") then
      @var.post_from_group(var)
    else
      []
    end
  end
  
  def function(arglist)
    defined? @var ? (@var.publisher.open?) : (false)
  end;Fi[	@ñiºI"¯            [:val, var[0], Integer(var[0])]
          end
        end
      else
        []
      end
    end
    
    def function(arglist)
      var = /"([^"\\]|\\["\/\\bfnrt]|\\u[0-9a-fA-F]{4})*"/.match(var)
      raise(Error, "STRING") if var.!;Fi[@TI"&if list_of_color_stops?(var) then;FI"var = (var.value + var);FI"var = nil;FI"end;FI"end;Fi[[	@+
iÙI"      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      if color_stop?(var) then;Fi[	@+
i˛I"5      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      if (var.size == 1) and list_of_color_stops?(var.first) then;Fi[	@+
iI"@      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      var = var if (var.size == 1) and var = list_of_color_stops?(var.first);Fi[@TI"clean_url(var);FI"end;FI"end;FI"end;FI" ;Fi[[	I"Adata//compass_proj/compass/sass_extensions/functions/urls.rb;Ti(I"Õ      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  
  module FontUrl
    def self.included(arglist);Fi[	@â*iAI"Œ      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  
  module ImageUrl
    def self.included(arglist);Fi[	@â*itI"ﬁ      var = "STRING" if var
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  
  module GeneratedImageUrl
    def self.included(arglist);Fi[@Æ@œ%I"@var = [];FI"end;FI" ;FI"def function(arglist);Fi[[	@ë'i$I"         replace(klass.find(var).index_by { |arglist| var.id }.values_at(*var))
      end
      
      def function(arglist)
        super
        @var = []
      end
      
      def function(arglist)
        if block_given? then
          load_target.select.each { |arglist| yield(var) };Fi[	I"Ddata//rspec_proj/spec/runner/formatter/nested_text_formatter.rb;TiI"      class NestedTextFormatter < BaseTextFormatter
        INDENT = "STRING"
        
        def function(arglist)
          super
          @var = []
        end
        
        def function(arglist)
          super
          var.nested_descriptions.each_with_index do |arglist|;Fi[	I"@data//rspec_proj/spec/runner/formatter/profile_formatter.rb;Ti
I"˜  module Runner
    module Formatter
      class ProfileFormatter < ProgressBarFormatter
        def function(arglist)
          super
          @var = []
        end
        
        def function(arglist)
          @var.puts("STRING")
        end;Fi[@TI"/render(:nothing => (true), :status => 404);FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	I"8data//diaspora_proj/controllers/posts_controller.rb;Ti2I"R    if var.present? then
      var = OEmbedPresenter.new(var, params.slice(:format, :maxheight, :minheight))
      render(:json => (var))
    else
      render(:nothing => (true), :status => 404)
    end
  end
  
  def function(arglist)
    var = Post.visible_from_author(@var.author, current_user).newer(@var)
    respond_to do |arglist|;Fi[	I":data//diaspora_proj/controllers/publics_controller.rb;Ti#I"!    @var = Person.find_by_guid_and_closed_account(params[:guid], false)
    if @var.present? and @var.local? then
      render("STRING")
    else
      render(:nothing => (true), :status => 404)
    end
  end
  
  def function(arglist)
    render("STRING", :content_type => "STRING")
  end;Fi[	@§iìI"@    var = User.find_by_username(var)
    if var.present? then
      redirect_to(var.image_url)
    else
      render(:nothing => (true), :status => 404)
    end
  end
  
  def function(arglist)
    if current_user.confirm_email(params[:token]) then
      flash[:notice] = I18n.t("STRING", :email => (current_user.email));Fi[@T@Ö)I"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@`ipI"%    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then;Fi[	@`iyI"%    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then;Fi[	@`i}I"D    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    if ["STRING", "STRING"].all? { |arglist| var[var].present? } then
      var["STRING"] = "STRING" if var["STRING"].blank?;Fi[@TI"var = var["STRING"];FI"out(("STRING" + var));FI"(if (var["STRING"] == "STRING") then;FI"out("STRING");FI";out("STRING") if var.!=("STRING") and var.!=("STRING");Fi[[	@ñiI"T      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]));Fi[	@ôiI"T      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]));Fi[	@úi˚I"T      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]));Fi[@TI"return super(var);FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@ñi9I"“  def function(arglist)
    if (@var["STRING"] == "STRING") then
      return GetMBStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"];Fi[	@ôi)I"‘  def function(arglist)
    if (@var["STRING"] == "STRING") then
      return GetSJISStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"];Fi[	@úi(I"“  def function(arglist)
    if (@var["STRING"] == "STRING") then
      return GetMBStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"];Fi[@TI"-super(var, var, var, var, var, var, var);FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@ñiSI"Ù  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var;Fi[	@ôiHI"ˆ  def function(arglist)
    if (@var["STRING"] == "STRING") then
      SJISMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var;Fi[	@úiBI"Ù  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var;Fi[@TI"<Cell(var, var, var[var, (var - var)], var, 2, var, var);FI";var = (var[var].chr == "STRING") ? ((var + 1)) : (var);FI"end;FI"var = -1;FI"var = var;Fi[[	@ñiâI"ê        if ((var == -1) or (var == var)) then
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1);Fi[	@ôiÉI"Ç        if ((var == -1) or (var == var)) then
          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1);Fi[	@úi}I"ê        if ((var == -1) or (var == var)) then
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1);Fi[@T@a%I"@var = var;F@÷"I"	else;F@ÿ"i[[	@ñiöI"Ú    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end;Fi[	@ôiïI"Ú    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end;Fi[	@úiâI"Ú    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end;Fi[@Æ@¥I"execute("STRING");FI"rescue;FI"# do nothing;FI"end;Fi[[	@ÎiˇI"¥        @var.affected_rows
      end
      
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      ;Fi[	@ÎiI"®      end
      
      #:nodoc:
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      ;Fi[	@ÎiI"®      end
      
      #:nodoc:
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      ;Fi[@TI"super(var, var, var, var);FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@ñißI"Ó  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var);Fi[	@ôi¢I"  def function(arglist)
    if (@var["STRING"] == "STRING") then
      SJISWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var);Fi[	@úiñI"Ó  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var);Fi[@TI"DCell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);FI";var = (var[var].chr == "STRING") ? ((var + 1)) : (var);FI"end;FI"var = -1;FI"var = var;Fi[[	@ñi◊I"É          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then;Fi[	@ôiﬂI"u          end
          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then;Fi[	@úi∆I"É          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then;Fi[@TI"$out(("STRING" + var["STRING"]));FI"out("STRING");FI"out("STRING");FI".out((("STRING" + (@var + 1)) + "STRING"));FI".out((("STRING" + (@var + 2)) + "STRING"));Fi[[	@ñiI"ú        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then;Fi[	@ôiI"ú        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then;Fi[	@úiI"ú        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then;Fi[@TI"out("STRING");FI"end;FI"end;FI"end;FI"out("STRING");Fi[[	@ñiI"9          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then;Fi[	@ôi$I"9          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then;Fi[	@úi
I"9          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then;Fi[@Æ@{@|@}@ƒI""STRING";Fi[[	@0iI"¶        (@var << var)
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        "STRING"
      end
      ;Fi[	@Xi:I"|    self.class.name
  end
  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    "STRING"
  end
  ;Fi[	@ÖiI"•module Paperclip
  class NilAdapter < AbstractAdapter
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      "STRING"
    end
    ;Fi[I"protected;FI" ;FI"B# Only runs validations on the action if using <tt>:save</tt>;FI"def function(arglist);FI"(action == :save);FI"end;Fi[[	@ÛiI"¯        super
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute;Fi[	@˘iI"˝        ["STRING"]
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute;Fi[	@¸iI"¯        super
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute;Fi[@@@ÍI"var = @var;F@ "I"newobj;Fi[[	@ñiÌI"©    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING");Fi[	@ôiˆI"©    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING");Fi[	@úi‹I"©    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING");Fi[@Æ@{@|@}@ƒI"
false;Fi[[	@êi;I"Ç      0
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end
  end;Fi[	@òiI"å      # do nothing
    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end
    ;Fi[	@Â	i3I"Ó      
      # The default finalize method -- it is a no-op.
      # This could print out a message or something.
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        false
      end
      ;Fi[I"var = nil;FI"	else;F@u*@v*I"var = nil;FI"end;Fi[[	@+
iÛI"    def function(arglist)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!;Fi[	@+
i˝I"      var = nil if var and var.to_bool.!
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!;Fi[	@+
iI"    def function(arglist)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!;Fi[@e@f@gI" ;FI"# :nodoc:;FI"def function(arglist);Fi[[	I"'data//rspec_proj/autotest/rspec.rb;Ti(I"€  def function(arglist)
    var.keys.inject({}) do |arglist|
      var[File.expand_path(var)] = []
      var
    end
  end
  
  # :nodoc:
  def function(arglist)
    File.exist?("STRING") ? ("STRING") : ("STRING")
  end;Fi[	I"+data//rspec_proj/spec/rake/spectask.rb;TiôI"Õ        when Proc then
          var.call
        else
          var
        end
      end
      
      # :nodoc:
      def function(arglist)
        if ENV["STRING"] then
          FileList[ENV["STRING"]];Fi[	@˚i*I"›        when Symbol then
          mock_framework_path(var.to_s)
        else
          var
        end
      end
      
      # :nodoc:
      def function(arglist)
        @var ||= mock_framework_path("STRING")
      end;Fi[@Æ@{@|@}@ƒI"var;Fi[[	@ËiI"†        # do nothing
      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var
      end
      ;Fi[	@iËI"…      
      module Fields
        class Type
          def function(arglist)
            # do nothing
          end
          
          def function(arglist)
            var
          end
        end;Fi[	@˛iI"Ì    class PostgreSQLAdapter < AbstractAdapter
      module OID
        class Type
          def function(arglist)
            # do nothing
          end
          
          def function(arglist)
            var
          end
        end;Fi[@Æ@¸@^"I"end;FI" ;FI"L# Execute the cascading deletion for the relation if it already exists.;Fi[[	@`iI"≠        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.;Fi[	@eiI"≠        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.;Fi[	@jiI"≠        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.;Fi[@e@f@g@h@iI" ;Fi[[	@Di^I"                var = ("STRING" == var[5]) ? ((var[4].to_i - 1)) : (var[4].to_i)
                (var..var)
              else
                var
              end
            end
          end
        end
        
        def function(arglist)
          if var.nil? then;Fi[	@|i@I"æ          var.options = options
          var.send(:"to_#{var}")
        else
          var
        end
      end
    end
  end
  
  def function(arglist)
    CSS2FallbackValue.new(var, var);Fi[	@æi0I"ı          if var.all? { |arglist| (var == "STRING") } then
            escape_double_quotes(var)
          else
            var
          end
        end
      end
    end
    
    def function(arglist)
      var.gsub(/'/) { |arglist| "STRING" };Fi[@Æ@ÌI"reconnect_ssl;FI"end;FI" ;FI"##;Fi[[	I"=data//bundler_proj/bundler/vendor/net/http/persistent.rb;TiI"Â  # For Net::HTTP parity
  ##
  # Sets the SSL certificate authority file.
  def function(arglist)
    @var = var
    reconnect_ssl
  end
  
  ##
  # Overrides the default SSL certificate store used for verifying
  # connections.;Fi[	@ù+iI",  ##
  # Overrides the default SSL certificate store used for verifying
  # connections.
  def function(arglist)
    @var = var
    reconnect_ssl
  end
  
  ##
  # Finishes all connections on the given +thread+ that were created before
  # the given +generation+ in the threads +generation_key+ list.;Fi[	@ù+i‚I"A  # Setting this to VERIFY_NONE is a VERY BAD IDEA and should NEVER be used.
  # Securely transfer the correct certificate and update the default
  # certificate store or set the ca file instead.
  def function(arglist)
    @var = var
    reconnect_ssl
  end
  
  ##
  # SSL verification callback.
  def function(arglist);Fi[@Æ@Ì@`@aI"private;FI" ;Fi[[	@»
iBI"à      end
    end
    
    def function(arglist)
      @var = var
    end
    
    private
    
    def function(arglist)
      "STRING";Fi[	@Ii'I"=    # Because Rubyforge cannot be trusted to provide valid specifications
    # once the remote gem is downloaded, the backend specification will
    # be swapped out.
    def function(arglist)
      @var = var
    end
    
    private
    
    def function(arglist)
      @var ||= @var.fetch_spec([@var, @var, @var]);Fi[	@Êi!I"¿    # @return [ Logger ] The new logger.
    #
    # @since 3.0.0
    def function(arglist)
      @var = var
    end
    
    private
    
    # Gets the default Mongoid logger - stdout.
    #;Fi[@ÆI"raise("STRING");FI"end;FI"end;FI"end;FI"end;Fi[[	I"Odata//activemodel_proj/active_model/deprecated_mass_assignment_security.rb;TiI"}        raise("STRING")
      end
      
      def function(arglist)
        raise("STRING")
      end
    end
  end
end;Fi[	@πi>I"Ç          end
        end
        
        def function(arglist)
          raise("STRING")
        end
      end
    end
  end
end;Fi[	I">data//rspec_proj/spec/runner/heckle_runner_unsupported.rb;TiI"Çmodule Spec
  module Runner
    class HeckleRunner
      def function(arglist)
        raise("STRING")
      end
    end
  end
end;Fi[@P@QI"A# @param [ Array ] args The names of the fields to validate.;FI"#;FI"# @since 2.4.0;FI"def function(arglist);Fi[[	I"2data//mongoid_proj/lib/mongoid/validations.rb;TiéI"S      #     field :title
      #
      #     validates_format_of :title, with: /^[a-z0-9 \-_]*$/i
      #   end
      #
      # @param [ Array ] args The names of the fields to validate.
      #
      # @since 2.4.0
      def function(arglist)
        validates_with(Mongoid::Validations::FormatValidator, _merge_attributes(var))
      end;Fi[	@√+iüI"G      #     field :title
      #
      #     validates_length_of :title, minimum: 100
      #   end
      #
      # @param [ Array ] args The names of the fields to validate.
      #
      # @since 2.4.0
      def function(arglist)
        validates_with(Mongoid::Validations::LengthValidator, _merge_attributes(var))
      end;Fi[	@√+i∞I"'      #     field :title
      #
      #     validates_presence_of :title
      #   end
      #
      # @param [ Array ] args The names of the fields to validate.
      #
      # @since 2.4.0
      def function(arglist)
        validates_with(PresenceValidator, _merge_attributes(var))
      end;Fi[@e@f@˛@:I" var = [var.first, var.last];FI"var;Fi[[	@<
i’I"·      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      ;Fi[	@<
i⁄I"¿      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      ;Fi[	@<
iﬂI"¿      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      ;Fi[@e@f@˛@:I"%var = Node.new(:COMBINATOR, var);FI"var;Fi[[	@<
i*I"›      # reduce 50 omitted
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      ;Fi[	@<
i/I"       
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      ;Fi[	@<
i4I"       
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      ;Fi[@e@f@˛I"J# Turn the object from the ruby type we deal with to a Mongo friendly;FI"# type.;FI"#;Fi[[	@ºikI"
          var = (var[var] or var[var.to_i])
          var = var
        end
        var
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize;Fi[	@øi¯I"        #
        # @since 3.0.0
        def function(arglist)
          var
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Object.mongoize("123.11");Fi[	@—i3I"[          if Mongoid::Config.use_activesupport_time_zone? then
            var = var.in_time_zone(Mongoid.time_zone)
          end
          var
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Time.mongoize("2012-1-1");Fi[@P@QI"8# @return [ Object ] The return value of the block.;FI"#;FI"# @since 2.1.0;FI"def function(arglist);Fi[[	@6i2I""      # @example Execute in binding mode.
      #   binding do
      #     relation.push(doc)
      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING");Fi[	@6iQI"%      # @example Execute in building mode.
      #   _building do
      #     relation.push(doc)
      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING");Fi[	@6i™I":        # @example Execute in creating mode.
        #   creating do
        #     relation.push(doc)
        #   end
        #
        # @return [ Object ] The return value of the block.
        #
        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING");Fi[@@@I" ;F@*I"def function(arglist);Fi[[	@Ûi?I"e        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end;Fi[	@˘i8I"e        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end;Fi[	@¸i<I"e        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end;Fi[@P@QI"-# @return [ Enumerator ] The enumerator.;FI"#;FI"# @since 3.0.0;FI"def function(arglist);Fi[[	@yi"I"4      # @example Iterate over the results.
      #   map_reduce.each do |doc|
      #     p doc
      #   end
      #
      # @return [ Enumerator ] The enumerator.
      #
      # @since 3.0.0
      def function(arglist)
        block_given? ? (documents.each { |arglist| yield(var) }) : (to_enum)
      end;Fi[	@›iaI"^      # @example Iterate over the context.
      #   context.each do |doc|
      #     puts doc.name
      #   end
      #
      # @return [ Enumerator ] The enumerator.
      #
      # @since 3.0.0
      def function(arglist)
        if block_given? then
          (documents[(skipping or 0), (limiting or documents.length)] or []).each do |arglist|;Fi[	I"7data//mongoid_proj/lib/mongoid/contextual/mongo.rb;TiqI"       # @example Iterate over the context.
      #   context.each do |doc|
      #     puts doc.name
      #   end
      #
      # @return [ Enumerator ] The enumerator.
      #
      # @since 3.0.0
      def function(arglist)
        if block_given? then
          selecting do |arglist|;Fi[I"#   class Person;FI"+#     include ActiveModel::Validations;FI"#;FI"#     attr_accessor :name;FI"&#     validates_presence_of :name;FI"#   end;Fi[[	I"7data//activemodel_proj/active_model/validations.rb;Ti∫I"     # Returns the +Errors+ object that holds all information about attribute
    # error messages.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new;Fi[	@,iÀI"*    # Runs all the specified validations and returns +true+ if no errors were
    # added otherwise +false+.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new;Fi[	@,iÚI"+    # Performs the opposite of <tt>valid?</tt>. Returns +true+ if errors were
    # added, +false+ otherwise.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new;Fi[@#$I"	else;FI"
false;TI"end;TI"end;TI" ;Fi[[	@)iåI"Ó    def function(arglist)
      case peek
      when LONG_RE, SHORT_RE, EQ_RE, SHORT_NUM, SHORT_SQ_RE then
        true
      else
        false
      end
    end
    
    def function(arglist)
      peek and (parsing_options?.! or super);Fi[	@ñ'iMI"œ    end
    if self.profile.update_attributes(var) then
      Postzord::Dispatcher.build(self, profile).post
      true
    else
      false
    end
  end
  
  def self.build(arglist)
    var = User.new(var);Fi[	@üiËI"œ  
  def function(arglist)
    if ((@var + var) > @var) and (@var.! and self.AcceptPageBreak) then
      true
    else
      false
    end
  end
  
  def function(arglist)
    if self.BreakThePage?(var) then;Fi[@^I"K# @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline;FI"<# @note This method can only return up to 3,200 Tweets.;FI"# @rate_limited Yes;FI",# @authentication Requires user context;FI"g# @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[[	@O'i'I"      (alias :mentions :mentions_timeline)
      
      # Returns the 20 most recent Tweets posted by the specified user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @overload user_timeline(user, options={});Fi[	@O'i?I"%      end
      
      # Returns the 20 most recent retweets posted by the specified user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[	@O'iWI",      (alias :retweeted_by :retweeted_by_user)
      
      # Returns the 20 most recent retweets posted by the authenticating user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.;Fi[@^@4$@5$I"S@var = Array(@var[:sources]).map { |arglist| Twitter::User.fetch_or_new(var) };FI"end;FI" ;Fi[[	@;$iI"f      attr_reader(:max_position, :min_position, :target_objects)
      
      # A collection of users who followed a user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:sources]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
      
      # A collection containing the followed user
      #;Fi[	@>$iI"[      attr_reader(:max_position, :min_position)
      
      # A collection of users who added a user to a list
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:sources]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
      
      # A collection of lists that were added to
      #;Fi[	@A$iI"J      attr_reader(:max_position, :min_position)
      
      # A collection of users who mentioned a user
      #
      # @return [Array<Twitter::User>]
      def function(arglist)
        @var = Array(@var[:sources]).map { |arglist| Twitter::User.fetch_or_new(var) }
      end
      
      # The user who mentioned a user
      #;Fi[@^@G'I"def function(arglist);FI"9@var = Array(@var[:target_objects]).map do |arglist|;F@y(I"end;Fi[[	@A$iI"      end
      
      # A collection of tweets that mention a user
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      ;Fi[	@Å(i
I"=  module Action
    class Reply < Twitter::Action::Tweet
      # A collection of tweets that reply to a user
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      ;Fi[	@D$i
I",  module Action
    class Retweet < Twitter::Action::Tweet
      # A collection of retweets
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      ;Fi[@$@$I"end;FI"end;TI"end;FI"end;Fi[[	@·iåI"          (require("diff/lcs")
          true)
        rescue LoadError
          false
        end
      end
    end
  end
end;Fi[	@‰iI"          (require("diff/lcs")
          true)
        rescue LoadError
          false
        end
      end
    end
  end
end;Fi[	I"5data//rspec_proj/spec/runner/drb_command_line.rb;TiI"í          true)
        rescue DRb::DRbConnError
          var.error_stream.puts("STRING")
          false
        end
      end
    end
  end
end;Fi[@^I"#     property :id, Serial;FI"##     property :state, :String;FI"#;F@$@$i[[	@®iI"O        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end;Fi[	@®iTI"O        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end;Fi[	@®ixI"O        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end;Fi[@^I"#   class Vehicle;FI"7#     state_machine :state, :initial => :parked do;FI"#       ...;FI"#     end;FI"#;Fi[[	@$iÂI"D    # the default behavior of initializing the state machine attributes is used.
    # 
    # In the following example, a custom +initialize+ method is defined:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     
    #     def initialize
    #     end;Fi[	@$iˆI"b    # never get initialized.  In order to ensure that all initialization hooks
    # are called, the custom method *must* call +super+ without any arguments
    # like so:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     
    #     def initialize(attributes = {})
    #       ...;Fi[	@$iI"?    # 
    # If you want to avoid calling the superclass's constructor, but still want
    # to initialize the state machine attributes:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     
    #     def initialize(attributes = {})
    #       ...;Fi[@^@c,I"'#     include DataMapper::Resource;FI"#;F@X,@Y,i[[	@®iI"T        # information about the various configuration options available.
        # 
        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do;Fi[	@®iQI"E        # as the given requirements match the transition.
        # 
        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do;Fi[	@®iuI"Y        # be performed so long as the given requirements match the transition.
        # 
        # == Example
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do;Fi[@^@c,@†I"!#       other_states :idling;FI"#     end;FI"#   end;Fi[[	I"?data//state_machine_proj/state_machine/state_collection.rb;TiI"Ï    # will be raised.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states;Fi[	@,i(I"    # the value of the attribute on the object.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states;Fi[	@,iCI"‰    # raised.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states;Fi[@<@=@[@\@g!I"#   person.pets;Ti[[	@&ièI"H      # This will _always_ remove the records ignoring the +:dependent+
      # option.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&iI"s      # You can pass +Fixnum+ or +String+ values, it finds the records
      # responding to the +id+ and executes delete on them.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&iI"á      # This method will _always_ remove record from the database ignoring
      # the +:dependent+ option. Returns an array with the removed records.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[@^@c,@†I"#       event :ignite do;FI",#         transition :parked => :idling;FI"#       end;Fi[[	I"9data//state_machine_proj/state_machine/extensions.rb;Ti?I"     # transitions will continue to run their own callbacks.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do;Fi[	@î,iqI".    # See StateMachine::InstanceMethods#fire_events for more information.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do;Fi[	@ài˚I"    # will revert the state back to the +from+ value.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end;Fi[@)@˜*I"rescue;FI"# do nothing;FI"end;FI"end;Fi[[	@Îi I"µ      end
      
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      
      def function(arglist);Fi[	@ÎiI"≠      
      #:nodoc:
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      
      #:nodoc:;Fi[	@ÎiI"∫      
      #:nodoc:
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      
      def function(arglist);Fi[@^I"!# Valid requirement options:;FI"N# * <tt>:from</tt> - One or more states being transitioned from.  If none;FI"E#   are specified, then this will be the object's current state.;FI"N# * <tt>:to</tt> - One or more states being transitioned to.  If none are;FI"6#   specified, then this will match any to state.;Fi[[	I"4data//state_machine_proj/state_machine/event.rb;TiI"a    
    # Finds and builds the next transition that can be performed on the given
    # object.  If no transitions can be made, then this will return nil.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.;Fi[	@,iI"    end
    
    # Gets the list of events that can be fired on the given object.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.;Fi[	@,i1I"    end
    
    # Gets the list of transitions that can be run on the given object.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.;Fi[@)@¨(@≠(@Æ(@Ø(I"
retry;Fi[[	@≤(iI"_        else
          var = Iconv.new("STRING", "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success);Fi[	@≤(i>I"Z        else
          var = Iconv.new("STRING", var)
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success);Fi[	@≤(i|I"Z        else
          var = Iconv.new(var, "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success);Fi[@^I"# == Example;FI"#;FI"#   class Vehicle;FI"/#     state_machine :initial => :parked do;F@è,i[[	@î,i=I"ä    # On the other hand, any <tt>:halt</tt> error that's thrown within an
    # *after* callback with only affect that event's transition.  Other
    # transitions will continue to run their own callbacks.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@î,ioI"O    # a StateMachine::InvalidTransition exception will be raised.
    # 
    # See StateMachine::InstanceMethods#fire_events for more information.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@ài˘I"F    
    # Rolls back changes made to the object's state via this transition.  This
    # will revert the state back to the +from+ value.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[@@@@ƒI"var;FI"end;Fi[[	@ËiI"ß      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var
      end
      
      def function(arglist);Fi[	@iÈI"À      module Fields
        class Type
          def function(arglist)
            # do nothing
          end
          
          def function(arglist)
            var
          end
        end
        ;Fi[	@˛iI"»      module OID
        class Type
          def function(arglist)
            # do nothing
          end
          
          def function(arglist)
            var
          end
        end
        ;Fi[@@@@ƒI"
false;FI"end;Fi[[	@êi<I"~    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end
  end
end;Fi[	@òiI"é    end
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end
    
    class Shell < UI;Fi[	@Â	i4I"      # The default finalize method -- it is a no-op.
      # This could print out a message or something.
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        false
      end
      
      def function(arglist);Fi[@@@@ƒI""STRING";FI"end;Fi[[	@0iI"¨      end
      
      def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist);Fi[	@Xi;I"Ä  end
  
  def function(arglist)
    # do nothing
  end
  
  def function(arglist)
    "STRING"
  end
  
  def function(arglist);Fi[	@ÖiI"ømodule Paperclip
  class NilAdapter < AbstractAdapter
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      "STRING"
    end
    
    def function(arglist);Fi[@^@È+I"#;FI"# @since 2.1.0;F@Ï+I"
begin;Fi[[	@6i3I"	      #   binding do
      #     relation.push(doc)
      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield);Fi[	@6iRI"      #   _building do
      #     relation.push(doc)
      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield);Fi[	@6i´I"         #   creating do
        #     relation.push(doc)
        #   end
        #
        # @return [ Object ] The return value of the block.
        #
        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield);Fi[@@@¨@≠I" ;FI"def function(arglist);Fi[[	@x'ihI"˙      when "STRING", "STRING" then
        problem("STRING".undent)
      else
        # do nothing
      end
    end
  end
  
  def function(arglist)
    f.conflicts.each do |arglist|
      var = Formula.factory(var.formula) rescue problem("STRING");Fi[	@x'i∞I"      when /macports\/trunk/ then
        problem("STRING")
      else
        # do nothing
      end
    end
  end
  
  def function(arglist)
    if text =~ /<(Formula|AmazonWebServicesFormula|ScriptFileFormula|GithubGistFormula)/ then
      problem("STRING");Fi[	@üiªI"¡      when "STRING" then
        :p7zip
      else
        # do nothing
      end
    end
  end
  
  def function(arglist)
    /^#!\s*\S+/.===(open("STRING") { |arglist| var.read(1024) })
  end;Fi[@^I"#   class Game;FI"$#     include Mongoid::Document;FI"#     belongs_to :person;FI"#   end;FI"#;Fi[[	@‡i|I"         # another database or collection.
        #
        # @example Define the relation.
        #
        #   class Game
        #     include Mongoid::Document
        #     belongs_to :person
        #   end
        #
        #   class Person
        #     include Mongoid::Document;Fi[	@‡iïI"L        #     include Mongoid::Document
        #     has_many :posts
        #   end
        #
        #   class Game
        #     include Mongoid::Document
        #     belongs_to :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.;Fi[	@‡i I"         # another database or collection.
        #
        # @example Define the relation.
        #
        #   class Game
        #     include Mongoid::Document
        #     belongs_to :person
        #   end
        #
        #   class Person
        #     include Mongoid::Document;Fi[@@I"	else;FI""STRING";FI"end;FI"end;Fi[[	@I
iI"˚          when :pdf then
            pdf_task(var, var, :label => (var), :markers => (true), :height => 0.8)
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      
      def function(arglist);Fi[	@I
i2I"˚          when :pdf then
            pdf_task(var, var, :label => (var), :markers => (true), :height => 0.8)
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      
      def function(arglist);Fi[	@I
imI"Ë          when :pdf then
            pdf_task(var, var, :label => (var))
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      
      if Object.const_defined?(:Magick) then;Fi[@^I"N# @option options [ true, false ] :continue Continue binding the inverse.;FI"E# @option options [ true, false ] :binding Are we in build mode?;FI"#;FI"# @since 2.0.0.rc.1;FI"def function(arglist);Fi[[	@iI"÷          #   name.person = Person.new
          #
          # @param [ Hash ] options The binding options.
          #
          # @option options [ true, false ] :continue Continue binding the inverse.
          # @option options [ true, false ] :binding Are we in build mode?
          #
          # @since 2.0.0.rc.1
          def function(arglist)
            base.metadata = metadata.inverse_metadata(target) unless base.metadata
            base.parentize(target);Fi[	@#iI"ª          #
          # @param [ Document ] doc The single document to bind.
          # @param [ Hash ] options The binding options.
          #
          # @option options [ true, false ] :continue Continue binding the inverse.
          # @option options [ true, false ] :binding Are we in build mode?
          #
          # @since 2.0.0.rc.1
          def function(arglist)
            var.parentize(base)
            binding do |arglist|;Fi[	@#i&I"‘          #   person.addresses.unbind_one(document)
          #
          # @param [ Hash ] options The binding options.
          #
          # @option options [ true, false ] :continue Continue binding the inverse.
          # @option options [ true, false ] :binding Are we in build mode?
          #
          # @since 2.0.0.rc.1
          def function(arglist)
            binding { |arglist| var.do_or_do_not(metadata.inverse_setter(target), nil) }
          end;Fi[@^I"0# @return [ Class ] The class being set up.;FI"#;FI"# @since 2.0.0.rc.1;FI"def function(arglist);FI",re_define_method("STRING") do |arglist|;Fi[[	@1iŸI"≠        #
        # @param [ String, Symbol ] name The name of the relation.
        # @param [ Metadata ] metadata The metadata for the relation.
        #
        # @return [ Class ] The class being set up.
        #
        # @since 2.0.0.rc.1
        def function(arglist)
          re_define_method("STRING") do |arglist|
            without_autobuild do |arglist|
              if (var.many? or get_relation(var, var)) then;Fi[	@$i.I"Ä        #   Person.builder("name")
        #
        # @param [ String, Symbol ] name The name of the relation.
        #
        # @return [ Class ] The class being set up.
        #
        # @since 2.0.0.rc.1
        def function(arglist)
          re_define_method("STRING") do |arglist|
            var, var = parse_args(*var)
            var = Factory.build(var.klass, var, var);Fi[	@$iGI"Ä        #   Person.creator("name")
        #
        # @param [ String, Symbol ] name The name of the relation.
        #
        # @return [ Class ] The class being set up.
        #
        # @since 2.0.0.rc.1
        def function(arglist)
          re_define_method("STRING") do |arglist|
            var, var = parse_args(*var)
            var = Factory.build(var.klass, var, var);Fi[@^I".# @param [ Class ] klass The model class.;FI"4# @param [ Hash ] spec The index specification.;FI"1# @param [ Hash ] options The index options.;FI"#;FI"<# @raise [ Errors::InvalidIndex ] If validation failed.;Fi[[	@ŸiI"u        #
        # @example Validate the index spec.
        #   Options.validate(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0;Fi[	@Ÿi(I"z        #
        # @example Validate the options.
        #   Options.validate_options(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0;Fi[	@Ÿi>I"t        #
        # @example Validate the spec.
        #   Options.validate_spec(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0;Fi[@^I"5# @param [ String, Symbol ] name The config key.;FI"0# @param [ Hash ] config The configuration.;FI"#;FI"# @since 3.0.0;FI"def function(arglist);Fi[[	I"@data//mongoid_proj/lib/mongoid/config/validators/session.rb;Ti&I"î        #
        # @example Validate the session has database.
        #   validator.validate_session_database(:default, {})
        #
        # @param [ String, Symbol ] name The config key.
        # @param [ Hash ] config The configuration.
        #
        # @since 3.0.0
        def function(arglist)
          raise(Errors::NoSessionDatabase.new(var, var)) if no_database_or_uri?(var)
        end;Fi[	@D-i5I"à        #
        # @example Validate the session has hosts.
        #   validator.validate_session_hosts(:default, {})
        #
        # @param [ String, Symbol ] name The config key.
        # @param [ Hash ] config The configuration.
        #
        # @since 3.0.0
        def function(arglist)
          raise(Errors::NoSessionHosts.new(var, var)) if no_hosts_or_uri?(var)
        end;Fi[	@D-iEI"ö        #
        # @example Validate the uri and options.
        #   validator.validate_session_uri(:default, {})
        #
        # @param [ String, Symbol ] name The config key.
        # @param [ Hash ] config The configuration.
        #
        # @since 3.0.0
        def function(arglist)
          if both_uri_and_standard?(var) then
            raise(Errors::MixedSessionConfiguration.new(var, var));Fi[@^I"# @since 2.0.0.rc.1;FI"def function(arglist);FI"@var ||= "STRING";FI"end;FI" ;Fi[[	@€i*I"      #   metadata.foreign_key_setter
      #
      # @return [ String ] The foreign_key plus =.
      #
      # @since 2.0.0.rc.1
      def function(arglist)
        @var ||= "STRING"
      end
      
      # Returns the index option of the relation.
      #;Fi[	@€i∫I";      #   metadata.setter # => "person="
      #
      # @return [ String ] The name plus "=".
      #
      # @since 2.0.0.rc.1
      def function(arglist)
        @var ||= "STRING"
      end
      
      # Returns the name of the field in which to store the name of the class
      # for the polymorphic relation.;Fi[	@€iEI"      #   metadata.classify
      #
      # @return [ String ] The classified name.
      #
      # @since 2.0.0.rc.1
      def function(arglist)
        @var ||= "STRING"
      end
      
      # Get the name for the inverse field.
      #;Fi[@^I"# @since 2.2.0;FI"def function(arglist);FI"@var ||= {};FI"end;FI" ;Fi[[	I"7data//mongoid_proj/lib/mongoid/atomic/modifiers.rb;Ti’I"¸      #   modifiers.pull_fields
      #
      # @return [ Array<String> ] The pull fields.
      #
      # @since 2.2.0
      def function(arglist)
        @var ||= {}
      end
      
      # Get the names of the fields that need to be pushed.
      #;Fi[	@_-i·I"˘      #   modifiers.push_fields
      #
      # @return [ Array<String> ] The push fields.
      #
      # @since 2.2.0
      def function(arglist)
        @var ||= {}
      end
      
      # Get the names of the fields that need to be set.
      #;Fi[	@_-iÌI"˙      #   modifiers.set_fields
      #
      # @return [ Array<String> ] The set fields.
      #
      # @since 2.2.0
      def function(arglist)
        @var ||= {}
      end
      
      # Get the $pullAll operations or intialize a new one.
      #;Fi[@^@X-@Y-I"conflicts["STRING"] ||= {};FI"end;FI" ;Fi[[	@_-i•I"      #   modifiers.conflicting_pulls
      #
      # @return [ Hash ] The conflicting pull operations.
      #
      # @since 2.2.0
      def function(arglist)
        conflicts["STRING"] ||= {}
      end
      
      # Get the conflicting push modifications.
      #;Fi[	@_-i±I"      #   modifiers.conflicting_pushs
      #
      # @return [ Hash ] The conflicting push operations.
      #
      # @since 2.2.0
      def function(arglist)
        conflicts["STRING"] ||= {}
      end
      
      # Get the conflicting set modifications.
      #;Fi[	@_-iΩI"$      #   modifiers.conflicting_sets
      #
      # @return [ Hash ] The conflicting set operations.
      #
      # @since 2.2.0
      def function(arglist)
        conflicts["STRING"] ||= {}
      end
      
      # Get the push operations that would have conflicted with the sets.
      #;Fi[@ÄI"Ñvar = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"));FI"end;FI"out((var + "STRING"));FI"out("STRING");FI"end;Fi[[	@ñi/I"v            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end;Fi[	@ôi8I"v            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end;Fi[	@úiI"v            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end;Fi[I"var = [];FI"5var = ((var % var) == 0) ? (var) : ((var + var));FI"2var.step(var, var) { |arglist| (var << var) };FI"return var;FI"end;FI" ;Fi[[	@Ÿ(i!I"c        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist)
        (var / 2.0);Fi[	@‹(iI"e        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist)
        @var[:fields];Fi[	@ziYI"r        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist)
        var = { :x => 0, :y => 0 };Fi[@^@Æ"@Ø"I"
begin;FI"((Threaded.begin_execution("STRING");FI"yield);Fi[[	@6i5I"      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING");Fi[	@6iTI"      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING");Fi[	@6i≠I"&        #   end
        #
        # @return [ Object ] The return value of the block.
        #
        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield)
          ensure
            Threaded.exit_execution("STRING");Fi[@^@w@xI"
false;FI"end;FI" ;Fi[[	@øiéI"∆      #   object.multi_arged?
      #
      # @return [ false ] false.
      #
      # @since 3.0.0
      def function(arglist)
        false
      end
      
      # Is the object a number?
      #;Fi[	@øiöI"Â      #   object.numeric?
      #
      # @return [ false ] Always false.
      #
      # @since 3.0.0
      def function(arglist)
        false
      end
      
      # Remove the instance variable for the provided name.
      #;Fi[	@øiπI"ÿ      #   object.resizable?
      #
      # @return [ false ] false.
      #
      # @since 3.0.0
      def function(arglist)
        false
      end
      
      # Get the substitutable version of an object.
      #;Fi[@I"3# Creates a new instance of the storage object;FI"def function(arglist);FI"super(var, var);FI"@var ||= 22;FI"@var ||= "STRING";Fi[[	@—	iI".      # Path to store backups to
      ##
      # Flag to use local backups
      ##
      # Creates a new instance of the storage object
      def function(arglist)
        super(var, var)
        @var ||= 22
        @var ||= "STRING"
        @var ||= false
        instance_eval(&var) if block_given?;Fi[	@ﬁiI"G      # Server IP Address and SCP port
      ##
      # Path to store backups to
      ##
      # Creates a new instance of the storage object
      def function(arglist)
        super(var, var)
        @var ||= 22
        @var ||= "STRING"
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING");Fi[	@ZiI"H      # Server IP Address and SFTP port
      ##
      # Path to store backups to
      ##
      # Creates a new instance of the storage object
      def function(arglist)
        super(var, var)
        @var ||= 22
        @var ||= "STRING"
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING");Fi[@^@w@xI"6super(compose_message("STRING", :klass => (var)));FI"end;FI"end;Fi[[	@•'iI"Î      #   NoMetadata.new(Address)
      #
      # @param [ Class ] klass The document class.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[	@®'iI"˜      #   NoParent.new(klass)
      #
      # @param [ Class ] klass The class of the embedded document.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[	@´'iI"Ù      #   VersioningNotOnRoot.new(Address)
      #
      # @param [ Class ] klass The embedded class.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[I"+#     include ActiveModel::Validations;FI"#;F@,@,I"#   end;FI"#;Fi[[	@,iªI"Ù    # error messages.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new
    #   person.valid? # => false;Fi[	@,iÃI"ı    # added otherwise +false+.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new
    #   person.name = '';Fi[	@,iÛI"ˆ    # added, +false+ otherwise.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new
    #   person.name = '';Fi[I"#       end;FI"#;FI"#       event :park do;FI",#         transition :idling => :parked;FI"#       end;FI"#     end;Fi[[	@î,iDI"T    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #     
    #     state_machine :alarm_state, :namespace => 'alarm', :initial => :on do;Fi[	@î,ivI"X    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #     
    #     state_machine :alarm_state, :namespace => 'alarm', :initial => :active do;Fi[	@$i°I"    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #   end
    #   ;Fi[@¸@^"I"end;FI" ;F@Ö+I"I# This should be optimized in the future potentially not to load all;Fi[[	@`iI"w        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #;Fi[	@eiI"w        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #;Fi[	@jiI"w        #
        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #;Fi[@^@w@x@2)I"end;FI"end;Fi[[	@9)iI"       #
      # @param [ Symbol ] name The name of the session config.
      # @param [ Hash ] config The configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[	@<)iI"       #
      # @param [ Symbol, String ] name The db config key.
      # @param [ Hash ] config The hash configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[	@?)iI"       #
      # @param [ Symbol, String ] name The db config key.
      # @param [ Hash ] config The hash configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[I"nil;FI"end;FI"end;FI"end;FI"end;FI"end;Fi[[	@«i*I"˛          if engine_version.!=(var.engine_version) and @var then
            [:engine_version, engine_version, var.engine_version]
          else
            nil
          end
        end
      end
    end
  end
  
  class SystemRubyVersion < RubyVersion;Fi[	@&iI"á            Gherkin::Formatter::Argument.new(var, var)
          end
        else
          nil
        end
      end
    end
  end
end;Fi[	@Œi≤I"e            nil
          end
        else
          nil
        end
      end
    end
  end
end;Fi[I"O# * <tt>:if</tt> - Specifies a method, proc or string to call to determine;FI"M#   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,;FI"O#   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,;FI"O#   proc or string should return or evaluate to a +true+ or +false+ value.;FI"I# * <tt>:unless</tt> - Specifies a method, proc or string to call to;FI"^#   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,;Fi[[	@,i)I"m      #   (e.g. <tt>on: :create</tt> or <tt>on: :custom_validation_context</tt>)
      # * <tt>:allow_nil</tt> - Skip validation if attribute is +nil+.
      # * <tt>:allow_blank</tt> - Skip validation if attribute is blank.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+;Fi[	@,icI"m      #   (e.g. <tt>on: :create</tt> or <tt>on: :custom_validation_context</tt>)
      # * <tt>:allow_nil</tt> - Skip validation if attribute is +nil+.
      # * <tt>:allow_blank</tt> - Skip validation if attribute is blank.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+;Fi[	I"Ddata//activerecord_proj/active_record/validations/associated.rb;Ti)I"P      # * <tt>:on</tt> - Specifies when this validation is active. Runs in all
      #   validation contexts by default (+nil+), other options are <tt>:create</tt>
      #   and <tt>:update</tt>.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+;Fi[@¯-@˘-@˙-@˚-@¸-I"L#   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The;Fi[[	@,i*I"-      # * <tt>:allow_nil</tt> - Skip validation if attribute is +nil+.
      # * <tt>:allow_blank</tt> - Skip validation if attribute is blank.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.;Fi[	@,idI"-      # * <tt>:allow_nil</tt> - Skip validation if attribute is +nil+.
      # * <tt>:allow_blank</tt> - Skip validation if attribute is blank.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.;Fi[	@.i*I"      #   validation contexts by default (+nil+), other options are <tt>:create</tt>
      #   and <tt>:update</tt>.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.;Fi[@˘-@˙-@˚-@¸-@.I"P#   method, proc or string should return or evaluate to a +true+ or +false+;Fi[[	@,i+I"      # * <tt>:allow_blank</tt> - Skip validation if attribute is blank.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist);Fi[	@,ieI"      # * <tt>:allow_blank</tt> - Skip validation if attribute is blank.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist);Fi[	@.i+I"Ÿ      #   and <tt>:update</tt>.
      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist);Fi[@˙-@˚-@¸-@.@.I"#   value.;Fi[[	@,i,I"˛      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist)
        validates_with(BlockValidator, _merge_attributes(var), &var);Fi[	@,ifI"‹      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist)
        var = var.extract_options!;Fi[	@.i,I"˝      # * <tt>:if</tt> - Specifies a method, proc or string to call to determine
      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist)
        validates_with(AssociatedValidator, _merge_attributes(var));Fi[I"I# * <tt>:unless</tt> - Specifies a method, proc or string to call to;F@¸-@.@.I"#   value.;FI"def function(arglist);Fi[[	@,i-I"∑      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist)
        validates_with(BlockValidator, _merge_attributes(var), &var)
      end;Fi[	@,igI"©      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist)
        var = var.extract_options!
        if var.key?(:on) then;Fi[	@.i-I"∂      #   if the validation should occur (e.g. <tt>if: :allow_validation</tt>,
      #   or <tt>if: Proc.new { |user| user.signup_step > 2 }</tt>). The method,
      #   proc or string should return or evaluate to a +true+ or +false+ value.
      # * <tt>:unless</tt> - Specifies a method, proc or string to call to
      #   determine if the validation should not occur (e.g. <tt>unless: :skip_validation</tt>,
      #   or <tt>unless: Proc.new { |user| user.signup_step <= 2 }</tt>). The
      #   method, proc or string should return or evaluate to a +true+ or +false+
      #   value.
      def function(arglist)
        validates_with(AssociatedValidator, _merge_attributes(var))
      end;Fi[I"(var << var);FI"end;FI"end;FI"(var << "STRING");FI"var.run;FI"if var.success? then;Fi[[	@ƒiLI"      if @var.compressor then
        @var.compressor.compress_with do |arglist|
          (var << var)
          (var << var)
        end
      end
      (var << "STRING")
      var.run
      if var.success? then
        Logger.message("STRING")
      else;Fi[	@ i>I"        if @var.compressor then
          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else;Fi[	I"4data//backup_proj/backup/database/postgresql.rb;Ti>I"        if @var.compressor then
          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else;Fi[@,.I"(var << var);FI"end;FI"end;FI"(var << "STRING");FI"var.run;Fi[[	@ƒiKI"      (var << ("STRING" + "STRING"))
      if @var.compressor then
        @var.compressor.compress_with do |arglist|
          (var << var)
          (var << var)
        end
      end
      (var << "STRING")
      var.run
      if var.success? then
        Logger.message("STRING");Fi[	@ i=I"%        (var << mysqldump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING");Fi[	@8.i=I""        (var << pgdump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING");Fi[@^@w@xI"@var.!.!;FI"end;FI" ;Fi[[	@	,i!I"ˆ      #   context.cached?
      #
      # @return [ true, false ] If the context is cached.
      #
      # @since 3.0.0
      def function(arglist)
        @var.!.!
      end
      
      # Get the number of documents matching the query.
      #;Fi[	@	,iÒI"      #   context.cache_loaded?
      #
      # @return [ true, false ] If the cache is loaded.
      #
      # @since 3.0.0
      def function(arglist)
        @var.!.!
      end
      
      # Get the documents for cached queries.
      #;Fi[	@èiqI"    #   criteria.embedded?
    #
    # @return [ true, false ] If the criteria is embedded.
    #
    # @since 3.0.0
    def function(arglist)
      @var.!.!
    end
    
    # Extract a single id from the provided criteria. Could be in an $and
    # query or a straight _id query.;Fi[@@@ @!@"I"def function(arglist);Ti[[	@&i‡I"u      #   person.pets.count # => 3
      #
      #   person.pets.find(1, 2, 3)
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      def function(arglist)
        @var.create(var, &var)
      end;Ti[	@&i}I"e      #
      #   person.pets.count # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      def function(arglist)
        @var.count(var, var)
      end;Ti[	@&i±I"£      #   # Because the collection is loaded, you can
      #   # call the collection with no additional queries:
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      def function(arglist)
        @var.length
      end;Ti[@I";#   #       #<Pet id: 4, name: "Benny", person_id: 1>,;TI";#   #       #<Pet id: 5, name: "Brain", person_id: 1>,;TI":#   #       #<Pet id: 6, name: "Boss",  person_id: 1>;TI"#   #    ];TI"#;Ti[[	@&iEI"{      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 4, name: "Benny", person_id: 1>,
      #   #       #<Pet id: 5, name: "Brain", person_id: 1>,
      #   #       #<Pet id: 6, name: "Boss",  person_id: 1>
      #   #    ]
      #
      #   person.pets.destroy("4")
      #   # => #<Pet id: 4, name: "Benny", person_id: 1>;Ti[	@&i`I"F      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 4, name: "Benny", person_id: 1>,
      #   #       #<Pet id: 5, name: "Brain", person_id: 1>,
      #   #       #<Pet id: 6, name: "Boss",  person_id: 1>
      #   #    ]
      #
      #   other_pets = person.pets.to_ary
      #   # => [;Ti[	@&igI"b      #   #    ]
      #
      #   other_pets = person.pets.to_ary
      #   # => [
      #   #       #<Pet id: 4, name: "Benny", person_id: 1>,
      #   #       #<Pet id: 5, name: "Brain", person_id: 1>,
      #   #       #<Pet id: 6, name: "Boss",  person_id: 1>
      #   #    ]
      #
      #   other_pets.replace([Pet.new(name: 'BooGoo')])
      #;Ti[I"#   #    ];FI"#;TI"#   person.pets.delete_all;TI"#   # => [;TI"A#   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;TI";#   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[[	@&iBI"€      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&icI"€      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&i|I"€      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[I"@var = nil;FI"@var = nil;FI"@var = nil;FI"@var = nil;FI"@var = nil;FI"@var = nil;Fi[[	I":data//activerecord_proj/active_record/model_schema.rb;TiI"*        connection.clear_cache!
        undefine_attribute_methods
        connection.schema_cache.clear_table_cache!(table_name) if table_exists?
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil;Fi[	@.iI";        undefine_attribute_methods
        connection.schema_cache.clear_table_cache!(table_name) if table_exists?
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil unless defined? @var and @var;Fi[	@.iI"+        connection.schema_cache.clear_table_cache!(table_name) if table_exists?
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil
        @var = nil unless defined? @var and @var
        @var = nil;Fi[@w.I"end;FI" ;FI"private;FI" ;FI"def function(arglist);Fi[[	I"4data//activerecord_proj/active_record/result.rb;Ti2I"˛    def function(arglist)
      @var = columns.dup
      @var = rows.dup
      @var = nil
    end
    
    private
    
    def function(arglist)
      @var ||= (var = @var.map { |arglist| var.dup.freeze }
      @var.map { |arglist| Hash[var.zip(var)] });Fi[	@∏imI"∫      def function(arglist)
        @var = var
        yield
        @var = nil
      end
      
      private
      
      def function(arglist)
        (@var.failed? or @var)
      end;Fi[	@äiÅI"±      end
      
      def function(arglist)
        @var = nil
      end
      
      private
      
      def function(arglist)
        if @var then
          @var = @var.call;Fi[I"@var = true;FI"end;FI" ;FI"def function(arglist);FI"@var = false;FI"end;Fi[[	@Ωi5I"Ø      end
      
      def function(arglist)
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist);Fi[	@ΩiWI"π      
      def function(arglist)
        @var = true
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist);Fi[	@ﬂi:I"π      
      def function(arglist)
        @var = true
        @var = true
      end
      
      def function(arglist)
        @var = false
      end
      
      def function(arglist);Fi[@"I"var = @var;F@*@*@%@%i[[	@ñiYI"3  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING");Fi[	@ôiNI"3  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING");Fi[	@úiHI"3  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING");Fi[@"@¢.I"if (var > 0) then;FI"@var = 0;FI"out("STRING");FI"end;Fi[[	@üiI"”  
  def function(arglist)
    if self.BreakThePage?(var) then
      var = @var
      var = @var
      if (var > 0) then
        @var = 0
        out("STRING")
      end
      self.AddPage(@var)
      @var = var;Fi[	@ài÷I"    var = @var
    if ((@var + var) > @var) and (@var.! and AcceptPageBreak) then
      if @var[(@var + 1)].nil? then
        var = @var
        var = @var
        if (var > 0) then
          @var = 0
          out("STRING")
        end
        AddPage(@var)
        @var = var;Fi[	@àiWI"    var = @var
    if (@var > @var) and (@var.! and AcceptPageBreak) then
      if @var[(@var + 1)].nil? then
        var = @var
        var = @var
        if (var > 0) then
          @var = 0
          out("STRING")
        end
        AddPage(@var)
        @var = var;Fi[@"@*@*@%@%I"var = var.length;Fi[[	@ñiZI"9  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0;Fi[	@ôiOI"9  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0;Fi[	@úiII"9  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0;Fi[@^@w@xI"@var ||= {};FI"end;FI" ;Fi[[	@i"I"Ó    #   document.delayed_atomic_unsets
    #
    # @return [ Hash ] The atomic unsets
    #
    # @since 3.0.0
    def function(arglist)
      @var ||= {}
    end
    
    # Flag the document as destroyed and return the atomic path.
    #;Fi[	I"-data//mongoid_proj/lib/mongoid/config.rb;Ti’I"’    #   config.sessions
    #
    # @return [ Hash ] The sessions configuration.
    #
    # @since 3.0.0
    def function(arglist)
      @var ||= {}
    end
    
    # Set the session configuration options.
    #;Fi[	I"9data//mongoid_proj/lib/mongoid/contextual/command.rb;TiI"“      #   command.command
      #
      # @return [ Hash ] The db command.
      #
      # @since 3.0.0
      def function(arglist)
        @var ||= {}
      end
      
      # Get the database session.
      #;Fi[@"I"if (var > 0) then;FI"@var = 0;FI"out("STRING");FI"end;FI"AddPage(@var);Fi[[	@ài◊I"     if ((@var + var) > @var) and (@var.! and AcceptPageBreak) then
      if @var[(@var + 1)].nil? then
        var = @var
        var = @var
        if (var > 0) then
          @var = 0
          out("STRING")
        end
        AddPage(@var)
        @var = var
        if (var > 0) then;Fi[	@ài(I"b      var = 1 if (var >= 1)
      if ((var + ((var["STRING"] * var) / (@var * @var))) > @var) and (@var.! and AcceptPageBreak) then
        if @var[(@var + 1)].nil? then
          var = @var
          if (var > 0) then
            @var = 0
            out("STRING")
          end
          AddPage(@var)
          if (var > 0) then
            @var = var;Fi[	@àiXI"    if (@var > @var) and (@var.! and AcceptPageBreak) then
      if @var[(@var + 1)].nil? then
        var = @var
        var = @var
        if (var > 0) then
          @var = 0
          out("STRING")
        end
        AddPage(@var)
        @var = var
        if (var > 0) then;Fi[@^I"=# base - The String path to the dir containing the file.;FI".# name - The String filename of the file.;FI"#;FI"# Returns nothing.;FI"def function(arglist);Fi[[	I",data//jekyll_proj/jekyll/convertible.rb;TiI"    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist)
      begin
        (self.content = File.read(File.join(var, var));Fi[	@Oi!I"4    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist)
      super(var, var)
      self.data["STRING"] = "STRING" unless self.data.has_key?("STRING");Fi[	I"%data//jekyll_proj/jekyll/post.rb;Ti?I"4    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist)
      super(var, var)
      self.data["STRING"] = "STRING" unless self.data.has_key?("STRING");Fi[@^I"# @example;FI"#   class Person;FI"$#     include Mongoid::Document;FI"#     field :title;FI"#;Fi[[	@√+iáI"ˇ      end
      
      # Validates the format of a field.
      #
      # @example
      #   class Person
      #     include Mongoid::Document
      #     field :title
      #
      #     validates_format_of :title, with: /^[a-z0-9 \-_]*$/i
      #   end;Fi[	@√+iòI"Û      end
      
      # Validates the length of a field.
      #
      # @example
      #   class Person
      #     include Mongoid::Document
      #     field :title
      #
      #     validates_length_of :title, minimum: 100
      #   end;Fi[	@√+i©I"
      end
      
      # Validates whether or not a field is present - meaning nil or empty.
      #
      # @example
      #   class Person
      #     include Mongoid::Document
      #     field :title
      #
      #     validates_presence_of :title
      #   end;Fi[@^I"# == Examples;FI"#;FI"#   class Vehicle;F@†@z,i[[	@,iI"Q    # object's current value doesn't match the state, then this will return
    # false, otherwise true.  If the given state is unknown, then an IndexError
    # will be raised.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end;Fi[	@,i&I"g    # Determines the current state of the given object as configured by this
    # state machine.  This will attempt to find a known state that matches
    # the value of the attribute on the object.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end;Fi[	@,iAI"D    # Determines the current state of the given object as configured by this
    # state machine.  If no state is found, then an ArgumentError will be
    # raised.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end;Fi[@^I" #   person.pets.size # => 2;TI"#   person.pets;TI"#   # => [;TI";#   #       #<Pet id: 2, name: "Spook", person_id: 1>,;TI">#   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[[	@&iΩI"Y      #
      #   person.pets.delete(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&iıI"Y      #
      #   person.pets.delete(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&i-I"Z      #
      #   person.pets.destroy(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[@^@k.I"#   # => [;T@m.@n.I">#   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[[	@&iCI"†      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&idI"†      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@&i}I"†      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[@^I"def function(arglist);FI"var = case var;F@>I""STRING";F@À)i[[	@Œ	i.I"ê      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then;Fi[	@*i,I"ê      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then;Fi[	@0i*I"ê      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then;Fi[@^@!@,I"#   end;FI"#;FI"#   person = Person.new;Fi[[	@,iºI"E    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new
    #   person.valid? # => false
    #   person.errors # => #<ActiveModel::Errors:0x007fe603816640 @messages={name:["can't be blank"]}>;Fi[	@,iÕI"˜    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new
    #   person.name = ''
    #   person.valid? # => false;Fi[	@,iÙI"¯    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #
    #   person = Person.new
    #   person.name = ''
    #   person.invalid? # => true;Fi[@^@!@,I"#;F@,@,i[[	@,iπI"	    
    # Returns the +Errors+ object that holds all information about attribute
    # error messages.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #;Fi[	@,i I"    
    # Runs all the specified validations and returns +true+ if no errors were
    # added otherwise +false+.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #;Fi[	@,iÒI"    
    # Performs the opposite of <tt>valid?</tt>. Returns +true+ if errors were
    # added, +false+ otherwise.
    #
    #   class Person
    #     include ActiveModel::Validations
    #
    #     attr_accessor :name
    #     validates_presence_of :name
    #   end
    #;Fi[I"#extend(ActiveSupport::Concern);FI" ;FI"&def self.required_fields(arglist);FI"[];FI"end;FI" ;Fi[[	I"4data//devise_proj/devise/models/omniauthable.rb;Ti	I"¯module Devise
  module Models
    module Omniauthable
      extend(ActiveSupport::Concern)
      
      def self.required_fields(arglist)
        []
      end
      
      module ClassMethods
        Devise::Models.config(self, :omniauth_providers);Fi[	I"4data//devise_proj/devise/models/registerable.rb;TiI"module Devise
  module Models
    module Registerable
      extend(ActiveSupport::Concern)
      
      def self.required_fields(arglist)
        []
      end
      
      module ClassMethods
        # A convenience method that receives both parameters and session to;Fi[	I"3data//devise_proj/devise/models/timeoutable.rb;Ti	I"module Devise
  module Models
    module Timeoutable
      extend(ActiveSupport::Concern)
      
      def self.required_fields(arglist)
        []
      end
      
      # Checks whether the user session has expired based on configured time.
      def function(arglist);Fi[@
I"%# @param request_method [Symbol];FI"# @param path [String];FI"# @param args [Array];FI"%# @return [Array<Twitter::User>];FI"def function(arglist);Fi[[	@ç(i I"!      end
      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop);Fi[	@∆)iI"5      DEFAULT_CURSOR = -1
      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |arglist|;Fi[	@∆)iI"ç          object_from_response(Twitter::User, var, var, merge_user(var.options, var))
        end
      end
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        unless (var.options[:user_id] or var.options[:screen_name]) then;Fi[@
@*@ˆ+@^(I"end;FI" ;Fi[[	@ÛiBI"U          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute;Fi[	@˘i;I"U          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute;Fi[	@¸i?I"U          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute;Fi[@I"when (5..8) then;FI""STRING";FI"	else;FI"'raise(ActiveRecordError, "STRING");FI"end;Fi[[	@ÎiÂI"          when 3 then
            "STRING"
          when nil, 4, 11 then
            "STRING"
          when (5..8) then
            "STRING"
          else
            raise(ActiveRecordError, "STRING")
          end
        when "STRING" then
          case var;Fi[	@öi.I"*            when 1, 2 then
              "STRING"
            when 3, 4 then
              "STRING"
            when (5..8) then
              "STRING"
            else
              raise(ActiveRecordError, "STRING")
            end
          when "STRING" then
            return super unless var;Fi[	@öi@I"            return "STRING" unless var
            case var
            when (1..4) then
              "STRING"
            when (5..8) then
              "STRING"
            else
              raise(ActiveRecordError, "STRING")
            end
          else
            super;Fi[@
I"H# Uses around callbacks to run state events if using the :save hook;FI"def function(arglist);FI"#if (action_hook == :save) then;F@l(I"	else;Fi[[	@Ûi9I"o      def function(arglist)
        define_helper(:instance, "STRING", "STRING", (468 + 1))
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end;Fi[	@˘i2I"a        owner_class.key(attribute, String) unless attribute_key
        super
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end;Fi[	@¸i6I"p        owner_class.field(attribute, :type => (String)) unless attribute_field
        super
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end;Fi[@
@B+@C+@D+I"end;FI" ;Fi[[	@ÛiI"      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist);Fi[	@˘iI"      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist);Fi[	@¸iI"      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist);Fi[@@À)I""STRING";FI"when :failure then;FI""STRING";FI"	else;Fi[[	@Œ	i2I"Ï      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end;Fi[	@*i0I"Ï      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end;Fi[	@0i.I"Ï      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end;Fi[@@É/I""STRING";FI"	else;FI"# do nothing;FI"end;Fi[[	@Œ	i4I"Ú        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var);Fi[	@*i2I"Ú        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var);Fi[	@0i0I"Ú        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var);Fi[@
I")def self.matching_ancestors(arglist);FI"["STRING"];FI"end;FI" ;FI"def self.extended(arglist);Fi[[	@ÛiI"      require("state_machine/integrations/active_record/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      def self.extended(arglist)
        require("active_record/version")
        super;Fi[	@ˆiI"\      require("state_machine/integrations/data_mapper/versions")
      
      @var = { :action => :save, :use_transactions => (false) }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      def self.extended(arglist)
        require("dm-core/version") unless ::DataMapper.const_defined?("STRING")
        super;Fi[	@¸iI"      require("state_machine/integrations/mongoid/versions")
      
      @var = { :action => :save }
      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      def self.extended(arglist)
        require("mongoid/version")
        super;Fi[@/@0@/@/I"#   #    ];TI"#;Ti[[	@&iøI"~      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1)
      #   # => #<Pet id: 1, name: "Fancy-Fancy", person_id: nil>;Ti[	@&i˜I"Ü      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1)
      #   # => ActiveRecord::RecordNotFound: Couldn't find Pet with id=1;Ti[	@&i/I"p      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.destroy(Pet.find(2), Pet.find(3))
      #   # => [;Ti[@
I"Aattr_reader(:plugin_path, :plugin_name, :plugin_pretty_name);FI" ;FI"def function(arglist);FI"
super;FI" @var = file_name.underscore;Fi[[	@+iI"*class RedminePluginGenerator < Rails::Generators::NamedBase
  source_root(File.expand_path("STRING", "STRING"))
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING";Fi[	I"cdata//redmine_proj/generators/redmine_plugin_controller/redmine_plugin_controller_generator.rb;TiI"4  argument(:controller, :type => :string)
  
  argument(:actions, :type => :array, :default => ([]), :banner => "STRING")
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING";Fi[	I"Ydata//redmine_proj/generators/redmine_plugin_model/redmine_plugin_model_generator.rb;TiI"M  class_option(:parent, :type => :string, :desc => "STRING")
  
  class_option(:indexes, :type => :boolean, :default => (true), :desc => "STRING")
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING";Fi[@=@ö @õ @g!I"#   person.pets;TI"#   # => [;Ti[[	@&iêI"=      # option.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@&iI"i      # responding to the +id+ and executes delete on them.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@&i I"y      # the +:dependent+ option. Returns an array with the removed records.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[@@ @!I"#   #    ];TI"#;T@k.i[[	@&i?I"ñ      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&i`I"ñ      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[	@&iyI"ñ      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;Ti[@ @!I"#   #    ];TI"#;T@k.I"#   # => [;Ti[[	@&i@I"±      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@&iaI"±      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@&izI"±      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[@!I"#   #    ];TI"#;T@k.I"#   # => [;T@m.i[[	@&iAI"€      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&ibI"€      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@&i{I"€      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[@
@J#I" ;F@L#I" ;FI"def function(arglist);Fi[[	@˜	iI"Ì      undef_method(:content)
      
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        "STRING"
      end;Fi[	@R#iI"Ù      undef_method(:content)
      
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        attributes.keys
      end;Fi[	@˝	i	I"  module XML
    class ElementDecl < Nokogiri::XML::Node
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist)
        "STRING"
      end;Fi[@k.I"#   # => [;T@m.@n.@/I"#   #    ];Ti[[	@&iDI"Ö      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.size # => 0;Ti[	@&ieI"      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1, 2, 3);Ti[	@&i~I"      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1, 2, 3);Ti[I"?#   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>];TI"#;T@/I"#   person.pets;TI"#   # => [;T@/i[[	@&iºI"b      #   #    ]
      #
      #   person.pets.delete(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&iÙI"b      #   #    ]
      #
      #   person.pets.delete(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@&i,I"c      #   #    ]
      #
      #   person.pets.destroy(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[@/I"#   person.pets;TI"#   # => [;T@/@/I"#   #    ];Ti[[	@&iæI"g      #   person.pets.delete(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1);Ti[	@&iˆI"g      #   person.pets.delete(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1);Ti[	@&i.I"ä      #   person.pets.destroy(Pet.find(1))
      #   # => [#<Pet id: 1, name: "Fancy-Fancy", person_id: 1>]
      #
      #   person.pets.size # => 2
      #   person.pets
      #   # => [
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.destroy(Pet.find(2), Pet.find(3));Ti[I"unless var.empty? then;FI"opoo("STRING");FI"puts("STRING");FI"puts("STRING");FI"puts(var);FI"@var = true;Fi[[	@¢i=I"–      next if var.directory?
      var.include?(var.extname).!
    end
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end;Fi[	@¢iII"  def function(arglist)
    return unless f.bin.directory?
    var = f.bin.children.select { |arglist| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end;Fi[	@¢iUI"  def function(arglist)
    return unless f.sbin.directory?
    var = f.sbin.children.select { |arglist| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end;Fi[@
@H#I" ;F@J#I" ;F@L#i[[	@˜	iI"˘      undef_method(:attributes)
      
      undef_method(:content)
      
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist);Fi[	@R#iI"ı      undef_method(:values)
      
      undef_method(:content)
      
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def function(arglist);Fi[	@h	iI"      undef_method(:attribute_nodes)
      
      undef_method(:attributes)
      
      undef_method(:namespace)
      
      undef_method(:namespace_definitions)
      
      undef_method(:line) if method_defined?(:line)
      
      def self.new(arglist);Fi[@
I"!# Read the YAML frontmatter.;FI"#;F@⁄.@€.I"#;Fi[[	@·.iI"    def function(arglist)
      (self.content or "STRING")
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist);Fi[	@OiI"      self.process(var)
      self.read_yaml(File.join(var, var), var)
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist);Fi[	@Ê.i=I"(        self.categories = self.data.pluralized_array("STRING", "STRING")
      end
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist);Fi[@
I"url("STRING");FI" ;FI"sha1("STRING");FI" ;FI"bottle do |arglist|;Fi[[	@KiìI"£end
class OldBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle do |arglist|
    url("STRING")
    sha1("STRING");Fi[	@Ki¯I"¡end
class AllCatsBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle do |arglist|
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion);Fi[	@Ki	I"¥end
class RevisedBottleSpecTestBall < Formula
  homepage("STRING")
  
  url("STRING")
  
  sha1("STRING")
  
  bottle do |arglist|
    version(1)
    sha1("STRING" => :snowleopard);Fi[@
I"$attr_reader(:gherkin_statement);FI" ;FI"def function(arglist);FI"@var ||= var;FI"end;Fi[[	@biI"ˆ      def function(arglist)
        @var, @var, @var, @var, @var = var, var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist);Fi[	@iI"      def function(arglist)
        @var, @var, @var, @var, @var, @var, @var = var, var, var, var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist);Fi[	@$iI"Î      def function(arglist)
        @var, @var, @var, @var = var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist);Fi[@
I"I# Returns the command that will sync the given revision to the given;FI"L# destination directory. The perforce client has a fixed destination so;FI"K# the files must be copied from there to their intended resting place.;FI"def function(arglist);FI"$p4_sync(var, var, p4sync_flags);Fi[[	@ÜiI"é        def function(arglist)
          "STRING"
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        ;Fi[	@ÜiI"•        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        ;Fi[	@Üi!I"•        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        ;Fi[@äI"while (var < var) do;FI"?var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);FI"if (var < 128) then;FI"/var = (var + var[var.chr]) if var[var.chr];FI"var = (var + 1);Fi[[	@ñiBI"*    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000);Fi[	@ôi2I"=    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        if (var >= 161) and (var <= 223) then;Fi[	@úi1I"*    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000);Fi[@ä@˚)I"var = var.length;FI"var = 0;F@˛)@a0i[[	@ñi?I"  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr];Fi[	@ôi/I"  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr];Fi[	@úi.I"  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr];Fi[@
I";# Increments the given revision number and returns it.;FI"def function(arglist);FI"(var.to_i + 1);FI"end;FI" ;Fi[[	I"?data//capistrano_proj/capistrano/recipes/deploy/scm/bzr.rb;TiAI"˝          var = scm("STRING", repository)
          var = yield(var)
        end
        
        # Increments the given revision number and returns it.
        def function(arglist)
          (var.to_i + 1)
        end
        
        private
        ;Fi[	@Üi8I"∞          var = scm(authentication, :changes, "STRING", "STRING", "STRING")
          yield(var)[/Change (\d+) on/, 1]
        end
        
        # Increments the given revision number and returns it.
        def function(arglist)
          (var.to_i + 1)
        end
        
        # Determines what the response should be for a particular bit of text
        # from the SCM. Password prompts, connection requests, passphrases,;Fi[	@Ëi=I"≠          raise("STRING") unless Hash.===(var)
          [(var["STRING"] or 0).to_i, (var["STRING"] or 0).to_i].max
        end
        
        # Increments the given revision number and returns it.
        def function(arglist)
          (var.to_i + 1)
        end
        
        # Determines what the response should be for a particular bit of text
        # from the SCM. Password prompts, connection requests, passphrases,;Fi[@
I"attr_accessor(:source);FI" ;FI"def function(arglist);FI"@var = var;FI"@var = var;Fi[[	@»
iI"›    include(MatchPlatform)
    
    attr_reader(:name, :version, :platform, :dependencies)
    
    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var;Fi[	@’iI"‹    include(MatchPlatform)
    
    attr_reader(:name, :version, :dependencies, :platform)
    
    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = []
      @var = var;Fi[	@IiI"Œ    include(MatchPlatform)
    
    attr_reader(:name, :version, :platform)
    
    attr_accessor(:source)
    
    def function(arglist)
      @var = var
      @var = var
      @var = var
      @var = var;Fi[@
@å@»@é@…I"def function(arglist);Fi[[	@J	iôI"y    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :array, :lazy_default => ([]), :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    def function(arglist)
      begin
        (var = options.dup;Fi[	@J	i‰I"ç    method_option("STRING", :type => :array, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    def function(arglist)
      var = Array(options[:source])
      Bundler.ui.be_quiet! if options[:quiet];Fi[	@J	ifI""    end
    
    desc("STRING", "STRING", :hide => (true))
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    method_option("STRING", :type => :boolean, :banner => "STRING")
    
    def function(arglist)
      begin
        (Bundler.definition.validate_ruby!;Fi[I"@var.each do |arglist|;FI"newobj;F@""I"out("STRING");FI"end;FI" @var.each_pair do |arglist|;Fi[[	@ñiÒI"‰  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var;Fi[	@ôi˙I"‰  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var;Fi[	@úi‡I"‰  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var;Fi[I"C# +binds+ as the bind substitutes. +name+ is logged along with;FI"$# the executed +sql+ statement.;FI"def function(arglist);FI"exec_query(var, var, var);FI"end;FI" ;Fi[[	@‹iII"Ø      end
      
      # Executes insert +sql+ statement in the context of this connection using
      # +binds+ as the bind substitutes. +name+ is logged along with
      # the executed +sql+ statement.
      def function(arglist)
        exec_query(var, var, var)
      end
      
      # Executes delete +sql+ statement in the context of this connection using
      # +binds+ as the bind substitutes. +name+ is logged along with;Fi[	@‹iPI"Ø      end
      
      # Executes delete +sql+ statement in the context of this connection using
      # +binds+ as the bind substitutes. +name+ is logged along with
      # the executed +sql+ statement.
      def function(arglist)
        exec_query(var, var, var)
      end
      
      # Executes update +sql+ statement in the context of this connection using
      # +binds+ as the bind substitutes. +name+ is logged along with;Fi[	@‹iWI"f      end
      
      # Executes update +sql+ statement in the context of this connection using
      # +binds+ as the bind substitutes. +name+ is logged along with
      # the executed +sql+ statement.
      def function(arglist)
        exec_query(var, var, var)
      end
      
      # Returns the last auto-generated ID from the affected table.
      #;Fi[@
I"#:nodoc:;FI"def function(arglist);FI"@var.each do |arglist|;FI"
begin;FI"(log("STRING");Fi[[	I"9data//paperclip_proj/paperclip/storage/filesystem.rb;Ti"I"        after_flush_writes
        @var = {}
      end
      
      #:nodoc:
      def function(arglist)
        @var.each do |arglist|
          begin
            (log("STRING")
            FileUtils.rm(var) if File.exist?(var))
          rescue Errno::ENOENT => var;Fi[	@iπI"B      def function(arglist)
        s3_interface.buckets.create(bucket_name)
      end
      
      #:nodoc:
      def function(arglist)
        @var.each do |arglist|
          begin
            (log("STRING")
            var = (@var[var] or @var[:default])
            var = var.call(self, var) if var.respond_to?(:call);Fi[	@i’I"        after_flush_writes
        @var = {}
      end
      
      #:nodoc:
      def function(arglist)
        @var.each do |arglist|
          begin
            (log("STRING")
            s3_bucket.objects[var.sub(/^\//, "STRING")].delete)
          rescue AWS::Errors::Base => var;Fi[I"# == Examples;FI"#;FI"#   class Vehicle;F@†@z,I"#     end;Fi[[	@,iI"    # false, otherwise true.  If the given state is unknown, then an IndexError
    # will be raised.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   ;Fi[	@,i'I"#    # state machine.  This will attempt to find a known state that matches
    # the value of the attribute on the object.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   ;Fi[	@,iBI"     # state machine.  If no state is found, then an ArgumentError will be
    # raised.
    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   ;Fi[@
@ˆ@∆@ñ"I" var = remote_path_for(@var);FI"connection do |arglist|;Fi[[	@Wi7I"7          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|
          create_remote_path(var, var)
          files_to_transfer_for(@var) do |arglist|;Fi[	@ﬁi+I".          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|
          var.exec!("STRING")
          files_to_transfer_for(@var) do |arglist|;Fi[	@Zi)I"7          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|
          create_remote_path(var, var)
          files_to_transfer_for(@var) do |arglist|;Fi[@yI"rescue;FI"# do nothing;FI"end;FI"end;FI" ;Fi[[	@ÎiI"π      
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      
      def function(arglist)
        begin;Fi[	@ÎiI"¬      #:nodoc:
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      
      #:nodoc:
      def function(arglist);Fi[	@ÎiI"Õ      #:nodoc:
      def function(arglist)
        begin
          execute("STRING")
        rescue
          # do nothing
        end
      end
      
      def function(arglist)
        execute("STRING");Fi[@yI"var;FI"end;FI"end;FI"end;FI"end;Fi[[	@ÈiI"Ù            if document[field] then
              var = document.send(field)
              (value > 0) ? (var.pop) : (var.shift)
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end;Fi[	@ÓiI"‡            if document[field] then
              var = document.send(field)
              var.delete(value)
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end;Fi[	@ÛiI"˛            if document[field] then
              var = document.send(field)
              var.delete_if { |arglist| value.include?(var) }
              execute("STRING")
              var
            end
          end
        end
      end
    end
  end;Fi[@
I"end;FI"end;FI"var;FI"end;FI" ;Fi[[	@<
iÚI"            var = ["STRING", "STRING", "STRING", "STRING"]
            var = Node.new(:AN_PLUS_B, var)
          else
            var = var
          end
        end
        var
      end
      
      def function(arglist)
        if (var[1] == "STRING") then;Fi[	@≤(iI"ø          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var
    end
    
    def self.to_utf8(arglist)
      return var if var.nil?;Fi[	@≤(iGI"           rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var
    end
    
    def self.to_utf8_by_setting(arglist)
      return var if var.nil?;Fi[@
@I"var = (var + 1);FI"$var = var if var and (var == 2);FI"	next;FI"end;Fi[[	@ñizI"        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end
      if var.! then
        var = var;Fi[	@ôioI"%        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end
      if (var < 128) then
        var = (var + (var[var.chr] or 0));Fi[	@úiiI"        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end
      if var.! then
        var = var;Fi[@
@/@0@H/@I/@J/i[[	@ç(i˛I"       def function(arglist)
        list_from_response_with_users(:post, "STRING", var)
      end
      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist);Fi[	I"-data//twitter_proj/twitter/api/tweets.rb;Ti¸I""          object_from_response(Twitter::OEmbed, :get, "STRING", var.options)
        end
      end
      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::Tweet>]
      def function(arglist);Fi[	@∆)iI"ˆ  module API
    module Utils
      DEFAULT_CURSOR = -1
      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist);Fi[@
@I"var = 0;FI"var = (var + 1);F@8%I"	else;Fi[[	@üiZI"        self.Cell(var, var, var[(var..var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        if (var == "STRING"[0]) then
          var = var;Fi[	@üiwI"Î            var = (var + 1)
          end
          var = -1
          var = var
          var = 0
          var = 0
          var = (var + 1)
          var = var if var and (var == 2)
        else
          var = (var + 1)
        end;Fi[	@àizI"’          var = (var + 1)
        end
        var = -1
        var = var
        var = 0
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + 1)
      end;Fi[@
@/@0@5I" ;FI"def function(arglist);Fi[[	@ÛiMI"‚        var.concat(["STRING", "STRING"])
        Kernel.system(*var)
      end
      
      private
      
      attr_reader :function
      
      def function(arglist)
        configuration.merge("STRING" => (nil))
      end;Fi[	@	iAI"Ÿ        set_psql_env
        Kernel.system("STRING")
      end
      
      private
      
      attr_reader :function
      
      def function(arglist)
        (configuration["STRING"] or DEFAULT_ENCODING)
      end;Fi[	@iSI"Ó    def function(arglist)
      to.responds_to?(:read) ? ("STRING") : (to)
    end
    
    private
    
    attr_reader :function
    
    def function(arglist)
      logger.info("STRING") if logger
      @var = sessions.map do |arglist|;Fi[@NI"	else;F@Ö)I"end;FI"end;FI" ;Fi[[	@`ioI"	  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING");Fi[	@`ixI"	  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING");Fi[	@`i|I"%  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    if ["STRING", "STRING"].all? { |arglist| var[var].present? } then;Fi[@
@/@0@ÍI"var = @var;F@ "i[[	@ñiÏI"Ô      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"));Fi[	@ôiıI"Ô      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"));Fi[	@úi€I"Ô      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"));Fi[I"super(var, var, var, var);FI"end;FI"end;FI" ;FI"def function(arglist);F@•i[[	@ñi®I"    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var);Fi[	@ôi£I"    if (@var["STRING"] == "STRING") then
      SJISWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var);Fi[	@úióI"    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var);Fi[I"rescue;FI"var = (var + $!.success);FI"end;FI"var = var;FI"end;FI"end;Fi[[	@≤(iI"Ù            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var
    end;Fi[	@≤(iDI"Ù            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var
    end;Fi[	@≤(iÇI"            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
    end
  end;Fi[@
@/@0@ÍI"# do nothing;FI"end;Fi[[	@ibI"À            yield(@var) if block_given?
          end
        end
        
        private
        
        def function(arglist)
          # do nothing
        end
        
        def function(arglist);Fi[	@’iDI"œ    def function(arglist)
      @var ||= [name, version, source, platform, dependencies].hash
    end
    
    private
    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist);Fi[	@•iI"‰            private_methods.any? { |arglist| [var.to_s, var.to_sym].include?(var) }.!
          end
        end
        
        private
        
        def function(arglist)
          # do nothing
        end
      end
    end;Fi[@
@I"(@var.nil? ? ("STRING") : ("STRING");FI"end;FI" ;FI"def function(arglist);Fi[[	@‰iQI"Ì          "STRING"
        end
      end
      
      def function(arglist)
        @var.nil? ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        caller.first(3).find { |arglist| var =~ /should_not/ }
      end;Fi[	@Èi2I"#      def function(arglist)
        @var.nil? ? (true) : ((@var == var.method(var).arity))
      end
      
      def function(arglist)
        @var.nil? ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        (@var.length == 1) ? ("STRING") : (@var.inspect)
      end;Fi[	@Ûi3I"–      end
      
      private
      
      def function(arglist)
        @var.nil? ? ("STRING") : ("STRING")
      end
      
      def function(arglist)
        @var.nil? ? ("STRING") : ("STRING")
      end;Fi[@
@I"var = max_value;FI"var = min_value;F@‘(@’(i[[	@Ÿ(iI"+      def function(arglist)
        @var[:fields]
      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0));Fi[	@‹(iI"      end
      
      protected
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0));Fi[	@ziPI"^        var = (((@var[:fields][0].length / 2) * font_size) * 0.6)
        @var = var if (var > @var)
      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0));Fi[@
@I"@var[:fields];FI"end;FI" ;FI"def function(arglist);Fi[[	@Ÿ(iI"∂      end
      
      protected
      
      def function(arglist)
        @var[:fields]
      end
      
      def function(arglist)
        var = max_value
        var = min_value;Fi[	@‹(i#I"◊        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist)
        @var[:fields]
      end
      
      def function(arglist)
        (var / -2.0)
      end;Fi[	@ziFI"ﬁ        end
        return var
      end
      
      def function(arglist)
        @var[:fields]
      end
      
      def function(arglist)
        super
        var = (((@var[:fields][0].length / 2) * font_size) * 0.6);Fi[@ÍI"var[2] = "STRING";FI"when "STRING" then;FI"var[2] = "STRING";FI"when "STRING" then;FI"var[2] = "STRING";Fi[[	I"9data//redmine_proj/plugins/rfpdf/lib/core/rmagick.rb;TiI"    var[0] = var.columns
    var[1] = var.rows
    case var.mime_type
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING";Fi[	@é1iI"    case var.mime_type
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING";Fi[	@é1iI"Ô      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    when "STRING" then
      var[2] = "STRING"
    else
      # do nothing;Fi[@
@@ë@íI"	else;F@îi[[	@üiI",  end
  
  attr_reader :function
  
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    out(@var) if (@var > 0);Fi[	@üiI"2    end
    out(@var) if (@var > 0)
  end
  
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    @var = @var.!=(@var);Fi[	@üiI"C    @var = @var.!=(@var)
    out(@var) if (@var > 0)
  end
  
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    @var = @var.!=(@var);Fi[I"rescue LoadError;FI"
false;FI"end;FI"end;FI"end;FI"end;Fi[[	@·iãI"î        @var = begin
          (require("diff/lcs")
          true)
        rescue LoadError
          false
        end
      end
    end
  end
end;Fi[	@‰i~I"î        @var = begin
          (require("diff/lcs")
          true)
        rescue LoadError
          false
        end
      end
    end
  end
end;Fi[	@iVI"§      (require("rubygems")
      require("ruby-debug")
      true)
    rescue LoadError
      false
    end
    end
  end
end
def function(arglist)
  puts("STRING");Fi[@û1I"STDERR.puts("STRING");FI"STDERR.puts("STRING");FI"(raise(FatalException.new("STRING"));FI"end;FI"when "STRING" then;Fi[[	@diI"H          end
          @var = {}
          @var["STRING"]["STRING"].each { |arglist| @var[var.to_sym] = true })
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          require("kramdown");Fi[	@di'I"      when "STRING" then
        begin
          require("kramdown")
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          (require("rdiscount");Fi[	@di0I"L        begin
          (require("rdiscount")
          @var = @var["STRING"]["STRING"].map { |arglist| var.to_sym })
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          (require("maruku");Fi[@∞I"	else;FI"var = (var + 1000);FI"var = (var + 2);FI"end;FI"end;Fi[[	@ñiGI"      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000)
        var = (var + 2)
      end
    end
    return ((var * @var) / 1000)
  end;Fi[	@ôi;I"Ò      else
        if (var >= 161) and (var <= 223) then
          var = (var + 500)
          var = (var + 1)
        else
          var = (var + 1000)
          var = (var + 2)
        end
      end
    end
    return ((var * @var) / 1000);Fi[	@úi6I"      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000)
        var = (var + 2)
      end
    end
    return ((var * @var) / 1000)
  end;Fi[@∞@±@≤@≥I"var = (var + 1);F@1i[[	@ñixI"      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end;Fi[	@ôimI"      var = var
      if (var == 10) then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end;Fi[	@úigI"      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end;Fi[@
@I"out("STRING");F@Õ$@Œ$@œ$i[[	@ñi8I"      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING");Fi[	@ôiAI"      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING");Fi[	@úi'I"      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING");Fi[@
@@)@)@)@)i[[	@ñiI"È    var = var.gsub("STRING", "STRING")
    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -130, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  ;Fi[	@ôiI"ﬁ    var = (@var.length + 1)
    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -120, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  ;Fi[	@úiI"È    var = var.gsub("STRING", "STRING")
    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -130, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  ;Fi[@
@I"it("STRING") do |arglist|;FI"Eexpect((last_request.env["STRING"] or {})["STRING"]).to(eq(var));FI"end;FI"end;Fi[[	I"5data//omni_proj/omniauth/test/strategy_macros.rb;TiI"           expect(last_request.env["STRING"]).to(be_kind_of(Hash))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      
      def function(arglist);Fi[	@Ê1iI")          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      
      def function(arglist);Fi[	@Ê1iI"          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
    end
  end;Fi[@Î)I"end;FI" ;F@Ó)@Ô)I"end;Fi[[	@Ú)i9I"Ê    end
    
    def function(arglist)
      Thread.current["STRING"]
    end
    
    def function(arglist)
      Thread.current["STRING"] = var
    end
    
    # Returns the configuration of the associated connection as a hash:;Fi[	@OiyI"        end
        
        def function(arglist)
          Thread.current["STRING"]
        end
        
        def function(arglist)
          Thread.current["STRING"] = var
        end
        
        # The ignore_default_scope flag is used to prevent an infinite recursion;Fi[	@˜)iI"Œ    module ClassMethods
      #:nodoc:
      def function(arglist)
        Thread.current["STRING"]
      end
      
      def function(arglist)
        Thread.current["STRING"] = var
      end
    end
    ;Fi[I"var = case var;F@>I""STRING";F@À)I""STRING";F@É/i[[	@Œ	i0I"      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else;Fi[	@*i.I"      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else;Fi[	@0i,I"      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else;Fi[I"var.puts("STRING");FI"var.puts(var);FI"end;FI"end;FI"end;FI"end;Fi[[	@õiI"ª        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	@ûi$I"ª        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	@ßiI"ª        end.to_yaml
        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[@
@@‘+I"var;FI"end;FI" ;Fi[[	@<
i,I"Ï        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var);Fi[	@<
i1I"Ï        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var);Fi[	@<
i6I"€        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      # reduce 55 omitted
      # reduce 56 omitted;Fi[@
@@ +I"var;FI"end;FI" ;Fi[[	@<
i◊I"˛        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      def function(arglist)
        var = [var.first, var.last];Fi[	@<
i‹I"›        var = [var.first, var.last]
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      def function(arglist)
        var = [var.first, var.last];Fi[	@<
i·I"—        var = [var.first, var.last]
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      # reduce 41 omitted
      # reduce 42 omitted;Fi[@
@I"3to_a.any? { |arglist| (var.to_s == "STRING") };FI"end;FI" ;FI"def function(arglist);Fi[[	@zi>I"‰  def function(arglist)
    (alpha? or (beta? or rc?))
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end;Fi[	@ziBI"¯  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end;Fi[	@ziFI"  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    return unless var.is_a?(Version)
    return 0 if head? and var.head?;Fi[I"if @var.compressor then;FI"/@var.compressor.compress_with do |arglist|;FI"(var << var);FI"(var << var);FI"end;FI"end;Fi[[	@ƒiII"      var = "STRING"
      var = Pipeline.new
      (var << ("STRING" + "STRING"))
      if @var.compressor then
        @var.compressor.compress_with do |arglist|
          (var << var)
          (var << var)
        end
      end
      (var << "STRING")
      var.run;Fi[	@ i;I"        var = Pipeline.new
        var = "STRING"
        (var << mysqldump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run;Fi[	@8.i;I"        var = Pipeline.new
        var = "STRING"
        (var << pgdump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run;Fi[@42I"(var << var);FI"(var << var);FI"end;FI"end;FI"(var << "STRING");Fi[[	@ƒiJI"      var = Pipeline.new
      (var << ("STRING" + "STRING"))
      if @var.compressor then
        @var.compressor.compress_with do |arglist|
          (var << var)
          (var << var)
        end
      end
      (var << "STRING")
      var.run
      if var.success? then;Fi[	@ i<I"        var = "STRING"
        (var << mysqldump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then;Fi[	@8.i<I"        var = "STRING"
        (var << pgdump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then;Fi[@
@@i)I"var.write("STRING");F@k)I"assert(var.i386?.!);Fi[[	@iâI"	  def function(arglist)
    (HOMEBREW_PREFIX / "STRING").unlink
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@iôI"    assert((var.arch == :dunno))
    assert_match(/text executable/, file(var))
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@i©I"    assert((var.arch == :dunno))
    assert_match(/text executable/, file(var))
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[@
@I"include?("STRING");FI"end;FI" ;FI"def function(arglist);Fi[[	@wicI"Ó  def function(arglist)
    (include?("STRING") or ENV["STRING"])
  end
  
  def function(arglist)
    include?("STRING")
  end
  
  def function(arglist)
    var = find { |arglist| var =~ /--json=.+/ }
    var.split("STRING").last if var;Fi[	@wilI"…    var = find { |arglist| var =~ /--json=.+/ }
    var.split("STRING").last if var
  end
  
  def function(arglist)
    include?("STRING")
  end
  
  def function(arglist)
    include?("STRING")
  end;Fi[	@wipI"±  def function(arglist)
    include?("STRING")
  end
  
  def function(arglist)
    include?("STRING")
  end
  
  def function(arglist)
    (build_head? or build_devel?).!
  end;Fi[@
@I"#describe_path(which("STRING"));FI"end;FI" ;FI"def function(arglist);Fi[[	@[iOI"ˇ    return "STRING" unless MacOS::XQuartz.installed?
    return ("STRING" + describe_path(MacOS::XQuartz.prefix))
  end
  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    describe_path(which("STRING"))
  end;Fi[	@[iSI"»  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    describe_path(which("STRING"))
  end;Fi[	@[iWI"≤  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    "STRING"
  end;Fi[@
@@Å)@Ç)I"super(var);FI"	else;Fi[[	@`ikI"E    var = Hash[var.map { |arglist| [var[var], var] }]
    self.attributes.merge(var) { |arglist| var.blank? ? (var) : (var) }
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end;Fi[	@`itI"ˆ      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end;Fi[	@`i}I"ˆ      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end;Fi[@
@@í)I"end;FI" ;FI"def function(arglist);Fi[[	@ï)i:I"˚  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist)
    if (self.post.author == var.person) then
      return Notifications::CommentOnPost;Fi[	@)i+I"¥  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist)
    false
  end;Fi[	@ö)i2I"≈  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist)
    self.conversation.guid
  end;Fi[@
@@üI"	self;FI"end;FI" ;Fi[[	@πiI"’      (var.arity > 0) ? (yield(var)) : (var.instance_eval(&var))
      var
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist)
      (@var << "STRING");Fi[	@πiI"±      (@var << "STRING")
      self
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist)
      label_field(var, var);Fi[	@πi@I"Ø      (@var << "STRING")
      self
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist)
      return self if @var;Fi[@
@I"broadcast(var);FI"end;FI" ;FI"def function(arglist);Fi[[	@™iI"      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end;Fi[	@™i%I"Ë      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var, var)
      end;Fi[	@™ijI"Ë      def function(arglist)
        broadcast(var, var)
      end
      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end;Fi[@
@I"var = @var;F@ "I"newobj;F@""i[[	@ñiÓI"©  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end;Fi[	@ôi˜I"©  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end;Fi[	@úi›I"©  end
  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end;Fi[@
@@ú2I"var = @var;F@*@*i[[	@ñiWI"      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING");Fi[	@ôiLI"      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING");Fi[	@úiFI"      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING");Fi[@
@I"@var.accept_hook?(var);FI"end;FI" ;FI"def function(arglist);Fi[[	@iBI"Ó      def function(arglist)
        source_tags.map { |arglist| var.name }
      end
      
      def function(arglist)
        @var.accept_hook?(var)
      end
      
      def function(arglist)
        init
        var = @var.index(var);Fi[	@!i I"≥        end
        nil
      end
      
      def function(arglist)
        @var.accept_hook?(var)
      end
      
      def function(arglist)
        @var.source_tags
      end;Fi[	@!iÇI"…            end
          end
        end
        
        def function(arglist)
          @var.accept_hook?(var)
        end
        
        def function(arglist)
          (@var or @var)
        end;Fi[@
@I"(@var == "STRING");FI"end;FI" ;FI"def function(arglist);Fi[[	@ÄixI"·            @var = var
            @var = var
          end
          
          def function(arglist)
            (@var == "STRING")
          end
          
          def function(arglist)
            "STRING"
          end;Fi[	I"&data//homebrew_proj/exceptions.rb;TiVI"˝    var = var.map { |arglist| var.to_s.gsub("STRING", "STRING") }.join("STRING")
    super(var, "STRING")
  end
  
  def function(arglist)
    (@var == "STRING")
  end
  
  def function(arglist)
    @var ||= GitHub.issues_for_formula(formula.name)
  end;Fi[	@zi6I"ò  end
  
  attr_reader :function
  
  def function(arglist)
    (@var == "STRING")
  end
  
  def function(arglist)
    (alpha? or (beta? or rc?))
  end;Fi[@
@I"	name;FI"end;FI" ;FI"def function(arglist);Fi[[	I")data//diaspora_proj/models/aspect.rb;TiI"Ã  attr_accessible(:name, :contacts_visible, :order_id)
  
  before_validation { |arglist| name.strip! }
  
  def function(arglist)
    name
  end
  
  def function(arglist)
    case var
    when Post then;Fi[	I"Idata//redmine_proj/plugins/awesome_nested_set/spec/support/models.rb;Ti$I"Œ  def function(arglist)
    @@var
  end
  
  def function(arglist)
    name
  end
  
  def function(arglist)
    var.call(self, lambda { |arglist| self.children.each { |arglist| var.recurse(&var) } })
  end;Fi[	I"Ldata//redmine_proj/plugins/awesome_nested_set/test/fixtures/category.rb;TiI"ﬂclass Category < ActiveRecord::Base
  acts_as_nested_set
  
  def function(arglist)
    name
  end
  
  def function(arglist)
    var.call(self, lambda { |arglist| self.children.each { |arglist| var.recurse(&var) } })
  end;Fi[@
@I"(@var or @var);FI"end;FI" ;FI"def function(arglist);Fi[[	@ˇi¬I"ƒ        end
        Digest::SHA1.hexdigest(var)
      end
      
      def function(arglist)
        (@var or @var)
      end
      
      def function(arglist)
        options["STRING"]
      end;Fi[	@!iÜI"ﬁ        def function(arglist)
          @var.accept_hook?(var)
        end
        
        def function(arglist)
          (@var or @var)
        end
        
        def function(arglist)
          @var = var
        end;Fi[	@≈iI"ö  module Parser
    class GherkinBuilder
      include(Gherkin::Rubify)
      
      def function(arglist)
        (@var or @var)
      end
      
      def function(arglist)
        @var = Ast::Feature.new(nil, Ast::Comment.new(var.comments.map { |arglist| var.value }.join("STRING")), Ast::Tags.new(nil, var.tags), var.keyword, var.name.lstrip, var.description.rstrip, [])
        @var.gherkin_statement(var);Fi[@
@I"var = 0;F@˚)I"var = var.length;FI"var = 0;Fi[[	@ñi=I"‡      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@ôi-I"‡      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@úi,I"‡      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[@
@I"var;FI"end;FI" ;FI"def function(arglist);Fi[[	@ËiI"       def function(arglist)
        # do nothing
      end
      
      def function(arglist)
        var
      end
      
      def function(arglist)
        var[var].lower.eq(var.lower(var))
      end;Fi[	@Îi!I"Ë      def function(arglist)
        var.case_sensitive? ? (super) : (var[var].eq(var))
      end
      
      def function(arglist)
        var
      end
      
      def function(arglist)
        @var.fetch(:strict, true)
      end;Fi[	@˜iI"é    def function(arglist)
      true
    end
    
    def function(arglist)
      var
    end
    
    def function(arglist)
      var
    end;Fi[@
@I"@var.clear;FI"end;FI" ;FI"def function(arglist);Fi[[	@¢iΩI"∂      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        @var.clear
      end;Fi[	@¢i¡I"∏      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        @var.clear
      end;Fi[	@¢i≈I"¿      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        class << @var
          self;Fi[@
@@4@5I"end;FI"end;Fi[[	@i3I"£          owner.connection.delete(var.where(var).compile_delete)
        end
      end
      
      def function(arglist)
        false
      end
    end
  end
end;Fi[	@êi>I"v    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end
  end
end;Fi[	@1ibI"º      def function(arglist)
        ((var.index(File::SEPARATOR) == 0) or (var.index("STRING") == 1))
      end
      
      def function(arglist)
        false
      end
    end
  end
end;Fi[@
@@
I"end;FI" ;FI"def function(arglist);Fi[[	@W
i5I"⁄        end
      end
    end
    
    def function(arglist)
      @var ||= "STRING"
    end
    
    def function(arglist)
      var = "STRING"
      model.class_attribute(var.to_sym) unless model.method_defined?(var);Fi[	@êi*I"ú    def function(arglist)
      false
    end
    
    def function(arglist)
      @var ||= "STRING"
    end
    
    def function(arglist)
      {}
    end;Fi[	@’i<I"Û    def function(arglist)
      (super or @var.respond_to?(*var))
    end
    
    def function(arglist)
      @var ||= "STRING"
    end
    
    def function(arglist)
      @var ||= [name, version, source, platform, dependencies].hash
    end;Fi[@
@@◊@ÿI"end;FI"end;Fi[[	@'izI"ö        def function(arglist)
          :comment
        end
        
        def function(arglist)
          true
        end
      end
    end
  end
end;Fi[	I"%data//haml_proj/haml/template.rb;TiI"Ë  
  module Util
    undef :rails_xss_safe? if defined? rails_xss_safe?
    
    def function(arglist)
      true
    end
  end
end
Haml::Template.options[:ugly] = Rails.env.development?.!
Haml::Template.options[:escape_html] = true;Fi[	@ÖiI"˜    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      true
    end
  end
end
Paperclip.io_adapters.register(Paperclip::NilAdapter) do |arglist|
  (var.nil? or (Paperclip::Attachment.===(var) and var.present?.!));Fi[@>I""STRING";F@À)I""STRING";F@É/I""STRING";Fi[[	@Œ	i1I"Ë      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing;Fi[	@*i/I"Ë      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing;Fi[	@0i-I"Ë      #
      def function(arglist)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing;Fi[@À)I""STRING";F@É/I""STRING";FI"	else;FI"# do nothing;Fi[[	@Œ	i3I"Ô        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var);Fi[	@*i1I"Ô        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var);Fi[	@0i/I"Ô        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var);Fi[@É/I""STRING";FI"	else;FI"# do nothing;FI"end;F@Ô i[[	@Œ	i5I"·          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end;Fi[	@*i3I"·          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end;Fi[	@0i1I"·          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end;Fi[@Ã@Õ@Œ@˜@I"var = [];Fi[[	@6i\I"´      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |arglist| (var << ("STRING" + "STRING")) }
        Logger.message(var.join("STRING"));Fi[	@◊i%I"û      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |arglist| (var << "STRING") }
        Logger.message(var.join("STRING"));Fi[	@ﬁi:I"´      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(arglist)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |arglist| (var << ("STRING" + "STRING")) }
        Logger.message(var.join("STRING"));Fi[@
@I"
super;F@µ/I" @var = plugin_name.titleize;FI"@var = "STRING";Fi[[	@+i	I"˜  source_root(File.expand_path("STRING", "STRING"))
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING"
  end
  ;Fi[	@∫/iI",  argument(:actions, :type => :array, :default => ([]), :banner => "STRING")
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING"
    @var = controller.camelize
  end;Fi[	@Ω/iI"@  class_option(:indexes, :type => :boolean, :default => (true), :desc => "STRING")
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING"
    @var = model.camelize
    @var = @var.tableize;Fi[@
@@¯@˘I"end;FI" ;Fi[[	@]
i	I"„  module ConnectionAdapters
    class Transaction
      attr_reader(:connection)
      
      def function(arglist)
        @var = var
      end
    end
    
    class ClosedTransaction < Transaction
      def function(arglist);Fi[	I"4data//activerecord_proj/active_record/errors.rb;TiaI"›  
  class MultiparameterAssignmentErrors < ActiveRecordError
    attr_reader(:errors)
    
    def function(arglist)
      @var = var
    end
  end
  
  class UnknownPrimaryKey < ActiveRecordError
    attr_reader(:model);Fi[	I"9data//devise_proj/devise/controllers/rememberable.rb;TiI"        include(Devise::Controllers::Rememberable)
        
        delegate(:cookies, :env, :to => :@warden)
        
        def function(arglist)
          @var = var
        end
      end
      
      # Remembers the given resource by setting up a cookie
      def function(arglist);Fi[@
@I"raise("STRING");FI"end;FI"end;FI"end;Fi[[	@µ+iI"î      def function(arglist)
        raise("STRING")
      end
      
      def function(arglist)
        raise("STRING")
      end
    end
  end
end;Fi[	I"-data//devise_proj/devise/rails/routes.rb;Ti|I"Ñ        ::OmniAuth.config.path_prefix = var
      end
    end
    
    def function(arglist)
      raise("STRING")
    end
  end
end;Fi[	@πi=I"í            end
          end
        end
        
        def function(arglist)
          raise("STRING")
        end
      end
    end
  end
end;Fi[I"if (var == 0) then;FI"@var = var;F@÷"I"	else;F@ÿ"I"end;Fi[[	@ñiõI"º    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end;Fi[	@ôiñI"º    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end;Fi[	@úiäI"º    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end;Fi[I"var = (var - var);F@’(@÷(I",var = (scale_divisions or (var / 10.0));FI":var = (var < 1) ? (1) : (var.round) if scale_integers;FI"var = [];Fi[[	@Ÿ(iI"¶      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) };Fi[	@‹(iI"¶      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) };Fi[	@ziTI"¶      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) };Fi[I"
true);FI"rescue LoadError;FI"
false;FI"end;FI"end;FI"end;Fi[[	@·iäI"π        return @var unless @var.nil?
        @var = begin
          (require("diff/lcs")
          true)
        rescue LoadError
          false
        end
      end
    end
  end
end;Fi[	@‰i}I"π        return @var unless @var.nil?
        @var = begin
          (require("diff/lcs")
          true)
        rescue LoadError
          false
        end
      end
    end
  end
end;Fi[	@iUI"ù    begin
      (require("rubygems")
      require("ruby-debug")
      true)
    rescue LoadError
      false
    end
    end
  end
end
def function(arglist);Fi[I"if var["STRING"] then;FI"!if var["STRING"].nil?.! then;FI"9out((("STRING" + (var + var["STRING"])) + "STRING"));FI"	else;F@*+I"end;Fi[[	@ñiI"b          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end;Fi[	@ôi!I"b          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end;Fi[	@úiI"b          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end;Fi[@úI"	else;F@a%I"@var = var;F@÷"I"	else;Fi[[	@ñiôI"0    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end;Fi[	@ôiîI"0    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end;Fi[	@úiàI"0    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end;Fi[@
I"protected;FI" ;F@B+@C+@D+i[[	@ÛiI"È        require("active_record/version")
        super
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      ;Fi[	@˘iI"      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      ;Fi[	@¸iI"„        require("mongoid/version")
        super
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      ;Fi[@
@™I"J# Turn the object from the ruby type we deal with to a Mongo friendly;FI"# type.;FI"#;FI"$# @example Mongoize the object.;Fi[[	@©iI"      def function(arglist)
        true
      end
      
      module ClassMethods
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Float.mongoize("123.11")
        #;Fi[	@ºi¶I"8        var = var.fields[var.to_s]
        var ? (var.mongoize(var)) : (var)
      end
      
      module ClassMethods
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Hash.mongoize([ 1, 2, 3 ])
        #;Fi[	@Æi+I"      def function(arglist)
        true
      end
      
      module ClassMethods
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   BigDecimal.mongoize("123.11")
        #;Fi[@úI"@var = (@var + var);FI"!var = ((@var - @var) - @var);FI"/var = (((var - (2 * @var)) * 1000) / @var);FI"var = (var + 1);FI"var = (var + 1);Fi[[	@ñiÕI"V      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end;Fi[	@úiºI"V      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end;Fi[	@üi≠I"D      if (var > var) then
        if (var == -1) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end;Fi[I"
retry;FI"rescue;F@N1I"end;FI"var = var;FI"end;Fi[[	@≤(iI"          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var;Fi[	@≤(iCI"          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var;Fi[	@≤(iÅI"          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
    end;Fi[I"var.unshift(var);FI"var = nil;FI"	else;F@u*@v*I"var = nil;Fi[[	@+
iÚI"È    
    def function(arglist)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end;Fi[	@+
i¸I"˝      end
      var = nil if var and var.to_bool.!
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end;Fi[	@+
iI"È    
    def function(arglist)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end;Fi[I"6source_root(File.expand_path("STRING", "STRING"));FI" ;FI"desc("STRING");FI" ;FI"
hide!;FI" ;Fi[[	@Y	i-I"&    class SharedViewsGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      
      # Override copy_views to just copy mailer and shared.
      def function(arglist);Fi[	@Y	iPI"    class ErbGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      
      def function(arglist)
        view_directory(:mailer);Fi[	@Y	i^I"    class MarkerbGenerator < Rails::Generators::Base
      include(ViewPathTemplates)
      
      source_root(File.expand_path("STRING", "STRING"))
      
      desc("STRING")
      
      hide!
      
      def function(arglist)
        view_directory(:markerb, target_path);Fi[@ÏI"var = 0;F@˛)@a0@b0@c0i[[	@ñiAI"'  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else;Fi[	@ôi1I"'  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else;Fi[	@úi0I"'  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else;Fi[@Ï@S(I"var = 0;FI"if var then;F@\I"var = "STRING";Fi[[	@ñi_I"U    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING";Fi[	@ôiTI"U    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING";Fi[	@úiNI"U    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING";Fi[@(@s-I"out("STRING");FI"end;FI"end;FI"end;Fi[[	@ñi1I"1          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  ;Fi[	@ôi:I"1          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  ;Fi[	@úi I"1          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  ;Fi[@(I"var = var["STRING"];FI"if var then;F@q-I"end;F@s-i[[	@ñi-I"¥          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end;Fi[	@ôi6I"¥          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end;Fi[	@úiI"¥          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end;Fi[@S0@T0@U0@V0@W0I"end;Fi[[	@ÜiI"Ω          "STRING"
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given;Fi[	@ÜiI"‘          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given;Fi[	@Üi"I"◊          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will do an "p4 diff2" for the two revisions.;Fi[@T0@U0@V0@W0I"end;FI" ;Fi[[	@ÜiI"˙        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so;Fi[	@ÜiI"˙        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so;Fi[	@Üi#I"À        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will do an "p4 diff2" for the two revisions.
        def function(arglist);Fi[I"@var = (@var + var);F@À3@Ã3@Õ3@Œ3I"	next;Fi[[	@ñiŒI"v        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end
          var = (var + (var ? (1) : (2))) if (var == var);Fi[	@úiΩI"v        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end
          var = (var + (var ? (1) : (2))) if (var == var);Fi[	@üiÆI"T        if (var == -1) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end
          var = (var + 1) if (var == var);Fi[I"if color_stop?(var) then;FI"var.unshift(var);FI"var = nil;FI"	else;F@u*@v*i[[	@+
iÒI"Á    end
    
    def function(arglist)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end;Fi[	@+
i˚I"ˇ        end
      end
      var = nil if var and var.to_bool.!
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end;Fi[	@+
iI"Á    end
    
    def function(arglist)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end;Fi[@u*@v*I"var = nil;FI"end;FI"end;FI"'var = nil if var and var.to_bool.!;Fi[[	@+
iıI"        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      if color_stop?(var) then
        var.unshift(var);Fi[	@+
iˇI"4        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      if (var.size == 1) and list_of_color_stops?(var.first) then
        var = var.first.value;Fi[	@+
iI"Y        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      var = var if (var.size == 1) and var = list_of_color_stops?(var.first)
      LinearGradient.new(var, send(:color_stops, *var));Fi[I"if var.to_bool then;FI".Sass::Script::String.new(clean_path(var));FI"	else;FI"clean_url(var);FI"end;FI"end;Fi[[	@â*i&I"≥        end
      end
      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  ;Fi[	@â*i?I"’        Compass.configuration.http_fonts_path
      end
      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  ;Fi[	@â*irI"∫        end
      end
      var = "STRING" if var
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  ;Fi[@_4I"	else;FI"clean_url(var);FI"end;FI"end;FI"end;Fi[[	@â*i'I"∏      end
      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  
  module FontUrl;Fi[	@â*i@I"π      end
      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  
  module ImageUrl;Fi[	@â*isI"…      end
      var = "STRING" if var
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var)
      end
    end
  end
  
  module GeneratedImageUrl;Fi[@F0I" ;F@H0I"@var ||= var;FI"end;FI" ;Fi[[	@biI"        @var, @var, @var, @var, @var = var, var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        return if Cucumber.wants_to_quit;Fi[	@iI"        @var, @var, @var, @var, @var, @var, @var = var, var, var, var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        @var.feature = self if @var;Fi[	@$iI"›        @var, @var, @var, @var = var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      
      def function(arglist)
        false;Fi[I" @var.each_pair do |arglist|;FI"newobj;FI"@var[var]["STRING"] = @var;FI"5var = (FPDF_FONTPATH + var) if defined("STRING");FI"var = filesize(var);FI"Error("STRING") if var.!;Fi[[	@ñiˆI"K      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING");Fi[	@ôiˇI"K      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING");Fi[	@úiÂI"K      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING");Fi[@Ö4@Ü4@á4I"out("STRING");FI"(if (var["STRING"] == "STRING") then;FI"putType0(var);Fi[[	@ñiI"ˆ      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"];Fi[	@ôiI"ˆ      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"];Fi[	@úiıI"ˆ      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"];Fi[@(I"out("STRING");FI"out("STRING");FI"'if var["STRING"].!=("STRING") then;FI"newobj;FI"var = var["STRING"];Fi[[	@ñiI"1              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) };Fi[	@ôi(I"1              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) };Fi[	@úiI"1              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) };Fi[@•@Ÿ@⁄@0@¥)I"end;Fi[[	@™i@I"ÿ      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      
      def function(arglist);Fi[	@™idI"ÿ      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      
      def function(arglist);Fi[	@™itI"      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var, var)
      end
      
      # Print +messages+. This method can be called from within StepDefinitions.;Fi[@(I"if var.!=(var) then;FI"XCell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var);FI"end;FI"end;FI" ;Fi[[	@ñiÁI"‘      else
        var = (var + (var ? (1) : (2)))
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  ;Fi[	@ôiI"›        var = (var + var)
        var = var if (var >= 128)
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  ;Fi[	@úi÷I"‘      else
        var = (var + (var ? (1) : (2)))
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  ;Fi[I"STDERR.puts("STRING");FI"STDERR.puts("STRING");F@Æ1I"end;F@∞1I"
begin;Fi[[	@di I"S          @var = {}
          @var["STRING"]["STRING"].each { |arglist| @var[var.to_sym] = true })
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          require("kramdown")
        rescue LoadError;Fi[	@di(I"M        begin
          require("kramdown")
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          (require("rdiscount")
          @var = @var["STRING"]["STRING"].map { |arglist| var.to_sym });Fi[	@di1I"i          (require("rdiscount")
          @var = @var["STRING"]["STRING"].map { |arglist| var.to_sym })
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          (require("maruku")
          if @var["STRING"]["STRING"] then;Fi[@(I"Fvar = (var + "STRING") if var and var.to_s.index("STRING").nil?.!;FI"<Cell(var, var, var[var, (var - var)], var, 2, var, var);FI"if (var == 1) then;FI"@var = @var;FI"	else;Fi[[	@ñiïI"$      else
        var = (var + (var ? (1) : (2)))
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var;Fi[	@ôiêI"-        var = (var + var)
        var = var if (var >= 128)
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var;Fi[	@úiÑI"$      else
        var = (var + (var ? (1) : (2)))
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var;Fi[@(@à@â@äI"var = (var + 1);FI"$var = var if var and (var == 2);Fi[[	@ñiåI"A        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)));Fi[	@ôiÜI"3        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + var);Fi[	@úi{I"A        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)));Fi[I"@var.text!("STRING");FI"'@var.span(var, :class => "STRING");FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@-iàI""        @var = true
        @var.h3(:id => "STRING") do |arglist|
          @var.span(var, :class => "STRING")
          @var.text!("STRING")
          @var.span(var, :class => "STRING")
        end
      end
      
      def function(arglist)
        @var = (@var + 1)
        @var = false;Fi[	@-iûI"-        @var = false
        @var.h3(:id => "STRING") do |arglist|
          @var.span((var + "STRING"), :class => "STRING")
          @var.text!("STRING")
          @var.span(var, :class => "STRING")
        end
      end
      
      def function(arglist)
        @var = 0
        (@var << "STRING");Fi[	@-i∏I"      def function(arglist)
        @var.h4 do |arglist|
          @var.span(var, :class => "STRING")
          @var.text!("STRING")
          @var.span(var, :class => "STRING")
        end
      end
      
      def function(arglist)
        (@var << "STRING")
      end;Fi[I"if (var > var) then;FI"*if ((var == -1) or (var == var)) then;FI"if (@var > @var) then;FI"@var = @var;F@ 3@À3i[[	@ñi I"í      end
      var = var if (var.! or (var.chr == "STRING"))
      var = (var + (var ? (var[var.chr]) : (1000) or 0))
      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1);Fi[	@ôi“I"G          var = var
        end
      end
      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + var);Fi[	@úiπI"í      end
      var = var if (var.! or (var.chr == "STRING"))
      var = (var + (var ? (var[var.chr]) : (1000) or 0))
      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1);Fi[I"var = var["STRING"];FI"var = "STRING";F@)@)I"out("STRING");FI"newobj;Fi[[	@ñi$I"n        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|;Fi[	@ôi-I"n        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|;Fi[	@úiI"n        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|;Fi[@˛4@∫*@ª*I"out("STRING");F@Ω*I"	else;Fi[[	@ñiI"X      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING");Fi[	@ôiI"X      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING");Fi[	@úi¸I"X      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING");Fi[@˛4I"if var then;F@q-I"end;F@s-I"out("STRING");Fi[[	@ñi.I"ù          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end;Fi[	@ôi7I"ù          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end;Fi[	@úiI"ù          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end;Fi[@(I"!return ((var * @var) / 1000);FI"end;FI" ;FI"def function(arglist);FI")if (@var["STRING"] == "STRING") then;Fi[[	@ñiLI"Ì        var = (var + 1000)
        var = (var + 2)
      end
    end
    return ((var * @var) / 1000)
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBMultiCell(var, var, var, var, var, var, var)
    else;Fi[	@ôiAI"‚          var = (var + 2)
        end
      end
    end
    return ((var * @var) / 1000)
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      SJISMultiCell(var, var, var, var, var, var, var)
    else;Fi[	@úi;I"Ì        var = (var + 1000)
        var = (var + 2)
      end
    end
    return ((var * @var) / 1000)
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBMultiCell(var, var, var, var, var, var, var)
    else;Fi[I"var.json do |arglist|;FI"8render(:json => (@var.stream_posts.map do |arglist|;FI"ILastThreeCommentsDecorator.new(PostPresenter.new(var, current_user));FI"
end));FI"end;FI"end;Fi[[	@IieI"G    end
    respond_to do |arglist|
      var.all { |arglist| respond_with(@var, :locals => ({ :post_type => :all })) }
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  ;Fi[	I":data//diaspora_proj/controllers/streams_controller.rb;Ti?I"K    respond_with do |arglist|
      var.html { |arglist| render("STRING") }
      var.mobile { |arglist| render("STRING") }
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  ;Fi[	@Ui'I"à    gon.tagFollowings = tags if user_signed_in?
    @var = Stream::Tag.new(current_user, params[:name], :max_time => (max_time), :page => (params[:page]))
    respond_with do |arglist|
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  ;Fi[@-5@.5I"
end));FI"end;FI"end;FI"end;Fi[[	@IifI"é    respond_to do |arglist|
      var.all { |arglist| respond_with(@var, :locals => ({ :post_type => :all })) }
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  
  # hovercards fetch some the persons public profile data via json and display;Fi[	@65i@I"E      var.html { |arglist| render("STRING") }
      var.mobile { |arglist| render("STRING") }
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  
  def function(arglist);Fi[	@Ui(I"b    @var = Stream::Tag.new(current_user, params[:name], :max_time => (max_time), :page => (params[:page]))
    respond_with do |arglist|
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  
  private;Fi[@.5I"
end));FI"end;FI"end;FI"end;FI" ;Fi[[	@IigI"†      var.all { |arglist| respond_with(@var, :locals => ({ :post_type => :all })) }
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  
  # hovercards fetch some the persons public profile data via json and display
  # it next to the avatar image in a nice box;Fi[	@65iAI"W      var.mobile { |arglist| render("STRING") }
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  
  def function(arglist)
    session[:a_ids] = params[:a_ids] if params[:a_ids].present?;Fi[	@Ui)I"˙    respond_with do |arglist|
      var.json do |arglist|
        render(:json => (@var.stream_posts.map do |arglist|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end
    end
  end
  
  private
  ;Fi[@é)I"end;FI" ;FI"def function(arglist);F@í)I"end;Fi[[	@ï)i8I"›  
  #should be in relayable (pending on fixing Message)
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist);Fi[	@)i)I"≠  end
  
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist);Fi[	@ö)i0I"ﬂ  #sign comment as commenter
  #sign comment as post owner
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist);Fi[@N I"var = var.length;FI"var = 0;F@˛)@a0@b0i[[	@ñi@I"  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1);Fi[	@ôi0I"  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1);Fi[	@úi/I"  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1);Fi[@N @*@%@%I"var = var.length;F@S(i[[	@ñi[I"F  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then;Fi[	@ôiPI"F  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then;Fi[	@úiJI"F  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then;Fi[@(I"it("STRING") do |arglist|;FI"-var = Category.create(:name => "STRING");F@á#@à#@â#i[[	@é#i=I"ó    (categories(:top_level).right.should == 13)
    (categories(:child_2_1).left.should == 8)
    (categories(:child_2_1).right.should == 9)
  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text;Fi[	@é#iHI"r    Category.update_all("STRING")
    Category.rebuild!
    (Category.roots.last.to_text.should == var)
  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text;Fi[	@é#iSI"_    Category.update_all("STRING")
    Category.rebuild!
    (Category.roots.last.to_text.should == var)
  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var.name = nil;Fi[@Å)@Ç)I"super(var);FI"	else;F@Ö)I"end;Fi[[	@`imI"–  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  ;Fi[	@`ivI"–  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  ;Fi[	@`iI"–  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  ;Fi[@Ç)I"super(var);FI"	else;F@Ö)I"end;FI"end;Fi[[	@`inI"‚  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist);Fi[	@`iwI"‚  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist);Fi[	@`i{I"‚  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist);Fi[@Æ@D+I"end;FI" ;FI"6# Gets the db default for the machine's attribute;FI"def function(arglist);Fi[[	@ÛiI"Ö      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist)
        if owner_class.connected? and (owner_class.table_exists? and var = owner_class.columns_hash[attribute.to_s]) then
          var.default;Fi[	@˘iI"5      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist)
        attribute_key and attribute_key.default_value
      end;Fi[	@¸iI"3      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist)
        attribute_field and attribute_field.default
      end;Fi[@(@Ö4I"newobj;F@á4I"out("STRING");F@î4i[[	@ñiI"˚      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else;Fi[	@ôiI"˚      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else;Fi[	@úiÙI"˚      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else;Fi[I"opoo("STRING");FI"puts("STRING");FI"puts("STRING");FI"puts(var);FI"@var = true;FI"end;Fi[[	@¢i>I"∂      var.include?(var.extname).!
    end
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  ;Fi[	@¢iJI"    return unless f.bin.directory?
    var = f.bin.children.select { |arglist| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  ;Fi[	@¢iVI"    return unless f.sbin.directory?
    var = f.sbin.children.select { |arglist| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  ;Fi[@(@Ö4@†5@á4@à4@â4i[[	@ñiıI"+      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var));Fi[	@ôi˛I"+      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var));Fi[	@úi‰I"+      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var));Fi[I"(HOMEBREW_REPOSITORY.cd do |arglist|;FI"var = UpdaterMock.new;FI"!var.in_repo_expect("STRING");FI"+var.in_repo_expect("STRING", "STRING");FI"!var.in_repo_expect("STRING");FI"!var.in_repo_expect("STRING");Fi[[	I"-data//homebrew_proj/test/test_updater.rb;Ti1I"J  end
  
  def function(arglist)
    HOMEBREW_REPOSITORY.cd do |arglist|
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING");Fi[	@»5iCI"e  
  def function(arglist)
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do |arglist|
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var);Fi[	@»5iWI"e  
  def function(arglist)
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do |arglist|
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var);Fi[@(I"def function(arglist);F@k(@î(I"end;FI"
super;Fi[[	@#ifI"y      version("STRING") do |arglist|
        def self.active?(arglist)
          (::ActiveRecord::VERSION::MAJOR == 3) and (::ActiveRecord::VERSION::MINOR == 0)
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end;Fi[	@&iNI"q      version("STRING") do |arglist|
        def self.active?(arglist)
          defined? ::MongoMapper::Version and ::MongoMapper::Version =~ /^0\.9\./
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end;Fi[	@∞iI"C        end
        def function(arglist)
          define_helper(:instance, "STRING", "STRING", (26 + 1))
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end;Fi[@(I"(var << "STRING");FI"var.run;F@1.I"Logger.message("STRING");FI"	else;Fi[[	@ƒiNI"	          (var << var)
          (var << var)
        end
      end
      (var << "STRING")
      var.run
      if var.success? then
        Logger.message("STRING")
      else
        raise(Errors::Archive::PipelineError, ("STRING" + var.error_messages))
      end;Fi[	@ i@I"             (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages))
        end;Fi[	@8.i@I"             (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages))
        end;Fi[@(I"	@var;FI"end;FI"end;FI"end;FI"end;Fi[[	@-iVI"€        @var = {}
        [:token, :user, :message, :title, :priority, :device].each do |arglist|
          @var.merge!(var => (self.instance_variable_get("STRING")))
        end
        @var
      end
    end
  end
end;Fi[	I"1data//capistrano_proj/capistrano/cli/help.rb;Ti}I"~          else
            @var = self.class.ui.output_cols
          end
        end
        @var
      end
    end
  end
end;Fi[	@∂iZI"]        var = (var + TimeEntryActivityCustomField.all)
        var.select { |arglist| ["STRING", "STRING"].include?(var.field_format) }.each do |arglist|
          @var["STRING"] = { :sql => "STRING", :joins => (var.join_for_order_statement), :format => (var.field_format), :label => (var.name) }
        end
        @var
      end
    end
  end
end;Fi[I"var.write("STRING");F@k)I"assert(var.i386?.!);F@m)I"assert(var.ppc7400?.!);FI"assert(var.ppc64?.!);Fi[[	@iåI"  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@iúI"  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@i¨I"  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[I"0assert_version_equal("STRING", var.version);FI""assert_nil(var.head.checksum);FI"$assert_equal("STRING", var.url);FI"=assert_equal(GitDownloadStrategy, var.download_strategy);FI"<assert_instance_of(GitDownloadStrategy, var.downloader);FI"3assert_instance_of(HeadSoftwareSpec, var.head);Fi[[	I"-data//homebrew_proj/test/test_formula.rb;Ti∂I"ã    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  ;Fi[	@6i≈I"ã    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  ;Fi[	@6i‘I"ã    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  ;Fi[I"$assert_equal("STRING", var.url);FI"=assert_equal(GitDownloadStrategy, var.download_strategy);FI"<assert_instance_of(GitDownloadStrategy, var.downloader);F@6I"end;FI" ;Fi[[	@6i∏I"ô    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new;Fi[	@6i«I"ô    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new;Fi[	@6i÷I"ô    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = ExplicitStrategySpecTestBall.new;Fi[I""assert_nil(var.head.checksum);F@6@6@6@6I"end;Fi[[	@6i∑I"à    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist);Fi[	@6i∆I"à    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist);Fi[	@6i’I"à    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist);Fi[I",assert_equal(var.head, var.active_spec);FI"0assert_version_equal("STRING", var.version);F@6@6@6@6i[[	@6iµI"£    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[	@6iƒI"£    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[	@6i”I"£    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[@6@6@6I"end;FI" ;FI"def function(arglist);Fi[[	@6iπI"ä    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head);Fi[	@6i»I"ä    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head);Fi[	@6i◊I"å    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = ExplicitStrategySpecTestBall.new
    assert_not_nil(var.stable);Fi[I"assert_nil(var.bottle);FI"assert_nil(var.devel);FI",assert_equal(var.head, var.active_spec);FI"0assert_version_equal("STRING", var.version);F@6@6i[[	@6i≥I"™    var = HeadOnlySpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader);Fi[	@6i¬I"≤    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader);Fi[	@6i—I"≤    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader);Fi[I"assert_not_nil(var.head);FI"assert_nil(var.stable);FI"assert_nil(var.bottle);F@<6@=6@>6i[[	@6i±I"L  
  def function(arglist)
    var = HeadOnlySpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url);Fi[	@6i¿I"T  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url);Fi[	@6iœI"T  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url);Fi[@H6@I6@<6@=6@>6@6i[[	@6i≤I"Ü  def function(arglist)
    var = HeadOnlySpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy);Fi[	@6i¡I"é  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy);Fi[	@6i–I"é  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy);Fi[@<6@=6@>6@6@6@6i[[	@6i¥I"∫    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head);Fi[	@6i√I"∫    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head);Fi[	@6i“I"∫    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head);Fi[@(@ÙI""STRING";FI"end;FI"end;FI" ;Fi[[	@I
iI"¸            pdf_task(var, var, :label => (var), :markers => (true), :height => 0.8)
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      
      def function(arglist)
        case var[:format];Fi[	@I
i3I"$            pdf_task(var, var, :label => (var), :markers => (true), :height => 0.8)
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      
      def function(arglist)
        while @var.any? and var.is_descendant_of?(@var.last).! do;Fi[	@I
inI"Ò            pdf_task(var, var, :label => (var))
          else
            # do nothing
          end
        else
          "STRING"
        end
      end
      
      if Object.const_defined?(:Magick) then
        # Generates a gantt image;Fi[@(@Ù@¿%I"end;FI"end;FI" ;Fi[[	@@iI"        var.mobile do |arglist|
          render(:partial => "STRING", :locals => ({ :post => (@var.post), :comment => (@var) }))
        end
      end
    else
      render(:nothing => (true), :status => 422)
    end
  end
  
  def function(arglist)
    @var = Comment.find(params[:id]);Fi[	@FiI"2        var.json do |arglist|
          render(:json => (@var.as_api_response(:backbone)), :status => 201)
        end
      end
    else
      render(:nothing => (true), :status => 422)
    end
  end
  
  def function(arglist)
    @var = Like.find_by_id_and_author_id!(params[:id], current_user.person.id);Fi[	@LiHI"ˆ        end
      else
        render(:nothing => (true), :status => 422)
      end
    else
      render(:nothing => (true), :status => 422)
    end
  end
  
  def function(arglist)
    var = current_user.photos.where(:id => (params[:id])).first;Fi[@v)@w)I"assert(var.i386?.!);F@@@i[[	@iI")end
class MachOPathnameTests < Test::Unit::TestCase
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!);Fi[	@i5I"˛  end
  
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?);Fi[	@iCI"   end
  
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[@(@Ô
@
@Ò
I" ;FI"def function(arglist);Fi[[	@>ibI"z            super(var, var)
            serialized_attributes.each do |arglist|
              var[var] = Attribute.new(var, var[var], var) if var.key?(var)
            end
            var
          end
        end
        
        def function(arglist)
          if var and var = self.class.serialized_attributes[var.name] then
            Attribute.new(var, var, :unserialized);Fi[	@˜iQI"|              var = custom_values.detect { |arglist| (var.custom_field == var) }
              var ||= custom_values.build(:customized => (self), :custom_field => (var), :value => (nil))
              var.value = var.value
            end
            var
          end
        end
        
        def function(arglist)
          custom_field_values.select(&:visible?)
        end;Fi[	@D
ißI"¿        (var == "STRING") ? (var = "STRING") : (var = "STRING")
      else
        var = (var + var)
      end
      var
    end
  end
  
  def function(arglist)
    (@var << var)
    "STRING";Fi[@(@)I"out("STRING");FI"out("STRING");F@†4I"newobj;Fi[[	@ñiI"ı            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING";Fi[	@ôi'I"ı            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING";Fi[	@úiI"ı            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING";Fi[@@@@@\!I"assert(var.mach_o_bundle?);Fi[[	@iGI"=    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match(/Mach-O (64-bit )?bundle/, file(var))
  end;Fi[	@iUI"3    assert(var.universal?.!)
    assert(var.i386?)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match(/Mach-O bundle/, file(var))
  end;Fi[	@icI":    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match(/Mach-O 64-bit bundle/, file(var))
  end;Fi[@(@)I"if var.!=(var) then;F@µ4I"end;FI"end;Fi[[	@ñiÊI"È        var = (var + 1)
      else
        var = (var + (var ? (1) : (2)))
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private;Fi[	@ôiÔI"Â      else
        var = (var + var)
        var = var if (var >= 128)
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private;Fi[	@úi’I"È        var = (var + 1)
      else
        var = (var + (var ? (1) : (2)))
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private;Fi[@i)I"var.write("STRING");F@k)I"assert(var.i386?.!);F@m)@ˆ5i[[	@iãI"˚  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@iõI"˚  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@i´I"˚  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[@(@)@Õ4@Œ4I"if (var == 1) then;FI"@var = @var;Fi[[	@ñiîI"9        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)))
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then;Fi[	@ôièI"%      else
        var = (var + var)
        var = var if (var >= 128)
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then;Fi[	@úiÉI"9        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)))
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then;Fi[@è!@ê!I"end;FI"end;FI"end;FI" ;Fi[[	@ñ!i$I"    shutup do |arglist|
      DefaultPatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|;Fi[	@ñ!i.I"    shutup do |arglist|
      ListPatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|;Fi[	@ñ!i8I"    shutup do |arglist|
      P0PatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|;Fi[@ê!I"end;FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@ñ!i%I";      DefaultPatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|
      ListPatchBall.new("STRING").brew do |arglist|;Fi[	@ñ!i/I"6      ListPatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|
      P0PatchBall.new("STRING").brew do |arglist|;Fi[	@ñ!i9I"4      P0PatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|
      P1PatchBall.new("STRING").brew do |arglist|;Fi[I"8assert((TARGET_FOLDER + "STRING").exist?, "STRING");FI"8assert((TARGET_FOLDER + "STRING").exist?, "STRING");FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	I"6data//homebrew_proj/test/test_pathname_install.rb;Ti%I"U  def function(arglist)
    with_temp_folder do |arglist|
      TARGET_FOLDER.install(["STRING", "STRING"])
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      TARGET_FOLDER.install(Dir["STRING"]);Fi[	@‡6i-I"E  def function(arglist)
    with_temp_folder do |arglist|
      TARGET_FOLDER.install(Dir["STRING"])
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      FileUtils.mkdir_p("STRING");Fi[	@‡6i7I"O      FileUtils.mkdir_p("STRING")
      system("STRING")
      TARGET_FOLDER.install("STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      TARGET_FOLDER.install("STRING" => "STRING");Fi[@ÿ6I"end;FI"end;FI" ;FI"def function(arglist);FI""with_temp_folder do |arglist|;Fi[[	@‡6i&I"w    with_temp_folder do |arglist|
      TARGET_FOLDER.install(["STRING", "STRING"])
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      TARGET_FOLDER.install(Dir["STRING"])
      assert((TARGET_FOLDER + "STRING").exist?, "STRING");Fi[	@‡6i.I"D    with_temp_folder do |arglist|
      TARGET_FOLDER.install(Dir["STRING"])
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      FileUtils.mkdir_p("STRING")
      system("STRING");Fi[	@‡6i8I"g      system("STRING")
      TARGET_FOLDER.install("STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      TARGET_FOLDER.install("STRING" => "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING");Fi[I":assert((TARGET_FOLDER + "STRING").exist?.!, "STRING");FI"end;FI"end;FI" ;FI"def function(arglist);FI""with_temp_folder do |arglist|;Fi[[	@‡6iI"t    with_temp_folder do |arglist|
      TARGET_FOLDER.install("STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      TARGET_FOLDER.install(["STRING", "STRING"])
      assert((TARGET_FOLDER + "STRING").exist?, "STRING");Fi[	@‡6iAI"∞      TARGET_FOLDER.install("STRING" => "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      TARGET_FOLDER.install("STRING" => "STRING", "STRING" => "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING");Fi[	@‡6iKI"o      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
    end
  end
  
  def function(arglist)
    with_temp_folder do |arglist|
      FileUtils.mkdir_p("STRING")
      system("STRING");Fi[@¡5@¬5@√5@ƒ5@≈5I"+var.in_repo_expect("STRING", "STRING");Fi[[	@»5i2I"T  
  def function(arglist)
    HOMEBREW_REPOSITORY.cd do |arglist|
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.pull!;Fi[	@»5iDI"r  def function(arglist)
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do |arglist|
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var)
      var.pull!;Fi[	@»5iXI"r  def function(arglist)
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do |arglist|
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var)
      var.pull!;Fi[@(@)@5I"end;FI" ;FI"def function(arglist);Fi[[	@ñiKI"Ô      else
        var = (var + 1000)
        var = (var + 2)
      end
    end
    return ((var * @var) / 1000)
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBMultiCell(var, var, var, var, var, var, var);Fi[	@ôi@I"ˆ          var = (var + 1000)
          var = (var + 2)
        end
      end
    end
    return ((var * @var) / 1000)
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      SJISMultiCell(var, var, var, var, var, var, var);Fi[	@úi:I"Ô      else
        var = (var + 1000)
        var = (var + 2)
      end
    end
    return ((var * @var) / 1000)
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBMultiCell(var, var, var, var, var, var, var);Fi[@(@)I"@var = [];FI"end;FI" ;FI"def function(arglist);Fi[[	@ïiI"Ô              (var.width <=> var.width)
            else
              (var.height <=> var.height)
            end
          end
          @var = []
        end
        
        def function(arglist)
          send("STRING")
          @var;Fi[	@Ω0i6I"‚            # do nothing
          rescue SystemCallError => var
            log("STRING")
          end
        end
        @var = []
      end
      
      def function(arglist)
        FileUtils.cp(path(var), var)
      end;Fi[	@iﬁI"            s3_bucket.objects[var.sub(/^\//, "STRING")].delete)
          rescue AWS::Errors::Base => var
            # do nothing
          end
        end
        @var = []
      end
      
      def function(arglist)
        begin
          (log("STRING");Fi[@(@)I"(var << "STRING");FI"var.run;F@1.@‹5i[[	@ƒiMI"2        @var.compressor.compress_with do |arglist|
          (var << var)
          (var << var)
        end
      end
      (var << "STRING")
      var.run
      if var.success? then
        Logger.message("STRING")
      else
        raise(Errors::Archive::PipelineError, ("STRING" + var.error_messages));Fi[	@ i?I"I          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages));Fi[	@8.i?I"I          @var.compressor.compress_with do |arglist|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages));Fi[I"#sha1("STRING" => :snowleopard);FI"sha1("STRING" => :lion);FI"$sha1("STRING" => :mountainlion);FI"end;FI" ;FI"def function(arglist);Fi[[	@KitI"º  end
  
  bottle do |arglist|
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end;Fi[	@Ki˛I"«  sha1("STRING")
  
  bottle do |arglist|
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end;Fi[	@KiI"≈  
  bottle do |arglist|
    version(1)
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end;Fi[@-7@.7I"end;FI" ;FI"def function(arglist);FI"super("STRING");Fi[[	@KiuI"∫  
  bottle do |arglist|
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end;Fi[	@KiˇI"∫  
  bottle do |arglist|
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end;Fi[	@KiI"∆  bottle do |arglist|
    version(1)
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end;Fi[@.7I"end;FI" ;FI"def function(arglist);FI"super("STRING");FI"end;Fi[[	@KivI"„  bottle do |arglist|
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end
class ExplicitVersionSpecTestBall < Formula;Fi[	@Ki I"·  bottle do |arglist|
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end
class RevisedBottleSpecTestBall < Formula;Fi[	@KiI"‘    version(1)
    sha1("STRING" => :snowleopard)
    sha1("STRING" => :lion)
    sha1("STRING" => :mountainlion)
  end
  
  def function(arglist)
    super("STRING")
  end
end
class CustomVersionScheme < Version;Fi[@(@)I"
super;FI"end;FI" ;FI"def function(arglist);Fi[[	@
i!I"˚          var.flatten.each do |arglist|
            raise_on_type_mismatch(var)
            var.save! if var.new_record?
          end
        end
        super
      end
      
      def function(arglist)
        ensure_not_nested
        var = super;Fi[	@qiI"0          if respond_to?(var) and (respond_to?("STRING") and self.send(var).nil?) then
            write_attribute(var.to_s, var)
          end
        end
      end
      super
    end
    
    def function(arglist)
      if should_record_timestamps? then
        var = current_time_from_proper_timezone;Fi[	@qi(I"?          var = var.to_s
          next if attribute_changed?(var)
          write_attribute(var, var)
        end
      end
      super
    end
    
    def function(arglist)
      self.record_timestamps and (partial_writes?.! or (changed? or attributes.keys.&(self.class.serialized_attributes.keys).present?))
    end;Fi[@.0I"#;F@⁄.@€.I"#;FI"# Returns nothing.;Fi[[	@·.iI"      (self.content or "STRING")
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist)
      begin;Fi[	@Oi I"      self.read_yaml(File.join(var, var), var)
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist)
      super(var, var);Fi[	@Ê.i>I"ı      end
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(arglist)
      super(var, var);Fi[I"QUERY = "STRING";FI" ;FI"def self.process(arglist);FI"gvar = Sequel.mysql(var, :user => (var), :password => (var), :host => (var), :encoding => "STRING");FI" FileUtils.mkdir_p("STRING");FI"!var[QUERY].each do |arglist|;Fi[[	@ûiI"<      CSV.process
    end
    
    QUERY = "STRING"
    
    def self.process(arglist)
      var = Sequel.mysql(var, :user => (var), :password => (var), :host => (var), :encoding => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |arglist|
        var = var[:title]
        var = var[:permalink];Fi[	@°iI"frequire("yaml")
module Jekyll
  module MT
    QUERY = "STRING"
    
    def self.process(arglist)
      var = Sequel.mysql(var, :user => (var), :password => (var), :host => (var), :encoding => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |arglist|
        var = var[:entry_title]
        var = var[:entry_basename].gsub(/_/, "STRING");Fi[	@ßiI"Prequire("yaml")
module Jekyll
  module TextPattern
    QUERY = "STRING"
    
    def self.process(arglist)
      var = Sequel.mysql(var, :user => (var), :password => (var), :host => (var), :encoding => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |arglist|
        var = var[:Title]
        var = var[:url_title];Fi[@(@)@*I"out("STRING");FI"out("STRING");F@†4i[[	@ñiI"              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"];Fi[	@ôi&I"              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"];Fi[	@úiI"              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"];Fi[@(@)@*I"	self;FI"end;FI" ;Fi[[	@Ö
i<I"“        else
          Rails.logger.info("STRING")
          return
        end
      end
    end
    self
  end
  
  def function(arglist)
    verify_signature(self.parent_author_signature, self.parent.author);Fi[	@1i‰I"}                set_relation(var, get_relation(var, var).substitute(var.substitutable))
              else
                __build__(var, var.substitutable, var)
              end
            end
          end
          self
        end
        
        # Defines the setter method that allows you to set documents
        # in this relation by their ids. The defined setter, finds;Fi[	I"-data//nokogiri_proj/nokogiri/css/node.rb;Ti;I"d              var = var.value[0].value.first
              var.value[0].value = ["STRING"]
              var.value[1] = Node.new(:COMBINATOR, [Node.new(:FUNCTION, ["STRING"]), Node.new(:FUNCTION, ["STRING", var])])
            end
          end
        end
        self
      end
      
      # Find a node by type using +types+
      def function(arglist);Fi[@(@)@*I"return var;FI"end;FI" ;Fi[[	@-iíI"2              next if var.nil?
              var = var.instance_variable_get("STRING")
              var = (var + var.size) unless var.nil?
            end
          end
        end
        return var
      end
      
      def function(arglist)
        var = var.format_args(lambda { |arglist| "STRING" });Fi[	@àiÍ
I"§          (var << 65533)
          var = []
          var = 1
        end
      end
    end
    return var
  end
  
  #
  	# Converts UTF-8 strings to UTF16-BE.<br>;Fi[	@ài+I"Ÿ          (var << var.&(255).chr)
          (var << (var >> 8).chr)
          (var << var.&(255).chr)
        end
      end
    end
    return var
  end
  
  # ====================================================
  	#;Fi[@«!I"end;FI"end;FI"end;FI"end;FI"end;Fi[[	@õiI"ß        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	@ûi%I"ß        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	@ßiI"ß        File.open("STRING", "STRING") do |arglist|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[I"!if var["STRING"].nil?.! then;F@û3I"	else;F@*+I"end;FI"end;Fi[[	@ñiI"`          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING");Fi[	@ôi"I"`          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING");Fi[	@úiI"`          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING");Fi[I"var = ("STRING" + var);FI")var["STRING"].each_pair do |arglist|;FI"8var = (var + ((("STRING" + var) + "STRING") + var));FI"end;F@4I"if var then;Fi[[	@ñi*I"π          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end;Fi[	@ôi3I"π          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end;Fi[	@úiI"π          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end;Fi[I"# @since 3.0.0;F@x@2)I"end;FI"end;FI"end;Fi[[	@9)iI"      # @param [ Symbol ] name The name of the session config.
      # @param [ Hash ] config The configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[	@<)iI"      # @param [ Symbol, String ] name The db config key.
      # @param [ Hash ] config The hash configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[	@?)iI"      # @param [ Symbol, String ] name The db config key.
      # @param [ Hash ] config The hash configuration options.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :name => (var), :config => (var)))
      end
    end
  end
end;Fi[@ø7@x@Ø-I"end;FI"end;FI"end;Fi[[	@•'iI"…      #
      # @param [ Class ] klass The document class.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[	@®'iI"Ÿ      #
      # @param [ Class ] klass The class of the embedded document.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[	@´'iI"…      #
      # @param [ Class ] klass The embedded class.
      #
      # @since 3.0.0
      def function(arglist)
        super(compose_message("STRING", :klass => (var)))
      end
    end
  end
end;Fi[@ø7@xI"	true;FI"end;FI" ;FI"module ClassMethods;Fi[[	@´ixI"Î      #
      # @return [ true ] true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Convert the provided object to a propery array of foreign keys.
        #;Fi[	@©iI"¸      #
      # @return [ true ] Always true.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.;Fi[	@ƒi(I"Ó      #
      # @return [ true ] True.
      #
      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Convert the object from its mongo friendly ruby type to this type.
        #;Fi[I"# @since 2.1.0;F@Ø"I"
begin;F@ç-I"yield);FI"ensure;Fi[[	@6i6I"      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end;Fi[	@6iUI"      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end;Fi[	@6iÆI"$        #
        # @return [ Object ] The return value of the block.
        #
        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield)
          ensure
            Threaded.exit_execution("STRING")
          end;Fi[I")var["STRING"].each_pair do |arglist|;F@¥7I"end;F@4I"if var then;F@q-i[[	@ñi+I"π          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"));Fi[	@ôi4I"π          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"));Fi[	@úiI"π          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"));Fi[I"$#     include Mongoid::Document;FI"#     embedded_in :person;FI"#   end;FI"#;F@⁄@€i[[	@‡i-I"p        #   end
        #
        #   class Address
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist);Fi[	@‡iKI"p        #   end
        #
        #   class Address
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist);Fi[	@‡igI"m        #   end
        #
        #   class Name
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist);Fi[@(@)@*@@I"end;Fi[[	@¸i`I"¢              (var << "STRING")
            else
              (var << "STRING")
            end
          end
        end
        var
      end
    end
  end
end;Fi[	@©iI"µ              (var << "STRING")
            end
            (var << "STRING")
          end
        end
      end
      var
    end
  end
end
Compass::Frameworks.discover(:defaults);Fi[	@∂iJI"∆        next if (var.symlink? or var.directory?)
        if (var.dylib? or (var.mach_o_bundle? or var.mach_o_executable?)) then
          (var << var)
        end
      end
    end
    var
  end
end;Fi[@(@)@*@+@ZI"private;Fi[[	@Oi7I"≠          redirect_to(getting_started_path)
        else
          redirect_to(edit_profile_path)
        end
      end
    end
  end
  
  private
  
  def function(arglist);Fi[	@liI"Ë          exec("STRING", *(ARGV.kegs + ["STRING", "STRING", "STRING", "STRING"]))
        else
          ARGV.kegs.each { |arglist| PrettyListing.new(var) }
        end
      end
    end
  end
  
  private
  
  def function(arglist);Fi[	@∂iI"º              end
            end
          end
        end
      end
    end
  end
  
  private
  
  OTOOL_RX = /\t(.*) \(compatibility version (\d+\.)*\d+, current version (\d+\.)*\d+\)/;Fi[@0-@1-@2-I"#;F@4-I"#;Fi[[	@ŸiI"â        # @example Validate the index spec.
        #   Options.validate(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist);Fi[	@Ÿi)I"é        # @example Validate the options.
        #   Options.validate_options(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist);Fi[	@Ÿi?I"à        # @example Validate the spec.
        #   Options.validate_spec(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist);Fi[@(@)@*@Ü	I"'initializer("STRING") do |arglist|;FI")config.after_initialize do |arglist|;Fi[[	I".data//mongoid_proj/lib/mongoid/railtie.rb;Ti,I"à            handle_configuration_error(var)
          rescue ::Mongoid::Errors::NoSessionHosts => var
            handle_configuration_error(var)
          end
        end
      end
      
      initializer("STRING") do |arglist|
        config.after_initialize do |arglist|
          unless (Rails.root.join("STRING", "STRING").file? or ::Mongoid.configured?) then
            puts("STRING");Fi[	@%8i5I"¨          unless (Rails.root.join("STRING", "STRING").file? or ::Mongoid.configured?) then
            puts("STRING")
            puts("STRING")
          end
        end
      end
      
      initializer("STRING") do |arglist|
        config.after_initialize do |arglist|
          unless config.action_dispatch.rescue_responses then
            ActionDispatch::ShowExceptions.rescue_responses.update(Railtie.rescue_responses);Fi[	@%8iRI"™          ::Mongoid.instantiate_observers
          ActionDispatch::Reloader.to_prepare do |arglist|
            ::Mongoid.instantiate_observers
          end
        end
      end
      
      initializer("STRING") do |arglist|
        config.after_initialize do |arglist|
          if defined? Unicorn and Unicorn::HttpServer::START_CTX.empty?.! then
            ::Mongoid.default_session.disconnect if ::Mongoid.configured?;Fi[@(@)@*@Ü	@Ω"I" ;Fi[[	@‹i‹I"           rescue Exception
            rollback_transaction
            raise
          end
        end
      end
      
      attr_reader :function
      
      def function(arglist)
        @var.open?;Fi[	@ìiI"            var
          else
            raise(InverseOfAssociationNotFoundError.new(self, var))
          end
        end
      end
      
      attr_reader :function
      
      # Returns whether or not the association should be validated as part of
      # the parent's validation.;Fi[	@@i–I"‹          if var =~ /^(\w+)=(.*)$/ then
            @var[:env_vars][$1] = $2
            true
          end
        end
      end
      
      attr_reader :function
      
      def function(arglist)
        if @var then;Fi[@(@)@*@Ü	@~I"out("STRING");Fi[[	@ñi5I"Ô          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]));Fi[	@ôi>I"Ô          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]));Fi[	@úi$I"Ô          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]));Fi[@1-@2-I"#;F@4-I"#;FI"# @since 3.0.0;Fi[[	@ŸiI"Ñ        #   Options.validate(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist)
          validate_spec(var, var, var);Fi[	@Ÿi*I"ä        #   Options.validate_options(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist)
          var.each_pair do |arglist|;Fi[	@Ÿi@I"≤        #   Options.validate_spec(Band, name: 1)
        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist)
          raise(Errors::InvalidIndex.new(var, var, var)) if var.is_a?(::Hash).!;Fi[@2-I"#;F@4-I"#;FI"# @since 3.0.0;FI"def function(arglist);Fi[[	@ŸiI"Ç        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist)
          validate_spec(var, var, var)
          validate_options(var, var, var);Fi[	@Ÿi+I"ä        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist)
          var.each_pair do |arglist|
            unless VALID_OPTIONS.include?(var) then;Fi[	@ŸiAI"¶        #
        # @param [ Class ] klass The model class.
        # @param [ Hash ] spec The index specification.
        # @param [ Hash ] options The index options.
        #
        # @raise [ Errors::InvalidIndex ] If validation failed.
        #
        # @since 3.0.0
        def function(arglist)
          raise(Errors::InvalidIndex.new(var, var, var)) if var.is_a?(::Hash).!
          var.each_pair do |arglist|;Fi[@(@)@*@Ü	@~I")if (@var["STRING"] == "STRING") then;Fi[[	@ñi†I"Õ        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var)
    else;Fi[	@ôiõI"œ        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      SJISWrite(var, var, var, var)
    else;Fi[	@úièI"Õ        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var)
    else;Fi[@(@)@*@Ü	@~I"shutup do |arglist|;Fi[[	@ñ!i&I"&        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|
      ListPatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING");Fi[	@ñ!i0I"$        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|
      P0PatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING");Fi[	@ñ!i:I"$        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING")
      end
    end
  end
  
  def function(arglist)
    shutup do |arglist|
      P1PatchBall.new("STRING").brew do |arglist|
        var = read_file("STRING");Fi[@(@)@*@Ü	@~I"case var;Fi[[	@DiÅI"ˆ              "STRING"
            else
              ("STRING" % var.to_s.gsub(/(["\\])/, "STRING"))
            end
          end
        end
        
        def function(arglist)
          case var
          when "STRING" then
            var;Fi[	@ziüI"        [:pre_vars, :vars].each do |arglist|
          options[var].keys.each do |arglist|
            options[var][var] = coerce_variable(options[var][var])
          end
        end
      end
      
      def function(arglist)
        case var
        when /^"(.*)"$/ then
          $1;Fi[	@L
iìI"Ô          rescue LoadError
            warn("STRING")
            @var = false
          end
        end
      end
      
      def function(arglist)
        case var
        when :context, "STRING", "STRING" then
          @var = :context;Fi[@(@)@*@Ü	@~I"var = "STRING";Fi[[	@ë'i©I"Í            var.call(owner, var)
          else
            var.send(var, owner, var)
          end
        end
      end
      
      def function(arglist)
        var = "STRING"
        (owner.class.send(var.to_sym) or [])
      end;Fi[	@fi¨I"/        ensure
          if var and var then
            Dir.chdir(var) { |arglist| FileUtils.rm_rf(var) if File.exist?(var) }
          end
        end
      end
      
      def function(arglist)
        var = "STRING"
        return unless Gem.respond_to?(var)
        Gem.send(var).each do |arglist|;Fi[	@©inI"          var
        else
          "STRING".gsub(/^ {8}/, "STRING")
        end
      end
    end
    
    def function(arglist)
      var = "STRING"
      var = Compass::Frameworks::ALL.inject(0) do |arglist|
        var = var.template_directories.inject(0) do |arglist|;Fi[@(@)@Á@*@ˆ+@^(i[[	@Ûi@I"E          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      ;Fi[	@˘i9I"E          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      ;Fi[	@¸i=I"E          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      ;Fi[@(@)@Á@ò#@ô#I"#;Fi[[	@i#I"k              else
                target.do_or_do_not(metadata.inverse_setter(target), base)
              end
            end
          end
          
          # Unbinds the base object and the inverse, caused by setting the
          # reference to nil.
          #
          # @example Unbind the document.
          #   name.person.unbind(:continue => true);Fi[	@&iI"à            target.parentize(base)
            binding do |arglist|
              target.do_or_do_not(metadata.inverse_setter(target), base)
            end
          end
          
          # Unbinds the base object and the inverse, caused by setting the
          # reference to nil.
          #
          # @example Unbind the document.
          #   person.name.unbind(:continue => true);Fi[	@+i$I"7                  end
                end
              end
            end
          end
          
          # Unbinds the base object and the inverse, caused by setting the
          # reference to nil.
          #
          # @example Unbind the document.
          #   game.person.unbind(:continue => true);Fi[@(@)@ÁI"J# Turn the object from the ruby type we deal with to a Mongo friendly;FI"# type.;FI"#;Fi[[	@´iâI"W            var.blank? ? (var) : (var.convert(var))
          else
            var.blank? ? ([]) : (var.convert(Array(var)))
          end
        end
        
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Array.mongoize([ 1, 2, 3 ]);Fi[	@∑iI"$        else
          var = to_time
          var.respond_to?(:getlocal) ? (var.getlocal) : (var)
        end
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   date_time.mongoize;Fi[	@øi}I"          return instance_variable_get("STRING")
        else
          false
        end
      end
      
      # Turn the object from the ruby type we deal with to a Mongo friendly
      # type.
      #
      # @example Mongoize the object.
      #   object.mongoize;Fi[I"3# @param [ Hash ] options The binding options.;FI"#;F@-@-I"#;F@-i[[	@iI"Â          #   name.person.bind(:continue => true)
          #   name.person = Person.new
          #
          # @param [ Hash ] options The binding options.
          #
          # @option options [ true, false ] :continue Continue binding the inverse.
          # @option options [ true, false ] :binding Are we in build mode?
          #
          # @since 2.0.0.rc.1
          def function(arglist)
            base.metadata = metadata.inverse_metadata(target) unless base.metadata;Fi[	@#iI"À          #   person.addresses.bind_one(address)
          #
          # @param [ Document ] doc The single document to bind.
          # @param [ Hash ] options The binding options.
          #
          # @option options [ true, false ] :continue Continue binding the inverse.
          # @option options [ true, false ] :binding Are we in build mode?
          #
          # @since 2.0.0.rc.1
          def function(arglist)
            var.parentize(base);Fi[	@#i%I"          # @example Unbind the document.
          #   person.addresses.unbind_one(document)
          #
          # @param [ Hash ] options The binding options.
          #
          # @option options [ true, false ] :continue Continue binding the inverse.
          # @option options [ true, false ] :binding Are we in build mode?
          #
          # @since 2.0.0.rc.1
          def function(arglist)
            binding { |arglist| var.do_or_do_not(metadata.inverse_setter(target), nil) };Fi[@(@)@Á@ˆ@8I"def function(arglist);Fi[[	@J	i€I"      else
        (Bundler.ui.error("STRING")
        exit(1))
      end
    end
    
    desc("STRING", "STRING")
    
    def function(arglist)
      var = [ENV["STRING"], ENV["STRING"], ENV["STRING"]].find do |arglist|
        var.nil?.! and var.empty?.!;Fi[	@J	iÌI"„        end
      else
        Bundler.ui.info("STRING")
      end
    end
    
    desc("STRING", "STRING")
    
    def function(arglist)
      if var then
        Bundler.require(:default, *group.split.map! { |g| g.to_sym });Fi[	@J	i¢I"Ò        Bundler.ui.confirm(var.map { |arglist| "STRING" }.join("STRING"))
      else
        Bundler.ui.confirm("STRING")
      end
    end
    
    desc("STRING", "STRING")
    
    def function(arglist)
      Env.new.write($stdout)
    end;Fi[@(@)@Á@~I"out("STRING");F@Õ$i[[	@ñi6I"          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[	@ôi?I"          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[	@úi%I"          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[@(@)@Á@~@•@¶i[[	@ñi©I"      MBWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING");Fi[	@ôi§I"      SJISWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING");Fi[	@úiòI"      MBWrite(var, var, var, var)
    else
      super(var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING");Fi[@(@)@Á@~I"var = @var;FI"var = @var;Fi[[	@ñiUI"˚      MBMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0);Fi[	@ôiJI"˝      SJISMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0);Fi[	@úiDI"˚      MBMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0);Fi[@(@)@Á@~@‡1@·1i[[	@Ê1iI";      def function(arglist)
        it("STRING") do |arglist|
          expect(last_request.env["STRING"]).to(be_kind_of(Hash))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end;Fi[	@Ê1iI"D      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end;Fi[	@Ê1iI"D      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end;Fi[@(@)@Á@~I"var = 0;F@˚)i[[	@ñi;I"≤      return GetMBStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0;Fi[	@ôi+I"¥      return GetSJISStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0;Fi[	@úi*I"≤      return GetMBStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0;Fi[@(@)@Á@~@##I"end;Fi[[	@iI"      # If it's not, it will execute the given block.
      def function(arglist)
        ActiveRecord::Base.connected? ? (connection.uncached(&var)) : (yield)
      end
    end
    
    def function(arglist)
      @var = var
    end
    
    def function(arglist);Fi[	@»
i?I"‰    def function(arglist)
      if @var and File.exists?(local_specification_path) then
        eval(File.read(local_specification_path))
      end
    end
    
    def function(arglist)
      @var = var
    end
    
    private;Fi[	@üiùI"§      @var = var
    else
      raise(("STRING" + var)) if var.!=("STRING")
    end
  end
  
  def function(arglist)
    @var = var
  end
  
  def function(arglist);Fi[@^"I"end;FI" ;F@Ö+@÷-I"# objects from the db.;Fi[[	@`iI"û        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading delete.;Fi[	@eiI"ü        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading destroy.;Fi[	@jiI"û        # @return [ Strategy ] The new strategy.
        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading delete.;Fi[@˙7I"#   end;FI"#;F@⁄@€@‹i[[	@‡i.I"ö        #
        #   class Address
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          if ancestors.include?(Mongoid::Versioning) then;Fi[	@‡iLI"ù        #
        #   class Address
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          var = characterize(var, Embedded::Many, var, &var);Fi[	@‡ihI"ô        #
        #   class Name
        #     include Mongoid::Document
        #     embedded_in :person
        #   end
        #
        # @param [ Symbol ] name The name of the relation.
        # @param [ Hash ] options The relation options.
        # @param [ Proc ] block Optional block for defining extensions.
        def function(arglist)
          var = characterize(var, Embedded::One, var, &var);Fi[I"#     relation.push(doc);FI"#   end;FI"#;F@È+I"#;FI"# @since 2.1.0;Fi[[	@6i1I"¸      #
      # @example Execute in binding mode.
      #   binding do
      #     relation.push(doc)
      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin;Fi[	@6iPI"ˇ      #
      # @example Execute in building mode.
      #   _building do
      #     relation.push(doc)
      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin;Fi[	@6i©I"        #
        # @example Execute in creating mode.
        #   creating do
        #     relation.push(doc)
        #   end
        #
        # @return [ Object ] The return value of the block.
        #
        # @since 2.1.0
        def function(arglist)
          begin;Fi[@È+I"#;FI"# @since 2.1.0;F@Ï+I"
begin;F@ç-i[[	@6i4I"      #     relation.push(doc)
      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure;Fi[	@6iSI"      #     relation.push(doc)
      #   end
      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure;Fi[	@6i¨I"        #     relation.push(doc)
        #   end
        #
        # @return [ Object ] The return value of the block.
        #
        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield)
          ensure;Fi[@NI"var;FI"end;FI" ;F@º'I"Avar = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);Fi[[	@<
i≈I"      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end;Fi[	@<
i I"      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end;Fi[	@<
iœI"      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end;Fi[@%)I"var;FI"end;FI" ;F@))@‘+i[[	@<
i)I"      # reduce 49 omitted
      # reduce 50 omitted
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end;Fi[	@<
i.I"Õ      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end;Fi[	@<
i3I"Õ      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end;Fi[@(@œ@*@ˆ+@^(I"end;Fi[[	@ÛiAI"A        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or;Fi[	@˘i:I"A        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or;Fi[	@¸i>I"A        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or;Fi[I"it("STRING") do |arglist|;F@t5@á#@à#@â#@ä#i[[	@é#i>I"â    (categories(:child_2_1).left.should == 8)
    (categories(:child_2_1).right.should == 9)
  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING");Fi[	@é#iII"r    Category.rebuild!
    (Category.roots.last.to_text.should == var)
  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var = Category.roots.last.to_text
    Category.update_all("STRING");Fi[	@é#iTI"a    Category.rebuild!
    (Category.roots.last.to_text.should == var)
  end
  it("STRING") do |arglist|
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var = Category.create(:name => "STRING")
    var.move_to_child_of(var)
    var.move_to_child_of(var)
    var.name = nil
    var.save!(:validate => (false));Fi[I"while (var < var) do;F@a0@b0@c0I"var = (var + 1);FI"	else;Fi[[	@ñiCI"6    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000)
        var = (var + 2);Fi[	@ôi3I"M    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        if (var >= 161) and (var <= 223) then
          var = (var + 500);Fi[	@úi2I"6    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000)
        var = (var + 2);Fi[@±/I" ;FI"def function(arglist);FI"
super;F@µ/@S3i[[	@+iI"0class RedminePluginGenerator < Rails::Generators::NamedBase
  source_root(File.expand_path("STRING", "STRING"))
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING"
  end;Fi[	@∫/iI")  
  argument(:actions, :type => :array, :default => ([]), :banner => "STRING")
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING"
    @var = controller.camelize;Fi[	@Ω/iI"*  
  class_option(:indexes, :type => :boolean, :default => (true), :desc => "STRING")
  
  attr_reader(:plugin_path, :plugin_name, :plugin_pretty_name)
  
  def function(arglist)
    super
    @var = file_name.underscore
    @var = plugin_name.titleize
    @var = "STRING"
    @var = model.camelize;Fi[@(@œ@l/@m/@n/@l(i[[	@Ûi8I"Ü      # Initializes dynamic states
      def function(arglist)
        define_helper(:instance, "STRING", "STRING", (468 + 1))
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super;Fi[	@˘i1I"q      def function(arglist)
        owner_class.key(attribute, String) unless attribute_key
        super
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super;Fi[	@¸i5I"Ä      def function(arglist)
        owner_class.field(attribute, :type => (String)) unless attribute_field
        super
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super;Fi[@(@œ@Ö+@÷-@‡8I"#;Fi[[	@`iI"ä        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading delete.
        #   strategy.cascade;Fi[	@eiI"ã        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading destroy.
        #   strategy.cascade;Fi[	@jiI"ä        def function(arglist)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading delete.
        #   strategy.cascade;Fi[@(@œI"# Obtain destination path.;FI"#;FI"5# dest - The String path to the destination dir.;FI"#;Fi[[	@OisI"d    # Returns the Hash representation of this Page.
    def function(arglist)
      self.data.deep_merge("STRING" => (File.join(@var, self.url)), "STRING" => (self.content))
    end
    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #
    # Returns the destination file path String.
    def function(arglist);Fi[	@Ê.iøI"k    def function(arglist)
      var = { "STRING" => ({ "STRING" => (related_posts(var["STRING"]["STRING"])) }), "STRING" => (self.to_liquid) }.deep_merge(var)
      do_layout(var, var)
    end
    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #
    # Returns destination file path String.
    def function(arglist);Fi[	@iI"    # Returns source file path.
    def function(arglist)
      File.join(@var, @var, @var)
    end
    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #
    # Returns destination file path.
    def function(arglist);Fi[@(@œ@.0I"#;F@⁄.@€.i[[	@·.iI"    # Returns the contents as a String.
    def function(arglist)
      (self.content or "STRING")
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.;Fi[	@OiI"      @var = var
      self.process(var)
      self.read_yaml(File.join(var, var), var)
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.;Fi[	@Ê.i<I"3      if self.categories.empty? then
        self.categories = self.data.pluralized_array("STRING", "STRING")
      end
    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.;Fi[I"0(categories(:child_2).should == var.parent);FI"6(categories(:child_2).id.should == var.parent_id);FI" var.left.should_not(be_nil);FI"!var.right.should_not(be_nil);FI"$Category.valid?.should(be_true);FI"end;Fi[[	@é#iÛI"∂  end
  it("STRING") do |arglist|
    var = Category.create!(:name => "STRING", :parent_id => (categories(:child_2).id))
    (categories(:child_2).should == var.parent)
    (categories(:child_2).id.should == var.parent_id)
    var.left.should_not(be_nil)
    var.right.should_not(be_nil)
    Category.valid?.should(be_true)
  end
  it("STRING") do |arglist|
    var = Category.create!(:name => "STRING", :parent => (categories(:child_2)));Fi[	@é#i˚I"¢  end
  it("STRING") do |arglist|
    var = Category.create!(:name => "STRING", :parent => (categories(:child_2)))
    (categories(:child_2).should == var.parent)
    (categories(:child_2).id.should == var.parent_id)
    var.left.should_not(be_nil)
    var.right.should_not(be_nil)
    Category.valid?.should(be_true)
  end
  it("STRING") do |arglist|
    var = Category.create!(:name => "STRING", :parent_id => (nil));Fi[	@é#i'I"”  end
  it("STRING") do |arglist|
    var = categories(:child_2).children.create!(:name => "STRING")
    (categories(:child_2).should == var.parent)
    (categories(:child_2).id.should == var.parent_id)
    var.left.should_not(be_nil)
    var.right.should_not(be_nil)
    Category.valid?.should(be_true)
  end
  # create a new top-level node and move single-node top-level tree inside it.
  # create a new top-level node and move an entire top-level tree inside it.;Fi[I"%categories(:child_2).save(false);FI".assert(Category.left_and_rights_valid?.!);FI"end;FI" ;FI"def function(arglist);FI",assert(Category.left_and_rights_valid?);Fi[[	@ì#iﬁI"R  def function(arglist)
    assert(Category.left_and_rights_valid?)
    categories(:child_2)[:lft] = nil
    categories(:child_2).save(false)
    assert(Category.left_and_rights_valid?.!)
  end
  
  def function(arglist)
    assert(Category.left_and_rights_valid?)
    categories(:child_2)[:rgt] = nil
    categories(:child_2).save(false);Fi[	@ì#iÂI"u  def function(arglist)
    assert(Category.left_and_rights_valid?)
    categories(:child_2)[:rgt] = nil
    categories(:child_2).save(false)
    assert(Category.left_and_rights_valid?.!)
  end
  
  def function(arglist)
    assert(Category.left_and_rights_valid?)
    categories(:top_level_2)[:lft] = categories(:top_level_2)[:rgt]
    categories(:top_level_2).save(false);Fi[	@ì#iÛI"Ñ  def function(arglist)
    assert(Category.left_and_rights_valid?)
    categories(:child_2)[:lft] = categories(:top_level)[:lft]
    categories(:child_2).save(false)
    assert(Category.left_and_rights_valid?.!)
  end
  
  def function(arglist)
    assert(Category.left_and_rights_valid?)
    categories(:child_2)[:rgt] = categories(:top_level)[:rgt]
    categories(:child_2).save(false);Fi[I"@var[var]["STRING"] = @var;F@à4@â4@ä4I"out(("STRING" + var));FI"+out("STRING") if (var[-2] == "STRING");Fi[[	@ñi¯I"Å    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?;Fi[	@ôiI"Å    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?;Fi[	@úiÁI"    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) if var["STRING"].nil?.!;Fi[@å9I"out("STRING");F@î4I"putType0(var);FI"	else;FI"var = var["STRING"];Fi[[	@ñiI"    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then;Fi[	@ôiI"    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then;Fi[	@úi˜I"    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then;Fi[@Õ#@""I"out("STRING");FI"end;F@ü0I"newobj;Fi[[	@ñiÚI"  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING");Fi[	@ôi˚I"  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING");Fi[	@úi·I"  def function(arglist)
    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING");Fi[@Õ#@á4@à4@â4@ä4@ç9i[[	@ñi˜I"H      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]));Fi[	@ôi I"H      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]));Fi[	@úiÊI"H      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]));Fi[@Õ#@á4I"out("STRING");F@î4I"putType0(var);FI"	else;Fi[[	@ñiI"      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var));Fi[	@ôiI"      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var));Fi[	@úiˆI"      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var));Fi[@Õ#@¢4I"var = "STRING";F@)@)I"out("STRING");Fi[[	@ñi#I"U        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var);Fi[	@ôi,I"U        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var);Fi[	@úiI"U        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var);Fi[@Õ#@)@≥7@¥7I"end;F@4i[[	@ñi)I"¯          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"));Fi[	@ôi2I"¯          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"));Fi[	@úiI"¯          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"));Fi[@Õ#@Œ#I"out("STRING");F@‡#I"iout((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));FI"3out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[[	@ôiII"Ü    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    var = "STRING"
    var["STRING"].keys.sort.each do |arglist|;Fi[	@úi/I"Ç    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    if (var["STRING"] == "STRING") then
      var = "STRING";Fi[	@àiêI"Ü    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    var = "STRING"
    var["STRING"].keys.sort.each do |arglist|;Fi[@(@œ@F0I" ;F@H0I"@var ||= var;Fi[[	@biI"·      
      def function(arglist)
        @var, @var, @var, @var, @var = var, var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      ;Fi[	@iI"˜      
      def function(arglist)
        @var, @var, @var, @var, @var, @var, @var = var, var, var, var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      ;Fi[	@$iI"÷      
      def function(arglist)
        @var, @var, @var, @var = var, var, var, var
      end
      
      attr_reader(:gherkin_statement)
      
      def function(arglist)
        @var ||= var
      end
      ;Fi[@@@ +@!+I"if var["STRING"] then;F@ù3i[[	@ñiI"¨          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else;Fi[	@ôiI"¨          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else;Fi[	@úiI"¨          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else;Fi[@@@†4I"newobj;F@¢4@¡9i[[	@ñi I"5            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"));Fi[	@ôi)I"5            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"));Fi[	@úiI"5            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"));Fi[@(@œ@S0@T0@U0@V0i[[	@ÜiI"ñ        # depot.
        def function(arglist)
          "STRING"
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end;Fi[	@ÜiI"Î        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end;Fi[	@Üi I"Î        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end
        
        # Returns the command that will sync the given revision to the given
        # destination directory. The perforce client has a fixed destination so
        # the files must be copied from there to their intended resting place.
        def function(arglist)
          p4_sync(var, var, p4sync_flags)
        end;Fi[@(@œ@w0@x0@y0I"end;Fi[[	@~0i@I"          return var unless (:head == var)
          var = scm("STRING", repository)
          var = yield(var)
        end
        
        # Increments the given revision number and returns it.
        def function(arglist)
          (var.to_i + 1)
        end
        
        private;Fi[	@Üi7I"ê          return var if var.to_s =~ /^\d+$/
          var = scm(authentication, :changes, "STRING", "STRING", "STRING")
          yield(var)[/Change (\d+) on/, 1]
        end
        
        # Increments the given revision number and returns it.
        def function(arglist)
          (var.to_i + 1)
        end
        
        # Determines what the response should be for a particular bit of text;Fi[	@Ëi<I"Ä          var = YAML.load(var)
          raise("STRING") unless Hash.===(var)
          [(var["STRING"] or 0).to_i, (var["STRING"] or 0).to_i].max
        end
        
        # Increments the given revision number and returns it.
        def function(arglist)
          (var.to_i + 1)
        end
        
        # Determines what the response should be for a particular bit of text;Fi[@(@œ@À@∆@ñ"@–0i[[	@Wi6I"1          var.passive = true if passive_mode
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|
          create_remote_path(var, var);Fi[	@ﬁi*I"W        Net::SSH.start(ip, username, :password => (password), :port => (port)) do |arglist|
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|
          var.exec!("STRING");Fi[	@Zi(I"a        Net::SFTP.start(ip, username, :password => (password), :port => (port)) do |arglist|
          yield(var)
        end
      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function(arglist)
        var = remote_path_for(@var)
        connection do |arglist|
          create_remote_path(var, var);Fi[@@@@I"end;FI"end;Fi[[	@ñi]I"á    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@ôi^I"á    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@úiHI"á    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[@I"end;F@ü0I"newobj;F@á4@à4i[[	@ñiÙI"*    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!;Fi[	@ôi˝I"*    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!;Fi[	@úi„I"*    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!;Fi[@@:@ü0@:@á4I"out("STRING");Fi[[	@ñiI"      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var);Fi[	@ôiI"      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var);Fi[	@úiÛI"      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var);Fi[@@:I"end;FI"end;FI"out("STRING");FI"out("STRING");Fi[[	@ñiI"*            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj;Fi[	@ôi%I"*            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj;Fi[	@úiI"*            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj;Fi[@@:@1:@2:I"end;FI" ;Fi[[	@ñi3I"'            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING");Fi[	@ôi<I"'            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING");Fi[	@úi"I"'            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist)
    out("STRING");Fi[@I"var = fopen(var, "STRING");FI"putstream(fread(var, var));FI"fclose(var);FI"out("STRING");FI"end;Fi[[	@ñi I"P      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj;Fi[	@ôi	I"P      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj;Fi[	@úiÔI"N      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) if var["STRING"].nil?.!
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj;Fi[@@î4I"putType0(var);FI"	else;F@ö9@∫*i[[	@ñi	I"*    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING");Fi[	@ôiI"*    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING");Fi[	@úi¯I"*    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING");Fi[@@Ω*I"	else;F@+I"out("STRING");FI"out("STRING");Fi[[	@ñiI"ç        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"));Fi[	@ôiI"ç        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"));Fi[	@úiˇI"ç        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"));Fi[@@ +@!+@È9@ù3@û3i[[	@ñiI"á        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING");Fi[	@ôiI"á        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING");Fi[	@úiI"á        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING");Fi[@@†4I"newobj;F@¢4@¡9@)i[[	@ñi!I"=          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING");Fi[	@ôi*I"=          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING");Fi[	@úiI"=          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING");Fi[@@ö$@)@≥7@¥7I"end;Fi[[	@ñi(I"Ä          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then;Fi[	@ôi1I"Ä          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then;Fi[	@úiI"Ä          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then;Fi[@@ö$@õ$I"out("STRING");F@‡#@’9i[[	@ôiHI"|    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    var = "STRING";Fi[	@úi.I"ë    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    if (var["STRING"] == "STRING") then;Fi[	@àièI"|    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    var = "STRING";Fi[@(@œI"#:nodoc:;FI"def function(arglist);F@∏0I"
begin;Fi[[	@Ω0i!I"Ò        end
        after_flush_writes
        @var = {}
      end
      
      #:nodoc:
      def function(arglist)
        @var.each do |arglist|
          begin
            (log("STRING")
            FileUtils.rm(var) if File.exist?(var));Fi[	@i∏I"	      
      def function(arglist)
        s3_interface.buckets.create(bucket_name)
      end
      
      #:nodoc:
      def function(arglist)
        @var.each do |arglist|
          begin
            (log("STRING")
            var = (@var[var] or @var[:default]);Fi[	@i‘I"˛        end
        after_flush_writes
        @var = {}
      end
      
      #:nodoc:
      def function(arglist)
        @var.each do |arglist|
          begin
            (log("STRING")
            s3_bucket.objects[var.sub(/^\//, "STRING")].delete);Fi[@(@œ@–I"out("STRING");F@Õ$@Œ$i[[	@ñi7I"¸        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING");Fi[	@ôi@I"¸        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING");Fi[	@úi&I"¸        end
      end
    end
  end
  
  def function(arglist)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING");Fi[@(@œ@–@)@)@)i[[	@ñiI"    var = (@var.length + 1)
    var = var.gsub("STRING", "STRING")
    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -130, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end;Fi[	@ôiI"    Error("STRING") unless @var[var].nil?
    var = (@var.length + 1)
    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -120, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end;Fi[	@úiI"    var = (@var.length + 1)
    var = var.gsub("STRING", "STRING")
    @var[var] = { "STRING" => (var), "STRING" => "STRING", "STRING" => (var), "STRING" => -130, "STRING" => 40, "STRING" => (var), "STRING" => (var), "STRING" => (var) }
  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end;Fi[@)@)@)@)I"end;FI" ;Fi[[	@ñiI"F  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist)
    var = Big5_widths;Fi[	@ôiI"C  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist)
    var = "STRING";Fi[	@úiI"E  end
  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist)
    var = UHC_widths;Fi[@)@)@)I"end;FI" ;FI"def function(arglist);Fi[[	@ñiI"S  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist)
    var = Big5_widths
    var = "STRING";Fi[	@ôiI"S  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist)
    var = "STRING"
    var = SJIS_widths;Fi[	@úiI"R  
  def function(arglist)
    AddCIDFont(var, "STRING", var, var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
    AddCIDFont(var, "STRING", (var + "STRING"), var, var, var)
  end
  
  def function(arglist)
    var = UHC_widths
    var = "STRING";Fi[I"return super(var);FI"end;FI"end;FI" ;FI"def function(arglist);FI"var = 0;Fi[[	@ñi:I"œ    if (@var["STRING"] == "STRING") then
      return GetMBStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length;Fi[	@ôi*I"—    if (@var["STRING"] == "STRING") then
      return GetSJISStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length;Fi[	@úi)I"œ    if (@var["STRING"] == "STRING") then
      return GetMBStringWidth(var)
    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length;Fi[@”*I"end;FI"end;FI" ;FI"def function(arglist);FI"var = @var;Fi[[	@ñiTI"ı    if (@var["STRING"] == "STRING") then
      MBMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"];Fi[	@ôiII"˜    if (@var["STRING"] == "STRING") then
      SJISMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"];Fi[	@úiCI"ı    if (@var["STRING"] == "STRING") then
      MBMultiCell(var, var, var, var, var, var, var)
    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"];Fi[@*@%@%I"var = var.length;F@S(I"var = 0;Fi[[	@ñi\I"G    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then;Fi[	@ôiQI"G    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then;Fi[	@úiKI"G    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then;Fi[@%@%@%@S(I"var = 0;FI"if var then;Fi[[	@ñi]I"O    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING";Fi[	@ôiRI"O    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING";Fi[	@úiLI"O    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING";Fi[@(@œ@–@‡1@·1I"end;Fi[[	@Ê1iI"&        it("STRING") do |arglist|
          expect(last_request.env["STRING"]).to(be_kind_of(Hash))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      ;Fi[	@Ê1iI"/        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      ;Fi[	@Ê1iI"0        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
      
      def function(arglist)
        it("STRING") do |arglist|
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var))
        end
      end
    end;Fi[@S(I"var = 0;FI"if var then;F@\I"var = "STRING";FI"var = "STRING";Fi[[	@ñi`I"1    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else;Fi[	@ôiUI"1    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else;Fi[	@úiOI"1    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else;Fi[@(@œ@–@‘+I"var;FI"end;Fi[[	@<
i+I"ﬂ      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist);Fi[	@<
i0I"ﬂ      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist);Fi[	@<
i5I"›      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      # reduce 55 omitted;Fi[@\@]I"	else;F@a%I"@var = var;F@÷"i[[	@ñiòI".    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2);Fi[	@ôiìI".    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2);Fi[	@úiáI".    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2);Fi[@(@œ@–@ +I"var;FI"end;Fi[[	@<
i÷I"ˆ      def function(arglist)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      def function(arglist);Fi[	@<
i€I"’      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      def function(arglist);Fi[	@<
i‡I"”      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      def function(arglist)
        var = [var.first, var.last]
        var
      end
      
      # reduce 41 omitted;Fi[@«(@»(@…(I"end;FI"end;FI"var = -1;Fi[[	@ñiiI"3        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0;Fi[	@ôi^I"3        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0;Fi[	@úiXI"3        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0;Fi[@»(@…(I"end;FI"end;FI"var = -1;FI"var = 0;Fi[[	@ñijI"(      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0;Fi[	@ôi_I"(      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0;Fi[	@úiYI"(      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0;Fi[@…(I"end;FI"end;FI"var = -1;FI"var = 0;FI"var = 0;Fi[[	@ñikI")        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[	@ôi`I")        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[	@úiZI")        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[@(@œ@–@'2I"end;FI" ;Fi[[	@zi=I"·  
  def function(arglist)
    (alpha? or (beta? or rc?))
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") };Fi[	@ziAI"ı  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") };Fi[	@ziEI"Á  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    to_a.any? { |arglist| (var.to_s == "STRING") }
  end
  
  def function(arglist)
    return unless var.is_a?(Version);Fi[@á@à@âI"var = (var + 1);F@1I"	next;Fi[[	@ñiyI"      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end
      if var.! then;Fi[	@ôinI"      if (var == 10) then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end
      if (var < 128) then;Fi[	@úihI"      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next
      end
      if var.! then;Fi[@á@à@â@M;@1I"	else;Fi[[	@ñiçI">          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)))
      end;Fi[	@ôiáI"H          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + var)
        var = var if (var >= 128);Fi[	@úi|I">          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)))
      end;Fi[@(@œ@–@i)I"var.write("STRING");F@k)i[[	@iàI"class TextExecutableTests < Test::Unit::TestCase
  def function(arglist)
    (HOMEBREW_PREFIX / "STRING").unlink
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	@iòI"    assert_equal([], var.archs)
    assert((var.arch == :dunno))
    assert_match(/text executable/, file(var))
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	@i®I"    assert_equal([], var.archs)
    assert((var.arch == :dunno))
    assert_match(/text executable/, file(var))
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[@(@œ@–I"!var = Pathname.new("STRING");F@r!I"assert(var.i386?.!);Fi[[	@i$I"5    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!)
    assert_match(/Mach-O dynamically linked shared library/, file(var))
  end
  
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!);Fi[	@i\I"    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match(/Mach-O bundle/, file(var))
  end
  
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!);Fi[	@ijI"!    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match(/Mach-O 64-bit bundle/, file(var))
  end
  
  def function(arglist)
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[@]%I"var = (var + 1);FI"var = -1;FI"var = var;FI"var = 0;FI"var = (var + 1);Fi[[	@ñiwI"T      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next;Fi[	@ôilI"B      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var
      if (var == 10) then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next;Fi[	@úifI"T      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], var, 2, var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
        next;Fi[@]%I"	else;F@‡*@·*I"end;FI"var = -1;Fi[[	@ñiàI"í      if (var > var) then
        if ((var == -1) or (var == var)) then
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0;Fi[	@ôiÇI"Ñ      if (var > var) then
        if ((var == -1) or (var == var)) then
          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0;Fi[	@úi|I"í      if (var > var) then
        if ((var == -1) or (var == var)) then
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0;Fi[@]%@·*I"end;FI"var = -1;FI"var = var;FI"var = 0;Fi[[	@ñiäI"ä          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2);Fi[	@ôiÑI"|          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2);Fi[	@úi~I"ä          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2);Fi[@(@œ@–@d2I"end;FI" ;Fi[[	@[iNI"  def function(arglist)
    return "STRING" unless MacOS::XQuartz.installed?
    return ("STRING" + describe_path(MacOS::XQuartz.prefix))
  end
  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    describe_path(which("STRING"));Fi[	@[iRI"≈  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    describe_path(which("STRING"));Fi[	@[iVI"Ø  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    describe_path(which("STRING"))
  end
  
  def function(arglist)
    "STRING";Fi[@Û4@Ù4I"@var = @var;F@ 3@À3@Ã3i[[	@ñiÀI"§      var = var if (var.! or (var.chr == "STRING"))
      var = (var + (var ? (var[var.chr]) : (1000) or 0))
      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1);Fi[	@ôi”I"O        end
      end
      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + var)
            var = (var + 1);Fi[	@úi∫I"§      var = var if (var.! or (var.chr == "STRING"))
      var = (var + (var ? (var[var.chr]) : (1000) or 0))
      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1);Fi[@·*I"end;FI"var = -1;FI"var = var;FI"var = 0;FI"var = (var + 1);Fi[[	@ñiãI"[          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else;Fi[	@ôiÖI"[          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else;Fi[	@úiI"[          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else;Fi[@·*@™;@´;@¨;@≠;@ãi[[	@ñiŸI"x          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var);Fi[	@ôi·I"x          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var);Fi[	@úi»I"x          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var);Fi[@Õ4@Œ4I"if (var == 1) then;FI"@var = @var;FI"	else;F@a%i[[	@ñiñI"4        var = (var + (var ? (1) : (2)))
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var);Fi[	@ôiëI".        var = var if (var >= 128)
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var);Fi[	@úiÖI"4        var = (var + (var ? (1) : (2)))
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var);Fi[@(@œ@–@Å)@Ç)I"super(var);Fi[[	@`ijI"¢    var = { "STRING" => "STRING", "STRING" => "STRING", "STRING" => "STRING", "STRING" => "STRING" }
    var = Hash[var.map { |arglist| [var[var], var] }]
    self.attributes.merge(var) { |arglist| var.blank? ? (var) : (var) }
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var));Fi[	@`isI"˜    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var));Fi[	@`i|I"˜    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var));Fi[@ÿ"I"end;FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	@ñiüI"◊        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var);Fi[	@ôiöI"Ÿ        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      SJISWrite(var, var, var, var);Fi[	@úiéI"◊        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var);Fi[@(@œ@–@í)I"end;FI" ;Fi[[	@ï)i9I"  #should be in relayable (pending on fixing Message)
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist)
    if (self.post.author == var.person) then;Fi[	@)i*I"±  
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist)
    false;Fi[	@ö)i1I"›  #sign comment as post owner
  def function(arglist)
    self.author.diaspora_handle
  end
  
  def function(arglist)
    self.author = Webfinger.new(var).fetch
  end
  
  def function(arglist)
    self.conversation.guid;Fi[@(@œ@–@üI"	self;FI"end;Fi[[	@πiI"‡      var = OmniAuth::Form.new(var)
      (var.arity > 0) ? (yield(var)) : (var.instance_eval(&var))
      var
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist);Fi[	@πiI"Ø    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist);Fi[	@πi?I"¥      self.instance_eval(&var)
      (@var << "STRING")
      self
    end
    
    def function(arglist)
      (@var << "STRING")
      self
    end
    
    def function(arglist);Fi[@¶@ß@Õ3@Œ3I"	next;FI"end;Fi[[	@ñiœI"í          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);Fi[	@úiæI"í          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);Fi[	@üiØI"Ö          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next
          end
          var = (var + 1) if (var == var)
          self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var);Fi[@(@œ@–I"broadcast(var);FI"end;FI" ;Fi[[	@™iI"ˇ      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) };Fi[	@™i$I"Â      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) }
      end
      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var, var);Fi[	@™iiI"Â      
      def function(arglist)
        broadcast(var, var)
      end
      
      def function(arglist)
        broadcast(var)
      end
      
      def function(arglist)
        broadcast(var) { |arglist| var.accept(self) };Fi[@˘%I"	else;F@+@+I"end;FI"var = -1;Fi[[	@ñi÷I"y            next
          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0;Fi[	@ôiﬁI"k            next
          end
          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0;Fi[	@úi≈I"y            next
          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0;Fi[@˘%@+I"end;FI"var = -1;FI"var = var;FI"var = 0;Fi[[	@ñiÿI"ã          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var;Fi[	@ôi‡I"}          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var;Fi[	@úi«I"ã          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var;Fi[@Ù4I"@var = @var;F@ 3@À3@Ã3@Õ3i[[	@ñiÃI"Å      var = (var + (var ? (var[var.chr]) : (1000) or 0))
      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next;Fi[	@úiªI"Å      var = (var + (var ? (var[var.chr]) : (1000) or 0))
      if (var > var) then
        if ((var == -1) or (var == var)) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next;Fi[	@üi¨I"a      var = (var + GetCharWidth(var, var))
      if (var > var) then
        if (var == -1) then
          if (@var > @var) then
            @var = @var
            @var = (@var + var)
            var = ((@var - @var) - @var)
            var = (((var - (2 * @var)) * 1000) / @var)
            var = (var + 1)
            var = (var + 1)
            next;Fi[I"if var.!=(var) then;F@µ4I"end;FI"end;FI" ;FI"private;Fi[[	@ñiËI"·        var = (var + (var ? (1) : (2)))
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist);Fi[	@ôiÒI"€        var = var if (var >= 128)
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist);Fi[	@úi◊I"·        var = (var + (var ? (1) : (2)))
      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist);Fi[@µ4I"end;FI"end;FI" ;FI"private;FI" ;Fi[[	@ñiÈI"»      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var;Fi[	@ôiÚI"»      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var;Fi[	@úiÿI"»      end
    end
    if var.!=(var) then
      Cell(((var * @var) / 1000.0), var, var[var, (var - var)], 0, 0, "STRING", var, var)
    end
  end
  
  private
  
  def function(arglist)
    var = @var;Fi[@""I"out("STRING");FI"end;F@ü0I"newobj;F@á4i[[	@ñiÛI"    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var);Fi[	@ôi¸I"    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var);Fi[	@úi‚I"    var = @var
    @var.each do |arglist|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var);Fi[@à4@â4@ä4@ç9@é9@&&i[[	@ñi˘I"ç    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING");Fi[	@ôiI"ç    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING");Fi[	@úiËI"ã    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) if var["STRING"].nil?.!
      out("STRING");Fi[@ç9@ª*I"out("STRING");F@Ω*I"	else;F@+i[[	@ñiI"F        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING");Fi[	@ôiI"F        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING");Fi[	@úi˝I"F        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING");Fi[@&&@'&@(&@ +@!+@È9i[[	@ñiI"≥          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"));Fi[	@ôiI"≥          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"));Fi[	@úiI"≥          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"));Fi[@(@œ@–I"var = @var;FI"var = @var;F@*i[[	@ñiVI"ı    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var);Fi[	@ôiKI"ı    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var);Fi[	@úiEI"ı    else
      super(var, var, var, var, var, var, var)
    end
  end
  
  def function(arglist)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var);Fi[@(@œ@–I"@var.language;FI"end;FI" ;Fi[[	@!i>I"˜      
      def function(arglist)
        @var.visit_scenario_name(var, var)
      end
      
      def function(arglist)
        @var.language
      end
      
      class ExampleRow < Cells
        class InvalidForHeaderRowError < NoMethodError;Fi[	@!i¶I"≤        
        def function(arglist)
          "STRING"
        end
        
        def function(arglist)
          @var.language
        end
        
        private
        ;Fi[	@$iVI".      
      def function(arglist)
        @var ||= @var.file_colon_line(@var) unless @var.nil?
      end
      
      def function(arglist)
        @var.language
      end
      
      def function(arglist)
        @var ||= file_colon_line.gsub(/\//, "STRING").gsub(/\./, "STRING").gsub(/:/, "STRING");Fi[@G:@H:I"fclose(var);FI"out("STRING");FI"end;FI" @var.each_pair do |arglist|;Fi[[	@ñiI"D      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var;Fi[	@ôi
I"D      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var;Fi[	@úiI"B      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) if var["STRING"].nil?.!
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var;Fi[@H:I"fclose(var);FI"out("STRING");FI"end;F@Ä<I"newobj;Fi[[	@ñiI"2      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING");Fi[	@ôiI"2      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING");Fi[	@úiÒI"0      out((("STRING" + var["STRING"]) + "STRING")) if var["STRING"].nil?.!
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING");Fi[I"fclose(var);FI"out("STRING");FI"end;F@Ä<I"newobj;FI"@var[var]["STRING"] = @var;Fi[[	@ñiI"      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then;Fi[	@ôiI"      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then;Fi[	@úiÚI"      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then;Fi[@î4I"putType0(var);FI"	else;F@ö9@∫*@ª*i[[	@ñi
I"K      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING");Fi[	@ôiI"K      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING");Fi[	@úi˘I"K      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING");Fi[@î4I"out("STRING");F@Ω*I"	else;F@+I"out("STRING");Fi[[	@ñiI"d      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"));Fi[	@ôiI"d      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"));Fi[	@úi˛I"d      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"));Fi[I"putType0(var);FI"	else;F@ö9@∫*@ª*I"out("STRING");Fi[[	@ñiI"K      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else;Fi[	@ôiI"K      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else;Fi[	@úi˙I"K      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else;Fi[@Ω*I"	else;F@+I"out("STRING");FI"out("STRING");F@ +i[[	@ñiI"ë        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then;Fi[	@ôiI"ë        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then;Fi[	@úi I"ë        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then;Fi[@ +@!+@È9@ù3@û3I"	else;Fi[[	@ñiI"ä          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end;Fi[	@ôiI"ä          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end;Fi[	@úiI"ä          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end;Fi[@!+@È9@ù3@û3I"	else;F@*+i[[	@ñiI"n          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end;Fi[	@ôi I"n          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end;Fi[	@úiI"n          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end;Fi[@û3I"	else;F@*+I"end;FI"end;FI"end;Fi[[	@ñiI"B          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING");Fi[	@ôi#I"B          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING");Fi[	@úi	I"B          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING");Fi[@†4I"newobj;F@¢4@¡9@)@)i[[	@ñi"I"@        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj;Fi[	@ôi+I"@        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj;Fi[	@úiI"@        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj;Fi[@)@)I"out("STRING");FI"newobj;F@)@≥7i[[	@ñi&I"{          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end;Fi[	@ôi/I"{          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end;Fi[	@úiI"{          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end;Fi[@)I"out("STRING");FI"newobj;F@)@≥7@¥7i[[	@ñi'I"à          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"];Fi[	@ôi0I"à          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"];Fi[	@úiI"à          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"];Fi[@)@˛<I"end;FI"end;FI"end;FI"end;Fi[[	@ñi2I"+          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist);Fi[	@ôi;I"+          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist);Fi[	@úi!I"+          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist);Fi[@¥7I"end;F@4I"if var then;F@q-I"end;Fi[[	@ñi,I"π          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING");Fi[	@ôi5I"π          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING");Fi[	@úiI"π          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING");Fi[@q-I"end;F@s-I"out("STRING");FI"end;FI"end;Fi[[	@ñi0I"<          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end;Fi[	@ôi9I"<          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end;Fi[	@úiI"<          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end;Fi[@(@œ@–@Ø2I"end;FI" ;Fi[[	@iAI"◊      
      def function(arglist)
        source_tags.map { |arglist| var.name }
      end
      
      def function(arglist)
        @var.accept_hook?(var)
      end
      
      def function(arglist)
        init;Fi[	@!iI"˛          var.configuration.expand? ? (var.accept(var)) : (var.visit_table_row(var))
        end
        nil
      end
      
      def function(arglist)
        @var.accept_hook?(var)
      end
      
      def function(arglist)
        @var.source_tags;Fi[	@!iÅI"œ              end
            end
          end
        end
        
        def function(arglist)
          @var.accept_hook?(var)
        end
        
        def function(arglist)
          (@var or @var);Fi[@(@œ@–I"var = 0;F@˚)I"var = var.length;Fi[[	@ñi<I"®    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do;Fi[	@ôi,I"®    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do;Fi[	@úi+I"®    else
      return super(var)
    end
  end
  
  def function(arglist)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do;Fi[@(@œ@–I"	self;FI"end;FI" ;Fi[[	I"+data//bundler_proj/bundler/spec_set.rb;TiBI"ï      @var = nil
      @var = nil
      var
    end
    
    def function(arglist)
      self
    end
    
    def function(arglist)
      sorted.dup;Fi[	I"3data//cucumber_prok/cucumber/ast/doc_string.rb;TiI"—      def function(arglist)
        @var = var
        super(var)
      end
      
      def function(arglist)
        self
      end
      
      def function(arglist)
        return if Cucumber.wants_to_quit;Fi[	@Èi%I"Æ      def function(arglist)
        @var = var
        self
      end
      
      def function(arglist)
        self
      end
      
      alias :arguments :argument
      ;Fi[@ë@íI"	else;F@îI"end;FI"@var = @var.!=(@var);Fi[[	@üiI"0  end
  
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    @var = @var.!=(@var)
    out(@var) if (@var > 0)
  end;Fi[	@üiI"  end
  
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    @var = @var.!=(@var)
  end
  ;Fi[	@àiÀI"é  	# @see SetDrawColor(), SetTextColor(), Rect(), Cell(), MultiCell()
  	#
  def function(arglist)
    if (((var == 0) and ((var == 0) and (var == 0))) or (var == -1)) then
      @var = sprintf("STRING", (var / 255.0))
    else
      @var = sprintf("STRING", (var / 255.0), (var / 255.0), (var / 255.0))
    end
    @var = @var.!=(@var)
    out(@var) if (@var > 0)
    @var = [var, var, var] if var;Fi[@(@œ@–@¡
I"@var = var;FI"@var = var;Fi[[	@∏i!I";            var = File.expand_path(var)
            return var.gsub("STRING", "STRING") if var.include?(var)
          end
        end
        
        def function(arglist)
          @var = var
          @var = var
          @var = var
          @var = var
          @var["STRING"] ||= Sass::Script::Bool.new(true);Fi[	@ñiI"»        def function(arglist)
          new(var).execute!
        end
      end
      
      def function(arglist)
        @var = var
        @var = var
        @var = var
        @var = nil
      end;Fi[	@æi#I"       var.text
      var.html
    end
  end
  
  def function(arglist)
    @var = var
    @var = var
    @var = var
    @var = var
    var = { :to => (var), :from => (AppConfig.mail.sender_address), :subject => (I18n.t("STRING", :name => (@var.name))), :host => (AppConfig.pod_uri.host) };Fi[@(@œ@–@¡
@W=I"end;Fi[[	@˝i'I"Ÿ      
      def self.matches_condition?(arglist)
        raise(NotImplemented, "STRING")
      end
      
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist);Fi[	@ÄirI"
            var = var[1]
            var = (var[3] or "STRING")
            InternalRevision.new(var, var)
          end
          
          def function(arglist)
            @var = var
            @var = var
          end
          
          def function(arglist);Fi[	@i
I"⁄    class TemplateContext
      def self.ctx(arglist)
        new(*var).send(:get_binding)
      end
      
      def function(arglist)
        @var = var
        @var = var
      end
      
      def function(arglist);Fi[I"# @since 1.1;FI"#;FI"def function(arglist);FI"@var = var;FI"end;FI" ;Fi[[	@ài5I"±  	#
   	# Set header font.
  	# @param array :font font
  	# @since 1.1
  	#
  def function(arglist)
    @var = var
  end
  
  alias_method(:set_header_font, :SetHeaderFont)
  ;Fi[	@ài@I"∞  #
   	# Set footer font.
  	# @param array :font font
  	# @since 1.1
  	#
  def function(arglist)
    @var = var
  end
  
  alias_method(:set_footer_font, :SetFooterFont)
  ;Fi[	@àiKI"∏  #
   	# Set language array.
  	# @param array :language
  	# @since 1.1
  	#
  def function(arglist)
    @var = var
  end
  
  alias_method(:set_language_array, :SetLanguageArray)
  ;Fi[@¨(@≠(@Æ(@Ø(I"
retry;FI"rescue;Fi[[	@≤(iI"`          var = Iconv.new("STRING", "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end;Fi[	@≤(i?I"[          var = Iconv.new("STRING", var)
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end;Fi[	@≤(i}I"[          var = Iconv.new(var, "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end;Fi[@≠(@Æ(@Ø(I"
retry;FI"rescue;F@N1i[[	@≤(iI"F          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var;Fi[	@≤(i@I"F          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var;Fi[	@≤(i~I"F          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var;Fi[@Æ(@Ø(I"
retry;FI"rescue;F@N1I"end;Fi[[	@≤(iI"9          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end;Fi[	@≤(iAI"9          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end;Fi[	@≤(iI"9          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end;Fi[@Ø(I"
retry;FI"rescue;F@N1I"end;FI"var = var;Fi[[	@≤(iI"3            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end;Fi[	@≤(iBI"3            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end;Fi[	@≤(iÄI"3            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end;Fi[I"var = max_value;FI"var = min_value;F@‘(@’(@÷(@É3i[[	@Ÿ(iI"H      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = [];Fi[	@‹(iI"N      protected
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = [];Fi[	@ziRI"H      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = [];Fi[I"var = min_value;F@‘(@’(@÷(@É3@Ñ3i[[	@Ÿ(iI"w      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var));Fi[	@‹(iI"w      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var));Fi[	@ziSI"w      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var));Fi[@’(@÷(@É3@Ñ3I"var = [];F@-i[[	@Ÿ(iI"ù        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var;Fi[	@‹(iI"ù        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var;Fi[	@ziUI"ù        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var;Fi[@÷(@É3@Ñ3I"var = [];F@-@Ä-i[[	@Ÿ(iI"è        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end;Fi[	@‹(iI"è        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end;Fi[	@ziVI"è        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end;Fi[@É3@Ñ3I"var = [];F@-@Ä-I"return var;Fi[[	@Ÿ(iI"~        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      ;Fi[	@‹(iI"~        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      ;Fi[	@ziWI"~        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      ;Fi[@Ñ3I"var = [];F@-@Ä-I"return var;FI"end;Fi[[	@Ÿ(i I"Ä        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist);Fi[	@‹(iI"Ä        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist);Fi[	@ziXI"Ä        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist);Fi[@-@Ä-I"return var;FI"end;FI" ;FI"def function(arglist);Fi[[	@Ÿ(i"I"K        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist)
        (var / 2.0)
      end;Fi[	@‹(iI"M        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist)
        @var[:fields]
      end;Fi[	@ziZI"n        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist| (var << var) }
        return var
      end
      
      def function(arglist)
        var = { :x => 0, :y => 0 }
        var[:x] = (var * var);Fi[I"# == Example;FI"#;FI"#   class Vehicle;F@¡,@è,@ê,i[[	@î,i>I"M    # *after* callback with only affect that event's transition.  Other
    # transitions will continue to run their own callbacks.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       ;Fi[	@î,ipI"    # 
    # See StateMachine::InstanceMethods#fire_events for more information.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       ;Fi[	@ài˙I"O    # Rolls back changes made to the object's state via this transition.  This
    # will revert the state back to the +from+ value.
    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end;Fi[@(@œ@–@‘&@’&I"end;Fi[[	@iÍI"’        class Type
          def function(arglist)
            # do nothing
          end
          
          def function(arglist)
            var
          end
        end
        
        class Identity < Type;Fi[	@˛iI"’        class Type
          def function(arglist)
            # do nothing
          end
          
          def function(arglist)
            var
          end
        end
        
        class Identity < Type;Fi[	@˜iI"p    
    def function(arglist)
      var
    end
    
    def function(arglist)
      var
    end
  end
end;Fi[@I"end;FI" ;F@@¢(I"end;Fi[[	@?	i{I"ÿ        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist);Fi[	@?	iI"ÿ        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist);Fi[	@?	iÉI"ÿ        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist)
          colour(var, "STRING")
        end
        
        def function(arglist);Fi[I"#   class Vehicle;F@†@z,I"#     end;FI"#   end;FI"#;Fi[[	@,iI"-    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   vehicle = Vehicle.new               # => #<Vehicle:0xb7c464b0 @state="parked">;Fi[	@,i)I"ﬂ    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   ;Fi[	@,iDI"ﬂ    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   ;Fi[@>@p,I"#;F@X,@Y,I"#;Fi[[	@®iI",        # 
        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do;Fi[	@®iRI",        # 
        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do;Fi[	@®ivI"+        # 
        # == Example
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do;Fi[@(@œ@–I"@var.clear;FI"end;FI" ;Fi[[	@¢iºI"≥      
      def function(arglist)
        "STRING"
      end
      
      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        @var.clear;Fi[	@¢i¿I"µ      
      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        @var.clear;Fi[	@¢iƒI"∏      
      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        @var.clear
      end
      
      def function(arglist)
        class << @var;Fi[@†@z,I"#     end;FI"#   end;FI"#;FI".#   states = Vehicle.state_machine.states;Fi[[	@,iI"/    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   vehicle = Vehicle.new               # => #<Vehicle:0xb7c464b0 @state="parked">
    #   ;Fi[	@,i*I")    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   
    #   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">;Fi[	@,iEI")    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   
    #   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">;Fi[@(@œ@–@@I"end;Fi[[	@i2I"±          end
          owner.connection.delete(var.where(var).compile_delete)
        end
      end
      
      def function(arglist)
        false
      end
    end
  end
end;Fi[	@êi=I"{    
    def function(arglist)
      # do nothing
    end
    
    def function(arglist)
      false
    end
  end
end;Fi[	@1iaI"√      
      def function(arglist)
        ((var.index(File::SEPARATOR) == 0) or (var.index("STRING") == 1))
      end
      
      def function(arglist)
        false
      end
    end
  end
end;Fi[@_@`@aI"#;F@»-@…-i[[	@î,iBI"    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end;Fi[	@î,itI"    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end;Fi[	@$iüI"    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end;Fi[@`I"#       end;FI"#     end;FI"#   end;FI"#;FI"#   class VehicleObserver;Fi[[	@®iI"1        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver
        #     include DataMapper::Observer
        #     ;Fi[	@®iZI"1        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver
        #     include DataMapper::Observer
        #     ;Fi[	@®i~I"L        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver
        #     after_transition_failure do |transition|
        #       # log failure;Fi[@`@G>I"#;F@»-@…-I"#       end;Fi[[	@î,iCI"    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #     ;Fi[	@î,iuI"    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #     ;Fi[	@$i†I"#    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #   end;Fi[I"#       event :disable do;FI"%#         transition all => :off;FI"#       end;FI"#     end;FI"#   end;FI"#;Fi[[	@î,iPI"v    #         transition all => :active
    #       end
    #       
    #       event :disable do
    #         transition all => :off
    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                         # => #<Vehicle:0xb7c02850 @state="parked", @alarm_state="active">
    #   vehicle.state                                 # => "parked";Fi[	@î,i}I"r    #         transition all => :active
    #       end
    #       
    #       event :disable do
    #         transition all => :off
    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                         # => #<Vehicle:0xb7c02850 @state="parked", @alarm_state="active">
    #   vehicle.fire_events(:ignite, :disable_alarm)  # => true;Fi[	@$iîI"O    #     end
    #     
    #     state_machine :alarm_state, :initial => :active do
    #       event :disable do
    #         transition all => :off
    #       end
    #     end
    #   end
    #   
    #   # Fire +ignite+ event directly
    #   vehicle = Vehicle.create    # => #<Vehicle id=1 state="parked" alarm_state="active">;Fi[@k(@î(I"end;FI"
super;FI"end;FI"end;Fi[[	@#ihI"@          (::ActiveRecord::VERSION::MAJOR == 3) and (::ActiveRecord::VERSION::MINOR == 0)
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end
    end
  end;Fi[	@&iPI"8          defined? ::MongoMapper::Version and ::MongoMapper::Version =~ /^0\.9\./
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end
    end
  end;Fi[	@∞iI"E          define_helper(:instance, "STRING", "STRING", (26 + 1))
        end
        def function(arglist)
          if (action_hook == :save) then
            owner_class.set_callback(:save, :after, "STRING", :prepend => (true))
          end
          super
        end
      end
      
      version("STRING") do |arglist|;Fi[@k(@l(I"	else;FI"
super;FI"end;FI"end;Fi[[	@Ûi<I"V      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action;Fi[	@˘i5I"V      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action;Fi[	@¸i9I"V      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action;Fi[@(@œ@–@ÂI"end;FI" ;Fi[[	@W
i4I"√          @var = proc { |arglist| extending(var) }
        end
      end
    end
    
    def function(arglist)
      @var ||= "STRING"
    end
    
    def function(arglist)
      var = "STRING";Fi[	@êi)I"ô    
    def function(arglist)
      false
    end
    
    def function(arglist)
      @var ||= "STRING"
    end
    
    def function(arglist)
      {};Fi[	@’i;I"    
    def function(arglist)
      (super or @var.respond_to?(*var))
    end
    
    def function(arglist)
      @var ||= "STRING"
    end
    
    def function(arglist)
      @var ||= [name, version, source, platform, dependencies].hash;Fi[@B+@C+@D+I"end;FI" ;F@ñ5i[[	@ÛiI"v      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist)
        if owner_class.connected? and (owner_class.table_exists? and var = owner_class.columns_hash[attribute.to_s]) then;Fi[	@˘iI"2      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist)
        attribute_key and attribute_key.default_value;Fi[	@¸iI"0      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end
      
      # Gets the db default for the machine's attribute
      def function(arglist)
        attribute_field and attribute_field.default;Fi[@l/@m/@n/@l(I"	else;FI"
super;Fi[[	@Ûi:I"]        define_helper(:instance, "STRING", "STRING", (468 + 1))
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end;Fi[	@˘i3I"+        super
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end;Fi[	@¸i7I"+        super
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end;Fi[@l(I"	else;FI"
super;FI"end;FI"end;FI" ;Fi[[	@Ûi=I"k      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist);Fi[	@˘i6I"k      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist);Fi[	@¸i:I"k      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist);Fi[@*@ˆ+@^(I"end;FI" ;F@a(i[[	@ÛiCI"a        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist);Fi[	@˘i<I"a        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist);Fi[	@¸i@I"a        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist);Fi[@^(I"end;FI" ;F@a(@b(I"def function(arglist);Fi[[	@ÛiEI"ï      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist)
        create_scope(var, lambda { |arglist| ["STRING", var] })
      end;Fi[	@˘i>I"¡      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist)
        define_scope(var, lambda do |arglist|
          { :conditions => ({ attribute => ({ "STRING" => (var) }) }) };Fi[	@¸iBI"Æ      
      # Runs state events around the machine's :save action
      def function(arglist)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(arglist)
        define_scope(var, lambda { |arglist| { attribute => ({ "STRING" => (var) }) } })
      end;Fi[@p,I"#;F@X,@Y,I"#;F@$i[[	@®iI"Q        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling;Fi[	@®iSI"Q        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling;Fi[	@®iwI"P        # == Example
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling;Fi[@X,@Y,I"#;F@$@$@$i[[	@®iI"V        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end;Fi[	@®iUI"V        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end;Fi[	@®iyI"V        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end;Fi[@Y,I"#;F@$@$@$I"#       end;Fi[[	@®iI"L        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end;Fi[	@®iVI"L        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end;Fi[	@®izI"L        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end;Fi[@G'@@,@A,@y(I"end;FI"end;Fi[[	@A$iI"=      
      # A collection of tweets that mention a user
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      
      # A collection containing the mentioned user;Fi[	@Å(iI"f    class Reply < Twitter::Action::Tweet
      # A collection of tweets that reply to a user
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      
      # A collection that contains the replied-to tweets;Fi[	@D$iI"O    class Retweet < Twitter::Action::Tweet
      # A collection of retweets
      #
      # @return [Array<Twitter::Tweet>]
      def function(arglist)
        @var = Array(@var[:target_objects]).map do |arglist|
          Twitter::Tweet.fetch_or_new(var)
        end
      end
      
      # A collection containing the retweeted user;Fi[@(@œ@–I"raise("STRING");FI"end;FI"end;Fi[[	@µ+iI"¨    module ClassMethods
      def function(arglist)
        raise("STRING")
      end
      
      def function(arglist)
        raise("STRING")
      end
    end
  end
end;Fi[	@q3i{I"è      else
        ::OmniAuth.config.path_prefix = var
      end
    end
    
    def function(arglist)
      raise("STRING")
    end
  end
end;Fi[	@πi<I"†              end
            end
          end
        end
        
        def function(arglist)
          raise("STRING")
        end
      end
    end
  end;Fi[@(@œ@ç@é@H/@I/i[[	@ç(i˝I"R      #     Twitter.list_remove_members(7505382, 8863586, [813286, 18755393])
      def function(arglist)
        list_from_response_with_users(:post, "STRING", var)
      end
      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>];Fi[	I"5data//twitter_proj/twitter/api/places_and_geo.rb;TipI"¿      #   Twitter.place_create(:name => "@sferik's Apartment", :token => "22ff5b1f7159032cf69218c4d8bb78bc", :contained_within => "41bcb736f84a799e", :lat => "37.783699", :long => "-122.393581")
      def function(arglist)
        object_from_response(Twitter::Place, :post, "STRING", var)
      end
      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param params [Hash]
      # @return [Array];Fi[	@1i˚I"4        var.flatten.threaded_map do |arglist|
          object_from_response(Twitter::OEmbed, :get, "STRING", var.options)
        end
      end
      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::Tweet>];Fi[@e'@f'@g'I" # @return [Twitter::Cursor];FI"'# @overload friend_ids(options={});FI"[#   Returns an array of numeric IDs for every user the authenticated user is following;Fi[[	I"<data//twitter_proj/twitter/api/friends_and_followers.rb;TiI"      include(Twitter::API::Utils)
      
      # @see https://dev.twitter.com/docs/api/1.1/get/friends/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.;Fi[	@?i I"J      # Returns a cursored collection of user objects for users following the specified user.
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/followers/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.;Fi[	@?iI"y      # Returns a cursored collection of user objects for every user the specified user is following (otherwise known as their "friends").
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.;Fi[@(@œ@ç@é@ÌI"4# Establishes a connection to the remote server;Fi[[	@Wi"I"Ë        @var ||= false
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server
      #
      # Note:;Fi[	@ﬁiI"?        @var ||= "STRING"
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server
      # and yields the Net::SSH connection.
      # Net::SCP will use this connection to transfer backups;Fi[	@ZiI"N        @var ||= "STRING"
        instance_eval(&var) if block_given?
        @var = path.sub(/^\~\//, "STRING")
      end
      
      private
      
      ##
      # Establishes a connection to the remote server
      def function(arglist)
        Net::SFTP.start(ip, username, :password => (password), :port => (port)) do |arglist|;Fi[@e'@f'@g'I";# @param options [Hash] A customizable set of options.;FI"Í# @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.;FI""# @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.;Fi[[	@˙>iI"˛      #
      # @see https://dev.twitter.com/docs/api/1.1/get/geo/reverse_geocode
      # @note This request is an informative call and will deliver generalized results about geography.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :accuracy ('0m') A hint on the "region" in which to search. If a number, then this is a radius in meters, but it can also take a string that is suffixed with ft to specify feet. If coming from a device, in practice, this value is whatever accuracy the device has measuring its location (whether it be coming from a GPS, WiFi triangulation, etc.).
      # @option options [String] :granularity ('neighborhood') This is the minimal granularity of place types to return and must be one of: 'poi', 'neighborhood', 'city', 'admin' or 'country'.;Fi[	@˙>i2I"ø      # Search for places that can be attached to a {Twitter::API::Tweets#update}
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/geo/search
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :query Free-form text to match against while executing a geo-based query, best suited for finding nearby locations by name.
      # @option options [String] :ip An IP address. Used when attempting to fix geolocation based off of the user's IP address.;Fi[	@˙>iLI"õ      #
      # @see https://dev.twitter.com/docs/api/1.1/get/geo/similar_places
      # @note Conceptually, you would use this method to get a list of known places to choose from first. Then, if the desired place doesn't exist, make a request to {Twitter::API::PlacesAndGeo#place} to create a new one. The token contained in the response is the token necessary to create a new place.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :name The name a place is known as.
      # @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.;Fi[@f'@g'@ˇ>@ ?@?I"#;Fi[[	@?iI"      
      # @see https://dev.twitter.com/docs/api/1.1/get/friends/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@?iI"       #
      # @see https://dev.twitter.com/docs/api/1.1/get/followers/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@?i I""      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[@(@œ@ç@é@ÍI"# do nothing;Fi[[	@iaI"√            add_procs
            yield(@var) if block_given?
          end
        end
        
        private
        
        def function(arglist)
          # do nothing
        end
        ;Fi[	@’iCI"∫    
    def function(arglist)
      @var ||= [name, version, source, platform, dependencies].hash
    end
    
    private
    
    def function(arglist)
      # do nothing
    end
    ;Fi[	@•iI"Î          else
            private_methods.any? { |arglist| [var.to_s, var.to_sym].include?(var) }.!
          end
        end
        
        private
        
        def function(arglist)
          # do nothing
        end
      end;Fi[@g'@ˇ>@ ?@?I"#;FI"=#   @param options [Hash] A customizable set of options.;Fi[[	@?iI"U      # @see https://dev.twitter.com/docs/api/1.1/get/friends/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's friends' IDs;Fi[	@?iI"ö      # @see https://dev.twitter.com/docs/api/1.1/get/followers/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[	@?i!I"ú      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[@(@œ@a@b@B+@C+i[[	@ÛiI"      def self.extended(arglist)
        require("active_record/version")
        super
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end;Fi[	@˘iI"      
      def self.matching_ancestors(arglist)
        ["STRING"]
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end;Fi[	@¸iI"˝      def self.extended(arglist)
        require("mongoid/version")
        super
      end
      
      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function(arglist)
        (action == :save)
      end;Fi[@(@œ@ß@æ3I"# type.;FI"#;Fi[[	@©iI"      # @since 3.0.0
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Float.mongoize("123.11");Fi[	@ºi•I"J      def function(arglist)
        var = var.fields[var.to_s]
        var ? (var.mongoize(var)) : (var)
      end
      
      module ClassMethods
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   Hash.mongoize([ 1, 2, 3 ]);Fi[	@Æi*I""      # @since 2.2.1
      def function(arglist)
        true
      end
      
      module ClassMethods
        # Turn the object from the ruby type we deal with to a Mongo friendly
        # type.
        #
        # @example Mongoize the object.
        #   BigDecimal.mongoize("123.11");Fi[@(@(@(@L'@(I"´# @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[[	@O'iHI"}      # @return [Array<Twitter::Tweet>]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @example Return the 20 most recent retweets posted by @sferik
      #   Twitter.retweeted_by_user('sferik');Fi[	@O'i_I"ä      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @example Return the 20 most recent retweets posted by the authenticating user
      #   Twitter.retweeted_by_me;Fi[	@O'iÜI"˛      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @option options [Boolean, String, Integer] :include_entities The tweet entities node will be disincluded when set to false.
      # @example Return the 20 most recent retweets posted by users followed by the authenticating user;Fi[@ˇ>@ ?@?I"#;F@2?I"-#   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[[	@?iI"2      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids;Fi[	@?iI"ﬂ      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi[	@?i"I"ﬂ      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi[I">#   @example Return the authenticated user's friends' IDs;FI"#     Twitter.friend_ids;FI"-# @overload friend_ids(user, options={});FI"W#   Returns an array of numeric IDs for every user the specified user is following;FI"#;FI"`#   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[[	@?iI"ﬁ      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@?iI"£      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@?i*I"£      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[@a?@b?@c?I"#;F@e?I"=#   @param options [Hash] A customizable set of options.;Fi[[	@?iI"      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return @sferik's friends' IDs;Fi[	@?iI"Ò      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[	@?i+I"Ò      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[@b?@c?I"#;F@e?@o?I"Õ#   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[[	@?iI"Ò      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return @sferik's friends' IDs
      #     Twitter.friend_ids('sferik');Fi[	@?iI"˘      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi[	@?i,I"˘      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi[I"ï#   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.;FI"=#   @param options [Hash] A customizable set of options.;FI"def function(arglist);FI">threaded_user_objects_from_response(:post, "STRING", var);FI"end;FI" ;Fi[[	@?i≥I"Û      #   @example Unfollow @sferik
      #     Twitter.unfollow('sferik')
      # @overload unfollow(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(arglist)
        threaded_user_objects_from_response(:post, "STRING", var)
      end
      
      (alias :friendship_destroy :unfollow)
      ;Fi[	I",data//twitter_proj/twitter/api/users.rb;Ti’I"      #     Twitter.block('sferik')
      #     Twitter.block(7505382)  # Same as above
      # @overload block(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(arglist)
        threaded_user_objects_from_response(:post, "STRING", var)
      end
      
      # Un-blocks the users specified by the authenticating user
      #;Fi[	@å?iËI"      #     Twitter.unblock('sferik')
      #     Twitter.unblock(7505382)  # Same as above
      # @overload unblock(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(arglist)
        threaded_user_objects_from_response(:post, "STRING", var)
      end
      
      # Returns extended information for up to 100 users
      #;Fi[@H/@I/@J/@K/@L/I"+var = Twitter::API::Arguments.new(var);Fi[[	@ç(iI"g      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options[:owner_id] or var.options[:owner_screen_name]) then;Fi[	@∆)iI"q      
      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |arglist|
          object_from_response(Twitter::User, var, var, merge_user(var.options, var));Fi[	@∆)iI"t        end
      end
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(arglist)
        var = Twitter::API::Arguments.new(var)
        unless (var.options[:user_id] or var.options[:screen_name]) then
          merge_user!(var.options, (var.pop or screen_name));Fi[@(,@),@*,@+,@,,I"&# @return [Array<Twitter::Tweet>];Fi[[	@O'i(I"M      
      # Returns the 20 most recent Tweets posted by the specified user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @overload user_timeline(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[	@O'i@I"X      
      # Returns the 20 most recent retweets posted by the specified user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      # @param options [Hash] A customizable set of options.;Fi[	@O'iXI"      
      # Returns the 20 most recent retweets posted by the authenticating user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[I"/var[var] = Twitter::Size.fetch_or_new(var);FI"var;FI"end;FI"end;FI"end;FI"end;Fi[[	I"0data//twitter_proj/twitter/configuration.rb;TiI"⁄    # @return [Array<Twitter::Size>]
    def function(arglist)
      @var ||= Array(@var[:photo_sizes]).inject({}) do |arglist|
        var[var] = Twitter::Size.fetch_or_new(var)
        var
      end
    end
  end
end;Fi[	@≥iI"Í      # @return [Array<Twitter::Size>]
      def function(arglist)
        @var ||= Array(@var[:sizes]).inject({}) do |arglist|
          var[var] = Twitter::Size.fetch_or_new(var)
          var
        end
      end
    end
  end
end;Fi[	I"1data//twitter_proj/twitter/profile_banner.rb;TiI"‘    # @return [Array<Twitter::Size>]
    def function(arglist)
      @var ||= Array(@var[:sizes]).inject({}) do |arglist|
        var[var] = Twitter::Size.fetch_or_new(var)
        var
      end
    end
  end
end;Fi
