[{:loci ÿ:projects[I"backup_proj:ETI"compass_proj;TI"devise_proj;TI"diaspora_proj;TI"haml_proj;TI"homebrew_proj;TI"jekyll_proj;TI"nokogiri_proj;TI"omni_proj;TI"rescue_proj;TI"sinatra_proj;TI"state_machine_proj;TI"twitter_proj;TI"whenever_proj;T[a[	I",# @authentication Requires user context;FI"g# @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fii[i[	I":projects//twitter_proj/twitter/api/direct_messages.rb;TiI"∏      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::DirectMessage>] Direct messages sent to the authenticating user.;Fi[	@i&I"∏      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::DirectMessage>] Direct messages sent by the authenticating user.;Fi[	@i9I"ò      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::DirectMessage] The requested messages.;Fi[	@iGI"ü      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::DirectMessage>] The requested messages.;Fi[	@ipI"û      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::DirectMessage>] Deleted direct message.;Fi[	@i}I"5      # @see https://dev.twitter.com/docs/api/1.1/post/direct_messages/new
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::DirectMessage] The sent message.;Fi[	I"4projects//twitter_proj/twitter/api/favorites.rb;TiI".      # @see https://dev.twitter.com/docs/api/1.1/get/favorites/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] favorite Tweets.;Fi[	@)i4I"9      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The un-favorited Tweets.;Fi[	@)iLI"5      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The favorited Tweets.;Fi[	I"@projects//twitter_proj/twitter/api/friends_and_followers.rb;TiI"      # @see https://dev.twitter.com/docs/api/1.1/get/friends/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@0i+I"      # @see https://dev.twitter.com/docs/api/1.1/get/followers/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@0iFI"5      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/lookup
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The requested users.;Fi[	@0i\I"      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/incoming
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@0ikI"      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/outgoing
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@0izI"4      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The followed users.;Fi[	@0iéI"4      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The followed users.;Fi[	@0i´I"7      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The unfollowed users.;Fi[	@0iøI"!      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/update
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Relationship];Fi[	@0i—I"      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Relationship];Fi[	@0iÎI"A      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Boolean] true if user_a follows user_b, otherwise false.;Fi[	@0i˝I"      # @see https://dev.twitter.com/docs/api/1.1/get/followers/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@0iI"      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@0i=I"#      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/no_retweets/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Integer>];Fi[	I"/projects//twitter_proj/twitter/api/help.rb;TiI";      # @see https://dev.twitter.com/docs/api/1.1/get/help/configuration
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Configuration] Twitter's configuration.;Fi[	@MiI"       # @see https://dev.twitter.com/docs/api/1.1/get/help/languages
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Language>];Fi[	@Mi+I"      # @see https://dev.twitter.com/docs/api/1.1/get/help/privacy
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [String];Fi[	@Mi8I"      # @see https://dev.twitter.com/docs/api/1.1/get/help/tos
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [String];Fi[	I"0projects//twitter_proj/twitter/api/lists.rb;TiI"      # @see https://dev.twitter.com/docs/api/1.1/get/lists/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::List>];Fi[	@Vi0I"      # @see https://dev.twitter.com/docs/api/1.1/get/lists/statuses
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	@ViUI"&      # @see https://dev.twitter.com/docs/api/1.1/post/lists/members/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The list.;Fi[	@VirI"      # @see https://dev.twitter.com/docs/api/1.1/get/lists/memberships
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@ViÜI"A      # @see https://dev.twitter.com/docs/api/1.1/get/lists/subscribers
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor] The subscribers of the specified list.;Fi[	@Vi°I"3      # @see https://dev.twitter.com/docs/api/1.1/post/lists/subscribers/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The specified list.;Fi[	@Vi∫I"\      # @see https://dev.twitter.com/docs/api/1.1/get/lists/subscribers/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Boolean] true if user is a subscriber of the specified list, otherwise false.;Fi[	@ViÿI"4      # @see https://dev.twitter.com/docs/api/1.1/post/lists/subscribers/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The specified list.;Fi[	@ViÚI"f      # @note Lists are limited to having 500 members, and you are limited to adding up to 100 members to a list at a time with this method.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The list.;Fi[	@Vi-I"      # @see https://dev.twitter.com/docs/api/1.1/get/lists/members
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@ViJI"      # @note Lists are limited to having 500 members.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The list.;Fi[	@VigI"      # @note Must be owned by the authenticated user.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The deleted list.;Fi[	@ViÅI"%      # @see https://dev.twitter.com/docs/api/1.1/post/lists/update
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The created list.;Fi[	@Vi†I"      # @note Accounts are limited to 20 lists.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The created list.;Fi[	@Vi≤I"F      # @note Private lists will only be shown if the authenticated user owns the specified list.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The specified list.;Fi[	@ViÃI"      # @see https://dev.twitter.com/docs/api/1.1/get/lists/subscriptions
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor];Fi[	@Vi„I"*      # @see https://dev.twitter.com/docs/api/1.1/post/lists/members/destroy_all
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The list.;Fi[	I"9projects//twitter_proj/twitter/api/places_and_geo.rb;TiI"|      # @see https://dev.twitter.com/docs/api/1.1/get/geo/id/:place_id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param place_id [String] A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.;Fi[	@yi I"U      # @note This request is an informative call and will deliver generalized results about geography.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.;Fi[	@yi3I".      # @see https://dev.twitter.com/docs/api/1.1/get/geo/search
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.;Fi[	@yiMI"      # @note Conceptually, you would use this method to get a list of known places to choose from first. Then, if the desired place doesn't exist, make a request to {Twitter::API::PlacesAndGeo#place} to create a new one. The token contained in the response is the token necessary to create a new place.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.;Fi[	@yibI".      # @see https://dev.twitter.com/docs/api/1.1/post/geo/place
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.;Fi[	I"9projects//twitter_proj/twitter/api/saved_searches.rb;TiI"˘      
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::SavedSearch>] The saved searches.;Fi[	@i2I"9      # @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::SavedSearch] The saved searches.;Fi[	@iAI"=      # @see https://dev.twitter.com/docs/api/1.1/post/saved_searches/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::SavedSearch] The created saved search.;Fi[	@iQI"L      # @note The search specified by ID must be owned by the authenticating user.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::SavedSearch>] The deleted saved searches.;Fi[	I"1projects//twitter_proj/twitter/api/search.rb;TiI"±      # @note Please note that Twitter's search service and, by extension, the Search API is not meant to be an exhaustive source of Tweets. Not all Tweets will be indexed or made available via the search interface.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param q [String] A search term.;Fi[	I"9projects//twitter_proj/twitter/api/spam_reporting.rb;TiI"4      # @see https://dev.twitter.com/docs/api/1.1/post/users/report_spam
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The reported users.;Fi[	I":projects//twitter_proj/twitter/api/suggested_users.rb;TiI"      # @return [Array<Twitter::Suggestion>]
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @overload suggestions(options={});Fi[	I"4projects//twitter_proj/twitter/api/timelines.rb;TiI"      # @note This method can only return up to 800 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	@ëi+I"      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	@ëiCI"      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	@ëi[I"      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	@ëipI"(      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	@ëiÇI"(      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	@ëióI"&      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets_of_me
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	I"1projects//twitter_proj/twitter/api/trends.rb;TiI"      # @see https://dev.twitter.com/docs/api/1.1/get/trends/place
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param id [Integer] The {https://developer.yahoo.com/geo/geoplanet Yahoo! Where On Earth ID} of the location to return trending information for. WOEIDs can be retrieved by calling {Twitter::API::Trends#trend_locations}. Global information is available by using 1 as the WOEID.;Fi[	@†i'I"4      # @see https://dev.twitter.com/docs/api/1.1/get/trends/available
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.;Fi[	@†i7I"2      # @see https://dev.twitter.com/docs/api/1.1/get/trends/closest
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.;Fi[	I"1projects//twitter_proj/twitter/api/tweets.rb;TiI"$      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>];Fi[	@ßi%I"      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array];Fi[	@ßi9I".      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The requested Tweet.;Fi[	@ßiII"6      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The requested Tweets.;Fi[	@ßi]I"=      # @note The authenticating user must be the author of the specified Tweets.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The deleted Tweets.;Fi[	@ßisI"h      # @note A status update with text identical to the authenticating user's current status will be ignored to prevent duplicates.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The created Tweet.;Fi[	@ßiÉI"W      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/retweet/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The original tweets with retweet details embedded.;Fi[	@ßiπI"[      # @note A status update with text/media identical to the authenticating user's current status will NOT be ignored
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The created Tweet.;Fi[	@ßiœI"8      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::OEmbed] OEmbed for the requested Tweet.;Fi[	@ßiÂI"A      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::OEmbed>] OEmbeds for the requested Tweets.;Fi[	I"7projects//twitter_proj/twitter/api/undocumented.rb;TiI"˜      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array] An array of actions;Fi[	@ºi2I"”      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      #;Fi[	@ºiPI"      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The requested Tweet.;Fi[	@ºicI"	      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The requested Tweets.;Fi[	I"0projects//twitter_proj/twitter/api/users.rb;TiI"      # @see https://dev.twitter.com/docs/api/1.1/get/account/settings
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Settings];Fi[	@≈i.I"9      # @see https://dev.twitter.com/docs/api/1.1/get/account/verify_credentials
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.;Fi[	@≈i?I"=      # @see https://dev.twitter.com/docs/api/1.1/post/account/update_delivery_device
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.;Fi[	@≈iOI"!      # @note Only the options specified will be updated.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.;Fi[	@≈iaI"F      # @see https://dev.twitter.com/docs/api/1.1/post/account/update_profile_background_image
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.;Fi[	@≈iqI"<      # @see https://dev.twitter.com/docs/api/1.1/post/account/update_profile_colors
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.;Fi[	@≈iÅI"      # @note This method asynchronously processes the uploaded file before updating the user's profile image URL. You can either update your local cache the next time you request the user's information, or, at least 5 seconds after uploading the image, ask for the updated URL using GET users/show.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.;Fi[	@≈iêI"P      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] User objects that the authenticating user is blocking.;Fi[	@≈iüI"?      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array] Numeric user ids the authenticating user is blocking.;Fi[	@≈i∞I"Y      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Boolean] true if the authenticating user is blocking the target user, otherwise false.;Fi[	@≈iÃI"0      # @note Destroys a friendship to the blocked user if it exists.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The blocked users.;Fi[	@≈iﬂI"2      # @see https://dev.twitter.com/docs/api/1.1/post/blocks/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The un-blocked users.;Fi[	@≈iÚI"/      # @see https://dev.twitter.com/docs/api/1.1/get/users/lookup
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The requested users.;Fi[	@≈iI"%      # @see https://dev.twitter.com/docs/api/1.1/get/users/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The requested user.;Fi[	@≈i+I"¸      #
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Boolean] true if the user exists, otherwise false.;Fi[	@≈i@I"      # @see https://dev.twitter.com/docs/api/1.1/get/users/search
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>];Fi[	@≈iQI"       # @see https://dev.twitter.com/docs/api/1.1/get/users/contributees
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>];Fi[	@≈ihI"       # @see https://dev.twitter.com/docs/api/1.1/get/users/contributors
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>];Fi[	@≈iI"      # @see https://dev.twitter.com/docs/api/1.1/post/account/remove_profile_banner
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [nil];Fi[	@≈i•I"9      # @note If the user has not uploaded a profile banner, a HTTP 404 will be served instead.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::ProfileBanner];Fi[	I"def function;FI""STRING";Fi_[_[	I"5projects//backup_proj/backup/database/mongodb.rb;Ti®I"›      # Returns the MongoDB syntax for determining where to output all the database dumps,
      # e.g. ~/Backup/.tmp/databases/MongoDB/<databases here>/<database collections>
      def function
        "STRING"
      end;Fi[	I"3projects//backup_proj/backup/database/redis.rb;TiWI"h      ##
      # Returns the Redis database file name
      def function
        "STRING"
      end;Fi[	I"2projects//backup_proj/backup/database/riak.rb;Ti6I"}      ##
      # Builds the full riak-admin string based on all attributes
      def function
        "STRING"
      end;Fi[	I",projects//backup_proj/backup/package.rb;Ti+I"9    end
    
    def function
      "STRING"
    end;Fi[	I"7projects//backup_proj/backup/storage/cloudfiles.rb;Ti#I"}      ##
      # This is the provider that Fog uses for the Cloud Files Storage
      def function
        "STRING"
      end;Fi[	I"5projects//backup_proj/backup/storage/ninefold.rb;TiI"      ##
      # This is the provider that Fog uses for the Ninefold storage
      def function
        "STRING"
      end;Fi[	I"2projects//backup_proj/backup/storage/rsync.rb;TiyI"z      ##
      # Returns Rsync syntax for defining a port to connect to
      def function
        "STRING"
      end;Fi[	@i{I"ò      # RSync options
      # -z = Compresses the bytes that will be transferred to reduce bandwidth usage
      def function
        "STRING"
      end;Fi[	I"/projects//backup_proj/backup/storage/s3.rb;Ti!I"y      ##
      # This is the provider that Fog uses for the S3 Storage
      def function
        "STRING"
      end;Fi[	I"=projects//backup_proj/backup/syncer/cloud/cloud_files.rb;Ti@I"        ##
        # This is the provider that Fog uses for the Cloud Files
        def function
          "STRING"
        end;Fi[	I"4projects//backup_proj/backup/syncer/cloud/s3.rb;Ti7I"        ##
        # This is the provider that Fog uses for the Cloud Files
        def function
          "STRING"
        end;Fi[	I"6projects//backup_proj/backup/syncer/rsync/base.rb;Ti)I"~        ##
        # Returns Rsync syntax for invoking "archive" mode
        def function
          "STRING"
        end;Fi[	I"6projects//backup_proj/backup/syncer/rsync/push.rb;TiOI"        ##
        # Returns Rsync syntax for defining a port to connect to
        def function
          "STRING"
        end;Fi[	I"Yprojects//compass_proj/compass/app_integration/stand_alone/configuration_defaults.rb;TiI"M        end
        
        def function
          "STRING"
        end;Fi[	@iI"M        end
        
        def function
          "STRING"
        end;Fi[	@iI"M        end
        
        def function
          "STRING"
        end;Fi[	@iI"M        end
        
        def function
          "STRING"
        end;Fi[	@iI"M        end
        
        def function
          "STRING"
        end;Fi[	I"=projects//compass_proj/compass/configuration/defaults.rb;TiI"]  module Configuration
    module Defaults
      def function
        "STRING"
      end;Fi[	@#iI"C      end
      
      def function
        "STRING"
      end;Fi[	@#iI"C      end
      
      def function
        "STRING"
      end;Fi[	I"6projects//compass_proj/compass/installers/base.rb;Ti†I"Ü      
      # Emits an HTML fragment that can be used to link to the compiled css files
      def function
        "STRING"
      end;Fi[	I"Mprojects//compass_proj/compass/sass_extensions/sprites/sprite_methods.rb;Ti,I"M        end
        
        def function
          "STRING"
        end;Fi[	I"+projects//devise_proj/devise/models.rb;TiI"C      end
      
      def function
        "STRING"
      end;Fi[	I"Gprojects//devise_proj/generators/active_record/devise_generator.rb;Ti&I"C      end
      
      def function
        "STRING"
      end;Fi[	I";projects//devise_proj/generators/devise/orm_helpers.rb;TiI"\  module Generators
    module OrmHelpers
      def function
        "STRING"
      end;Fi[	I"?projects//devise_proj/generators/devise/views_generator.rb;TihI"C      end
      
      def function
        "STRING"
      end;Fi[	I"Aprojects//devise_proj/generators/mongoid/devise_generator.rb;TiI"C      end
      
      def function
        "STRING"
      end;Fi[	I"Cprojects//diaspora_proj/models/notifications/also_commented.rb;Ti
I"/  end
  
  def function
    "STRING"
  end;Fi[	@?iI"/  end
  
  def function
    "STRING"
  end;Fi[	I"Dprojects//diaspora_proj/models/notifications/comment_on_post.rb;Ti
I"/  end
  
  def function
    "STRING"
  end;Fi[	@DiI"/  end
  
  def function
    "STRING"
  end;Fi[	I":projects//diaspora_proj/models/notifications/liked.rb;Ti
I"/  end
  
  def function
    "STRING"
  end;Fi[	@IiI"/  end
  
  def function
    "STRING"
  end;Fi[	I">projects//diaspora_proj/models/notifications/mentioned.rb;Ti
I"/  end
  
  def function
    "STRING"
  end;Fi[	@NiI"/  end
  
  def function
    "STRING"
  end;Fi[	I"Dprojects//diaspora_proj/models/notifications/private_message.rb;Ti
I"/  end
  
  def function
    "STRING"
  end;Fi[	I"Eprojects//diaspora_proj/models/notifications/request_accepted.rb;Ti
I"/  end
  
  def function
    "STRING"
  end;Fi[	I"=projects//diaspora_proj/models/notifications/reshared.rb;Ti
I"/  end
  
  def function
    "STRING"
  end;Fi[	@YiI"/  end
  
  def function
    "STRING"
  end;Fi[	I"Dprojects//diaspora_proj/models/notifications/started_sharing.rb;Ti
I"/  end
  
  def function
    "STRING"
  end;Fi[	I"-projects//diaspora_proj/models/person.rb;Ti∆I"/  end
  
  def function
    "STRING"
  end;Fi[	I"+projects//diaspora_proj/models/post.rb;Ti>I"/  end
  
  def function
    "STRING"
  end;Fi[	I"8projects//diaspora_proj/models/services/facebook.rb;TiI"F  MAX_CHARACTERS = "LITERAL"
  
  def function
    "STRING"
  end;Fi[	@gi'I"/  end
  
  def function
    "STRING"
  end;Fi[	I"6projects//diaspora_proj/models/services/tumblr.rb;TiI"F  MAX_CHARACTERS = "LITERAL"
  
  def function
    "STRING"
  end;Fi[	I"7projects//diaspora_proj/models/services/twitter.rb;TiI"L  SHORTENED_URL_LENGTH = "LITERAL"
  
  def function
    "STRING"
  end;Fi[	I"9projects//diaspora_proj/uploaders/processed_image.rb;TiI"L  include(CarrierWave::MiniMagick)
  
  def function
    "STRING"
  end;Fi[	I";projects//diaspora_proj/uploaders/unprocessed_image.rb;TiI"L  include(CarrierWave::MiniMagick)
  
  def function
    "STRING"
  end;Fi[	I"'projects//haml_proj/haml/buffer.rb;Ti£I"]    
    if (RUBY_VERSION < "STRING") then
      def function
        "STRING"
      end;Fi[	I")projects//haml_proj/haml/compiler.rb;Ti>I"√    # Returns the string used as the return value of the precompiled method.
    # This method exists so it can be monkeypatched to return modified values.
    def function
      "STRING"
    end;Fi[	I"(projects//haml_proj/haml/helpers.rb;TiI"y      
      # @return [String] A human-readable string representation
      def function
        "STRING"
      end;Fi[	I")projects//haml_proj/haml/template.rb;TiI"Imodule Haml
  class Compiler
    def function
      "STRING"
    end;Fi[	I"'projects//homebrew_proj/bottles.rb;Ti=I"F  Pathname::OLD_BOTTLE_EXTNAME_RX
end
def function
  "STRING"
end;Fi[	I",projects//homebrew_proj/cmd/--config.rb;TiXI"/  end
  
  def function
    "STRING"
  end;Fi[	I"4projects//homebrew_proj/compat/compatibility.rb;TiLI"/  end
  
  def function
    "STRING"
  end;Ti[	I"*projects//homebrew_proj/exceptions.rb;Ti;I"wend
class FormulaInstallationAlreadyAttemptedError < Homebrew::InstallationError
  def function
    "STRING"
  end;Fi[	I"-projects//homebrew_proj/macos/xquartz.rb;Ti'I"/  end
  
  def function
    "STRING"
  end;Fi[	I"2projects//homebrew_proj/test/test_patching.rb;TiI"arequire("test/testball")
class DefaultPatchBall < TestBall
  def function
    "STRING"
  end;Fi[	I")projects//jekyll_proj/jekyll/page.rb;TiáI"]    
    # Returns the object as a debug String.
    def function
      "STRING"
    end;Fi[	I")projects//jekyll_proj/jekyll/post.rb;TiﬂI"m    
    # Returns the shorthand String identifier of this Post.
    def function
      "STRING"
    end;Fi[	I"Aprojects//nokogiri_proj/nokogiri/html/element_description.rb;TiI"i      ###
      # Convert this description to a string
      def function
        "STRING"
      end;Fi[	@úiI"[      ###
      # Inspection information
      def function
        "STRING"
      end;Fi[	I";projects//nokogiri_proj/nokogiri/xml/attribute_decl.rb;TiI"u      undef_method("LITERAL") if method_defined?("LITERAL")
      
      def function
        "STRING"
      end;Fi[	I"2projects//nokogiri_proj/nokogiri/xml/cdata.rb;Ti
I"d      ###
      # Get the name of this CDATA node
      def function
        "STRING"
      end;Fi[	I"5projects//nokogiri_proj/nokogiri/xml/document.rb;TiRI"w      
      # The name of this document.  Always returns "document"
      def function
        "STRING"
      end;Fi[	@ßiﬂI"G      private
      
      def function
        "STRING"
      end;Fi[	I">projects//nokogiri_proj/nokogiri/xml/document_fragment.rb;TiI"i      ###
      # return the name for DocumentFragment
      def function
        "STRING"
      end;Fi[	I"9projects//nokogiri_proj/nokogiri/xml/element_decl.rb;TiI"u      undef_method("LITERAL") if method_defined?("LITERAL")
      
      def function
        "STRING"
      end;Fi[	I"8projects//nokogiri_proj/nokogiri/xml/entity_decl.rb;TiI"C      end
      
      def function
        "STRING"
      end;Fi[	I"1projects//nokogiri_proj/nokogiri/xml/node.rb;TikI"C      end
      
      def function
        "STRING"
      end;Fi[	I"5projects//nokogiri_proj/nokogiri/xml/node_set.rb;Ti<I"s      ###
      # Return a nicely formated string representation
      def function
        "STRING"
      end;Fi[	I">projects//nokogiri_proj/nokogiri/xml/pp/character_data.rb;TiI"S        
        # :nodoc:
        def function
          "STRING"
        end;Fi[	I")projects//omni_proj/omniauth/form.rb;TiZI"?    protected
    
    def function
      "STRING"
    end;Fi[	I"-projects//omni_proj/omniauth/strategy.rb;Ti|I"9    end
    
    def function
      "STRING"
    end;Fi[	I"$projects//omni_proj/omniauth.rb;TicI"E    module_function
    
    def function
      "STRING"
    end;Fi[	I"+projects//rescue_proj/resque/worker.rb;TiÁI"9    end
    
    def function
      "STRING"
    end;Fi[	I"$projects//rescue_proj/resque.rb;Ti∂I"/  end
  
  def function
    "STRING"
  end;Fi[	I"Oprojects//state_machine_proj/state_machine/integrations/sequel/versions.rb;Ti4I"—          (defined? ::Sequel::MAJOR.! or ((::Sequel::MAJOR == "LITERAL") or ((::Sequel::MAJOR == "LITERAL") and (::Sequel::MINOR <= "LITERAL"))))
        end
        def function
          "STRING"
        end;Fi[	@Õi7I"W          "STRING"
        end
        def function
          "STRING"
        end;Fi[	@Õi@I"œ          defined? ::Sequel::MAJOR and ((::Sequel::MAJOR == "LITERAL") and ((::Sequel::MINOR >= "LITERAL") and (::Sequel::MINOR <= "LITERAL")))
        end
        def function
          "STRING"
        end;Fi[	@ÕiCI"W          "STRING"
        end
        def function
          "STRING"
        end;Fi[	I":projects//state_machine_proj/state_machine/matcher.rb;Ti0I"t    
    # A human-readable description of this matcher.  Always "all".
    def function
      "STRING"
    end;Fi[	@÷i\I"d    
    # A human-readable description of this matcher
    def function
      "STRING"
    end;Fi[	@÷iqI"u    
    # A human-readable description of this matcher.  Always "same".
    def function
      "STRING"
    end;Fi[	I"=projects//state_machine_proj/state_machine/transition.rb;Ti'I"    #   transition = StateMachine::Transition.new(object, machine, :ignite, :parked, :idling)
    #   transition   # => #<StateMachine::Transition attribute=:state event=:ignite from="parked" from_name=:parked to="idling" to_name=:idling>
    def function
      "STRING"
    end;Fi[	I"5projects//whenever_proj/whenever/command_line.rb;TimI"9    end
    
    def function
      "STRING"
    end;Fi[	@‡iqI"9    end
    
    def function
      "STRING"
    end;Fi[	@‡iuI"9    end
    
    def function
      "STRING"
    end;Fi[	I";projects//whenever_proj/whenever/output_redirection.rb;Ti;I"C      end
      
      def function
        "STRING"
      end;Fi[	I"when "LITERAL" then;F@Ôi[[[[	I"6projects//backup_proj/backup/notifier/campfire.rb;Ti1I"~      def function(var)
        var = case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Ìi3I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Ìi5I"o        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        else;Fi[	I"3projects//backup_proj/backup/notifier/prowl.rb;Ti/I"~      def function(var)
        var = case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Ùi1I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Ùi3I"o        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        else;Fi[	I"6projects//backup_proj/backup/notifier/pushover.rb;Ti9I"~      def function(var)
        var = case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@˚i;I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@˚i=I"o        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        else;Fi[	I"5projects//backup_proj/backup/notifier/twitter.rb;Ti-I"~      def function(var)
        var = case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@i/I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@i1I"o        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        else;Fi[	I"Mprojects//compass_proj/compass/sass_extensions/functions/inline_image.rb;Ti'I"f    return var if var
    case var
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i)I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i+I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i-I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i/I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i1I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i3I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i5I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i7I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@	i9I"[    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    else;Fi[	I")projects//homebrew_proj/blacklist.rb;TiI"e  when "STRING" then
    "STRING".undent
  when "LITERAL" then
    "STRING"
  when "STRING" then;Fi[	I"*projects//homebrew_proj/cmd/update.rb;TièI"y      when "LITERAL" then
        File.basename(var, "STRING")
      when "LITERAL" then
        "STRING"
      else;Fi[	I"1projects//homebrew_proj/download_strategy.rb;Ti"I"π        safe_system(@@git, "STRING", "STRING", @url)
        safe_system(@@git, "STRING", "STRING", case @spec
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@$i$I"o        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        else;Fi[	I"(projects//homebrew_proj/hardware.rb;Ti.I"  def self.cores_as_words
    case Hardware.processor_count
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@)i0I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@)i2I"[    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    else;Fi[	I"+projects//homebrew_proj/macos/xcode.rb;TiI"i  def function
    case MacOS.version
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@0i I"[    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    else;Fi[	@0iPI"û    raise if ($1.nil? or $?.success?.!)
    $1) rescue case MacOS.llvm_build_version.to_i
      when "LITERAL" then
        "STRING"
      when "LITERAL" then;Fi[	@0iRI"t      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then;Fi[	@0iTI"t      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then;Fi[	@0iVI"t      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then;Fi[	@0iXI"t      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then;Fi[	@0iZI"e      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING"
      else;Fi[	@0i^I"é      else
        case (MacOS.clang_version.to_f * "LITERAL").to_i
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0i`I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0ibI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0idI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0ifI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0ihI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0ijI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0ilI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0inI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@0ipI"o        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        else;Fi[	@êiI"ç      if (prefix.to_s == "STRING") then
        case MacOS.version
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@êiI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@êiI"o        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        else;Fi[	I",projects//homebrew_proj/requirements.rb;Ti/I"e  def function
    case @language
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@[i1I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@[i3I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@[i5I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@[i7I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@[i9I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@[i;I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@[i=I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@[i?I"[    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    else;Fi[	I"1projects//homebrew_proj/test/test_formula.rb;TiZI"ò    assert_equal("LITERAL", var.devel.checksum.hash_type)
    assert_equal(case MacOS.cat
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@ni\I"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@ni^I"[    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    else;Fi[	@niÒI"è    assert_equal("LITERAL", var.bottle.revision)
    assert_equal(case MacOS.cat
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@niÛI"j    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then;Fi[	@niıI"[    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING"
    else;Fi[	I",projects//jekyll_proj/jekyll/filters.rb;TiwI"t      var = "STRING"
      case var.length
      when "LITERAL" then
        "STRING"
      when "LITERAL" then;Fi[	@{i{I"p      when "LITERAL" then
        var["LITERAL"].to_s
      when "LITERAL" then
        "STRING"
      else;Fi[	@ôizI"    def function
      case self.site.permalink_style
      when "LITERAL" then
        "STRING"
      when "LITERAL" then;Fi[	@ôi|I"t      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then;Fi[	@ôi~I"e      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING"
      else;Fi[	I":projects//nokogiri_proj/nokogiri/css/xpath_visitor.rb;TiI"£        return self.send(var, var) if self.respond_to?(var)
        case var.value.first
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@ÜiI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@ÜiI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@ÜiI"ë            (var == "LITERAL") ? ("STRING") : ("STRING")
          end
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Üi!I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Üi#I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Üi%I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Üi'I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@Üi)I"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@ÜiMI"é        when "LITERAL" then
          ((var + "STRING") + "STRING")
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@ÜiOI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@ÜiQI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@ÜiSI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	I"Bprojects//twitter_proj/twitter/request/multipart_with_file.rb;TiI"x      def function(var)
        case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@°iI"~        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then;Fi[	@°iI"o        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING"
        else;Fi[	I"# @rate_limited Yes;F@iO[O[	@iI"ù      # @see https://dev.twitter.com/docs/api/1.1/get/direct_messages
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@i%I"¢      # @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/sent
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@i8I"¢      # @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@iFI"^      
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@)iI"¸      
      # @see https://dev.twitter.com/docs/api/1.1/get/favorites/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0iI"˘      
      # @see https://dev.twitter.com/docs/api/1.1/get/friends/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0i*I"˚      
      # @see https://dev.twitter.com/docs/api/1.1/get/followers/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0iEI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/lookup
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0i[I"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/incoming
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0ijI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/outgoing
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0i–I"ˇ      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0iÍI"ˇ      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0i¸I"˝      #
      # @see https://dev.twitter.com/docs/api/1.1/get/followers/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0iI"ˇ      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0i<I"      # =>
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/no_retweets/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@MiI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/help/configuration
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@MiI"˝      #
      # @see https://dev.twitter.com/docs/api/1.1/get/help/languages
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Mi*I"˚      #
      # @see https://dev.twitter.com/docs/api/1.1/get/help/privacy
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Mi7I"˜      #
      # @see https://dev.twitter.com/docs/api/1.1/get/help/tos
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViI"˘      #
      # @see https://dev.twitter.com/docs/api/1.1/get/lists/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Vi/I"˝      #
      # @see https://dev.twitter.com/docs/api/1.1/get/lists/statuses
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViqI"       #
      # @see https://dev.twitter.com/docs/api/1.1/get/lists/memberships
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViÖI"       #
      # @see https://dev.twitter.com/docs/api/1.1/get/lists/subscribers
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViπI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/lists/subscribers/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Vi,I"¸      #
      # @see https://dev.twitter.com/docs/api/1.1/get/lists/members
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Vi±I"S      # @see https://dev.twitter.com/docs/api/1.1/get/lists/show
      # @note Private lists will only be shown if the authenticated user owns the specified list.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViÀI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/lists/subscriptions
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@yiI"ˇ      #
      # @see https://dev.twitter.com/docs/api/1.1/get/geo/id/:place_id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@yiI"b      # @see https://dev.twitter.com/docs/api/1.1/get/geo/reverse_geocode
      # @note This request is an informative call and will deliver generalized results about geography.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@yi2I"˘      #
      # @see https://dev.twitter.com/docs/api/1.1/get/geo/search
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@yiLI")      # @see https://dev.twitter.com/docs/api/1.1/get/geo/similar_places
      # @note Conceptually, you would use this method to get a list of known places to choose from first. Then, if the desired place doesn't exist, make a request to {Twitter::API::PlacesAndGeo#place} to create a new one. The token contained in the response is the token necessary to create a new place.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@yiaI"˘      #
      # @see https://dev.twitter.com/docs/api/1.1/post/geo/place
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@iI"⁄      include(Twitter::API::Utils)
      
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@i1I"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@àiI"ø      # @see https://dev.twitter.com/docs/using-search
      # @note Please note that Twitter's search service and, by extension, the Search API is not meant to be an exhaustive source of Tweets. Not all Tweets will be indexed or made available via the search interface.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ãiI"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/users/report_spam
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@éiI"‰      
      # @return [Array<Twitter::Suggestion>]
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@éi-I"Ë      #
      # @see https://dev.twitter.com/docs/api/1.1/get/users/suggestions/:slug/members
      # @rate_limited Yes
      # @authentication Requires user context
      # @param slug [String] The short name of list or a category.;Fi[	@ëiI"=      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/mentions_timeline
      # @note This method can only return up to 800 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ëi*I";      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ëiBI";      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ëiZI";      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ëioI"M      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ëiÅI"M      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ëiñI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets_of_me
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@†iI"˚      #
      # @see https://dev.twitter.com/docs/api/1.1/get/trends/place
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@†i&I"ˇ      #
      # @see https://dev.twitter.com/docs/api/1.1/get/trends/available
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@†i6I"˝      #
      # @see https://dev.twitter.com/docs/api/1.1/get/trends/closest
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßiI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßi$I"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßi8I"       #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßiHI"       #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßiÇI"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/retweet/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßiúI"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/retweet/:id
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::AlreadyRetweeted] Error raised when tweet has already been retweeted.;Fi[	@ßiŒI"˛      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßi‰I"˛      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ºiI"”      #
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ºi#I"‘      #
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid./;Fi[	@ºi1I"“      
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ºiOI"”      #
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ºibI"”      #
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iI"?      # @see https://dev.twitter.com/docs/api/1.1/post/account/settings
      # @see https://dev.twitter.com/docs/api/1.1/get/account/settings
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈i-I"	      #
      # @see https://dev.twitter.com/docs/api/1.1/get/account/verify_credentials
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈ièI"˙      #
      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/list
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iûI"˘      #
      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iØI"˘      #
      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iÀI";      # @see https://dev.twitter.com/docs/api/1.1/post/blocks/create
      # @note Destroys a friendship to the blocked user if it exists.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iÒI"˚      #
      # @see https://dev.twitter.com/docs/api/1.1/get/users/lookup
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iI"¯      
      # @see https://dev.twitter.com/docs/api/1.1/get/users/show
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈i*I"Í      # Returns true if the specified user exists
      #
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈i?I"˚      #
      # @see https://dev.twitter.com/docs/api/1.1/get/users/search
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iPI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/users/contributees
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈igI"      #
      # @see https://dev.twitter.com/docs/api/1.1/get/users/contributors
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈i§I"[      # @see https://dev.twitter.com/docs/api/1.1/get/users/profile_banner
      # @note If the user has not uploaded a profile banner, a HTTP 404 will be served instead.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Ô@ÍiK[K[	@Ìi2I"y        var = case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@Ìi4I"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@Ùi0I"y        var = case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@Ùi2I"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@˚i:I"y        var = case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@˚i<I"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@i.I"y        var = case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@i0I"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@	i(I"_    case var
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@	i*I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@	i,I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@	i.I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@	i0I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@	i2I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@	i4I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@	i6I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@	i8I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	I"*projects//homebrew_proj/cmd/search.rb;TiI"y        var = case var
        when nil then
          "STRING"
        when "LITERAL" then
          Regexp.new($1);Fi[	@$i#I"ó        safe_system(@@git, "STRING", "STRING", case @spec
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@)i/I"t    case Hardware.processor_count
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@)i1I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@0iI"i    case MacOS.version
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@0iQI"á    $1) rescue case MacOS.llvm_build_version.to_i
      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING";Fi[	@0iSI"k        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING";Fi[	@0iUI"k        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING";Fi[	@0iWI"k        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING";Fi[	@0iYI"k        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING";Fi[	@0i_I"ñ        case (MacOS.clang_version.to_f * "LITERAL").to_i
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@0iaI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@0icI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@0ieI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@0igI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@0iiI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@0ikI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@0imI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@0ioI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@êiI"}        case MacOS.version
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@êiI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@[i0I"e    case @language
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@[i2I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@[i4I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@[i6I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@[i8I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@[i:I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@[i<I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@[i>I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@ni[I"r    assert_equal(case MacOS.cat
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@ni]I"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@niÚI"r    assert_equal(case MacOS.cat
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	@niÙI"a      "STRING"
    when "LITERAL" then
      "STRING"
    when "LITERAL" then
      "STRING";Fi[	I"'projects//homebrew_proj/version.rb;TiI"j      "STRING"
    when "STRING", "STRING" then
      "STRING"
    when "LITERAL" then
      var.to_i;Fi[	@{ixI"{      case var.length
      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        var["LITERAL"].to_s;Fi[	@ôi{I"      case self.site.permalink_style
      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING";Fi[	@ôi}I"k        "STRING"
      when "LITERAL" then
        "STRING"
      when "LITERAL" then
        "STRING";Fi[	@ÜiI"        case var.value.first
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@ÜiI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@ÜiI"Ã          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          if var.value["LITERAL"].is_a?(Nokogiri::CSS::Node) and (var.value["LITERAL"].type == "LITERAL") then;Fi[	@Üi I"p          end
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@Üi"I"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@Üi$I"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@Üi&I"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@Üi(I"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@Üi*I"â          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          var.value["LITERAL"].accept(self);Fi[	@ÜiNI"Ö          ((var + "STRING") + "STRING")
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@ÜiPI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@ÜiRI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@ÜiTI"}          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          ("STRING" + "STRING");Fi[	@°iI"s        case var
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	@°iI"u          "STRING"
        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          "STRING";Fi[	I"-projects//whenever_proj/whenever/cron.rb;TiGI"~        var = case @time
        when *KEYWORDS then
          "STRING"
        when "LITERAL" then
          "LITERAL".months;Fi[	I"#   end;FI"#;FiG[G[	I"2projects//backup_proj/backup/encryptor/gpg.rb;Ti^I"i      #       s2k-digest-algo SHA512
      #     EOF
      #   end
      #
      # @see #gpg_homedir;Fi[	@‘iíI"~      #
      #     EOS
      #   end
      #
      # All leading spaces/tabs will be stripped from the key, so the above;Fi[	@‘iÊI"Z      #       ]
      #     end
      #   end
      #
      # @return [String, Array];Fi[	@‘iI"≠      #   Backup::Encryptor::GPG.defaults do |enc|
      #     enc.passphrase = 'secret phrase'
      #   end
      #
      #   Backup::Model.new(:my_backup, 'My Backup') do;Fi[	@‘iI"}      #       enc.passphrase_file = '/path/to/passphrase.file'
      #     end
      #   end
      #
      # @return [String];Fi[	@‘i%I"~      #
      #     encryptor.recipients = 'joe@example.com'
      #   end
      #
      #   # Encryptor set in the model;Fi[	@‘i+I"}      #     # archives, storage options, etc...
      #     encrypt_with GPG
      #   end
      #
      # @!scope class;Fi[	@‘iGI"R      #
      #     notify_by Mail
      #   end
      #
      # @api private;Fi[	I"8projects//devise_proj/devise/controllers/helpers.rb;TiòI"—      #   map.namespace :user do |user|
      #     user.root :controller => 'users' # creates user_root_path
      #   end
      #
      # If the resource root path is not defined, root_path is used. However,;Fi[	@Âi§I"Z      #         super
      #       end
      #   end
      #
      def function(var);Fi[	I";projects//devise_proj/devise/models/authenticatable.rb;TiôI"¶        #     conditions[:active] = true
        #     super
        #   end
        #
        # Finally, notice that Devise also queries for users in other scenarios;Fi[	I"Dprojects//devise_proj/devise/models/database_authenticatable.rb;TiGI"s      #     params.delete(:email)
      #     super(params)
      #   end
      #
      def function(var, var);Fi[	I"1projects//devise_proj/devise/rails/routes.rb;TiäI"î    #   scope "/my" do
    #     devise_for :users
    #   end
    #
    # However, since Devise uses the request path to retrieve the current user,;Fi[	@iëI"à    #   scope ":locale" do
    #     devise_for :users
    #   end
    #
    # you are required to configure default_url_options in your;Fi[	@iöI"    #       { :locale => I18n.locale }
    #     end
    #   end
    #
    # ==== Adding custom actions to override controllers;Fi[	@i·I"h    #   authenticate do
    #     resources :post
    #   end
    #
    #   authenticate(:admin) do;Fi[	@iÂI"ä    #   authenticate(:admin) do
    #     resources :users
    #   end
    #
    #   authenticate :user, lambda {|u| u.role == "admin"} do;Fi[	@iÈI"ó    #   authenticate :user, lambda {|u| u.role == "admin"} do
    #     root :to => "admin/dashboard#show"
    #   end
    #
    def function(var, var);Fi[	@i¯I"|    #   authenticated :admin do
    #     root :to => 'admin/dashboard#show'
    #   end
    #
    #   authenticated do;Fi[	@i¸I"ê    #   authenticated do
    #     root :to => 'dashboard#show'
    #   end
    #
    #   authenticated :user, lambda {|u| u.role == "admin"} do;Fi[	@i I"†    #   authenticated :user, lambda {|u| u.role == "admin"} do
    #     root :to => "admin/dashboard#show"
    #   end
    #
    #   root :to => 'landing#show';Fi[	@iI"|    #       root :to => 'devise/registrations#new'
    #     end
    #   end
    #
    #   root :to => 'dashboard#show';Fi[	@i!I"≠    #   as :user do
    #     get "sign_in", :to => "devise/sessions#new"
    #   end
    #
    # Notice you cannot have two scopes mapping to the same URL. And remember, if;Fi[	I"6projects//nokogiri_proj/nokogiri/html/document.rb;Ti=I"â      #   node.serialize(:encoding => 'UTF-8') do |config|
      #     config.format.as_xml
      #   end
      #
      def function(var);Fi[	@µi§I"é      #   node.serialize(:encoding => 'UTF-8') do |config|
      #     config.format.as_xml
      #   end
      #
      def function(var, var);Fi[	@¡i-I"Å      #       c.foo = 'bar'
      #     end
      #   end
      #
      # @example Using a hash to configure the default options.;Fi[	I",projects//rescue_proj/resque/helpers.rb;Ti)I"æ    #     C               # => 'inside'
    #     'C'.constantize # => 'outside', same as ::C
    #   end
    #
    # NameError is raised when the name is not in CamelCase or the constant is;Fi[	I"?projects//state_machine_proj/state_machine/eval_helpers.rb;TiI"V    #       @name
    #     end
    #   end
    #   
    #   class PersonCallback;Fi[	@i I"g    #       person.name
    #     end
    #   end
    # 
    #   person = Person.new('John Smith');Fi[	I"Cprojects//state_machine_proj/state_machine/event_collection.rb;Ti#I"m    #       end
    #     end
    #   end
    #   
    #   events = Vehicle.state_machine(:state).events;Fi[	@iHI"e    #       end
    #     end
    #   end
    #   
    #   events = Vehicle.state_machine.events;Fi[	@ihI"è    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                       # => #<Vehicle id: nil, state: "parked">;Fi[	I"=projects//state_machine_proj/state_machine/extensions.rb;TiTI"™    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                         # => #<Vehicle:0xb7c02850 @state="parked", @alarm_state="active">;Fi[	@iÅI"™    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                         # => #<Vehicle:0xb7c02850 @state="parked", @alarm_state="active">;Fi[	I"Dprojects//state_machine_proj/state_machine/integrations/base.rb;TiDI"ë        #       end
        #     end
        #   end
        # 
        # In the above example, a version override is defined for the ORMLibrary;Fi[	I"Tprojects//state_machine_proj/state_machine/integrations/data_mapper/observer.rb;Ti I"i        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver;Fi[	@%i9I"ò        #       # log message
        #     end
        #   end
        # 
        # *Note* that in each of the above +before_transition+ callbacks, the;Fi[	@%i]I"i        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver;Fi[	@%iiI"†        #       # track end time
        #     end
        #   end
        # 
        # See +before_transition+ for a description of the possible configurations;Fi[	@%i|I"i        #       end
        #     end
        #   end
        #   
        #   class VehicleObserver;Fi[	@%iÜI"ù        #       # log failure
        #     end
        #   end
        # 
        # See +before_transition+ for a description of the possible configurations;Fi[	I"Eprojects//state_machine_proj/state_machine/machine_collection.rb;TiI"›    #   machines.initialize_states(object) do
    #     # After static state initialization, before dynamic state initialization
    #   end
    # 
    # If no block is provided, then all states will still be initialized.;Fi[	I"@projects//state_machine_proj/state_machine/macro_methods.rb;Ti@I"|    #       end
    #     end
    #   end
    # 
    # The above example will define a state machine named "state" that will;Fi[	@5iLI"X    #       ...
    #     end
    #   end
    # 
    # With a static initial state:;Fi[	@5iTI"Y    #       ...
    #     end
    #   end
    # 
    # With a dynamic initial state:;Fi[	@5i\I"L    #       ...
    #     end
    #   end
    # 
    # == Class Methods;Fi[	@5ivI"w    #       end
    #     end
    #   end
    #   
    #   Vehicle.human_state_name(:parked)         # => "parked";Fi[	@5ißI"U    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new;Fi[	@5i‹I"}    #       ...
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">;Fi[	@5iÌI"{    #     def initialize
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c77678>;Fi[	@5i I"Å    #       super()
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">;Fi[	@5iI"ì    #       initialize_state_machines
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">;Fi[	@5i,I"}    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">;Fi[	@5iII"|    #       end
    #     end
    #   end
    # 
    # ...will generate the following instance methods (assuming they're not;Fi[	@5iaI"ã    #       start :idling, :value => 1
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new # => #<Vehicle:0xb712da60 @state=0>;Fi[	@5iòI"^    #       end
    #     end
    #   end
    #   
    #   # Fire +ignite+ event directly;Fi[	@5i”I"Ñ    #       end
    #     end
    #   end
    # 
    # The above class defines two state machines: +heater_state+ and +alarm_state+.;Fi[	I"Bprojects//state_machine_proj/state_machine/matcher_helpers.rb;TiI"}    #       end
    #     end
    #   end
    # 
    # In the above example, +all+ will match the following states since they;Fi[	@Ti0I"    #       end
    #     end
    #   end
    # 
    # In the above example, +same+ will match whichever the from state is.  In;Fi[	I"Cprojects//state_machine_proj/state_machine/state_collection.rb;TiI"v    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states;Fi[	@Yi-I"v    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states;Fi[	@YiHI"v    #       other_states :idling
    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states;Fi[	I"@projects//state_machine_proj/state_machine/state_context.rb;Ti(I"ı    #       transition :from => [:idling, :parked], :on => :park, :unless => :seatbelt_on?  # Transitions to :parked if seatbelt is off
    #     end
    #   end
    # 
    # See StateMachine::Machine#transition for a description of the possible;Fi[	@›iµI"U    #       ...
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new;Fi[	@›iÈI"U    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new;Fi[	@›iI"    #       end
    #     end
    #   end
    #   
    #   vehicle = Vehicle.new     # => #<Vehicle:0xb7b7f568 @state="parked">;Fi[	I"puts("STRING");F@ii@[@[	I"0projects//backup_proj/backup/cli/utility.rb;Ti©I"u        unless options.any? then
          puts
          puts("STRING")
          puts("STRING")
          puts;Fi[	@li¨I"m          puts("STRING")
          puts
          puts("STRING")
          puts("STRING")
          puts;Fi[	@liØI"m          puts("STRING")
          puts
          puts("STRING")
          puts("STRING")
          exit;Fi[	@li∑I"|            puts
            puts(var)
            puts("STRING")
            puts("STRING")
            puts("STRING");Fi[	@li∏I"Å            puts(var)
            puts("STRING")
            puts("STRING")
            puts("STRING")
            puts("STRING");Fi[	@liπI"~            puts("STRING")
            puts("STRING")
            puts("STRING")
            puts("STRING")
          end;Fi[	@liøI"|        if options["LITERAL"] then
          puts
          puts("STRING")
          puts("STRING")
          puts("STRING");Fi[	@li¿I"w          puts
          puts("STRING")
          puts("STRING")
          puts("STRING")
          puts("STRING");Fi[	@li¡I"€          puts("STRING")
          puts("STRING")
          puts("STRING")
          puts("STRING")
          puts(`gem install #{options["LITERAL"]}STRING#{Backup::Dependency.all[options["LITERAL"]]["LITERAL"]}STRING`);Fi[	I"3projects//compass_proj/compass/dependencies.rb;TiI"[    require("sass")
  rescue LoadError
    puts("STRING")
    puts("STRING")
    raise;Fi[	I"%projects//homebrew_proj/build.rb;Ti^I"Ç    if ARGV.flag?("STRING") then
      ohai("STRING")
      puts("STRING")
      puts("STRING")
      if ARGV.flag?("STRING") then;Fi[	@ÇiaI"|      puts("STRING")
      if ARGV.flag?("STRING") then
        puts("STRING")
        puts("STRING")
        puts("STRING");Fi[	@ÇibI"v      if ARGV.flag?("STRING") then
        puts("STRING")
        puts("STRING")
        puts("STRING")
      end;Fi[	@áigI"é  # we try to keep output minimal
  def function
    puts("STRING")
    puts("STRING")
    puts("STRING") if HOMEBREW_PREFIX.to_s.!=("STRING");Fi[	@áisI"t      puts("STRING") if gcc_40
      puts("STRING") if gcc_42
      puts("STRING")
      puts("STRING")
    end;Fi[	@áiÄI"O  
  def function
    puts("STRING")
    puts("STRING")
    puts("STRING");Fi[	@áiÅI"_  def function
    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts("STRING");Fi[	@áiÇI"c    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts("STRING");Fi[	@áiÉI"c    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts(hardware);Fi[	@áiãI"Ü    puts("STRING") if gcc_40
    puts("STRING") if gcc_42
    puts("STRING")
    puts("STRING")
    puts("STRING") if macports_or_fink;Fi[	@áiéI"w    puts("STRING")
    puts("STRING") if macports_or_fink
    puts("STRING")
    puts("STRING")
    puts("STRING");Fi[	@áièI"w    puts("STRING") if macports_or_fink
    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts("STRING");Fi[	@áiêI"c    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts("STRING");Fi[	@áiëI"V    puts("STRING")
    puts("STRING")
    puts("STRING")
    puts("STRING")
  end;Fi[	I"*projects//homebrew_proj/cmd/bottle.rb;TiI"•      ohai("STRING")
      safe_system("STRING", "STRING", (var / var), "STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING") if (var > "LITERAL");Fi[	@üiI"u      puts("STRING")
      puts("STRING") if (var > "LITERAL")
      puts("STRING")
      puts("STRING")
    end;Fi[	I")projects//homebrew_proj/cmd/fetch.rb;TiI"á      next unless var.kind_of?(Pathname)
      puts("STRING") unless var
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@§iI"o      puts("STRING") unless var
      puts("STRING")
      puts("STRING")
      puts("STRING")
      begin;Fi[	I"+projects//homebrew_proj/cmd/install.rb;Ti:I"y    unless MacOS.macports_or_fink.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
    end;Fi[	I"'projects//homebrew_proj/cmd/irb.rb;TiI"v  def function
    if ARGV.include?("STRING") then
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@¨iI"|    if ARGV.include?("STRING") then
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@¨iI"a      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
    else;Fi[	I"'projects//homebrew_proj/cmd/tap.rb;Ti#I"Ñ    puts("STRING")
    curl("STRING", "STRING", "STRING") rescue puts
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@≥i$I"Ü    curl("STRING", "STRING", "STRING") rescue puts
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@≥i%I"c      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts;Fi[	@≥i(I"Y      puts("STRING")
      puts
      puts("STRING")
      puts("STRING")
      puts;Fi[	I"&projects//homebrew_proj/debrew.rb;Ti]I"Iend
def function(var, var)
  puts("STRING")
  puts("STRING")
  begin;Fi[	@$iÓI"ü    if (var["LITERAL"] == "LITERAL") and (var["LITERAL"] <= "LITERAL") then
      onoe("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@$iÔI"`      onoe("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
    end;Fi[	I"1projects//homebrew_proj/formula_installer.rb;Ti“I"à    rescue Exception => var
      onoe("STRING")
      puts("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?;Fi[	@√iÁI"à    rescue Exception => var
      onoe("STRING")
      puts("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?;Fi[	@√iÛI"Å      if f.class.skip_clean_all? then
        opoo("STRING")
        puts("STRING")
        puts("STRING")
        puts("STRING");Fi[	@√iÙI"w        opoo("STRING")
        puts("STRING")
        puts("STRING")
        puts("STRING")
        puts("STRING");Fi[	@√iıI"o        puts("STRING")
        puts("STRING")
        puts("STRING")
        puts("STRING")
        return;Fi[	@√iI"é    if (f.prefix + "STRING").directory? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      @show_summary_heading = true;Fi[	@√i#I"é    if (f.prefix + "STRING").directory? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      @show_summary_heading = true;Fi[	@√i.I"s    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√i/I"m      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√i0I"m      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√i1I"h      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts(var);Fi[	@√iAI"n    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var);Fi[	@√iMI"n    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var);Fi[	@√iYI"n    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var);Fi[	I"/projects//homebrew_proj/formula_support.rb;TiI"Ü    rescue ChecksumMissingError
      opoo("STRING")
      puts("STRING")
      puts("STRING")
    rescue ChecksumMismatchError => var;Fi[	I"3projects//jekyll_proj/jekyll/commands/build.rb;TiI"      rescue Jekyll::FatalException => var
        puts
        puts("STRING")
        puts("STRING")
        puts(var.message);Fi[	I">projects//jekyll_proj/jekyll/migrators/wordpressdotcom.rb;Ti*I"t          end)
        rescue => var
          puts("STRING")
          puts("STRING")
          puts("STRING");Fi[	@Êi+I"~        rescue => var
          puts("STRING")
          puts("STRING")
          puts("STRING")
          puts("STRING");Fi[	@Êi,I"w          puts("STRING")
          puts("STRING")
          puts("STRING")
          puts("STRING")
          next;Fi[	@‡i)I"Ä        else
          puts(Whenever.cron(@options))
          puts("STRING")
          puts("STRING")
          exit("LITERAL");Fi[	@ÓI"
begin;Fi8[8[	@li%I"ﬁ      # as well as the base path for any option specified as a relative path.
      # Any option given as an absolute path will be used "as-is".
      def function
        begin
          (Logger.quiet = options["LITERAL"];Fi[	@ÚiAI"π      # collections to dump, it'll loop through the array of collections and invoke the
      # 'mongodump' command once per collection
      def function
        begin
          (super;Fi[	@‘iZI"R      #
      # @api private
      def function
        begin
          (log!;Fi[	@‘iíI"â      # secret key listing which we don't care about.
      #
      def function
        begin
          (return false unless gpg_homedir;Fi[	@‘i≤I"ü      # We'll cleanup to remove the tempdir (if created) and raise an error.
      #
      def function
        begin
          (return false unless gpg_config;Fi[	@‘iI"ú      # Return false if no :passphrase is set or an error occurs.
      #
      def function
        begin
          (return false if passphrase.to_s.empty?;Fi[	I"-projects//backup_proj/backup/pipeline.rb;Ti'I"ˆ    # Use `#success?` to determine if all commands in the pipeline succeeded.
    # If `#success?` returns `false`, use `#error_messages` to get an error report.
    def function
      begin
        (Open4.popen4(pipeline) do |var, var, var, var|;Fi[	I"4projects//backup_proj/backup/storage/dropbox.rb;Ti3I"Ë      # connection with Dropbox at a later time. This allows the user to avoid
      # having to go to a new Dropbox URL to authorize over and over again.
      def function
        begin
          (return @connection if @connection;Fi[	@i|I"º      # Create a new session, write a serialized version of it to the
      # .cache directory, and return the session object
      def function
        begin
          (require("timeout");Fi[	@i:I"ô      ##
      # Transfers the archived file to the specified remote server
      def function
        begin
          (write_password_file! unless local;Fi[	I"6projects//backup_proj/backup/syncer/rsync/pull.rb;TiI"ë        # Performs the RSync::Pull operation
        # debug options: -vhP
        def function
          begin
            (write_password_file!;Fi[	@i.I"ê        # Performs the RSync:Push operation
        # debug options: -vhP
        def function
          begin
            (write_password_file!;Fi[	I"Lprojects//compass_proj/compass/app_integration/stand_alone/installer.rb;TiI"ò        end
        
        def function
          begin
            (var, Compass.configuration.project_path = Compass.configuration.project_path, nil;Fi[	I":projects//compass_proj/compass/exec/sub_command_ui.rb;TiI"F    end
    
    def function
      begin
        return perform!;Fi[	@iI"S    protected
    
    def function
      begin
        ($command = args.shift;Fi[	I"5projects//compass_proj/compass/exec/switch_ui.rb;TiI"?    end
    
    def function
      begin
        perform!;Fi[	I"1projects//compass_proj/compass/frameworks.rb;Ti2I"I    end
    
    def function
      begin
        (@registered = nil;Fi[	I"@projects//compass_proj/compass/installers/bare_installer.rb;TiI"é      end
      
      def function
        begin
          (var, Compass.configuration.project_path = Compass.configuration.project_path, nil;Fi[	I"-projects//compass_proj/compass/logger.rb;TiI"W    end
    
    def function
      begin
        ($stderr.write(color("LITERAL"));Fi[	@i*I"W    end
    
    def function
      begin
        ($stderr.write(color("LITERAL"));Fi[	I"=projects//diaspora_proj/controllers/photos_controller.rb;TiµI"2  end
  
  def function
    begin
      yield;Fi[	I"6projects//diaspora_proj/models/invitation_code.rb;TiI"U  end
  
  def function
    begin
      self.token = SecureRandom.hex("LITERAL");Fi[	I"4projects//diaspora_proj/models/o_embed_cache.rb;TiI"≠  end
  
  def function
    begin
      var = OEmbed::Providers.get(self.url, "LITERAL" => "LITERAL", "LITERAL" => "LITERAL", "LITERAL" => "LITERAL", "LITERAL" => "LITERAL");Fi[	@ai∫I"T  end
  
  def function
    begin
      (var = URI.parse(@attributes["STRING"]);Fi[	I"+projects//diaspora_proj/models/user.rb;TigI"i  
  #@deprecated
  def function
    begin
      self.invitations_to_me.first.sender.invitation_code;Fi[	I"%projects//haml_proj/haml/exec.rb;TiI"Q      #
      # @see #parse
      def function
        begin
          parse;Fi[	I"?projects//haml_proj/haml/helpers/action_view_extensions.rb;Ti/I"ª      # @yield A block in which all input to `#haml_concat` is treated as raw.
      # @see Haml::Util#rails_xss_safe?
      def function
        begin
          (@_haml_concat_raw = true;Fi[	@~iI"      #
      # @raise [Haml::Error] The error
      def function
        begin
          raise(Haml::Error.new(@message));Fi[	@~iHI"}    #
    # @yield A block which won't register as Haml
    def function
      begin
        (var = @haml_buffer.active?;Fi[	I"'projects//haml_proj/haml/parser.rb;Ti\I"b    end
    
    def function
      begin
        (@root = @parent = ParseNode.new("LITERAL");Fi[	I"%projects//haml_proj/haml/util.rb;Ti/I"í    #
    # @yield A block in which no output will be printed to STDERR
    def function
      begin
        (var, $stderr = $stderr, StringIO.new;Fi[	@ÇiI"∏# the whole of everything must be run in at_exit because the formula has to
# be the run script as __END__ must work for *that* formula.
def function
  begin
    (if ENV["STRING"] then;Fi[	I"-projects//homebrew_proj/cmd/uninstall.rb;Ti
I"f  extend(self)
  
  def function
    begin
      (raise(KegUnspecifiedError) if ARGV.named.empty?;Fi[	@!i0I"M  private
  
  def function
    begin
      if Dir["STRING"].empty? then;Fi[	@$i8I"H  end
  
  def function
    begin
      (if @local_bottle_path then;Fi[	I"+projects//homebrew_proj/extend/ARGV.rb;TiI"7  end
  
  def function
    begin
      (var = nil;Fi[	@Ji°I"9  end
  
  def function
    begin
      (var = clone;Fi[	I"0projects//homebrew_proj/extend/fileutils.rb;TiI"æ  # Create a temporary directory then yield. When the block returns,
  # recursively delete the temporary directory.
  def function
    begin
      (var = (ENV["STRING"].chuzzle or "STRING");Fi[	I"/projects//homebrew_proj/extend/pathname.rb;Ti|I"¶  # it is slow to enumerate the whole directory just to see if it is empty,
  # instead rely on good ol' libc and the filesystem
  def function
    begin
      (rmdir;Fi[	@RiI"7  end
  
  def function
    begin
      (var = nil;Fi[	@√i"I"M  end
  
  def function
    begin
      (if @@attempted.include?(f) then;Fi[	@√i°I"I  end
  
  def function
    begin
      (FileUtils.rm(Dir["STRING"]);Fi[	@√i‚I"P  end
  
  def function
    begin
      Keg.new(f.prefix).fix_install_names;Fi[	@√iÓI"M  end
  
  def function
    begin
      (ohai("STRING") if ARGV.verbose?;Fi[	I"7projects//jekyll_proj/jekyll/converters/textile.rb;TiI"^    pygments_suffix("STRING")
    
    def function
      begin
        (return if @setup;Fi[	I")projects//jekyll_proj/jekyll/site.rb;Ti©I"Z    #
    # Returns nothing.
    def function
      begin
        (var = site_payload;Fi[	I"0projects//nokogiri_proj/nokogiri/version.rb;Ti;I"G    end
    
    def function
      begin
        require("psych");Fi[	I"(projects//rescue_proj/resque/job.rb;TimI"    
    # returns true if payload_class does not raise NameError
    def function
      begin
        payload_class.!=(Object);Fi[	@«i5I"º    # is processing will not be completed. Send the child a TERM signal,
    # wait 5 seconds, and then a KILL signal if it has not quit
    def function
      begin
        if @child then;Fi[	@›i3I"°    # This will return true if the given block halts for a reason other than
    # getting paused.
    def function
      begin
        var = catch("LITERAL") do;Fi[	I"Hprojects//state_machine_proj/state_machine/transition_collection.rb;TiöI"®    # occur will automatically result in the transition rolling back any changes
    # that were made to the object involved.
    def function
      begin
        yield;Fi[	@ÔI"	else;Fi2[2[	@‘i‰I"y        end
        if var and File.exist?(var) then
          "STRING"
        else
          Logger.warn("STRING");Fi[	@Ìi6I"j          "STRING"
        when "LITERAL" then
          "STRING"
        else
          # do nothing;Fi[	@Ùi4I"j          "STRING"
        when "LITERAL" then
          "STRING"
        else
          # do nothing;Fi[	@˚i>I"j          "STRING"
        when "LITERAL" then
          "STRING"
        else
          # do nothing;Fi[	@i2I"j          "STRING"
        when "LITERAL" then
          "STRING"
        else
          # do nothing;Fi[	I"=projects//compass_proj/compass/configuration/comments.rb;TiI"ñ      def function
        if (top_level.preferred_syntax == "LITERAL") and top_level.sass_dir then
          "STRING"
        else
          "STRING";Fi[	I"@projects//compass_proj/compass/configuration/inheritance.rb;Ti<I"â          def function(var)
            if var = @data.raw(var) then
              "STRING"
            else
              var = "STRING";Fi[	I"Jprojects//compass_proj/compass/sass_extensions/functions/constants.rb;Ti!I"á            "STRING"
          when "STRING" then
            "STRING"
          else
            (Compass::Util.compass_warn("STRING");Fi[	@ÖiMI"`          "STRING"
        when "STRING" then
          "STRING"
        else
          var;Fi[	@	i:I"i      "STRING"
    when "LITERAL" then
      "STRING"
    else
      raise(Compass::Error, "STRING");Fi[	@#iÅI"î      var = var["LITERAL"]
      var = if (request.content_type.to_s == "STRING") then
        "STRING"
      else
        request.content_type.to_s;Fi[	I">projects//diaspora_proj/models/acts_as_taggable_on/tag.rb;TiI"ö  def self.normalize(var)
    if var =~ "LITERAL" then
      "STRING"
    else
      var.gsub(/[^#{self.tag_text_regexp}STRING/, "STRING").downcase if var;Fi[	@{i®I"è            (block_given?.! or (var["LITERAL"] or var["LITERAL"]))
          end) then
            "STRING"
          else
            "STRING";Fi[	@{iÎI"h      end
      if @options.html5? then
        "STRING"
      else
        if @options.xhtml? then;Fi[	@{iÔI"≥        if @options.xhtml? then
          if (@node.value["LITERAL"] == "STRING") then
            "STRING"
          else
            if (@node.value["LITERAL"] == "STRING") then;Fi[	@{iÚI"ô          else
            if (@node.value["LITERAL"] == "STRING") then
              "STRING"
            else
              case @node.value["LITERAL"];Fi[	@{i˛I"~                "STRING"
              when "STRING" then
                "STRING"
              else
                "STRING";Fi[	@{i
I"y              "STRING"
            when "STRING" then
              "STRING"
            else
              "STRING";Fi[	@~i≤I"ü    def function(var, var, var)
      var = if var.empty? then
        "STRING"
      else
        ("STRING" << var.map { |var, var| "STRING" }.join("STRING"));Fi[	@≥icI"`      "STRING"
    when /^#{HOMEBREW_LIBRARY}STRING/ then
      "STRING"
    else
      nil;Fi[	@!iêI"t        File.basename(var, "STRING")
      when "LITERAL" then
        "STRING"
      else
        # do nothing;Fi[	@$i%I"f          "STRING"
        when "LITERAL" then
          "STRING"
        else
          "STRING";Fi[	@)i3I"b      "STRING"
    when "LITERAL" then
      "STRING"
    else
      Hardware.processor_count;Fi[	@0i!I"Ç      "STRING"
    when "LITERAL" then
      "STRING"
    else
      (MacOS.version >= "LITERAL") ? ("STRING") : (raise("STRING"));Fi[	@0i[I"        "STRING"
      when "LITERAL" then
        "STRING"
      else
        case (MacOS.clang_version.to_f * "LITERAL").to_i;Fi[	@0iqI"f          "STRING"
        when "LITERAL" then
          "STRING"
        else
          "STRING";Fi[	@êiI"g          "STRING"
        when "LITERAL" then
          "STRING"
        else
          "LITERAL";Fi[	@[i@I"V      "STRING"
    when "LITERAL" then
      "STRING"
    else
      # do nothing;Fi[	@ni_I"V      "STRING"
    when "LITERAL" then
      "STRING"
    else
      # do nothing;Fi[	@niˆI"V      "STRING"
    when "LITERAL" then
      "STRING"
    else
      # do nothing;Fi[	I"?projects//homebrew_proj/vendor/multi_json/vendor/okjson.rb;TisI"é        "STRING"
      when nil then
        "STRING"
      else
        var.respond_to?("LITERAL") ? (var.to_json) : (raise(Error, "STRING"));Fi[	@{i|I"g        var["LITERAL"].to_s
      when "LITERAL" then
        "STRING"
      else
        "STRING";Fi[	I"1projects//jekyll_proj/jekyll/migrators/mt.rb;Ti&I"é    def self.suffix(var)
      if (var.nil? or var.include?("STRING")) then
        "STRING"
      else
        if var.include?("STRING") then;Fi[	@∫i)I"û      else
        if var.include?("STRING") then
          "STRING"
        else
          ((var == "STRING") or var.include?("STRING")) ? ("STRING") : (var);Fi[	@ñiBI"ç    def function
      if (self.site.permalink_style == "LITERAL") and (index?.! and html?) then
        "STRING"
      else
        "STRING";Fi[	@ôiI"r        "STRING"
      when "LITERAL" then
        "STRING"
      else
        self.site.permalink_style.to_s;Fi[	@Üi?I"≤      def function(var)
        var = if ((var.value.first.type == "LITERAL") or var.value.first.value.first =~ "LITERAL") then
          "STRING"
        else
          "STRING";Fi[	@ÜirI"~            "STRING"
          when "STRING" then
            "STRING"
          else
            (var.value.first + "STRING");Fi[	I"5projects//sinatra_proj/sinatra/showexceptions.rb;Ti0I"∫    def function(var)
      if var.filename =~ "LITERAL" then
        "STRING"
      else
        if ((defined? Gem and var.filename.include?(Gem.dir)) or var.filename =~ "LITERAL") then;Fi[	@«i3I"û      else
        if ((defined? Gem and var.filename.include?(Gem.dir)) or var.filename =~ "LITERAL") then
          "STRING"
        else
          "STRING";Fi[	I"Kprojects//state_machine_proj/state_machine/integrations/data_mapper.rb;Ti"I"q      def function(var)
        if var.errors.empty? then
          "STRING"
        else
          var = [];Fi[	I"9projects//twitter_proj/twitter/error/client_error.rb;TiI"~      def self.parse_error(var)
        if var.nil? then
          "STRING"
        else
          if var["LITERAL"] then;Fi[	@°iI"f          "STRING"
        when "LITERAL" then
          "STRING"
        else
          "STRING";Fi[	@ÁiI"o          redirect_from_hash
        when NilClass then
          "STRING"
        else
          "STRING";Fi[	@Ái5I"c          "STRING"
        when stdout then
          "STRING"
        else
          "STRING";Fi[	@rI"# do nothing;Fi2[2[	@Ìi7I"c        when "LITERAL" then
          "STRING"
        else
          # do nothing
        end;Fi[	I"5projects//backup_proj/backup/notifier/hipchat.rb;TiOI"t        when "LITERAL" then
          ["STRING", failure_color]
        else
          # do nothing
        end;Fi[	I"2projects//backup_proj/backup/notifier/mail.rb;TiâI"k        when "LITERAL" then
          ["STRING", true]
        else
          # do nothing
        end;Fi[	@‡i±I"\        when "STRING" then
          {}
        else
          # do nothing
        end;Fi[	@Ùi5I"c        when "LITERAL" then
          "STRING"
        else
          # do nothing
        end;Fi[	@˚i?I"c        when "LITERAL" then
          "STRING"
        else
          # do nothing
        end;Fi[	@i3I"c        when "LITERAL" then
          "STRING"
        else
          # do nothing
        end;Fi[	I"?projects//devise_proj/devise/strategies/authenticatable.rb;Ti$I"`          fail!(var)
          return false
        else
          # do nothing
        end;Fi[	I"6projects//diaspora_proj/helpers/o_embed_helper.rb;TiI"d        var = link_to_oembed_image(var, "STRING")
      end
    else
      # do nothing
    end;Fi[	I"1projects//diaspora_proj/models/invitation.rb;Ti_I"V    when "STRING" then
      I18n.t("STRING")
    else
      # do nothing
    end;Fi[	@ÒikI"K    when "STRING" then
      false
    else
      # do nothing
    end;Fi[	@;i€I"m        when "STRING" then
          var["STRING"] = var
        else
          # do nothing
        end;Fi[	@iI"K  when "STRING" then
    "STRING".undent
  else
    # do nothing
  end;Fi[	I")projects//homebrew_proj/cmd/--env.rb;Ti*I"j          print("STRING") if File.symlink?(var)
        end
      else
        # do nothing
      end;Fi[	I")projects//homebrew_proj/cmd/audit.rb;TigI"r      when "STRING", "STRING" then
        problem("STRING".undent)
      else
        # do nothing
      end;Fi[	@˝iïI"Z      when "LITERAL" then
        "LITERAL"
      else
        # do nothing
      end;Fi[	@˝i´I"b      when "LITERAL" then
        problem("STRING")
      else
        # do nothing
      end;Fi[	I"(projects//homebrew_proj/cmd/edit.rb;Ti+I"^      when "STRING", "STRING" then
        true
      else
        # do nothing
      end;Fi[	@©i I"h    when "LITERAL", "LITERAL" then
      abort("STRING".undent)
    else
      # do nothing
    end;Fi[	@!i|I"a      when "LITERAL" then
        Pathname.new($1)
      else
        # do nothing
      end;Fi[	@!iëI"Y      when "LITERAL" then
        "STRING"
      else
        # do nothing
      end;Fi[	I")projects//homebrew_proj/compilers.rb;Ti;I"f    when "LITERAL" then
      MacOS.gcc_42_build_version.to_i
    else
      # do nothing
    end;Fi[	@i}I"?        end
      end
    else
      # do nothing
    end);Fi[	@$iI"i      @spec = @specs.keys.first
      @ref = @specs.values.first
    else
      # do nothing
    end;Fi[	@$izI"f    when "LITERAL" then
      Dir.chdir(var.first) rescue nil
    else
      # do nothing
    end;Fi[	@$i2I"w      when "LITERAL", "LITERAL" then
        ((var << "STRING") << @ref)
      else
        # do nothing
      end;Fi[	@$iHI"ó            quiet_safe_system(@@git, "STRING", { "LITERAL" => "STRING" }, @ref, "STRING")
          end
        else
          # do nothing
        end;Fi[	@Ri∫I"Y      when "STRING" then
        "LITERAL"
      else
        # do nothing
      end;Fi[	I"#projects//homebrew_proj/keg.rb;TiOI"X    when "LITERAL" then
      (self / "STRING")
    else
      # do nothing
    end;Fi[	I"'projects//homebrew_proj/patches.rb;TiYI"ó      @compressed_filename = (@patch_filename + "STRING")
      FileUtils.mv(@patch_filename, @compressed_filename)
    else
      # do nothing
    end;Fi[	@[i(I"y    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    else
      # do nothing
    end;Fi[	@[iAI"O    when "LITERAL" then
      "STRING"
    else
      # do nothing
    end;Fi[	@ni`I"o    when "LITERAL" then
      "STRING"
    else
      # do nothing
    end, var.bottle.checksum.hexdigest);Fi[	@ni˜I"o    when "LITERAL" then
      "STRING"
    else
      # do nothing
    end, var.bottle.checksum.hexdigest);Fi[	I"8projects//jekyll_proj/jekyll/converters/markdown.rb;TiwI"g      when "STRING" then
        Maruku.new(var).to_html
      else
        # do nothing
      end;Fi[	I"-projects//jekyll_proj/jekyll/core_ext.rb;Ti*I"m        when Array then
          var = var[var].compact
        else
          # do nothing
        end;Fi[	I"5projects//jekyll_proj/jekyll/migrators/tumblr.rb;TiZI"ã        var = var["STRING"]
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?
      else
        # do nothing
      end;Fi[	I"+projects//rescue_proj/resque/server.rb;TiAI"p        when "STRING" then
          Resque.redis.hlen(var)
        else
          # do nothing
        end;Fi[	@3iSI"s        when "STRING" then
          Resque.redis.hgetall(var)
        else
          # do nothing
        end;Fi[	I"Kprojects//state_machine_proj/state_machine/yard/handlers/transition.rb;Ti+I"l              AllMatcher.instance
            end
          else
            # do nothing
          end;Fi[	@ViJI"|          var["LITERAL"] = var.id
          merge_owner!(var, var.user)
        else
          # do nothing
        end;Fi[	@≈i¡I"e        when Twitter::User then
          var.id
        else
          # do nothing
        end;Fi[	I"0projects//twitter_proj/twitter/api/utils.rb;TiëI"ï        when Twitter::User then
          var[[var, "STRING"].compact.join("STRING").to_sym] = var.id
        else
          # do nothing
        end;Fi[	@?iØI"x          when Twitter::User then
            (var << var.id)
          else
            # do nothing
          end;Fi[	@ÕiRI"i        when "LITERAL" then
          "LITERAL".hour
        else
          # do nothing
        end;Fi[	@ÓI"0assert_version_detected("STRING", "STRING");Fi1[1[	I"2projects//homebrew_proj/test/test_versions.rb;TiPI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiTI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiXI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii\I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii`I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IidI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IihI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IilI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IipI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IitI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IixI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii|I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii{I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiÉI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiáI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiãI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IièI"|  end
  
  def function
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiîI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiòI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiúI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii†I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii§I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii®I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii¨I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii∞I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii¥I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii∏I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiºI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii¿I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiƒI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii»I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiÃI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii–I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii‘I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiÿI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii‹I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii‡I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii‰I"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiËI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiÏI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@IiÙI"R  end
  
  def function
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii¸I"È  #   assert_version_detected '7.0.4', 'http://www.haskell.org/ghc/dist/7.0.4/ghc-7.0.4-i386-apple-darwin.tar.bz2'
  # end
  def function
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	I"#     end;F@–i0[0[	@‘iÂI"c      #         'mary.smith@example.com'
      #       ]
      #     end
      #   end
      #;Fi[	@‘i
I"á      #       enc.passphrase = nil
      #       enc.passphrase_file = '/path/to/passphrase.file'
      #     end
      #   end
      #;Fi[	@iôI"r    #     def self.default_url_options
    #       { :locale => I18n.locale }
    #     end
    #   end
    #;Fi[	@iI"m    #     as :user do
    #       root :to => 'devise/registrations#new'
    #     end
    #   end
    #;Fi[	@¡i,I"c      #     configure do |c|
      #       c.foo = 'bar'
      #     end
      #   end
      #;Fi[	@iI"L    #     def name
    #       @name
    #     end
    #   end
    #   ;Fi[	@iI"\    #     def self.run(person)
    #       person.name
    #     end
    #   end
    # ;Fi[	@i"I"c    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   ;Fi[	@iGI"c    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   ;Fi[	@igI"c    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   ;Fi[	@iSI"\    #         transition all => :off
    #       end
    #     end
    #   end
    #   ;Fi[	@iÄI"\    #         transition all => :off
    #       end
    #     end
    #   end
    #   ;Fi[	@"iCI"[        #         end
        #       end
        #     end
        #   end
        # ;Fi[	@%iI"w        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   ;Fi[	@%i8I"        #     before_transition do |transition|
        #       # log message
        #     end
        #   end
        # ;Fi[	@%i\I"w        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   ;Fi[	@%ihI"m        #       block.call
        #       # track end time
        #     end
        #   end
        # ;Fi[	@%i{I"w        #         transition :parked => :idling
        #       end
        #     end
        #   end
        #   ;Fi[	@%iÖI"É        #     after_transition_failure :on => :ignite do
        #       # log failure
        #     end
        #   end
        # ;Fi[	@5i?I"G    #         ...
    #       end
    #     end
    #   end
    # ;Fi[	@5iKI"u    #     state_machine :status, :attribute => :status_value do
    #       ...
    #     end
    #   end
    # ;Fi[	@5iSI"m    #     state_machine :status, :initial => :parked do
    #       ...
    #     end
    #   end
    # ;Fi[	@5i[I"ö    #     state_machine :status, :initial => lambda {|vehicle| vehicle.speed == 0 ? :parked : :idling} do
    #       ...
    #     end
    #   end
    # ;Fi[	@5iuI"g    #         transition :idling => :first_gear
    #       end
    #     end
    #   end
    #   ;Fi[	@5i¶I"c    #         transition :idling => :parked
    #       end
    #     end
    #   end
    #   ;Fi[	@5i€I"n    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #   end
    #   ;Fi[	@5iÏI"K    #     
    #     def initialize
    #     end
    #   end
    #   ;Fi[	@5iˇI"K    #       ...
    #       super()
    #     end
    #   end
    #   ;Fi[	@5iI"]    #       ...
    #       initialize_state_machines
    #     end
    #   end
    #   ;Fi[	@5i+I"I    #         ...
    #       end
    #     end
    #   end
    #   ;Fi[	@5iHI"]    #         transition all => :idling
    #       end
    #     end
    #   end
    # ;Fi[	@5i`I"u    #       state :parked, :value => 0
    #       start :idling, :value => 1
    #     end
    #   end
    #   ;Fi[	@5ióI"\    #         transition all => :off
    #       end
    #     end
    #   end
    #   ;Fi[	@5i“I"Z    #         transition all => :off
    #       end
    #     end
    #   end
    # ;Fi[	@TiI"h    #         transition all - :parked => :stalled
    #       end
    #     end
    #   end
    # ;Fi[	@Ti/I"m    #         transition [:idling, :first_gear] => same
    #       end
    #     end
    #   end
    # ;Fi[	@YiI"w    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   ;Fi[	@Yi,I"w    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   ;Fi[	@YiGI"w    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end
    #   ;Fi[	@`i'I"    #       transition :to => :idling, :on => [:ignite, :shift_up]                          # Transitions to :idling
    #       transition :from => [:idling, :parked], :on => :park, :unless => :seatbelt_on?  # Transitions to :parked if seatbelt is off
    #     end
    #   end
    # ;Fi[	@›i¥I"c    #     state_machine :action => :save do
    #       ...
    #     end
    #   end
    #   ;Fi[	@›iËI"c    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   ;Fi[	@›iI"c    #         transition :parked => :idling
    #       end
    #     end
    #   end
    #   ;Fi[	@ÍI""LITERAL";Fi&[&[	@˝ièI"{      next if var.nil?
      var = case var.hash_type
      when "LITERAL" then
        "LITERAL"
      when "LITERAL" then;Fi[	@˝iëI"v      when "LITERAL" then
        "LITERAL"
      when "LITERAL" then
        "LITERAL"
      when "LITERAL" then;Fi[	@˝iìI"g      when "LITERAL" then
        "LITERAL"
      when "LITERAL" then
        "LITERAL"
      else;Fi[	@Ri§I"Ñ    File.open(self) { |var| var = var.read("LITERAL") }
    case var
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@Ri¶I"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@Ri®I"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@Ri™I"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@Ri¨I"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@RiÆI"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@Ri∞I"]    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    else;Fi[	@)i	I"å    @@cpu_type ||= `/usr/sbin/sysctl -n hw.cputype`.to_i
    case @@cpu_type
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@)iI"]    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    else;Fi[	@)iI"ñ    @@intel_family ||= `/usr/sbin/sysctl -n hw.cpufamily`.to_i
    case @@intel_family
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@)iI"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@)iI"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@)iI"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@)iI"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@)iI"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@)i!I"]    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    else;Fi[	@izI"l        when *var then
          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        else;Fi[	@iÅI"~        when "STRING" then
          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        when "STRING" then;Fi[	@iâI"        when "STRING" then
          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        when "LITERAL" then;Fi[	@iãI"        when "LITERAL" then
          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        when "STRING" then;Fi[	@ièI"~        when "STRING" then
          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        when "STRING" then;Fi[	I"$projects//homebrew_proj/mach.rb;TiGI"i  def function
    case archs.length
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	I"%projects//homebrew_proj/macos.rb;TiQI"g  def function
    case default_cc
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@1iSI"k    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "STRING" then;Fi[	I"3projects//homebrew_proj/test/test_compilers.rb;TiI"}    var.select_compiler
    assert_equal(case MacOS.clang_build_version
    when "LITERAL" then
      "LITERAL"
    else;Fi[	@6iYI"}    var.select_compiler
    assert_equal(case MacOS.clang_build_version
    when "LITERAL" then
      "LITERAL"
    else;Fi[	@¶i´I"v  compare do |var|
    super(Version.new(case var
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@¶i≠I"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@¶iØI"l    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then;Fi[	@¶i±I"]    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    else;Fi[	I"module Nokogiri;FI"module XML;Fi%[%[	I"1projects//nokogiri_proj/nokogiri/xml/attr.rb;Ti I"7module Nokogiri
  module XML
    class Attr < Node;Fi [	@°i I"Omodule Nokogiri
  module XML
    class AttributeDecl < Nokogiri::XML::Node;Fi [	I"4projects//nokogiri_proj/nokogiri/xml/builder.rb;Ti I"3module Nokogiri
  module XML
    class Builder;Fi [	@§i I"Gmodule Nokogiri
  module XML
    class CDATA < Nokogiri::XML::Text;Fi [	I";projects//nokogiri_proj/nokogiri/xml/character_data.rb;Ti I"Omodule Nokogiri
  module XML
    class CharacterData < Nokogiri::XML::Node;Fi [	@ßi I"Jmodule Nokogiri
  module XML
    class Document < Nokogiri::XML::Node;Fi [	@¨i I"Rmodule Nokogiri
  module XML
    class DocumentFragment < Nokogiri::XML::Node;Fi [	I"0projects//nokogiri_proj/nokogiri/xml/dtd.rb;Ti I"Emodule Nokogiri
  module XML
    class DTD < Nokogiri::XML::Node;Fi [	I"<projects//nokogiri_proj/nokogiri/xml/element_content.rb;Ti I":module Nokogiri
  module XML
    class ElementContent;Fi [	@Øi I"Mmodule Nokogiri
  module XML
    class ElementDecl < Nokogiri::XML::Node;Fi [	@≤i I"Lmodule Nokogiri
  module XML
    class EntityDecl < Nokogiri::XML::Node;Fi [	I"6projects//nokogiri_proj/nokogiri/xml/namespace.rb;Ti I"5module Nokogiri
  module XML
    class Namespace;Fi [	I">projects//nokogiri_proj/nokogiri/xml/node/save_options.rb;Ti I"0module Nokogiri
  module XML
    class Node;Fi [	@µiI"nrequire("stringio")
require("nokogiri/xml/node/save_options")
module Nokogiri
  module XML
    class Node;Fi[	@∏i I"3module Nokogiri
  module XML
    class NodeSet;Fi [	I"5projects//nokogiri_proj/nokogiri/xml/notation.rb;Ti I"bmodule Nokogiri
  module XML
    class Notation < Struct.new("LITERAL", "LITERAL", "LITERAL");Fi [	I":projects//nokogiri_proj/nokogiri/xml/parse_options.rb;Ti I"8module Nokogiri
  module XML
    class ParseOptions;Fi [	@ªi I"/module Nokogiri
  module XML
    module PP;Fi [	I"4projects//nokogiri_proj/nokogiri/xml/pp/node.rb;Ti I"/module Nokogiri
  module XML
    module PP;Fi [	I"Cprojects//nokogiri_proj/nokogiri/xml/processing_instruction.rb;Ti I"Hmodule Nokogiri
  module XML
    class ProcessingInstruction < Node;Fi [	I"3projects//nokogiri_proj/nokogiri/xml/reader.rb;Ti I"2module Nokogiri
  module XML
    class Reader;Fi [	I"5projects//nokogiri_proj/nokogiri/xml/relax_ng.rb;Ti I"3module Nokogiri
  module XML
    class << self;Fi [	I"9projects//nokogiri_proj/nokogiri/xml/sax/document.rb;Ti I"0module Nokogiri
  module XML
    module SAX;Fi [	I"7projects//nokogiri_proj/nokogiri/xml/sax/parser.rb;Ti I"0module Nokogiri
  module XML
    module SAX;Fi [	I"?projects//nokogiri_proj/nokogiri/xml/sax/parser_context.rb;Ti I"0module Nokogiri
  module XML
    module SAX;Fi [	I"<projects//nokogiri_proj/nokogiri/xml/sax/push_parser.rb;Ti I"0module Nokogiri
  module XML
    module SAX;Fi [	I"3projects//nokogiri_proj/nokogiri/xml/schema.rb;Ti I"3module Nokogiri
  module XML
    class << self;Fi [	I"9projects//nokogiri_proj/nokogiri/xml/syntax_error.rb;Ti I"Qmodule Nokogiri
  module XML
    class SyntaxError < ::Nokogiri::SyntaxError;Fi [	I"1projects//nokogiri_proj/nokogiri/xml/text.rb;Ti I"Omodule Nokogiri
  module XML
    class Text < Nokogiri::XML::CharacterData;Fi [	I"?projects//nokogiri_proj/nokogiri/xml/xpath/syntax_error.rb;Ti I"1module Nokogiri
  module XML
    class XPath;Fi [	I"2projects//nokogiri_proj/nokogiri/xml/xpath.rb;TiI"\require("nokogiri/xml/xpath/syntax_error")
module Nokogiri
  module XML
    class XPath;Fi[	I":projects//nokogiri_proj/nokogiri/xml/xpath_context.rb;Ti I"8module Nokogiri
  module XML
    class XPathContext;Fi [	I"# @rate_limited No;F@i$[$[	@ioI"•      # @see https://dev.twitter.com/docs/api/1.1/post/direct_messages/destroy
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@i|I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/direct_messages/new
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@)i3I"       #
      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@)iKI"ˇ      #
      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@)ilI"¸      #
      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::AlreadyFavorited] Error raised when tweet has already been favorited.;Fi[	@0iyI"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0içI"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0i™I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@0iæI"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/update
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViTI"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/lists/members/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Vi†I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/lists/subscribers/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Vi◊I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/lists/subscribers/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViÒI"å      # @see https://dev.twitter.com/docs/api/1.1/post/lists/members/create_all
      # @note Lists are limited to having 500 members, and you are limited to adding up to 100 members to a list at a time with this method.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViII"2      # @see https://dev.twitter.com/docs/api/1.1/post/lists/members/create
      # @note Lists are limited to having 500 members.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@VifI"+      # @see https://dev.twitter.com/docs/api/1.1/post/lists/destroy
      # @note Must be owned by the authenticated user.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViÄI"˚      #
      # @see https://dev.twitter.com/docs/api/1.1/post/lists/update
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ViüI"#      # @see https://dev.twitter.com/docs/api/1.1/post/lists/create
      # @note Accounts are limited to 20 lists.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@Vi‚I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/lists/members/destroy_all
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@i@I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/saved_searches/create
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@iPI"T      # @see https://dev.twitter.com/docs/api/1.1/post/saved_searches/destroy/:id
      # @note The search specified by ID must be owned by the authenticating user.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßi\I"M      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/destroy/:id
      # @note The authenticating user must be the author of the specified Tweets.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßirI"{      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/update
      # @note A status update with text identical to the authenticating user's current status will be ignored to prevent duplicates.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@ßi∏I"y      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/update_with_media
      # @note A status update with text/media identical to the authenticating user's current status will NOT be ignored
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈i>I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/account/update_delivery_device
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iNI"7      # @see https://dev.twitter.com/docs/api/1.1/post/account/update_profile
      # @note Only the options specified will be updated.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈i`I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/account/update_profile_background_image
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈ipI"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/account/update_profile_colors
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iÄI"c      # @note Updates the authenticating user's profile image. Note that this method expects raw multipart data, not a URL to an image.
      # @note This method asynchronously processes the uploaded file before updating the user's profile image URL. You can either update your local cache the next time you request the user's information, or, at least 5 seconds after uploading the image, ask for the updated URL using GET users/show.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iﬁI"˝      #
      # @see https://dev.twitter.com/docs/api/1.1/post/blocks/destroy
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈i~I"      #
      # @see https://dev.twitter.com/docs/api/1.1/post/account/remove_profile_banner
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;Fi[	@≈iéI"˛      # @note Uploads a profile banner on behalf of the authenticating user. For best results, upload an <5MB image that is exactly 1252px by 626px. Images will be resized for a number of display options. Users with an uploaded profile banner will have a profile_banner_url node in their Users objects. More information about sizing variations can be found in User Profile Images and Banners.
      # @note Profile banner images are processed asynchronously. The profile_banner_url and its variant sizes will not necessary be available directly after upload.
      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::BadRequest] Error raised when either an image was not provided or the image data could not be processed.;Fi[	@—I"#   class Vehicle;Fi#[#[	@iI"i    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@i=I"i    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@i?I"h    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@iqI"h    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@%iI"u        # 
        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource;Fi[	@%iQI"u        # 
        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource;Fi[	@%iuI"t        # 
        # == Example
        # 
        #   class Vehicle
        #     include DataMapper::Resource;Fi[	@5i9I"    # 
    # With the default name/attribute and no configuration:
    # 
    #   class Vehicle
    #     state_machine do;Fi[	@5iGI"â    # 
    # With a custom name / attribute:
    # 
    #   class Vehicle
    #     state_machine :status, :attribute => :status_value do;Fi[	@5iOI"~    # 
    # With a static initial state:
    # 
    #   class Vehicle
    #     state_machine :status, :initial => :parked do;Fi[	@5iWI"±    # 
    # With a dynamic initial state:
    # 
    #   class Vehicle
    #     state_machine :status, :initial => lambda {|vehicle| vehicle.speed == 0 ? :parked : :idling} do;Fi[	@5ikI"r    # 
    # For example,
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do;Fi[	@5iúI"r    # 
    # For example,
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do;Fi[	@5i◊I"r    # 
    # For example,
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do;Fi[	@5iÂI"£    # 
    # In the following example, a custom +initialize+ method is defined:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do;Fi[	@5iˆI"∞    # are called, the custom method *must* call +super+ without any arguments
    # like so:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do;Fi[	@5iI"’    # If you want to avoid calling the superclass's constructor, but still want
    # to initialize the state machine attributes:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do;Fi[	@5iI"÷    # set.  For example, the following example forces the initialization of
    # static states regardless of their current value:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do;Fi[	@5iBI"ø    # When a new state is defined, a predicate method for that state is
    # generated on the class.  For example,
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@5iXI"ß    # state machine.  In most cases, this is the same as the name of the state
    # machine.  For example:
    # 
    #   class Vehicle
    #     attr_accessor :state;Fi[	@5iàI"b    # 
    # For example,
    # 
    #   class Vehicle
    #     include DataMapper::Resource;Fi[	@5iæI"â    # 
    # For example,
    # 
    #   class Vehicle
    #     state_machine :heater_state, :initial => :off, :namespace => 'heater' do;Fi[	@Ti
I"U    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine do;Fi[	@Ti)I"U    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine do;Fi[	@YiI"i    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@Yi(I"i    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@YiCI"i    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@›i∞I"f    # 
    # == Examples
    # 
    #   class Vehicle
    #     state_machine :action => :save do;Fi[	@›i‚I"T    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine do;Fi[	@›i˚I"h    # 
    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do;Fi[	@˚@ri![![	I"4projects//devise_proj/devise/models/lockable.rb;Ti\I"é        else
          if lock_strategy_enabled?("LITERAL") and attempts_exceeded? then
            "LITERAL"
          else
            super;Fi[	I"6projects//diaspora_proj/helpers/publics_helper.rb;TiI"é    if var.save then
      if (var["LITERAL"] == "STRING") then
        "LITERAL"
      else
        "LITERAL" if (var["LITERAL"] == "STRING");Fi[	I"'projects//homebrew_proj/cleaner.rb;Ti&I"  def function(var)
    var = if (var.mach_o_executable? or var.text_executable?) then
      "LITERAL"
    else
      "LITERAL";Fi[	@˝iîI"b        "LITERAL"
      when "LITERAL" then
        "LITERAL"
      else
        # do nothing;Fi[	I"*projects//homebrew_proj/cmd/create.rb;TiI"Ñ    var.url = var
    var.mode = if ARGV.include?("STRING") then
      "LITERAL"
    else
      "LITERAL" if ARGV.include?("STRING");Fi[	@ijI"ß    when "LITERAL" then
      if @compilers.include?("LITERAL") then
        "LITERAL"
      else
        @compilers.include?("LITERAL") ? ("LITERAL") : (ENV.compiler);Fi[	@ipI"Ω    when "LITERAL" then
      if @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then
        "LITERAL"
      else
        if @compilers.include?("LITERAL") then;Fi[	@isI"¢      else
        if @compilers.include?("LITERAL") then
          "LITERAL"
        else
          @compilers.include?("LITERAL") ? ("LITERAL") : (ENV.compiler);Fi[	@izI"Ω    when "LITERAL" then
      if @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then
        "LITERAL"
      else
        if @compilers.include?("LITERAL") then;Fi[	@i}I"¢      else
        if @compilers.include?("LITERAL") then
          "LITERAL"
        else
          @compilers.include?("LITERAL") ? ("LITERAL") : (ENV.compiler);Fi[	@Ri±I"X      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    else
      case extname;Fi[	@RiπI"a        "LITERAL"
      when "STRING" then
        "LITERAL"
      else
        # do nothing;Fi[	@)iI"U      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    else
      "LITERAL";Fi[	@)i"I"U      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    else
      "LITERAL";Fi[	@i{I"i          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        else
          "LITERAL";Fi[	@iíI"h          "LITERAL"
        when "STRING" then
          "LITERAL"
        else
          "LITERAL";Fi[	@.i.I"l        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      else
        "LITERAL";Fi[	@.i8I"j        "LITERAL"
      when "LITERAL", "LITERAL" then
        "LITERAL"
      else
        "LITERAL";Fi[	@1iI"t  def function
    if (version == "LITERAL") then
      "LITERAL"
    else
      if (version == "LITERAL") then;Fi[	@1iI"v    else
      if (version == "LITERAL") then
        "LITERAL"
      else
        if (version == "LITERAL") then;Fi[	@1iI"â      else
        if (version == "LITERAL") then
          "LITERAL"
        else
          (version == "LITERAL") ? ("LITERAL") : (nil);Fi[	@1iVI"n      "LITERAL"
    when "STRING" then
      "LITERAL"
    else
      if (Xcode.version >= "STRING") then;Fi[	@1iYI"è    else
      if (Xcode.version >= "STRING") then
        "LITERAL"
      else
        (Xcode.version >= "STRING") ? ("LITERAL") : ("LITERAL");Fi[	@[i`I"≈    raise(TypeError, "STRING") unless var.is_a?(X11Dependency)
    if var.min_version.nil? then
      "LITERAL"
    else
      @min_version.nil? ? ("LITERAL") : ((@min_version <=> var.min_version));Fi[	@6iI"u    assert_equal(case MacOS.clang_build_version
    when "LITERAL" then
      "LITERAL"
    else
      "LITERAL";Fi[	@6iZI"u    assert_equal(case MacOS.clang_build_version
    when "LITERAL" then
      "LITERAL"
    else
      "LITERAL";Fi[	@µi)I"Ú        var[(var + "LITERAL")] = Utagx.|((var >> "LITERAL").&(Umaskx)).chr
        var[(var + "LITERAL")] = Utagx.|(var.&(Umaskx)).chr
        "LITERAL"
      else
        (var[(var + "LITERAL")] = Utag4.|((var >> "LITERAL").&("LITERAL")).chr;Fi[	@¶i≤I"T      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    else
      var.to_s;Fi[	@r@Ôi [ [	@‡iúI"±        var = if ["STRING", "STRING", "STRING", "STRING", "STRING"].index(@delivery_method.to_s) then
          @delivery_method.to_s
        else
          "STRING"
        end;Fi[	@iI"è        if (top_level.preferred_syntax == "LITERAL") and top_level.sass_dir then
          "STRING"
        else
          "STRING"
        end;Fi[	@#i|I"x        if var = top_level.fonts_dir_without_default then
          var
        else
          "STRING"
        end;Fi[	I"Bprojects//compass_proj/compass/configuration/serialization.rb;TiCI"ä        if var.respond_to?("LITERAL") then
          (var.serialize_to_config(var) + "STRING")
        else
          "STRING"
        end;Fi[	@i8I"ø      if Compass.configuration.color_output and (var and COLORS.has_key?(var.to_sym)) then
        defined? $boring and $boring ? ("STRING") : ("STRING")
      else
        "STRING"
      end;Fi[	I"?projects//diaspora_proj/controllers/profiles_controller.rb;TiJI"ä    @profile_attrs["LITERAL"] = if params["LITERAL"] then
      params["LITERAL"].gsub("STRING", "STRING")
    else
      "STRING"
    end;Fi[	I":projects//diaspora_proj/helpers/application_helper.rb;Ti	I"w    if AppConfig.settings.pod_name.present? then
      AppConfig.settings.pod_name
    else
      "STRING"
    end;Fi[	I"9projects//diaspora_proj/helpers/open_graph_helper.rb;Ti'I"¢    var = if AppConfig.services.facebook.open_graph_namespace.present? then
      AppConfig.services.facebook.open_graph_namespace
    else
      "STRING"
    end;Fi[	@oi;I"s    if image_path("STRING").include?("STRING") then
      image_path("STRING")
    else
      "STRING"
    end;Fi[	@{i©I"a          end) then
            "STRING"
          else
            "STRING"
          end));Fi[	@{iˇI"|              when "STRING" then
                "STRING"
              else
                "STRING"
              end;Fi[	@{iI"r            when "STRING" then
              "STRING"
            else
              "STRING"
            end;Fi[	@{i"I"Ä      var = if (var.! or (@dont_indent_next_line or @options["LITERAL"])) then
        var
      else
        "STRING"
      end;Fi[	@$i&I"`        when "LITERAL" then
          "STRING"
        else
          "STRING"
        end);Fi[	@0irI"_        when "LITERAL" then
          "STRING"
        else
          "STRING"
        end;Fi[	@{i}I"U      when "LITERAL" then
        "STRING"
      else
        "STRING"
      end;Fi[	@ñiCI"Ü      if (self.site.permalink_style == "LITERAL") and (index?.! and html?) then
        "STRING"
      else
        "STRING"
      end;Fi[	I"1projects//jekyll_proj/jekyll/tags/include.rb;TiI"          var = Liquid::Template.parse(var)
          var.stack { var.render(var) }
        else
          "STRING"
        end;Fi[	@ÜiI"Ã          if var.value["LITERAL"].is_a?(Nokogiri::CSS::Node) and (var.value["LITERAL"].type == "LITERAL") then
            an_plus_b(var.value["LITERAL"])
          else
            "STRING"
          end;Fi[	@Üi@I"¶        var = if ((var.value.first.type == "LITERAL") or var.value.first.value.first =~ "LITERAL") then
          "STRING"
        else
          "STRING"
        end;Fi[	@ iDI"~    self.redis = if Redis.respond_to?("LITERAL") then
      Redis.connect("LITERAL" => (true))
    else
      "STRING"
    end;Fi[	@«i4I"ü        if ((defined? Gem and var.filename.include?(Gem.dir)) or var.filename =~ "LITERAL") then
          "STRING"
        else
          "STRING"
        end;Fi[	I"Hprojects//state_machine_proj/state_machine/yard/handlers/machine.rb;TirI"≤          @state_type ||= if machine.states.any? then
            machine.states.map { |var| var.name }.compact.first.class.to_s
          else
            "STRING"
          end;Fi[	@°i I"_        when "LITERAL" then
          "STRING"
        else
          "STRING"
        end;Fi[	I",projects//whenever_proj/whenever/job.rb;TiI"†      @options["LITERAL"] = if var.has_key?("LITERAL") then
        Whenever::Output::Redirection.new(var["LITERAL"]).to_s
      else
        "STRING"
      end;Fi[	@ÁiI"^        when NilClass then
          "STRING"
        else
          "STRING"
        end;Fi[	@Ái6I"\        when stdout then
          "STRING"
        else
          "STRING"
        end;Fi[	@—I"def function(var);Fi [ [	@‘i'I"†      # @raise [Backup::Errors::Encryptor::GPG::InvalidModeError]
      #   if mode given is invalid.
      #
      def function(var)
        @mode = var.to_sym;Fi[	@‘i«I"≥      # If so, raise the returned error message.
      # Note that Cli::Helpers#run may also raise an error here.
      #
      def function(var)
        var = run("STRING").chomp;Fi[	@‘i9I"Œ      # Strip out all spaces, upcase non-email identifiers,
      # and wrap email addresses in <> to perform exact matching.
      #
      def function(var)
        var = var.to_s.gsub("LITERAL", "STRING");Fi[	@Ìi.I"À      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var = case var;Fi[	@›iFI"–      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var, var = case var;Fi[	@‡iÄI"–      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var, var = case var;Fi[	@Ùi,I"À      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var = case var;Fi[	@˚i6I"À      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var = case var;Fi[	@i*I"À      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var = case var;Fi[	@Âi]I"Æ      #   sign_out :user     # sign_out(scope)
      #   sign_out @user     # sign_out(resource)
      #
      def function(var)
        return sign_out_all_scopes unless var;Fi[	@Âi~I"î      #
      #   redirect_to stored_location_for(:user) || root_path
      #
      def function(var)
        var = Devise::Mapping.find_scope!(var);Fi[	@Âi•I"~      #       end
      #   end
      #
      def function(var)
        (stored_location_for(var) or signed_in_root_path(var));Fi[	I"4projects//devise_proj/devise/mailers/helpers.rb;TiTI"ˆ      #         confirmation_instructions:
      #           subject: '...'
      #
      def function(var)
        I18n.t(:"#{devise_mapping.name}STRING", "LITERAL" => (["LITERAL", "LITERAL", var]), "LITERAL" => (["LITERAL", var.to_s.humanize]));Fi[	@0i2I"¬    # with specific values for this model. Please check your Devise initializer
    # for a complete description on those values.
    #
    def function(var)
      var = var.extract_options!.dup;Fi[	@i≥I"¨    #       post "deactivate", :to => "registrations#deactivate", :as => "deactivate_registration"
    #     end
    #
    def function(var)
      @devise_finalized = false;Fi[	@iI"ô    #
    #   root :to => 'dashboard#show'
    #
    def function(var)
      var = lambda { |var| var.env["STRING"].authenticate?("LITERAL" => (var)).! };Fi[	I"1projects//devise_proj/devise/test_helpers.rb;Ti3I"•    #   sign_out :user     # sign_out(scope)
    #   sign_out @user     # sign_out(resource)
    #
    def function(var)
      var = Devise::Mapping.find_scope!(var);Fi[	@;i¥I"    #   (however, "x?," is a method call and not a literal)
    # - and not "?\," which is a character literal
    #
    def function(var)
      var and ((var.length > "LITERAL") and ((var["LITERAL"] == "STRING") and (var["LITERAL"] =~ "LITERAL" or (var["LITERAL"] == "STRING")).!));Fi[	@i>I"é      #     config.format.as_xml
      #   end
      #
      def function(var)
        var["LITERAL"] ||= XML::Node::SaveOptions::DEFAULT_HTML;Fi[	@µiÜI"z      #     end
      #   }.new)
      #
      def function(var)
        return NodeSet.new(document) unless document;Fi[	@µi∑I"¬      # found in an XML document, where tags names are case-sensitive
      # (e.g., "H1" is distinct from "h1").
      #
      def function(var)
        var, var, var, var = extract_params(var);Fi[	@µi‘I"ò      # Equivalent to <tt>xpath(paths).first</tt>
      # See Node#xpath for more information.
      #
      def function(var)
        xpath(*var).first;Fi[	@µi›I"í      # Equivalent to <tt>css(rules).first</tt>
      # See Node#css for more information.
      #
      def function(var)
        css(*var).first;Fi[	@∏iÑI"õ      # Equivalent to <tt>xpath(paths).first</tt>
      # See NodeSet#xpath for more information.
      #
      def function(var)
        xpath(*var).first;Fi[	@∏içI"ï      # Equivalent to <tt>css(rules).first</tt>
      # See NodeSet#css for more information.
      #
      def function(var)
        css(*var).first;Fi[	I"1projects//whenever_proj/whenever/job_list.rb;TiDI"“    # and breaks it into variable/value pairs. Used for setting variables at runtime from the command line.
    # Only works for setting values as strings.
    #
    def function(var)
      return if var.blank?;Fi[	@“i`I"Î    # at 3:02am and 4:02am, instead of creating two jobs this method combines
    # them into one that runs on the 2nd minute at the 3rd and 4th hour.
    #
    def function(var)
      var.map! { |var| var.split("LITERAL", "LITERAL") };Fi[	I"#       end;F@¢i[[	@Íi]I"      #       def pending_notifications
      #         @pending_notifications ||= []
      #       end
      #     end
      #;Fi[	@~i§I"b    #           haml_concat "more_data"
    #         end
    #       end
    #     end
    #;Fi[	@i!I"w    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end;Fi[	@iFI"w    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end;Fi[	@ifI"w    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end;Fi[	@iHI"t    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #     ;Fi[	@iRI"q    #       event :disable do
    #         transition all => :off
    #       end
    #     end
    #   end;Fi[	@izI"t    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #     ;Fi[	@iI"q    #       event :disable do
    #         transition all => :off
    #       end
    #     end
    #   end;Fi[	@"iBI"h        #           end
        #         end
        #       end
        #     end
        #   end;Fi[	@%iI"Ü        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end;Fi[	@%i[I"Ü        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end;Fi[	@%iI"Ü        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end
        #   end;Fi[	@5i>I"[    #       event :park do
    #         ...
    #       end
    #     end
    #   end;Fi[	@5itI"}    #       event :shift_up do
    #         transition :idling => :first_gear
    #       end
    #     end
    #   end;Fi[	@5i•I"u    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end
    #   end;Fi[	@5i*I"|    #       initialize_state_machines(:static => :force) do
    #         ...
    #       end
    #     end
    #   end;Fi[	@5iGI"s    #       event :ignite do
    #         transition all => :idling
    #       end
    #     end
    #   end;Fi[	@5iêI"v    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #     ;Fi[	@5iñI"q    #       event :disable do
    #         transition all => :off
    #       end
    #     end
    #   end;Fi[	@5i«I"q    #       event :turn_off do
    #         transition all => :off
    #       end
    #     end
    #     ;Fi[	@5i—I"r    #       event :turn_off do
    #         transition all => :off
    #       end
    #     end
    #   end;Fi[	@TiI"z    #       event :crash
    #         transition all - :parked => :stalled
    #       end
    #     end
    #   end;Fi[	@Ti.I"{    #       event :ignite
    #         transition [:idling, :first_gear] => same
    #       end
    #     end
    #   end;Fi[	@›iÁI"w    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end;Fi[	@›i I"w    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end
    #   end;Fi[	I"+problem("STRING") if text =~ "LITERAL";F@	i[[	@˝i≤I"å  
  def function
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    unless (f.name == "STRING") then;Fi[	@˝iπI"í      end
    end
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i∫I"≥    end
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iªI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iºI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iΩI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iæI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iøI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i¿I"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i¡I"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i¬I"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i√I"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iƒI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i≈I"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i∆I"Ó    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL" and text =~ "LITERAL".!;Fi[	@˝i…I"Ó    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL" and text =~ "LITERAL".!
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i I"Ó    problem("STRING") if text =~ "LITERAL" and text =~ "LITERAL".!
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iÀI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iÃI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iÕI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iŒI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝iœI"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i–I"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i—I"÷    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL";Fi[	@˝i“I"±    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
    problem("STRING") if text =~ "LITERAL"
  end;Fi[	I"(when var = @ss.scan("LITERAL") then;FI" action { ["LITERAL", var] };Fi[[	I"6projects//nokogiri_proj/nokogiri/css/tokenizer.rb;Ti;I"®        when nil then
          case
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	i=I"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	i?I"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iAI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iCI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iEI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iGI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iII"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iKI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iMI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iOI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iQI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iSI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iUI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iWI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iYI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	i[I"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	i]I"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	i_I"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iaI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	icI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	ieI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	igI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	iiI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@G	ikI"Ÿ          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then;Fi[	@˚@Íi[[	@˝iêI"{      var = case var.hash_type
      when "LITERAL" then
        "LITERAL"
      when "LITERAL" then
        "LITERAL";Fi[	@˝iíI"n        "LITERAL"
      when "LITERAL" then
        "LITERAL"
      when "LITERAL" then
        "LITERAL";Fi[	@Ri•I"a    case var
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@RißI"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@Ri©I"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@Ri´I"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@Ri≠I"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@RiØI"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@)i
I"h    case @@cpu_type
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@)iI"l    case @@intel_family
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@)iI"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@)iI"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@)iI"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@)iI"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@)i I"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@iyI"s          "LITERAL"
        when *var then
          "LITERAL"
        when "LITERAL" then
          "LITERAL";Fi[	@iÄI"w          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "LITERAL" then
          "LITERAL";Fi[	@iàI"w          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "LITERAL" then
          "LITERAL";Fi[	@iäI"x          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        when "LITERAL" then
          "LITERAL";Fi[	@iéI"w          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "LITERAL" then
          "LITERAL";Fi[	@.iHI"l    case archs.length
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      archs.first;Fi[	@1iRI"h    case default_cc
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@¶i¨I"s    super(Version.new(case var
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@¶iÆI"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@¶i∞I"d      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "LITERAL" then
      "LITERAL";Fi[	@D	@C	i[[	@G	i<I"∫          case
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	i>I"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	i@I"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iBI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iDI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iFI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iHI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iJI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iLI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iNI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iPI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iRI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iTI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iVI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iXI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	iZI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	i\I"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	i^I"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	i`I"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	ibI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	idI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	ifI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	ihI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	ijI"”            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] };Fi[	@G	ilI"Õ            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { ["LITERAL", var] }
          when var = @ss.scan("LITERAL") then
            action { [var, var] };Fi[	@—I"# Returns nothing.;Fi[[	I"0projects//jekyll_proj/jekyll/convertible.rb;TiI"¢    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(var, var);Fi[	@Â	i#I"o    
    # Transform the contents based on the content type.
    #
    # Returns nothing.
    def function;Fi[	@Â	i=I"é    # payload - The site payload Hash.
    # layouts - A Hash of {"name" => "layout"}.
    #
    # Returns nothing.
    def function(var, var);Fi[	I":projects//jekyll_proj/jekyll/generators/pagination.rb;TiI"T    #
    # site - The Site.
    #
    # Returns nothing.
    def function(var);Fi[	I"+projects//jekyll_proj/jekyll/layout.rb;Ti&I"r    #
    # name - The String filename of the layout file.
    #
    # Returns nothing.
    def function(var);Fi[	@ñi$I"¢    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(var, var);Fi[	@ñi\I"p    #
    # name - The String filename of the page file.
    #
    # Returns nothing.
    def function(var);Fi[	@ñigI"ö    # layouts      - The Hash of {"name" => "layout"}.
    # site_payload - The site payload Hash.
    #
    # Returns nothing.
    def function(var, var);Fi[	@ñi~I"r    #
    # dest - The String path to the destination dir.
    #
    # Returns nothing.
    def function(var);Fi[	@ôiBI"¢    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(var, var);Fi[	@ôiYI"p    #
    # name - The String filename of the post file.
    #
    # Returns nothing.
    def function(var);Fi[	@ôi∫I"ò    # layouts      - A Hash of {"name" => "layout"}.
    # site_payload - The site payload hash.
    #
    # Returns nothing.
    def function(var, var);Fi[	@ôiœI"r    #
    # dest - The String path to the destination dir.
    #
    # Returns nothing.
    def function(var);Fi[	@bi"I"s    
    # Public: Read, process, and write this Site to output.
    #
    # Returns nothing.
    def function;Fi[	@biBI"|    
    # Load necessary libraries, plugins, converters, and generators.
    #
    # Returns nothing.
    def function;Fi[	@biUI"|    
    # Read Site data from disk and load it into internal data structures.
    #
    # Returns nothing.
    def function;Fi[	@bi^I"ê    # Read all the files in <source>/<layouts> and create a new Layout object
    # with each one.
    #
    # Returns nothing.
    def function;Fi[	@bipI"Ñ    #
    # dir - The String relative path of the directory to read. Default: ''.
    #
    # Returns nothing.
    def function(var);Fi[	@biàI"|    #
    # dir - The String relative path of the directory to read.
    #
    # Returns nothing.
    def function(var);Fi[	@bi†I"Y    
    # Run each of the Generators.
    #
    # Returns nothing.
    def function;Fi[	@bißI"a    
    # Render the site to the destination.
    #
    # Returns nothing.
    def function;Fi[	@bi∂I"y    
    # Remove orphaned files and empty directories in destination.
    #
    # Returns nothing.
    def function;Fi[	@biŸI"c    
    # Write static files, pages, and posts.
    #
    # Returns nothing.
    def function;Fi[	I"$projects//jekyll_proj/jekyll.rb;Ti
I"k#
# path - The String relative path from here to the directory.
#
# Returns nothing.
def function(var);Fi[	@ÓI"
super;Fi[[	I"3projects//backup_proj/backup/database/mysql.rb;Ti6I"¨      # Performs the mysqldump command and outputs the
      # data to the specified path based on the 'trigger'
      def function
        super
        var = Pipeline.new;Fi[	I"8projects//backup_proj/backup/database/postgresql.rb;Ti6I"©      # Performs the pgdump command and outputs the
      # data to the specified path based on the 'trigger'
      def function
        super
        var = Pipeline.new;Fi[	@ıi4I"€      # the Redis server to persist the current state to the dump file
      # before copying the dump to get the most recent updates in to the backup
      def function
        super
        invoke_save! if invoke_save;Fi[	@¯i%I"Ã      # Performs the riak-admin command and outputs the
      # data to the specified path based on the 'trigger'
      def function
        super
        FileUtils.chown_R("STRING", "STRING", @dump_path);Fi[	I"6projects//backup_proj/backup/syncer/cloud/base.rb;Ti'I"Œ        # the Cloud specific defaults are set here before evaluating
        # any block provided in the user's configuration file.
        def function
          super
          @concurrency_type ||= false;Fi[	@iI"Æ        # Instantiates a new RSync Syncer object
        # and sets the default configuration
        def function
          super
          @additional_options ||= Array.new;Fi[	I"<projects//compass_proj/compass/commands/project_base.rb;TiI"@      end
      
      def function
        super
      end;Fi[	I"0projects//compass_proj/compass/test_case.rb;TiI"Z    Test::Unit::TestCase
  end
    def function
      super
      @last_compile = nil;Fi[	I"7projects//devise_proj/devise/models/confirmable.rb;TißI"Y      end
      
      def function
        super
        confirm! unless confirmed?;Fi[	I"Dprojects//diaspora_proj/controllers/registrations_controller.rb;TiI",  end
  
  def function
    super
  end;Fi[	@1i—I"–      # Processes the options set by the command-line arguments,
      # and runs the Haml compiler appropriately.
      def function
        super
        @options["LITERAL"]["LITERAL"] = @options["LITERAL"];Fi[	@Ri\I"fend
module ObserverPathnameExtension
  def function
    super
    puts("STRING") if ARGV.verbose?;Fi[	@RibI"J  end
  
  def function
    super
    puts("STRING") if ARGV.verbose?;Fi[	@RimI"J  end
  
  def function
    super
    puts("STRING") if ARGV.verbose?;Fi[	@RirI"J  end
  
  def function
    super
    puts("STRING") if ARGV.verbose?;Fi[	I"Lprojects//state_machine_proj/state_machine/integrations/active_model.rb;TirI"Ñ      
      # Initializes class-level extensions and defaults for this machine
      def function
        super
        load_locale;Fi[	@A
iñI"®      
      # Adds hooks into validation for automatically firing events
      def function
        super
        define_validation_hook if runs_validations_on_action?;Fi[	I"Vprojects//state_machine_proj/state_machine/integrations/active_record/versions.rb;Ti>I"Õ          (action == "LITERAL") ? ("LITERAL") : (super)
        end
        def function
          super
          unless (::ActiveRecord::Observer < StateMachine::Integrations::ActiveModel::Observer) then;Fi[	@Ãi2I"ë      
      # Initializes class-level extensions and defaults for this machine
      def function
        super
        load_observer_extensions;Fi[	@ÃijI"™      
      # Adds hooks into validation for automatically firing events
      def function
        super
        if (action == "LITERAL") and supports_validations? then;Fi[	I"Fprojects//state_machine_proj/state_machine/integrations/sequel.rb;TiWI"≠      
      # Defines validation hooks if the machine's action is to save the model
      def function
        super
        define_validation_hook if (action == "LITERAL");Fi[	@oi’I"_    
    # Resets callback tracking
    def function
      super
      @before_run = false;Fi[	@oi€I"É    
    # Resets the event attribute so it can be re-evaluated if attempted again
    def function
      super
      each do |var|;Fi[	I"Zprojects//state_machine_proj/state_machine/yard/templates/default/class/html/setup.rb;TiI"ërequire("tempfile")
# Define where state machine descriptions will be rendered
def function
  super
  sections.place("LITERAL").before("LITERAL");Fi[	I"when "STRING" then;F@Ôi[[	@ÖiI"ù        if var.is_a?(Sass::Script::String) then
          var = case var.value
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÖiI"Ä          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÖiI"Ä          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÖiI"Ä          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@Öi I"w          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else;Fi[	@ÖiDI"ç      var = var.value.split("LITERAL").map do |var|
        case var
        when "STRING" then
          "STRING"
        when "STRING" then;Fi[	@ÖiFI"{        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then;Fi[	@ÖiHI"{        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then;Fi[	@ÖiJI"{        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then;Fi[	@ÖiLI"m        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        else;Fi[	@{iıI"ï            else
              case @node.value["LITERAL"]
              when "STRING" then
                "STRING"
              when "STRING" then;Fi[	@{i˜I"î              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then;Fi[	@{i˘I"î              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then;Fi[	@{i˚I"î              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then;Fi[	@{i˝I"Ü              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              else;Fi[	@{iI"û          if @options.html4? then
            case @node.value["LITERAL"]
            when "STRING" then
              "STRING"
            when "STRING" then;Fi[	@{i	I"|            when "STRING" then
              "STRING"
            when "STRING" then
              "STRING"
            else;Fi[	@ÜigI"ä          when "STRING", "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÜiiI"Ä          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÜikI"Ä          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÜimI"Ä          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÜioI"Ä          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then;Fi[	@ÜiqI"w          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else;Fi[	I"version("STRING") do;FI"def self.active?;Fi[[	I"Uprojects//state_machine_proj/state_machine/integrations/active_model/versions.rb;TiI"æ  module Integrations
    module ActiveModel
      version("STRING") do
        def self.active?
          (defined? ::ActiveModel::VERSION.! or (::ActiveModel::VERSION::MAJOR == "LITERAL"));Fi[	@å
iI"“      end
      
      version("STRING") do
        def self.active?
          defined? ::ActiveModel::VERSION and ((::ActiveModel::VERSION::MAJOR == "LITERAL") and (::ActiveModel::VERSION::MINOR == "LITERAL"));Fi[	@F
iI"   module Integrations
    module ActiveRecord
      version("STRING") do
        def self.active?
          ((::ActiveRecord::VERSION::MAJOR == "LITERAL") or ((::ActiveRecord::VERSION::MAJOR == "LITERAL") and (::ActiveRecord::VERSION::MINOR == "LITERAL")));Fi[	@F
iI"|      end
      
      version("STRING") do
        def self.active?
          (::ActiveRecord::VERSION::MAJOR == "LITERAL");Fi[	@F
iHI"≠      end
      
      version("STRING") do
        def self.active?
          (::ActiveRecord::VERSION::MAJOR == "LITERAL") and (::ActiveRecord::VERSION::MINOR < "LITERAL");Fi[	@F
iQI"ﬁ      end
      
      version("STRING") do
        def self.active?
          (::ActiveRecord::VERSION::MAJOR == "LITERAL") and ((::ActiveRecord::VERSION::MINOR < "LITERAL") or (::ActiveRecord::VERSION::TINY < "LITERAL"));Fi[	@F
iZI"·      end
      
      version("STRING") do
        def self.active?
          (::ActiveRecord::VERSION::MAJOR == "LITERAL") and ((::ActiveRecord::VERSION::MINOR == "LITERAL") and (::ActiveRecord::VERSION::TINY >= "LITERAL"));Fi[	@F
icI"Æ      end
      
      version("STRING") do
        def self.active?
          (::ActiveRecord::VERSION::MAJOR == "LITERAL") and (::ActiveRecord::VERSION::MINOR == "LITERAL");Fi[	I"Tprojects//state_machine_proj/state_machine/integrations/data_mapper/versions.rb;TiI"å  module Integrations
    module DataMapper
      version("STRING") do
        def self.active?
          ::DataMapper::VERSION =~ "LITERAL";Fi[	@ù
iI"ô      end
      
      version("STRING") do
        def self.active?
          (::DataMapper::VERSION =~ "LITERAL" or ::DataMapper::VERSION =~ "LITERAL");Fi[	@ù
iI"v      end
      
      version("STRING") do
        def self.active?
          ::DataMapper::VERSION =~ "LITERAL";Fi[	@ù
i%I"w      end
      
      version("STRING") do
        def self.active?
          (::DataMapper::VERSION == "STRING");Fi[	I"Uprojects//state_machine_proj/state_machine/integrations/mongo_mapper/versions.rb;TiI"å  module Integrations
    module MongoMapper
      version("STRING") do
        def self.active?
          defined? ::MongoMapper::Plugins.!;Fi[	@¶
iI"ô      end
      
      version("STRING") do
        def self.active?
          (defined? ::MongoMapper::Version.! or ::MongoMapper::Version =~ "LITERAL");Fi[	@¶
iI"ô      end
      
      version("STRING") do
        def self.active?
          (defined? ::MongoMapper::Version.! or ::MongoMapper::Version =~ "LITERAL");Fi[	@¶
iBI"Ω      end
      
      version("STRING") do
        def self.active?
          defined? ::MongoMapper::Plugins and (defined? ::MongoMapper::Version.! or ::MongoMapper::Version =~ "LITERAL");Fi[	@¶
iKI"ñ      end
      
      version("STRING") do
        def self.active?
          defined? ::MongoMapper::Version and ::MongoMapper::Version =~ "LITERAL";Fi[	I"Pprojects//state_machine_proj/state_machine/integrations/mongoid/versions.rb;TiI"Ü  module Integrations
    module Mongoid
      version("STRING") do
        def self.active?
          ::Mongoid::VERSION =~ "LITERAL";Fi[	@±
iI"s      end
      
      version("STRING") do
        def self.active?
          ::Mongoid::VERSION =~ "LITERAL";Fi[	@ÕiI"Ì  module Integrations
    module Sequel
      version("STRING") do
        def self.active?
          (defined? ::Sequel::MAJOR.! or ((::Sequel::MAJOR == "LITERAL") or ((::Sequel::MAJOR == "LITERAL") and (::Sequel::MINOR <= "LITERAL"))));Fi[	@ÕiI"≤      end
      
      version("STRING") do
        def self.active?
          (defined? ::Sequel::MAJOR.! or ((::Sequel::MAJOR == "LITERAL") and (::Sequel::MINOR <= "LITERAL")));Fi[	@Õi0I"÷      end
      
      version("STRING") do
        def self.active?
          (defined? ::Sequel::MAJOR.! or ((::Sequel::MAJOR == "LITERAL") or ((::Sequel::MAJOR == "LITERAL") and (::Sequel::MINOR <= "LITERAL"))));Fi[	@Õi<I"‘      end
      
      version("STRING") do
        def self.active?
          defined? ::Sequel::MAJOR and ((::Sequel::MAJOR == "LITERAL") and ((::Sequel::MINOR >= "LITERAL") and (::Sequel::MINOR <= "LITERAL")));Fi[	@Ó@ÿi[[	I"1projects//backup_proj/backup/storage/base.rb;Ti2I"á      ##
      # Each subclass must define a +path+ where remote files will be stored
      def function
        # do nothing
      end;Fi[	@iOI"j      ##
      # Note: Storage::RSync doesn't cycle
      def function
        # do nothing
      end;Fi[	@i.I"Q        end
        
        def function
          # do nothing
        end;Fi[	@iI"i  module Installers
    class BareInstaller < Base
      def function
        # do nothing
      end;Fi[	@iI"G      end
      
      def function
        # do nothing
      end;Fi[	@*i&I"§      # The default prepare method -- it is a no-op.
      # Generally you would create required directories, etc.
      def function
        # do nothing
      end;Fi[	@Íi.I"G      end
      
      def function
        # do nothing
      end;Fi[	@ÌiQI"G      end
      
      def function
        # do nothing
      end;Fi[	I"7projects//devise_proj/devise/models/recoverable.rb;TiQI"G      end
      
      def function
        # do nothing
      end;Fi[	I"Aprojects//devise_proj/devise/models/token_authenticatable.rb;Ti%I"m      
      # Hook called after token authentication.
      def function
        # do nothing
      end;Fi[	@di:I"3  end
  
  def function
    # do nothing
  end;Fi[	I".projects//diaspora_proj/models/service.rb;Ti'I"3  end
  
  def function
    # do nothing
  end;Fi[	@{izI"=    end
    
    def function
      # do nothing
    end;Fi[	I"=projects//haml_proj/haml/helpers/safe_erubis_template.rb;TiI"nmodule Haml
  class SafeErubisTemplate < Tilt::ErubisTemplate
    def function
      # do nothing
    end;Fi[	I"-projects//homebrew_proj/extend/string.rb;Ti%I"=end
class NilClass
  def function
    # do nothing
  end;Fi[	@ÄiI"v        ###
        # Called when document starts parsing
        def function
          # do nothing
        end;Fi[	@ÄiI"t        ###
        # Called when document ends parsing
        def function
          # do nothing
        end;Fi[	I"<projects//omni_proj/omniauth/test/strategy_test_case.rb;TiI"G      end
      
      def function
        # do nothing
      end;Fi[	I"1projects//rescue_proj/resque/failure/base.rb;TiI"Ñ      #
      # This is where you POST or PUT or whatever to your Failure service.
      def function
        # do nothing
      end;Fi[	@¶
i1I"{          (action == "LITERAL") ? ("LITERAL") : (super)
        end
        def function
          # do nothing
        end;Fi[	@¶
i:I"W          true
        end
        def function
          # do nothing
        end;Fi[	@Õi"I"±          (defined? ::Sequel::MAJOR.! or ((::Sequel::MAJOR == "LITERAL") and (::Sequel::MINOR <= "LITERAL")))
        end
        def function
          # do nothing
        end;Fi[	@Õi%I"_          # do nothing
        end
        def function
          # do nothing
        end;Fi[	@—I"# == Examples;Fi[[	I"=projects//state_machine_proj/state_machine/assertions.rb;Ti	I"≤    # Validates that the given hash *only* includes the specified valid keys.
    # If any invalid keys are found, an ArgumentError will be raised.
    #
    # == Examples
    # ;Fi[	@˘
iI"|    # exclusive keys.  If more than one key is found, an ArgumentError will be
    # raised.
    # 
    # == Examples
    # ;Fi[	I"9projects//state_machine_proj/state_machine/branch.rb;Ti=I"ñ    # and to state, this will also check whether the configured :if/:unless
    # conditions pass on the given object.
    # 
    # == Examples
    # ;Fi[	@˛
ifI"•    # * <tt>:guard</tt> - Whether to guard matches with the if/unless
    #   conditionals defined for this branch.  Default is true.
    # 
    # == Examples
    # ;Fi[	@iI"I    # * Method / Proc
    # * String
    # 
    # == Examples
    # ;Fi[	@iI"¶    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # 
    # == Examples
    # ;Fi[	@i;I"¶    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # 
    # == Examples
    # ;Fi[	@i_I"î    # *Note* that if a transition has already been generated for the event, then
    # that transition will be used.
    # 
    # == Examples
    # ;Fi[	@%iI"≥        # See StateMachine::Machine#before_transition for more
        # information about the various configuration options available.
        # 
        # == Examples
        # ;Fi[	@%iOI"µ        # Creates a callback that will be invoked *around* a transition so long
        # as the given requirements match the transition.
        # 
        # == Examples
        # ;Fi[	@5i5I"à    # For examples on the types of state machine configurations and blocks, see
    # the section below.
    # 
    # == Examples
    # ;Fi[	@÷iI"n    
    # Generates a blacklist matcher based on the given set of values
    # 
    # == Examples
    # ;Fi[	@÷i8I"É    # Checks whether the given value exists within the whitelist configured
    # for this matcher.
    # 
    # == Examples
    # ;Fi[	@÷iKI"Ñ    # Checks whether the given value exists outside the blacklist configured
    # for this matcher.
    # 
    # == Examples
    # ;Fi[	@÷ifI"û    # Checks whether the given value matches what the value originally was.
    # This value should be defined in the context.
    # 
    # == Examples
    # ;Fi[	@TiI"|  module MatcherHelpers
    # Represents a state that matches all known states in a machine.
    # 
    # == Examples
    # ;Fi[	@Ti'I"§    # Represents a state that matches the original +from+ state.  This is useful
    # for defining transitions which are loopbacks.
    # 
    # == Examples
    # ;Fi[	@YiI"Ö    # false, otherwise true.  If the given state is unknown, then an IndexError
    # will be raised.
    # 
    # == Examples
    # ;Fi[	@Yi&I"ö    # state machine.  This will attempt to find a known state that matches
    # the value of the attribute on the object.
    # 
    # == Examples
    # ;Fi[	@YiAI"|    # state machine.  If no state is found, then an ArgumentError will be
    # raised.
    # 
    # == Examples
    # ;Fi[	@›iÆI"ô    # with the transition.  The action associated with the transition/machine
    # can be skipped by passing in +false+.
    # 
    # == Examples
    # ;Fi[	I"var = var["LITERAL"];F@%i[[	@xiπI"≠    # just like you can do with `dom_id()` and `dom_class()` in Rails
    def function(var)
      var = var["LITERAL"]
      var = var["LITERAL"]
      return {} if var.nil?;Fi[	@{iÑI"z        var = false
      else
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@{iÖI"~      else
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@{iÜI"ê        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@{iáI"}        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
      end;Fi[	I"5projects//jekyll_proj/jekyll/migrators/drupal.rb;TiI"∂      File.open("STRING", "STRING") { |var| var.puts("STRING") }
      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@2iI"í      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@2iI"é        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = Time.at(var);Fi[	I"5projects//jekyll_proj/jekyll/migrators/joomla.rb;TiI"à      var = "STRING"
      var[var].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@9iI"ê      var[var].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@9iI"≤        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = ("STRING" % [var.year, var.month, var.day, var]);Fi[	I"7projects//jekyll_proj/jekyll/migrators/mephisto.rb;TiI"ó      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@@iI"í      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@@iI"¡        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = ([var.year, var.month, var.day, var].join("STRING") + "STRING");Fi[	@∫iI"™        var = var["LITERAL"]
        var = var["LITERAL"].gsub("LITERAL", "STRING")
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@∫iI"™        var = var["LITERAL"].gsub("LITERAL", "STRING")
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@∫iI"©        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = ((var + "STRING") + var) if var.!=(nil);Fi[	I":projects//jekyll_proj/jekyll/migrators/textpattern.rb;TiI"ó      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@MiI"í      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@MiI"ª        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = var["LITERAL"]
        var = ([var.strftime("STRING"), var].join("STRING") + "STRING");Fi[	I"8projects//jekyll_proj/jekyll/migrators/wordpress.rb;Ti"I"ï    
    def self.process_post(var, var, var)
      var = var["LITERAL"]
      var = var["LITERAL"]
      var = clean_entities(var) if var["LITERAL"];Fi[	I"# For example,;F@—i[[	@~i≈I"Ω    # Returns a hash containing default assignments for the `xmlns`, `lang`, and `xml:lang`
    # attributes of the `html` HTML element.
    # For example,
    #
    #     %html{html_attrs};Fi[	@~iôI"^    # use \{#capture\_haml\}.
    #
    # For example,
    #
    #     haml_tag :table do;Fi[	@i0I"v    # then all arguments are required.
    # 
    # For example,
    # 
    #   person = Person.new('John Smith');Fi[	I"8projects//state_machine_proj/state_machine/event.rb;TiƒI"°    # Generates a nicely formatted description of this event's contents.
    # 
    # For example,
    # 
    #   event = StateMachine::Event.new(machine, :park);Fi[	@5ijI"n    #   libraries if supported by the integration.
    # 
    # For example,
    # 
    #   class Vehicle;Fi[	@5iõI"x    #   conditionals defined for each one.  Default is true.
    # 
    # For example,
    # 
    #   class Vehicle;Fi[	@5i÷I"c    # without properly calling +super+.
    # 
    # For example,
    # 
    #   class Vehicle;Fi[	@5iáI"O    # the machine).
    # 
    # For example,
    # 
    #   class Vehicle;Fi[	@5iΩI"f    # between the various states / events.
    # 
    # For example,
    # 
    #   class Vehicle;Fi[	@5iÚI"≥    # attribute set to one of a given set of states.
    # 
    # For example,
    # 
    #   Vehicle.with_state(:parked)               # => All vehicles where the state is parked;Fi[	I"7projects//state_machine_proj/state_machine/path.rb;Ti+I"¨    # Lists all of the from states that can be reached through this path.
    # 
    # For example,
    # 
    #   path.to_states  # => [:parked, :idling, :first_gear, ...];Fi[	@oi:I"™    # Lists all of the to states that can be reached through this path.
    # 
    # For example,
    # 
    #   path.to_states  # => [:parked, :idling, :first_gear, ...];Fi[	@oiCI"ù    # Lists all of the events that can be fired through this path.
    # 
    # For example,
    # 
    #   path.events # => [:park, :ignite, :shift_up, ...];Fi[	I"Bprojects//state_machine_proj/state_machine/path_collection.rb;Ti-I"{    # this collection.
    # 
    # For example,
    # 
    #   paths.from_states # => [:parked, :idling, :first_gear, ...];Fi[	@vi7I"~    # this collection.
    # 
    # For example,
    # 
    #   paths.to_states # => [:idling, :first_gear, :second_gear, ...];Fi[	@viAI"s    # collection.
    # 
    # For example,
    # 
    #   paths.events  # => [:park, :ignite, :shift_up, ...];Fi[	I"8projects//state_machine_proj/state_machine/state.rb;TipI"«    # Generates a human-readable description of this state's name / value:
    # 
    # For example,
    # 
    #   State.new(machine, :parked).description                               # => "parked";Fi[	@}iÅI"Ö    # returned.
    # 
    # For example,
    # 
    #   State.new(machine, :parked, :value => 1).value                        # => 1;Fi[	@}iòI"y    # Otherwise, the matcher will determine the result.
    # 
    # For example,
    # 
    #   # Without a matcher;Fi[	@}i‹I"¬    # Generates a nicely formatted description of this state's contents.
    # 
    # For example,
    # 
    #   state = StateMachine::State.new(machine, :parked, :value => 1, :initial => true);Fi[	@›i#I"Õ    # Generates a nicely formatted description of this transitions's contents.
    # 
    # For example,
    # 
    #   transition = StateMachine::Transition.new(object, machine, :ignite, :parked, :idling);Fi[	I"var;F@ri[[	@‘iI"ò          var = clean_identifier(var)
          if system_identifiers.include?(var) then
            var
          else
            var = user_keys[var];Fi[	@#i{I"      def function
        if var = top_level.fonts_dir_without_default then
          var
        else
          "STRING";Fi[	@#i®I"é      def function(var)
        if (File::SEPARATOR == "STRING") then
          var
        else
          var.gsub(File::SEPARATOR, "STRING");Fi[	I"<projects//compass_proj/compass/configuration/helpers.rb;Ti!I"î        else
          if var.is_a?(Compass::Configuration::Data) then
            var
          else
            if var.respond_to?("LITERAL") then;Fi[	@ikI"      else
        if var = var.manifest(var).help then
          var
        else
          "STRING".gsub("LITERAL", "STRING");Fi[	I"Qprojects//compass_proj/compass/sass_extensions/functions/gradient_support.rb;Ti›I"ö      Sass::Script::List.new(var.map do |var|
        if ColorStop.===(var) then
          var
        else
          if Sass::Script::Color.===(var) then;Fi[	I"8projects//devise_proj/devise/models/rememberable.rb;Ti5I"è        else
          if respond_to?("LITERAL") and var = authenticatable_salt then
            var
          else
            raise("STRING");Fi[	I":projects//diaspora_proj/helpers/markdownify_helper.rb;Ti#I"n    var.gsub("LITERAL") do |var|
      if var =~ "LITERAL" then
        var
      else
        var.strip!;Fi[	I"7projects//diaspora_proj/helpers/notifier_helper.rb;TiI"≈      var = truncate(var.formatted_message("LITERAL" => (true)), "LITERAL" => (var["LITERAL"]))
      var = process_newlines(var) if var["LITERAL"]
      var
    else
      I18n.translate("STRING");Fi[	I"3projects//diaspora_proj/models/notification.rb;Ti"I"h        if var then
          var.email_the_user(var, var)
          var
        else
          nil;Fi[	@¢iNI"Y        nil
      end
      var
    else
      make_notification(var, var, var, var);Fi[	@{i!I"ñ    def function(var, var, var)
      var = if (var.! or (@dont_indent_next_line or @options["LITERAL"])) then
        var
      else
        "STRING";Fi[	I"9projects//haml_proj/haml/helpers/action_view_mods.rb;TiwI"º          var = (form_tag_without_haml(var, var, *var, &var) + "STRING")
          (var << "STRING") if var
          var
        else
          form_tag_without_haml(var, var, *var, &var);Fi[	@$iÌI"Õ  def self.detect(var, var)
    if var.is_a?(Class) and var.ancestors.include?(AbstractDownloadStrategy) then
      var
    else
      var.is_a?(Symbol) ? (detect_from_symbol(var)) : (detect_from_url(var));Fi[	@0i*I"¢    @prefix ||= (var = Pathname.new(folder)
    if var.absolute? and (var / "STRING").executable? then
      var
    else
      if File.executable?("STRING") then;Fi[	I"Nprojects//homebrew_proj/vendor/multi_json/adapters/nsjsonserialization.rb;TiI"É        if var then
          var = symbolize_keys(var) if var["LITERAL"]
          var
        else
          super(var, var = {});Fi[	@bi I"å      var = self.converters.select { |var| (var.class == var) }
      if var = var.first then
        var
      else
        raise("STRING");Fi[	@µieI"°          var = var.to_s
          if var =~ "LITERAL" then
            var
          else
            CSS.xpath_for(var, "LITERAL" => (var), "LITERAL" => (var));Fi[	@¡iYI"|        var = options[var].call(env)
        return nil unless var.is_a?(String)
        var
      else
        options[var];Fi[	@«i-I"V        var = new(*var)
        var.to_s = var
        var
      else
        nil;Fi[	@A
i∞I"           @callbacks[((var == "LITERAL") ? ("LITERAL") : (var))].insert("LITERAL", (var = Callback.new(var, var, &var)))
          add_states(var.known_states)
          var
        else
          super;Fi[	@ˆ
@—i[[	@˘
i
I"ö    # If any invalid keys are found, an ArgumentError will be raised.
    #
    # == Examples
    # 
    #   options = {:name => 'John Smith', :age => 30};Fi[	@˘
iI"d    # raised.
    # 
    # == Examples
    # 
    #   options = {:only => :on, :except => :off};Fi[	@˛
i>I"ò    # conditions pass on the given object.
    # 
    # == Examples
    # 
    #   branch = StateMachine::Branch.new(:parked => :idling, :on => :ignite);Fi[	@˛
igI"≠    #   conditionals defined for this branch.  Default is true.
    # 
    # == Examples
    # 
    #   branch = StateMachine::Branch.new(:parked => :idling, :on => :ignite);Fi[	@iI"    # * String
    # 
    # == Examples
    # 
    # Below are examples of the various ways that a method can be evaluated;Fi[	@iI"w    #   conditionals defined for each one.  Default is true.
    # 
    # == Examples
    # 
    #   class Vehicle;Fi[	@i<I"w    #   conditionals defined for each one.  Default is true.
    # 
    # == Examples
    # 
    #   class Vehicle;Fi[	@i`I"s    # that transition will be used.
    # 
    # == Examples
    # 
    #   class Vehicle < ActiveRecord::Base;Fi[	@%iI"é        # information about the various configuration options available.
        # 
        # == Examples
        # 
        #   class Vehicle;Fi[	@%iPI"        # as the given requirements match the transition.
        # 
        # == Examples
        # 
        #   class Vehicle;Fi[	@5i6I"y    # the section below.
    # 
    # == Examples
    # 
    # With the default name/attribute and no configuration:;Fi[	@÷iI"≠    # Generates a blacklist matcher based on the given set of values
    # 
    # == Examples
    # 
    #   matcher = StateMachine::AllMatcher.instance - [:parked, :idling];Fi[	@÷i9I"Ä    # for this matcher.
    # 
    # == Examples
    # 
    #   matcher = StateMachine::WhitelistMatcher.new([:parked, :idling]);Fi[	@÷iLI"Ä    # for this matcher.
    # 
    # == Examples
    # 
    #   matcher = StateMachine::BlacklistMatcher.new([:parked, :idling]);Fi[	@÷igI"ã    # This value should be defined in the context.
    # 
    # == Examples
    # 
    #   matcher = StateMachine::LoopbackMatcher.instance;Fi[	@Ti	I"    # Represents a state that matches all known states in a machine.
    # 
    # == Examples
    # 
    #   class Vehicle;Fi[	@Ti(I"n    # for defining transitions which are loopbacks.
    # 
    # == Examples
    # 
    #   class Vehicle;Fi[	@YiI"P    # will be raised.
    # 
    # == Examples
    # 
    #   class Vehicle;Fi[	@Yi'I"j    # the value of the attribute on the object.
    # 
    # == Examples
    # 
    #   class Vehicle;Fi[	@YiBI"H    # raised.
    # 
    # == Examples
    # 
    #   class Vehicle;Fi[	@›iØI"f    # can be skipped by passing in +false+.
    # 
    # == Examples
    # 
    #   class Vehicle;Fi[	@—@Ói[[	@‘ikI"≥      ##
      # Remove any temporary directories and reset all instance variables.
      #
      def function
        FileUtils.rm_rf(@tempdirs, "LITERAL" => (true)) if @tempdirs;Fi[	@‘i}I"ª      # messages, like the "Reading passphrase from file descriptor..."
      # messages during symmetric encryption
      #
      def function
        @base_options ||= (var = ["STRING"];Fi[	@‘iëI"´      # we will get those messages on STDOUT for the log, without the actual
      # secret key listing which we don't care about.
      #
      def function
        begin;Fi[	@‘i±I"∫      # If any errors occur during this process, we can not proceed.
      # We'll cleanup to remove the tempdir (if created) and raise an error.
      #
      def function
        begin;Fi[	@‘i–I"’      # Returns the options needed for the gpg command line to perform
      # the encryption based on the #mode.
      #
      def function
        @mode_options ||= (var = symmetric_options if mode.!=("LITERAL");Fi[	@‘i›I"≥      # try to use :passphrase_file if it's set.
      # If the option can not be set, log a warning and return nil.
      #
      def function
        var = setup_passphrase_file;Fi[	@‘iÔI"≠      # return the file's path. If an error occurs, log a warning.
      # Return false if no :passphrase is set or an error occurs.
      #
      def function
        begin;Fi[	@‘iI"÷      # and return the command line options to perform asymmetric encryption.
      # Log a warning and return nil if no valid recipients are found.
      #
      def function
        if user_recipients.empty? then;Fi[	@‘iI"‘      # a warning will be issued; as we will attempt to encrypt the backup
      # and proceed if at all possible.
      #
      def function
        @user_recipients ||= [recipients].flatten.compact.map do |var|;Fi[	@‘i-I"»      # (Hash keys) 'cleaned' for exact matching. If the cleaning process
      # creates duplicate keys, the user will be warned.
      #
      def function
        @user_keys ||= (var = (keys or {});Fi[	@‘iXI"“      # keyring (based on #gpg_homedir setting) and return an Array of all
      # identifiers which could be used to specify a valid key.
      #
      def function
        @system_identifiers ||= (var = false;Fi[	@1
i}I"˘      #   # allow_unconfirmed_access_for = 0.days
      #   confirmation_period_valid?   # will always return false
      #
      def function
        confirmation_sent_at and (confirmation_sent_at.utc >= self.class.allow_unconfirmed_access_for.ago);Fi[	@1
içI"È      #   # confirm_within = nil
      #   confirmation_period_expired?  # will always return false
      #
      def function
        self.class.confirm_within and (Time.now > (self.confirmation_sent_at + self.class.confirm_within));Fi[	@—
i3I"Ò      #   # reset_password_within = 0.days
      #   reset_password_period_valid?   # will always return false
      #
      def function
        reset_password_sent_at and (reset_password_sent_at.utc >= self.class.reset_password_within.ago);Fi[	@Îi?I"˚      #   * If any of the authorization headers were sent;
      #   * If all authentication keys are present;
      #
      def function
        http_authenticatable? and (request.authorization and with_authentication_hash("LITERAL", http_auth_hash));Fi[	@ÎiJI"      #   * If the params[scope] returns a hash with credentials;
      #   * If all authentication keys are present;
      #
      def function
        params_authenticatable? and (valid_params_request? and (valid_params? and with_authentication_hash("LITERAL", params_auth_hash)));Fi[	@~i9I"¨    #     context.init_haml_helpers
    #     context.haml_tag :p, "Stuff"
    #
    def function
      @haml_buffer = Haml::Buffer.new(haml_buffer, Options.new.for_buffer);Fi[	@ßixI"«      # namespaces, and as a result the order may be dependent on the
      # implementation of the underlying XML library.
      #
      def function
        xpath("STRING").inject({}) do |var, var|;Fi[	@ßiöI"Ø      #   irb> doc.slop!
      #   ... which does absolutely nothing.
      #
      def function
        unless decorators(XML::Node).include?(Nokogiri::Decorators::Slop) then;Fi[	@«iI"q    
    # Find Resque worker pids on Linux and OS X.
    #
    def function
      get_worker_pids("STRING");Fi[	@«iI"j    
    # Find Resque worker pids on Solaris.
    #
    def function
      get_worker_pids("STRING");Fi[	@õ@Ôi[[	I":projects//compass_proj/compass/installers/manifest.rb;TitI"[      end
      
      def function(var)
        begin
          (@manifest_file = var;Fi[	@{iI"y    end
    
    def function(var)
      begin
        (var = instance_variable_defined?("STRING") ? (@node) : (nil);Fi[	@~iI"∫    # @param i [Fixnum] The number of tabs to use
    # @yield A block in which the indentation will be `i` spaces
    def function(var)
      begin
        (var = haml_buffer.tabulation;Fi[	@~i5I"‘    # @param buffer [Haml::Buffer] The Haml buffer to use temporarily
    # @yield A block in which the given buffer should be used
    def function(var)
      begin
        (@haml_buffer, var = var, @haml_buffer;Fi[	@>iWI"J    end
    
    def function(var)
      begin
        (var = var.pos;Fi[	@ÇiqI"q  end
end
def function(var)
  begin
    (var = if var.linked_keg.directory? and var.linked_keg.symlink? then;Fi[	@áizI"@  end
  
  def function(var)
    begin
      (var = $stdout;Fi[	I"(projects//homebrew_proj/cmd/info.rb;TiBI"k  end
  
  def function(var)
    begin
      (exec("STRING", github_info(var)) if ARGV.flag?("STRING");Fi[	@©iSI"R  end
  
  def function(var)
    begin
      (var = FormulaInstaller.new(var);Fi[	I"+projects//homebrew_proj/cmd/upgrade.rb;Ti0I"V  end
  
  def function(var)
    begin
      (var = if var.opt_prefix.exist? then;Fi[	@√ijI"M  end
  
  def function(var)
    begin
      (var = Tab.for_formula(var);Fi[	@‡iI"P  end
  
  def function(var)
    begin
      var.verify_checksum(@checksum);Fi[	@icI"b  end
  
  def function(var)
    begin
      (raise("STRING") if linked_keg_record.directory?;Fi[	@iØI"_  protected
  
  def function(var)
    begin
      if var.symlink? and var.directory? then;Fi[	@ôi[I"z    #
    # Returns nothing.
    def function(var)
      begin
        (var, var, var, var, var = *var.match(MATCHER);Fi[	@hiùI"O    end
    
    def function(var)
      begin
        (var = (args or []);Fi[	I"/projects//rescue_proj/resque/json_coder.rb;Ti	I"emodule Resque
  class JsonCoder < Coder
    def function(var)
      begin
        JSON.dump(var);Fi[	@«iùI"Ä    
    # Processes a given job in the child.
    def function(var)
      begin
        (run_hook("LITERAL", var) if will_fork?;Fi[	@ i⁄I"\  #
  # Returns a Ruby object.
  def function(var)
    begin
      queue(var).pop(true);Fi[	@«iI"J    end
    
    def function(var)
      begin
        @app.call(var);Fi[	I"+projects//twitter_proj/twitter/base.rb;TiAI"É    #
    # @param method [String, Symbol] Message to send to the object
    def function(var)
      begin
        send(var.to_sym);Fi[	@—@Wi[[	@~iòI"s    # If you need to get the results as a string,
    # use \{#capture\_haml\}.
    #
    # For example,
    #;Fi[	@i/I"î    # the method defines additional arguments other than the object context,
    # then all arguments are required.
    # 
    # For example,
    # ;Fi[	@`i√I"s    
    # Generates a nicely formatted description of this event's contents.
    # 
    # For example,
    # ;Fi[	@5iiI"õ    #   the given event.  This may be generated by internationalization
    #   libraries if supported by the integration.
    # 
    # For example,
    # ;Fi[	@5iöI"ß    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # 
    # For example,
    # ;Fi[	@5i’I"ì    # behavior will *not* work if the class defines an +initialize+ method
    # without properly calling +super+.
    # 
    # For example,
    # ;Fi[	@5iÜI"Ä    # generated for the event) or indirectly (through attributes defined on
    # the machine).
    # 
    # For example,
    # ;Fi[	@5iºI"ò    # has multiple state machines and it would be difficult to differentiate
    # between the various states / events.
    # 
    # For example,
    # ;Fi[	@5iÒI"£    # be automatically created for assisting in finding objects that have the
    # attribute set to one of a given set of states.
    # 
    # For example,
    # ;Fi[	@oi*I"t    
    # Lists all of the from states that can be reached through this path.
    # 
    # For example,
    # ;Fi[	@oi9I"r    
    # Lists all of the to states that can be reached through this path.
    # 
    # For example,
    # ;Fi[	@oiBI"m    
    # Lists all of the events that can be fired through this path.
    # 
    # For example,
    # ;Fi[	@vi,I"à    # Lists all of the states that can be transitioned from through the paths in
    # this collection.
    # 
    # For example,
    # ;Fi[	@vi6I"Ü    # Lists all of the states that can be transitioned to through the paths in
    # this collection.
    # 
    # For example,
    # ;Fi[	@vi@I"|    # Lists all of the events that can be fired through the paths in this
    # collection.
    # 
    # For example,
    # ;Fi[	@}ioI"u    
    # Generates a human-readable description of this state's name / value:
    # 
    # For example,
    # ;Fi[	@}iÄI"}    # original block if it's a lambda block.  Otherwise, the static value is
    # returned.
    # 
    # For example,
    # ;Fi[	@}ióI"¢    # configured, then this will check whether the values are equivalent.
    # Otherwise, the matcher will determine the result.
    # 
    # For example,
    # ;Fi[	@}i€I"s    
    # Generates a nicely formatted description of this state's contents.
    # 
    # For example,
    # ;Fi[	@›i"I"y    
    # Generates a nicely formatted description of this transitions's contents.
    # 
    # For example,
    # ;Fi[	@Ô@W
i[[	@ÖiI"Ç          var = case var.value
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÖiI"}            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÖiI"}            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÖiI"}            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÖiEI"q        case var
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING";Fi[	@ÖiGI"s          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING";Fi[	@ÖiII"s          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING";Fi[	@ÖiKI"s          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING";Fi[	@{iˆI"ù              case @node.value["LITERAL"]
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING";Fi[	@{i¯I"å                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING";Fi[	@{i˙I"å                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING";Fi[	@{i¸I"å                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING";Fi[	@{iI"ì            case @node.value["LITERAL"]
            when "STRING" then
              "STRING"
            when "STRING" then
              "STRING";Fi[	@iI"d    "STRING".undent
  when "LITERAL" then
    "STRING"
  when "STRING" then
    "STRING".undent;Fi[	@ÜifI"Ç            "STRING"
          when "STRING", "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÜihI"}            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÜijI"}            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÜilI"}            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÜinI"}            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@ÜipI"}            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@õ@
i[[	@‘iJI"s      #
      # @api private
      def function(var)
        super
        instance_eval(&var) if block_given?;Fi[	I"7projects//backup_proj/backup/encryptor/open_ssl.rb;TiI"π      # Creates a new instance of Backup::Encryptor::OpenSSL and
      # sets the password attribute to what was provided
      def function(var)
        super
        @base64 ||= false;Fi[	@i(I"Ë        # Once pre-configured defaults and Cloud specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var)
          super
          instance_eval(&var) if block_given?;Fi[	@iI"Ë        # Once pre-configured defaults and Cloud specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var)
          super
          instance_eval(&var) if block_given?;Fi[	I"7projects//backup_proj/backup/syncer/rsync/local.rb;TiI"Ë        # Once pre-configured defaults and RSync specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var)
          super
          instance_eval(&var) if block_given?;Fi[	@i$I"ÿ        # Once pre-configured defaults and RSync specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var)
          super
          @port ||= "LITERAL";Fi[	I"Aprojects//compass_proj/compass/exec/global_options_parser.rb;TiI"hmodule Compass::Exec::GlobalOptionsParser
  def function(var)
    super
    set_global_options(var);Fi[	I"Bprojects//compass_proj/compass/exec/project_options_parser.rb;TiI"jmodule Compass::Exec::ProjectOptionsParser
  def function(var)
    super
    set_project_options(var);Fi[	I"<projects//devise_proj/devise/strategies/rememberable.rb;TiI"j      private
      
      def function(var)
        super
        if var.respond_to?("LITERAL") then;Fi[	@1iûI"û    class Haml < Generic
      # @param args [Array<String>] The command-line arguments
      def function(var)
        super
        @options["LITERAL"] = {};Fi[	@1i®I"s      #
      # @param opts [OptionParser]
      def function(var)
        super
        var.banner = "STRING";Fi[	@;iúI"ü    
    class ParseNode < Struct.new("LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL")
      def function(var)
        super
        self.children ||= [];Fi[	@RihI"E  end
  
  def function(var)
    super
    $n = ($n + "LITERAL");Fi[	I"6projects//nokogiri_proj/xsd/xmlparser/nokogiri.rb;TiI"o      ###
      # Handle the end_element event with +name+
      def function(var)
        super
      end;Fi[	I"5projects//rescue_proj/resque/failure/multiple.rb;TiI"~      end
      
      def function(var)
        super
        @backends = self.class.classes.map { |var| var.new(*var) };Fi[	@`iEI"÷    # Creates a copy of this event in addition to the list of associated
    # branches to prevent conflicts across events within a class hierarchy.
    def function(var)
      super
      @branches = @branches.dup;Fi[	I"Bprojects//state_machine_proj/state_machine/node_collection.rb;Ti(I"ü    # Creates a copy of this collection such that modifications don't affect
    # the original collection
    def function(var)
      super
      var = @nodes;Fi[	@oiI"L    end
    
    def function(var)
      super
      @transitions = nil;Fi[	@}iUI"√    # Creates a copy of this state in addition to the list of associated
    # methods to prevent conflicts across different states.
    def function(var)
      super
      @methods = methods.dup;Fi[	I"/projects//twitter_proj/twitter/identity.rb;TiI"     # @raise [ArgumentError] Error raised when supplied argument is missing an :id key.
    # @return [Twitter::Identity]
    def function(var)
      super
      raise(ArgumentError, "STRING") unless id;Fi[	@ÓI"	true;Fi[[	@#i(I"?      end
      
      def function
        true
      end;Fi[	@#içI"?      end
      
      def function
        true
      end;Fi[	I"Vprojects//compass_proj/compass/sass_extensions/functions/cross_browser_support.rb;TiI"5    end
    
    def function
      true
    end;Fi[	@ñiBI"5    end
    
    def function
      true
    end;Fi[	@Íi&I"?      end
      
      def function
        true
      end;Fi[	I"7projects//devise_proj/devise/models/validatable.rb;Ti)I"?      end
      
      def function
        true
      end;Fi[	@∞i$I"?      end
      
      def function
        true
      end;Fi[	I"Eprojects//devise_proj/devise/strategies/token_authenticatable.rb;TiI"è      
      # Token Authenticatable can be authenticated with params in any controller and any verb.
      def function
        true
      end;Fi[	I"7projects//diaspora_proj/models/account_deletion.rb;Ti-I"+  end
  
  def function
    true
  end;Fi[	I"=projects//diaspora_proj/models/activity_streams/photo.rb;TiI"^  # A better solution is needed.
  # @return [Boolean] true
  def function
    true
  end;Fi[	I",projects//diaspora_proj/models/photo.rb;TiÄI"+  end
  
  def function
    true
  end;Fi[	I";projects//diaspora_proj/models/relayable_retraction.rb;Ti!I"+  end
  
  def function
    true
  end;Fi[	I"0projects//haml_proj/haml/template/plugin.rb;TiI"~    # the ERB handler does.
    # In Rails 3.1+, we don't need to include Compilable.
    def function
      true
    end;Fi[	@ÅiI"]    undef "LITERAL" if defined? rails_xss_safe?
    
    def function
      true
    end;Fi[	I"1projects//homebrew_proj/test/test_bottles.rb;TiI"U  def function
    alias "LITERAL" "LITERAL"
    def function
      true
    end;Fi[	@F
i/I"ë          defined? I18n ? (super) : (var ? (var.to_s.humanize.downcase) : ("STRING"))
        end
        def function
          true
        end;Fi[	@F
i2I"O          true
        end
        def function
          true
        end;Fi[	@F
i5I"O          true
        end
        def function
          true
        end;Fi[	@¶
i7I"P          false
        end
        def function
          true
        end;Fi[	@õI"+var = Twitter::API::Arguments.new(var);Fi[[	@ibI"Î      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var.empty? then;Fi[	@)i&I"’      #   @example Return the 20 most recent favorite Tweets for @sferik
      #     Twitter.favorites('sferik')
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var = var.pop then;Fi[	@)iVI"˙      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@)ixI"˙      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@0iRI"H      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        merge_users!(var.options, var);Fi[	@0iÄI".      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean] :follow (false) Enable notifications for the target user.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.options["LITERAL"] = true if var.options.delete("LITERAL").!.!;Fi[	@0iôI".      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean] :follow (false) Enable notifications for the target user.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.options["LITERAL"] = true if var.options.delete("LITERAL").!.!;Fi[	@ViHI"›      #     Twitter.list_timeline(7505382, 'presidents')
      #     Twitter.list_timeline(7505382, 8863586)
      def function(var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop);Fi[	@i%I"Î      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var.empty? then;Fi[	@i[I"˙      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@éi!I"”      #   @example Return the users in the Art & Design category
      #     Twitter.suggestions("art-design")
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var = var.pop then;Fi[	@ßiéI"l      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@ßi©I"l      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@ßiˆI"Œ      #   @option options [String] :related A value for the TWT related parameter, as described in {https://dev.twitter.com/docs/intents Web Intents}. This value will be forwarded to all Web Intents calls.
      #   @option options [String] :lang Language code for the rendered embed. This will affect the text and localization of the rendered HTML.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@ºimI"      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map { |var| status_activity(var, var.options) };Fi[	@≈i¶I"”      # @overload block(options={})
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        merge_user!(var.options, var.pop);Fi[	@≈iI"7      #     Twitter.users('sferik', 'pengwynn', :method => :get) # Retrieve users with a GET request
      #     Twitter.users(7505382, 14100886, :method => :get)    # Same as above
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var = (var.options.delete("LITERAL") or "LITERAL");Fi[	@≈iI"∫      #     Twitter.user('sferik')
      #     Twitter.user(7505382)  # Same as above
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var = var.pop then;Fi[	@≈i∞I"˜      #     Twitter.profile_banner('sferik')
      #     Twitter.profile_banner(7505382)  # Same as above
      def function(var)
        var = Twitter::API::Arguments.new(var)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then;Fi[	I"module Twitter;FI"class Error;Fi[[	I">projects//twitter_proj/twitter/error/already_favorited.rb;TiI"prequire("twitter/error/forbidden")
module Twitter
  class Error
    class AlreadyFavorited < Twitter::Error;Fi[	I">projects//twitter_proj/twitter/error/already_retweeted.rb;TiI"prequire("twitter/error/forbidden")
module Twitter
  class Error
    class AlreadyRetweeted < Twitter::Error;Fi[	I"8projects//twitter_proj/twitter/error/bad_gateway.rb;TiI"zrequire("twitter/error/server_error")
module Twitter
  class Error
    class BadGateway < Twitter::Error::ServerError;Fi[	I"8projects//twitter_proj/twitter/error/bad_request.rb;TiI"zrequire("twitter/error/client_error")
module Twitter
  class Error
    class BadRequest < Twitter::Error::ClientError;Fi[	@œiI"arequire("twitter/error")
module Twitter
  class Error
    class ClientError < Twitter::Error;Fi[	I"@projects//twitter_proj/twitter/error/configuration_error.rb;TiI"irequire("twitter/error")
module Twitter
  class Error
    class ConfigurationError < ::ArgumentError;Fi[	I"9projects//twitter_proj/twitter/error/decode_error.rb;TiI"arequire("twitter/error")
module Twitter
  class Error
    class DecodeError < Twitter::Error;Fi[	I"6projects//twitter_proj/twitter/error/forbidden.rb;TiI"yrequire("twitter/error/client_error")
module Twitter
  class Error
    class Forbidden < Twitter::Error::ClientError;Fi[	I"<projects//twitter_proj/twitter/error/gateway_timeout.rb;TiI"~require("twitter/error/server_error")
module Twitter
  class Error
    class GatewayTimeout < Twitter::Error::ServerError;Fi[	I"Cprojects//twitter_proj/twitter/error/identity_map_key_error.rb;TiI"grequire("twitter/error")
module Twitter
  class Error
    class IdentityMapKeyError < ::IndexError;Fi[	I"Bprojects//twitter_proj/twitter/error/internal_server_error.rb;TiI"~require("twitter/error/server_error")
module Twitter
  class Error
    class InternalServerError < Twitter::Error::ServerError;Fi[	I";projects//twitter_proj/twitter/error/not_acceptable.rb;TiI"}require("twitter/error/client_error")
module Twitter
  class Error
    class NotAcceptable < Twitter::Error::ClientError;Fi[	I"6projects//twitter_proj/twitter/error/not_found.rb;TiI"xrequire("twitter/error/client_error")
module Twitter
  class Error
    class NotFound < Twitter::Error::ClientError;Fi[	I"9projects//twitter_proj/twitter/error/server_error.rb;TiI"arequire("twitter/error")
module Twitter
  class Error
    class ServerError < Twitter::Error;Fi[	I"@projects//twitter_proj/twitter/error/service_unavailable.rb;TiI"}require("twitter/error/server_error")
module Twitter
  class Error
    class ServiceUnavailable < Twitter::Error::ServerError;Fi[	I">projects//twitter_proj/twitter/error/too_many_requests.rb;TiI"require("twitter/error/client_error")
module Twitter
  class Error
    class TooManyRequests < Twitter::Error::ClientError;Fi[	I"9projects//twitter_proj/twitter/error/unauthorized.rb;TiI"|require("twitter/error/client_error")
module Twitter
  class Error
    class Unauthorized < Twitter::Error::ClientError;Fi[	I"Aprojects//twitter_proj/twitter/error/unprocessable_entity.rb;TiI"~require("twitter/error/client_error")
module Twitter
  class Error
    class UnprocessableEntity < Twitter::Error::ClientError;Fi[	@¢@—i[[	@‘i:I"è      #     archive :my_archive do |archive|
      #       archive.add '/some/directory'
      #     end
      #
      #     compress_with Gzip;Fi[	@‘iBI"∞      #       encryptor.passphrase = 'a secret'
      #       encryptor.recipients = ['joe@example.com', 'mary@example.com']
      #     end
      #
      #     store_with SFTP;Fi[	@Íi^I"y      #         @pending_notifications ||= []
      #       end
      #     end
      #
      def function(var, var);Fi[	@i≤I"≠    #     devise_scope :owner do
    #       post "deactivate", :to => "registrations#deactivate", :as => "deactivate_registration"
    #     end
    #
    def function(var);Fi[	@~i•I"H    #         end
    #       end
    #     end
    #
    # outputs;Fi[	@iI"h    #     def initialize(name)
    #       @name = name
    #     end
    #     
    #     def name;Fi[	@iII"§    #         transition :idling => :parked
    #       end
    #     end
    #     
    #     state_machine :alarm_state, :namespace => 'alarm', :initial => :on do;Fi[	@i{I"®    #         transition :idling => :parked
    #       end
    #     end
    #     
    #     state_machine :alarm_state, :namespace => 'alarm', :initial => :active do;Fi[	@%i)I"Ü        #     before :save do
        #       # log message
        #     end
        #     
        #     # Target all state machines;Fi[	@%i.I"∏        #     before_transition :parked => :idling, :on => :ignite do
        #       # put on seatbelt
        #     end
        #     
        #     # Target a specific state machine;Fi[	@%i3I"ª        #     before_transition :state, any => :idling do
        #       # put on seatbelt
        #     end
        #     
        #     # Target all state machines without requirements;Fi[	@%iÅI"Æ        #     after_transition_failure do |transition|
        #       # log failure
        #     end
        #     
        #     after_transition_failure :on => :ignite do;Fi[	@5iÈI"}    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     
    #     def initialize;Fi[	@5i˙I"â    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     
    #     def initialize(attributes = {});Fi[	@5iI"â    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     
    #     def initialize(attributes = {});Fi[	@5i$I"v    #       state nil, :idling
    #       ...
    #     end
    #     
    #     def initialize(attributes = {});Fi[	@5iëI"ë    #         transition :parked => :idling
    #       end
    #     end
    #     
    #     state_machine :alarm_state, :initial => :active do;Fi[	@5i»I"°    #         transition all => :off
    #       end
    #     end
    #     
    #     state_machine :alarm_state, :initial => :active, :namespace => 'alarm' do;Fi[	@ÓI"var = "STRING";Fi[[	@di)I"~      end
      
      def function
        var = "STRING"
        (required_libraries or []).each { |var| (var << "STRING") };Fi[	I"Dprojects//compass_proj/compass/installers/manifest_installer.rb;Ti'I"i      end
      
      def function
        var = "STRING"
        manifest.each_stylesheet do |var|;Fi[	I"@projects//devise_proj/generators/devise/devise_generator.rb;TiI"Õ      class_option("LITERAL", "LITERAL" => "STRING", "LITERAL" => "LITERAL", "LITERAL" => (true))
      
      def function
        var = "STRING"
        (var << "STRING") if class_name.include?("STRING");Fi[	@.i®I"y  end
  
  def function
    var = "STRING"
    if self.username_changed? and Person.exists?("LITERAL" => (var)) then;Fi[	@{i∆I"V    end
    
    def function
      var = "STRING"
      unless block_given? then;Fi[	@;i°I"^      end
      
      def function
        var = "STRING"
        children.each do |var|;Fi[	@RiëI"S  end
  
  def function
    var = "STRING"
    var = `find #{to_s}STRING`.to_i;Fi[	@√ifI"Nend
class Formula
  def function
    var = "STRING"
    (var << "STRING");Fi[	@[iûI"Y  end
  
  def function
    var = "STRING"
    (var << "STRING") if @opts["LITERAL"];Fi[	I"3projects//homebrew_proj/test/test_inreplace.rb;TiI"érequire("extend/string")
class InreplaceTest < Test::Unit::TestCase
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@ûiI"X  end
  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@ûiI"X  end
  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@ûiI"X  end
  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@ûi$I"X  end
  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@ûi+I"X  end
  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@ûi2I"X  end
  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension);Fi[	@"iPI"π        # integration.  This file will only exist for integrations that actually
        # support i18n.
        def function
          var = "STRING"
          var if File.exists?(var);Fi[	@(I"module API;Fi[[	I"4projects//twitter_proj/twitter/api/arguments.rb;Ti I"<module Twitter
  module API
    class Arguments < Array;Fi [	@i	I"trequire("twitter/direct_message")
require("twitter/user")
module Twitter
  module API
    module DirectMessages;Fi[	@)iI"frequire("twitter/tweet")
require("twitter/user")
module Twitter
  module API
    module Favorites;Fi[	@0iI"wrequire("twitter/relationship")
require("twitter/user")
module Twitter
  module API
    module FriendsAndFollowers;Fi[	@MiI"mrequire("twitter/configuration")
require("twitter/language")
module Twitter
  module API
    module Help;Fi[	@ViI"brequire("twitter/tweet")
require("twitter/user")
module Twitter
  module API
    module Lists;Fi[	@yiI"nrequire("twitter/api/utils")
require("twitter/place")
module Twitter
  module API
    module PlacesAndGeo;Fi[	@iI"vrequire("twitter/api/utils")
require("twitter/saved_search")
module Twitter
  module API
    module SavedSearches;Fi[	@àiI"qrequire("twitter/api/utils")
require("twitter/search_results")
module Twitter
  module API
    module Search;Fi[	@ãiI"nrequire("twitter/api/utils")
require("twitter/user")
module Twitter
  module API
    module SpamReporting;Fi[	@éi	I"prequire("twitter/suggestion")
require("twitter/user")
module Twitter
  module API
    module SuggestedUsers;Fi[	@ëiI"frequire("twitter/tweet")
require("twitter/user")
module Twitter
  module API
    module Timelines;Fi[	@†iI"drequire("twitter/place")
require("twitter/trend")
module Twitter
  module API
    module Trends;Fi[	@ßiI"erequire("twitter/oembed")
require("twitter/tweet")
module Twitter
  module API
    module Tweets;Fi[	@ºiI"irequire("twitter/tweet")
require("twitter/user")
module Twitter
  module API
    module Undocumented;Fi[	@≈iI"erequire("twitter/settings")
require("twitter/user")
module Twitter
  module API
    module Users;Fi[	@?iI"crequire("twitter/cursor")
require("twitter/user")
module Twitter
  module API
    module Utils;Fi[	I"module StateMachine;FI"module Integrations;Fi[[	I"Uprojects//state_machine_proj/state_machine/integrations/active_model/observer.rb;Ti I"Emodule StateMachine
  module Integrations
    module ActiveModel;Fi [	I"\projects//state_machine_proj/state_machine/integrations/active_model/observer_update.rb;Ti I"Emodule StateMachine
  module Integrations
    module ActiveModel;Fi [	@å
i I"Emodule StateMachine
  module Integrations
    module ActiveModel;Fi [	@A
i I"Emodule StateMachine
  module Integrations
    module ActiveModel;Fi [	@F
i I"Fmodule StateMachine
  module Integrations
    module ActiveRecord;Fi [	I"Mprojects//state_machine_proj/state_machine/integrations/active_record.rb;TiI"yrequire("state_machine/integrations/active_model")
module StateMachine
  module Integrations
    module ActiveRecord;Fi[	@"i I">module StateMachine
  module Integrations
    module Base;Fi [	@%i I"Dmodule StateMachine
  module Integrations
    module DataMapper;Fi [	@ù
i I"Dmodule StateMachine
  module Integrations
    module DataMapper;Fi [	@Ãi I"Dmodule StateMachine
  module Integrations
    module DataMapper;Fi [	@¶
i I"Emodule StateMachine
  module Integrations
    module MongoMapper;Fi [	I"Lprojects//state_machine_proj/state_machine/integrations/mongo_mapper.rb;TiI"xrequire("state_machine/integrations/active_model")
module StateMachine
  module Integrations
    module MongoMapper;Fi[	@±
i I"Amodule StateMachine
  module Integrations
    module Mongoid;Fi [	I"Gprojects//state_machine_proj/state_machine/integrations/mongoid.rb;Ti I"Amodule StateMachine
  module Integrations
    module Mongoid;Fi [	@Õi I"@module StateMachine
  module Integrations
    module Sequel;Fi [	@M
i I"@module StateMachine
  module Integrations
    module Sequel;Fi [	I"Y#   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.;FI"=#   @param options [Hash] A customizable set of options.;Fi[[	@Vi4I"w      # @return [Array<Twitter::Tweet>]
      # @overload list_timeline(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@Vi>I"∑      # @overload list_timeline(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@ViäI"Î      # @return [Twitter::Cursor] The subscribers of the specified list.
      # @overload list_subscribers(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@ViíI"
      # @overload list_subscribers(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@Vi•I"K      # @return [Twitter::List] The specified list.
      # @overload list_subscribe(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Subscribe to the authenticated user's "presidents" list;Fi[	@Vi¨I"p      # @overload list_subscribe(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Subscribe to @sferik's "presidents" list;Fi[	@Vi‹I"Q      # @return [Twitter::List] The specified list.
      # @overload list_unsubscribe(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Unsubscribe from the authenticated user's "presidents" list;Fi[	@Vi„I"v      # @overload list_unsubscribe(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Unsubscribe from @sferik's "presidents" list;Fi[	@Vi1I"¿      # @return [Twitter::Cursor]
      # @overload list_members(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@Vi9I"      # @overload list_members(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@VikI"A      # @return [Twitter::List] The deleted list.
      # @overload list_destroy(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Delete the authenticated user's "presidents" list;Fi[	@VirI"h      # @overload list_destroy(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Delete @sferik's "presidents" list;Fi[	@ViÖI"|      # @return [Twitter::List] The created list.
      # @overload list_update(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [String] :mode ('public') Whether your list is public or private. Values can be 'public' or 'private'.;Fi[	@ViéI"≤      # @overload list_update(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [String] :mode ('public') Whether your list is public or private. Values can be 'public' or 'private'.;Fi[	@Vi∂I"9      # @return [Twitter::List] The specified list.
      # @overload list(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Show the authenticated user's "presidents" list;Fi[	@ViΩI"^      # @overload list(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Show @sferik's "presidents" list;Fi[	@õI"super("STRING");Fi[[	I"-projects//homebrew_proj/test/testball.rb;TiI"yend
class TestBallWithRealPath < TestBall
  def function(var)
    super("STRING")
    @path = Pathname.new("STRING");Fi[	@%iI"H  mirror("STRING")
  
  def function(var)
    super("STRING")
  end;Fi[	@%i+I"k  
  # name parameter required for some Formula::factory
  def function(var)
    super("STRING")
  end;Fi[	@%iyI";  end
  
  def function(var)
    super("STRING")
  end;Fi[	@%içI";  end
  
  def function(var)
    super("STRING")
  end;Fi[	@%iùI";  end
  
  def function(var)
    super("STRING")
  end;Fi[	@%i¨I"M  bottle_sha1("STRING")
  
  def function(var)
    super("STRING")
  end;Fi[	@%iµI"F  head("STRING")
  
  def function(var)
    super("STRING")
  end;Fi[	@%i¿I"F  sha1("STRING")
  
  def function(var)
    super("STRING")
  end;Fi[	@%iÀI"I  version("STRING")
  
  def function(var)
    super("STRING")
  end;Fi[	@%iÿI"~  devel { url("STRING", "LITERAL" => "LITERAL", "LITERAL" => "STRING") }
  
  def function(var)
    super("STRING")
  end;Fi[	@%iÂI"^  bottle { sha1("STRING" => "LITERAL") }
  
  def function(var)
    super("STRING")
  end;Fi[	@%iÚI"^  bottle { sha1("STRING" => "LITERAL") }
  
  def function(var)
    super("STRING")
  end;Fi[	@%iI";  end
  
  def function(var)
    super("STRING")
  end;Fi[	@%iI";  end
  
  def function(var)
    super("STRING")
  end;Fi[	@%i$I"b  version("STRING" => (CustomVersionScheme))
  
  def function(var)
    super("STRING")
  end;Fi[	I"# For example:;F@—i[[	@i°I"ˆ    # list of known actions.  This is important if you add a custom action to a controller that
    # overrides an out of the box Devise controller.
    # For example:
    #
    #    class RegistrationsController < Devise::RegistrationsController;Fi[	I"'projects//haml_proj/haml/engine.rb;TiEI"õ    # It extends {Haml::Helpers}, and various instance variables are set
    # (all prefixed with `haml_`).
    # For example:
    #
    #     s = "foobar";Fi[	@KiNI"⁄    #
    # `locals` is a hash of local variables to make available to the template.
    # For example:
    #
    #     Haml::Engine.new("%p= foo").render(Object.new, :foo => "Hello, world!") #=> "<p>Hello, world!</p>";Fi[	@KiáI"©    # the local variables which can be assigned must be pre-declared.
    # This is done with the `local_names` argument.
    # For example:
    #
    #     # This works;Fi[	@Ki©I"ó    # If `object` is a class or module,
    # the method will instead by defined as an instance method.
    # For example:
    #
    #     t = Time.now;Fi[	@Ki∂I"©    # the local variables which can be assigned must be pre-declared.
    # This is done with the `local_names` argument.
    # For example:
    #
    #     # This works;Fi[	@~i1I"´    # This is useful if you want to use the helpers in a context
    # other than the normal setup with ActionView.
    # For example:
    #
    #     context = Object.new;Fi[	@~i{I"¥    # and putting the result into `<li>` elements.
    # This creates a list of the results of the block.
    # For example:
    #
    #     = list_of([['hello'], ['yall']]) do |i|;Fi[	@~i’I"ê    # Increments the number of tabs the buffer automatically adds
    # to the lines of the template.
    # For example:
    #
    #     %h1 foo;Fi[	@~iıI"|    # to the lines of the template,
    # but only for the duration of the block.
    # For example:
    #
    #     %h1 foo;Fi[	@~iI"ë    # Surrounds a block of Haml code with strings,
    # with no whitespace in between.
    # For example:
    #
    #     = surround '(', ')' do;Fi[	@~i-I"è    # Prepends a string to the beginning of a Haml block,
    # with no whitespace between.
    # For example:
    #
    #     = precede '*' do;Fi[	@~i>I"}    # Appends a string to the end of a Haml block,
    # with no whitespace between.
    # For example:
    #
    #     click;Fi[	@>i»I"€    # All conditionals based on the static parameters
    # are done as embedded Ruby within this template.
    # For example:
    #
    #     def_static_method(Foo, :my_static_method, [:foo, :bar], :baz, :bang, <<RUBY);Fi[	@>iI"é    
    # Moves a scanner through a balanced pair of characters.
    # For example:
    #
    #     Foo (Bar (Baz bang) bop) (Bang (bop bip));Fi[	@r@˚i[[	I"Kprojects//compass_proj/compass/sass_extensions/functions/font_files.rb;TiI"Ä      var = if (var > (var + "LITERAL")) then
        var[(var + "LITERAL")].value.to_sym
      else
        "LITERAL"
      end;Fi[	I"Dprojects//compass_proj/compass/sass_extensions/sprites/image.rb;Ti[I"£          @offset ||= if (position.unitless? or (position.unit_str == "STRING")) then
            position.value
          else
            "LITERAL"
          end;Fi[	@#iI"a        "LITERAL" if (var["LITERAL"] == "STRING")
      end
    else
      "LITERAL"
    end;Fi[	@&i'I"x    var = if (var.mach_o_executable? or var.text_executable?) then
      "LITERAL"
    else
      "LITERAL"
    end;Fi[	@)iI"M    when "LITERAL" then
      "LITERAL"
    else
      "LITERAL"
    end;Fi[	@)i#I"M    when "LITERAL" then
      "LITERAL"
    else
      "LITERAL"
    end;Fi[	@i|I"a        when "LITERAL" then
          "LITERAL"
        else
          "LITERAL"
        end;Fi[	@iìI"`        when "STRING" then
          "LITERAL"
        else
          "LITERAL"
        end;Fi[	@.i/I"d      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      else
        "LITERAL"
      end;Fi[	@.i9I"b      when "LITERAL", "LITERAL" then
        "LITERAL"
      else
        "LITERAL"
      end;Fi[	@.iKI"O    when "LITERAL" then
      archs.first
    else
      "LITERAL"
    end;Fi[	@êiI"`        when "LITERAL" then
          "STRING"
        else
          "LITERAL"
        end;Fi[	@6iI"\    when "LITERAL" then
      "LITERAL"
    else
      "LITERAL"
    end, ENV.compiler);Fi[	@6i[I"\    when "LITERAL" then
      "LITERAL"
    else
      "LITERAL"
    end, ENV.compiler);Fi[	@èi1I"ö          if var and ["LITERAL", "LITERAL"].include?(var.type) then
            extract_node_name(var)
          else
            "LITERAL"
          end);Fi[	I"def function(var, var);FI"super(var);Fi[[	@Úi0I"û      ##
      # Creates a new instance of the MongoDB database object
      def function(var, var)
        super(var)
        @only_collections ||= Array.new;Fi[	@
i)I"ñ      ##
      # Creates a new instance of the MySQL adapter object
      def function(var, var)
        super(var)
        @skip_tables ||= Array.new;Fi[	@
i*I"»      # Sets the PGPASSWORD environment variable to the password
      # so it doesn't prompt and hang in the process
      def function(var, var)
        super(var)
        @skip_tables ||= Array.new;Fi[	@ıi&I"û      ##
      # Creates a new instance of the Redis database object
      def function(var, var)
        super(var)
        @additional_options ||= Array.new;Fi[	@¯iI"û      ##
      # Creates a new instance of the Riak adapter object
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?;Fi[	I"+projects//backup_proj/backup/errors.rb;TiI"}      end
      
      def function(var, var)
        super(var)
        set_backtrace(var.backtrace) if @orig_err = var;Fi[	@ÌiI"á      ##
      # Campfire account's room id
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?;Fi[	@›i(I"Œ      # The background color of an error message.
      # One of :yellow, :red, :green, :purple, or :random. (default: yellow)
      def function(var, var)
        super(var)
        @notify_users ||= false;Fi[	@‡ilI"∞      # Default location is '$HOME/Backup/emails'
      # Example: '/tmp/test-mails'
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?;Fi[	@ÙiI"≤      # API-Key
      # Create a Prowl account and request an API key on prowlapp.com.
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?;Fi[	@˚iI"ç      ##
      # The priority of the notification
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?;Fi[	@iI"~      ##
      # OAuth credentials
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?;Fi[	I"&projects//haml_proj/haml/error.rb;TiI"ï    # @param message [String] The error message
    # @param line [Fixnum] See \{#line}
    def function(var, var)
      super(var)
      @line = var;Fi[	@çi2I"^    attr("LITERAL")
    
    def function(var, var)
      super(var)
      @formula = var;Fi[	@oiI"…    # * <tt>:after</tt> - Whether to run after callbacks
    # * <tt>:transaction</tt> - Whether to wrap transitions within a transaction
    def function(var, var)
      super(var)
      @valid = all?;Fi[	I"Gassert_equal("LITERAL", (version("STRING") <=> version("STRING")));F@Æi[[	@IiI"Ê  
  def function
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"*  def function
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@IiI"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@Ii I"b    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")));Fi[	@Ii!I"L    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_equal("LITERAL", (version("STRING") <=> version("STRING")))
    assert_nil((version("STRING") <=> "STRING"));Fi[	I"`#   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;F@˛i[[	@Vi=I"^      #     Twitter.list_timeline(8863586)
      # @overload list_timeline(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViaI"≥      #     Twitter.list_remove_member(8863586, 'BarackObama')
      # @overload list_remove_member(user, list, user_to_remove, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_remove [Integer, String] The user id or screen name of the list member to remove.;Fi[	@ViëI"d      #     Twitter.list_subscribers(8863586)
      # @overload list_subscribers(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@Vi´I"`      #     Twitter.list_subscribe(8863586)
      # @overload list_subscribe(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@Vi∆I"≥      #     Twitter.list_subscriber?('presidents', 'BarackObama')
      # @overload list_subscriber?(user, list, user_to_check, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[	@Vi‚I"d      #     Twitter.list_unsubscribe(8863586)
      # @overload list_unsubscribe(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViˇI"‹      #     Twitter.list_add_members(8863586, [813286, 18755393])
      # @overload list_add_members(user, list, users, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.;Fi[	@ViI"õ      #     Twitter.list_member?(8863586, 'BarackObama')
      # @overload list_member?(user, list, user_to_check, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String] The user ID or screen name of the list member.;Fi[	@Vi8I"\      #     Twitter.list_members(8863586)
      # @overload list_members(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViUI"ñ      #     Twitter.list_add_member(8863586, 813286)
      # @overload list_add_member(user, list, user_to_add, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_add [Integer, String] The user id or screen name to add to the list.;Fi[	@ViqI"\      #     Twitter.list_destroy(8863586)
      # @overload list_destroy(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViçI"ò      #     Twitter.list_update(8863586, :description => "Presidents of the United States of America")
      # @overload list_update(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViºI"L      #     Twitter.list(8863586)
      # @overload list(user, list, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViI"‚      #     Twitter.list_remove_members(8863586, [813286, 18755393])
      # @overload list_remove_members(user, list, users, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.;Fi[	@ÓI"var = [];Fi[[	@˛i\I"˝    # may be interleaved. Interleaving of the "index|exit status" outputs
    # should not be an issue, given the small byte size of the data being written.
    def function
      var = []
      @commands.each_with_index { |var, var| (var << "STRING") };Fi[	I"=projects//compass_proj/compass/configuration/adapters.rb;TiI"z      end
      
      def function
        var = []
        (var << [sass_path, css_path]) if sass_path and css_path;Fi[	@Òi>I"a      end
      
      def function
        var = []
        (var << sass_path) if sass_path;Fi[	@li;I"±  # Require jQuery from CDN if possible, falling back to vendored copy, and require
  # vendored jquery_ujs
  def function
    var = []
    if AppConfig.privacy.jquery_cdn? then;Fi[	I"7projects//diaspora_proj/helpers/language_helper.rb;TiI"vmodule LanguageHelper
  def function
    var = []
    AVAILABLE_LANGUAGES.each { |var, var| (var << [var, var]) };Fi[	I"'projects//homebrew_proj/caveats.rb;TiI"@  end
  
  def function
    var = []
    (var << f.caveats);Fi[	@˚i*I"a  end
  
  def function
    var = []
    if (f.plist or (keg and keg.plist_installed?)) then;Fi[	@˝iXI"C  end
  
  def function
    var = []
    var = Formula.aliases;Fi[	I"(projects//homebrew_proj/cmd/deps.rb;Ti$I"Tend
class Array
  def function
    var = []
    each { |var| var = var.|(var) };Fi[	I"+projects//homebrew_proj/cmd/missing.rb;Ti	I"\  extend(self)
  
  def function
    var = []
    HOMEBREW_CELLAR.subdirs.each do |var|;Fi[	I",projects//homebrew_proj/cmd/versions.rb;TiI"Mend
class Formula
  def function
    var = []
    rev_list.each do |var|;Fi[	I"5projects//homebrew_proj/keg_fix_install_names.rb;Ti@I"R  end
  
  def function
    var = []
    var = ["STRING", "STRING", "STRING"];Fi[	@1iÇI"¨  # http://github.com/mxcl/homebrew/issues/#issue/41
  # http://github.com/mxcl/homebrew/issues/#issue/48
  def function
    var = []
    ["STRING", "STRING"].each do |var|;Fi[	@oicI"z      alias "LITERAL" "LITERAL"
      
      def function
        var = []
        self.class.constants.each do |var|;Fi[	@õ@ÿi[[	I"Aprojects//compass_proj/compass/exec/command_option_parser.rb;TiI"B    end
    
    def function(var)
      # do nothing
    end;Fi[	@*i1I"°      # The default finalize method -- it is a no-op.
      # This could print out a message or something.
      def function(var)
        # do nothing
      end;Fi[	@iRI"K  
  class NullLogger
    def function(var)
      # do nothing
    end;Fi[	@iVI"B    end
    
    def function(var)
      # do nothing
    end;Fi[	I",projects//compass_proj/compass/stats.rb;TiI"L      end
      
      def function(var)
        # do nothing
      end;Fi[	@i"I"L      end
      
      def function(var)
        # do nothing
      end;Fi[	@Ki“I"f    
    if (RUBY_VERSION < "STRING") then
      def function(var)
        # do nothing
      end;Fi[	I"+projects//jekyll_proj/jekyll/plugin.rb;Ti&I"Y    #
    # Returns a new instance.
    def function(var)
      # do nothing
    end;Fi[	@Äi'I"á        # Called at the end of an element
        # +name+ is the tag name
        def function(var)
          # do nothing
        end;Fi[	@ÄiHI"z        #
        # +string+ contains the character data
        def function(var)
          # do nothing
        end;Fi[	@ÄiOI"ò        # Called when comments are encountered
        # +string+ contains the comment data
        def function(var)
          # do nothing
        end;Fi[	@ÄiVI"ä        # Called on document warnings
        # +string+ contains the warning
        def function(var)
          # do nothing
        end;Fi[	@Äi]I"Ü        # Called on document errors
        # +string+ contains the error
        def function(var)
          # do nothing
        end;Fi[	@ÄidI"ó        # Called when cdata blocks are found
        # +string+ contains the cdata content
        def function(var)
          # do nothing
        end;Fi[	I"opoo("STRING");F@ii[[	@+iWI"b    require("erb")
    if version.nil? then
      opoo("STRING")
      puts("STRING")
    end;Fi[	@©i9I"{  def function
    unless MacOS.macports_or_fink.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING");Fi[	I"(projects//homebrew_proj/cmd/link.rb;TiI"u    ARGV.kegs.each do |var|
      if var.linked? then
        opoo("STRING")
        puts("STRING")
        next;Fi[	@äi=I"à  # up in the DSL section.
  def function(var, var)
    opoo("STRING")
    puts("STRING")
    FailsWithLLVM.new(var, var).handle_failure;Ti[	@√iÚI"ë      (ohai("STRING") if ARGV.verbose?
      if f.class.skip_clean_all? then
        opoo("STRING")
        puts("STRING")
        puts("STRING");Fi[	@√i¸I"â      Cleaner.new(f))
    rescue Exception => var
      opoo("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?;Fi[	@√iI"¡        var = (HOMEBREW_PREFIX / var.basename).realpath
        unless ORIGINAL_PATHS.include?(var) then
          opoo("STRING")
          puts("STRING")
          @show_summary_heading = true;Fi[	@√iI"  def function
    if (f.prefix + "STRING").directory? then
      opoo("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√i"I"  def function
    if (f.prefix + "STRING").directory? then
      opoo("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√i-I"ô    var = f.lib.children.select { |var| var.to_s =~ "LITERAL" }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√i@I"f    end
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√iLI"©    var = f.bin.children.select { |var| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√iXI"™    var = f.sbin.children.select { |var| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING");Fi[	@‡iI"É      var.verify_checksum(@checksum)
    rescue ChecksumMissingError
      opoo("STRING")
      puts("STRING")
      puts("STRING");Fi[	@ˇ@õi[[	@iaI"      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@iyI"1      # @overload direct_message_destroy(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        threaded_object_from_response(Twitter::DirectMessage, "LITERAL", "STRING", var);Fi[	@)i=I"      # @overload unfavorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        threaded_object_from_response(Twitter::Tweet, "LITERAL", "STRING", var);Fi[	@)iUI"˙      # @overload favorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@)iwI"˙      # @overload favorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@0iQI"P      # @overload friendships(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@0i¥I"d      # @overload unfollow(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        threaded_user_objects_from_response("LITERAL", "STRING", var);Fi[	@i$I"       #   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@iZI"      # @overload saved_search_destroy(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@ãiI"g      # @overload report_spam(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        threaded_user_objects_from_response("LITERAL", "STRING", var);Fi[	@ºilI"      # @overload statuses_activity(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@≈i•I"»      #   Twitter.blocking_ids
      # @overload block(options={})
      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@≈i÷I"a      # @overload block(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        threaded_user_objects_from_response("LITERAL", "STRING", var);Fi[	@≈iÈI"c      # @overload unblock(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var)
        threaded_user_objects_from_response("LITERAL", "STRING", var);Fi[	@ÓI"Jdefine_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"));Fi[[	@å
iI"⁄          (defined? ::ActiveModel::VERSION.! or (::ActiveModel::VERSION::MAJOR == "LITERAL"))
        end
        def function
          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end;Fi[	@F
iI"          ((::ActiveRecord::VERSION::MAJOR == "LITERAL") or ((::ActiveRecord::VERSION::MAJOR == "LITERAL") and (::ActiveRecord::VERSION::MINOR == "LITERAL")))
        end
        def function
          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end;Fi[	@Âi1I"ì      
      # Initializes static states
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
      end;Fi[	@Âi6I"î      
      # Initializes dynamic states
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
      end;Fi[	@ÃiUI"≈      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
      end;Fi[	@¶
iI"ä          var
        end
        def function
          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end;Fi[	@¶
iFI"ı          defined? ::MongoMapper::Plugins and (defined? ::MongoMapper::Version.! or ::MongoMapper::Version =~ "LITERAL")
        end
        def function
          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end;Fi[	@Úi)I"≈      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
      end;Fi[	@±
iI"¶          ::Mongoid::VERSION =~ "LITERAL"
        end
        def function
          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end;Fi[	@˜i-I"≈      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
      end;Fi[	@ÕiI"          (defined? ::Sequel::MAJOR.! or ((::Sequel::MAJOR == "LITERAL") or ((::Sequel::MAJOR == "LITERAL") and (::Sequel::MINOR <= "LITERAL"))))
        end
        def function
          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end;Fi[	@ÕiI"Ã          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end
        def function
          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end;Fi[	@M
iII"≈      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
      end;Fi[	@M
i]I"¥      
      # Adds hooks into validation for automatically firing events
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
      end;Fi[	I" def function(var, var, var);FI"var = "LITERAL";Fi[[	I"3projects//nokogiri_proj/nokogiri/css/parser.rb;Ti>I"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiCI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiHI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiMI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiRI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiWI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiPI"k      
      # reduce 62 omitted
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiUI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiZI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îi_I"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îidI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îiiI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	@îinI"[      end
      
      def function(var, var, var)
        var = "LITERAL"
        var;Fi[	I"# @return [Integer];F@Ói[[	I"0projects//twitter_proj/twitter/geo/point.rb;Ti	I"{  class Geo
    class Point < Twitter::Geo
      # @return [Integer]
      def function
        coordinates["LITERAL"];Fi[	@≤iI"w      alias "LITERAL" "LITERAL"
      
      # @return [Integer]
      def function
        coordinates["LITERAL"];Fi[	@ i&I"R    end
    
    # @return [Integer]
    def function
      @attrs["LITERAL"];Fi[	I",projects//twitter_proj/twitter/place.rb;TiI"a    end
    
    # @return [Integer]
    def function
      @parent_id ||= @attrs["LITERAL"];Fi[	I"1projects//twitter_proj/twitter/rate_limit.rb;TiI"W    end
    
    # @return [Integer]
    def function
      var = @attrs["STRING"];Fi[	@ºiI"W    end
    
    # @return [Integer]
    def function
      var = @attrs["STRING"];Fi[	@ºi"I"x    end
    
    # @return [Integer]
    def function
      [(reset_at - Time.now).ceil, "LITERAL"].max if reset_at;Fi[	I"5projects//twitter_proj/twitter/search_results.rb;TiI"q    end
    
    # @return [Integer]
    def function
      @attrs["LITERAL"]["LITERAL"] if search_metadata?;Fi[	@√iI"q    end
    
    # @return [Integer]
    def function
      @attrs["LITERAL"]["LITERAL"] if search_metadata?;Fi[	@√i%I"q    end
    
    # @return [Integer]
    def function
      @attrs["LITERAL"]["LITERAL"] if search_metadata?;Fi[	@√i0I"q    end
    
    # @return [Integer]
    def function
      @attrs["LITERAL"]["LITERAL"] if search_metadata?;Fi[	I",projects//twitter_proj/twitter/tweet.rb;Ti"I"X    end
    
    # @return [Integer]
    def function
      var = @attrs["LITERAL"];Fi[	@Ãi]I"X    end
    
    # @return [Integer]
    def function
      var = @attrs["LITERAL"];Fi[	@Õ@ˇi[[	@)i I"o      #   Returns the 20 most recent favorite Tweets for the specified user
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.;Fi[	@0iI"’      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@0i8I"2      #   Returns an array of numeric IDs for every user following the specified user
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@0iI"’      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@0i+I"’      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@Vi!I"#      #     Twitter.lists
      # @overload lists(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Return all lists that @sferik subscribes to;Fi[	@Vi|I"ƒ      #     Twitter.memberships
      # @overload memberships(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@Vi’I"»      #     Twitter.subscriptions
      # @overload subscriptions(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@ëi/I"~      # @return [Array<Twitter::Tweet>]
      # @overload user_timeline(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@ºiAI"ƒ      #   Returns users following followers of the authenticated user
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@≈iI"      #   Returns extended information for a given user
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Return extended information for @sferik;Fi[	@≈iZI"t      #     Twitter.contributees
      # @overload contributees(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[	@≈iqI"t      #     Twitter.contributors
      # @overload contributors(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[	I"yield);FI"ensure;Fi[[	@iwI"q        begin
          (@manifest_file = var
          yield)
        ensure
          @manifest_file = nil;Fi[	@i#I"ï        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL"))
        yield)
      ensure
        ($stderr.write(color("LITERAL"));Fi[	@i.I"ï        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL"))
        yield)
      ensure
        ($stderr.write(color("LITERAL"));Fi[	@ioI"ê        var.merge!(var.slice("LITERAL", "LITERAL", "LITERAL"))
        @scope.merge!(var)
        yield)
      ensure
        @scope.merge!(var);Fi[	@4i3I"á          (@_haml_concat_raw = true
          var = @_haml_concat_raw
          yield)
        ensure
          @_haml_concat_raw = var;Fi[	@~iLI"Ö        (var = @haml_buffer.active?
        @haml_buffer.active = false
        yield)
      ensure
        @haml_buffer.active = var;Fi[	@~i	I"ã        (var = haml_buffer.tabulation
        haml_buffer.tabulation = var
        yield)
      ensure
        haml_buffer.tabulation = var;Fi[	@~i:I"∞        var.active, var = false, var.active? if var
        @haml_buffer.active, var = true, @haml_buffer.active?
        yield)
      ensure
        (@haml_buffer.active = var;Fi[	@>i2I"p      begin
        (var, $stderr = $stderr, StringIO.new
        yield)
      ensure
        $stderr = var;Fi[	@JißI"Ç      var.concat(["STRING", "STRING"]) if quieter?
      var.each { |var| delete(var) }
      yield)
    ensure
      replace(var);Fi[	@OiI"P      var = pwd
      cd(var)
      yield)
    ensure
      (cd(var) if var;Fi[	@RiI"W        chmod("LITERAL")
      end
      yield)
    ensure
      chmod(var) if var;Fi[	I"0projects//homebrew_proj/test/testing_env.rb;Ti.I"Ñ      $stderr.reopen("STRING", "STRING")
      $stdout.reopen("STRING", "STRING")
      yield)
    ensure
      ($stderr.reopen(var);Fi[	I"module Devise;FI"module Models;Fi[[	@ÍiI"arequire("devise/hooks/activatable")
module Devise
  module Models
    module Authenticatable;Fi[	@1
i I"9module Devise
  module Models
    module Confirmable;Fi [	@ÌiI"àrequire("devise/strategies/database_authenticatable")
require("bcrypt")
module Devise
  module Models
    module DatabaseAuthenticatable;Fi[	@ iI"Wrequire("devise/hooks/lockable")
module Devise
  module Models
    module Lockable;Fi[	I"8projects//devise_proj/devise/models/omniauthable.rb;TiI"Urequire("devise/omniauth")
module Devise
  module Models
    module Omniauthable;Fi[	@—
i I"9module Devise
  module Models
    module Recoverable;Fi [	I"8projects//devise_proj/devise/models/registerable.rb;Ti I":module Devise
  module Models
    module Registerable;Fi [	@ôiI"}require("devise/hooks/rememberable")
require("devise/hooks/forgetable")
module Devise
  module Models
    module Rememberable;Fi[	I"7projects//devise_proj/devise/models/timeoutable.rb;TiI"]require("devise/hooks/timeoutable")
module Devise
  module Models
    module Timeoutable;Fi[	@‘
iI"urequire("devise/strategies/token_authenticatable")
module Devise
  module Models
    module TokenAuthenticatable;Fi[	I"5projects//devise_proj/devise/models/trackable.rb;TiI"Yrequire("devise/hooks/trackable")
module Devise
  module Models
    module Trackable;Fi[	@€i I"9module Devise
  module Models
    module Validatable;Fi [	@0i I"Mmodule Devise
  module Models
    class MissingAttribute < StandardError;Fi [	@r@ài[[	@+
iZI"d            File.join(var, var["LITERAL"])
          end
        else
          var
        end;Fi[	@Òi*I"¶          if var.is_a?(String) and (project_path and absolute_path?(var).!) then
            File.join(project_path, var)
          else
            var
          end;Fi[	@ëiÅI"õ        if (var[("LITERAL"..(var.size - "LITERAL"))] == var) then
          var[((var.size + "LITERAL").."LITERAL")]
        else
          var
        end;Fi[	@ÖiNI"Y        when "STRING" then
          "STRING"
        else
          var
        end;Fi[	@‘i?I"l          var.options = options
          var.send(:"to_#{var}")
        else
          var
        end;Fi[	@ñi”I"|        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"])
        else
          var
        end;Fi[	I"Fprojects//compass_proj/compass/sass_extensions/functions/lists.rb;Ti^I"Ñ      if defined? Sass::Script::List and var.is_a?(Sass::Script::List) then
        var.value.first
      else
        var
      end;Fi[	I"Hprojects//compass_proj/compass/sass_extensions/functions/sprites.rb;Ti»I"i    when Sass::Script::Bool then
      Sass::Script::String.new(var.to_s)
    else
      var
    end;Fi[	@øiGI"è        if (var.first == "LITERAL") and intercept_401?(var) then
          _process_unauthenticated(var)
        else
          var
        end;Fi[	@øiLI"e      when Hash then
        _process_unauthenticated(var, var)
      else
        var
      end;Fi[	I"1projects//haml_proj/haml/helpers/xss_mods.rb;TirI"h        when String then
          Haml::Util.html_safe(var)
        else
          var
        end;Fi[	I"Bprojects//homebrew_proj/vendor/multi_json/adapters/ok_json.rb;Ti'I"^            var.merge!(var => (var))
          end
        else
          var
        end;Fi[	@¶iI"F    when "LITERAL" then
      var.to_i
    else
      var
    end;Fi[	I"require("twitter/base");F@(i[[	I"4projects//twitter_proj/twitter/configuration.rb;Ti I"Qrequire("twitter/base")
module Twitter
  class Configuration < Twitter::Base;Fi [	I"-projects//twitter_proj/twitter/entity.rb;Ti I"Jrequire("twitter/base")
module Twitter
  class Entity < Twitter::Base;Fi [	I"*projects//twitter_proj/twitter/geo.rb;Ti I"Grequire("twitter/base")
module Twitter
  class Geo < Twitter::Base;Fi [	I"/projects//twitter_proj/twitter/language.rb;Ti I"Lrequire("twitter/base")
module Twitter
  class Language < Twitter::Base;Fi [	I"/projects//twitter_proj/twitter/metadata.rb;Ti I"Lrequire("twitter/base")
module Twitter
  class Metadata < Twitter::Base;Fi [	I"-projects//twitter_proj/twitter/oembed.rb;Ti I"Jrequire("twitter/base")
module Twitter
  class OEmbed < Twitter::Base;Fi [	I"5projects//twitter_proj/twitter/profile_banner.rb;Ti I"Qrequire("twitter/base")
module Twitter
  class ProfileBanner < Twitter::Base;Fi [	I"3projects//twitter_proj/twitter/relationship.rb;Ti I"Prequire("twitter/base")
module Twitter
  class Relationship < Twitter::Base;Fi [	@√i I"Qrequire("twitter/base")
module Twitter
  class SearchResults < Twitter::Base;Fi [	I"/projects//twitter_proj/twitter/settings.rb;Ti I"Lrequire("twitter/base")
module Twitter
  class Settings < Twitter::Base;Fi [	I"+projects//twitter_proj/twitter/size.rb;Ti I"Hrequire("twitter/base")
module Twitter
  class Size < Twitter::Base;Fi [	I"1projects//twitter_proj/twitter/suggestion.rb;Ti I"Nrequire("twitter/base")
module Twitter
  class Suggestion < Twitter::Base;Fi [	I",projects//twitter_proj/twitter/trend.rb;Ti I"Irequire("twitter/base")
module Twitter
  class Trend < Twitter::Base;Fi [	@ë@ài[[	@îi?I"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îiDI"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îiII"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îiNI"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îiSI"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îiXI"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îiQI"n      # reduce 62 omitted
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îiVI"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îi[I"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îi`I"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îieI"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îijI"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	@îioI"[      
      def function(var, var, var)
        var = "LITERAL"
        var
      end;Fi[	I"&Backup::Dependency.load("STRING");FI"module Backup;Fi[[	@Ìi I"FBackup::Dependency.load("STRING")
module Backup
  module Notifier;Fi [	@›i I"FBackup::Dependency.load("STRING")
module Backup
  module Notifier;Fi [	@‡i I"FBackup::Dependency.load("STRING")
module Backup
  module Notifier;Fi [	@Ùi I"FBackup::Dependency.load("STRING")
module Backup
  module Notifier;Fi [	@i I"FBackup::Dependency.load("STRING")
module Backup
  module Notifier;Fi [	@˛i I"EBackup::Dependency.load("STRING")
module Backup
  module Storage;Fi [	@i I"EBackup::Dependency.load("STRING")
module Backup
  module Storage;Fi [	@i I"EBackup::Dependency.load("STRING")
module Backup
  module Storage;Fi [	@i I"EBackup::Dependency.load("STRING")
module Backup
  module Storage;Fi [	@	i I"EBackup::Dependency.load("STRING")
module Backup
  module Storage;Fi [	I"0projects//backup_proj/backup/storage/scp.rb;TiI"gBackup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup
  module Storage;Fi[	I"1projects//backup_proj/backup/storage/sftp.rb;TiI"gBackup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup
  module Storage;Fi[	@&
iI"fBackup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup
  module Syncer;Fi[	I",#         transition :parked => :idling;F@◊i[[	@i I"x    #       
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end;Fi[	@iEI"x    #       
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end;Fi[	@ieI"ï    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end;Fi[	@iCI"î    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       ;Fi[	@iuI"î    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       ;Fi[	@%iI"©        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end;Fi[	@%iZI"©        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end;Fi[	@%i~I"©        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end
        #     end;Fi[	@5ioI"ú    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       ;Fi[	@5i†I"ú    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       ;Fi[	@5ièI"ï    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end;Fi[	@›iÊI"Å    #     state_machine do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end;Fi[	@›iˇI"ï    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #     end;Fi[	I"#       event :ignite do;F@≥i[[	@iI"z    #       end
    #       
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@iDI"z    #       end
    #       
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@idI"≤    #   class Vehicle < ActiveRecord::Base
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@iBI"ù    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@itI"ù    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@%iI"¶        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end;Fi[	@%iYI"¶        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end;Fi[	@%i}I"¶        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling
        #       end;Fi[	@5inI"•    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@5iüI"•    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@5iéI"í    #     
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@›iÂI"â    #   class Vehicle
    #     state_machine do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@›i˛I"ù    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling
    #       end;Fi[	@◊@—i[[	@ÍiSI"µ      #       def send_devise_notification(notification)
      #         pending_notifications << notification
      #       end
      #
      #       def send_pending_notifications;Fi[	@ÍiYI"ã      #           devise_mailer.send(n, self).deliver
      #         end
      #       end
      #
      #       def pending_notifications;Fi[	@iI"Ä    #       event :park do
    #         transition :idling => :parked
    #       end
    #       
    #       event :ignite do;Fi[	@iBI"Ä    #       event :park do
    #         transition :idling => :parked
    #       end
    #       
    #       event :ignite do;Fi[	@iDI"Ä    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do;Fi[	@iNI"    #       event :enable do
    #         transition all => :active
    #       end
    #       
    #       event :disable do;Fi[	@ivI"Ä    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do;Fi[	@i{I"    #       event :enable do
    #         transition all => :active
    #       end
    #       
    #       event :disable do;Fi[	@5ipI"Ñ    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :shift_up do;Fi[	@5i°I"Ä    #       event :ignite do
    #         transition :parked => :idling
    #       end
    #       
    #       event :park do;Fi[	@5i√I"}    #       event :turn_on do
    #         transition all => :on
    #       end
    #       
    #       event :turn_off do;Fi[	@5iÕI"Å    #       event :turn_on do
    #         transition all => :active
    #       end
    #       
    #       event :turn_off do;Fi[	@TiI"z    #       event :park
    #         transition all => :parked
    #       end
    #       
    #       event :crash;Fi[	@ä@
i[[	@$i&I"^  attr_writer("LITERAL")
  
  def function(var, var)
    super
    @mirrors = var.mirrors;Fi[	@$i§I"éend
class CurlBottleDownloadStrategy < CurlDownloadStrategy
  def function(var, var)
    super
    @tarball_path = (HOMEBREW_CACHE / "STRING");Fi[	@$i∞I"~end
class SubversionDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @@svn ||= "STRING";Fi[	@$iI"wend
class GitDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @@git ||= "STRING";Fi[	@$iYI"¶end
class CVSDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"));Fi[	@$iÑI"¨end
class MercurialDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"));Fi[	@$i¨I"©end
class BazaarDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"));Fi[	@$i I"©end
class FossilDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"));Fi[	@‡iAI"Wend
class HeadSoftwareSpec < SoftwareSpec
  def function(var, var)
    super
  end;Fi[	@‡iNI"]  attr_reader("LITERAL")
  
  def function(var, var)
    super
    @revision = "LITERAL";Fi[	@iÜI"          end
          
          def function(var, var)
            super
            throw(@jumptag, @encoding) if @encoding;Fi[	@ªiI"ª      ###
      # Create a new XSD parser with +host+ and +opt+
      def function(var, var)
        super
        @parser = ::Nokogiri::XML::SAX::Parser.new(self, (@charset or "STRING"));Fi[	I"I#   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;F@ˇi[[	@i`I"ˆ      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@ixI"      #     Twitter.direct_message_destroy(1825785544)
      # @overload direct_message_destroy(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@)i<I"˘      #     Twitter.unfavorite(25938088801)
      # @overload unfavorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@)iTI"ı      #     Twitter.favorite(25938088801)
      # @overload favorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@)ivI"ı      #     Twitter.favorite(25938088801)
      # @overload favorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@i#I"˘      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@iYI"
      #     Twitter.saved_search_destroy(16129012)
      # @overload saved_search_destroy(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@ßiQI"ö      #     Twitter.statuses(25938088801)
      # @overload statuses(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ßieI"¶      #     Twitter.status_destroy(25938088801)
      # @overload status_destroy(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ßiãI"ò      #     Twitter.retweet(28561922516)
      # @overload retweet(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ßi¶I"ö      #     Twitter.retweet!(28561922516)
      # @overload retweet!(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ºikI"      #     Twitter.statuses_activity(25938088801)
      # @overload statuses_activity(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	I"(var << "STRING");F@>i[[	I"5projects//diaspora_proj/helpers/mobile_helper.rb;Ti(I"Z    else
      var = "STRING"
      (var << "STRING")
      (var << "STRING")
    end;Fi[	@˚i@I"¶        f.plist_startup ? ((var << "STRING")) : ((var << "STRING"))
        if f.plist_manual then
          (var << "STRING")
          (var << "STRING")
        end;Fi[	@˚iGI"ó          (var << "STRING")
          if f.plist_startup then
            (var << "STRING")
            (var << "STRING")
            (var << "STRING");Fi[	@˚iHI"ä          if f.plist_startup then
            (var << "STRING")
            (var << "STRING")
            (var << "STRING")
          else;Fi[	@˚iKI"{            (var << "STRING")
          else
            (var << "STRING")
            (var << "STRING")
          end;Fi[	@!iII"ß    (var << "STRING") if ARGV.include?("STRING")
    (var << "STRING") unless ARGV.verbose?
    (var << "STRING")
    (var << "STRING")
    safe_system("STRING", *var);Fi[	@1iDI"√        (var << `#{locate("STRING")}STRING#{var}STRING`.chomp)
      end
      (var << "STRING")
      (var << "STRING")
      var.map { |var| Pathname.new(var) }.detect { |var| var.directory? });Fi[	@îiI"Ø      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          (var << "STRING")
          (var << "STRING")
          var = Node.new("LITERAL", var);Fi[	@3i∂I"s      var = Resque.info
      var = []
      (var << "STRING")
      (var << "STRING")
      (var << "STRING");Fi[	@3i∑I"s      var = []
      (var << "STRING")
      (var << "STRING")
      (var << "STRING")
      (var << "STRING");Fi[	@3i∏I"|      (var << "STRING")
      (var << "STRING")
      (var << "STRING")
      (var << "STRING")
      (var << "STRING");Fi[	@3iπI"î      (var << "STRING")
      (var << "STRING")
      (var << "STRING")
      (var << "STRING")
      Resque.queues.each { |var| (var << "STRING") };Fi[	I"ï#   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.;F@ˇi[[	@0iPI"g      #     Twitter.friendships(7505382, 14100886)    # Same as above
      # @overload friendships(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@0i}I"á      #     Twitter.follow('sferik')
      # @overload follow(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean] :follow (false) Enable notifications for the target user.;Fi[	@0iñI"â      #     Twitter.follow!('sferik')
      # @overload follow!(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean] :follow (false) Enable notifications for the target user.;Fi[	@0i≥I"E      #     Twitter.unfollow('sferik')
      # @overload unfollow(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@Vi˜I"—      # @overload list_add_members(list, users, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @example Add @BarackObama and @pengwynn to the authenticated user's "presidents" list;Fi[	@ViI"Ë      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @example Add @BarackObama and @pengwynn to @sferik's "presidents" list;Fi[	@ViËI"Ÿ      # @overload list_remove_members(list, users, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @example Remove @BarackObama and @pengwynn from the authenticated user's "presidents" list;Fi[	@ViÚI"Ì      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @example Remove @BarackObama and @pengwynn from @sferik's "presidents" list;Fi[	@ãiI"[      #     Twitter.report_spam(14589771) # Same as above
      # @overload report_spam(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@≈i’I"O      #     Twitter.block(7505382)  # Same as above
      # @overload block(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@≈iËI"S      #     Twitter.unblock(7505382)  # Same as above
      # @overload unblock(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      def function(var);Fi[	@≈i˚I"·      #     Twitter.users(7505382, 14100886)     # Same as above using twitter_ids
      # @overload users(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Symbol, String] :method Requests users via a GET request instead of the standard POST request if set to ':get'.;Fi[	@F@Fi[[	@IiêI"|  
  def function
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end;Fi[	@Ii˝I"¶  # end
  def function
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@Ii˛I"Œ  def function
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiˇI"Ô    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@Ii I"Ô    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiI"Ô    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiI"Ô    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiI"Ô    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiI"Ô    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiI"Ô    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiI"Ô    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@IiI"≈    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end;Fi[	@r@
i[[	I":projects//backup_proj/backup/configuration/helpers.rb;TijI"•          self.class.log_deprecation_warning(var, var)
          var["LITERAL"].call(self, var["LITERAL"]) if var["LITERAL"]
        else
          super
        end;Fi[	@ñiòI"Œ      if ["STRING", "STRING"].include?(var) and (position_or_angle.is_a?(Sass::Script::Number) and position_or_angle.numerator_units.include?("STRING")) then
        false
      else
        super
      end;Fi[	I"Iprojects//compass_proj/compass/sass_extensions/sprites/sprite_map.rb;TiEI"õ          if @evaluation_context.respond_to?(var) then
            @evaluation_context.send(var, *var, &var)
          else
            super
          end;Fi[	@ i]I"è          if lock_strategy_enabled?("LITERAL") and attempts_exceeded? then
            "LITERAL"
          else
            super
          end;Fi[	@ßi…I"|          raise("STRING") if (var.size > "LITERAL")
          super(var.first)
        else
          super
        end;Fi[	@A
i±I"b          add_states(var.known_states)
          var
        else
          super
        end;Fi[	@Âi>I"ß        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else
          super
        end;Fi[	@Úi7I"ß        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else
          super
        end;Fi[	@˜i;I"ß        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else
          super
        end;Fi[	@ÕiI"®          if (action == "LITERAL") then
            define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
          else
            super
          end;Fi[	@oi…I"v          each { |var| var.machine.write(object, "LITERAL", var) }
        end
      else
        super
      end;Fi[	I",projects//twitter_proj/twitter/error.rb;TiI"      if @wrapped_exception.respond_to?("LITERAL") then
        @wrapped_exception.backtrace
      else
        super
      end;Fi[	@ˇI"Õ#   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[[	@0i I"≠      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return @sferik's friends' IDs;Fi[	@0i1I"∂      #   Returns an array of numeric IDs for every user following the authenticated user
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's followers' IDs;Fi[	@0iI"˛      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[	@0i,I"˛      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[	@VivI"<      # @return [Twitter::Cursor]
      # @overload memberships(options={})
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :filter_to_owned_lists When set to true, t or 1, will return just lists the authenticating user owns, and the user represented by user_id or screen_name is a member of.;Fi[	@Vi}I"Ç      # @overload memberships(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :filter_to_owned_lists When set to true, t or 1, will return just lists the authenticating user owns, and the user represented by user_id or screen_name is a member of.;Fi[	@ViãI"˙      # @overload list_subscribers(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the subscribers of the authenticated user's "presidents" list;Fi[	@ViìI"      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the subscribers of @sferik's "presidents" list;Fi[	@Vi2I"Ú      # @overload list_members(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the members of the authenticated user's "presidents" list;Fi[	@Vi:I"      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the members of @sferik's "presidents" list;Fi[	@Vi–I"°      # @return [Twitter::Cursor]
      # @overload subscriptions(options={})
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example List the lists the authenticated user follows;Fi[	@Vi÷I"›      # @overload subscriptions(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example List the lists that @sferik follows;Fi[	I"# @return [String];F@Ói[[	@ÒiXI"G  end
  
  # @return [String]
  def function
    case self.service;Fi[	@ÒidI"G  end
  
  # @return [String]
  def function
    case self.service;Fi[	I".projects//diaspora_proj/models/request.rb;Ti!I"X  
  # Alias of sender_handle
  # @return [String]
  def function
    sender_handle;Fi[	@Œi'I"i  
  # @note Used for XML marshalling
  # @return [String]
  def function
    sender.diaspora_handle;Fi[	@Œi1I"l  
  # @note Used for XML marshalling
  # @return [String]
  def function
    recipient.diaspora_handle;Fi[	@πiI"z    end
    
    # @return [String]
    def function
      @country_code ||= (@attrs["LITERAL"] or @attrs["LITERAL"]);Fi[	@πiI"ñ    end
    
    # @return [String]
    def function
      @place_type ||= (@attrs["LITERAL"] or (@attrs["LITERAL"] and @attrs["LITERAL"]["LITERAL"]));Fi[	@√i I"p    end
    
    # @return [String]
    def function
      @attrs["LITERAL"]["LITERAL"] if search_metadata?;Fi[	@Ãi2I"É    alias "LITERAL" "LITERAL"
    
    # @return [String]
    def function
      (@attrs["LITERAL"] or (user and user.screen_name));Fi[	@ÃizI"    alias "LITERAL" "LITERAL"
    
    # @return [String]
    def function
      var = (@attrs["LITERAL"] or @attrs["LITERAL"]);Fi[	I".projects//twitter_proj/twitter/version.rb;TiI"|    
    class << self
      # @return [String]
      def function
        [MAJOR, MINOR, PATCH, PRE].compact.join("STRING");Fi[	I"assert(var.ppc7400?.!);FI"assert(var.ppc64?.!);Fi[[	I".projects//homebrew_proj/test/test_mach.rb;TiI"|    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?);Fi[	@ÊiI"    assert(var.i386?)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?);Fi[	@Êi+I"    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?);Fi[	@Êi9I"~    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@ÊiGI"~    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@ÊiUI"|    assert(var.i386?)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@ÊicI"|    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@ÊiqI"~    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@ÊiêI"~    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@Êi†I"~    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@Êi∞I"~    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!);Fi[	@W
@˚i[[	@Ri∂I"å      when "STRING", "STRING", "STRING", "STRING" then
        "LITERAL"
      when "STRING" then
        "LITERAL"
      when "STRING" then;Fi[	@Ri∏I"e      when "STRING" then
        "LITERAL"
      when "STRING" then
        "LITERAL"
      else;Fi[	@irI"â      link_dir("STRING", var) do |var|
        case var.to_s
        when "STRING" then
          "LITERAL"
        when INFOFILE_RX then;Fi[	@i}I"Ü      link_dir("STRING", var) do |var|
        case var.to_s
        when "STRING" then
          "LITERAL"
        when "STRING" then;Fi[	@iI"~        when "STRING" then
          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "LITERAL" then;Fi[	@iÉI"~        when "LITERAL" then
          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "STRING" then;Fi[	@iÖI"}        when "STRING" then
          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "STRING" then;Fi[	@iáI"~        when "STRING" then
          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "LITERAL" then;Fi[	@içI"        when "LITERAL" then
          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "LITERAL" then;Fi[	@iëI"p        when "LITERAL" then
          "LITERAL"
        when "STRING" then
          "LITERAL"
        else;Fi[	@1iUI"\    when "LITERAL" then
      "LITERAL"
    when "STRING" then
      "LITERAL"
    else;Fi[	I"var.add("LITERAL");F@i[[	I"+projects//diaspora_proj/models/like.rb;TiI"q  
  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@iI"Ä  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@iI"f    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
  end;Fi[	I"4projects//diaspora_proj/models/participation.rb;TiI"q  
  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@iI"Ä  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@iI"f    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
  end;Fi[	@aiI"q  
  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@aiI"≥  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
    var.add(lambda { |var| var.diaspora_handle }, "LITERAL" => "LITERAL");Fi[	@ÈiI"q  
  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@ÈiI"Ä  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@ÈiI"|    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
    var.add("LITERAL")
    var.add(lambda do |var|;Fi[	@rI"(flash["LITERAL"] = I18n.t("STRING");Fi[[	I"Dprojects//diaspora_proj/controllers/conversations_controller.rb;Ti$I"◊      Postzord::Dispatcher.build(current_user, @conversation).post
      flash["LITERAL"] = I18n.t("STRING")
    else
      flash["LITERAL"] = I18n.t("STRING")
      flash["LITERAL"] = I18n.t("STRING") if var.blank?;Fi[	I"?projects//diaspora_proj/controllers/messages_controller.rb;TiI"ï      Rails.logger.info("STRING")
      Postzord::Dispatcher.build(current_user, var).post
    else
      flash["LITERAL"] = I18n.t("STRING")
    end;Fi[	I"=projects//diaspora_proj/controllers/people_controller.rb;TiçI"∏      @aspects_with_person = @contact.aspects
      @aspect_ids = @aspects_with_person.map(&"LITERAL")
    else
      flash["LITERAL"] = I18n.t("STRING")
      redirect_to(people_path);Fi[	@#i"I"l        end
      end
    else
      flash["LITERAL"] = I18n.t("STRING")
      redirect_to(people_path);Fi[	@ii,I"ú    if current_user.update_profile(@profile_attrs) then
      flash["LITERAL"] = I18n.t("STRING")
    else
      flash["LITERAL"] = I18n.t("STRING")
    end;Fi[	I"?projects//diaspora_proj/controllers/services_controller.rb;Ti$I"ƒ      end
      flash["LITERAL"] = I18n.t("STRING")
    else
      flash["LITERAL"] = I18n.t("STRING")
      if var = Service.where("LITERAL" => (var.type.to_s), "LITERAL" => (var.uid)).first then;Fi[	I"<projects//diaspora_proj/controllers/users_controller.rb;Ti(I"ì            var = true
            flash["LITERAL"] = I18n.t("STRING")
          else
            flash["LITERAL"] = I18n.t("STRING")
          end;Fi[	@Ci/I"µ            if @user.update_attributes(var) then
              flash["LITERAL"] = I18n.t("STRING")
            else
              flash["LITERAL"] = I18n.t("STRING")
            end;Fi[	@Ci7I"π                I18n.locale = @user.language
                flash["LITERAL"] = I18n.t("STRING")
              else
                flash["LITERAL"] = I18n.t("STRING")
              end;Fi[	@Ci@I"Î                  (@user.mail_confirm_email == @user.email)
                  flash["LITERAL"] = I18n.t("STRING") if @user.unconfirmed_email
                else
                  flash["LITERAL"] = I18n.t("STRING")
                end;Fi[	@CiGI"”                  if @user.update_attributes(var) then
                    flash["LITERAL"] = I18n.t("STRING")
                  else
                    flash["LITERAL"] = I18n.t("STRING")
                  end;Fi[	@—I"# Configuration options:;Fi[[	@`i/I"›    # branches/transitions as the source
    # Creates a new event within the context of the given machine
    # 
    # Configuration options:
    # * <tt>:human_name</tt> - The human-readable version of this event's name;Fi[	@`i∑I"≥    # 
    # A collection of the generated edges will be returned.
    # 
    # Configuration options:
    # * <tt>:human_name</tt> - Whether to use the event's human name for the;Fi[	@5i
I"Í    # Creates a new state machine with the given name.  The default name, if not
    # specified, is <tt>:state</tt>.
    # 
    # Configuration options:
    # * <tt>:attribute</tt> - The name of the attribute to store the state value;Fi[	@√iI"ﬂ    # Creates a new collection of nodes for the given state machine.  By default,
    # the collection is empty.
    # 
    # Configuration options:
    # * <tt>:index</tt> - One or more attributes to automatically generate;Fi[	@oiI"–    # empty path.  In order to start walking the path, it must be populated with
    # an initial transition.
    # 
    # Configuration options:
    # * <tt>:target</tt> - The target state to end the path on;Fi[	@viI"∆    # The target state for each path
    # Creates a new collection of paths with the given requirements.
    # 
    # Configuration options:
    # * <tt>:from</tt> - The initial state to start from;Fi[	@}i/I"›    # Maps :method_name => UnboundMethod
    # Creates a new state within the context of the given machine.
    # 
    # Configuration options:
    # * <tt>:initial</tt> - Whether this state is the beginning state for the;Fi[	@}iwI"/    #   State.new(machine, :parked, :value => 1).description                  # => "parked (1)"
    #   State.new(machine, :parked, :value => lambda {Time.now}).description  # => "parked (*)
    # 
    # Configuration options:
    # * <tt>:human_name</tt> - Whether to use this state's human name in the;Fi[	@}iŒI"∂    # 
    # The actual node generated on the graph will be returned.
    # 
    # Configuration options:
    # * <tt>:human_name</tt> - Whether to use the state's human name for the;Fi[	@›iÃI"Ù    # Runs the before / after callbacks for this transition.  If a block is
    # provided, then it will be executed between the before and after callbacks.
    # 
    # Configuration options:
    # * +before+ - Whether to run before callbacks.;Fi[	@oiI"ˇ    # Creates a new collection of transitions that can be run in parallel.  Each
    # transition *must* be for a different attribute.
    # 
    # Configuration options:
    # * <tt>:actions</tt> - Whether to run the action configured for each transition;Fi[	@—@äi[[	@ëi;I"        # method previously used to set the accessor being deprecated.
        # #method_missing will handle any calls to `name=`.
        #
        def function(var, var)
          deprecations[var] = { "LITERAL" => (nil), "LITERAL" => (nil), "LITERAL" => (nil) }.merge(var);Fi[	@ëi^I"¿      # i.e. if an option will accept being set as an Array, then it
      # should be explicitly set as such. e.g. option = [val1, val2]
      #
      def function(var, var)
        var = nil;Fi[	@‘iCI"©      # If the import fails, return nil.
      # Note that errors raised by Cli::Helpers#run may also be rescued here.
      #
      def function(var, var)
        begin;Fi[	@ÂiDI"È      #   sign_in @user, :event => :authentication  # sign_in(resource, options)
      #   sign_in @user, :bypass => true            # sign_in(resource, options)
      #
      def function(var, var)
        var = var.extract_options!;Fi[	@Íi_I"~      #       end
      #     end
      #
      def function(var, var)
        devise_mailer.send(var, self, var).deliver;Fi[	@ÌiHI"o      #     super(params)
      #   end
      #
      def function(var, var)
        var.delete("LITERAL");Fi[	@iÍI"z    #     root :to => "admin/dashboard#show"
    #   end
    #
    def function(var, var)
      var = lambda do |var|;Fi[	@iI"j    #
    #   root :to => 'landing#show'
    #
    def function(var, var)
      var = lambda do |var|;Fi[	@øi$I"º    #   sign_in :user, @user   # sign_in(scope, resource)
    #   sign_in @user          # sign_in(resource)
    #
    def function(var, var)
      var ||= Devise::Mapping.find_scope!(var);Fi[	@µi•I"      #     config.format.as_xml
      #   end
      #
      def function(var, var)
        var = if var.first.is_a?(Hash) then;Fi[	@µiÁI"£      #
      #   node.write_to(io, :indent_text => '-', :indent => 2
      #
      def function(var, var)
        var = var.first.is_a?(Hash) ? (var.shift) : ({});Fi[	I"%# @param request_method [Symbol];FI"# @param path [String];Fi[[	@ViI"y      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array];Fi[	@yitI"z      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param params [Hash];Fi[	@ßiˇI"y      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array];Fi[	@?iI"y      private
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array];Fi[	@?iI"u      end
      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array];Fi[	@?i'I"É      
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array];Fi[	@?i2I"Ö      
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param options [Hash];Fi[	@?iCI"É      
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array];Fi[	@?iOI"Ö      
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param options [Hash];Fi[	@?iZI"§      # @param collection_name [Symbol]
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array];Fi[	@?iiI"¶      # @param collection_name [Symbol]
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param options [Hash];Fi[	@ÓI"
false;Fi[[	@¿
i,I"∏      # Provider defaults to false. Overridden when using a service-based
      # storage such as Amazon S3, Rackspace Cloud Files or Dropbox
      def function
        false
      end;Fi[	@+
icI"@      end
      
      def function
        false
      end;Fi[	@*i5I"@      end
      
      def function
        false
      end;Fi[	@‡iI"j      
      # Do not use remember_me behavior with token.
      def function
        false
      end;Fi[	I"3projects//diaspora_proj/models/conversation.rb;Ti0I",  end
  
  def function
    false
  end;Fi[	@diwI"Ö  
  # @return Returns true if this Post will accept updates (i.e. updates to the caption of a photo).
  def function
    false
  end;Fi[	@di{I",  end
  
  def function
    false
  end;Fi[	@~i\I"∞  # Because `ActionView` helpers may be included in non-`ActionView::Base` classes,
  # it's a good idea to define \{#is\_haml?} for all objects.
  def function
    false
  end;Fi[	@>iMI"G    #
    # @return [Boolean]
    def function
      false
    end;Fi[	@A
iAI"¨      # invoked?  This is used to determine whether to fire off attribute-based
      # event transitions when the action is run.
      def function
        false
      end;Fi[	@¶
i4I"X          # do nothing
        end
        def function
          false
        end;Fi[	I"module SassExtensions;FI"module Sprites;Fi[[	I"Xprojects//compass_proj/compass/sass_extensions/sprites/engines/chunky_png_engine.rb;TiI"Ñend
module Compass
  module SassExtensions
    module Sprites
      class ChunkyPngEngine < Compass::SassExtensions::Sprites::Engine;Fi[	I"Fprojects//compass_proj/compass/sass_extensions/sprites/engines.rb;TiI"Qmodule Compass
  module SassExtensions
    module Sprites
      class Engine;Fi[	@miI"Pmodule Compass
  module SassExtensions
    module Sprites
      class Image;Fi[	I"Lprojects//compass_proj/compass/sass_extensions/sprites/image_methods.rb;TiI"Xmodule Compass
  module SassExtensions
    module Sprites
      module ImageMethods;Fi[	I"Hprojects//compass_proj/compass/sass_extensions/sprites/image_row.rb;TiI"jrequire("forwardable")
module Compass
  module SassExtensions
    module Sprites
      class ImageRow;Fi[	I"Mprojects//compass_proj/compass/sass_extensions/sprites/layout_methods.rb;TiI"Ymodule Compass
  module SassExtensions
    module Sprites
      module LayoutMethods;Fi[	I"Iprojects//compass_proj/compass/sass_extensions/sprites/row_fitter.rb;TiI"krequire("forwardable")
module Compass
  module SassExtensions
    module Sprites
      class RowFitter;Fi[	@ñiI"lmodule Compass
  module SassExtensions
    module Sprites
      class SpriteMap < Sass::Script::Literal;Fi[	@-iI"Ymodule Compass
  module SassExtensions
    module Sprites
      module SpriteMethods;Fi[	I">projects//compass_proj/compass/sass_extensions/sprites.rb;TiI"irequire("compass/sprite_importer")
module Compass
  module SassExtensions
    module Sprites
    end;Fi[	I"+unless method_defined?("LITERAL") then;F@Ói[[	I"-projects//homebrew_proj/extend/symbol.rb;TiI"zclass Symbol
  unless method_defined?("LITERAL") then
    def function
      proc { |var, var| var.send(self, *var) };Fi[	I"Jprojects//nokogiri_proj/nokogiri/html/element_description_defaults.rb;TiI"~      private("LITERAL")
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@ÿiI"t      end
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@ÿi%I"t      end
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@ÿi,I"t      end
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@ÿi3I"t      end
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@ÿi:I"t      end
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@ÿiAI"t      end
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@ÿiHI"t      end
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@ÿiOI"t      end
      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc;Fi[	@îI"module Storage;Fi[[	@¿
i I"2module Backup
  module Storage
    class Base;Fi [	@˛iI"aBackup::Dependency.load("STRING")
module Backup
  module Storage
    class CloudFiles < Base;Fi[	@iI"^Backup::Dependency.load("STRING")
module Backup
  module Storage
    class Dropbox < Base;Fi[	I"0projects//backup_proj/backup/storage/ftp.rb;TiI"Krequire("net/ftp")
module Backup
  module Storage
    class FTP < Base;Fi[	I"2projects//backup_proj/backup/storage/local.rb;Ti I":module Backup
  module Storage
    class Local < Base;Fi [	@iI"_Backup::Dependency.load("STRING")
module Backup
  module Storage
    class Ninefold < Base;Fi[	@iI"\Backup::Dependency.load("STRING")
module Backup
  module Storage
    class RSync < Base;Fi[	@	iI"YBackup::Dependency.load("STRING")
module Backup
  module Storage
    class S3 < Base;Fi[	@´iI"|Backup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup
  module Storage
    class SCP < Base;Fi[	@ÆiI"}Backup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup
  module Storage
    class SFTP < Base;Fi[	@rI"if var["LITERAL"] then;Fi[[	I".projects//compass_proj/compass/actions.rb;Ti-I"¨          log_action("LITERAL", basename(var), var)
          var = true
        else
          if var["LITERAL"] then
            log_action("LITERAL", basename(var), var);Fi[	@ëifI"√          if var.raw_project_type then
            add_configuration(var.raw_project_type.to_sym)
          else
            if var["LITERAL"] then
              add_configuration(var["LITERAL"]);Fi[	@Ci2I"¶              flash["LITERAL"] = I18n.t("STRING")
            end
          else
            if var["LITERAL"] then
              if @user.update_attributes(var) then;Fi[	@Ci:I"¥                flash["LITERAL"] = I18n.t("STRING")
              end
            else
              if var["LITERAL"] then
                @user.unconfirmed_email = var["LITERAL"];Fi[	@CiCI"∫                  flash["LITERAL"] = I18n.t("STRING")
                end
              else
                if var["LITERAL"] then
                  if @user.update_attributes(var) then;Fi[	@ÈiWI"©      var = var.delete("LITERAL")
      var.unprocessed_image.store!(var)
    else
      if var["LITERAL"] then
        var.remote_unprocessed_image_url = var["LITERAL"];Fi[	@µi¡I"        if var["LITERAL"] and var["LITERAL"].! then
          ["LITERAL", var["LITERAL"], (Integer(var["LITERAL"]) * ("LITERAL" ** Integer(var["LITERAL"]["LITERAL"])))]
        else
          if var["LITERAL"] then
            ["LITERAL", var["LITERAL"], Float(var["LITERAL"])];Fi[	I"8projects//nokogiri_proj/nokogiri/decorators/slop.rb;TiI"µ            if var["LITERAL"] then
              var = css("STRING")
            else
              if var["LITERAL"] then
                var = Array(var["LITERAL"]).join("STRING");Fi[	@œiI"y        if var.nil? then
          "STRING"
        else
          if var["LITERAL"] then
            var["LITERAL"];Fi[	@œiI"ù          if var["LITERAL"] then
            var["LITERAL"]
          else
            if var["LITERAL"] then
              var = Array(var["LITERAL"]).first;Fi[	I"require("twitter/user");F@(i[[	@iI"wrequire("twitter/api/utils")
require("twitter/direct_message")
require("twitter/user")
module Twitter
  module API;Fi[	@)i
I"trequire("twitter/error/forbidden")
require("twitter/tweet")
require("twitter/user")
module Twitter
  module API;Fi[	@0i
I"{require("twitter/error/forbidden")
require("twitter/relationship")
require("twitter/user")
module Twitter
  module API;Fi[	@ViI"irequire("twitter/list")
require("twitter/tweet")
require("twitter/user")
module Twitter
  module API;Fi[	@ãiI"Urequire("twitter/api/utils")
require("twitter/user")
module Twitter
  module API;Fi[	@éiI"srequire("twitter/api/utils")
require("twitter/suggestion")
require("twitter/user")
module Twitter
  module API;Fi[	@ëiI"nrequire("twitter/api/utils")
require("twitter/tweet")
require("twitter/user")
module Twitter
  module API;Fi[	@ºi
I"krequire("twitter/cursor")
require("twitter/tweet")
require("twitter/user")
module Twitter
  module API;Fi[	@≈iI"vrequire("twitter/profile_banner")
require("twitter/settings")
require("twitter/user")
module Twitter
  module API;Fi[	@?iI"srequire("twitter/api/arguments")
require("twitter/cursor")
require("twitter/user")
module Twitter
  module API;Fi[	@õI"case var;Fi[[	@<iI"ømodule Compass::SassExtensions::Functions::Lists
  # Returns true when the object is false, an empty string, or an empty list
  def function(var)
    case var
    when Sass::Script::Bool then;Fi[	@?i¬I"P  end
  
  def function(var)
    case var
    when Sass::Script::Color then;Fi[	I"+projects//compass_proj/compass/util.rb;TiI"Q  end
  
  def function(var)
    case var
    when NilClass, FalseClass then;Fi[	I"-projects//diaspora_proj/models/aspect.rb;TiI"A  end
  
  def function(var)
    case var
    when Post then;Fi[	@µiYI"Ç    # is not a String.
    # Strings contained in x must be valid UTF-8.
    def function(var)
      case var
      when Hash then;Fi[	@µidI"K    end
    
    def function(var)
      case var
      when Hash then;Fi[	@µiÅI"M    end
    
    def function(var)
      case var
      when String then;Fi[	@ i"I"õ  #   5. An instance of `Redis`, `Redis::Client`, `Redis::DistRedis`,
  #      or `Redis::Namespace`.
  def function(var)
    case var
    when String then;Fi[	@°iI"^      private
      
      def function(var)
        case var
        when "LITERAL" then;Fi[	I":projects//twitter_proj/twitter/response/parse_json.rb;Ti
I"è  module Response
    class ParseJson < Faraday::Response::Middleware
      def function(var)
        case var
        when "LITERAL", nil then;Fi[	I"module Compass;F@≥i[[	@∑i
I"Z  require("chunky_png")
end
module Compass
  module SassExtensions
    module Sprites;Fi[	@∫i I">module Compass
  module SassExtensions
    module Sprites;Fi [	@mi I">module Compass
  module SassExtensions
    module Sprites;Fi [	@øi I">module Compass
  module SassExtensions
    module Sprites;Fi [	@¬iI"Urequire("forwardable")
module Compass
  module SassExtensions
    module Sprites;Fi[	@≈i I">module Compass
  module SassExtensions
    module Sprites;Fi [	@»iI"Urequire("forwardable")
module Compass
  module SassExtensions
    module Sprites;Fi[	@ñi I">module Compass
  module SassExtensions
    module Sprites;Fi [	@-i I">module Compass
  module SassExtensions
    module Sprites;Fi [	@œiI"wrequire("digest/md5")
require("compass/sprite_importer")
module Compass
  module SassExtensions
    module Sprites;Fi[	I"##;FI"3# Creates a new instance of the storage object;Fi[[	@¿
iI"„      # same type. This will be appended to the YAML storage file used for
      # cycling backups.
      ##
      # Creates a new instance of the storage object
      # * Called with super(model, storage_id) from each subclass;Fi[	@˛iI"û      ##
      # Rackspace Cloud Files container name and path
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@iI"ô      ##
      # Path to where the backups will be stored
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@ÙiI"Ç      ##
      # use passive mode?
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@˜iI"ñ      ##
      # Path where the backup will be stored.
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@iI"à      ##
      # Ninefold directory path
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@iI"ä      ##
      # Flag to use local backups
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@	iI"í      ##
      # Region of the specified S3 bucket
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@´iI"â      ##
      # Path to store backups to
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@ÆiI"â      ##
      # Path to store backups to
      ##
      # Creates a new instance of the storage object
      def function(var, var, var);Fi[	@rI"raise("STRING");Fi[[	@ëi/I"¬                  if var.is_a?(Symbol) then
                    Compass::AppIntegration.lookup(var).configuration
                  else
                    raise("STRING")
                  end;Fi[	@ôi6I"ê          if respond_to?("LITERAL") and var = authenticatable_salt then
            var
          else
            raise("STRING")
          end;Fi[	I"5projects//diaspora_proj/helpers/stream_helper.rb;TiI"∂          if controller.instance_of?(StreamsController) then
            stream_path("LITERAL" => (time_for_scroll(@stream)))
          else
            raise("STRING")
          end;Fi[	@>i$I"Z    when Photo then
      (self.photos << var)
    else
      raise("STRING")
    end;Fi[	@1iI"{          when ::Haml::Error then
            raise("STRING")
          else
            raise("STRING")
          end;Fi[	@&irI"{        ohai("STRING")
        var.children.each { |var| puts("STRING") }
      else
        raise("STRING")
      end;Fi[	@$i,I"d    when "LITERAL" then
      SubversionDownloadStrategy
    else
      raise("STRING")
    end;Fi[	@.i"I"}    when "LITERAL", "LITERAL", "LITERAL", "LITERAL" then
      (var << "LITERAL")
    else
      raise("STRING")
    end;Fi[	I"1projects//homebrew_proj/test/test_updater.rb;TiI"\      (@called << var)
      @outputs[var].shift
    else
      raise("STRING")
    end;Fi[	@bi!I"[      if var = var.first then
        var
      else
        raise("STRING")
      end;Fi[	I"# states for the attribute;F@õi[[	@ÂiII"ƒ      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var)
        create_scope(var, lambda { |var| ["STRING", var] });Fi[	@ÂiOI"«      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var)
        create_scope(var, lambda { |var| ["STRING", var] });Fi[	@ÃiwI"¬      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var)
        lambda { |var, var| var.all(attribute => (var)) };Fi[	@Ãi}I"–      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var)
        lambda { |var, var| var.all(attribute.to_sym.not => (var)) };Fi[	@ÚiBI"      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var)
        define_scope(var, lambda { |var| { "LITERAL" => ({ attribute => ({ "STRING" => (var) }) }) } });Fi[	@ÚiHI"Û      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var)
        define_scope(var, lambda { |var| { "LITERAL" => ({ attribute => ({ "STRING" => (var) }) }) } });Fi[	@˜iFI"›      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var)
        define_scope(var, lambda { |var| { attribute => ({ "STRING" => (var) }) } });Fi[	@˜iLI"‡      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var)
        define_scope(var, lambda { |var| { attribute => ({ "STRING" => (var) }) } });Fi[	@M
isI"ﬂ      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var)
        create_scope(var, lambda { |var, var| var.filter(attribute_column => (var)) });Fi[	@M
iyI"„      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var)
        create_scope(var, lambda { |var, var| var.exclude(attribute_column => (var)) });Fi[	@ä@Ôi[[	@‘iDI"∫      # Note that errors raised by Cli::Helpers#run may also be rescued here.
      #
      def function(var, var)
        begin
          (var = Tempfile.open("STRING", Config.tmp_path);Fi[	@iYI"q    end
    
    def function(var, var)
      begin
        (var, @scope["LITERAL"] = @scope["LITERAL"], nil;Fi[	@~i]I"‘    # @yield [args] A block of Haml code that will be converted to a string
    # @yieldparam args [Array] `args`
    def function(var, var)
      begin
        (var = (eval("STRING", var.binding) or haml_buffer);Fi[	@:i(I"∆  # Allows us to ensure a puts happens before the block exits so that if say,
  # an exception is thrown, its output starts on a new line.
  def function(var, var)
    begin
      (Kernel.print(var);Fi[	I"-projects//homebrew_proj/extend/object.rb;TiI"zclass Object
  unless method_defined?("LITERAL") then
    def function(var, var)
      begin
        (var = "LITERAL";Fi[	@Â	iI"    #
    # Returns nothing.
    def function(var, var)
      begin
        (self.content = File.read(File.join(var, var));Fi[	@3ifI"p      end
      attr_reader :function
      def function(var, var)
        begin
          (@partial = true;Fi[	@«ieI"∫    # Also accepts a block which will be passed the job as soon as it
    # has completed processing. Useful for testing.
    def function(var, var)
      begin
        (var = Float(var);Fi[	@«ièI"√    # DEPRECATED. Processes a single job. If none is given, it will
    # try to produce one. Usually run in the child.
    def function(var, var)
      begin
        (unless var ||= reserve then;Fi[	@≈i2I"ü      #   Twitter.user?('sferik')
      #   Twitter.user?(7505382)  # Same as above
      def function(var, var)
        begin
          (merge_user!(var, var);Fi[	I"var = "LITERAL".match(var);FI".return var.captures.first unless var.nil?;Fi[[	@¶i{I"—    var = "LITERAL".match(var)
    return var.captures.first.gsub("STRING", "STRING") unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶i}I"∏    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶iI"∏    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶i|I"∏    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶i~I"∏    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶iÄI"∏    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶iÇI"∏    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶iÑI"∏    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶iÜI"∏    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶iàI"Ω    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s);Fi[	@≈@ƒi[[	@¶ixI"Â    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first.gsub("STRING", "STRING") unless var.nil?;Fi[	@¶i|I"‡    return var.captures.first.gsub("STRING", "STRING") unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@¶i~I"«    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@¶i{I"«    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@¶i}I"«    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@¶iI"«    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@¶iÅI"«    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@¶iÉI"«    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@¶iÖI"«    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@¶iáI"«    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?;Fi[	@rI"8render("LITERAL" => (true), "LITERAL" => "LITERAL");Fi[[	I"?projects//diaspora_proj/controllers/comments_controller.rb;TiI"e        end
      end
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
    end;Fi[	I"<projects//diaspora_proj/controllers/likes_controller.rb;TiI"e        end
      end
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
    end;Fi[	@9iI"≤      Webfinger.in_background(params["LITERAL"], "LITERAL" => (true))
      render("LITERAL" => (true))
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
    end;Fi[	@#iFI"o          end
        end
      else
        render("LITERAL" => (true), "LITERAL" => "LITERAL")
      end;Fi[	@#iII"ê        render("LITERAL" => (true), "LITERAL" => "LITERAL")
      end
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
    end;Fi[	I"<projects//diaspora_proj/controllers/posts_controller.rb;Ti0I"ø      var = OEmbedPresenter.new(var, params.slice("LITERAL", "LITERAL", "LITERAL"))
      render("LITERAL" => (var))
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
    end;Fi[	I">projects//diaspora_proj/controllers/publics_controller.rb;Ti#I"ë    if @person.present? and @person.local? then
      render("STRING")
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
    end;Fi[	I"?projects//diaspora_proj/controllers/reshares_controller.rb;TiI"#      current_user.dispatch_post(@reshare, "LITERAL" => (post_url(@reshare)), "LITERAL" => (@reshare.root_author))
      render("LITERAL" => (ExtremePostPresenter.new(@reshare, current_user)), "LITERAL" => "LITERAL")
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
    end;Fi[	I"Eprojects//diaspora_proj/controllers/tag_followings_controller.rb;TiI"∂      if @tag_following.save then
        render("LITERAL" => (@tag.to_json), "LITERAL" => "LITERAL")
      else
        render("LITERAL" => (true), "LITERAL" => "LITERAL")
      end;Fi[	@CièI"Ñ    if var.present? then
      redirect_to(var.image_url)
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
    end;Fi[	@äI" assert_type(var, "LITERAL");Fi[[	I"Gprojects//compass_proj/compass/sass_extensions/functions/colors.rb;TiI"€module Compass::SassExtensions::Functions::Colors
  # a genericized version of lighten/darken so that negative values can be used.
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	@iI"  # If the amount is negative, the color is scaled darker, if positive, it is scaled lighter.
  # This will never return a pure light or dark color unless the amount is 100%.
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	@iI"≥  
  # a genericized version of saturation/desaturate so that negative values can be used.
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	@i I"  # If the amount is negative, the color is desaturated, if positive, it is saturated.
  # This will never return a pure saturated or desaturated color unless the amount is 100%.
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	@i&I"f  end
  
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	@i-I"f  end
  
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	@ñiGI"¥    
    # returns the end position of the gradient from the color stop
    def function(var, var)
      assert_type(var, "LITERAL")
      var = Sass::Script::Number.new("LITERAL");Fi[	@<i I"Å  
  # Get the nth value from a list
  def function(var, var)
    assert_type(var, "LITERAL")
    if (var.value == "STRING") then;Fi[	I"Eprojects//compass_proj/compass/sass_extensions/functions/math.rb;Ti$I"å  Sass::Script::Functions.declare("LITERAL", [])
  
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	@#i:I"ï  Sass::Script::Functions.declare("LITERAL", ["LITERAL"])
  
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	I"	puts;F@ii[[	@li®I"y      def function
        unless options.any? then
          puts
          puts("STRING")
          puts("STRING");Fi[	@li´I"w          puts("STRING")
          puts("STRING")
          puts
          puts("STRING")
          puts("STRING");Fi[	@liÆI"w          puts("STRING")
          puts("STRING")
          puts
          puts("STRING")
          puts("STRING");Fi[	@liæI"t        end
        if options["LITERAL"] then
          puts
          puts("STRING")
          puts("STRING");Fi[	@&iNI"      puts(var.homepage)
      if var.keg_only? then
        puts
        puts("STRING")
        puts(var.keg_only_reason);Fi[	@ciI"Æ        if var.to_s.empty?.! and ($stdout.tty? and var = blacklisted?(var)) then
          unless var.empty? then
            puts
            puts("STRING")
            puts;Fi[	@≥i'I"c      puts("STRING")
      puts("STRING")
      puts
      puts("STRING")
      puts("STRING");Fi[	@çiaI"X  def function
    if ARGV.verbose?.! then
      puts
      puts("STRING")
    else;Fi[	@„iI"~        var.process
      rescue Jekyll::FatalException => var
        puts
        puts("STRING")
        puts("STRING");Fi[	@I"&# @return [Array<Twitter::Tweet>];Fi[[	@Vi1I"
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @overload list_timeline(list, options={});Fi[	@ëiI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.;Fi[	@ëi,I"
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @overload user_timeline(user, options={});Fi[	@ëiDI"8      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[	@ëi\I"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.;Fi[	@ëiqI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.;Fi[	@ëiÉI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.;Fi[	@ëiòI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.;Fi[	@ßiI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param id [Integer] The numerical ID of the desired Tweet.;Fi[	@I" # @return [Twitter::Cursor];Fi[[	@0iI"˚      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={});Fi[	@0i,I"˝      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload follower_ids(options={});Fi[	@0i]I"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @param options [Hash] A customizable set of options.;Fi[	@0ilI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @param options [Hash] A customizable set of options.;Fi[	@0i˛I"˚      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={});Fi[	@0iI"˚      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={});Fi[	@VisI"¸      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload memberships(options={});Fi[	@Vi.I"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload list_members(list, options={});Fi[	@ViÕI"˛      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload subscriptions(options={});Fi[	@@i[[	@iI"  # a genericized version of lighten/darken so that negative values can be used.
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var.with("LITERAL" => (Compass::Util.restrict((var.lightness + var.value), "LITERAL")));Fi[	@iI"Ú  # This will never return a pure light or dark color unless the amount is 100%.
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var.with("LITERAL" => (scale_color_value(var.lightness, var.value)));Fi[	@iI"  # a genericized version of saturation/desaturate so that negative values can be used.
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var.with("LITERAL" => (Compass::Util.restrict((var.saturation + var.value), "LITERAL")));Fi[	@i!I"˛  # This will never return a pure saturated or desaturated color unless the amount is 100%.
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var.with("LITERAL" => (scale_color_value(var.saturation, var.value)));Fi[	@i'I"†  
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var = Sass::Script::Color.new(["LITERAL", "LITERAL", "LITERAL"]);Fi[	@i.I"†  
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var = Sass::Script::Color.new(["LITERAL", "LITERAL", "LITERAL"]);Fi[	@#i%I"ó  
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    raise(Sass::SyntaxError, "STRING") unless var.unitless?;Fi[	@#i;I"ó  
  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    raise(Sass::SyntaxError, "STRING") unless var.unitless?;Fi[	@?iòI"≠  #     background-position: 3px -36px;
  def function(var, var, var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var = convert_sprite_name(var);Fi[	@ﬂI"/#     state_machine :initial => :parked do;Fi[[	@iI"}    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :park do;Fi[	@i>I"}    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :park do;Fi[	@i@I"~    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do;Fi[	@irI"~    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do;Fi[	@5iCI"î    # generated on the class.  For example,
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do;Fi[	@YiI"~    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling;Fi[	@Yi)I"~    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling;Fi[	@YiDI"~    # == Examples
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling;Fi[	@›i¸I"~    # == Example
    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do;Fi[	@rI"nil;Fi[[	@ñiªI"è        if var.value.is_a?(Array) and var.value.all? { |var| color_stop?(var) } then
          var.value
        else
          nil
        end;Fi[	I"Iprojects//diaspora_proj/controllers/aspect_memberships_controller.rb;TiI"~    var = if @contact then
      @contact.aspect_memberships.where("LITERAL" => (@aspect_id)).first
    else
      nil
    end;Fi[	@¢i#I"`          var.email_the_user(var, var)
          var
        else
          nil
        end;Fi[	@ai|I"â      if var["LITERAL"].present? and var = User.find_by_username(var["LITERAL"]) then
        var.person
      else
        nil
      end;Fi[	@KigI"ä      (var = if var.instance_variable_defined?("STRING") then
        var.instance_variable_get("STRING")
      else
        nil
      end;Fi[	@≥idI"Y    when /^#{HOMEBREW_LIBRARY}STRING/ then
      "STRING"
    else
      nil
    end;Fi[	@ôiÁI"é      if var and (var < (self.site.posts.length - "LITERAL")) then
        self.site.posts[(var + "LITERAL")]
      else
        nil
      end;Fi[	@«i.I"H        var.to_s = var
        var
      else
        nil
      end;Fi[	I"Eprojects//state_machine_proj/state_machine/yard/handlers/base.rb;TiI"v          when "LITERAL" then
            var.jump("LITERAL").source
          else
            nil
          end;Fi[	I"# Produces:;F@—i[[	@~iÄI"L    #       = i[0]
    #
    # Produces:
    #
    #     <li>hello</li>;Fi[	@~iãI"C    #       %p= val
    #
    # Produces:
    #
    #     <li>;Fi[	@~iõI"g    #       %a{ href="#" }= item
    #
    # Produces:
    #
    #     <li class='nav' role='nav'>;Fi[	@~i›I"M    #     %strong baz
    #
    # Produces:
    #
    #     <h1>foo</h1>;Fi[	@~i¸I"M    #     %strong baz
    #
    # Produces:
    #
    #     <h1>foo</h1>;Fi[	@~iI"o    #       %a{:href => "food"} chicken
    #
    # Produces:
    #
    #     (<a href='food'>chicken</a>);Fi[	@~iI"]    #       %strong angry
    #
    # Produces:
    #
    #     *<strong>angry</strong>*;Fi[	@~i2I"t    #       %span.small Not really
    #
    # Produces:
    #
    #     *<span class='small'>Not really</span>;Fi[	@~iDI"T    #       %a{:href=>"thing"} here
    #
    # Produces:
    #
    #     click;Fi[	@‚	@Ói[[	@Â	i$I"ö    # Transform the contents based on the content type.
    #
    # Returns nothing.
    def function
      self.content = converter.convert(self.content);Fi[	@bi#I"    # Public: Read, process, and write this Site to output.
    #
    # Returns nothing.
    def function
      self.reset;Fi[	@biCI"ö    # Load necessary libraries, plugins, converters, and generators.
    #
    # Returns nothing.
    def function
      require("classifier") if self.lsi;Fi[	@biVI"è    # Read Site data from disk and load it into internal data structures.
    #
    # Returns nothing.
    def function
      self.read_layouts;Fi[	@bi_I"|    # with each one.
    #
    # Returns nothing.
    def function
      var = File.join(self.source, self.config["STRING"]);Fi[	@bi°I"á    # Run each of the Generators.
    #
    # Returns nothing.
    def function
      self.generators.each { |var| var.generate(self) };Fi[	@bi®I"h    # Render the site to the destination.
    #
    # Returns nothing.
    def function
      begin;Fi[	@bi∑I"É    # Remove orphaned files and empty directories in destination.
    #
    # Returns nothing.
    def function
      var = Set.new;Fi[	@bi⁄I"é    # Write static files, pages, and posts.
    #
    # Returns nothing.
    def function
      self.posts.each { |var| var.write(self.dest) };Fi[	@ˇI"&var.flatten.threaded_map do |var|;Fi[[	@)iWI"ø      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin;Fi[	@)iyI"ø      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin;Fi[	@i\I"      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          object_from_response(Twitter::SavedSearch, "LITERAL", "STRING", var.options);Fi[	@ßièI"=      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin;Fi[	@ßi™I"=      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin;Fi[	@ßi˜I"R      #   @option options [String] :lang Language code for the rendered embed. This will affect the text and localization of the rendered HTML.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          object_from_response(Twitter::OEmbed, "LITERAL", "STRING", var.options);Fi[	@ßiI"ı      # @return [Array<Twitter::Tweet>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          object_from_response(Twitter::Tweet, var, (var + "STRING"), var.options);Fi[	@?iI"˜      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          object_from_response(Twitter::User, var, var, merge_user(var.options, var));Fi[	@?iHI"Ï      # @return [Array]
      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          object_from_response(var, var, var, var.options.merge("LITERAL" => (var)));Fi[	@—@ßi[[	@~iI"e    #     = list_of([['hello'], ['yall']]) do |i|
    #       = i[0]
    #
    # Produces:
    #;Fi[	@~iäI"R    #       %h3= key.humanize
    #       %p= val
    #
    # Produces:
    #;Fi[	@~iöI"ú    #     = list_of(["Home", "About", "Contact", "FAQ"], {class: "nav", role: "nav"}) do |item|
    #       %a{ href="#" }= item
    #
    # Produces:
    #;Fi[	@~i‹I"K    #     - tab_down
    #     %strong baz
    #
    # Produces:
    #;Fi[	@~i˚I"I    #       %p bar
    #     %strong baz
    #
    # Produces:
    #;Fi[	@~iI"i    #     = surround '(', ')' do
    #       %a{:href => "food"} chicken
    #
    # Produces:
    #;Fi[	@~iI"V    #     = surround '*' do
    #       %strong angry
    #
    # Produces:
    #;Fi[	@~i1I"^    #     = precede '*' do
    #       %span.small Not really
    #
    # Produces:
    #;Fi[	@~iCI"_    #     = succeed '.' do
    #       %a{:href=>"thing"} here
    #
    # Produces:
    #;Fi[	I"#       ...;F@¢i[[	@5iJI"    #   class Vehicle
    #     state_machine :status, :attribute => :status_value do
    #       ...
    #     end
    #   end;Fi[	@5iRI"|    #   class Vehicle
    #     state_machine :status, :initial => :parked do
    #       ...
    #     end
    #   end;Fi[	@5iZI"©    #   class Vehicle
    #     state_machine :status, :initial => lambda {|vehicle| vehicle.speed == 0 ? :parked : :idling} do
    #       ...
    #     end
    #   end;Fi[	@5i⁄I"{    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #   end;Fi[	@5iËI"z    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     ;Fi[	@5i˘I"z    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     ;Fi[	@5iI"z    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end
    #     ;Fi[	@5i#I"~    #     state_machine :state, :initial => :parked do
    #       state nil, :idling
    #       ...
    #     end
    #     ;Fi[	@›i≥I"p    #   class Vehicle
    #     state_machine :action => :save do
    #       ...
    #     end
    #   end;Fi[	@CI"module HTML;Fi[[	I"5projects//nokogiri_proj/nokogiri/html/builder.rb;Ti I"Mmodule Nokogiri
  module HTML
    class Builder < Nokogiri::XML::Builder;Fi [	@i I"Omodule Nokogiri
  module HTML
    class Document < Nokogiri::XML::Document;Fi [	I"?projects//nokogiri_proj/nokogiri/html/document_fragment.rb;Ti I"_module Nokogiri
  module HTML
    class DocumentFragment < Nokogiri::XML::DocumentFragment;Fi [	@úi I"?module Nokogiri
  module HTML
    class ElementDescription;Fi [	@ÿi I"?module Nokogiri
  module HTML
    class ElementDescription;Fi [	I";projects//nokogiri_proj/nokogiri/html/entity_lookup.rb;Ti I"lmodule Nokogiri
  module HTML
    class EntityDescription < Struct.new("LITERAL", "LITERAL", "LITERAL");Fi [	I"8projects//nokogiri_proj/nokogiri/html/sax/parser.rb;Ti I"1module Nokogiri
  module HTML
    module SAX;Fi [	I"@projects//nokogiri_proj/nokogiri/html/sax/parser_context.rb;Ti I"1module Nokogiri
  module HTML
    module SAX;Fi [	I"=projects//nokogiri_proj/nokogiri/html/sax/push_parser.rb;Ti I"1module Nokogiri
  module HTML
    module SAX;Fi [	@êI"#var = Node.new("LITERAL", var);Fi[[	@îijI"j      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@îipI"z      
      # reduce 13 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@îiuI"j      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@îiêI"z      
      # reduce 22 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@îi(I"à      # reduce 49 omitted
      # reduce 50 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@îi-I"j      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@îi2I"j      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@îi7I"j      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@îi@I"à      # reduce 57 omitted
      # reduce 58 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var;Fi[	@—I"# == Example;Fi[[	@i=I"¢    # *after* callback with only affect that event's transition.  Other
    # transitions will continue to run their own callbacks.
    # 
    # == Example
    # ;Fi[	@ioI"t    # 
    # See StateMachine::InstanceMethods#fire_events for more information.
    # 
    # == Example
    # ;Fi[	@"i3I"†        # integration is activated, each version that is marked as active will
        # also extend the integration.
        # 
        # == Example
        # ;Fi[	@%isI"Ã        # Creates a callback that will be invoked *after* a transition failures to
        # be performed so long as the given requirements match the transition.
        # 
        # == Example
        # ;Fi[	@›iìI"~    # Does this transition represent a loopback (i.e. the from and to state
    # are the same)
    # 
    # == Example
    # ;Fi[	@›i°I"©    # A hash of all the core attributes defined for this transition with their
    # names as keys and values of the attributes as values.
    # 
    # == Example
    # ;Fi[	@›i‡I"ë    # transition.  Once the state is persisted, it cannot be persisted again
    # until this transition is reset.
    # 
    # == Example
    # ;Fi[	@›i˘I"£    # Rolls back changes made to the object's state via this transition.  This
    # will revert the state back to the +from+ value.
    # 
    # == Example
    # ;Fi[	@›iñI"í    # Gets a hash of the context defining this unique transition (including
    # event, from state, and to state).
    # 
    # == Example
    # ;Fi[	@)@ài[[	@îikI"j      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@îiqI"}      # reduce 13 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@îivI"j      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@îiëI"}      # reduce 22 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@îi)I"}      # reduce 50 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@îi.I"j      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@îi3I"j      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@îi8I"j      
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@îiAI"}      # reduce 58 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", var)
        var
      end;Fi[	@‚	@õi[[	@Ï	iI"p    # site - The Site.
    #
    # Returns nothing.
    def function(var)
      var.pages.dup.each do |var|;Fi[	@Ô	i'I"ä    # name - The String filename of the layout file.
    #
    # Returns nothing.
    def function(var)
      self.ext = File.extname(var);Fi[	@ñi]I"à    # name - The String filename of the page file.
    #
    # Returns nothing.
    def function(var)
      self.ext = File.extname(var);Fi[	@ñiI"Ñ    # dest - The String path to the destination dir.
    #
    # Returns nothing.
    def function(var)
      var = destination(var);Fi[	@ôiZI"v    # name - The String filename of the post file.
    #
    # Returns nothing.
    def function(var)
      begin;Fi[	@ôi–I"Ñ    # dest - The String path to the destination dir.
    #
    # Returns nothing.
    def function(var)
      var = destination(var);Fi[	@biqI"¶    # dir - The String relative path of the directory to read. Default: ''.
    #
    # Returns nothing.
    def function(var)
      var = File.join(self.source, var);Fi[	@biâI"£    # dir - The String relative path of the directory to read.
    #
    # Returns nothing.
    def function(var)
      var = File.join(self.source, var, "STRING");Fi[	@
iI"ù# path - The String relative path from here to the directory.
#
# Returns nothing.
def function(var)
  var = File.join(File.dirname("STRING"), var, "STRING");Fi[	I"module Jobs;FI"module Mail;Fi[[	I"?projects//diaspora_proj/models/jobs/mail/also_commented.rb;Ti I"=module Jobs
  module Mail
    class AlsoCommented < Base;Fi [	I"@projects//diaspora_proj/models/jobs/mail/comment_on_post.rb;Ti I"=module Jobs
  module Mail
    class CommentOnPost < Base;Fi [	I">projects//diaspora_proj/models/jobs/mail/confirm_email.rb;Ti I"<module Jobs
  module Mail
    class ConfirmEmail < Base;Fi [	I"Eprojects//diaspora_proj/models/jobs/mail/invite_user_by_email.rb;Ti I"Amodule Jobs
  module Mail
    class InviteUserByEmail < Base;Fi [	I"6projects//diaspora_proj/models/jobs/mail/liked.rb;Ti I"5module Jobs
  module Mail
    class Liked < Base;Fi [	I":projects//diaspora_proj/models/jobs/mail/mentioned.rb;Ti I"9module Jobs
  module Mail
    class Mentioned < Base;Fi [	I"@projects//diaspora_proj/models/jobs/mail/private_message.rb;Ti I">module Jobs
  module Mail
    class PrivateMessage < Base;Fi [	I"9projects//diaspora_proj/models/jobs/mail/reshared.rb;Ti I"8module Jobs
  module Mail
    class Reshared < Base;Fi [	I"@projects//diaspora_proj/models/jobs/mail/started_sharing.rb;Ti I">module Jobs
  module Mail
    class StartedSharing < Base;Fi [	@êI"super(var, var);Fi[[	@˛iI"ï      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @servicenet ||= false;Fi[	@i I"í      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @path ||= "STRING";Fi[	@ÙiI"ì      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL";Fi[	@˜iI"Ã      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @path ||= File.join(File.expand_path((ENV["STRING"] or "STRING")), "STRING");Fi[	@iI"í      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @path ||= "STRING";Fi[	@iI"ì      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL";Fi[	@	iI"í      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @path ||= "STRING";Fi[	@´iI"ì      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL";Fi[	@ÆiI"ì      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL";Fi[	@NI"module Configuration;Fi[[	@Òi I">module Compass
  module Configuration
    module Adapters;Fi [	@i I">module Compass
  module Configuration
    module Comments;Fi [	I"9projects//compass_proj/compass/configuration/data.rb;Ti I"9module Compass
  module Configuration
    class Data;Fi [	@#i I">module Compass
  module Configuration
    module Defaults;Fi [	I">projects//compass_proj/compass/configuration/file_data.rb;Ti I"Dmodule Compass
  module Configuration
    class FileData < Data;Fi [	@ëi I"Hmodule Compass
  module Configuration
    @callbacks_loaded = false;Fi [	@Çi I"Amodule Compass
  module Configuration
    module Inheritance;Fi [	@di I"Cmodule Compass
  module Configuration
    module Serialization;Fi [	I"4projects//compass_proj/compass/configuration.rb;Ti I"Zmodule Compass
  module Configuration
    def self.attributes_for_directory(var, var);Fi [	@Õ@ri[[	@Çi¥I"ö        def function(var)
          if super then
            true
          else
            inherited_data ? (inherited_data.respond_to?(var)) : (false);Fi[	@ÂiNI"µ        else
          if (warden.user(var) == var) and var.delete("LITERAL").! then
            true
          else
            warden.set_user(var, var.merge!("LITERAL" => (var)));Fi[	@ iJI"õ        unlock_access! if lock_expired?
        if super and access_locked?.! then
          true
        else
          self.failed_attempts ||= "LITERAL";Fi[	@Îi)I"{        if var then
          decorate(var)
          true
        else
          fail!(var.unauthenticated_message) if var;Fi[	@.iKI"Ñ    if self.profile.update_attributes(var) then
      Postzord::Dispatcher.build(self, profile).post
      true
    else
      false;Fi[	@{iYI"¢    def function(var)
      if var.value and var.value["LITERAL"] then
        true
      else
        var.parent ? (nuke_inner_whitespace?(var.parent)) : (false);Fi[	I"+projects//homebrew_proj/cmd/cleanup.rb;TiWI"t  def function
    if (keg_only?.! or ARGV.force?) then
      true
    else
      if opt_prefix.directory? then;Fi[	@i*I"r      case File.basename(var)
      when "STRING", "STRING" then
        true
      else
        # do nothing;Fi[	@ÃiaI"µ            var = self.class.state_machine(var)
            if var.states.match(self) then
              true
            else
              [false, var.generate_message("LITERAL")];Fi[	@>@—i[[	@i>I"u    # transitions will continue to run their own callbacks.
    # 
    # == Example
    # 
    #   class Vehicle;Fi[	@ipI"~    # See StateMachine::InstanceMethods#fire_events for more information.
    # 
    # == Example
    # 
    #   class Vehicle;Fi[	@"i4I"v        # also extend the integration.
        # 
        # == Example
        # 
        #   module StateMachine;Fi[	@%itI"ì        # be performed so long as the given requirements match the transition.
        # 
        # == Example
        # 
        #   class Vehicle;Fi[	@›iîI"c    # are the same)
    # 
    # == Example
    # 
    #   machine = StateMachine.new(Vehicle);Fi[	@›i¢I"Ü    # names as keys and values of the attributes as values.
    # 
    # == Example
    # 
    #   machine = StateMachine.new(Vehicle);Fi[	@›i·I"_    # until this transition is reset.
    # 
    # == Example
    # 
    #   class Vehicle;Fi[	@›i˙I"o    # will revert the state back to the +from+ value.
    # 
    # == Example
    # 
    #   class Vehicle;Fi[	@›ióI"w    # event, from state, and to state).
    # 
    # == Example
    # 
    #   machine = StateMachine.new(Vehicle);Fi[	I"assert(var.x86_64?.!);F@‚i[[	@ÊiI"Ä    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÊiI"Ä    assert(var.universal?.!)
    assert(var.i386?)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@Êi8I"Ä    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÊiFI"Ä    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÊiTI"Ä    assert(var.universal?.!)
    assert(var.i386?)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÊipI"Ç    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÊièI"Ç    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÊiüI"Ç    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÊiØI"Ç    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	I"%# @return [Array<Twitter::User>];F@Ói[[	I"4projects//twitter_proj/twitter/action/follow.rb;TiI"Õ      # A collection of users who followed a user
      #
      # @return [Array<Twitter::User>]
      def function
        @sources = Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	@iI"Õ      # A collection containing the followed user
      #
      # @return [Array<Twitter::User>]
      def function
        @targets = Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	I"?projects//twitter_proj/twitter/action/list_member_added.rb;TiI"‘      # A collection of users who added a user to a list
      #
      # @return [Array<Twitter::User>]
      def function
        @sources = Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	@iI"“      # A collection of users who were added to a list
      #
      # @return [Array<Twitter::User>]
      def function
        @targets = Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	I"5projects//twitter_proj/twitter/action/mention.rb;TiI"Œ      # A collection of users who mentioned a user
      #
      # @return [Array<Twitter::User>]
      def function
        @sources = Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	@i%I"Œ      # A collection containing the mentioned user
      #
      # @return [Array<Twitter::User>]
      def function
        @targets = Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	I"5projects//twitter_proj/twitter/action/retweet.rb;TiI"Œ      # A collection containing the retweeted user
      #
      # @return [Array<Twitter::User>]
      def function
        @targets = Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	I"3projects//twitter_proj/twitter/action/tweet.rb;TiI"¬      attr_reader("LITERAL", "LITERAL")
      
      # @return [Array<Twitter::User>]
      def function
        @sources = Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	@qiI"ö    end
    
    # @return [Array<Twitter::User>]
    def function
      @users ||= Array(@attrs["LITERAL"]).map { |var| Twitter::User.fetch_or_new(var) };Fi[	@˚@W
i[[	@RiµI"Ü      case extname
      when "STRING", "STRING", "STRING", "STRING" then
        "LITERAL"
      when "STRING" then
        "LITERAL";Fi[	@Ri∑I"l        "LITERAL"
      when "STRING" then
        "LITERAL"
      when "STRING" then
        "LITERAL";Fi[	@i~I"x        case var.to_s
        when "STRING" then
          "LITERAL"
        when "STRING" then
          "LITERAL";Fi[	@iÇI"w          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        when "STRING" then
          "LITERAL";Fi[	@iÑI"v          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "STRING" then
          "LITERAL";Fi[	@iÜI"v          "LITERAL"
        when "STRING" then
          "LITERAL"
        when "STRING" then
          "LITERAL";Fi[	@iåI"w          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        when "STRING" then
          "LITERAL";Fi[	@iêI"w          "LITERAL"
        when "LITERAL" then
          "LITERAL"
        when "STRING" then
          "LITERAL";Fi[	@1iTI"c      "LITERAL"
    when "LITERAL" then
      "LITERAL"
    when "STRING" then
      "LITERAL";Fi[	@{@–i[[	@icI"©    # 
    #   class Vehicle < ActiveRecord::Base
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling;Fi[	@iAI"î    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling;Fi[	@isI"î    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling;Fi[	@%iI"π        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling;Fi[	@%iXI"π        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling;Fi[	@%i|I"π        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do
        #         transition :parked => :idling;Fi[	@5iDI"ê    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition all => :idling;Fi[	@5içI"°    #     property :id, Serial
    #     
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling;Fi[	@›i˝I"î    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling;Fi[	@f@êi[[	@˛iI"≠      # Rackspace Cloud Files container name and path
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	@iI"®      # Path to where the backups will be stored
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	@ÙiI"ë      # use passive mode?
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	@˜iI"•      # Path where the backup will be stored.
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	@iI"ó      # Ninefold directory path
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	@iI"ô      # Flag to use local backups
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	@	iI"°      # Region of the specified S3 bucket
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	@´iI"ò      # Path to store backups to
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	@ÆiI"ò      # Path to store backups to
      ##
      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var);Fi[	I"%require("twitter/api/arguments");FI"!require("twitter/api/utils");Fi[[	@i I"drequire("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/direct_message");Fi [	@)i I"mrequire("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/error/already_favorited");Fi [	@0i I"\require("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/cursor");Fi [	@Vi I"irequire("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/core_ext/enumerable");Fi [	@i I"brequire("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/saved_search");Fi [	@éi I"`require("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/suggestion");Fi [	@ßi I"mrequire("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/error/already_retweeted");Fi [	@ºiI"~require("twitter/action_factory")
require("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/cursor");Fi[	@≈i I"irequire("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/core_ext/enumerable");Fi [	@ÓI"var = default_desc;Fi[[	@ÿiI"å      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.startTag) : (nil);Fi[	@ÿiI"ä      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.endTag) : (nil);Fi[	@ÿi&I"é      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.saveEndTag) : (nil);Fi[	@ÿi-I"à      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.depr) : (nil);Fi[	@ÿi4I"à      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.desc) : (nil);Fi[	@ÿi;I"ë      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.defaultsubelt) : (nil);Fi[	@ÿiBI"å      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.attrs_opt) : ([]);Fi[	@ÿiII"ç      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.attrs_depr) : ([]);Fi[	@ÿiPI"å      
      unless method_defined?("LITERAL") then
        def function
          var = default_desc
          var ? (var.attrs_req) : ([]);Fi[	@—I"# @param hash [Hash];Fi[[	@Vi=I"∆      
      # Take a list and merge it into the hash with the correct key
      #
      # @param hash [Hash]
      # @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.;Fi[	@ViQI"Œ      
      # Take an owner and merge it into the hash with the correct key
      #
      # @param hash [Hash]
      # @param user[Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.;Fi[	@?i|I"Õ      
      # Take a user and merge it into the hash with the correct key
      #
      # @param hash [Hash]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.;Fi[	@?iÖI"Õ      
      # Take a user and merge it into the hash with the correct key
      #
      # @param hash [Hash]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.;Fi[	@?iòI"      
      # Take a multiple users and merge them into the hash with the correct keys
      #
      # @param hash [Hash]
      # @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.;Fi[	@?i°I"      
      # Take a multiple users and merge them into the hash with the correct keys
      #
      # @param hash [Hash]
      # @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.;Fi[	I"1projects//twitter_proj/twitter/exceptable.rb;TiI"~    
    # Return a hash that includes everything but the given keys.
    #
    # @param hash [Hash]
    # @param key [Symbol];Fi[	@ïiI"r    
    # Replaces the hash without the given keys.
    #
    # @param hash [Hash]
    # @param key [Symbol];Fi[	@Ó@˚i[[	@i	I"r    module StandAlone
      module ConfigurationDefaults
        def function
          "LITERAL"
        end;Fi[	@#iI"D      end
      
      def function
        "LITERAL"
      end;Fi[	@#iI"D      end
      
      def function
        "LITERAL"
      end;Fi[	@#iëI"D      end
      
      def function
        "LITERAL"
      end;Fi[	@#iïI"D      end
      
      def function
        "LITERAL"
      end;Fi[	@Íi"I"D      end
      
      def function
        "LITERAL"
      end;Fi[	@Íi*I"D      end
      
      def function
        "LITERAL"
      end;Fi[	I"2projects//haml_proj/haml/sass_rails_filter.rb;TiI"b      self.default_mime_type = "STRING"
      
      def function
        "LITERAL"
      end;Fi[	@i@ri[[	@’i)I"ü            if Keg.new(var).linked?.! then
              if ARGV.dry_run? then
                puts("STRING")
              else
                puts("STRING");Fi[	@’iFI"ÿ        if ((var and (var.version > var)) or ((ARGV.switch?("STRING") and (var and var.installed?.!)) or var)) then
          if ARGV.dry_run? then
            puts("STRING")
          else
            puts("STRING");Fi[	I"'projects//homebrew_proj/cmd/diy.rb;TiI"™    var = ((HOMEBREW_CELLAR / var) / var)
    if File.file?("STRING") then
      puts("STRING")
    else
      File.file?("STRING") ? (puts("STRING")) : (raise("STRING"));Fi[	@¨iI"a      puts("STRING")
      puts("STRING")
      puts("STRING")
    else
      ohai("STRING");Fi[	I",projects//homebrew_proj/cmd/outdated.rb;TiI"µ      if $stdout.tty? and ARGV.flag?("STRING").! then
        var = var.rack.subdirs.map { |var| Keg.new(var).version }.sort
        puts("STRING")
      else
        puts(var.name);Fi[	@!i'I"    Homebrew.link_tap_formula(var.new_tapped_formula)
    if var.empty? then
      puts("STRING")
    else
      puts("STRING");Fi[	@$i@I"l      ohai("STRING")
      if @tarball_path.exist? then
        puts("STRING")
      else
        begin;Fi[	@çibI"g    if ARGV.verbose?.! then
      puts
      puts("STRING")
    else
      require("cmd/--config");Fi[	@0@ri[[	@3i#I"œ    if var.present? and @conversation.save then
      Postzord::Dispatcher.build(current_user, @conversation).post
      flash["LITERAL"] = I18n.t("STRING")
    else
      flash["LITERAL"] = I18n.t("STRING");Fi[	@ii+I"ú    end
    if current_user.update_profile(@profile_attrs) then
      flash["LITERAL"] = I18n.t("STRING")
    else
      flash["LITERAL"] = I18n.t("STRING");Fi[	@@i#I"æ        Resque.enqueue(Jobs::FetchProfilePhoto, current_user.id, var.id, var["STRING"])
      end
      flash["LITERAL"] = I18n.t("STRING")
    else
      flash["LITERAL"] = I18n.t("STRING");Fi[	@Ci"I"–      if var["LITERAL"] then
        @user.update_user_preferences(var["LITERAL"])
        flash["LITERAL"] = I18n.t("STRING")
      else
        if var["LITERAL"] and (var["LITERAL"] and var["LITERAL"]) then;Fi[	@Ci'I"∑          if @user.update_with_password(var) then
            var = true
            flash["LITERAL"] = I18n.t("STRING")
          else
            flash["LITERAL"] = I18n.t("STRING");Fi[	@Ci.I"⁄          if (var["LITERAL"] or var["LITERAL"]) then
            if @user.update_attributes(var) then
              flash["LITERAL"] = I18n.t("STRING")
            else
              flash["LITERAL"] = I18n.t("STRING");Fi[	@Ci6I"⁄              if @user.update_attributes(var) then
                I18n.locale = @user.language
                flash["LITERAL"] = I18n.t("STRING")
              else
                flash["LITERAL"] = I18n.t("STRING");Fi[	@CiFI"‰                if var["LITERAL"] then
                  if @user.update_attributes(var) then
                    flash["LITERAL"] = I18n.t("STRING")
                  else
                    flash["LITERAL"] = I18n.t("STRING");Fi[	@eI"I# Removes the transferred archive file(s) from the storage location.;Fi[[	@˛i9I"ú      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling;Fi[	@i[I"ú      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling;Fi[	@ÙiCI"ú      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling;Fi[	@˜i$I"ú      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling;Fi[	@iAI"ú      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling;Fi[	@	i<I"ú      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling;Fi[	@´i9I"ú      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling;Fi[	@Æi7I"ú      end
      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling;Fi[	@r@ii[[	@ÒiBI"|    if email_like_identifer then
      EmailInviter.new(self.identifier, sender).send!
    else
      puts("STRING")
    end;Fi[	@’i*I"è              if ARGV.dry_run? then
                puts("STRING")
              else
                puts("STRING")
                rm_rf(var);Fi[	@’iGI"}          if ARGV.dry_run? then
            puts("STRING")
          else
            puts("STRING")
            rm(var);Fi[	@&iI"U          puts("STRING")
        end
      else
        puts("STRING")
      end;Fi[	@&iaI"t          puts("STRING") unless var.used_options.empty?
        end
      else
        puts("STRING")
      end;Fi[	I"(projects//homebrew_proj/cmd/list.rb;TiiI"R    when "LITERAL" then
      puts(var)
    else
      puts("STRING")
    end;Fi[	@!i(I"]    if var.empty? then
      puts("STRING")
    else
      puts("STRING")
      var.dump;Fi[	@ºi)I"b      when "LITERAL" then
        var = var.first
      else
        puts("STRING")
      end;Fi[	@õ@Ïi[[	@jiI"Î  FONT_TYPES = { "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => "STRING" }
  
  def function(var)
    var = []
    var = var.length;Fi[	@ñiûI"Ø    
    # Output the original webkit gradient syntax
    def function(var)
      var = []
      (var << grad_point((position_or_angle or Sass::Script::String.new("STRING"))));Fi[	@ÚiKI";  end
  
  def function(var)
    var = []
    var = [];Fi[	@RiI"b  OLD_BOTTLE_EXTNAME_RX = "LITERAL"
  
  def function(var)
    var = []
    var.each do |var|;Fi[	@Ri;I"n  
  # Creates symlinks to sources in this folder.
  def function(var)
    var = []
    var.each do |var|;Fi[	@µinI"≥    # Scans s and returns a list of json tokens,
    # excluding white space (as defined in RFC 4627).
    def function(var)
      var = []
      while (var.length > "LITERAL") do;Fi[	I"1projects//nokogiri_proj/nokogiri/css/node.rb;TiBI"É      
      # Find a node by type using +types+
      def function(var)
        var = []
        (var << self) if (to_type == var);Fi[	@«i"I"º    # Returns an Array of string pids of all the other workers on this
    # machine. Useful when pruning dead workers on startup.
    def function(var)
      var = []
      var = `#{var}`;Fi[	I"require("rubygems");FI"require("sequel");Fi[[	@2i I"?require("rubygems")
require("sequel")
require("fileutils");Fi [	I"3projects//jekyll_proj/jekyll/migrators/enki.rb;TiI"Mrequire("fileutils")
require("rubygems")
require("sequel")
module Jekyll;Fi[	@9i I"?require("rubygems")
require("sequel")
require("fileutils");Fi [	@@i I"?require("rubygems")
require("sequel")
require("fastercsv");Fi [	@∫i I"?require("rubygems")
require("sequel")
require("fileutils");Fi [	@Mi I"?require("rubygems")
require("sequel")
require("fileutils");Fi [	I"3projects//jekyll_proj/jekyll/migrators/typo.rb;TiI"Orequire("fileutils")
require("rubygems")
require("sequel")
require("yaml");Fi[	@Ti I"?require("rubygems")
require("sequel")
require("fileutils");Fi [	@ÓI"!var = Pathname.new("STRING");Fi[[	@Êi
I"~end
class MachOPathnameTests < Test::Unit::TestCase
  def function
    var = Pathname.new("STRING")
    assert(var.universal?);Fi[	@ÊiI"Z  end
  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!);Fi[	@Êi&I"Z  end
  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!);Fi[	@Êi4I"X  end
  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?);Fi[	@ÊiBI"X  end
  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?);Fi[	@ÊiPI"Z  end
  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!);Fi[	@Êi^I"Z  end
  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!);Fi[	@ÊilI"Z  end
  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!);Fi[	@õI"var = remote_path_for(var);Fi[[	@˛i=I"Á      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        transferred_files_for(var) do |var, var|;Fi[	@i_I"«      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        var = [];Fi[	@ÙiGI"“      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        connection do |var|;Fi[	@˜i(I"«      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        var = [];Fi[	@iEI"ﬂ      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        if var = directory_for(var) then;Fi[	@	i@I"‘      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        connection.sync_clock;Fi[	@´i=I"«      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        var = [];Fi[	@Æi;I"“      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        connection do |var|;Fi[	@ÓI"$var = remote_path_for(@package);Fi[[	@˛i/I"ƒ      ##
      # Transfers the archived file to the specified Cloud Files container
      def function
        var = remote_path_for(@package)
        files_to_transfer_for(@package) do |var, var|;Fi[	@iQI"Ω      ##
      # Transfers the archived file to the specified Dropbox folder
      def function
        var = remote_path_for(@package)
        files_to_transfer_for(@package) do |var, var|;Fi[	@Ùi8I"¢      ##
      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package)
        connection do |var|;Fi[	@˜iI"ú      ##
      # Transfers the archived file to the specified path
      def function
        var = remote_path_for(@package)
        FileUtils.mkdir_p(var);Fi[	@i6I"©      ##
      # Transfers the archived file to the specified directory
      def function
        var = remote_path_for(@package)
        var = directory_for(var, true);Fi[	@	i1I"ß      ##
      # Transfers the archived file to the specified Amazon S3 bucket
      def function
        var = remote_path_for(@package)
        connection.sync_clock;Fi[	@´i.I"¢      ##
      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package)
        connection do |var|;Fi[	@Æi,I"¢      ##
      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package)
        connection do |var|;Fi[	@„I"assert(var.dylib?.!);Fi[[	@Êi:I"â    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?);Fi[	@ÊiHI"ã    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@ÊiVI"ã    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@ÊidI"â    assert(var.x86_64?)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@ÊirI"ã    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@ÊiëI"ã    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@Êi°I"ã    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@Êi±I"ã    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!);Fi[	@4@Íi[[	@	i&I"g  def function(var, var)
    return var if var
    case var
    when "LITERAL" then
      "STRING";Fi[	@{i7I"ô      var = "LITERAL"
      @to_merge.each do |var, var, var|
        case var
        when "LITERAL" then
          (var << inspect_obj(var)["LITERAL"]);Fi[	@!i~I"z  def function(var)
    fetch(var, []).map do |var|
      case var
      when "LITERAL" then
        Pathname.new($1);Fi[	@!iåI"Å  def function(var)
    fetch(var, []).map do |var|
      case var
      when "LITERAL" then
        File.basename(var, "STRING");Fi[	@$iˆI"f  
  def self.detect_from_url(var)
    case var
    when "LITERAL" then
      CVSDownloadStrategy;Fi[	@$iI"l  
  def self.detect_from_symbol(var)
    case var
    when "LITERAL" then
      BazaarDownloadStrategy;Fi[	@Ri£I"    var = nil
    File.open(self) { |var| var = var.read("LITERAL") }
    case var
    when "LITERAL" then
      "LITERAL";Fi[	@°iI"c      
      def function(var)
        case var
        when "LITERAL" then
          "STRING";Fi[	I"%assert(var.mach_o_executable?.!);FI"#assert(var.text_executable?.!);Fi[[	@ÊiI"õ    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert((var.arch == "LITERAL"));Fi[	@Êi I"ò    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!);Fi[	@Êi.I"ò    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!);Fi[	@ÊiJI"ò    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?);Fi[	@ÊiXI"ò    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?);Fi[	@ÊifI"ò    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?);Fi[	@ÊitI"ö    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!);Fi[	@Êi≥I"ô    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert_equal([], var.archs);Fi[	@îI"module Syncer;Fi[[	I"0projects//backup_proj/backup/syncer/base.rb;Ti I"1module Backup
  module Syncer
    class Base;Fi [	@&
iI"wBackup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup
  module Syncer
    module Cloud;Fi[	@i I"3module Backup
  module Syncer
    module Cloud;Fi [	@i I"3module Backup
  module Syncer
    module Cloud;Fi [	@i I"3module Backup
  module Syncer
    module RSync;Fi [	@•i I"3module Backup
  module Syncer
    module RSync;Fi [	@i I"3module Backup
  module Syncer
    module RSync;Fi [	@i I"3module Backup
  module Syncer
    module RSync;Fi [	@õ@Ôi[[	@Ìi}I"Ö        ##
        # Returns the url for the specified room (in JSON format)
        def function(var)
          "STRING"
        end;Fi[	I"/projects//compass_proj/compass/compiler.rb;Ti5I">    end
    
    def function(var)
      "STRING"
    end;Fi[	I".projects//diaspora_proj/models/profile.rb;Ti∆I"4  end
  
  def function(var)
    "STRING"
  end;Fi[	I"(projects//haml_proj/haml/filters.rb;Ti∏I"W      
      # @see Base#render
      def function(var)
        "STRING"
      end;Fi[	@Ñi-I"M  (var.bottle.revision + "LITERAL")
end
def function(var)
  "STRING"
end;Fi[	@*iZI">    end
    
    def function(var)
      "STRING"
    end;Fi[	@_iI">    end
    
    def function(var)
      "STRING"
    end;Fi[	@ÜiyI"H      end
      
      def function(var)
        "STRING"
      end;Fi[	@”I"6# Any error raised will be rescued during Cycling;Fi[[	@˛i:I"÷      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@i\I"÷      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@ÙiDI"÷      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@˜i%I"÷      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@iBI"÷      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@	i=I"÷      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@´i:I"÷      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@Æi8I"÷      
      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.;Fi[	@ΩI"B# and a warning will be logged, containing the error message.;Fi[[	@˛i;I"Á      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var);Fi[	@i]I"Á      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var);Fi[	@ÙiEI"Á      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var);Fi[	@˜i&I"Á      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var);Fi[	@iCI"Á      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var);Fi[	@	i>I"Á      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var);Fi[	@´i;I"Á      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var);Fi[	@Æi9I"Á      ##
      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var);Fi[	@–@õi[[	@˛i<I"      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var);Fi[	@i^I"      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var);Fi[	@ÙiFI"      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var);Fi[	@˜i'I"      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var);Fi[	@iDI"      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var);Fi[	@	i?I"      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var);Fi[	@´i<I"      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var);Fi[	@Æi:I"      # Removes the transferred archive file(s) from the storage location.
      # Any error raised will be rescued during Cycling
      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var);Fi[	I"require("testing_env");FI"require("test/testball");Fi[[	@Ùi I"Arequire("testing_env")
require("test/testball")
module MacOS;Fi [	I"0projects//homebrew_proj/test/test_bucket.rb;Ti I"Prequire("testing_env")
require("test/testball")
class MockFormula < Formula;Fi [	I"3projects//homebrew_proj/test/test_checksums.rb;Ti I"_require("testing_env")
require("test/testball")
class ChecksumTests < Test::Unit::TestCase;Fi [	@6i I"_require("testing_env")
require("test/testball")
class CompilerTests < Test::Unit::TestCase;Fi [	@ni I"Srequire("testing_env")
require("test/testball")
class AbstractDownloadStrategy;Fi [	I"-projects//homebrew_proj/test/test_keg.rb;Ti I"Crequire("testing_env")
require("test/testball")
require("keg");Fi [	I"1projects//homebrew_proj/test/test_patches.rb;Ti I"Crequire("testing_env")
require("test/testball")
require("set");Fi [	@ìi I"Vrequire("testing_env")
require("test/testball")
class DefaultPatchBall < TestBall;Fi [	@õ@ái[[	@irI"|    end
    
    def function(var)
      var = "STRING"
      var = Compass::Frameworks::ALL.inject("LITERAL") do |var, var|;Fi[	@ñi\I"x    end
    
    def function(var)
      var = "STRING"
      ((var << position.to_s(var)) << "STRING") if position;Fi[	@ñiàI"Ö    end
    
    def function(var)
      var = "STRING"
      ((var << position_or_angle.to_s(var)) << "STRING") if position_or_angle;Fi[	@ñi‘I"D    end
    
    def function(var)
      var = "STRING"
    end;Fi[	I"Uprojects//compass_proj/compass/sass_extensions/monkey_patches/browser_support.rb;Ti(I"D    end
    
    def function(var)
      var = "STRING"
    end;Fi[	@~i¸I"∑    # @param text [String] The string to sanitize
    # @return [String] The sanitized string
    def function(var)
      var = "STRING"
      var = if (RUBY_VERSION >= "STRING") then;Fi[	@~iI"∑    # @param text [String] The string to sanitize
    # @return [String] The sanitized string
    def function(var)
      var = "STRING"
      var = if (RUBY_VERSION >= "STRING") then;Fi[	@{itI"o    #
    # Returns the formatted String.
    def function(var)
      var = "STRING"
      case var.length;Fi[	I"# Example:;F@—i[[	I"6projects//compass_proj/compass/browser_support.rb;TiI"…    # No browsers implement this yet.
    # Adds support for one or more aspects for the given simple function
    # Example:
    #
    #   Compass::BrowserSupport.add_support("image", "moz", "webkit");Fi[	@#i&I"®    
    # Removes support for one or more aspects for the given simple function
    # Example:
    #
    #   Compass::BrowserSupport.remove_support("image", "o", "ms");Fi[	@Âi{I"ß      # the given scope. Useful for giving redirect backs after sign up:
      #
      # Example:
      #
      #   redirect_to stored_location_for(:user) || root_path;Fi[	@ÍiîI"Â        # Overwrite to add customized conditions, create a join, or maybe use a
        # namedscope to filter records while authenticating.
        # Example:
        #
        #   def self.find_for_authentication(conditions={});Fi[	@1
iuI"ƒ      # Confirm_within is a model configuration, must always be an integer value.
      #
      # Example:
      #
      #   # allow_unconfirmed_access_for = 1.day and confirmation_sent_at = today;Fi[	@ÌiBI"ô      # attributes you would not like to be updated without a password.
      #
      # Example:
      #
      #   def update_without_password(params={});Fi[	@—
i&I"∆      # reset_password_within is a model configuration, must always be an integer value.
      #
      # Example:
      #
      #   # reset_password_within = 1.day and reset_password_sent_at = today;Fi[	@ iPI"z  # Returns the number of jobs destroyed.
  #
  # Example:
  #
  #   # Removes all jobs of class `UpdateNetworkGraph`;Fi[	I"{# @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;FI"|# @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[[	@iI"      # @return [Array<Twitter::DirectMessage>] Direct messages sent to the authenticating user.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@i*I"      # @return [Array<Twitter::DirectMessage>] Direct messages sent by the authenticating user.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@ëiI"‚      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@ëiHI"      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@ëi_I"‚      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@ëitI"‚      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@ëiÜI"‚      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@ëiúI"u      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@5I"w# @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[[	@iI"      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Integer] :page Specifies the page of results to retrieve.;Fi[	@i+I"      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Integer] :page Specifies the page of results to retrieve.;Fi[	@ëiI"u      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ëiII"u      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ëi`I"u      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ëiuI"u      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ëiáI"u      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	I"# @return [Hash];F@äi[[	I"4projects//diaspora_proj/models/user/querying.rb;Ti~I"å  protected
  
  # @return [Hash]
  def function(var, var)
    var = { "LITERAL" => "STRING", "LITERAL" => "LITERAL", "LITERAL" => (false) };Fi[	@Vi@I"∏      # @param hash [Hash]
      # @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      # @return [Hash]
      def function(var, var)
        case var;Fi[	@ViTI"’      # @param hash [Hash]
      # @param user[Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.
      # @return [Hash]
      def function(var, var)
        merge_user!(var, var, "STRING");Fi[	@?iõI"      # @param hash [Hash]
      # @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.
      # @return [Hash]
      def function(var, var)
        merge_users!(var.dup, var);Fi[	@?i§I"      # @param hash [Hash]
      # @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.
      # @return [Hash]
      def function(var, var)
        var, var = [], [];Fi[	@ïiI"~    # @param hash [Hash]
    # @param key [Symbol]
    # @return [Hash]
    def function(var, var)
      except!(var.dup, var);Fi[	@ïi"I"}    # @param hash [Hash]
    # @param key [Symbol]
    # @return [Hash]
    def function(var, var)
      var.delete(var);Fi[	@[@Äi[[	@ÊiII"î    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	@ÊiWI"î    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	@ÊieI"î    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	@ÊisI"î    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	@ÊiíI"í    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?);Fi[	@Êi¢I"í    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?);Fi[	@Êi≤I"î    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	@ÓI""STRING".undent;Fi[[	@+ibI"6  end
  
  def function
    "STRING".undent
  end;Fi[	@[iI"V  satisfy { quiet_system(*the_test) }
  
  def function
    "STRING".undent
  end;Fi[	@[iYI"6  end
  
  def function
    "STRING".undent
  end;Fi[	@[iØI"l  satisfy("LITERAL" => (false)) { MacOS::Xcode.installed? }
  
  def function
    "STRING".undent
  end;Fi[	@[i∫I"N  satisfy { which("STRING") }
  
  def function
    "STRING".undent
  end;Fi[	@[i≈I"N  satisfy { which("STRING") }
  
  def function
    "STRING".undent
  end;Fi[	@[i–I"c  satisfy { (which("STRING") or which("STRING")) }
  
  def function
    "STRING".undent
  end;Fi[	@îI"module Notifier;Fi[[	I"2projects//backup_proj/backup/notifier/base.rb;Ti I"3module Backup
  module Notifier
    class Base;Fi [	@ÌiI"`Backup::Dependency.load("STRING")
module Backup
  module Notifier
    class Campfire < Base;Fi[	@›iI"_Backup::Dependency.load("STRING")
module Backup
  module Notifier
    class Hipchat < Base;Fi[	@‡iI"\Backup::Dependency.load("STRING")
module Backup
  module Notifier
    class Mail < Base;Fi[	@ÙiI"]Backup::Dependency.load("STRING")
module Backup
  module Notifier
    class Prowl < Base;Fi[	@˚iI"Srequire("net/https")
module Backup
  module Notifier
    class Pushover < Base;Fi[	@iI"_Backup::Dependency.load("STRING")
module Backup
  module Notifier
    class Twitter < Base;Fi[	@—I"# @param attrs [Hash];Fi[[	@Ci7I"f    
    # Initializes a new object
    #
    # @param attrs [Hash]
    # @return [Twitter::Base];Fi[	@CiNI"p    
    # Update the attributes of an object
    #
    # @param attrs [Hash]
    # @return [Twitter::Base];Fi[	I"-projects//twitter_proj/twitter/cursor.rb;TiI"°    
    # Initializes a new Cursor
    #
    # @param attrs [Hash]
    # @param collection_name [String, Symbol] The name of the method to return the collection;Fi[	@ iI"õ    
    # Initializes a new object
    #
    # @param attrs [Hash]
    # @raise [ArgumentError] Error raised when supplied argument is missing an :id key.;Fi[	@ºi*I"w    
    # Update the attributes of a RateLimit
    #
    # @param attrs [Hash]
    # @return [Twitter::RateLimit];Fi[	@fi	I"â  class Relationship < Twitter::Base
    # Initializes a new object
    #
    # @param attrs [Hash]
    # @return [Twitter::Relationship];Fi[	@fiI"}    
    # Update the attributes of a Relationship
    #
    # @param attrs [Hash]
    # @return [Twitter::Relationship];Fi[	I"u# +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.;F@—i[[	@µiI"I      ###
      # Add +node_or_tags+ as a child of this Node.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).;Fi[	@µiI"      ###
      # Add +node_or_tags+ as a child of this Node.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns self, to support chaining of calls (e.g., root << child1 << child2);Fi[	@µiI"T      ###
      # Insert +node_or_tags+ before this Node (as a sibling).
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).;Fi[	@µiI"S      ###
      # Insert +node_or_tags+ after this Node (as a sibling).
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).;Fi[	@µi'I"¸      ####
      # Insert +node_or_tags+ before this node (as a sibling).
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns self, to support chaining of calls.;Fi[	@µi]I"E      ####
      # Replace this Node with +node_or_tags+.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).;Fi[	@µiuI"Á      ####
      # Swap this Node for +node_or_tags+
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns self, to support chaining of calls.;Fi[	I"module Resque;FI"module Failure;Fi[[	I"5projects//rescue_proj/resque/failure/airbrake.rb;TiI"fend
require("resque/failure/thoughtbot")
module Resque
  module Failure
    class Airbrake < Base;Fi[	@Î
i I"2module Resque
  module Failure
    class Base;Fi [	I"4projects//rescue_proj/resque/failure/hoptoad.rb;TiI"eend
require("resque/failure/thoughtbot")
module Resque
  module Failure
    class Hoptoad < Base;Fi[	@æi I"=module Resque
  module Failure
    class Multiple < Base;Fi [	I"2projects//rescue_proj/resque/failure/redis.rb;TiI"Jrequire("time")
module Resque
  module Failure
    class Redis < Base;Fi[	I"7projects//rescue_proj/resque/failure/thoughtbot.rb;Ti I"9module Resque
  module Failure
    module Thoughtbot;Fi [	I",projects//rescue_proj/resque/failure.rb;Ti I"<module Resque
  module Failure
    def self.create(var);Fi [	I"# @return [Boolean];F@Ói[[	@>iLI"y    # Overridden in haml/template.rb if this is the case.
    #
    # @return [Boolean]
    def function
      false;Fi[	I"3projects//twitter_proj/twitter/configurable.rb;Ti!I"X    end
    
    # @return [Boolean]
    def function
      credentials.values.all?;Fi[	@•i>I"l    alias "LITERAL" "LITERAL"
    
    # @return [Boolean]
    def function
      previous_cursor.zero?;Fi[	@•iEI"h    alias "LITERAL" "LITERAL"
    
    # @return [Boolean]
    def function
      next_cursor.zero?;Fi[	@ÃiI"Ä    def_delegators("LITERAL", "LITERAL", "LITERAL")
    
    # @return [Boolean]
    def function
      @attrs["LITERAL"].nil?.!;Fi[	@ÃieI"p    alias "LITERAL" "LITERAL"
    
    # @return [Boolean]
    def function
      in_reply_to_status_id.!.!;Fi[	@ÃijI"U    end
    
    # @return [Boolean]
    def function
      retweeted_status.!.!;Fi[	I";# @param options [Hash] A customizable set of options.;F@4i[[	@iI"      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::DirectMessage>] Direct messages sent to the authenticating user.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@i)I"      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::DirectMessage>] Direct messages sent by the authenticating user.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@ëiI"“      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@ëiGI"…      # @return [Array<Twitter::Tweet>]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@ëi^I"“      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@ëisI"“      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@ëiÖI"“      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	I"*require("twitter/error/client_error");F@(i[[	@5i I"Grequire("twitter/error/client_error")
module Twitter
  class Error;Fi [	@@i I"Grequire("twitter/error/client_error")
module Twitter
  class Error;Fi [	@Li I"Grequire("twitter/error/client_error")
module Twitter
  class Error;Fi [	@Oi I"Grequire("twitter/error/client_error")
module Twitter
  class Error;Fi [	@Xi I"Grequire("twitter/error/client_error")
module Twitter
  class Error;Fi [	@[i I"Grequire("twitter/error/client_error")
module Twitter
  class Error;Fi [	@^i I"Grequire("twitter/error/client_error")
module Twitter
  class Error;Fi [	@r@ái[[	@i0I"∏          if var["LITERAL"] then
            log_action("LITERAL", basename(var), var)
          else
            var = "STRING"
            raise(Compass::FilesystemConflict.new(var));Fi[	@i:I"á      if var then
        FileUtils.touch(var) unless var["LITERAL"]
      else
        var = "STRING"
        (var << "STRING") if var;Fi[	@Çi=I"Ø            if var = @data.raw(var) then
              "STRING"
            else
              var = "STRING"
              if var = @data.instance_variable_get("STRING") then;Fi[	@Ai&I"≤    if (var > "LITERAL") then
      link_to("STRING", post_comments_path(var, "LITERAL" => "STRING"), "LITERAL" => "STRING")
    else
      var = "STRING"
      (var << "STRING");Fi[	@ÈihI"Ñ    if self.unprocessed_image.url.match("LITERAL") then
      var = self.unprocessed_image.url
    else
      var = "STRING"
    end;Fi[	@‡iúI"`      when "STRING" then
        var = "STRING"
      else
        var = "STRING"
      end;Fi[	@«iI"Ö          var = "STRING"
          var = [dump_exception(var)]
        else
          var = "STRING"
          var = pretty(var, var);Fi[	@áI")var.extend(StringInreplaceExtension);Fi[[	@ûi	I"¢class InreplaceTest < Test::Unit::TestCase
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@ûiI"  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@ûiI"  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@ûiI"  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@ûi%I"  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING");Fi[	@ûi,I"u  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.remove_make_var!("STRING");Fi[	@ûi3I"|  
  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.remove_make_var!(["STRING", "STRING"]);Fi[	I" require("twitter/identity");F@(i[[	I"1projects//twitter_proj/twitter/basic_user.rb;Ti I"Urequire("twitter/identity")
module Twitter
  class BasicUser < Twitter::Identity;Fi [	I"5projects//twitter_proj/twitter/direct_message.rb;TiI"vrequire("twitter/creatable")
require("twitter/identity")
module Twitter
  class DirectMessage < Twitter::Identity;Fi[	I"+projects//twitter_proj/twitter/list.rb;TiI"mrequire("twitter/creatable")
require("twitter/identity")
module Twitter
  class List < Twitter::Identity;Fi[	I"2projects//twitter_proj/twitter/media/photo.rb;Ti I">require("twitter/identity")
module Twitter
  module Media;Fi [	@πi I"Qrequire("twitter/identity")
module Twitter
  class Place < Twitter::Identity;Fi [	I"3projects//twitter_proj/twitter/saved_search.rb;TiI"trequire("twitter/creatable")
require("twitter/identity")
module Twitter
  class SavedSearch < Twitter::Identity;Fi[	@ÃiI"árequire("twitter/creatable")
require("twitter/exceptable")
require("twitter/identity")
module Twitter
  class Tweet < Twitter::Identity;Fi[	@ÓI"var = TestBall.new;Fi[[	@˛iI"b  end
  
  def function
    var = TestBall.new
    var.stable.instance_eval { md5("STRING") };Fi[	@˛iI"b  end
  
  def function
    var = TestBall.new
    var.stable.instance_eval { md5("STRING") };Fi[	@˛iI"c  end
  
  def function
    var = TestBall.new
    var.stable.instance_eval { sha1("STRING") };Fi[	@˛i"I"c  end
  
  def function
    var = TestBall.new
    var.stable.instance_eval { sha1("STRING") };Fi[	@˛i(I"e  end
  
  def function
    var = TestBall.new
    var.stable.instance_eval { sha256("STRING") };Fi[	@˛i.I"e  end
  
  def function
    var = TestBall.new
    var.stable.instance_eval { sha256("STRING") };Fi[	I"9projects//homebrew_proj/test/test_formula_install.rb;Ti)I"f  end
  
  def function
    var = TestBall.new
    assert_equal(Formula.path(var.name), var.path);Fi[	@ÿ@õi[[	@Ci`I"ß    
    # @param other [Twitter::Base]
    # @return [Boolean]
    def function(var)
      (self.class == var.class) and (var.attrs.empty?.! and (attrs == var.attrs));Fi[	@WiI"ñ    
    # @param other [Twitter::Geo]
    # @return [Boolean]
    def function(var)
      (super or (attr_equal("LITERAL", var) or attrs_equal(var)));Fi[	@ i!I"õ    
    # @param other [Twitter::Identity]
    # @return [Boolean]
    def function(var)
      (super or (attr_equal("LITERAL", var) or attrs_equal(var)));Fi[	@niI"å    
    # @param other [Twitter::Size]
    # @return [Boolean]
    def function(var)
      (super or (size_equal(var) or attrs_equal(var)));Fi[	@niI"ø    
    # @param other [Twitter::Size]
    # @return [Boolean]
    def function(var)
      (self.class == var.class) and (var.h.nil?.! and ((h == var.h) and (var.w.nil?.! and (w == var.w))));Fi[	@qiI"ù    
    # @param other [Twitter::Suggestion]
    # @return [Boolean]
    def function(var)
      (super or (attr_equal("LITERAL", var) or attrs_equal(var)));Fi[	@tiI"ò    
    # @param other [Twitter::Trend]
    # @return [Boolean]
    def function(var)
      (super or (attr_equal("LITERAL", var) or attrs_equal(var)));Fi[	I"require("yaml");FI"module Jekyll;Fi[[	@2iI"Yrequire("sequel")
require("fileutils")
require("yaml")
module Jekyll
  module Drupal;Fi[	@9iI"Yrequire("sequel")
require("fileutils")
require("yaml")
module Jekyll
  module Joomla;Fi[	@∫iI"Urequire("sequel")
require("fileutils")
require("yaml")
module Jekyll
  module MT;Fi[	I"2projects//jekyll_proj/jekyll/migrators/rss.rb;Ti
I"_require("open-uri")
require("fileutils")
require("yaml")
module Jekyll
  module MigrateRSS;Fi[	@MiI"^require("sequel")
require("fileutils")
require("yaml")
module Jekyll
  module TextPattern;Fi[	@iI"Vrequire("rubygems")
require("sequel")
require("yaml")
module Jekyll
  module Typo;Fi[	@TiI"\require("sequel")
require("fileutils")
require("yaml")
module Jekyll
  module WordPress;Fi[	I"require("fileutils");F@ei[[	@2iI"]require("rubygems")
require("sequel")
require("fileutils")
require("yaml")
module Jekyll;Fi[	@9iI"]require("rubygems")
require("sequel")
require("fileutils")
require("yaml")
module Jekyll;Fi[	@∫iI"]require("rubygems")
require("sequel")
require("fileutils")
require("yaml")
module Jekyll;Fi[	@oi	I"^require("rss/2.0")
require("open-uri")
require("fileutils")
require("yaml")
module Jekyll;Fi[	@MiI"]require("rubygems")
require("sequel")
require("fileutils")
require("yaml")
module Jekyll;Fi[	@TiI"]require("rubygems")
require("sequel")
require("fileutils")
require("yaml")
module Jekyll;Fi[	@ÊiI"`require("rubygems")
require("hpricot")
require("fileutils")
require("yaml")
require("time");Fi[	I"module MultiJson;FI"module Adapters;Fi[[	I"Fprojects//homebrew_proj/vendor/multi_json/adapters/json_common.rb;Ti I"=module MultiJson
  module Adapters
    module JsonCommon;Fi [	I"Cprojects//homebrew_proj/vendor/multi_json/adapters/json_gem.rb;TiI"Ürequire("json") unless defined? ::JSON
require("multi_json/adapters/json_common")
module MultiJson
  module Adapters
    class JsonGem;Fi[	I"Dprojects//homebrew_proj/vendor/multi_json/adapters/json_pure.rb;TiI"årequire("json/pure") unless defined? ::JSON
require("multi_json/adapters/json_common")
module MultiJson
  module Adapters
    class JsonPure;Fi[	@∞iI"ôframework("STRING")
require("multi_json/adapters/ok_json")
module MultiJson
  module Adapters
    class Nsjsonserialization < MultiJson::Adapters::OkJson;Fi[	I"=projects//homebrew_proj/vendor/multi_json/adapters/oj.rb;TiI"Wrequire("oj") unless defined? ::Oj
module MultiJson
  module Adapters
    class Oj;Fi[	@IiI"crequire("vendor/multi_json/vendor/okjson")
module MultiJson
  module Adapters
    class OkJson;Fi[	I"?projects//homebrew_proj/vendor/multi_json/adapters/yajl.rb;TiI"]require("yajl") unless defined? ::Yajl
module MultiJson
  module Adapters
    class Yajl;Fi[	@ﬂI"7#     state_machine :state, :initial => :parked do;Fi[[	@5ilI"É    # For example,
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       event :ignite do;Fi[	@5iùI"É    # For example,
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       event :ignite do;Fi[	@5iÿI"{    # For example,
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...;Fi[	@5iÊI"¨    # In the following example, a custom +initialize+ method is defined:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...;Fi[	@5i˜I"w    # like so:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...;Fi[	@5iI"ï    # to initialize the state machine attributes:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...;Fi[	@5i I"©    # static states regardless of their current value:
    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       state nil, :idling;Fi[	I".# to perform the database dumping process;F@Ói[[	@ÚiÜI"º      ##
      # Builds the MongoDB credentials syntax to authenticate the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|;Fi[	@ÚiêI"¿      ##
      # Builds the MongoDB connectivity options syntax to connect the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|;Fi[	@
iZI"∫      ##
      # Builds the credentials MySQL syntax to authenticate the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|;Fi[	@
idI"»      ##
      # Builds the MySQL connectivity options syntax to connect the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING", "STRING"].map do |var|;Fi[	@
iYI"Ã      ##
      # Builds the credentials PostgreSQL syntax to authenticate the user
      # to perform the database dumping process
      def function
        username.to_s.empty? ? ("STRING") : ("STRING");Fi[	@ıi]I"«      ##
      # Builds the Redis credentials syntax to authenticate the user
      # to perform the database dumping process
      def function
        password.to_s.empty? ? ("STRING") : ("STRING");Fi[	@ıidI"»      ##
      # Builds the Redis connectivity options syntax to connect the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING", "STRING"].map do |var|;Fi[	@ÍI"SubversionDownloadStrategy;Fi[[	@$iI"à    when "LITERAL" then
      MercurialDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then;Fi[	@$iI"â    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then;Fi[	@$i	I"à    when "LITERAL" then
      MercurialDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then;Fi[	@$iI"â    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then;Fi[	@$iI"â    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then;Fi[	@$iI"π    when "LITERAL" then
      CurlApacheMirrorDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when bottle_native_regex, bottle_regex, old_bottle_regex then;Fi[	@$i*I"}    when "LITERAL" then
      CurlPostDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    else;Fi[	@á@ái[[	I"6projects//compass_proj/compass/sprite_importer.rb;TiI"à    
    def self.find_all_sprite_map_files(var)
      var = "STRING"
      var = "STRING"
      Dir.glob(File.join(var, "STRING", var));Fi[	@xi…I"c      var = "STRING"
      if var then
        var = "STRING"
        var = "STRING"
      end;Fi[	@{iEI"|      var = var.keys if (Hash == var)
      (var.map do |var|
        var = "STRING"
        var = "STRING"
        "STRING";Fi[	@{iUI"ˆ      ((var << block_given?.!) << @options["LITERAL"])
      var = (@options["LITERAL"] and (var["LITERAL"] or (var["LITERAL"] or var["LITERAL"])).!)
      var = "STRING"
      var = "STRING"
      push_merged_text("STRING") unless var["LITERAL"];Fi[	@&i<I"i    else
      var.parent.cd { var = github_fork }
      var = "STRING"
      var = "STRING"
    end;Fi[	@iáI"Ø    if (@old_compiler == ENV.compiler) then
      var = Compiler.new(ENV.compiler)
      var = "STRING"
      var = "STRING"
      var = (var + "STRING") unless var.cause.nil?;Fi[	@@iI"k  module Mephisto
    def self.postgres(var)
      var = "STRING"
      var = "STRING"
      puts(var);Fi[	@—@Õi[[	@)iI""      # @overload favorites(user, options={})
      #   Returns the 20 most recent favorite Tweets for the specified user
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@0iI"0      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@0i7I"/      # @overload follower_ids(user, options={})
      #   Returns an array of numeric IDs for every user following the specified user
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@0iI"0      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@0i*I"0      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@ºi@I")      # @overload following_followers_of(user, options={})
      #   Returns users following followers of the authenticated user
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@≈iI"	      # @overload user(user, options={})
      #   Returns extended information for a given user
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@—@ˇi[[	@)iI":      # @overload favorites(options={})
      #   Returns the 20 most recent favorite Tweets for the authenticating user
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.;Fi[	@0iI"       # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@0i0I"ü      # @overload follower_ids(options={})
      #   Returns an array of numeric IDs for every user following the authenticated user
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@0iI"       # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@0i!I"       # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@ºi7I"ì      # @overload following_followers_of(options={})
      #   Returns users following followers of the specified user
      #
      #   @param options [Hash] A customizable set of options.
      #     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@≈iI"&      # @overload user(options={})
      #   Returns extended information for the authenticated user
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :skip_status Do not include user's Tweets when set to true, 't' or 1.;Fi[	I"+File.open("STRING", "STRING") do |var|;FI"var.puts(var);Fi[[	@2i#I"°          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING");Fi[	@9iI"°          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING");Fi[	@@i"I"°          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING");Fi[	@∫iI"°          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING");Fi[	I"8projects//jekyll_proj/jekyll/migrators/posterous.rb;Ti/I"´            (var.nil? or (var == "STRING"))
          end.to_yaml
          File.open("STRING", "STRING") do |var|
            var.puts(var)
            var.puts("STRING");Fi[	@MiI"°          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING");Fi[	@Ti]I"ó        (var.nil? or (var == "STRING"))
      end.to_yaml
      File.open("STRING", "STRING") do |var|
        var.puts(var)
        var.puts("STRING");Fi[	@—@i[[	@iI"      
      # A collection of users who followed a user
      #
      # @return [Array<Twitter::User>]
      def function;Fi[	@iI"      
      # A collection containing the followed user
      #
      # @return [Array<Twitter::User>]
      def function;Fi[	@iI"Å      
      # A collection of users who added a user to a list
      #
      # @return [Array<Twitter::User>]
      def function;Fi[	@iI"      
      # A collection of users who were added to a list
      #
      # @return [Array<Twitter::User>]
      def function;Fi[	@iI"{      
      # A collection of users who mentioned a user
      #
      # @return [Array<Twitter::User>]
      def function;Fi[	@i$I"{      
      # A collection containing the mentioned user
      #
      # @return [Array<Twitter::User>]
      def function;Fi[	@iI"{      
      # A collection containing the retweeted user
      #
      # @return [Array<Twitter::User>]
      def function;Fi[	@ÓI"with_temp_folder do;Fi[[	I":projects//homebrew_proj/test/test_pathname_install.rb;TiI"Z  end
  
  def function
    with_temp_folder do
      TARGET_FOLDER.install("STRING");Fi[	@*i"I"f  end
  
  def function
    with_temp_folder do
      TARGET_FOLDER.install(["STRING", "STRING"]);Fi[	@*i*I"_  end
  
  def function
    with_temp_folder do
      TARGET_FOLDER.install(Dir["STRING"]);Fi[	@*i2I"V  end
  
  def function
    with_temp_folder do
      FileUtils.mkdir_p("STRING");Fi[	@*i<I"f  end
  
  def function
    with_temp_folder do
      TARGET_FOLDER.install("STRING" => "STRING");Fi[	@*iEI"|  end
  
  def function
    with_temp_folder do
      TARGET_FOLDER.install("STRING" => "STRING", "STRING" => "STRING");Fi[	@*iOI"V  end
  
  def function
    with_temp_folder do
      FileUtils.mkdir_p("STRING");Fi[	I"end.to_yaml;F@i[[	@2i"I"Ó        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var);Fi[	@9iI"*        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var["LITERAL"]), "STRING" => (var["LITERAL"]), "STRING" => (var) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var);Fi[	@@i!I"˘        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var["LITERAL"]) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var);Fi[	@∫iI"        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var["LITERAL"]), "STRING" => (var) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var);Fi[	@i.I"¯          var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |var, var|
            (var.nil? or (var == "STRING"))
          end.to_yaml
          File.open("STRING", "STRING") do |var|
            var.puts(var);Fi[	@MiI"	        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var["LITERAL"].split("STRING")) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var);Fi[	@Ti\I"¬      var = { "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => ((var["LITERAL"].to_s == "STRING")), "STRING" => (var.to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var), "STRING" => (var), "STRING" => (var["LITERAL"]), "STRING" => (var["LITERAL"].to_s), "STRING" => (var), "STRING" => (var["LITERAL"] ? (var) : (nil)), "STRING" => (var["LITERAL"] ? (var) : (nil)), "STRING" => (var["LITERAL"] ? (var) : (nil)) }.delete_if do |var, var|
        (var.nil? or (var == "STRING"))
      end.to_yaml
      File.open("STRING", "STRING") do |var|
        var.puts(var);Fi[	I"assert(var.i386?.!);F@i[[	@ÊiI"à    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@Êi7I"à    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@ÊiEI"à    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@ÊioI"ä    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@ÊiéI"Å    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@ÊiûI"Å    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@ÊiÆI"Å    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@’I"module YARD;Fi[[	@§i I":module StateMachine
  module YARD
    module Handlers;Fi [	I"Fprojects//state_machine_proj/state_machine/yard/handlers/event.rb;Ti I":module StateMachine
  module YARD
    module Handlers;Fi [	@èiI"Nrequire("tempfile")
module StateMachine
  module YARD
    module Handlers;Fi[	I"Fprojects//state_machine_proj/state_machine/yard/handlers/state.rb;Ti I":module StateMachine
  module YARD
    module Handlers;Fi [	@8i I":module StateMachine
  module YARD
    module Handlers;Fi [	I"@projects//state_machine_proj/state_machine/yard/handlers.rb;Ti I":module StateMachine
  module YARD
    module Handlers;Fi [	I"7projects//state_machine_proj/state_machine/yard.rb;Ti I",module StateMachine
  module YARD
  end;Fi [	I"$(var.nil? or (var == "STRING"));F@9i[[	@2i!I"        var = ((var.strftime("STRING") + var) + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|;Fi[	@9iI"Q        var = ("STRING" % [var.year, var.month, var.day, var])
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var["LITERAL"]), "STRING" => (var["LITERAL"]), "STRING" => (var) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|;Fi[	@@i I"/        var = ([var.year, var.month, var.day, var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var["LITERAL"]) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|;Fi[	@∫iI"W        var = (([var.year, var.month, var.day, var].join("STRING") + "STRING") + self.suffix(var))
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var["LITERAL"]), "STRING" => (var) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|;Fi[	@i-I"          var = ("STRING" % [var.year, var.month, var.day, var])
          var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |var, var|
            (var.nil? or (var == "STRING"))
          end.to_yaml
          File.open("STRING", "STRING") do |var|;Fi[	@MiI"9        var = ([var.strftime("STRING"), var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var["LITERAL"].split("STRING")) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |var|;Fi[	@Ti[I"∂      end
      var = { "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => ((var["LITERAL"].to_s == "STRING")), "STRING" => (var.to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var), "STRING" => (var), "STRING" => (var["LITERAL"]), "STRING" => (var["LITERAL"].to_s), "STRING" => (var), "STRING" => (var["LITERAL"] ? (var) : (nil)), "STRING" => (var["LITERAL"] ? (var) : (nil)), "STRING" => (var["LITERAL"] ? (var) : (nil)) }.delete_if do |var, var|
        (var.nil? or (var == "STRING"))
      end.to_yaml
      File.open("STRING", "STRING") do |var|;Fi[	@W
I"var.print("STRING");Fi[[	@µiëI"ã      while (var < var.length) do
        case var[var]
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@µiìI"å        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@µiïI"å        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@µióI"å        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@µiôI"å        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@µiõI"å        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then;Fi[	@µiùI"~        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        else;Fi[	@(I"module Action;Fi[[	I"6projects//twitter_proj/twitter/action/favorite.rb;TiI"orequire("twitter/action/tweet")
module Twitter
  module Action
    class Favorite < Twitter::Action::Tweet;Fi[	@iI"yrequire("twitter/base")
require("twitter/creatable")
module Twitter
  module Action
    class Follow < Twitter::Base;Fi[	@iI"}require("twitter/base")
require("twitter/creatable")
module Twitter
  module Action
    class ListMemberAdded < Twitter::Base;Fi[	@iI"zrequire("twitter/base")
require("twitter/creatable")
module Twitter
  module Action
    class Mention < Twitter::Base;Fi[	I"3projects//twitter_proj/twitter/action/reply.rb;TiI"lrequire("twitter/action/tweet")
module Twitter
  module Action
    class Reply < Twitter::Action::Tweet;Fi[	@iI"nrequire("twitter/action/tweet")
module Twitter
  module Action
    class Retweet < Twitter::Action::Tweet;Fi[	@iI"xrequire("twitter/base")
require("twitter/creatable")
module Twitter
  module Action
    class Tweet < Twitter::Base;Fi[	I"var.puts("STRING");F@i[[	@2i%I"á        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end;Fi[	@9iI"á        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end;Fi[	@@i$I"á        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end;Fi[	@∫iI"á        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end;Fi[	@i1I"ë          File.open("STRING", "STRING") do |var|
            var.puts(var)
            var.puts("STRING")
            var.puts(var)
          end;Fi[	@MiI"á        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end;Fi[	@Ti_I"}      File.open("STRING", "STRING") do |var|
        var.puts(var)
        var.puts("STRING")
        var.puts(var)
      end;Fi[	@ÄI"# @param args [Array];Fi[[	@ViI"ç      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>];Fi[	@ßi I"é      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::Tweet>];Fi[	@?iI"ç      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>];Fi[	@?iI"ç      
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>];Fi[	@?i(I"î      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array];Fi[	@?iDI"î      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array];Fi[	@?i[I"†      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @param method_name [Symbol];Fi[	I"require("formula");FI"module Homebrew;Fi[[	@i I"6require("formula")
module Homebrew
  extend(self);Fi [	@§i I"6require("formula")
module Homebrew
  extend(self);Fi [	@i I"6require("formula")
module Homebrew
  extend(self);Fi [	I"&projects//homebrew_proj/cmd/sh.rb;TiI"Jrequire("superenv")
require("formula")
module Homebrew
  extend(self);Fi[	@CiI"Erequire("keg")
require("formula")
module Homebrew
  extend(self);Fi[	I"(projects//homebrew_proj/cmd/uses.rb;Ti I"6require("formula")
module Homebrew
  extend(self);Fi [	@i I"6require("formula")
module Homebrew
  extend(self);Fi [	@ÓI"if ARGV.named.empty? then;Fi[[	I"+projects//homebrew_proj/cmd/--cache.rb;TiI"^  extend(self)
  
  def function
    if ARGV.named.empty? then
      puts(HOMEBREW_CACHE);Fi[	I",projects//homebrew_proj/cmd/--cellar.rb;TiI"_  extend(self)
  
  def function
    if ARGV.named.empty? then
      puts(HOMEBREW_CELLAR);Fi[	I",projects//homebrew_proj/cmd/--prefix.rb;TiI"_  extend(self)
  
  def function
    if ARGV.named.empty? then
      puts(HOMEBREW_PREFIX);Fi[	@’iI"l  extend(self)
  
  def function
    if ARGV.named.empty? then
      if HOMEBREW_CELLAR.directory? then;Fi[	I"(projects//homebrew_proj/cmd/home.rb;TiI"d  extend(self)
  
  def function
    if ARGV.named.empty? then
      exec_browser(HOMEBREW_WWW);Fi[	@&iI"`  end
  
  def function
    if ARGV.named.empty? then
      if ARGV.include?("STRING") then;Fi[	I"'projects//homebrew_proj/cmd/log.rb;TiI"a  extend(self)
  
  def function
    if ARGV.named.empty? then
      cd(HOMEBREW_REPOSITORY);Fi[	@rI"[];Fi[[	@i#I"W            File.basename(var)
          end
        else
          []
        end;Fi[	I"&projects//compass_proj/compass.rb;TiI"ï      if ENV["STRING"] and File.directory?(ENV["STRING"]) then
        [File.join(ENV["STRING"], "STRING", "STRING")]
      else
        []
      end;Fi[	I"Gprojects//diaspora_proj/helpers/interim_stream_hackiness_helper.rb;Ti#I"}    if defined? @stream and (params["LITERAL"] == "STRING") then
      @stream.post_from_group(var)
    else
      []
    end;Fi[	@µißI"}      if (var["LITERAL", "LITERAL"] == "STRING") then
        ["LITERAL", "STRING", nil]
      else
        []
      end;Fi[	@µiØI"~      if (var["LITERAL", "LITERAL"] == "STRING") then
        ["LITERAL", "STRING", true]
      else
        []
      end;Fi[	@µi∑I"      if (var["LITERAL", "LITERAL"] == "STRING") then
        ["LITERAL", "STRING", false]
      else
        []
      end;Fi[	@µi»I">          end
        end
      else
        []
      end;Fi[	@@•i[[	@2i$I"è        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var);Fi[	@9iI"è        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var);Fi[	@@i#I"è        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var);Fi[	@∫iI"è        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var);Fi[	@i0I"ô          end.to_yaml
          File.open("STRING", "STRING") do |var|
            var.puts(var)
            var.puts("STRING")
            var.puts(var);Fi[	@MiI"è        end.to_yaml
        File.open("STRING", "STRING") do |var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var);Fi[	@Ti^I"Ö      end.to_yaml
      File.open("STRING", "STRING") do |var|
        var.puts(var)
        var.puts("STRING")
        var.puts(var);Fi[	@rI"var.pos -= "LITERAL";Fi[[	I"Bprojects//rescue_proj/resque/vendor/utf8_util/utf8_util_18.rb;Ti I"v      if check_next_sequence(var) then
        return "LITERAL"
      else
        var.pos -= "LITERAL"
      end;Fi[	@i(I"Ö          if check_next_sequence(var) then
            return "LITERAL"
          else
            var.pos -= "LITERAL"
          end;Fi[	@i+I"k            var.pos -= "LITERAL"
          end
        else
          var.pos -= "LITERAL"
        end;Fi[	@i4I"ô              if check_next_sequence(var) then
                return "LITERAL"
              else
                var.pos -= "LITERAL"
              end;Fi[	@i7I"                var.pos -= "LITERAL"
              end
            else
              var.pos -= "LITERAL"
            end;Fi[	@i:I"u              var.pos -= "LITERAL"
            end
          else
            var.pos -= "LITERAL"
          end;Fi[	I"H# : Notification will be sent, including the Exception which caused;FI"F# : the failure, the Exception's backtrace, a copy of the current;Fi[[	@Ìi+I"      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@›iCI"      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@‡i}I"      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@Ùi)I"      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@˚i3I"      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@i'I"      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`;Fi[	@%I"K# : backup log and other information if `on_failure` was set to `true`;Fi[[	@Ìi,I"      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@›iDI"      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@‡i~I"      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@Ùi*I"      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@˚i4I"      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@i(I"      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #;Fi[	@ÓI"'if (action_hook == "LITERAL") then;Fi[[	@F
igI"          (::ActiveRecord::VERSION::MAJOR == "LITERAL") and (::ActiveRecord::VERSION::MINOR == "LITERAL")
        end
        def function
          if (action_hook == "LITERAL") then
            owner_class.set_callback("LITERAL", "LITERAL", "STRING", "LITERAL" => (true));Fi[	@Âi;I"‚      
      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true));Fi[	@¶
iOI"˘          defined? ::MongoMapper::Version and ::MongoMapper::Version =~ "LITERAL"
        end
        def function
          if (action_hook == "LITERAL") then
            owner_class.set_callback("LITERAL", "LITERAL", "STRING", "LITERAL" => (true));Fi[	@Úi4I"‚      
      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true));Fi[	@±
iI"˜          define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"))
        end
        def function
          if (action_hook == "LITERAL") then
            owner_class.set_callback("LITERAL", "LITERAL", "STRING", "LITERAL" => (true));Fi[	@˜i8I"‚      
      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true));Fi[	@‚	@äi[[	@Â	iI"v    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(var, var)
      begin;Fi[	@Â	i>I"≈    # layouts - A Hash of {"name" => "layout"}.
    #
    # Returns nothing.
    def function(var, var)
      var = { "LITERAL" => ([Jekyll::Filters]), "LITERAL" => ({ "LITERAL" => (self.site) }) };Fi[	@ñi%I"{    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(var, var)
      super(var, var);Fi[	@ñihI"ø    # site_payload - The site payload Hash.
    #
    # Returns nothing.
    def function(var, var)
      var = { "STRING" => (self.to_liquid), "STRING" => (pager.to_liquid) }.deep_merge(var);Fi[	@ôiCI"{    # name - The String filename of the file.
    #
    # Returns nothing.
    def function(var, var)
      super(var, var);Fi[	@ôiªI"Ë    # site_payload - The site payload hash.
    #
    # Returns nothing.
    def function(var, var)
      var = { "STRING" => ({ "STRING" => (related_posts(var["STRING"]["STRING"])) }), "STRING" => (self.to_liquid) }.deep_merge(var);Fi[	@4@W
i[[	@ÖiCI"à    def function(var)
      var = var.value.split("LITERAL").map do |var|
        case var
        when "STRING" then
          "STRING";Fi[	@;iHI"±      var = false if @options["LITERAL"]
      var = ((var == "STRING") or (var.!=("STRING") and @options["LITERAL"]))
      case var
      when "STRING" then
        var = true;Fi[	@;i’I"µ      var = {}
      var.scan("LITERAL") do |var, var|
        case var
        when "STRING" then
          var["STRING"] ? (var["STRING"] += "STRING") : (var["STRING"] = "STRING");Fi[	@iI"Ω  
  PRUNEABLE_DIRECTORIES = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"].map do |var|
    case var
    when "STRING" then
      (HOMEBREW_LIBRARY / var);Fi[	@µi I"õ      raise(Error, "STRING") if (var.length < "LITERAL")
      var, var, var = var["LITERAL"]
      case var
      when "STRING" then
        objparse(var);Fi[	@µi/I"õ      raise(Error, "STRING") if (var.length < "LITERAL")
      var, var, var = var["LITERAL"]
      case var
      when "STRING" then
        objparse(var);Fi[	@õI"return unless var;Fi[[	I"8projects//compass_proj/compass/commands/registry.rb;TiI"\    end
    
    def function(var)
      return unless var
      @commands ||= Hash.new;Fi[	@dicI"n  end
  
  def function(var)
    return unless var
    reshares.where("LITERAL" => (var.person.id)).first;Fi[	@dihI"k  end
  
  def function(var)
    return unless var
    likes.where("LITERAL" => (var.person.id)).first;Fi[	I"(projects//haml_proj/haml/options.rb;Ti—I"ô      attr_writer("LITERAL")
    else
      def function(var)
        return unless var
        @encoding = var.is_a?(Encoding) ? (var.name) : (var.to_s);Fi[	@>iWI"ô    # @param text [String, nil]
    # @return [String, nil] `text`, marked as HTML-safe
    def function(var)
      return unless var
      var.html_safe;Fi[	@:iI"K    end
    
    def function(var)
      return unless var
      begin;Fi[	@4@—i[[	@Ìi-I"˛      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var);Fi[	@›iEI"˛      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var);Fi[	@‡iI"˛      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var);Fi[	@Ùi+I"˛      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var);Fi[	@˚i5I"˛      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var);Fi[	@i)I"˛      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var);Fi[	I"%# : The backup operation failed.;F@$i[[	@Ìi*I"”      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@›iBI"”      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@‡i|I"”      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@Ùi(I"”      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@˚i2I"”      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@i&I"”      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current;Fi[	@[I"module Handlers;Fi[[	@§iI"jmodule StateMachine
  module YARD
    module Handlers
      class Base < ::YARD::Handlers::Ruby::Base;Fi[	@`iI"Smodule StateMachine
  module YARD
    module Handlers
      class Event < Base;Fi[	@èiI"irequire("tempfile")
module StateMachine
  module YARD
    module Handlers
      class Machine < Base;Fi[	@eiI"Smodule StateMachine
  module YARD
    module Handlers
      class State < Base;Fi[	@8iI"Xmodule StateMachine
  module YARD
    module Handlers
      class Transition < Base;Fi[	@jiI"Bmodule StateMachine
  module YARD
    module Handlers
    end;Fi[	I"# `:failure`;F@çi[[	@Ìi)I"√      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@›iAI"√      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@‡i{I"√      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@Ùi'I"√      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@˚i1I"√      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@i%I"√      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	I"6# : backup log, if `on_warning` was set to `true`;F@—i[[	@Ìi'I"›      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@›i?I"›      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@‡i~I"›      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@Ùi%I"›      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@˚i/I"›      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	@i#I"›      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`;Fi[	I"&merge_list!(var.options, var.pop);FI"Cunless (var.options["LITERAL"] or var.options["LITERAL"]) then;Fi[[	@ViJI"ı      def function(var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then
          merge_owner!(var.options, (var.pop or screen_name));Fi[	@ViI"ˇ      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then
          merge_owner!(var.options, (var.pop or screen_name));Fi[	@ViI"      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then
          merge_owner!(var.options, (var.pop or screen_name));Fi[	@ViI"          (var = Twitter::API::Arguments.new(var)
          merge_user!(var.options, var.pop)
          merge_list!(var.options, var.pop)
          unless (var.options["LITERAL"] or var.options["LITERAL"]) then
            merge_owner!(var.options, (var.pop or screen_name));Fi[	@Vi)I"        var = Twitter::API::Arguments.new(var)
        merge_user!(var.options, var.pop)
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then
          merge_owner!(var.options, (var.pop or screen_name));Fi[	@Vi3I"Û        var = Twitter::API::Arguments.new(var)
        var = var.pop
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then
          merge_owner!(var.options, (var.pop or screen_name));Fi[	I"var = case var;F@Íi[[	@Ìi0I"j      #
      def function(var)
        var = case var
        when "LITERAL" then
          "STRING";Fi[	@Ùi.I"j      #
      def function(var)
        var = case var
        when "LITERAL" then
          "STRING";Fi[	@˚i8I"j      #
      def function(var)
        var = case var
        when "LITERAL" then
          "STRING";Fi[	@i,I"j      #
      def function(var)
        var = case var
        when "LITERAL" then
          "STRING";Fi[	@øiUI"õ      var = var["STRING"]
      var = (var["LITERAL"] or var.result)
      var = case var
      when "LITERAL" then
        var = (var.message or "STRING");Fi[	@iJI"^  
  def function(var)
    var = case var
    when "LITERAL" then
      (self / "STRING");Fi[	I"MockFormula.new("STRING");F@Ëi[[	@˚iI"ã  def function
    assert_nothing_raised do
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING");Fi[	@˚iI"ú    assert_nothing_raised do
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING");Fi[	@˚iI"ü      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING");Fi[	@˚iI"ü      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING");Fi[	@˚iI"ü      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING");Fi[	@˚i I"á      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
      MockFormula.new("STRING")
    end;Fi[	I"C# : Notification will be sent, including a copy of the current;F@∫i[[	@Ìi&I"›      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@›i>I"›      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@‡i}I"›      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@Ùi$I"›      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@˚i.I"›      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@i"I"›      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #;Fi[	@ê@ˇi[[	@ViI"Ω      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop);Fi[	@Vi&I"ã      end
      
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_user!(var.options, var.pop);Fi[	@Vi0I"|      end
      
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        var = var.pop;Fi[	@ßiI"æ      # @param args [Array]
      # @return [Array<Twitter::Tweet>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@?iI"Ω      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@?iI"⁄      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then;Fi[	@—I"5# dest - The String path to the destination dir.;Fi[[	@ñivI"i    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #;Fi[	@ñi|I"á    
    # Write the generated page file to the destination directory.
    #
    # dest - The String path to the destination dir.
    #;Fi[	@ôi¬I"i    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #;Fi[	@ôiÕI"á    
    # Write the generated post file to the destination directory.
    #
    # dest - The String path to the destination dir.
    #;Fi[	I"0projects//jekyll_proj/jekyll/static_file.rb;TiI"i    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #;Fi[	@i2I"ç    
    # Write the static file to the destination directory (if modified).
    #
    # dest - The String path to the destination dir.
    #;Fi[	I"$var = "LITERAL".match(var.to_s);F@≈i[[	@¶ioI"Õ      "LITERAL".match(var.to_s) ? (Pathname.new(var.dirname).stem) : (var.stem)
    end
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s);Fi[	@¶iqI"«    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s);Fi[	@¶isI"«    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s);Fi[	@¶iuI"«    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s);Fi[	@¶iwI"¬    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var);Fi[	@¶iäI"§    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
  end;Fi[	@W
I"E["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[[	@µiÉI"™    def function(var)
      case var["LITERAL"]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then;Fi[	@µiÖI"‹      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then;Fi[	@µiáI"‹      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then;Fi[	@µiâI"‹      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then;Fi[	@µiãI"‹      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then;Fi[	@µiçI"‹      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then;Fi[	I"var.putc(var);F@Bi[[	@µiÕI"z            raise(Utf8Error)
          end
          var.putc(var)
          var.putc(var)
          return "LITERAL";Fi[	@µi◊I"÷          var = (var.&(Umask3) << "LITERAL").|((var.&(Umaskx) << "LITERAL")).|(var.&(Umaskx))
          raise(Utf8Error) if (var <= Uchar2max)
          var.putc(var)
          var.putc(var)
          var.putc(var);Fi[	@µiÿI"ì          raise(Utf8Error) if (var <= Uchar2max)
          var.putc(var)
          var.putc(var)
          var.putc(var)
          return "LITERAL";Fi[	@µi‚I"ˆ          var = (var.&(Umask4) << "LITERAL").|((var.&(Umaskx) << "LITERAL")).|((var.&(Umaskx) << "LITERAL")).|(var.&(Umaskx))
          raise(Utf8Error) if (var <= Uchar3max)
          var.putc(var)
          var.putc(var)
          var.putc(var);Fi[	@µi„I"ê          raise(Utf8Error) if (var <= Uchar3max)
          var.putc(var)
          var.putc(var)
          var.putc(var)
          var.putc(var);Fi[	@µi‰I"          var.putc(var)
          var.putc(var)
          var.putc(var)
          var.putc(var)
          return "LITERAL";Fi[	@Å@W
i[[	@µiíI"á        case var[var]
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@µiîI"è          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@µiñI"è          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@µiòI"è          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@µiöI"è          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@µiúI"è          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@õI"+var = Devise::Mapping.find_scope!(var);Fi[[	@ÂiI"ﬂ      #   redirect_to stored_location_for(:user) || root_path
      #
      def function(var)
        var = Devise::Mapping.find_scope!(var)
        is_navigational_format? ? (session.delete("STRING")) : (session["STRING"]);Fi[	@ÂiÅI"˙      # The scope root url to be used when he's signed in. By default, it first
      # tries to find a resource_root_path, otherwise it uses the root_path.
      def function(var)
        var = Devise::Mapping.find_scope!(var)
        var = "STRING";Fi[	@Âi≈I"ÿ      # Sign out a user and tries to redirect to the url specified by
      # after_sign_out_path_for.
      def function(var)
        var = Devise::Mapping.find_scope!(var)
        var = after_sign_out_path_for(var);Fi[	I"=projects//devise_proj/devise/controllers/rememberable.rb;TiI"æ      
      # Remembers the given resource by setting up a cookie
      def function(var)
        var = Devise::Mapping.find_scope!(var)
        var.remember_me!(var.extend_remember_period);Fi[	@hiI"ú      
      # Forgets the given resource by deleting a cookie
      def function(var)
        var = Devise::Mapping.find_scope!(var)
        var.forget_me!;Fi[	@øi4I"π    #   sign_out @user     # sign_out(resource)
    #
    def function(var)
      var = Devise::Mapping.find_scope!(var)
      @controller.instance_variable_set(:"@current_#{var}", nil);Fi[	I"D# : The backup completed successfully, but warnings were logged;F@˜i[[	@Ìi%I"›      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@›i=I"›      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@‡i|I"›      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@Ùi#I"›      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@˚i-I"›      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@i!I"›      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	I"# `:warning`;F@oi[[	@Ìi$I"Î      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@›i<I"Î      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@‡i{I"Î      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@Ùi"I"Î      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@˚i,I"Î      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	@i I"Î      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current;Fi[	I"D# : Notification will be sent if `on_success` was set to `true`;F@—i[[	@Ìi"I"†      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@›i:I"†      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@‡iyI"†      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@Ùi I"†      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@˚i*I"†      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	@iI"†      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`;Fi[	I"+# : The backup completed successfully.;F@çi[[	@Ìi!I"ï      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@›i9I"ï      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@‡ixI"ï      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@ÙiI"ï      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@˚i)I"ï      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@iI"ï      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #;Fi[	@—I"# Examples;Fi[[	@{i3I"F    #
    # date - The Time to format.
    #
    # Examples
    #;Fi[	@{iBI"I    #
    # input - The String to escape.
    #
    # Examples
    #;Fi[	@{iQI"I    #
    # input - The String to escape.
    #
    # Examples
    #;Fi[	@{imI"Q    #
    # array - The Array of Strings to join.
    #
    # Examples
    #;Fi[	@biÕI"®    
    # Private: creates a regular expression from the keep_files array
    # 
    # Examples
    #   ['.git','.svn'] creates the following regex: /\/(\.git|\/.svn)/;Fi[	@bi‰I"^    #
    # post_attr - The String name of the Post attribute.
    #
    # Examples
    #;Fi[	@3@W
i[[	@µiÑI"›      case var["LITERAL"]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	@µiÜI"        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	@µiàI"        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	@µiäI"        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	@µiåI"        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	@µiéI"ÿ        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        nulltok(var);Fi[	I"# `:success`;F@úi[[	@Ìi I"æ      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@›i8I"æ      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@‡iwI"æ      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@ÙiI"æ      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@˚i(I"æ      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@iI"æ      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`;Fi[	@=@Ói[[	@ïiI"—      # A collection containing the favorited tweet
      #
      # @return [Array<Twitter::Tweet>]
      def function
        @targets = Array(@attrs["LITERAL"]).map { |var| Twitter::Tweet.fetch_or_new(var) };Fi[	@iI"◊      # A collection of tweets that mention a user
      #
      # @return [Array<Twitter::Tweet>]
      def function
        @target_objects = Array(@attrs["LITERAL"]).map { |var| Twitter::Tweet.fetch_or_new(var) };Fi[	@ûiI"ÿ      # A collection of tweets that reply to a user
      #
      # @return [Array<Twitter::Tweet>]
      def function
        @target_objects = Array(@attrs["LITERAL"]).map { |var| Twitter::Tweet.fetch_or_new(var) };Fi[	@ûiI"÷      # A collection that contains the replied-to tweets
      #
      # @return [Array<Twitter::Tweet>]
      def function
        @targets = Array(@attrs["LITERAL"]).map { |var| Twitter::Tweet.fetch_or_new(var) };Fi[	@iI"≈      # A collection of retweets
      #
      # @return [Array<Twitter::Tweet>]
      def function
        @target_objects = Array(@attrs["LITERAL"]).map { |var| Twitter::Tweet.fetch_or_new(var) };Fi[	@√iI"∆module Twitter
  class SearchResults < Twitter::Base
    # @return [Array<Twitter::Tweet>]
    def function
      @results ||= Array(@attrs["LITERAL"]).map { |var| Twitter::Tweet.fetch_or_new(var) };Fi[	I""var.gsub!("STRING", "STRING");F@Âi[[	@TigI"‡      var.force_encoding("STRING") if var.respond_to?("LITERAL")
      var = HTMLEntities.new.encode(var, "LITERAL")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING");Fi[	@TihI"√      var = HTMLEntities.new.encode(var, "LITERAL")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING");Fi[	@TiiI"≥      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING");Fi[	@TijI"≥      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING");Fi[	@TikI"ô      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var.gsub!("STRING", "STRING")
      var;Fi[	I"Tprojects//state_machine_proj/state_machine/integrations/active_record/locale.rb;TiI"œif (defined? I18n::VERSION.! or (I18n::VERSION < "STRING")) then
  var["LITERAL"]["LITERAL"]["LITERAL"]["LITERAL"].each do |var, var|
    var.gsub!("STRING", "STRING")
    var.gsub!("STRING", "STRING")
  end;Fi[	@á@ri[[	@{iöI"ì      else
        if var.empty? then
          var = "STRING"
        else
          (var.size == "LITERAL") ? (var = "STRING") : (var = "STRING");Fi[	@~i∫I"û        if (var.count("STRING") > "LITERAL") then
          var = var.gsub("STRING", "STRING")
          var = "STRING"
        else
          var = var.strip;Fi[	@>i$I"á    def function(var)
      if var.include?("STRING").! then
        var = "STRING"
      else
        if var.include?("STRING").! then;Fi[	@>i'I"{      else
        if var.include?("STRING").! then
          var = "STRING"
        else
          return var.inspect;Fi[	@ÚiUI"£        if block_given? and yield(var) then
          puts(var)
          var = "STRING"
        else
          (var << var) unless (var.basename.to_s == "STRING");Fi[	@‡iõI"m        var = "STRING"
      when "STRING" then
        var = "STRING"
      else
        var = "STRING";Fi[	@@—i[[	@ñiwI"è    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #
    # Returns the destination file path String.;Fi[	@ñi}I"ô    # Write the generated page file to the destination directory.
    #
    # dest - The String path to the destination dir.
    #
    # Returns nothing.;Fi[	@ôi√I"ã    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #
    # Returns destination file path String.;Fi[	@ôiŒI"ô    # Write the generated post file to the destination directory.
    #
    # dest - The String path to the destination dir.
    #
    # Returns nothing.;Fi[	@iI"Ñ    # Obtain destination path.
    #
    # dest - The String path to the destination dir.
    #
    # Returns destination file path.;Fi[	@i3I"“    # Write the static file to the destination directory (if modified).
    #
    # dest - The String path to the destination dir.
    #
    # Returns false if the file was not modified since last time (no-op).;Fi[	I"/# `status` indicates one of the following:;F@—i[[	@ÌiI"ç      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@›i6I"ç      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@‡iuI"ç      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@ÙiI"ç      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@˚i&I"ç      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@iI"ç      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@)@ri[[	@îiÏI"         if (var["LITERAL"] == "STRING") then
          var = ["STRING", "STRING", "STRING", "STRING"]
          var = Node.new("LITERAL", var)
        else
          if (var["LITERAL"] == "STRING") then;Fi[	@îiI"π          if (var["LITERAL"] == "STRING") then
            var = ["STRING", "STRING", "STRING", "STRING"]
            var = Node.new("LITERAL", var)
          else
            var = var;Fi[	@îi˙I"∞      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          var = Node.new("LITERAL", var)
        else
          raise(Racc::ParseError, "STRING");Fi[	@îiI"±        if (var["LITERAL"] == "STRING") then
          var.unshift("STRING")
          var = Node.new("LITERAL", var)
        else
          if (var["LITERAL"] == "STRING") then;Fi[	@îi	I"Ø            var["LITERAL"] = "STRING"
            var.unshift("STRING")
            var = Node.new("LITERAL", var)
          else
            raise(Racc::ParseError, "STRING");Fi[	@îiI"ô          (var << "STRING")
          (var << "STRING")
          var = Node.new("LITERAL", var)
        else
          raise(Racc::ParseError, "STRING");Fi[	I"7# Notify the user of the backup operation results.;F@i[[	@ÌiI"Å      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@›i5I"Å      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@‡itI"Å      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@ÙiI"Å      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@˚i%I"Å      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	@iI"Å      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #;Fi[	I"# @param klass [Class];F@i[[	@?i&I"v      end
      
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String];Fi[	@?i1I"v      end
      
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String];Fi[	@?iBI"v      end
      
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String];Fi[	@?iNI"v      end
      
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String];Fi[	@?iYI"è      
      # @param collection_name [Symbol]
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String];Fi[	@?ihI"è      
      # @param collection_name [Symbol]
      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String];Fi[	I"respond_to do |var|;FI"var.json do;Fi[[	@ıiI"ﬁ    @comment = current_user.comment!(var, params["LITERAL"]) if var
    if @comment then
      respond_to do |var|
        var.json do
          render("LITERAL" => (CommentPresenter.new(@comment)), "LITERAL" => "LITERAL");Fi[	I"Dprojects//diaspora_proj/controllers/notifications_controller.rb;TiI"Ã    if var then
      var.set_read_state(params["LITERAL"].!=("STRING"))
      respond_to do |var|
        var.json do
          render("LITERAL" => ({ "LITERAL" => (var.id), "LITERAL" => (var.unread) }));Fi[	@9i#I"À    var = params["LITERAL"] ? (params["LITERAL"].to_i) : ("LITERAL")
    @people = Person.search(search_query, current_user)
    respond_to do |var|
      var.json do
        @people = @people.limit(var);Fi[	@#i-I"        @photo = current_user.build_post("LITERAL", params["LITERAL"])
        if @photo.save then
          respond_to do |var|
            var.json do
              render("LITERAL" => ({ "STRING" => (true), "STRING" => (@photo.as_api_response("LITERAL")) }));Fi[	@#i®I"Ã        current_user.update_profile(var)
      end
      respond_to do |var|
        var.json do
          render("LITERAL" => (false), "LITERAL" => ({ "STRING" => (true), "STRING" => (@photo) }.to_json));Fi[	@iiI"∏  def function
    @person = Person.find_by_guid!(params["LITERAL"])
    respond_to do |var|
      var.json do
        render("LITERAL" => (PersonPresenter.new(@person, current_user)));Fi[	I"rescue LoadError;FI"STDERR.puts("STRING");Fi[[	@*i!I"ú            @redcarpet_extensions[var.to_sym] = true
          end)
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING");Fi[	@*i)I"Ñ        begin
          require("kramdown")
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING");Fi[	@*i2I"–          (require("rdiscount")
          @rdiscount_extensions = @config["STRING"]["STRING"].map { |var| var.to_sym })
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING");Fi[	@*iHI"¨            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"]
          end)
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING");Fi[	@_iI"Ö        require("redcloth")
        @setup = true)
      rescue LoadError
        STDERR.puts("STRING")
        STDERR.puts("STRING");Fi[	@TitI"}        (require("unidecode")
        var = var.to_ascii)
      rescue LoadError
        STDERR.puts("STRING")
      end;Fi[	@—@´i[[	@Ìi(I"æ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@›i@I"æ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@‡iI"æ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@Ùi&I"æ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@˚i0I"æ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@i$I"æ      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.;Fi[	@ãI"(instance_eval(&var) if block_given?;Fi[[	@¯iI"«      # Creates a new instance of the Riak adapter object
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?
        @riak_admin_utility ||= utility("STRING");Fi[	@ÌiI"à      # Campfire account's room id
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?
      end;Fi[	@‡imI"à      # Example: '/tmp/test-mails'
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?
      end;Fi[	@ÙiI"¨      # Create a Prowl account and request an API key on prowlapp.com.
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?
      end;Fi[	@˚iI"é      # The priority of the notification
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?
      end;Fi[	@iI"      # OAuth credentials
      def function(var, var)
        super(var)
        instance_eval(&var) if block_given?
      end;Fi[	I" sha1("STRING" => "LITERAL");F@äi[[	@%itI"s  
  bottle do
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL");Fi[	@%iuI"v  bottle do
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
  end;Fi[	@%i˛I"s  
  bottle do
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL");Fi[	@%iˇI"v  bottle do
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
  end;Fi[	@%iI"Ç  bottle do
    version("LITERAL")
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL");Fi[	@%iI"|    version("LITERAL")
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL")
  end;Fi[	@r@Õi[[	I".projects//diaspora_proj/models/contact.rb;TiaI"R      (errors["LITERAL"] << "STRING")
      false
    else
      true
    end;Fi[	I".projects//diaspora_proj/models/message.rb;TiaI"é    if self.parent and self.parent.participants.include?(self.author).! then
      (errors["LITERAL"] << "STRING")
    else
      true
    end;Fi[	@Èi?I"ë    if self.status_message_guid and self.status_message.text_and_photos_blank? then
      self.status_message.destroy
    else
      true
    end;Fi[	@ÈiHI"|    if self.status_message_guid and var then
      (self.diaspora_handle == var.diaspora_handle)
    else
      true
    end;Fi[	@‡inI"Z    when "LITERAL" then
      (MacOS.version < "LITERAL")
    else
      true
    end;Fi[	@›iXI"N        raise(var) if var
        var.!
      else
        true
      end;Fi[	@e@.i[[	@ÌiI"á      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	@›i4I"á      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	@‡isI"á      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	@ÙiI"á      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	@˚i$I"á      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	@iI"á      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:;Fi[	@—@~i[[	@Ìi#I"”      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@›i;I"”      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@‡izI"”      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@Ùi!I"”      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@˚i+I"”      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@iI"”      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged;Fi[	@—@»i[[	@ÌiI"±      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@›i7I"±      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@‡ivI"±      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@ÙiI"±      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@˚i'I"±      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@iI"±      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.;Fi[	@I"6# @return [Twitter::User] The authenticated user.;Fi[[	@≈i/I"%      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param options [Hash] A customizable set of options.;Fi[	@≈i@I"%      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param device [String] Must be one of: 'sms', 'none'.;Fi[	@≈iPI"$      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param options [Hash] A customizable set of options.;Fi[	@≈ibI"	      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param image [File] The background image for the profile, base64-encoded. Must be a valid GIF, JPG, or PNG image of less than 800 kilobytes in size. Images with width larger than 2048 pixels will be forcibly scaled down. The image must be provided as raw multipart data, not a URL.;Fi[	@≈irI"$      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param options [Hash] A customizable set of options.;Fi[	@≈iÇI"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param image [File] The avatar image for the profile, base64-encoded. Must be a valid GIF, JPG, or PNG image of less than 700 kilobytes in size. Images with width larger than 500 pixels will be scaled down. Animated GIFs will be converted to a static GIF of the first frame, removing the animation.;Fi[	@NI"module Installers;Fi[[	@i I"6module Compass
  module Installers
    class Base;Fi [	@i I"Fmodule Compass
  module Installers
    class BareInstaller < Base;Fi [	@*i I"6module Compass
  module Installers
    class Base;Fi [	@i I":module Compass
  module Installers
    class Manifest;Fi [	@åi I"Jmodule Compass
  module Installers
    class ManifestInstaller < Base;Fi [	I"Bprojects//compass_proj/compass/installers/template_context.rb;Ti I"Amodule Compass
  module Installers
    class TemplateContext;Fi [	I"!var.in_repo_expect("STRING");FI"+var.in_repo_expect("STRING", "STRING");Fi[[	@ëi3I"¨    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING");Fi[	@ëi6I"¬      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING");Fi[	@ëiEI"¨    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING");Fi[	@ëiHI"«      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var);Fi[	@ëiYI"¨    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING");Fi[	@ëi\I"«      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var);Fi[	@rI"$raise(Compass::Error, "STRING");Fi[[	@qiI"ù          if (var.size > "LITERAL") then
            raise(Compass::Error, "STRING")
          else
            raise(Compass::Error, "STRING")
          end;Fi[	@¨i,I"∂      if (var.index(from) == "LITERAL") then
        var[((from.length + "LITERAL").."LITERAL")].sub("LITERAL", "STRING")
      else
        raise(Compass::Error, "STRING")
      end;Fi[	@*i{I"ã          if respond_to?(var) then
            targetize("STRING")
          else
            raise(Compass::Error, "STRING")
          end;Fi[	@	i;I"b    when "LITERAL" then
      "STRING"
    else
      raise(Compass::Error, "STRING")
    end;Fi[	@	iCI"à    if File.readable?(var) then
      File.open(var, "STRING") { |var| var.read }
    else
      raise(Compass::Error, "STRING")
    end;Fi[	@’i?I"~      if var.=~(SPRITE_IMPORTER_REGEX) then
        [$1, $3]
      else
        raise(Compass::Error, "STRING")
      end;Fi[	I"	end);F@Ói[[	@iGI"ê              run((("STRING" + "STRING") + "STRING"))
            end
          end)
        ensure
          remove_password_file! unless local;Fi[	@iI"†              Logger.message("STRING")
              run((("STRING" + "STRING") + "STRING"))
            end)
          ensure
            remove_password_file!;Fi[	@{iI"é        else
          send(:"compile_#{var.type}") { var.children.each { |var| compile(var) } }
        end)
      ensure
        @node = var;Fi[	@Ki{I"ü      rescue ::SyntaxError => var
        raise(SyntaxError, var.message)
      end)
      ensure
        var.instance_eval { @haml_buffer = var.upper if var };Fi[	@~inI"â          end
          var.map { |var| var[(var.."LITERAL")] }.join
        end)
      ensure
        haml_buffer.capture_position = nil;Fi[	@hi¢I"¨        unless @failure_hooks_ran then
          failure_hooks.each { |var| payload_class.send(var, var, *var) }
        end)
      ensure
        @failure_hooks_ran = true;Fi[	I"assert(var.universal?.!);F@Ji[[	@Êi(I"|  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?);Fi[	@Êi`I"|  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?);Fi[	@ÊinI"~  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	@ÊiçI"ç    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	@ÊiùI"ç    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	@Êi≠I"ç    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	@@Íi[[	@yi!I"ﬁ      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.;Fi[	@yi4I"ﬁ      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.;Fi[	@yiNI"ﬁ      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.;Fi[	@yicI"2      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [String] :name The name a place is known as.;Fi[	@†i(I"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @return [Array<Twitter::Place>];Fi[	@†i8I"Í      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat If provided with a :long option the available trend locations will be sorted by distance, nearest to furthest, to the co-ordinate pair. The valid ranges for latitude are -90.0 to +90.0 (North is positive) inclusive.;Fi[	@îI"module Database;Fi[[	I"2projects//backup_proj/backup/database/base.rb;Ti I"3module Backup
  module Database
    class Base;Fi [	@Úi I"=module Backup
  module Database
    class MongoDB < Base;Fi [	@
i I";module Backup
  module Database
    class MySQL < Base;Fi [	@
i I"@module Backup
  module Database
    class PostgreSQL < Base;Fi [	@ıi I";module Backup
  module Database
    class Redis < Base;Fi [	@¯i I":module Backup
  module Database
    class Riak < Base;Fi [	I"var = var["STRING"];F@Mi[[	@@iI"˙    var = var["STRING"]["STRING"]
    var = var["STRING"]["STRING"]
    var = var["STRING"]
    var = var["STRING"]
    var = "STRING".constantize.new("LITERAL" => (var["STRING"]), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var["STRING"]));Fi[	@„i	I"µ    def self.process(var)
      var = Jekyll::Site.new(var)
      var = var["STRING"]
      var = var["STRING"]
      var["STRING"] ? (self.watch(var, var)) : (self.build(var, var));Fi[	@„iI"o    
    def self.build(var, var)
      var = var["STRING"]
      var = var["STRING"]
      puts("STRING");Fi[	@„i I"à    def self.watch(var, var)
      require("directory_watcher")
      var = var["STRING"]
      var = var["STRING"]
      puts("STRING");Fi[	@0i6I"Ç      case var["STRING"]
      when "STRING" then
        var = var["STRING"]
        var = var["STRING"]
      when "STRING" then;Fi[	@0iWI"∞        (var << "STRING")
      when "STRING" then
        var = var["STRING"]
        var = var["STRING"]
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?;Fi[	@rI"var = var;Fi[[	I"Jprojects//compass_proj/compass/sass_extensions/functions/selectors.rb;Ti9I"≤        var = Sass::Script::Number.new("LITERAL")
        var = Sass::Script::Number.new("LITERAL")
      else
        var = var
        var = Sass::Script::Number.new("LITERAL");Fi[	@{iéI"É            var.include?(var) ? (var = var.gsub(var, var)) : (var = var)
          end
        else
          var = var
        end;Fi[	@KinI"¶        var = eval("STRING", var)
        var = var.instance_eval { binding } if block_given?
      else
        var = var
        var = var.instance_eval { binding };Fi[	@KiôI"†      if (var.is_a?(Binding) or var.is_a?(Proc)) then
        var = eval("STRING", var)
      else
        var = var
        var = var.instance_eval { binding };Fi[	@îiÒI"ò            var = ["STRING", "STRING", "STRING", "STRING"]
            var = Node.new("LITERAL", var)
          else
            var = var
          end;Fi[	@ÕiI"“        if var and var.is_a?(String) then
          var = var and var.=~(REGEX) ? ([var]) : (var.split("STRING"))
        else
          var = var
          var = [var] unless var and var.respond_to?("LITERAL");Fi[	@rI"raise(Error, "STRING");Fi[[	@µi%I"g      when "STRING" then
        arrparse(var)
      else
        raise(Error, "STRING")
      end;Fi[	@µi6I"{      when "LITERAL", "LITERAL" then
        [var, var["LITERAL"]]
      else
        raise(Error, "STRING")
      end;Fi[	@µiI"É              var = (var + ucharenc(var, var, var))
            end
          else
            raise(Error, "STRING")
          end;Fi[	@µiKI"ú      when (("STRING" <= var) and (var <= "STRING")) then
        ((var.ord - "STRING".ord) + "LITERAL")
      else
        raise(Error, "STRING")
      end;Fi[	@µi_I"b      when Array then
        arrenc(var)
      else
        raise(Error, "STRING")
      end;Fi[	@µiÖI"c      when String then
        strenc(var)
      else
        raise(Error, "STRING")
      end;Fi[	@îI"module Compressor;Fi[[	I"4projects//backup_proj/backup/compressor/base.rb;Ti I"5module Backup
  module Compressor
    class Base;Fi [	I"5projects//backup_proj/backup/compressor/bzip2.rb;Ti I"=module Backup
  module Compressor
    class Bzip2 < Base;Fi [	I"6projects//backup_proj/backup/compressor/custom.rb;Ti I">module Backup
  module Compressor
    class Custom < Base;Fi [	I"4projects//backup_proj/backup/compressor/gzip.rb;Ti I"<module Backup
  module Compressor
    class Gzip < Base;Fi [	I"4projects//backup_proj/backup/compressor/lzma.rb;Ti I"<module Backup
  module Compressor
    class Lzma < Base;Fi [	I"6projects//backup_proj/backup/compressor/pbzip2.rb;Ti I">module Backup
  module Compressor
    class Pbzip2 < Base;Fi [	@ÓI"8raise(FormulaUnspecifiedError) if ARGV.named.empty?;Fi
[
[	I"'projects//homebrew_proj/cmd/cat.rb;TiI"y  extend(self)
  
  def function
    raise(FormulaUnspecifiedError) if ARGV.named.empty?
    cd(HOMEBREW_REPOSITORY);Fi[	@§i	I"|  extend(self)
  
  def function
    raise(FormulaUnspecifiedError) if ARGV.named.empty?
    if ARGV.include?("STRING") then;Fi[	@©iI"á  extend(self)
  
  def function
    raise(FormulaUnspecifiedError) if ARGV.named.empty?
    raise("STRING") if ARGV.include?("STRING");Fi[	I"(projects//homebrew_proj/cmd/test.rb;TiI"}  extend(self)
  
  def function
    raise(FormulaUnspecifiedError) if ARGV.named.empty?
    ENV.extend(HomebrewEnvExtension);Fi[	@÷i	I"  extend(self)
  
  def function
    raise(FormulaUnspecifiedError) if ARGV.named.empty?
    var = Formula.select do |var|;Fi[	I"var["LITERAL"];F@ri
[
[	@+
iVI"§        if var["LITERAL"] then
          if absolute_path?(var["LITERAL"]) then
            var["LITERAL"]
          else
            File.join(var, var["LITERAL"]);Fi[	@#i}I"Ç  def function(var)
    if request.params["LITERAL"].is_a?(String).! then
      var["LITERAL"]
    else
      var = var["LITERAL"];Fi[	@{i§I"æ        else
          ("STRING" + (if (if var["LITERAL"] and @options.html? then
            var["LITERAL"]
          else
            (block_given?.! or (var["LITERAL"] or var["LITERAL"]));Fi[	@œiI"        else
          if var["LITERAL"] then
            var["LITERAL"]
          else
            if var["LITERAL"] then;Fi[	@“iI"ì      var = File.read(var)
      var = if var["LITERAL"] then
        var["LITERAL"]
      else
        File.read(var["LITERAL"]) if var["LITERAL"];Fi[	@ÓI"$if ARGV.include?("STRING") then;Fi
[
[	@+iI"  
  # Create a formula from a tarball URL
  def function
    if ARGV.include?("STRING") then
      exec_browser("STRING");Fi[	@iI"u  extend(self)
  
  def function
    if ARGV.include?("STRING") then
      Formula.each { |var| puts("STRING") };Fi[	@¨iI"^  extend(self)
  
  def function
    if ARGV.include?("STRING") then
      puts("STRING");Fi[	I"+projects//homebrew_proj/cmd/options.rb;TiI"jrequire("formula")
require("cmd/outdated")
def function
  if ARGV.include?("STRING") then
    Formula;Fi[	@ci
I"f  extend(self)
  
  def function
    if ARGV.include?("STRING") then
      exec_browser("STRING");Fi[	I"!var = var.patches["LITERAL"];FI")assert_equal("LITERAL", var.patch_p);Fi
[
[	@iI"ü    var = Patches.new("STRING")
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p)
  end;Fi[	@iI"∆    var = Patches.new(["STRING", "STRING"])
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p)
    var = var.patches["LITERAL"];Fi[	@iI"ô    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p)
  end;Fi[	@i$I"¨    var = Patches.new("LITERAL" => "STRING")
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p)
  end;Fi[	@i+I"¨    var = Patches.new("LITERAL" => "STRING")
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p)
  end;Fi[	I"L# Skips defining reader/writer methods since this is done automatically;F@Ói
[
[	@A
iäI"â      end
      
      # Skips defining reader/writer methods since this is done automatically
      def function
        var = self.name;Fi[	@ÃiYI"ª      end
      
      # Skips defining reader/writer methods since this is done automatically
      def function
        owner_class.property(attribute, String) unless attribute_property;Fi[	@Úi-I"±      end
      
      # Skips defining reader/writer methods since this is done automatically
      def function
        owner_class.key(attribute, String) unless attribute_key;Fi[	@˜i1I"ƒ      end
      
      # Skips defining reader/writer methods since this is done automatically
      def function
        owner_class.field(attribute, "LITERAL" => (String)) unless attribute_field;Fi[	@M
iMI"â      end
      
      # Skips defining reader/writer methods since this is done automatically
      def function
        var = self.name;Fi[	I"8assert((TARGET_FOLDER + "STRING").exist?, "STRING");F@’i
[
[	@*i%I"≈    with_temp_folder do
      TARGET_FOLDER.install(["STRING", "STRING"])
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end;Fi[	@*i-I"æ    with_temp_folder do
      TARGET_FOLDER.install(Dir["STRING"])
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end;Fi[	@*i7I"∏      system("STRING")
      TARGET_FOLDER.install("STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end;Fi[	@*iHI"    with_temp_folder do
      TARGET_FOLDER.install("STRING" => "STRING", "STRING" => "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING");Fi[	@*iUI"È      TARGET_FOLDER.install("STRING" => "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
    end;Fi[	I"require("twitter/error");F@(i
[
[	@œi I":require("twitter/error")
module Twitter
  class Error;Fi [	@:i I":require("twitter/error")
module Twitter
  class Error;Fi [	@=i I":require("twitter/error")
module Twitter
  class Error;Fi [	@Fi I":require("twitter/error")
module Twitter
  class Error;Fi [	@Ri I":require("twitter/error")
module Twitter
  class Error;Fi [	I"%#         transition all => :off;F@◊i
[
[	@iQI"r    #       
    #       event :disable do
    #         transition all => :off
    #       end
    #     end;Fi[	@i~I"r    #       
    #       event :disable do
    #         transition all => :off
    #       end
    #     end;Fi[	@5iïI"ù    #     state_machine :alarm_state, :initial => :active do
    #       event :disable do
    #         transition all => :off
    #       end
    #     end;Fi[	@5i∆I"s    #       
    #       event :turn_off do
    #         transition all => :off
    #       end
    #     end;Fi[	@5i–I"s    #       
    #       event :turn_off do
    #         transition all => :off
    #       end
    #     end;Fi[	@eI"# Server credentials;Fi
[
[	@ÙiI"W      attr_accessor("LITERAL")
      
      ##
      # Server credentials
      ##;Fi[	@iI"W      attr_accessor("LITERAL")
      
      ##
      # Server credentials
      ##;Fi[	@´iI"W      attr_accessor("LITERAL")
      
      ##
      # Server credentials
      ##;Fi[	@ÆiI"W      attr_accessor("LITERAL")
      
      ##
      # Server credentials
      ##;Fi[	@iI"a        attr_accessor("LITERAL")
        
        ##
        # Server credentials
        ##;Fi[	I"6# Gets the db default for the machine's attribute;F@Ói
[
[	@Âi!I"’      end
      
      # Gets the db default for the machine's attribute
      def function
        if owner_class.connected? and (owner_class.table_exists? and var = owner_class.columns_hash[attribute.to_s]) then;Fi[	@ÃiCI"ï      end
      
      # Gets the db default for the machine's attribute
      def function
        attribute_property and attribute_property.default;Fi[	@ÚiI"ë      end
      
      # Gets the db default for the machine's attribute
      def function
        attribute_key and attribute_key.default_value;Fi[	@˜i I"è      end
      
      # Gets the db default for the machine's attribute
      def function
        attribute_field and attribute_field.default;Fi[	@M
ifI"“      end
      
      # Gets the db default for the machine's attribute
      def function
        if owner_class.db.table_exists?(owner_class.table_name) and var = owner_class.db_schema[attribute.to_sym] then;Fi[	I"# @note Undocumented;F@®i
[
[	@ºiI"å      # Returns activity about me
      #
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ºi"I"é      # Returns activity by friends
      #
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ºi0I"x      end
      
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ºiNI"ó      # Returns activity summary for a Tweet
      #
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ºiaI"ñ      # Returns activity summary for Tweets
      #
      # @note Undocumented
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@—@=i
[
[	@ïiI"}      
      # A collection containing the favorited tweet
      #
      # @return [Array<Twitter::Tweet>]
      def function;Fi[	@iI"|      
      # A collection of tweets that mention a user
      #
      # @return [Array<Twitter::Tweet>]
      def function;Fi[	@ûi
I"ü    class Reply < Twitter::Action::Tweet
      # A collection of tweets that reply to a user
      #
      # @return [Array<Twitter::Tweet>]
      def function;Fi[	@ûiI"Ç      
      # A collection that contains the replied-to tweets
      #
      # @return [Array<Twitter::Tweet>]
      def function;Fi[	@i
I"é    class Retweet < Twitter::Action::Tweet
      # A collection of retweets
      #
      # @return [Array<Twitter::Tweet>]
      def function;Fi[	I"J# Defines an initialization hook into the owner class for setting the;FI"J# initial state of the machine *before* any attributes are set on the;Fi
[
[	@Âi(I"∑      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object;Fi[	@ÃiRI"∑      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object;Fi[	@Úi&I"∑      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object;Fi[	@˜i*I"∑      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object;Fi[	@M
iFI"∑      end
      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object;Fi[	@W
@Mi
[
[	@0i5I"ç    def self.post_to_hash(var, var)
      case var["STRING"]
      when "STRING" then
        var = var["STRING"]
        var = var["STRING"];Fi[	@0i<I"‹        var = "STRING"
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?
      when "STRING" then
        var = var["STRING"]
        var = var.keys.map { |var| var.gsub("STRING", "STRING").to_i }.max;Fi[	@0iMI"}          var = var.at["STRING"]
        end
      when "STRING" then
        var = var["STRING"]
        var = "STRING";Fi[	@0iQI"®        var = "STRING"
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?
      when "STRING" then
        var = var["STRING"]
        var = "STRING";Fi[	@0iVI"´        var["STRING"]["STRING"].each { |var| (var << "STRING") }
        (var << "STRING")
      when "STRING" then
        var = var["STRING"]
        var = var["STRING"];Fi[	@"@ i
[
[	@ÊiI"j  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?);Fi[	@Êi'I"l  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!);Fi[	@ÊiQI"j  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?);Fi[	@Êi_I"l  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!);Fi[	@ÊimI"l  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!);Fi[	@0I"# object;Fi
[
[	@Âi)I"¿      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function;Fi[	@ÃiSI"¿      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function;Fi[	@Úi'I"¿      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function;Fi[	@˜i+I"¿      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function;Fi[	@M
iGI"¿      
      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function;Fi[	@W
@{i
[
[	@iI"  when "STRING", "STRING" then
    "STRING".undent
  when "STRING" then
    "STRING".undent
  when "STRING", "STRING" then;Fi[	@iI"Ñ  when "STRING", "STRING" then
    "STRING".undent
  when "STRING" then
    "STRING".undent
  when "STRING", "STRING", "STRING" then;Fi[	@iI"  when "STRING", "STRING", "STRING" then
    "STRING".undent
  when "STRING" then
    "STRING".undent
  when "STRING" then;Fi[	@iI"l  when "STRING" then
    "STRING".undent
  when "STRING" then
    "STRING".undent
  when "LITERAL" then;Fi[	@iI"W  when "LITERAL" then
    "STRING"
  when "STRING" then
    "STRING".undent
  else;Fi[	I",#         transition :idling => :parked;F@◊i
[
[	@iI"í    #     state_machine :initial => :parked do
    #       event :park do
    #         transition :idling => :parked
    #       end
    #       ;Fi[	@iAI"í    #     state_machine :initial => :parked do
    #       event :park do
    #         transition :idling => :parked
    #       end
    #       ;Fi[	@iGI"v    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end;Fi[	@iyI"v    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end;Fi[	@5i§I"v    #       
    #       event :park do
    #         transition :idling => :parked
    #       end
    #     end;Fi[	I"#       event :park do;F@ni
[
[	@iI"õ    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :park do
    #         transition :idling => :parked
    #       end;Fi[	@i@I"õ    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :park do
    #         transition :idling => :parked
    #       end;Fi[	@iFI"x    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end;Fi[	@ixI"x    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end;Fi[	@5i£I"x    #       end
    #       
    #       event :park do
    #         transition :idling => :parked
    #       end;Fi[	@U@Ói
[
[	@Âi*I"·      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_static_state_initializer;Fi[	@ÃiTI"      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"));Fi[	@Úi(I"      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"));Fi[	@˜i,I"      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"));Fi[	@M
iHI"      # Defines an initialization hook into the owner class for setting the
      # initial state of the machine *before* any attributes are set on the
      # object
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"));Fi[	I"-var.change_make_var!("STRING", "STRING");FI" assert_equal("STRING", var);Fi
[
[	@ûiI"é    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@ûiI"é    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@ûiI"é    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@ûi I"é    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@ûi'I"é    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	I"G# Creates a scope for finding records *with* a particular state or;F@ñi
[
[	@ÂiHI"í      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var);Fi[	@ÃivI"í      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var);Fi[	@ÚiAI"í      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var);Fi[	@˜iEI"í      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var);Fi[	@M
irI"í      end
      
      # Creates a scope for finding records *with* a particular state or
      # states for the attribute
      def function(var);Fi[	@@îi
[
[	@ûi
I"ó  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@ûiI"ó  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@ûiI"ó  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@ûiI"ó  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@ûi&I"ó  def function
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@ô@ri
[
[	@ñióI"⁄    def function(var)
      if ["STRING", "STRING"].include?(var) and (position_or_angle.is_a?(Sass::Script::Number) and position_or_angle.numerator_units.include?("STRING")) then
        false
      else
        super;Fi[	@õi`I"é    if user and user.blocks.where("LITERAL" => (person_id)).exists? then
      (errors["LITERAL"] << "STRING")
      false
    else
      true;Fi[	@ÒijI"Y      self.identifier
    when "STRING" then
      false
    else
      # do nothing;Fi[	I"4projects//diaspora_proj/models/tag_following.rb;TiI"√  def self.user_is_following?(var, var)
    if var.nil? then
      false
    else
      joins("LITERAL").where("LITERAL" => ({ "LITERAL" => (var.downcase) })).where("LITERAL" => (var.id)).exists?;Fi[	@.iñI"{      self.remove_hidden_shareable(var, var)
      self.save
      false
    else
      self.add_hidden_shareable(var, var);Fi[	@Ó@ëi
[
[	@¨i[I"R    end
    
    def function
      var = "LITERAL"
      if new_config? then;Fi[	@˝iI"M  extend(self)
  
  def function
    var = "LITERAL"
    var = "LITERAL";Fi[	@≥iCI"P  end
  
  def function
    var = "LITERAL"
    Dir["STRING"].each do |var|;Fi[	@$iI"L  end
  
  def function
    var = "LITERAL"
    if var.match(@url) then;Fi[	@«i∆I"º    # Reconnect to Redis to avoid sharing a connection with the parent,
    # retry up to 3 times with increasing delay before giving up.
    def function
      var = "LITERAL"
      begin;Fi[	@4I"when Array then;Fi
[
[	@øiCI"ö      var = @controller.request.env
      var ||= {}
      case var
      when Array then
        if (var.first == "LITERAL") and intercept_401?(var) then;Fi[	@FimI"π      def function(var, var)
        var = with_output_buffer_without_haml_xss(*var, &var)
        case var
        when Array then
          var.map { |var| Haml::Util.html_safe(var) };Fi[	@RiI"g    var = []
    var.each do |var|
      case var
      when Array then
        if var.empty? then;Fi[	@Ri>I"Ç    var = []
    var.each do |var|
      case var
      when Array then
        var.each { |var| (var << install_symlink_p(var)) };Fi[	@IiI"á      
      def self.modify_keys(var, var)
        case var
        when Array then
          var.map { |var| modify_keys(var, &var) };Fi[	@≈@$i
[
[	@¶ipI"´    end
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?;Fi[	@¶irI"—    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?;Fi[	@¶itI"—    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?;Fi[	@¶ivI"—    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?;Fi[	@¶iâI"Ã    return var.captures.first unless var.nil?
    var = "LITERAL".match(var)
    return var.captures.first unless var.nil?
    var = "LITERAL".match(var.to_s)
    return var.captures.first unless var.nil?;Fi[	@õI"load_defaults!;Fi
[
[	@ÅiI"å      ##
      # Creates a new instance of Backup::Compressor::Bzip2
      def function(var)
        load_defaults!
        @level ||= false;Fi[	@ÑiI"ê      ##
      # Initializes a new custom compressor.
      def function(var)
        load_defaults!
        instance_eval(&var) if block_given?;Fi[	@áiI"ã      ##
      # Creates a new instance of Backup::Compressor::Gzip
      def function(var)
        load_defaults!
        @level ||= false;Fi[	@äiI"ä      ##
      # Creates a new instance of Backup::Compressor::Lzma
      def function(var)
        load_defaults!
        @best ||= false;Fi[	@çiI"å      ##
      # Creates a new instance of Backup::Compressor::Pbzip2
      def function(var)
        load_defaults!
        @best ||= false;Fi[	I"J# Creates a scope for finding records *without* a particular state or;F@ñi
[
[	@ÂiNI"ï      end
      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var);Fi[	@Ãi|I"ï      end
      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var);Fi[	@ÚiGI"ï      end
      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var);Fi[	@˜iKI"ï      end
      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var);Fi[	@M
ixI"ï      end
      
      # Creates a scope for finding records *without* a particular state or
      # states for the attribute
      def function(var);Fi[	@^@^i
[
[	@*i"I"ï          end)
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"));Fi[	@*i*I"§          require("kramdown")
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"));Fi[	@*i3I"ﬁ          @rdiscount_extensions = @config["STRING"]["STRING"].map { |var| var.to_sym })
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"));Fi[	@*iII"ï          end)
        rescue LoadError
          STDERR.puts("STRING")
          STDERR.puts("STRING")
          raise(FatalException.new("STRING"));Fi[	@_iI"ï        @setup = true)
      rescue LoadError
        STDERR.puts("STRING")
        STDERR.puts("STRING")
        raise(FatalException.new("STRING"));Fi[	@i@(i
[
[	@li™I"m          puts
          puts("STRING")
          puts("STRING")
          puts
          puts("STRING");Fi[	@li≠I"m          puts
          puts("STRING")
          puts("STRING")
          puts
          puts("STRING");Fi[	@ciI"p          unless var.empty? then
            puts
            puts("STRING")
            puts
          end;Fi[	@≥i&I"c      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts
      puts("STRING");Fi[	@≥i)I"J      puts
      puts("STRING")
      puts("STRING")
      puts
  end;Fi[	@†@˘i
[
[	@5iŸI"v    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end;Fi[	@5iÁI"v    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end;Fi[	@5i¯I"v    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end;Fi[	@5iI"v    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       ...
    #     end;Fi[	@5i\I"ó    #     attr_accessor :state
    #     
    #     state_machine :state, :initial => :parked do
    #       ...
    #       state :parked, :value => 0;Fi[	@õ@Ÿi
[
[	@Ìi/I"ü      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var = case var
        when "LITERAL" then;Fi[	@Ùi-I"ü      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var = case var
        when "LITERAL" then;Fi[	@˚i7I"ü      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var = case var
        when "LITERAL" then;Fi[	@i+I"ü      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var = case var
        when "LITERAL" then;Fi[	@iII"L  end
  
  def function(var)
    var = case var
    when "LITERAL" then;Fi[	@õI"@name = var;Fi
[
[	I"$projects//devise_proj/devise.rb;Ti˛I"F  
  class Getter
    def function(var)
      @name = var
    end;Fi[	@i2I"e  attr_reader("LITERAL", "LITERAL")
  
  def function(var)
    @name = var
    @build = case var;Fi[	@çiI"Q  attr("LITERAL")
  
  def function(var)
    @name = var
    super("STRING");Fi[	@çiI"Q  attr("LITERAL")
  
  def function(var)
    @name = var
    super("STRING");Fi[	@çi*I"7  end
  
  def function(var)
    @name = var
  end;Fi[	@—I",# Call with a block to register a hook.;Fi
[
[	@ i]I"≥  # changes you make will be permanent for the lifespan of the
  # worker.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.;Fi[	@ ilI"ÿ  # before every job, so be careful- any changes you make will be
  # permanent for the lifespan of the worker.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.;Fi[	@ i{I"ﬂ  # the current job. Any changes you make, therefore, will only live as
  # long as the job currently being processed.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.;Fi[	@ iõI"¬  # the job code is performed. This hook will run before any
  # Job.before_perform hook.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.;Fi[	@ i™I"¡  # the job code has performed. This hook will run after any
  # Job.after_perform hook.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.;Fi[	@´@—i
[
[	@{i4I"d    # date - The Time to format.
    #
    # Examples
    #
    #   date_to_xmlschema(Time.now);Fi[	@{iCI"i    # input - The String to escape.
    #
    # Examples
    #
    #   xml_escape('foo "bar" <baz>');Fi[	@{iRI"f    # input - The String to escape.
    #
    # Examples
    #
    #   cgi_escape('foo,bar;baz?');Fi[	@{inI"à    # array - The Array of Strings to join.
    #
    # Examples
    #
    #   array_to_sentence_string(["apples", "oranges", "grapes"]);Fi[	@biÂI"}    # post_attr - The String name of the Post attribute.
    #
    # Examples
    #
    #   post_attr_hash('categories');Fi[	@—I"Ü# Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).;Fi
[
[	@µiÒI"G      # Add +node_or_tags+ as a child of this Node.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #;Fi[	@µiI"R      # Insert +node_or_tags+ before this Node (as a sibling).
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #;Fi[	@µiI"Q      # Insert +node_or_tags+ after this Node (as a sibling).
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #;Fi[	@µiLI"K      # Set the inner html for this Node +node_or_tags+
      # +node_or_tags+ can be a Nokogiri::XML::Node, a Nokogiri::XML::DocumentFragment, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #;Fi[	@µi^I"B      # Replace this Node with +node_or_tags+.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #;Fi[	@õI"@options = var;Fi
[
[	@{iI"p    attr_accessor("LITERAL")
    
    def function(var)
      @options = var
      @output_tabs = "LITERAL";Fi[	@ei4I"´        # Integer representation of the SaveOptions
        # Create a new SaveOptions object with +options+
        def function(var)
          @options = var
        end;Fi[	@oiOI"Ø      # the default options used for parsing XML documents
      # the default options used for parsing HTML documents
      def function(var)
        @options = var
      end;Fi[	@‡iI"c    end
    
    def function(var)
      @options = var
      @options["LITERAL"] ||= "STRING";Fi[	@îi
I"|    attr_reader("LITERAL", "LITERAL")
    
    def function(var)
      @options = var
      @at = var.delete("LITERAL");Fi[	@I"module Strategies;Fi
[
[	@ÎiI"irequire("devise/strategies/base")
module Devise
  module Strategies
    class Authenticatable < Base;Fi[	I"4projects//devise_proj/devise/strategies/base.rb;Ti I"Rmodule Devise
  module Strategies
    class Base < ::Warden::Strategies::Base;Fi [	I"Hprojects//devise_proj/devise/strategies/database_authenticatable.rb;TiI"Çrequire("devise/strategies/authenticatable")
module Devise
  module Strategies
    class DatabaseAuthenticatable < Authenticatable;Fi[	@∞iI"|require("devise/strategies/authenticatable")
module Devise
  module Strategies
    class Rememberable < Authenticatable;Fi[	@‡iI"yrequire("devise/strategies/base")
module Devise
  module Strategies
    class TokenAuthenticatable < Authenticatable;Fi[	@@xi
[
[	@2iI"Orequire("rubygems")
require("sequel")
require("fileutils")
require("yaml");Fi[	@9iI"Orequire("rubygems")
require("sequel")
require("fileutils")
require("yaml");Fi[	@∫iI"Orequire("rubygems")
require("sequel")
require("fileutils")
require("yaml");Fi[	@MiI"Orequire("rubygems")
require("sequel")
require("fileutils")
require("yaml");Fi[	@TiI"Orequire("rubygems")
require("sequel")
require("fileutils")
require("yaml");Fi[	@–@Ôi
[
[	@)iXI"Ì      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin
            object_from_response(Twitter::Tweet, "LITERAL", "STRING", var.options.merge("LITERAL" => (var)));Fi[	@)izI"Ì      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin
            object_from_response(Twitter::Tweet, "LITERAL", "STRING", var.options.merge("LITERAL" => (var)));Fi[	@0iúI"        var = Twitter::API::Arguments.new(var)
        var.options["LITERAL"] = true if var.options.delete("LITERAL").!.!
        var.flatten.threaded_map do |var|
          begin
            object_from_response(Twitter::User, "LITERAL", "STRING", merge_user(var.options, var));Fi[	@ßiêI"´      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin
            post_retweet(var, var.options);Fi[	@ßi´I"´      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin
            post_retweet(var, var.options);Fi[	@¸@ei
[
[	@ÙiI"_      
      ##
      # Server credentials
      ##
      # Server IP Address and FTP port;Fi[	@iI"_      
      ##
      # Server credentials
      ##
      # Server IP Address and SSH port;Fi[	@´iI"_      
      ##
      # Server credentials
      ##
      # Server IP Address and SCP port;Fi[	@ÆiI"`      
      ##
      # Server credentials
      ##
      # Server IP Address and SFTP port;Fi[	@iI"i        
        ##
        # Server credentials
        ##
        # Server IP Address and SSH port;Fi[	@Ó@ii
[
[	@li—I"{      # [Version]
      # Returns the current version of the Backup gem
      def function
        puts("STRING")
      end;Fi[	@ñi3I"w        end
        
        def function
          puts("STRING")
          @images.each { |var| puts(var.file) };Fi[	@&iII"Pend
module NoiseyPathname
  def function
    puts("STRING")
    orig_unlink;Fi[	@áifI"^  
  # we try to keep output minimal
  def function
    puts("STRING")
    puts("STRING");Fi[	@áiI"B  end
  
  def function
    puts("STRING")
    puts("STRING");Fi[	I""def self.required_fields(var);F@Òi
[
[	@ÍiI"N      end
      
      def self.required_fields(var)
        []
      end;Fi[	@iI"i      extend(ActiveSupport::Concern)
      
      def self.required_fields(var)
        []
      end;Fi[	@i
I"i      extend(ActiveSupport::Concern)
      
      def self.required_fields(var)
        []
      end;Fi[	@"iI"i      extend(ActiveSupport::Concern)
      
      def self.required_fields(var)
        []
      end;Fi[	@€i
I"í      VALIDATIONS = ["LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL"].freeze
      
      def self.required_fields(var)
        []
      end;Fi[	I"-transferred_files_for(var) do |var, var|;FI"*Logger.message(("STRING" + "STRING"));Fi
[
[	@˛i?I"ﬁ      def function(var)
        var = remote_path_for(var)
        transferred_files_for(var) do |var, var|
          Logger.message(("STRING" + "STRING"))
          connection.delete_object(container, File.join(var, var));Fi[	@ÙiJI"œ        var = remote_path_for(var)
        connection do |var|
          transferred_files_for(var) do |var, var|
            Logger.message(("STRING" + "STRING"))
            var.delete(File.join(var, var));Fi[	@iII"Õ        if var = directory_for(var) then
          var = []
          transferred_files_for(var) do |var, var|
            Logger.message(("STRING" + "STRING"))
            if var = var.files.get(var) then;Fi[	@	iCI"·        var = remote_path_for(var)
        connection.sync_clock
        transferred_files_for(var) do |var, var|
          Logger.message(("STRING" + "STRING"))
          connection.delete_object(bucket, File.join(var, var));Fi[	@Æi>I"–        var = remote_path_for(var)
        connection do |var|
          transferred_files_for(var) do |var, var|
            Logger.message(("STRING" + "STRING"))
            var.remove!(File.join(var, var));Fi[	@CI"module CSS;Fi
[
[	@i I"0module Nokogiri
  module CSS
    class Node;Fi [	@îiI"|require("racc/parser.rb")
require("nokogiri/css/parser_extras")
module Nokogiri
  module CSS
    class Parser < Racc::Parser;Fi[	I"9projects//nokogiri_proj/nokogiri/css/syntax_error.rb;TiI"rrequire("nokogiri/syntax_error")
module Nokogiri
  module CSS
    class SyntaxError < ::Nokogiri::SyntaxError;Fi[	@G	i I"5module Nokogiri
  module CSS
    class Tokenizer;Fi [	@Üi I"8module Nokogiri
  module CSS
    class XPathVisitor;Fi [	I"@path ||= "STRING";F@{i
[
[	@˛iI"Ü        super(var, var)
        @servicenet ||= false
        @path ||= "STRING"
        instance_eval(&var) if block_given?
      end;Fi[	@iI"ä      def function(var, var, var)
        super(var, var)
        @path ||= "STRING"
        instance_eval(&var) if block_given?
      end;Fi[	@	iI"ä      def function(var, var, var)
        super(var, var)
        @path ||= "STRING"
        instance_eval(&var) if block_given?
      end;Fi[	@´iI"®        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING"
        instance_eval(&var) if block_given?
        @path = path.sub("LITERAL", "STRING");Fi[	@ÆiI"®        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING"
        instance_eval(&var) if block_given?
        @path = path.sub("LITERAL", "STRING");Fi[	I" Rails.logger.info("STRING");FI"return;Fi
[
[	@Ïi/I"r  def function(var, var)
    if self.target.nil? then
      Rails.logger.info("STRING")
      return
    else;Fi[	@Ïi:I"p          self.perform(var)
        else
          Rails.logger.info("STRING")
          return
        end;Fi[	I"1projects//diaspora_proj/models/retraction.rb;Ti8I"°    if (self.type == "STRING") then
      unless (self.person.guid.to_s == self.post_guid.to_s) then
        Rails.logger.info("STRING")
        return
      end;Fi[	I"8projects//diaspora_proj/models/signed_retraction.rb;TiUI"r  def function(var, var)
    if self.target.nil? then
      Rails.logger.info("STRING")
      return
    else;Fi[	@¯ i[I"f        self.perform(var)
      else
        Rails.logger.info("STRING")
        return
      end;Fi[	@—@ i
[
[	@ÂizI"ƒ      # Returns and delete (if it's navigational format) the url stored in the session for
      # the given scope. Useful for giving redirect backs after sign up:
      #
      # Example:
      #;Fi[	@1
itI"¡      # confirmation sent date does not exceed the confirm in time configured.
      # Confirm_within is a model configuration, must always be an integer value.
      #
      # Example:
      #;Fi[	@ÌiAI"≤      # method, you should probably override this method to protect other
      # attributes you would not like to be updated without a password.
      #
      # Example:
      #;Fi[	@—
i%I"“      # Returns true if the resource is not responding to reset_password_sent_at at all.
      # reset_password_within is a model configuration, must always be an integer value.
      #
      # Example:
      #;Fi[	@ iOI"G  #
  # Returns the number of jobs destroyed.
  #
  # Example:
  #;Fi[	@=@Íi
[
[	@ëiI"}      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@ëi]I"}      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@ëirI"}      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@ëiÑI"}      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@ëiôI"t      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@HI"¥# @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi
[
[	@ëiI"®      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @example Return the 20 most recent mentions (statuses containing @username) for the authenticating user;Fi[	@ëiJI"í      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi[	@ëiaI"í      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi[	@ëivI"í      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi[	@ëiàI"í      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi[	I"=# Call with no arguments to return all registered hooks.;F@õi
[
[	@ i_I"ª  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var)
    var ? (register_hook("LITERAL", var)) : (hooks("LITERAL"));Fi[	@ inI"ª  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var)
    var ? (register_hook("LITERAL", var)) : (hooks("LITERAL"));Fi[	@ i}I"ª  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var)
    var ? (register_hook("LITERAL", var)) : (hooks("LITERAL"));Fi[	@ iùI"ª  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var)
    var ? (register_hook("LITERAL", var)) : (hooks("LITERAL"));Fi[	@ i¨I"ª  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var)
    var ? (register_hook("LITERAL", var)) : (hooks("LITERAL"));Fi[	@ä@Ôi
[
[	@li!I"9  end
  
  def function(var, var)
    "STRING"
  end;Fi[	@~i8I"ò    # @param str [String] The string to add before the Haml
    # @yield A block of Haml to prepend to
    def function(var, var)
      "STRING"
    end;Fi[	@~iKI"ñ    # @param str [String] The string to add after the Haml
    # @yield A block of Haml to append to
    def function(var, var)
      "STRING"
    end;Fi[	@>iÎI"≠    # @param vars [Array<Boolean>] The static variable assignment
    # @return [String] The real name of the static method
    def function(var, var)
      "STRING"
    end;Fi[	I"3projects//jekyll_proj/jekyll/tags/highlight.rb;Ti2I"C    end
    
    def function(var, var)
      "STRING"
    end;Fi[	@—@«i
[
[	@‘isI"j      #
      # The given path will be expanded before use.
      #
      # @return [String]
      ##;Fi[	@‘iI"~      #
      # Use of this option will override the use of {#passphrase_file}.
      #
      # @return [String]
      ##;Fi[	@‘iI"L      #     end
      #   end
      #
      # @return [String]
      ##;Fi[	@xiI"Œ    # The string that holds the compiled HTML. This is aliased as
    # `_erbout` for compatibility with ERB-specific code.
    #
    # @return [String]
    # The options hash passed in from {Haml::Engine}.;Fi[	@Ki$I"«    # or `nil` if the indentation is ambiguous
    # (for example, for a single-level document).
    #
    # @return [String]
    # Tilt currently depends on these moved methods, provide a stable API;Fi[	@E @"!i
[
[	@ i^I"à  # worker.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var);Fi[	@ imI"™  # permanent for the lifespan of the worker.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var);Fi[	@ i|I"´  # long as the job currently being processed.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var);Fi[	@ iúI"ô  # Job.before_perform hook.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var);Fi[	@ i´I"ò  # Job.after_perform hook.
  #
  # Call with a block to register a hook.
  # Call with no arguments to return all registered hooks.
  def function(var);Fi[	@Ï@Ïi
[
[	@ÚiLI"H  
  def function(var)
    var = []
    var = []
    var = "STRING";Fi[	@ci&I"ù        if var then
          $found = var.length
          var = []
          var = []
          (var << Thread.new { search_tap("STRING", "STRING", var) });Fi[	@√iCI"ñ    end
    unless ignore_deps then
      var = []
      var = []
      if f.recursive_deps.any? { |var| (var.name == "STRING") } and ARGV.debug? then;Fi[	@Ti7I"i        end
      end
      var = []
      var = []
      if (var["LITERAL"] or var["LITERAL"]) then;Fi[	@“ivI"i    def function
      return if @jobs.empty?
      var = []
      var = []
      var = roles.empty?;Fi[	I"warn("STRING");FI"exit("LITERAL");Fi
[
[	@‡iI"ù      @options["LITERAL"] ||= default_identifier
      unless File.exists?(@options["LITERAL"]) then
        warn("STRING")
        exit("LITERAL")
      end;Fi[	@‡iI"≠      end
      if ([@options["LITERAL"], @options["LITERAL"], @options["LITERAL"]].compact.length > "LITERAL") then
        warn("STRING")
        exit("LITERAL")
      end;Fi[	@‡iI"      end
      unless @options["LITERAL"].to_s =~ "LITERAL" then
        warn("STRING")
        exit("LITERAL")
      end;Fi[	@‡iYI"≠    def function
      if read_crontab.=~(Regexp.new("STRING")) and read_crontab.=~(Regexp.new("STRING")).nil? then
        warn("STRING")
        exit("LITERAL")
      else;Fi[	@‡i]I"Æ      else
        if read_crontab.=~(Regexp.new("STRING")).nil? and read_crontab.=~(Regexp.new("STRING")) then
          warn("STRING")
          exit("LITERAL")
        end;Fi[	I"# @return [Twitter::User];F@Ói
[
[	@iI"Ä      # The user who mentioned a user
      #
      # @return [Twitter::User]
      def function
        @source = sources.first;Fi[	@3iI"î    alias "LITERAL" "LITERAL"
    
    # @return [Twitter::User]
    def function
      @recipient ||= Twitter::User.fetch_or_new(@attrs["LITERAL"]);Fi[	@3iI"{    end
    
    # @return [Twitter::User]
    def function
      @sender ||= Twitter::User.fetch_or_new(@attrs["LITERAL"]);Fi[	@6iI"‰    attr_reader("LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL")
    
    # @return [Twitter::User]
    def function
      @user ||= Twitter::User.fetch_or_new(@attrs["LITERAL"]);Fi[	@ÃiÉI"í    end
    
    # @return [Twitter::User]
    def function
      @user ||= fetch_or_new_without_self(Twitter::User, @attrs, "LITERAL", "LITERAL");Fi[	@^ @—i
[
[	@µiÚI"9      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #
      # Also see related method +<<+.;Fi[	@µiI"=      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #
      # Also see related method +before+.;Fi[	@µiI"<      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #
      # Also see related method +after+.;Fi[	@µiMI"A      # +node_or_tags+ can be a Nokogiri::XML::Node, a Nokogiri::XML::DocumentFragment, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #
      # Also see related method +inner_html=+;Fi[	@µi_I";      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).
      #
      # Also see related method +swap+.;Fi[	I"# Examples:;F@—i
[
[	@Âi>I"∂      # signed in, but we want to refresh the credentials in session.
      #
      # Examples:
      #
      #   sign_in :user, @user                      # sign_in(scope, resource);Fi[	@ÂiYI"É      # is no user logged in on the referred scope
      #
      # Examples:
      #
      #   sign_out :user     # sign_out(scope);Fi[	@1
iÉI"ª      
      # Checks if the user confirmation happens before the token becomes invalid
      # Examples:
      #
      #   # confirm_within = 3.days and confirmation_sent_at = 2.days.ago;Fi[	@øi I"í    # This method bypass any warden authentication callback.
    #
    # Examples:
    #
    #   sign_in :user, @user   # sign_in(scope, resource);Fi[	@øi/I"}    # This method bypass any warden logout callback.
    #
    # Examples:
    #
    #   sign_out :user     # sign_out(scope);Fi[	@r@Li
[
[	I">projects//diaspora_proj/controllers/aspects_controller.rb;TiI"ã        end
      end
    else
      respond_to do |var|
        var.js { render("LITERAL" => (I18n.t("STRING")), "LITERAL" => "LITERAL") };Fi[	@ıi+I"å        var.mobile { redirect_to(@comment.post) }
      end
    else
      respond_to do |var|
        var.mobile { redirect_to("LITERAL") };Fi[	I"Fprojects//diaspora_proj/controllers/status_messages_controller.rb;Ti<I"i        end
      end
    else
      respond_to do |var|
        var.html { redirect_to("LITERAL") };Fi[	@
i%I"f        end
      end
    else
      respond_to do |var|
        var.any("LITERAL", "LITERAL") do;Fi[	I";projects//diaspora_proj/controllers/tags_controller.rb;TiI"«        var.json { render("LITERAL" => (@tags.to_json), "LITERAL" => "LITERAL") }
      end
    else
      respond_to do |var|
        var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") };Fi[	@¬@Íi
[
[	@$iI"ë      MercurialDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy;Fi[	@$iI"é      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then
      FossilDownloadStrategy;Fi[	@$i
I"ë      MercurialDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy;Fi[	@$iI"í      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy;Fi[	@$iI"ò      SubversionDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy
    when "LITERAL" then
      CurlApacheMirrorDownloadStrategy;Fi[	@r@ôi
[
[	@;iI"T    when String, Array then
      var.length.zero?
    else
      false
    end;Fi[	@ ivI"õ        if unlock_strategy_enabled?("LITERAL") then
          locked_at and (locked_at < self.class.unlock_in.ago)
        else
          false
        end;Fi[	@.iãI"Ö    if self.hidden_shareables.has_key?(var) then
      self.hidden_shareables[var].include?(var.id.to_s)
    else
      false
    end;Fi[	@.iLI"a      Postzord::Dispatcher.build(self, profile).post
      true
    else
      false
    end;Fi[	@2i@I"ç      if (var.length == var.length) then
        TransitionCollection.new(var, "LITERAL" => (var)).perform
      else
        false
      end;Fi[	I"@(var << Thread.new { search_tap("STRING", "STRING", var) });F@ª!i
[
[	@ci(I"˜          var = []
          var = []
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) });Fi[	@ci)I"*          var = []
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) });Fi[	@ci*I"]          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) });Fi[	@ci+I"]          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) });Fi[	@ci,I"G          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          (var << Thread.new { search_tap("STRING", "STRING", var) })
          var.each { |var| (var << var.value) };Fi[	@˛@Zi	[	[	@ViˆI"õ      # @return [Twitter::List] The list.
      # @overload list_add_members(list, users, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.;Fi[	@Vi I"Ÿ      # @overload list_add_members(user, list, users, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViÁI"û      # @return [Twitter::List] The list.
      # @overload list_remove_members(list, users, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViÒI"‹      # @overload list_remove_members(user, list, users, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.;Fi[	@äI"@post = var;Fi	[	[	I"Aprojects//diaspora_proj/presenters/extreme_post_presenter.rb;TiI"`class ExtremePostPresenter
  def function(var, var)
    @post = var
    @current_user = var;Fi[	I"<projects//diaspora_proj/presenters/o_embed_presenter.rb;TiI"k  include(ActionView::Helpers::TextHelper)
  
  def function(var, var)
    @post = var
    @opts = var;Fi[	I"9projects//diaspora_proj/presenters/post_presenter.rb;Ti	I"n  attr_accessor("LITERAL", "LITERAL")
  
  def function(var, var)
    @post = var
    @current_user = var;Fi[	@€!iDI"hend
class PostInteractionPresenter
  def function(var, var)
    @post = var
    @current_user = var;Fi[	I"†# @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.;F@®i	[	[	@iI"<      #
      # @see https://dev.twitter.com/docs/api/1.1/get/direct_messages
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@i$I"A      #
      # @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/sent
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@i7I"A      #
      # @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@iEI"ˇ      end
      
      # @note This method requires an access token with RWD (read, write & direct message) permissions. Consult The Application Permission Model for more information.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	I"if @model.compressor then;FI"2@model.compressor.compress_with do |var, var|;Fi	[	[	I",projects//backup_proj/backup/archive.rb;TiII"™      var = Pipeline.new
      (var << ("STRING" + "STRING"))
      if @model.compressor then
        @model.compressor.compress_with do |var, var|
          (var << var);Fi[	@
i;I"§        var = "STRING"
        (var << mysqldump)
        if @model.compressor then
          @model.compressor.compress_with do |var, var|
            (var << var);Fi[	@
i;I"°        var = "STRING"
        (var << pgdump)
        if @model.compressor then
          @model.compressor.compress_with do |var, var|
            (var << var);Fi[	@¯i*I"≥        var = File.join(@dump_path, name)
        run("STRING")
        if @model.compressor then
          @model.compressor.compress_with do |var, var|
            run("STRING");Fi[	@Ï!I"(var << var);Fi	[	[	@Ô!iJI"®      (var << ("STRING" + "STRING"))
      if @model.compressor then
        @model.compressor.compress_with do |var, var|
          (var << var)
          (var << var);Fi[	@ÚiuI"´        Logger.message(("STRING" + "STRING"))
        (var << "STRING")
        @model.compressor.compress_with do |var, var|
          (var << var)
          (var << var);Fi[	@
i<I"¶        (var << mysqldump)
        if @model.compressor then
          @model.compressor.compress_with do |var, var|
            (var << var)
            (var << var);Fi[	@
i<I"£        (var << pgdump)
        if @model.compressor then
          @model.compressor.compress_with do |var, var|
            (var << var)
            (var << var);Fi[	I"safe_system("STRING");F@"i	[	[	@!i3I"Ü    begin
      if Dir["STRING"].empty? then
        safe_system("STRING")
        safe_system("STRING")
        safe_system("STRING");Fi[	@!i4I"ö      if Dir["STRING"].empty? then
        safe_system("STRING")
        safe_system("STRING")
        safe_system("STRING")
        safe_system("STRING");Fi[	@!i5I"ï        safe_system("STRING")
        safe_system("STRING")
        safe_system("STRING")
        safe_system("STRING")
        safe_system("STRING");Fi[	@!i6I"Å        safe_system("STRING")
        safe_system("STRING")
        safe_system("STRING")
        safe_system("STRING")
      end;Fi[	@¯!@¯!i	[	[	@Ô!iKI"è      if @model.compressor then
        @model.compressor.compress_with do |var, var|
          (var << var)
          (var << var)
        end;Fi[	@ÚivI"â        (var << "STRING")
        @model.compressor.compress_with do |var, var|
          (var << var)
          (var << var)
        end;Fi[	@
i=I"ô        if @model.compressor then
          @model.compressor.compress_with do |var, var|
            (var << var)
            (var << var)
          end;Fi[	@
i=I"ô        if @model.compressor then
          @model.compressor.compress_with do |var, var|
            (var << var)
            (var << var)
          end;Fi[	I"onoe("STRING");F@ii	[	[	@$iÌI"«    var = var.split("STRING").collect { |var| Integer(var) }
    if (var["LITERAL"] == "LITERAL") and (var["LITERAL"] <= "LITERAL") then
      onoe("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√iãI"|        Keg.new(f.prefix).optlink
      rescue Exception => var
        onoe("STRING")
        puts("STRING")
      end;Fi[	@√i—I"n      var.link
    rescue Exception => var
      onoe("STRING")
      puts("STRING")
      puts("STRING");Fi[	@√iÊI"Ñ      Keg.new(f.prefix).fix_install_names
    rescue Exception => var
      onoe("STRING")
      puts("STRING")
      puts("STRING");Fi[	@äI"!var["LITERAL"] ||= "LITERAL";Fi	[	[	@øiQI"i    end
    
    def function(var, var)
      var["LITERAL"] ||= "LITERAL"
      var = var["STRING"];Fi[	@üi	I"  # @param opts [Hash] Optional hash.  Accepts :length and :process_newlines parameters.
  # @return [String] The truncated and formatted post.
  def function(var, var)
    var["LITERAL"] ||= "LITERAL"
    if var.respond_to?("LITERAL") then;Fi[	@üiI"  # @param opts [Hash] Optional hash.  Accepts :length and :process_newlines parameters.
  # @return [String] The truncated and formatted comment.
  def function(var, var)
    var["LITERAL"] ||= "LITERAL"
    var = truncate(var.text, "LITERAL" => (var["LITERAL"]));Fi[	I"4projects//diaspora_proj/helpers/posts_helper.rb;TiI"h  end
  
  def function(var, var)
    var["LITERAL"] ||= "LITERAL"
    var["LITERAL"] ||= "LITERAL";Fi[	I"if @clone.exist? then;F@ii	[	[	@$iI"`      end
    end
    if @clone.exist? then
      puts("STRING")
      Dir.chdir(@clone) do;Fi[	@$iìI"~    raise("STRING") unless hgpath
    ohai("STRING")
    if @clone.exist? then
      puts("STRING")
      Dir.chdir(@clone) do;Fi[	@$iªI"ü    raise("STRING") unless bzrpath
    ohai("STRING")
    if @clone.exist? then
      puts("STRING")
      Dir.chdir(@clone) { safe_system(bzrpath, "STRING") };Fi[	@$iŸI"°    raise("STRING") unless fossilpath
    ohai("STRING")
    if @clone.exist? then
      puts("STRING")
      safe_system(fossilpath, "STRING", "STRING", @clone);Fi[	@>@ri	[	[	@jiI"|      end
      if FONT_TYPES.key?(var) then
        (var << "STRING")
      else
        raise(Sass::SyntaxError, "STRING");Fi[	@>i:I"	        (var << var.matched[("LITERAL"...("LITERAL" - var))])
        if ((var % "LITERAL") == "LITERAL") then
          (var << "STRING")
        else
          var = eval((("STRING" + balance(var, "STRING", "STRING", "LITERAL")["LITERAL"]["LITERAL"]) + "STRING"));Fi[	@˚i7I"í          (var << "STRING")
          (var << "STRING") unless var.directory?
          (var << "STRING")
        else
          (var << "STRING");Fi[	@˚iII"Ü            (var << "STRING")
            (var << "STRING")
            (var << "STRING")
          else
            (var << "STRING");Fi[	I"view_directory("LITERAL");F@D"i	[	[	@9iI"      
      def function
        view_directory("LITERAL")
        view_directory("LITERAL")
        view_directory("LITERAL");Fi[	@9iI"ö      def function
        view_directory("LITERAL")
        view_directory("LITERAL")
        view_directory("LITERAL")
        view_directory("LITERAL");Fi[	@9iI"©        view_directory("LITERAL")
        view_directory("LITERAL")
        view_directory("LITERAL")
        view_directory("LITERAL")
        view_directory("LITERAL");Fi[	@9iI"ë        view_directory("LITERAL")
        view_directory("LITERAL")
        view_directory("LITERAL")
        view_directory("LITERAL")
      end;Fi[	@>I"var.run;Fi	[	[	@Ô!iOI"[        end
      end
      (var << "STRING")
      var.run
      if var.success? then;Fi[	@ÚiyI"n          (var << var)
        end
        (var << "STRING")
        var.run
        if var.success? then;Fi[	@
iAI"e          end
        end
        (var << "STRING")
        var.run
        if var.success? then;Fi[	@
iAI"e          end
        end
        (var << "STRING")
        var.run
        if var.success? then;Fi[	I"desc("STRING");FI"task("LITERAL") do;Fi	[	[	I"*projects//rescue_proj/resque/tasks.rb;TiI"h    var.work((ENV["STRING"] or "LITERAL"))
  end
  desc("STRING")
  task("LITERAL") do
    var = [];Fi[	I"8projects//state_machine_proj/tasks/state_machine.rb;TiI"Onamespace("LITERAL") do
  desc("STRING")
  task("LITERAL") do
    var = {};Fi[	I";projects//whenever_proj/whenever/capistrano/recipes.rb;TiI"’  _cset("LITERAL") { "STRING" }
  namespace("LITERAL") do
    desc("STRING")
    task("LITERAL") do
      var = { "LITERAL" => (fetch("LITERAL")), "LITERAL" => (fetch("LITERAL")), "LITERAL" => (fetch("LITERAL")) };Fi[	@d"i!I"d      end
    end
    desc("STRING")
    task("LITERAL") do
      if whenever_servers.any? then;Fi[	I"-# Credentials for the specified database;F@ei	[	[	@ÚiI"ì      # Name of the database that needs to get dumped
      ##
      # Credentials for the specified database
      ##
      # Connectivity options;Fi[	@
iI"ü      # To dump all databases, set this to `:all` or leave blank.
      ##
      # Credentials for the specified database
      ##
      # Connectivity options;Fi[	@
iI"ì      # Name of the database that needs to get dumped
      ##
      # Credentials for the specified database
      ##
      # Connectivity options;Fi[	@ıiI"ü      # Name of and path to the database that needs to get dumped
      ##
      # Credentials for the specified database
      ##
      # Connectivity options;Fi[	I"*link_dir("STRING", var) { "LITERAL" };F@t"i	[	[	@ikI"È      var.concat("LITERAL".map { |var| "STRING" })
      var.concat("LITERAL".map { |var| "STRING" })
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" };Fi[	@ilI"‚      var.concat("LITERAL".map { |var| "STRING" })
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" };Fi[	@imI"€      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" };Fi[	@inI"÷      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) do |var|;Fi[	I"# Connectivity options;F@ei	[	[	@Úi!I"à      # Credentials for the specified database
      ##
      # Connectivity options
      ##
      # IPv6 support (disabled by default);Fi[	@
iI"è      # Credentials for the specified database
      ##
      # Connectivity options
      ##
      # Tables to skip while dumping the database;Fi[	@
iI"è      # Credentials for the specified database
      ##
      # Connectivity options
      ##
      # Tables to skip while dumping the database;Fi[	@ıiI"¶      # Credentials for the specified database
      ##
      # Connectivity options
      ##
      # Determines whether Backup should invoke the SAVE command through;Fi[	@O"I"if var.success? then;Fi	[	[	@Ô!iPI"p      end
      (var << "STRING")
      var.run
      if var.success? then
        Logger.message("STRING");Fi[	@ÚizI"Ç        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message(("STRING" + "STRING"));Fi[	@
iBI"z        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING");Fi[	@
iBI"z        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING");Fi[	I"%when ["LITERAL", "LITERAL"] then;F@˚i	[	[	@.i'I"¨    var.each do |var|
      var = case read("LITERAL", var).unpack("STRING")
      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then;Fi[	@.i)I"ò      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then;Fi[	@.i+I"ò      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then;Fi[	@.i-I"|      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      else;Fi[	I"Pathname.new("STRING");F@ri	[	[	@0i-I"ì    else
      if File.executable?("STRING") then
        Pathname.new("STRING")
      else
        if (V4_BUNDLE_PATH / "STRING").executable? then;Fi[	@êi6I"®  def function
    @prefix ||= if Pathname.new("STRING").exist? then
      Pathname.new("STRING")
    else
      Pathname.new("STRING") if Pathname.new("STRING").exist?;Fi[	@1i"I"≈    (@locate ||= {}).fetch(var.to_s) do
      @locate[var.to_s] = if File.executable?("STRING") then
        Pathname.new("STRING")
      else
        var = unless Xcode.bad_xcode_select_path? then;Fi[	@1i/I"Ê  def function
    @dev_tools_path ||= if File.exist?("STRING") and File.exist?("STRING") then
      Pathname.new("STRING")
    else
      if Xcode.bad_xcode_select_path?.! and `/usr/bin/xcrun -find make 2>/dev/null`.empty?.! then;Fi[	@ä@öi	[	[	@+
iI"≠      attr_accessor("LITERAL", "LITERAL", "LITERAL")
      
      def function(var, var)
        super(var, var)
        self.project_name = determine_project_name(var, var);Fi[	I"3projects//homebrew_proj/formula_specialties.rb;TiI"®end
class GithubGistFormula < ScriptFileFormula
  def function(var, var)
    super(var, var)
    @stable.version(File.basename(File.dirname(url))["LITERAL", "LITERAL"]);Fi[	@ñi&I"ñ    #
    # Returns nothing.
    def function(var, var)
      super(var, var)
      self.data["STRING"] = "STRING" unless self.data.has_key?("STRING");Fi[	@ôiDI"ñ    #
    # Returns nothing.
    def function(var, var)
      super(var, var)
      self.data["STRING"] = "STRING" unless self.data.has_key?("STRING");Fi[	@I"(# @return [Twitter::List] The list.;Fi	[	[	@ViVI"       # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The list.
      # @overload list_remove_member(list, user_to_remove, options={});Fi[	@ViÛI"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The list.
      # @overload list_add_members(list, users, options={});Fi[	@ViKI"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The list.
      # @overload list_add_member(list, user_to_add, options={});Fi[	@Vi‰I"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The list.
      # @overload list_remove_members(list, users, options={});Fi[	@ÍI"-["STRING", "STRING", "STRING", "STRING"];Fi	[	[	@[iI"Ø    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then;Fi[	@[iI"•    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then;Fi[	@[i I"√    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then;Fi[	@[i"I"•    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then;Fi[	@ÍI"7["STRING", "STRING", "STRING", "STRING", "STRING"];Fi	[	[	@[iI"ä  def function
    case @language
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then;Fi[	@[iI"π    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then;Fi[	@[i$I"Ø    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then;Fi[	@[i&I"™    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    else;Fi[	I"var = coerce(var);FI"$if var.is_a?(XML::NodeSet) then;Fi	[	[	@ßi≈I"≠      def function(var)
        raise("STRING") if root
        var = coerce(var)
        if var.is_a?(XML::NodeSet) then
          raise("STRING") if (var.size > "LITERAL");Fi[	@µiˆI"∞      # Also see related method +<<+.
      def function(var)
        var = coerce(var)
        if var.is_a?(XML::NodeSet) then
          var.each { |var| add_child_node(var) };Fi[	@µiiI"¶          return var.replace(var)
        end
        var = coerce(var)
        if var.is_a?(XML::NodeSet) then
          var.each { |var| add_previous_sibling(var) };Fi[	@µi3I"”        var = (var == "LITERAL") ? ("LITERAL") : ("LITERAL")
        var = (var == "LITERAL") ? ("LITERAL") : ("LITERAL")
        var = coerce(var)
        if var.is_a?(XML::NodeSet) then
          if text? then;Fi[	@rI"if var then;Fi	[	[	@&
iéI"q                end
              end
            else
              if var then
                if var then;Fi[	I"5projects//diaspora_proj/models/status_message.rb;TiTI"≤      if var["LITERAL"] then
        var ? (ERB::Util.h(var.name)) : (ERB::Util.h($~["LITERAL"]))
      else
        if var then
          person_link(var, "LITERAL" => "STRING");Fi[	@0iàI"~          var = "STRING"
          var = var
        else
          if var then
            var = "STRING" if var =~ "LITERAL";Fi[	@ÕiXI"°          @time = var
          parse_time
        else
          if var then
            if (@at.is_a?(Time) or (@at.is_a?(Numeric) and (@at > "LITERAL"))) then;Fi[	@ÍI"w# @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.;Fi	[	[	@ßiI"€      # @return [Array<Twitter::Tweet>]
      # @param id [Integer] The numerical ID of the desired Tweet.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ßi)I"À      # @return [Array]
      # @param id [Integer] The numerical ID of the desired Tweet.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ºiI"Ã      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array] An array of actions
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@ºi'I"Õ      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid./
      # @return [Array] An array of actions
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	I"!require("twitter/creatable");F@(i	[	[	@iI"Xrequire("twitter/base")
require("twitter/creatable")
module Twitter
  module Action;Fi[	@iI"Xrequire("twitter/base")
require("twitter/creatable")
module Twitter
  module Action;Fi[	@iI"Xrequire("twitter/base")
require("twitter/creatable")
module Twitter
  module Action;Fi[	@iI"Xrequire("twitter/base")
require("twitter/creatable")
module Twitter
  module Action;Fi[	@N@˙"i	[	[	@i I"Hrequire("twitter/base")
require("twitter/creatable")
module Twitter;Fi [	@i I"Hrequire("twitter/base")
require("twitter/creatable")
module Twitter;Fi [	@i I"Hrequire("twitter/base")
require("twitter/creatable")
module Twitter;Fi [	@i I"Hrequire("twitter/base")
require("twitter/creatable")
module Twitter;Fi [	@DI"module SAX;Fi	[	[	@ÄiI"Emodule Nokogiri
  module XML
    module SAX
      class Document;Fi[	@ÉiI"Cmodule Nokogiri
  module XML
    module SAX
      class Parser;Fi[	@ÜiI"Jmodule Nokogiri
  module XML
    module SAX
      class ParserContext;Fi[	@âiI"Gmodule Nokogiri
  module XML
    module SAX
      class PushParser;Fi[	@r@Ôi	[	[	@$iAI"h      if @tarball_path.exist? then
        puts("STRING")
      else
        begin
          _fetch;Fi[	I"#projects//homebrew_proj/tab.rb;TiI"Ç    if var.exist? then
      self.from_file(var)
    else
      begin
        self.dummy_tab(Formula.factory(var.parent.basename));Fi[	@	i(I"R  if ARGV.verbose? then
    yield
  else
    begin
      (var = $stderr.clone;Fi[	I",projects//omni_proj/omniauth/builder.rb;Ti&I"Ç      if var.is_a?(Class) then
        var = var
      else
        begin
          var = OmniAuth::Strategies.const_get("STRING");Fi[	@öI"@port ||= "LITERAL";Fi	[	[	@ÙiI"•      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING";Fi[	@iI"•      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING";Fi[	@´iI"•      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING";Fi[	@ÆiI"•      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING";Fi[	I""next if send(var).to_s.empty?;F@Ôi	[	[	@ÚiâI"ö      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING"
        end.compact.join("STRING");Fi[	@ÚiìI"ö      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING"
        end.compact.join("STRING");Fi[	@
igI"§      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING"
        end.compact.join("STRING");Fi[	@ıigI"§      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING"
        end.compact.join("STRING");Fi[	@—@á!i	[	[	@Âi=I"¿      # the user straight in session. This option is useful in cases the user is already
      # signed in, but we want to refresh the credentials in session.
      #
      # Examples:
      #;Fi[	@ÂiXI"´      # after deleting accounts. Returns true if there was a logout and false if there
      # is no user logged in on the referred scope
      #
      # Examples:
      #;Fi[	@øiI"õ    # sign_in a given resource by storing its keys in the session.
    # This method bypass any warden authentication callback.
    #
    # Examples:
    #;Fi[	@øi.I"ñ    # Sign out a given resource or scope by calling logout on Warden.
    # This method bypass any warden logout callback.
    #
    # Examples:
    #;Fi[	@—I"+# Pre-configured defaults specified in;Fi	[	[	@i I"≠        ##
        # Instantiates a new Cloud::CloudFiles Syncer.
        #
        # Pre-configured defaults specified in
        # Configuration::Syncer::Cloud::CloudFiles;Fi[	@iI"ù        ##
        # Instantiates a new Cloud::S3 Syncer.
        #
        # Pre-configured defaults specified in
        # Configuration::Syncer::Cloud::S3;Fi[	@•iI"£        ##
        # Instantiates a new RSync::Local Syncer.
        #
        # Pre-configured defaults specified in
        # Configuration::Syncer::RSync::Local;Fi[	@iI"≥        ##
        # Instantiates a new RSync::Push or RSync::Pull Syncer.
        #
        # Pre-configured defaults specified in
        # Configuration::Syncer::RSync::Push or;Fi[	I"if Nokogiri.jruby? then;FI"5var = var.first.is_a?(Hash) ? (var.shift) : ({});Fi	[	[	@LiYI"Í      # Convert this Builder object to XML
      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({})
          var["LITERAL"] = Node::SaveOptions::AS_BUILDER if var["LITERAL"].!;Fi[	@¨i(I"¿      # See Nokogiri::XML::NodeSet#to_html
      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({})
          if var["LITERAL"].! then;Fi[	@¨i6I"¡      # See Nokogiri::XML::NodeSet#to_xhtml
      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({})
          if var["LITERAL"].! then;Fi[	@∏iıI"∫      # Convert this NodeSet to HTML
      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({})
          if var["LITERAL"].! then;Fi[	I"Bvar = Nokogiri::XML::ParseOptions.new(var) if Fixnum.===(var);FI"yield(var) if block_given?;Fi	[	[	@iTI"È        # Nokogiri::XML::ParseOptions.
        def function(var, var, var, var)
          var = Nokogiri::XML::ParseOptions.new(var) if Fixnum.===(var)
          yield(var) if block_given?
          if var.respond_to?("LITERAL") then;Fi[	@ßiI"Œ      
      def self.parse(var, var, var, var, var)
        var = Nokogiri::XML::ParseOptions.new(var) if Fixnum.===(var)
        yield(var) if block_given?
        var = if var.respond_to?("LITERAL") then;Fi[	@µi‹I"      def function(var, var)
        var ||= document.html? ? (ParseOptions::DEFAULT_HTML) : (ParseOptions::DEFAULT_XML)
        var = Nokogiri::XML::ParseOptions.new(var) if Fixnum.===(var)
        yield(var) if block_given?
        var = var.respond_to?("LITERAL") ? (var.read) : (var);Fi[	@µi!I"¸      # passed to it, allowing more convenient modification of the parser options.
      def function(var, var)
        var = Nokogiri::XML::ParseOptions.new(var) if Fixnum.===(var)
        yield(var) if block_given?
        process_xincludes(var.to_i);Fi[	@r@3i	[	[	@’i.I"o                rm_rf(var)
              end
            else
              opoo("STRING")
            end;Fi[	@’i1I"i              opoo("STRING")
            end
          else
            opoo("STRING")
          end;Fi[	@äi{I"_        opoo("STRING")
        ENV.gcc
      else
        opoo("STRING")
        ENV.clang;Ti[	@iI"æ                if var and var.exist? then
                  system(MacOS.locate("STRING"), "STRING", var, var, var)
                else
                  opoo("STRING")
                end;Fi[	@ˇI"}#   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi	[	[	@iNI"ó      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@Vi5I"‘      # @overload list_timeline(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@Vi?I"      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@ëi0I"€      # @overload user_timeline(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi[	@¡"@Íi	[	[	@[iI"∆      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"];Fi[	@[iI"⁄      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	@[i!I"⁄      ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"];Fi[	@[i#I"∆      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	@ˇI"-#   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi	[	[	@0iI"      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's friends' IDs;Fi[	@0i9I"      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return @sferik's followers' IDs;Fi[	@0iI"Y      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[	@0i"I"Y      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi[	@ˇI"{#   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.;Fi	[	[	@≈iUI"R      # @return [Array<Twitter::User>]
      # @overload contributees(options={})
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @example Return the authenticated user's contributees;Fi[	@≈i[I"ç      # @overload contributees(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @example Return users @sferik can contribute to;Fi[	@≈ilI"R      # @return [Array<Twitter::User>]
      # @overload contributors(options={})
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @example Return the authenticated user's contributors;Fi[	@≈irI"õ      # @overload contributors(user, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @example Return users who can contribute to @sferik's account;Fi[	I"4var["LITERAL"] = ENV["STRING"] if ENV["STRING"];F@û#i	[	[	@a"i	I"Ω  task("LITERAL") do
    var = {}
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"];Fi[	@a"i
I"‹    var = {}
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"];Fi[	@a"iI"    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"];Fi[	@a"iI"    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = ENV["STRING"] if ENV["STRING"]
    var["LITERAL"] = (ENV["STRING"] == "STRING") if ENV["STRING"];Fi[	I"Ivar = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten);F@ài	[	[	@îi≈I"ã      
      def function(var, var, var)
        var = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten)
        var
      end;Fi[	@îi I"ã      
      def function(var, var, var)
        var = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten)
        var
      end;Fi[	@îiœI"ã      
      def function(var, var, var)
        var = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten)
        var
      end;Fi[	@îi‘I"ã      
      def function(var, var, var)
        var = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten)
        var
      end;Fi[	I"0var = Node.new("LITERAL", [var["LITERAL"]]);F@ài	[	[	@îiãI"Ö      # reduce 20 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", [var["LITERAL"]])
        var
      end;Fi[	@îiI"w      
      def function(var, var, var)
        var = Node.new("LITERAL", [var["LITERAL"]])
        var
      end;Fi[	@îi"I"w      
      def function(var, var, var)
        var = Node.new("LITERAL", [var["LITERAL"]])
        var
      end;Fi[	@îitI"w      
      def function(var, var, var)
        var = Node.new("LITERAL", [var["LITERAL"]])
        var
      end;Fi[	@ˇI"∂#   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi	[	[	@ßiRI"à      # @overload statuses(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var);Fi[	@ßifI"é      # @overload status_destroy(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var);Fi[	@ßiåI"á      # @overload retweet(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var);Fi[	@ßißI"à      # @overload retweet!(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var);Fi[	@s#I"~#   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;Fi	[	[	@iOI"
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@Vi6I"      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      #   @option options [Integer] :count The number of results to retrieve.;Fi[	@Vi@I"      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      #   @option options [Integer] :count The number of results to retrieve.;Fi[	@ëi1I"$      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[	@ä@ái	[	[	@iI"¨    
    # Record an action that has occurred
    def function(var, var)
      var = "STRING"
      (var << color(ACTION_COLORS[var])) if Compass.configuration.color_output;Fi[	@li%I"n  end
  
  def function(var, var)
    var = "STRING"
    var.photos.each { |var| var = (var + "STRING") };Fi[	@>i4I"{    end
    
    def function(var, var)
      var = "STRING"
      var = Haml::Util.handle_interpolation(var.dump) do |var|;Fi[	@·
i3I"ﬂ  # Looks for Makefile style variable defintions and replaces the
  # value with "new_value", or removes the definition entirely.
  def function(var, var)
    var = "STRING"
    var = gsub!(Regexp.new("STRING"), var, false);Fi[	@õ@Q#i	[	[	@LiXI"ß      ###
      # Convert this Builder object to XML
      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({});Fi[	@¨i'I"À      # Convert this DocumentFragment to html
      # See Nokogiri::XML::NodeSet#to_html
      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({});Fi[	@¨i5I"Õ      # Convert this DocumentFragment to xhtml
      # See Nokogiri::XML::NodeSet#to_xhtml
      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({});Fi[	@∏iÙI"°      ###
      # Convert this NodeSet to HTML
      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({});Fi[	I"2files_to_transfer_for(@package) do |var, var|;FI"Logger.message("STRING");Fi	[	[	@˛i1I"÷      def function
        var = remote_path_for(@package)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |var|;Fi[	@iSI"÷      def function
        var = remote_path_for(@package)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |var|;Fi[	@˜iI"         var = remote_path_for(@package)
        FileUtils.mkdir_p(var)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING")
          var = File.join(local_path, var);Fi[	@i9I"Í        var = remote_path_for(@package)
        var = directory_for(var, true)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |var|;Fi[	@rI"#raise(ArgumentError, "STRING");Fi	[	[	@∂i}I"Ω              @set_attributes["LITERAL"] = true
              @asset_cache_buster = Proc.new { |var, var| nil }
            else
              raise(ArgumentError, "STRING")
            end;Fi[	@: i>I"Ü      when Hash then
        var, var = var.keys.first, var.values.flatten
      else
        raise(ArgumentError, "STRING")
      end;Fi[	@iPI"â      when String then
        eval(var, var.instance_eval { binding }, &var)
      else
        raise(ArgumentError, "STRING")
      end;Fi[	I".projects//twitter_proj/twitter/factory.rb;TiI"º        var = var.gsub("LITERAL") { "STRING" }.gsub("LITERAL") { $1.upcase }
        var.const_get(var.to_sym).fetch_or_new(var)
      else
        raise(ArgumentError, "STRING")
      end;Fi[	@È#@≈ i	[	[	@Ùi<I"Ò        connection do |var|
          create_remote_path(var, var)
          files_to_transfer_for(@package) do |var, var|
            Logger.message(("STRING" + "STRING"))
            var.put(File.join(local_path, var), File.join(var, var));Fi[	@	i4I"Ó        var = remote_path_for(@package)
        connection.sync_clock
        files_to_transfer_for(@package) do |var, var|
          Logger.message(("STRING" + "STRING"))
          File.open(File.join(local_path, var), "STRING") do |var|;Fi[	@´i2I"        connection do |var|
          var.exec!("STRING")
          files_to_transfer_for(@package) do |var, var|
            Logger.message(("STRING" + "STRING"))
            var.scp.upload!(File.join(local_path, var), File.join(var, var));Fi[	@Æi0I"ı        connection do |var|
          create_remote_path(var, var)
          files_to_transfer_for(@package) do |var, var|
            Logger.message(("STRING" + "STRING"))
            var.upload!(File.join(local_path, var), File.join(var, var));Fi[	I":namespace.attributes["LITERAL"][var]["LITERAL"] = var;FI")var.docstring = ["STRING", "STRING"];Fi	[	[	@èiÑI"U            namespace.attributes["LITERAL"][var] = {}
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")));Fi[	@èiáI"$            var.docstring = ["STRING", "STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            var.parameters = ["STRING"];Fi[	@èièI"U            namespace.attributes["LITERAL"][var] = {}
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")));Fi[	@èiíI"$            var.docstring = ["STRING", "STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            var.parameters = ["STRING"];Fi[	@Ó@Í#i	[	[	@@i.I"±      # Logs a message to the console and log file to inform
      # the client that Backup is dumping the database
      def function
        Logger.message("STRING")
      end;Fi[	I"3projects//backup_proj/backup/encryptor/base.rb;TiI"≥      # Logs a message to the console and log file to inform
      # the client that Backup is encrypting the archive
      def function
        Logger.message("STRING")
      end;Fi[	@èiMI"∏      # Logs a message to the console and log file to inform
      # the client that Backup is notifying about the process
      def function
        Logger.message("STRING")
      end;Fi[	@&
inI"±          ##
          # Returns a String of file paths and their md5 hashes.
          def function
            Logger.message("STRING")
            `find '#{@directory}STRING`;Fi[	I"require("twitter/tweet");F@i	[	[	@)i	I"çrequire("twitter/error/already_favorited")
require("twitter/error/forbidden")
require("twitter/tweet")
require("twitter/user")
module Twitter;Fi[	@ViI"require("twitter/error/not_found")
require("twitter/list")
require("twitter/tweet")
require("twitter/user")
module Twitter;Fi[	@ëiI"arequire("twitter/api/utils")
require("twitter/tweet")
require("twitter/user")
module Twitter;Fi[	@ºi	I"{require("twitter/api/utils")
require("twitter/cursor")
require("twitter/tweet")
require("twitter/user")
module Twitter;Fi[	@ıI"require("dependencies");Fi	[	[	I"6projects//homebrew_proj/test/test_dependencies.rb;Ti I"brequire("testing_env")
require("dependencies")
class DependenciesTests < Test::Unit::TestCase;Fi [	I"4projects//homebrew_proj/test/test_dependency.rb;Ti I"`require("testing_env")
require("dependencies")
class DependableTests < Test::Unit::TestCase;Fi [	I">projects//homebrew_proj/test/test_dependency_collector.rb;Ti I"Irequire("testing_env")
require("dependencies")
require("extend/set");Fi [	I"5projects//homebrew_proj/test/test_requirement.rb;Ti I"arequire("testing_env")
require("dependencies")
class RequirementTests < Test::Unit::TestCase;Fi [	I"class Formula;F@Ói	[	[	@˝i#I"F  end
end
class Formula
  def function
    @head and @stable.nil?;Fi[	@’iTI"T  end
end
class Formula
  def function
    if (keg_only?.! or ARGV.force?) then;Fi[	@iI"8  end
end
class Formula
  def function
    var = [];Fi[	@√ieI">  end
end
class Formula
  def function
    var = "STRING";Fi[	I"module ClassMethods;F@õi	[	[	@ÇiI"e      end
      
      module ClassMethods
        def function(var)
          var.each do |var|;Fi[	@ñi4I"^    end
    
    module ClassMethods
      def function(var)
        class_eval("STRING");Fi[	@ÂiI"π      included { helper_method("LITERAL", "LITERAL", "LITERAL") }
      
      module ClassMethods
        def function(var)
          var["LITERAL"] ||= "LITERAL" unless var["LITERAL"];Fi[	@‘
i-I"î      end
      
      module ClassMethods
        def function(var)
          find_for_authentication("LITERAL" => (var[token_authentication_key]));Fi[	@—I"# @example;Fi	[	[	@>iI"ó    # Computes the powerset of the given array.
    # This is the set of all subsets of the array.
    #
    # @example
    #   powerset([1, 2, 3]) #=>;Fi[	@¡i@I"Õ      # @param name [Symbol] The key of the default option in your configuration hash.
      # @param value [Object] The value your object defaults to. Nil if not provided.
      #
      # @example
      #;Fi[	@¡i}I"†    # Direct access to the OmniAuth logger, automatically prefixed
    # with this strategy's name.
    #
    # @example
    #   log :warn, "This is a warning.";Fi[	@¡i=I"ú    # to true or by setting `:skip_info` to a Proc that takes a uid and
    # evaluates to true when you would like to skip info.
    #
    # @example
    #;Fi[	I"def self.included(var);FI"var.extend(ClassMethods);Fi	[	[	@ëiI"v  module Configuration
    module Helpers
      def self.included(var)
        var.extend(ClassMethods)
      end;Fi[	@ñi0I"[  
  module Gradient
    def self.included(var)
      var.extend(ClassMethods)
    end;Fi[	@€iI"x      end
      
      def self.included(var)
        var.extend(ClassMethods)
        assert_validations_api!(var);Fi[	@“iI"s  module Failure
    module Thoughtbot
      def self.included(var)
        var.extend(ClassMethods)
      end;Fi[	I"# Path to store backups to;F@ei	[	[	@ÙiI"x      # Server IP Address and FTP port
      ##
      # Path to store backups to
      ##
      # use passive mode?;Fi[	@iI"{      # Server IP Address and SSH port
      ##
      # Path to store backups to
      ##
      # Flag to use local backups;Fi[	@´iI"é      # Server IP Address and SCP port
      ##
      # Path to store backups to
      ##
      # Creates a new instance of the storage object;Fi[	@ÆiI"è      # Server IP Address and SFTP port
      ##
      # Path to store backups to
      ##
      # Creates a new instance of the storage object;Fi[	@]$I"'if var.respond_to?("LITERAL") then;Fi	[	[	I"Eprojects//compass_proj/compass/sass_extensions/functions/urls.rb;TiI"â  
  module StylesheetUrl
    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"]);Fi[	@w$i-I"É  
  module FontUrl
    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"]);Fi[	@w$iFI"Ñ  
  module ImageUrl
    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"]);Fi[	@w$iyI"ç  
  module GeneratedImageUrl
    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"]);Fi[	@&#@‡ i	[	[	@ÙiI"ê      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING"
        @passive_mode ||= false;Fi[	@iI"â      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING"
        @local ||= false;Fi[	@´iI"ú      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING"
        instance_eval(&var) if block_given?;Fi[	@ÆiI"ú      def function(var, var, var)
        super(var, var)
        @port ||= "LITERAL"
        @path ||= "STRING"
        instance_eval(&var) if block_given?;Fi[	I"%assert(ARGV.switch?("STRING").!);F@ä$i	[	[	I".projects//homebrew_proj/test/test_ARGV.rb;Ti(I"¥    assert(ARGV.switch?("STRING"))
    assert(ARGV.switch?("STRING"))
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!);Fi[	@ç$i)I"∂    assert(ARGV.switch?("STRING"))
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!);Fi[	@ç$i*I"∏    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!);Fi[	@ç$i+I"ô    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
    assert(ARGV.switch?("STRING").!)
  end;Fi[	I".#   vehicle.state           # => "parked";F@—i	[	[	@5iﬂI"’    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # 
    # In the above example, no +initialize+ method is defined.  As a result,;Fi[	@5iI"—    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # 
    # Because of the way the inclusion of modules works in Ruby, calling;Fi[	@5iI"ÿ    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # 
    # You may also need to call the +initialize_state_machines+ helper manually;Fi[	@5i/I"ÿ    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # 
    # The above example is also noteworthy because it demonstrates how to avoid;Fi[	I"A# Transfers the archived file to the specified remote server;F@Ói	[	[	@Ùi7I"ç      
      ##
      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package);Fi[	@i9I"x      
      ##
      # Transfers the archived file to the specified remote server
      def function
        begin;Fi[	@´i-I"ç      
      ##
      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package);Fi[	@Æi+I"ç      
      ##
      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package);Fi[	@I"module Generators;Fi	[	[	@èi I"_module Devise
  module Generators
    class DeviseGenerator < Rails::Generators::NamedBase;Fi [	I"Aprojects//devise_proj/generators/devise/install_generator.rb;TiI"srequire("securerandom")
module Devise
  module Generators
    class InstallGenerator < Rails::Generators::Base;Fi[	@6i I"<module Devise
  module Generators
    module OrmHelpers;Fi [	@9i I"Cmodule Devise
  module Generators
    module ViewPathTemplates;Fi [	@—I"# site - The Site.;Fi	[	[	@Ï	iI"ã    # This generator is safe from arbitrary code execution.
    # Generate paginated pages if necessary.
    #
    # site - The Site.
    #;Fi[	@Ï	iI"º    # directories, e.g.: page2/index.html, page3/index.html, etc and adds more
    # site-wide data.
    #
    # site - The Site.
    # page - The index.html Page that requires pagination.;Fi[	@Ô	iI"ì    # Gets/Sets the content of this layout.
    # Initialize a new Layout.
    #
    # site - The Site.
    # base - The String path to the source.;Fi[	@iI"©    # The cache of last modification times [path] -> mtime.
    # Initialize a new StaticFile.
    #
    # site - The Site.
    # base - The String path to the <source>.;Fi[	@õI"var = var.to_s;Fi	[	[	@w$i§I"ô  
  # Emits a path, taking off any leading "./"
  def function(var)
    var = var.to_s
    var = (var["LITERAL"] == "STRING") ? (var["LITERAL"]) : (var);Fi[	@FiI"•      # Don't escape text that's already safe,
      # output is always HTML safe
      def function(var)
        var = var.to_s
        return var if var.html_safe?;Fi[	@·
iI"ù    #               mollit anim id est laborum.
    #               EOS
    def function(var)
      var = var.to_s
      (self["LITERAL", var.length] == var);Fi[	I")projects//homebrew_proj/metafiles.rb;TiI"O  private
  
  def function(var)
    var = var.to_s
    var = var.downcase;Fi[	I"var[QUERY].each do |var|;F@%i	[	[	@2iI"ª      FileUtils.mkdir_p("STRING")
      File.open("STRING", "STRING") { |var| var.puts("STRING") }
      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@@iI"Î      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@∫iI"      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"].gsub("LITERAL", "STRING");Fi[	@MiI"Î      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	I"ovar = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING");FI" FileUtils.mkdir_p("STRING");Fi	[	[	@9iI"Í  module Joomla
    def self.process(var, var, var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var = "STRING";Fi[	@@iI"ﬂ    
    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|;Fi[	@∫iI"ﬂ    
    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|;Fi[	@MiI"ﬂ    
    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|;Fi[	@ÓI"setup_phase;Fi	[	[	@¡i§I"ë    
    # Performs the steps necessary to run the request phase of a strategy.
    def function
      setup_phase
      log("LITERAL", "STRING");Fi[	@¡iΩI"í    
    # Performs the steps necessary to run the callback phase of a strategy.
    def function
      setup_phase
      log("LITERAL", "STRING");Fi[	@¡iÌI"]    end
    
    def function
      setup_phase
      session["STRING"] = request.params;Fi[	@¡i˙I"b    end
    
    def function
      setup_phase
      var = OmniAuth.mock_auth_for(name.to_s);Fi[	@«I"require("keg");Fi	[	[	@’i I"9require("formula")
require("keg")
require("bottles");Fi [	@¨i I"5require("formula")
require("keg")
require("irb");Fi [	@∏i I"6require("formula")
require("keg")
module Homebrew;Fi [	@√iI"Krequire("exceptions")
require("formula")
require("keg")
require("tab");Fi[	I" assert_equal(case MacOS.cat;F@Íi	[	[	@ÙiI"ü    MacOS.bottles_are_supported do
      var = SnowLeopardBottleSpecTestBall.new
      assert_equal(case MacOS.cat
      when "LITERAL" then
        var.bottle;Fi[	@Ùi I"ë      end, var.active_spec)
      var = LionBottleSpecTestBall.new
      assert_equal(case MacOS.cat
      when "LITERAL" then
        var.bottle;Fi[	@niYI"ª    assert_equal("LITERAL", var.bottle.checksum.hash_type)
    assert_equal("LITERAL", var.devel.checksum.hash_type)
    assert_equal(case MacOS.cat
    when "LITERAL" then
      "STRING";Fi[	@niI"ü    var = RevisedBottleSpecTestBall.new
    assert_equal("LITERAL", var.bottle.revision)
    assert_equal(case MacOS.cat
    when "LITERAL" then
      "STRING";Fi[	@I"module Controllers;Fi	[	[	@Âi I":module Devise
  module Controllers
    module Helpers;Fi [	@hi I"?module Devise
  module Controllers
    module Rememberable;Fi [	I"=projects//devise_proj/devise/controllers/scoped_views.rb;Ti I">module Devise
  module Controllers
    module ScopedViews;Fi [	I"<projects//devise_proj/devise/controllers/url_helpers.rb;Ti I"=module Devise
  module Controllers
    module UrlHelpers;Fi [	@
@ri	[	[	@ iYI"ö      def function
        if Devise.paranoid then
          super
        else
          if lock_strategy_enabled?("LITERAL") and attempts_exceeded? then;Fi[	@#iI"©        def self.new(var, var)
          if ["LITERAL", "LITERAL"].all? { |var| var.respond_to?(var) } then
            super
          else
            memory(var, var);Fi[	@##i
I"a    def function(var, var)
      if rack14? then
        super
      else
        @app = var;Fi[	@F
i'I"µ        def function(var, var, var, var)
          if defined? I18n then
            super
          else
            var.errors.add(self.attribute(var), generate_message(var, var));Fi[	I"ohai("STRING");F@/"i	[	[	@$iI"Å  def function
    raise("STRING") unless which("STRING")
    ohai("STRING")
    if @clone.exist? then
      Dir.chdir(@clone) do;Fi[	@$iíI"w  def function
    raise("STRING") unless hgpath
    ohai("STRING")
    if @clone.exist? then
      puts("STRING");Fi[	@$i∫I"x  def function
    raise("STRING") unless bzrpath
    ohai("STRING")
    if @clone.exist? then
      puts("STRING");Fi[	@$iÿI"{  def function
    raise("STRING") unless fossilpath
    ohai("STRING")
    if @clone.exist? then
      puts("STRING");Fi[	@{I"@cmd = "STRING";Fi	[	[	@ÅiI"ã        load_defaults!
        @level ||= false
        instance_eval(&var) if block_given?
        @cmd = "STRING"
        @ext = "STRING";Fi[	@áiI"ã        load_defaults!
        @level ||= false
        instance_eval(&var) if block_given?
        @cmd = "STRING"
        @ext = "STRING";Fi[	@äiI"ã        @best ||= false
        @fast ||= false
        instance_eval(&var) if block_given?
        @cmd = "STRING"
        @ext = "STRING";Fi[	@çiI"ë        @fast ||= false
        @processors ||= false
        instance_eval(&var) if block_given?
        @cmd = "STRING"
        @ext = "STRING";Fi[	@—I"0# See Node#write_to for a list of +options+;Fi	[	[	@µi∆I"í      #
      #   doc.to_xml(:indent => 5, :encoding => 'UTF-8')
      #
      # See Node#write_to for a list of +options+
      def function(var);Fi[	@µi—I"î      #
      #   doc.to_xhtml(:indent => 5, :encoding => 'UTF-8')
      #
      # See Node#write_to for a list of +options+
      def function(var);Fi[	@µi˙I"í      ###
      # Write Node as HTML to +io+ with +options+
      #
      # See Node#write_to for a list of +options+
      def function(var, var);Fi[	@µiI"ì      ###
      # Write Node as XHTML to +io+ with +options+
      #
      # See Node#write_to for a list of +options+
      def function(var, var);Fi[	I"'assert(var.fails_with?("LITERAL"));F@?%i	[	[	@6i"I"æ    var = TestAllCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"));Fi[	@6i#I"∞    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    var.select_compiler;Fi[	@6iFI"≤    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    var.select_compiler;Fi[	@6iOI"Ã    var = TestEvenMoreMixedCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    assert(case MacOS.gcc_42_build_version;Fi[	I"6assert_equal("STRING", Formula.class_s("STRING"));F@J%i	[	[	@niI"≥  
  def function
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"));Fi[	@niI"Ê  def function
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"));Fi[	@niI"    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"));Fi[	@niI"›    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
    assert_equal("STRING", Formula.class_s("STRING"))
  end;Fi[	I"K#   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">;F@ñ$i	[	[	@5iﬁI"î    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # ;Fi[	@5iI"î    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # ;Fi[	@5iI"î    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # ;Fi[	@5i.I"î    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked"
    # ;Fi[	I"$assert_equal("STRING", var.url);FI"=assert_equal(GitDownloadStrategy, var.download_strategy);Fi	[	[	@niÑI"¯    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader);Fi[	@ni∏I"Ó    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader);Fi[	@ni«I"Ó    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader);Fi[	@ni÷I"Ó    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader);Fi[	@
@{i	[	[	@‘iKI"Ç      # @api private
      def function(var)
        super
        instance_eval(&var) if block_given?
        @mode ||= "LITERAL";Fi[	@i)I"Ã        # the block from the user's configuration file is evaluated.
        def function(var)
          super
          instance_eval(&var) if block_given?
          @path = path.sub("LITERAL", "STRING");Fi[	@i I"Ã        # the block from the user's configuration file is evaluated.
        def function(var)
          super
          instance_eval(&var) if block_given?
          @path = path.sub("LITERAL", "STRING");Fi[	@•iI"®        # the block from the user's configuration file is evaluated.
        def function(var)
          super
          instance_eval(&var) if block_given?
        end;Fi[	I"(var.declare("LITERAL", ["LITERAL"]);FI"3var.declare("LITERAL", ["LITERAL", "LITERAL"]);Fi	[	[	@w$iI"∞    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL"])
      end;Fi[	@w$i/I"∞    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL"])
      end;Fi[	@w$iHI"Ë    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL", "LITERAL"]);Fi[	@w$i{I"∞    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL"])
      end;Fi[	I"unless var.empty? then;F@3i	[	[	@√i,I"ß    return unless f.lib.directory?
    var = f.lib.children.select { |var| var.to_s =~ "LITERAL" }
    unless var.empty? then
      opoo("STRING")
      puts("STRING");Fi[	@√i?I"s      var.include?(var.extname).!
    end
    unless var.empty? then
      opoo("STRING")
      puts("STRING");Fi[	@√iKI"∑    return unless f.bin.directory?
    var = f.bin.children.select { |var| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING");Fi[	@√iWI"π    return unless f.sbin.directory?
    var = f.sbin.children.select { |var| (var.directory? or var.executable?.!) }
    unless var.empty? then
      opoo("STRING")
      puts("STRING");Fi[	I"puts(var);FI"!@show_summary_heading = true;Fi	[	[	@√i3I"i      puts("STRING")
      puts("STRING")
      puts(var)
      @show_summary_heading = true
    end;Fi[	@√iCI"i      puts("STRING")
      puts("STRING")
      puts(var)
      @show_summary_heading = true
    end;Fi[	@√iOI"i      puts("STRING")
      puts("STRING")
      puts(var)
      @show_summary_heading = true
    end;Fi[	@√i[I"i      puts("STRING")
      puts("STRING")
      puts(var)
      @show_summary_heading = true
    end;Fi[	@îI"module RSync;Fi	[	[	@iI"Smodule Backup
  module Syncer
    module RSync
      class Base < Syncer::Base;Fi[	@•iI"Lmodule Backup
  module Syncer
    module RSync
      class Local < Base;Fi[	@iI"Kmodule Backup
  module Syncer
    module RSync
      class Pull < Push;Fi[	@iI"Kmodule Backup
  module Syncer
    module RSync
      class Push < Base;Fi[	@—I":# This method is considered part of the `stable` API.;Fi	[	[	@ i&I"∂  # Returns true if the job was queued, nil if the job was rejected by a
  # before_enqueue hook.
  #
  # This method is considered part of the `stable` API.
  def function(var, var);Fi[	@ i3I"ª  # Returns true if the job was queued, nil if the job was rejected by a
  # before_enqueue hook.
  #
  # This method is considered part of the `stable` API.
  def function(var, var, var);Fi[	@ iWI"Â  #   # Removes all jobs of class `UpdateNetworkGraph` with matching args.
  #   Resque.dequeue(GitHub::Jobs::UpdateNetworkGraph, 'repo:135325')
  #
  # This method is considered part of the `stable` API.
  def function(var, var);Fi[	@ ijI"¡  # depending on whether a job can be obtained. You should pass it the
  # precise name of a queue: case matters.
  #
  # This method is considered part of the `stable` API.
  def function(var);Fi[	@
I"$puts("STRING") if ARGV.verbose?;Fi	[	[	@Ri]I"|module ObserverPathnameExtension
  def function
    super
    puts("STRING") if ARGV.verbose?
    $n = ($n + "LITERAL");Fi[	@RicI"^  
  def function
    super
    puts("STRING") if ARGV.verbose?
    $d = ($d + "LITERAL");Fi[	@RinI"J  
  def function
    super
    puts("STRING") if ARGV.verbose?
  end;Fi[	@RisI"J  
  def function
    super
    puts("STRING") if ARGV.verbose?
  end;Fi[	I",assert_equal(var.head, var.active_spec);FI"0assert_version_equal("STRING", var.version);Fi	[	[	@niÇI"≤    ARGV.push("STRING")
    var = SpecTestBall.new
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_equal("STRING", var.url);Fi[	@niµI"≤    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum);Fi[	@niƒI"≤    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum);Fi[	@ni”I"≤    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum);Fi[	@ì#I"É#   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi	[	[	@0iI"æ      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs;Fi[	@0iI"i      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      # @example Return the cursored collection of users following @sferik;Fi[	@0i$I"æ      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs;Fi[	@0i.I"l      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      # @example Return the cursored collection of users @sferik is following;Fi[	@\@ri	[	[	@?i¢I"ä    missing_image!(var, var) unless var
    if (var.unit_str == "STRING") then
      var = var
    else
      var = (var.value - var.left);Fi[	@äimI"u    if (var.nil? or var.kind_of?(Hash)) then
      @cause = "STRING"
      var = var
    else
      @cause = var;Ti[	@0iáI"i          var = true
          var = "STRING"
          var = var
        else
          if var then;Fi[	@##i%I"n    def function(var, var, var)
      if var.is_a?(Class) then
        var = var
      else
        begin;Fi[	@i@ç%i	[	[	@√i2I"v      puts("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @show_summary_heading = true;Fi[	@√iBI"v      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @show_summary_heading = true;Fi[	@√iNI"v      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @show_summary_heading = true;Fi[	@√iZI"v      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @show_summary_heading = true;Fi[	I"((var << "STRING") if var["LITERAL"];F@>i	[	[	@≤iúI"‘        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING")
        var = "STRING"
        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        (var << "STRING") if var["LITERAL"];Fi[	@≤iûI"ó        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        var;Fi[	@≤i¨I"‘        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING")
        var = "STRING"
        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        (var << "STRING") if var["LITERAL"];Fi[	@≤iÆI"ó        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        var;Fi[	I"J# * <tt>:guard</tt> - Whether to guard transitions with the if/unless;FI"=#   conditionals defined for each one.  Default is true.;Fi	[	[	@`iÄI"%    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    def function(var, var);Fi[	@iI"    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # ;Fi[	@i9I"    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # ;Fi[	@5iòI"	    # * <tt>:on</tt> - One or more events to transition on.  If none are
    #   specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # ;Fi[	@—@{i	[	[	@%iI"¨        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do;Fi[	@%iWI"¨        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do;Fi[	@%i{I"¨        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do
        #       event :ignite do;Fi[	@5iåI"ú    #     include DataMapper::Resource
    #     property :id, Serial
    #     
    #     state_machine :initial => :parked do
    #       event :ignite do;Fi[	@
I"K@unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"));Fi	[	[	@$iZI"Õclass CVSDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @co = (HOMEBREW_CACHE + @unique_token);Fi[	@$iÖI"÷class MercurialDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @clone = (HOMEBREW_CACHE + @unique_token);Fi[	@$i≠I"”class BazaarDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @clone = (HOMEBREW_CACHE + @unique_token);Fi[	@$iÀI"”class FossilDownloadStrategy < AbstractDownloadStrategy
  def function(var, var)
    super
    @unique_token = "STRING" unless (var.to_s.empty? or (var == "STRING"))
    @clone = (HOMEBREW_CACHE + @unique_token);Fi[	@ÓI"var = {};Fi	[	[	@Çi¬I"d        end
        
        def function
          var = {}
          ATTRIBUTES.each do |var|;Fi[	I"0projects//devise_proj/devise/failure_app.rb;TiUI"S    end
    
    def function
      var = {}
      var = :"new_#{scope}STRING";Fi[	@ei"I"J    end
    
    def function
      var = {}
      var["STRING"] = [];Fi[	@èibI"‰        # Gets a list of all attributes for the current class, including those
        # that are inherited
        def function
          var = {}
          loaded_superclasses.each { |var| var.merge!(var.instance_attributes) };Fi[	@õ@ài	[	[	@≤ièI"R      
      # @see Base#render
      def function(var)
        var
      end;Fi[	I"8projects//jekyll_proj/jekyll/converters/identity.rb;TiI"9    end
    
    def function(var)
      var
    end;Fi[	@!&iI"9    end
    
    def function(var)
      var
    end;Fi[	@÷i+I"^    
    # Always returns the given set of values
    def function(var)
      var
    end;Fi[	@ÓI"shutup do;Fi	[	[	@ìi I"W  end
  
  def function
    shutup do
      DefaultPatchBall.new("STRING").brew do;Fi[	@ìi*I"T  end
  
  def function
    shutup do
      ListPatchBall.new("STRING").brew do;Fi[	@ìi4I"R  end
  
  def function
    shutup do
      P0PatchBall.new("STRING").brew do;Fi[	@ìi>I"R  end
  
  def function
    shutup do
      P1PatchBall.new("STRING").brew do;Fi[	@ﬂI"'#     include DataMapper::Resource;Fi	[	[	@%iI"y        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     ;Fi[	@%iRI"y        # == Examples
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     ;Fi[	@%ivI"x        # == Example
        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     ;Fi[	@5iâI"z    # For example,
    # 
    #   class Vehicle
    #     include DataMapper::Resource
    #     property :id, Serial;Fi[	I"$var = CompilerSelector.new(var);F@?%i	[	[	@6i!I"¶  def function
    var = TestAllCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"));Fi[	@6i:I"™  def function
    var = TestMixedCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!);Fi[	@6iNI"∞  def function
    var = TestEvenMoreMixedCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"));Fi[	@6ibI"µ  def function
    var = TestBlockWithoutBuildCompilerFailure.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!);Fi[	I")# during the dumping of the database;F@Ói	[	[	@
i}I"æ      ##
      # Builds the MySQL syntax for specifying which tables to dump
      # during the dumping of the database
      def function
        only_tables.join("STRING") unless dump_all?;Fi[	@
iI"´      ##
      # Builds the MySQL syntax for specifying which tables to skip
      # during the dumping of the database
      def function
        skip_tables.map do |var|;Fi[	@
isI"…      ##
      # Builds the PostgreSQL syntax for specifying which tables to dump
      # during the dumping of the database
      def function
        only_tables.map { |var| "STRING" }.join("STRING");Fi[	@
izI"…      ##
      # Builds the PostgreSQL syntax for specifying which tables to skip
      # during the dumping of the database
      def function
        skip_tables.map { |var| "STRING" }.join("STRING");Fi[	I".# which in turn will invoke Syncer::Base.;F@—i	[	[	@i$I"Ô        # Configuration::Syncer::Cloud::CloudFiles
        # are set via a super() call to Cloud::Base,
        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and Cloud specific defaults are set,;Fi[	@iI"Á        # Configuration::Syncer::Cloud::S3
        # are set via a super() call to Cloud::Base,
        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and Cloud specific defaults are set,;Fi[	@•iI"Í        # Configuration::Syncer::RSync::Local
        # are set via a super() call to RSync::Base,
        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and RSync specific defaults are set,;Fi[	@i I"È        # Configuration::Syncer::RSync::Pull
        # are set via a super() call to RSync::Base,
        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and RSync specific defaults are set,;Fi[	I"Í# @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.;FI""# @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.;Fi	[	[	@yi#I";      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :accuracy ('0m') A hint on the "region" in which to search. If a number, then this is a radius in meters, but it can also take a string that is suffixed with ft to specify feet. If coming from a device, in practice, this value is whatever accuracy the device has measuring its location (whether it be coming from a GPS, WiFi triangulation, etc.).;Fi[	@yi6I"\      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :query Free-form text to match against while executing a geo-based query, best suited for finding nearby locations by name.;Fi[	@yiPI"      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :name The name a place is known as.;Fi[	@yihI"T      # @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.
      # @option options [String] :token The token found in the response from {Twitter::API::PlacesAndGeo#places_similar}.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :"attribute:street_address" This option searches for places which have this given street address. There are other well-known and application-specific attributes available. Custom attributes are also permitted.;Fi[	@ê@©#i	[	[	@îiƒI"ã      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten)
        var;Fi[	@îi…I"ã      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten)
        var;Fi[	@îiŒI"ã      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten)
        var;Fi[	@îi”I"ã      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [var.first.strip, var["LITERAL"]].flatten)
        var;Fi[	@ﬂI"#     state_machine do;Fi	[	[	@5i:I"é    # With the default name/attribute and no configuration:
    # 
    #   class Vehicle
    #     state_machine do
    #       event :park do;Fi[	@TiI"ã    # == Examples
    # 
    #   class Vehicle
    #     state_machine do
    #       before_transition any => :parked, :do => lambda {...};Fi[	@Ti*I"h    # == Examples
    # 
    #   class Vehicle
    #     state_machine do
    #       event :ignite;Fi[	@›i„I"j    # == Example
    # 
    #   class Vehicle
    #     state_machine do
    #       event :ignite do;Fi[	@—@U%i	[	[	@5i›I"õ    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked";Fi[	@5iI"õ    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked";Fi[	@5iI"õ    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked";Fi[	@5i-I"õ    #     end
    #   end
    #   
    #   vehicle = Vehicle.new   # => #<Vehicle:0xb7c8dbf8 @state="parked">
    #   vehicle.state           # => "parked";Fi[	@Ó @ri	[	[	@Ïi0I"≠    if self.target.nil? then
      Rails.logger.info("STRING")
      return
    else
      if (self.parent.author == var.person) and self.target_author_signature_valid? then;Fi[	@¯ iVI"Ü    if self.target.nil? then
      Rails.logger.info("STRING")
      return
    else
      if self.target_author_signature_valid? then;Fi[	@i√I"Å      if var.dry_run and var.overwrite then
        puts(var) if var.exist?
        return
      else
        if var.dry_run then;Fi[	@i«I"Ç        if var.dry_run then
          puts(var)
          return
        else
          var.delete if var.overwrite and var.exist?;Fi[	I"!when "STRING", "STRING" then;F@Ôi	[	[	@¶iI"Å    var = var.to_s.downcase
    @elem = case var
    when "STRING", "STRING" then
      "STRING"
    when "STRING", "STRING" then;Fi[	@¶iI"|    when "STRING", "STRING" then
      "STRING"
    when "STRING", "STRING" then
      "STRING"
    when "LITERAL" then;Fi[	@ÜicI"ø          return self.send(var, var) if self.respond_to?(var)
          case var.value.first
          when "STRING", "STRING" then
            "STRING"
          when "STRING", "STRING" then;Fi[	@ÜieI"î          when "STRING", "STRING" then
            "STRING"
          when "STRING", "STRING" then
            "STRING"
          when "STRING" then;Fi[	@«@ei	[	[	@‘iaI"y      #
      # @see #gpg_homedir
      # @return [String]
      ##
      # Set the GnuPG home directory to be used.;Fi[	@‘itI"ß      # The given path will be expanded before use.
      #
      # @return [String]
      ##
      # Specifies a Hash of public key identifiers and their public keys.;Fi[	@‘iÒI"¥      # Use of this option will override the use of {#passphrase_file}.
      #
      # @return [String]
      ##
      # Specifies the passphrase file to use symmetric encryption.;Fi[	@‘iI"{      #   end
      #
      # @return [String]
      ##
      # Configures default accessor values for new class instances.;Fi[	@BI"return "LITERAL";Fi	[	[	@µi¬I"|        var = var[var].ord
        if (var < Utagx) then
          var.putc(var)
          return "LITERAL"
        end;Fi[	@µiŒI"i          end
          var.putc(var)
          var.putc(var)
          return "LITERAL"
        end;Fi[	@µiŸI"s          var.putc(var)
          var.putc(var)
          var.putc(var)
          return "LITERAL"
        end;Fi[	@µiÂI"s          var.putc(var)
          var.putc(var)
          var.putc(var)
          return "LITERAL"
        end;Fi[	I"A# the block from the user's configuration file is evaluated.;F@õi	[	[	@i'I"ƒ        #
        # Once pre-configured defaults and Cloud specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var)
          super;Fi[	@iI"ƒ        #
        # Once pre-configured defaults and Cloud specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var)
          super;Fi[	@•iI"ƒ        #
        # Once pre-configured defaults and RSync specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var)
          super;Fi[	@i#I"ƒ        #
        # Once pre-configured defaults and RSync specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var)
          super;Fi[	@ê@¥#i	[	[	@îiäI"Ç      
      # reduce 20 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", [var["LITERAL"]])
        var;Fi[	@îiI"w      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [var["LITERAL"]])
        var;Fi[	@îi!I"w      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [var["LITERAL"]])
        var;Fi[	@îisI"w      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [var["LITERAL"]])
        var;Fi[	I"var = true;F@ri	[	[	@i,I"ì        if (var == var) then
          log_action("LITERAL", basename(var), var)
          var = true
        else
          if var["LITERAL"] then;Fi[	@jiI"ó      end
      if FONT_TYPES.key?(var) then
        var = true
      else
        var = var.to_s.split("STRING").last.gsub("LITERAL", "STRING").to_sym;Fi[	@hiNI"†        if around_hooks.empty? then
          var.perform(*var)
          var = true
        else
          var = around_hooks.reverse.inject(nil) do |var, var|;Fi[	@˛
iÑI"£        var = var["LITERAL"].filter(var)
        if var["LITERAL"].values.empty? then
          var = true
        else
          var = var["LITERAL"].values.first;Fi[	@!I"S# @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi	[	[	@ëiKI"¬      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[	@ëibI"¬      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[	@ëiwI"1      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :include_rts Specifies that the timeline should include native retweets in addition to regular tweets. Note: If you're using the trim_user parameter in conjunction with include_rts, the retweets will no longer contain a full user object.;Fi[	@ëiâI"¬      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[	@êI")if (var["LITERAL"] == "STRING") then;Fi	[	[	@îiÈI"ª      # reduce 41 omitted
      # reduce 42 omitted
      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          var = ["STRING", "STRING", "STRING", "STRING"];Fi[	@îi¯I"à      end
      
      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          var = Node.new("LITERAL", var);Fi[	@îiI"      end
      
      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          var.unshift("STRING");Fi[	@îiI"{      end
      
      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          (var << "STRING");Fi[	I"var.separator("STRING");F@Í&i	[	[	@i:I"∞    def function(var)
      var.banner = "STRING"
      var.separator("STRING")
      var.separator("STRING")
      var.on("STRING", "STRING", "LITERAL", "STRING", "STRING") do;Fi[	@iUI"ƒ        self.options["LITERAL"] = "LITERAL"
      end
      var.separator("STRING")
      var.separator("STRING")
      var.on("STRING", "STRING", "STRING") { |var| self.options["LITERAL"] = var };Fi[	@i`I"Û      var.on("STRING", "STRING") { self.options["LITERAL"] = "LITERAL" }
      var.on("STRING", "STRING") { |var| self.options["LITERAL"] = var.to_sym }
      var.separator("STRING")
      var.separator("STRING")
      set_project_options(var);Fi[	@icI"ñ      var.separator("STRING")
      set_project_options(var)
      var.separator("STRING")
      var.separator("STRING")
      set_global_options(var);Fi[	I"Ivar.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) };F@ı&i	[	[	@≠iI"y    var.on("STRING", "STRING", "STRING") { |var| self.options["LITERAL"] = var }
    var.on("STRING", "STRING") { |var| self.options["LITERAL"] = var.to_sym }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) };Fi[	@≠iI"q    var.on("STRING", "STRING") { |var| self.options["LITERAL"] = var.to_sym }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) };Fi[	@≠iI"l    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) };Fi[	@≠iI"w    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING") { |var| set_dir_or_path("LITERAL", var) }
    var.on("STRING", "STRING", ["LITERAL", "LITERAL"], "STRING", "STRING") do |var|;Fi[	@ÓI"@aspect = "LITERAL";Fi	[	[	@9iI"{  
  # 410 GONE
  def function
    @aspect = "LITERAL"
    var = params["LITERAL"] ? (params["LITERAL"].to_i) : ("LITERAL");Fi[	@9i6I"u  end
  
  def function
    @aspect = "LITERAL"
    @people = Person.where("LITERAL" => (search_query.downcase));Fi[	@CiI"_  respond_to("LITERAL")
  
  def function
    @aspect = "LITERAL"
    @user = current_user;Fi[	@CiwI"M  end
  
  def function
    @aspect = "LITERAL"
    @user = current_user;Fi[	@ê@
i	[	[	@åi
I"ë      attr_accessor("LITERAL")
      
      def function(var, var, var)
        super
        @manifest = Manifest.new(manifest_file, var) if var;Fi[	@9!iI"u    # 1. name
    # 2. name=value
    def function(var, var, var)
      super
      if var.strip.=~(SYNTAX) then;Fi[	@ÑiI"wmodule Jekyll
  class IncludeTag < Liquid::Tag
    def function(var, var, var)
      super
      @file = var.strip;Fi[	I"2projects//jekyll_proj/jekyll/tags/post_url.rb;TiI"n  
  class PostUrl < Liquid::Tag
    def function(var, var, var)
      super
      @orig_post = var.strip;Fi[	@ê@Ôi	[	[	@ihI"ù    DEVISE_SCOPE_KEYS = ["LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL"]
    
    def function(var, var, var)
      begin
        (var = {};Fi[	@KicI"Á    # @param block [#to_proc] A block that can be yielded to within the template
    # @return [String] The rendered template
    def function(var, var, var)
      begin
      (var = if var.instance_variable_defined?("STRING") then;Fi[	@µiºI"Æ    # If no valid UTF-8 char exists at position i,
    # ucharcopy writes Ustrerr and returns 1.
    def function(var, var, var)
      begin
        (var = (var.length - var);Fi[	@ViI"w      end
      
      def function(var, var, var)
        begin
          (var = Twitter::API::Arguments.new(var);Fi[	@ø#@õi	[	[	@ßiSI"û      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var)
        threaded_tweets_from_response("LITERAL", "STRING", var);Fi[	@ßigI"û      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var)
        threaded_tweets_from_response("LITERAL", "STRING", var);Fi[	@ßiçI"ç      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@ßi®I"ç      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@«@õi	[	[	I"+projects//twitter_proj/twitter/user.rb;Ti1I"Á    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mobile', 'mobile_retina', 'web', 'web_retina', 'ipad', or 'ipad_retina'
    # @return [String]
    def function(var)
      if profile_banner_url? then;Fi[	@,'i;I"
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mobile', 'mobile_retina', 'web', 'web_retina', 'ipad', or 'ipad_retina'
    # @return [String]
    def function(var)
      [@attrs["LITERAL"], var].join("STRING") if profile_banner_url?;Fi[	@,'iII"Ï    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mini', 'normal', 'bigger' or 'original'
    # @return [String]
    def function(var)
      insecure_url(profile_image_url_https(var)) if profile_image_url?;Fi[	@,'iQI"Ú    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mini', 'normal', 'bigger' or 'original'
    # @return [String]
    def function(var)
      resize_profile_image_url(@attrs["LITERAL"], var) if profile_image_url?;Fi[	@t$@v%i	[	[	@w$iI"Ω  module StylesheetUrl
    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL"]);Fi[	@w$i.I"∑  module FontUrl
    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL"]);Fi[	@w$iGI"∏  module ImageUrl
    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL"]);Fi[	@w$izI"¡  module GeneratedImageUrl
    def self.included(var)
      if var.respond_to?("LITERAL") then
        var.declare("LITERAL", ["LITERAL"])
        var.declare("LITERAL", ["LITERAL", "LITERAL"]);Fi[	@e@°$i	[	[	@Ùi6I"t      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function;Fi[	@i8I"t      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function;Fi[	@´i,I"t      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function;Fi[	@Æi*I"t      end
      
      ##
      # Transfers the archived file to the specified remote server
      def function;Fi[	@W
@Ôi	[	[	@*iI"      return if @setup
      case @config["STRING"]
      when "STRING" then
        begin
          (require("redcarpet");Fi[	@*i&I"~          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          require("kramdown");Fi[	@*i.I"Ä          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          (require("rdiscount");Fi[	@*i7I"}          raise(FatalException.new("STRING"))
        end
      when "STRING" then
        begin
          (require("maruku");Fi[	@eI"4# Establishes a connection to the remote server;Fi	[	[	@Ùi&I"`      private
      
      ##
      # Establishes a connection to the remote server
      #;Fi[	@i0I"g      end
      
      ##
      # Establishes a connection to the remote server
      def function;Fi[	@´i"I"      private
      
      ##
      # Establishes a connection to the remote server
      # and yields the Net::SSH connection.;Fi[	@Æi"I"k      private
      
      ##
      # Establishes a connection to the remote server
      def function;Fi[	@e@i$i	[	[	@ÙiI"g      ##
      # Server IP Address and FTP port
      ##
      # Path to store backups to
      ##;Fi[	@iI"g      ##
      # Server IP Address and SSH port
      ##
      # Path to store backups to
      ##;Fi[	@´iI"g      ##
      # Server IP Address and SCP port
      ##
      # Path to store backups to
      ##;Fi[	@ÆiI"h      ##
      # Server IP Address and SFTP port
      ##
      # Path to store backups to
      ##;Fi[	@{@W
i	[	[	@i
I"t    "STRING".undent
  when "STRING", "STRING" then
    "STRING".undent
  when "STRING" then
    "STRING".undent;Fi[	@iI"t    "STRING".undent
  when "STRING", "STRING" then
    "STRING".undent
  when "STRING" then
    "STRING".undent;Fi[	@iI"~    "STRING".undent
  when "STRING", "STRING", "STRING" then
    "STRING".undent
  when "STRING" then
    "STRING".undent;Fi[	@iI"j    "STRING".undent
  when "STRING" then
    "STRING".undent
  when "STRING" then
    "STRING".undent;Fi[	@NI"module AppIntegration;Fi	[	[	I"<projects//compass_proj/compass/app_integration/rails.rb;Ti I"<module Compass
  module AppIntegration
    module Rails;Fi [	@i I"Amodule Compass
  module AppIntegration
    module StandAlone;Fi [	I"Bprojects//compass_proj/compass/app_integration/stand_alone.rb;TiI"}  require("compass/app_integration/stand_alone/#{lib}")
end
module Compass
  module AppIntegration
    module StandAlone;Fi[	I"6projects//compass_proj/compass/app_integration.rb;TiI"mrequire("compass/app_integration/stand_alone")
module Compass
  module AppIntegration
    module Helpers;Fi[	@e@"i	[	[	@Úi I"k      ##
      # Credentials for the specified database
      ##
      # Connectivity options
      ##;Fi[	@
iI"k      ##
      # Credentials for the specified database
      ##
      # Connectivity options
      ##;Fi[	@
iI"k      ##
      # Credentials for the specified database
      ##
      # Connectivity options
      ##;Fi[	@ıiI"k      ##
      # Credentials for the specified database
      ##
      # Connectivity options
      ##;Fi[	@Û@Úi	[	[	@ëi4I"±      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING");Fi[	@ëi7I"•      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.pull!;Fi[	@ëiFI"±      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING");Fi[	@ëiZI"±      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING");Fi[	@e@i"i	[	[	@ÚiI"      ##
      # Name of the database that needs to get dumped
      ##
      # Credentials for the specified database
      ##;Fi[	@
iI"∏      # Name of the database that needs to get dumped
      # To dump all databases, set this to `:all` or leave blank.
      ##
      # Credentials for the specified database
      ##;Fi[	@
iI"      ##
      # Name of the database that needs to get dumped
      ##
      # Credentials for the specified database
      ##;Fi[	@ıiI"ã      ##
      # Name of and path to the database that needs to get dumped
      ##
      # Credentials for the specified database
      ##;Fi[	@ê@ri	[	[	@qiI"t      else
        if var =~ "LITERAL" then
          nil
        else
          if (var.size > "LITERAL") then;Fi[	@ëiI"è      def function(var, var, var)
        if var.nil? then
          nil
        else
          if var.is_a?(Compass::Configuration::Data) then;Fi[	@ÇiìI"î          else
            if inherited_data.nil? then
              nil
            else
              if inherited_data.respond_to?("STRING") then;Fi[	@KiI"Å        case var
        when "LITERAL", nil then
          nil
        else
          MultiJson.decode(var, "LITERAL" => (true));Fi[	@—@i	[	[	@ºiI"j      
      # Returns activity about me
      #
      # @note Undocumented
      # @rate_limited Yes;Fi[	@ºi!I"l      
      # Returns activity by friends
      #
      # @note Undocumented
      # @rate_limited Yes;Fi[	@ºiMI"u      
      # Returns activity summary for a Tweet
      #
      # @note Undocumented
      # @rate_limited Yes;Fi[	@ºi`I"t      
      # Returns activity summary for Tweets
      #
      # @note Undocumented
      # @rate_limited Yes;Fi[	I"rescue => var;F@ii	[	[	@Â	iI"{          self.data = YAML.load($1)
        end)
      rescue => var
        puts("STRING")
      rescue SyntaxError => var;Fi[	@Â	iEI"≥      begin
        self.content = Liquid::Template.parse(self.content).render!(var, var)
      rescue => var
        puts("STRING")
        var.backtrace.each { |var| puts(var) };Fi[	@Â	iRI"ª        begin
          self.output = Liquid::Template.parse(var.content).render!(var, var)
        rescue => var
          puts("STRING")
          var.backtrace.each { |var| puts(var) };Fi[	@Êi)I"à            var.puts(var.at("STRING").inner_text)
          end)
        rescue => var
          puts("STRING")
          puts("STRING");Fi[	I"*require("twitter/error/server_error");F@(i	[	[	@2i I"Grequire("twitter/error/server_error")
module Twitter
  class Error;Fi [	@Ci I"Grequire("twitter/error/server_error")
module Twitter
  class Error;Fi [	@Ii I"Grequire("twitter/error/server_error")
module Twitter
  class Error;Fi [	@Ui I"Grequire("twitter/error/server_error")
module Twitter
  class Error;Fi [	@eI"1# Tells Backup::Compressor::Lzma to compress;Fi	[	[	@äiI"ô      attr_accessor("LITERAL")
      
      ##
      # Tells Backup::Compressor::Lzma to compress
      # better (-9) rather than faster when set to true;Fi[	@äiI"ﬁ      # Tells Backup::Compressor::Lzma to compress
      # better (-9) rather than faster when set to true
      ##
      # Tells Backup::Compressor::Lzma to compress
      # faster (-1) rather than better when set to true;Fi[	@çiI"ô      attr_accessor("LITERAL")
      
      ##
      # Tells Backup::Compressor::Lzma to compress
      # better (-9) rather than faster when set to true;Fi[	@çiI"ﬁ      # Tells Backup::Compressor::Lzma to compress
      # better (-9) rather than faster when set to true
      ##
      # Tells Backup::Compressor::Lzma to compress
      # faster (-1) rather than better when set to true;Fi[	I"'api_accessible("LITERAL") do |var|;F@i	[	[	@iI"h  acts_as_api
  
  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@iI"h  acts_as_api
  
  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@aiI"h  acts_as_api
  
  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@ÈiI"h  acts_as_api
  
  api_accessible("LITERAL") do |var|
    var.add("LITERAL")
    var.add("LITERAL");Fi[	@ÅI"!assert(var.mach_o_bundle?.!);Fi	[	[	@Êi!I"¶    assert(var.dylib?)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!)
    assert_match("LITERAL", file(var));Fi[	@Êi/I"¶    assert(var.dylib?)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!)
    assert_match("LITERAL", file(var));Fi[	@Êi=I"¶    assert(var.dylib?.!)
    assert(var.mach_o_executable?)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!)
    assert_match("LITERAL", file(var));Fi[	@ÊiuI"•    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!)
    assert((var.arch == "LITERAL"));Fi[	@÷I"module ActiveModel;Fi	[	[	@ŸiI"[module StateMachine
  module Integrations
    module ActiveModel
      module Observer;Fi[	@‹iI"`module StateMachine
  module Integrations
    module ActiveModel
      class ObserverUpdate;Fi[	@å
iI"`module StateMachine
  module Integrations
    module ActiveModel
      version("STRING") do;Fi[	@A
iI"bmodule StateMachine
  module Integrations
    module ActiveModel
      def self.included(var);Fi[	I"var = read_file("STRING");FI"/assert(var.include?("STRING").!, "STRING");Fi	[	[	@ìi#I"¿    shutup do
      DefaultPatchBall.new("STRING").brew do
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING");Fi[	@ìi-I"Ω    shutup do
      ListPatchBall.new("STRING").brew do
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING");Fi[	@ìi7I"ª    shutup do
      P0PatchBall.new("STRING").brew do
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING");Fi[	@ìiAI"ª    shutup do
      P1PatchBall.new("STRING").brew do
        var = read_file("STRING")
        assert(var.include?("STRING").!, "STRING")
        assert(var.include?("STRING"), "STRING");Fi[	I"# @see Base#render;F@õi	[	[	@≤iéI"\      include(Base)
      
      # @see Base#render
      def function(var)
        var;Fi[	@≤i∑I"a      include(Base)
      
      # @see Base#render
      def function(var)
        "STRING";Fi[	@≤i¿I"w      include(Base)
      
      # @see Base#render
      def function(var)
        Haml::Helpers.html_escape(var);Fi[	@≤i◊I"t      include(Base)
      
      # @see Base#render
      def function(var)
        Haml::Helpers.preserve(var);Fi[	I"0assert_equal("LITERAL", var.patches.length);F@∫i	[	[	@iI"®  def function
    var = Patches.new("STRING")
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p);Fi[	@iI"¥  def function
    var = Patches.new(["STRING", "STRING"])
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p);Fi[	@i#I"µ  def function
    var = Patches.new("LITERAL" => "STRING")
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p);Fi[	@i*I"µ  def function
    var = Patches.new("LITERAL" => "STRING")
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"]
    assert_equal("LITERAL", var.patch_p);Fi[	@eI";# Logs a message to the console and log file to inform;Fi	[	[	@~iI"ç      end
      
      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is using the compressor;Fi[	@@i+I"ç      end
      
      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is dumping the database;Fi[	@$iI"è      end
      
      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is encrypting the archive;Fi[	@èiJI"î      end
      
      ##
      # Logs a message to the console and log file to inform
      # the client that Backup is notifying about the process;Fi[	I"!# Output is always HTML safe;F@äi	[	[	@FiI"ù      end
      
      # Output is always HTML safe
      def function(var, var)
        Haml::Util.html_safe(find_and_preserve_without_haml_xss(*var, &var));Fi[	@FiI"î      end
      
      # Output is always HTML safe
      def function(var, var)
        Haml::Util.html_safe(preserve_without_haml_xss(*var, &var));Fi[	@Fi!I"ì      end
      
      # Output is always HTML safe
      def function(var, var)
        Haml::Util.html_safe(list_of_without_haml_xss(*var, &var));Fi[	@Fi5I"ò      end
      
      # Output is always HTML safe
      def function(var, var)
        Haml::Util.html_safe(capture_haml_without_haml_xss(*var, &var));Fi[	@a%I"<assert_instance_of(GitDownloadStrategy, var.downloader);Fi	[	[	@niÖI"Ê    assert_version_equal("STRING", var.version)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    ARGV.delete("STRING");Fi[	@niπI"Ò    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head);Fi[	@ni»I"Ò    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head);Fi[	@ni◊I"Ò    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head);Fi[	@—I"2# Returns self, to support chaining of calls.;Fi[[	@µi(I"˘      # Insert +node_or_tags+ before this node (as a sibling).
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns self, to support chaining of calls.
      #;Fi[	@µi4I"¯      # Insert +node_or_tags+ after this node (as a sibling).
      # +node_or_tags+ can be a Nokogiri::XML::Node, a Nokogiri::XML::DocumentFragment, or a string containing markup.
      #
      # Returns self, to support chaining of calls.
      #;Fi[	@µivI"‰      # Swap this Node for +node_or_tags+
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns self, to support chaining of calls.
      #;Fi[	I"(assert_equal("LITERAL", var.length);FI"$assert(var.include?("LITERAL"));Fi[[	I"/projects//homebrew_proj/test/test_utils.rb;TiI"Ã    var = archs_for_command("STRING")
    if `sw_vers -productVersion` =~ "LITERAL" and ($1.to_i >= "LITERAL") then
      assert_equal("LITERAL", var.length)
      assert(var.include?("LITERAL"))
    else;Fi[	@D(iI"‘    else
      if `sw_vers -productVersion` =~ "LITERAL" and ($1.to_i == "LITERAL") then
        assert_equal("LITERAL", var.length)
        assert(var.include?("LITERAL"))
        assert(var.include?("LITERAL"));Fi[	@D(iI"ê        assert(var.include?("LITERAL"))
      else
        assert_equal("LITERAL", var.length)
        assert(var.include?("LITERAL"))
      end;Fi[	I"'var = (HOMEBREW_PREFIX / "STRING");FI"var.write("STRING");Fi[[	@ÊiãI"r  
  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!);Fi[	@ÊiõI"r  
  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!);Fi[	@Êi´I"r  
  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!);Fi[	I" assert_equal([], var.archs);FI"$assert((var.arch == "LITERAL"));Fi[[	@ÊiïI"∞    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs)
    assert((var.arch == "LITERAL"))
    assert_match("LITERAL", file(var));Fi[	@Êi•I"∞    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs)
    assert((var.arch == "LITERAL"))
    assert_match("LITERAL", file(var));Fi[	@ÊiµI"µ    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert_equal([], var.archs)
    assert((var.arch == "LITERAL"))
    assert_no_match("LITERAL", file(var));Fi[	@ÅI"assert(var.mach_o_bundle?);Fi[[	@ÊiKI"¶    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match("LITERAL", file(var));Fi[	@ÊiYI"¶    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match("LITERAL", file(var));Fi[	@ÊigI"¶    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?)
    assert_match("LITERAL", file(var));Fi[	@@≤'i[[	@li7I"ä            var.perform!
            Logger.clear!
          end)
        rescue => var
          Logger.error(Errors::CLIError.wrap(var));Fi[	@Â	iI"          self.content = $POSTMATCH
          self.data = YAML.load($1)
        end)
      rescue => var
        puts("STRING");Fi[	@Êi(I"é            var.puts("STRING")
            var.puts(var.at("STRING").inner_text)
          end)
        rescue => var
          puts("STRING");Fi[	@ÔI"
yield;Fi[[	@#i∂I"A  
  def function
    begin
      yield
    rescue TypeError;Fi[	I"0projects//diaspora_proj/models/jobs/base.rb;TiI"b    
    def self.suppress_annoying_errors(var)
      begin
        yield
      rescue => var;Fi[	@oiõI"s    # that were made to the object involved.
    def function
      begin
        yield
      rescue Exception;Fi[	@’I":assert((TARGET_FOLDER + "STRING").exist?.!, "STRING");Fi[[	@*iI"ª    with_temp_folder do
      TARGET_FOLDER.install("STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
    end;Fi[	@*i?I"˚    with_temp_folder do
      TARGET_FOLDER.install("STRING" => "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING");Fi[	@*iII"3      TARGET_FOLDER.install("STRING" => "STRING", "STRING" => "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING");Fi[	@€$@œ$i[[	@@iI"˜    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|
        var = var["LITERAL"];Fi[	@∫iI"˜    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|
        var = var["LITERAL"];Fi[	@MiI"˜    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var[QUERY].each do |var|
        var = var["LITERAL"];Fi[	I"assert(var.dylib?);F@Äi[[	@ÊiI"í    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	@ÊiI"í    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	@Êi-I"í    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!);Fi[	I"rescue Exception => var;F@"i[[	@√iäI"~      begin
        Keg.new(f.prefix).optlink
      rescue Exception => var
        onoe("STRING")
        puts("STRING");Fi[	@√i–I"c    begin
      var.link
    rescue Exception => var
      onoe("STRING")
      puts("STRING");Fi[	@√iÂI"~    begin
      Keg.new(f.prefix).fix_install_names
    rescue Exception => var
      onoe("STRING")
      puts("STRING");Fi[	I"return var;F@ri[[	I"0projects//backup_proj/backup/cli/helpers.rb;Ti%I"˙          Logger.message(var.lines.map { |var| "STRING" }.join) unless var.empty?
          Logger.warn(var.lines.map { |var| "STRING" }.join) unless var.empty?
          return var
        else
          raise(Errors::CLI::SystemCallError, "STRING");Fi[	@w$iøI"|    var = compute_cache_buster(var, var)
    if var.nil? then
      return var
    else
      if var.is_a?(String) then;Fi[	@©i7I"ñ            return ActionView::NonConcattingString.new(var)
          end
          return var
        else
          capture_without_haml(*var, &var);Fi[	I"var.each do |var|;F@4i[[	@RiI"`  def function(var)
    var = []
    var.each do |var|
      case var
      when Array then;Fi[	@Ri=I"`  def function(var)
    var = []
    var.each do |var|
      case var
      when Array then;Fi[	@ßi/I"°      def function(var, var, var)
        var = Nokogiri::XML::Element.new(var, self, &var)
        var.each do |var|
          case var
          when Hash then;Fi[	@—I"%# @return [Array<Twitter::Size>];Fi[[	@QiI"k    
    # Returns an array of photo sizes
    #
    # @return [Array<Twitter::Size>]
    def function;Fi[	@9iI"u      
      # Returns an array of photo sizes
      #
      # @return [Array<Twitter::Size>]
      def function;Fi[	@ci	I"á  class ProfileBanner < Twitter::Base
    # Returns an array of photo sizes
    #
    # @return [Array<Twitter::Size>]
    def function;Fi[	@ß(I""var = ("LITERAL" + "LITERAL");Fi[[	@ÇiI"©      module ClassMethods
        def function(var)
          var.each do |var|
            var = ("LITERAL" + "LITERAL")
            class_eval("STRING", "STRING", var);Fi[	@ÇiI"Õ        # reader but prefixes the attribute name with "raw_".
        def function(var)
          var.each do |var|
            var = ("LITERAL" + "LITERAL")
            class_eval("STRING", "STRING", var);Fi[	@ÇiRI"∑          inherited_reader(*var)
          inherited_writer(*var)
          var.each do |var|
            var = ("LITERAL" + "LITERAL")
            class_eval("STRING", "STRING", var);Fi[	I"var = UpdaterMock.new;F@Úi[[	@ëi2I"ò  def function
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING");Fi[	@ëiDI"•    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING");Fi[	@ëiXI"•    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING");Fi[	@„@ã(i[[	@ÊiI"â    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!);Fi[	@ÊiI"â    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!);Fi[	@Êi,I"á    assert(var.x86_64?)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?)
    assert(var.mach_o_executable?.!);Fi[	@Ú@Úi[[	@ëi5I"¬      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING");Fi[	@ëiGI"¬      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING");Fi[	@ëi[I"¬      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING");Fi[	@—I"K# @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline;Fi[[	@ëi'I"‡      
      # Returns the 20 most recent Tweets posted by the specified user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.;Fi[	@ëi?I"‚      
      # Returns the 20 most recent retweets posted by the specified user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.;Fi[	@ëiWI"Á      
      # Returns the 20 most recent retweets posted by the authenticating user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.;Fi[	I"url("STRING");FI"sha1("STRING");Fi[[	@%iÇI"M  
  devel do
    url("STRING")
    sha1("STRING")
    version("STRING");Fi[	@%iâI"Q  bottle do
    version("STRING")
    url("STRING")
    sha1("STRING")
  end;Fi[	@%iôI">  
  bottle do
    url("STRING")
    sha1("STRING")
  end;Fi[	@—I"E# @see https://dev.twitter.com/docs/api/1.1/get/friendships/show;Fi[[	@0iŒI"Ω      
      # Returns detailed information about the relationship between two users
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes;Fi[	@0iËI"Æ      
      # Test for the existence of friendship between two users
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes;Fi[	@0iI"˙      
      # Returns a cursored collection of user objects for every user the specified user is following (otherwise known as their "friends").
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes;Fi[	I"8# Returns the parsed value and any trailing tokens.;F@õi[[	@µi+I"º    
    # Parses a "value" in the sense of RFC 4627.
    # Returns the parsed value and any trailing tokens.
    def function(var)
      raise(Error, "STRING") if (var.length < "LITERAL");Fi[	@µi<I"§    
    # Parses an "object" in the sense of RFC 4627.
    # Returns the parsed value and any trailing tokens.
    def function(var)
      var = eat("STRING", var);Fi[	@µiWI"£    
    # Parses an "array" in the sense of RFC 4627.
    # Returns the parsed value and any trailing tokens.
    def function(var)
      var = eat("STRING", var);Fi[	@—I",#   machine = StateMachine.new(Vehicle);Fi[[	@›iïI"¥    # 
    # == Example
    # 
    #   machine = StateMachine.new(Vehicle)
    #   StateMachine::Transition.new(Vehicle.new, machine, :park, :parked, :parked).loopback?   # => true;Fi[	@›i£I"≠    # 
    # == Example
    # 
    #   machine = StateMachine.new(Vehicle)
    #   transition = StateMachine::Transition.new(Vehicle.new, machine, :ignite, :parked, :idling);Fi[	@›iòI"≠    # 
    # == Example
    # 
    #   machine = StateMachine.new(Vehicle)
    #   transition = StateMachine::Transition.new(Vehicle.new, machine, :ignite, :parked, :idling);Fi[	I"assert(var.universal?);F@Ji[[	@ÊiI"|  def function
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	@Êi6I"|  def function
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	@ÊiDI"|  def function
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!)
    assert(var.x86_64?.!);Fi[	I"F["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];F@W
i[[	@µiòI"‘        strtok(var)
      when Spc then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	@µiöI"        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	@µiúI"        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	@—I".#   states = Vehicle.state_machine.states;Fi[[	@YiI"ß    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   vehicle = Vehicle.new               # => #<Vehicle:0xb7c464b0 @state="parked">;Fi[	@Yi.I"^    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   ;Fi[	@YiII"^    #     end
    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   ;Fi[	I"*raise(Utf8Error) if (var < "LITERAL");FI"%var = var[(var + "LITERAL")].ord;Fi[[	@µi∆I"         end
        raise(Utf8Error) if (var < Utag2)
        raise(Utf8Error) if (var < "LITERAL")
        var = var[(var + "LITERAL")].ord
        raise(Utf8Error) if ((var < Utagx) or (Utag2 <= var));Fi[	@µi—I"ª          return "LITERAL"
        end
        raise(Utf8Error) if (var < "LITERAL")
        var = var[(var + "LITERAL")].ord
        raise(Utf8Error) if ((var < Utagx) or (Utag2 <= var));Fi[	@µi‹I"ª          return "LITERAL"
        end
        raise(Utf8Error) if (var < "LITERAL")
        var = var[(var + "LITERAL")].ord
        raise(Utf8Error) if ((var < Utagx) or (Utag2 <= var));Fi[	@"@	)i[[	@ÊiI"íclass MachOPathnameTests < Test::Unit::TestCase
  def function
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!);Fi[	@Êi5I"j  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!);Fi[	@ÊiCI"j  
  def function
    var = Pathname.new("STRING")
    assert(var.universal?)
    assert(var.i386?.!);Fi[	@%)I":raise(Utf8Error) if ((var < Utagx) or (Utag2 <= var));Fi[[	@µi«I"‹        raise(Utf8Error) if (var < Utag2)
        raise(Utf8Error) if (var < "LITERAL")
        var = var[(var + "LITERAL")].ord
        raise(Utf8Error) if ((var < Utagx) or (Utag2 <= var))
        if (var < Utag3) then;Fi[	@µi“I"æ        end
        raise(Utf8Error) if (var < "LITERAL")
        var = var[(var + "LITERAL")].ord
        raise(Utf8Error) if ((var < Utagx) or (Utag2 <= var))
        if (var < Utag4) then;Fi[	@µi›I"æ        end
        raise(Utf8Error) if (var < "LITERAL")
        var = var[(var + "LITERAL")].ord
        raise(Utf8Error) if ((var < Utagx) or (Utag2 <= var))
        if (var < Utag5) then;Fi[	@—I"*#     def initialize(attributes = {});Fi[[	@5i˚I"g    #       ...
    #     end
    #     
    #     def initialize(attributes = {})
    #       ...;Fi[	@5iI"g    #       ...
    #     end
    #     
    #     def initialize(attributes = {})
    #       ...;Fi[	@5i%I"u    #       ...
    #     end
    #     
    #     def initialize(attributes = {})
    #       @state = 'idling';Fi[	@—I"#   vehicle = Vehicle.new;Fi[[	@5i®I"Ä    #     end
    #   end
    #   
    #   vehicle = Vehicle.new
    #   vehicle.state                             # => "parked";Fi[	@›i∂I"ü    #     end
    #   end
    #   
    #   vehicle = Vehicle.new
    #   transition = StateMachine::Transition.new(vehicle, machine, :ignite, :parked, :idling);Fi[	@›iÍI"≠    #     end
    #   end
    #   
    #   vehicle = Vehicle.new
    #   transition = StateMachine::Transition.new(vehicle, Vehicle.state_machine, :ignite, :parked, :idling);Fi[	@—I"O# See +before_transition+ for a description of the possible configurations;Fi[[	@%iFI"ﬁ        # performed so long as the given configuration options match the
        # transition.
        # 
        # See +before_transition+ for a description of the possible configurations
        # for defining callbacks.;Fi[	@%ijI"°        #     end
        #   end
        # 
        # See +before_transition+ for a description of the possible configurations
        # for defining callbacks.;Fi[	@%iáI"“        #     end
        #   end
        # 
        # See +before_transition+ for a description of the possible configurations
        # for defining callbacks.  *Note* however that you cannot define the state;Fi[	@W
@)i[[	@µiôI"Ÿ      when Spc then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then;Fi[	@µiõI"ﬁ      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then;Fi[	@µiùI"–      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      when "STRING" then
        ["LITERAL", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]]
      else;Fi[	@—I"#   class VehicleObserver;Fi[[	@%i!I"{        #     end
        #   end
        #   
        #   class VehicleObserver
        #     include DataMapper::Observer;Fi[	@%i^I"{        #     end
        #   end
        #   
        #   class VehicleObserver
        #     include DataMapper::Observer;Fi[	@%i}I"á        #     end
        #   end
        #   
        #   class VehicleObserver
        #     after_transition_failure do |transition|;Fi[	I"assert_nil(var.devel);F@∫%i[[	@ni¥I"´    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version);Fi[	@ni√I"´    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version);Fi[	@ni“I"´    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version);Fi[	@—I"#     property :id, Serial;Fi[[	@%iI"ù        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String;Fi[	@%iTI"ù        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String;Fi[	@%ixI"ù        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String;Fi[	@—I"!#   vehicle.state = 'idling';Fi[[	@i)I"n    #   vehicle = Vehicle.new               # => #<Vehicle:0xb7c464b0 @state="parked">
    #   events.valid_for(vehicle)           # => [#<StateMachine::Event name=:ignite transitions=[:parked => :idling]>]
    #   
    #   vehicle.state = 'idling'
    #   events.valid_for(vehicle)           # => [#<StateMachine::Event name=:park transitions=[:idling => :parked]>];Fi[	@iNI"¸    #   vehicle = Vehicle.new                             # => #<Vehicle:0xb7c464b0 @state="parked">
    #   events.transitions_for(vehicle)                   # => [#<StateMachine::Transition attribute=:state event=:ignite from="parked" from_name=:parked to="idling" to_name=:idling>]
    #   
    #   vehicle.state = 'idling'
    #   events.transitions_for(vehicle)                   # => [#<StateMachine::Transition attribute=:state event=:park from="idling" from_name=:idling to="parked" to_name=:parked>];Fi[	@Yi3I"P    #   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">
    #   states.match(vehicle)         # => #<StateMachine::State name=:parked value="parked" initial=true>
    #   
    #   vehicle.state = 'idling'
    #   states.match(vehicle)         # => #<StateMachine::State name=:idling value="idling" initial=true>;Fi[	@i@Ôi[[	@§iI"z      puts("STRING")
      puts("STRING")
      puts("STRING")
      begin
        var.verify_download_integrity(var);Fi[	@ºi^I"Udef function(var, var)
  puts("STRING")
  puts("STRING")
  begin
    var = false;Fi[	@„iI"m      var = var["STRING"]
      var = var["STRING"]
      puts("STRING")
      begin
        var.process;Fi[	@—@{i[[	@iEI"è    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked;Fi[	@iwI"è    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked;Fi[	@5i¢I"è    #         transition :parked => :idling
    #       end
    #       
    #       event :park do
    #         transition :idling => :parked;Fi[	@—I"!# Valid requirement options:;Fi[[	@`iI"    # Finds and builds the next transition that can be performed on the given
    # object.  If no transitions can be made, then this will return nil.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none;Fi[	@iI"ø    
    # Gets the list of events that can be fired on the given object.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none;Fi[	@i1I"¬    
    # Gets the list of transitions that can be run on the given object.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none;Fi[	I"!# Read the YAML frontmatter.;F@—i[[	@Â	iI"u    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.;Fi[	@ñi I"u    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.;Fi[	@ôi>I"u    end
    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.;Fi[	I"assert_nil(var.bottle);F@k)i[[	@ni≥I"ò    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec);Fi[	@ni¬I"ò    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec);Fi[	@ni—I"ò    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec);Fi[	I"assert_nil(var.stable);F@®)i[[	@ni≤I"è    var = HeadOnlySpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel);Fi[	@ni¡I"ó    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel);Fi[	@ni–I"ó    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel);Fi[	I"assert_not_nil(var.head);F@±)i[[	@ni±I"Ñ  def function
    var = HeadOnlySpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle);Fi[	@ni¿I"å  def function
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle);Fi[	@niœI"å  def function
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle);Fi[	@,(I"3assert_instance_of(HeadSoftwareSpec, var.head);Fi[[	@ni∫I"’    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[	@ni…I"’    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[	@niÿI"’    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[	@i@é%i[[	@√iI"ï        unless ORIGINAL_PATHS.include?(var) then
          opoo("STRING")
          puts("STRING")
          @show_summary_heading = true
        end;Fi[	@√iI"n      opoo("STRING")
      puts("STRING")
      puts("STRING")
      @show_summary_heading = true
    end;Fi[	@√i$I"n      opoo("STRING")
      puts("STRING")
      puts("STRING")
      @show_summary_heading = true
    end;Fi[	@iI",ohai(var, var.backtrace) if ARGV.debug?;Fi[[	@√i”I"è      onoe("STRING")
      puts("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?
      @show_summary_heading = true;Fi[	@√iËI"è      onoe("STRING")
      puts("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?
      @show_summary_heading = true;Fi[	@√i˝I"ñ    rescue Exception => var
      opoo("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?
      @show_summary_heading = true;Fi[	I"=# base - The String path to the dir containing the file.;FI".# name - The String filename of the file.;Fi[[	@Â	iI"ó    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #;Fi[	@ñi"I"ó    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #;Fi[	@ôi@I"ó    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #;Fi[	@ﬁ)@—i[[	@Â	iI"ç    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.;Fi[	@ñi#I"ç    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.;Fi[	@ôiAI"ç    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.
    #
    # Returns nothing.;Fi[	@—I"R# If no queue can be inferred this method will raise a `Resque::NoQueueError`;Fi[[	@ i!I"È  # If either of those conditions are met, it will use the value obtained
  # from performing one of the above operations to determine the queue.
  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`
  #;Fi[	@ iGI"È  # If either of those conditions are met, it will use the value obtained
  # from performing one of the above operations to determine the queue.
  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`
  #;Fi[	@ iqI"ó  
  # Validates if the given klass could be a valid Resque job
  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`
  #;Fi[	I"I# Yields to the block the compressor command and filename extension.;F@Ói[[	@~iI"      
      ##
      # Yields to the block the compressor command and filename extension.
      def function
        log!;Fi[	@äiI"∫      
      ##
      # Yields to the block the compressor command and filename extension.
      def function
        Backup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"));Fi[	@çi$I"∫      
      ##
      # Yields to the block the compressor command and filename extension.
      def function
        Backup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"));Fi[	I""assert_nil(var.head.checksum);F@`%i[[	@ni∑I"ﬁ    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy);Fi[	@ni∆I"ﬁ    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy);Fi[	@ni’I"ﬁ    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy);Fi[	I"$# Returns the formatted String.;F@õi[[	@{i+I"}    # date - The Time to format.
    #
    # Returns the formatted String.
    def function(var)
      var.strftime("STRING");Fi[	@{i9I"|    #   # => "2011-04-24T20:34:46+08:00"
    #
    # Returns the formatted String.
    def function(var)
      var.xmlschema;Fi[	@{isI"    #   # => "apples, oranges, and grapes"
    #
    # Returns the formatted String.
    def function(var)
      var = "STRING";Fi[	I"@site = var;FI"@base = var;Fi[[	@Ô	iI"à    # name - The String filename of the post file.
    def function(var, var, var)
      @site = var
      @base = var
      @name = var;Fi[	@ñiI"á    # name - The String filename of the file.
    def function(var, var, var, var)
      @site = var
      @base = var
      @dir = var;Fi[	@iI"á    # name - The String filename of the file.
    def function(var, var, var, var)
      @site = var
      @base = var
      @dir = var;Fi[	@—I"#   class MyStrategy;Fi[[	@¡i&I"í      #
      # @example Using a yield to configure the default options.
      #
      #   class MyStrategy
      #     include OmniAuth::Strategy;Fi[	@¡i0I"ë      #
      # @example Using a hash to configure the default options.
      #
      #   class MyStrategy
      #     include OmniAuth::Strategy;Fi[	@¡iBI"g      #
      # @example
      #
      #   class MyStrategy
      #     include OmniAuth::Strategy;Fi[	@?%I")assert(var.fails_with?("LITERAL").!);Fi[[	@6iI"¥    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    var.select_compiler;Fi[	@6i;I"¬    var = TestMixedCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"));Fi[	@6icI"œ    var = TestBlockWithoutBuildCompilerFailure.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL").!);Fi[	@?%I"var.select_compiler;Fi[[	@6i$I"√    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    var.select_compiler
    assert_equal(MacOS.default_compiler, ENV.compiler);Fi[	@6i=I"∏    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"))
    var.select_compiler
    assert_equal("LITERAL", ENV.compiler);Fi[	@6iGI"∏    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    var.select_compiler
    assert_equal("LITERAL", ENV.compiler);Fi[	@&*@?%i[[	@6iI"∫    var = TestLLVMFailure.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!);Fi[	@6i<I"≤    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"))
    var.select_compiler;Fi[	@6iEI"∆    var = TestMoreMixedCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"));Fi[	@ª%@*i[[	@ni∂I"ª    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url);Fi[	@ni≈I"ª    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url);Fi[	@ni‘I"ª    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url);Fi[	@ÔI"(require("rubygems");Fi[[	I"0projects//compass_proj/compass/validator.rb;Ti I"Abegin
  (require("rubygems")
  require("compass-validator"));Fi [	@ºiRI"k  if can_use_readline? then
    begin
    begin
      (require("rubygems")
      require("ruby-debug");Fi[	@	i>I"ARGV.extend(HomebrewArgvExtension)
ENV.extend(HomebrewEnvExtension)
begin
  (require("rubygems")
  require("mocha/setup"));Fi[	@L(@ i[[	@ÊiåI"Ç  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!);Fi[	@ÊiúI"Ç  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!);Fi[	@Êi¨I"Ç  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!);Fi[	I";var.instance_eval { @version = Version.new("STRING") };FI"Gvar.active_spec.instance_eval { @version = Version.new("STRING") };Fi[[	@˚iNI"     require("cmd/cleanup")
    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") }
    var = TestBall.new;Fi[	@˚iQI"ˆ    var.active_spec.instance_eval { @version = Version.new("STRING") }
    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") }
    var = TestBall.new;Fi[	@˚iTI"Ô    var.active_spec.instance_eval { @version = Version.new("STRING") }
    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") }
    nostdout do;Fi[	@C@Z*i[[	@˚iMI"¬  def function
    require("cmd/cleanup")
    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") };Fi[	@˚iPI"    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") }
    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") };Fi[	@˚iSI"    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") }
    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") };Fi[	@I"var.puts(var.to_yaml);Fi[[	I"5projects//jekyll_proj/jekyll/migrators/marley.rb;TiI"“        var = File.dirname(var).split("LITERAL").last.gsub("LITERAL", "STRING")
        var = "STRING"
        File.open("STRING", "STRING") do |var|
          var.puts(var.to_yaml)
          var.puts("STRING");Fi[	@oiI"¬        var = "STRING"
        var = { "STRING" => "STRING", "STRING" => (var.title) }
        File.open("STRING", "STRING") do |var|
          var.puts(var.to_yaml)
          var.puts("STRING");Fi[	@Êi$I"¶        begin
          (FileUtils.mkdir_p("STRING")
          File.open("STRING", "STRING") do |var|
            var.puts(var.to_yaml)
            var.puts("STRING");Fi[	@•@•i[[	@2i.I"√            FileUtils.mkdir_p(var["LITERAL"])
            File.open("STRING", "STRING") do |var|
              var.puts("STRING")
              var.puts("STRING")
              var.puts("STRING");Fi[	@2i/I"∂            File.open("STRING", "STRING") do |var|
              var.puts("STRING")
              var.puts("STRING")
              var.puts("STRING")
              var.puts("STRING");Fi[	@2i0I"ì              var.puts("STRING")
              var.puts("STRING")
              var.puts("STRING")
              var.puts("STRING")
            end;Fi[	@—@
*i[[	@{i*I"k    #
    # date - The Time to format.
    #
    # Returns the formatted String.
    def function(var);Fi[	@{i8I"å    #   date_to_xmlschema(Time.now)
    #   # => "2011-04-24T20:34:46+08:00"
    #
    # Returns the formatted String.
    def function(var);Fi[	@{irI"¨    #   array_to_sentence_string(["apples", "oranges", "grapes"])
    #   # => "apples, oranges, and grapes"
    #
    # Returns the formatted String.
    def function(var);Fi[	I")def self.process(var, var, var, var);F@⁄$i[[	@@iI"’    QUERY = "STRING"
    
    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING");Fi[	@∫iI"’    QUERY = "STRING"
    
    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING");Fi[	@MiI"’    QUERY = "STRING"
    
    def self.process(var, var, var, var)
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING");Fi[	@—@›)i[[	@Â	iI"ñ    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.;Fi[	@ñi!I"ñ    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.;Fi[	@ôi?I"ñ    
    # Read the YAML frontmatter.
    #
    # base - The String path to the dir containing the file.
    # name - The String filename of the file.;Fi[	@l*@•i[[	@o*i I"¶        var = "STRING"
        File.open("STRING", "STRING") do |var|
          var.puts(var.to_yaml)
          var.puts("STRING")
          var.puts("STRING") if var;Fi[	@oiI"œ        var = { "STRING" => "STRING", "STRING" => (var.title) }
        File.open("STRING", "STRING") do |var|
          var.puts(var.to_yaml)
          var.puts("STRING")
          var.puts(var.description);Fi[	@Êi%I"           (FileUtils.mkdir_p("STRING")
          File.open("STRING", "STRING") do |var|
            var.puts(var.to_yaml)
            var.puts("STRING")
            var.puts(var.at("STRING").inner_text);Fi[	I"# Obtain destination path.;F@—i[[	@ñiuI"k    end
    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.;Fi[	@ôi¡I"k    end
    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.;Fi[	@iI"k    end
    
    # Obtain destination path.
    #
    # dest - The String path to the destination dir.;Fi[	@G$I"J# Attempt to find a user by its email. If a record is found, send new;Fi[[	@1
iºI"Ÿ      end
      
      module ClassMethods
        # Attempt to find a user by its email. If a record is found, send new
        # confirmation instructions to it. If not, try searching for a user by unconfirmed_email;Fi[	@ iÅI"∆      end
      
      module ClassMethods
        # Attempt to find a user by its email. If a record is found, send new
        # unlock instructions to it. If not user is found, returns a new user;Fi[	@—
iUI"»      end
      
      module ClassMethods
        # Attempt to find a user by its email. If a record is found, send new
        # password instructions to it. If user is not found, returns a new user;Fi[	I"require_all("STRING");F@±*i[[	@
i%I"|require("jekyll/generator")
require("jekyll/command")
require_all("STRING")
require_all("STRING")
require_all("STRING");Fi[	@
i&I"vrequire("jekyll/command")
require_all("STRING")
require_all("STRING")
require_all("STRING")
require_all("STRING");Fi[	@
i'I"jrequire_all("STRING")
require_all("STRING")
require_all("STRING")
require_all("STRING")
module Jekyll;Fi[	I"+var = var.value["LITERAL"].value.first;FI",var.value["LITERAL"].value = ["STRING"];Fi[[	@i*I"\        var.each do |var|
          if var.value["LITERAL"].value["LITERAL"].value["LITERAL"] =~ "LITERAL" then
            var = var.value["LITERAL"].value.first
            var.value["LITERAL"].value = ["STRING"]
            var.value["LITERAL"] = Node.new("LITERAL", [var.value["LITERAL"].value["LITERAL"], Node.new("LITERAL", ["STRING", var])]);Fi[	@i3I"Ö          if ["STRING", "STRING"].include?(var.value["LITERAL"].value.first) then
            var = var.value["LITERAL"].value.first.gsub("LITERAL", "STRING")
            var = var.value["LITERAL"].value.first
            var.value["LITERAL"].value = ["STRING"]
            var.value["LITERAL"] = Node.new("LITERAL", [Node.new("LITERAL", ["STRING"]), Node.new("LITERAL", ["STRING", var])]);Fi[	@i8I">          else
            if ("STRING" == var.value["LITERAL"].value.first) then
              var = var.value["LITERAL"].value.first
              var.value["LITERAL"].value = ["STRING"]
              var.value["LITERAL"] = Node.new("LITERAL", [Node.new("LITERAL", ["STRING"]), Node.new("LITERAL", ["STRING", var])]);Fi[	I" var = [var.first, var.last];F@ài[[	@îiŸI"g      
      def function(var, var, var)
        var = [var.first, var.last]
        var
      end;Fi[	@îiﬁI"g      
      def function(var, var, var)
        var = [var.first, var.last]
        var
      end;Fi[	@îi„I"g      
      def function(var, var, var)
        var = [var.first, var.last]
        var
      end;Fi[	@ı@«i[[	@Ri I"Grequire("testing_env")
require("formula")
require("test/testball");Fi [	@ëiI"eabort if ARGV.include?("STRING")
require("testing_env")
require("formula")
require("cmd/update");Fi[	@Ii I"Grequire("testing_env")
require("formula")
require("test/testball");Fi [	@—I"-#     $icons: sprite-map("icons/*.png");;Fi[[	@?i-I"&  # Creates a Compass::SassExtensions::Sprites::SpriteMap object. A sprite map, when used in a property is the same
  # as calling sprite-url. So the following background properties are equivalent:
  #
  #     $icons: sprite-map("icons/*.png");
  #     background: sprite-url($icons) no-repeat;;Fi[	@?iÜI"Ë  # Returns the position for the original image in the sprite.
  # This is suitable for use as a value to background-position:
  #
  #     $icons: sprite-map("icons/*.png");
  #     background-position: sprite-position($icons, new);;Fi[	@?iêI"Á  # You can adjust the background relative to this position by passing values for
  # `$offset-x` and `$offset-y`:
  #
  #     $icons: sprite-map("icons/*.png");
  #     background-position: sprite-position($icons, new, 3px, -2px);;Fi[	@@#i[[	@ iI"amodule Nokogiri
  module HTML
    module SAX
      class Parser < Nokogiri::XML::SAX::Parser;Fi[	@#iI"omodule Nokogiri
  module HTML
    module SAX
      class ParserContext < Nokogiri::XML::SAX::ParserContext;Fi[	@&iI"Hmodule Nokogiri
  module HTML
    module SAX
      class PushParser;Fi[	@R#I"if var["LITERAL"].! then;Fi[[	@¨i)I"      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({})
          if var["LITERAL"].! then
            var["LITERAL"] = Node::SaveOptions::NO_DECLARATION.|(Node::SaveOptions::NO_EMPTY_TAGS).|(Node::SaveOptions::AS_HTML);Fi[	@¨i7I"      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({})
          if var["LITERAL"].! then
            var["LITERAL"] = Node::SaveOptions::NO_DECLARATION.|(Node::SaveOptions::NO_EMPTY_TAGS).|(Node::SaveOptions::AS_XHTML);Fi[	@∏iˆI"      def function(var)
        if Nokogiri.jruby? then
          var = var.first.is_a?(Hash) ? (var.shift) : ({})
          if var["LITERAL"].! then
            var["LITERAL"] = Node::SaveOptions::NO_DECLARATION.|(Node::SaveOptions::NO_EMPTY_TAGS).|(Node::SaveOptions::AS_HTML);Fi[	@—I"1#   Backup::Encryptor::GPG.defaults do |enc|;Fi[[	@‘iQI"˜      # just specify this in the defaults. Leading tabs/spaces are stripped
      # before writing the given string to the temporary configuration file.
      #
      #   Backup::Encryptor::GPG.defaults do |enc|
      #     enc.gpg_config = <<-EOF;Fi[	@‘i{I"æ      # to use be setup in {#keys}. The best place to do this is in your defaults
      # in +config.rb+.
      #
      #   Backup::Encryptor::GPG.defaults do |enc|
      #     enc.keys = {};Fi[	@‘iˇI"ı      # but wish to use {#passphrase_file} with a specific {Model}, be sure
      # to clear {#passphrase} within that model's configuration.
      #
      #   Backup::Encryptor::GPG.defaults do |enc|
      #     enc.passphrase = 'secret phrase';Fi[	@ä@ÿi[[	@µiMI"z      # XInclude end type
      # DOCB document node type
      def function(var, var)
        # do nothing
      end;Fi[	@Äi I"”        # * +attrs+ are an assoc list of namespaces and attributes, e.g.:
        #     [ ["xmlns:foo", "http://sample.net"], ["size", "large"] ]
        def function(var, var)
          # do nothing
        end;Fi[	@ÄilI"ß        # +name+ is the target of the instruction
        # +content+ is the value of the instruction
        def function(var, var)
          # do nothing
        end;Fi[	@Í#I"=File.open(File.join(local_path, var), "STRING") do |var|;Fi[[	@˛i2I"
        var = remote_path_for(@package)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |var|
            connection.put_object(container, File.join(var, var), var);Fi[	@iTI"˝        var = remote_path_for(@package)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |var|
            connection.put_file(File.join(var, var), var);Fi[	@i:I"        var = directory_for(var, true)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING")
          File.open(File.join(local_path, var), "STRING") do |var|
            var.files.create("LITERAL" => (var), "LITERAL" => (var));Fi[	@Ã"@Íi[[	@[iI"¥    case @language
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	@[iI"–      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING"];Fi[	@[i%I"–      ["STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"]
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	I"#if (version == "LITERAL") then;F@˚i[[	@1iI"R  
  def function
    if (version == "LITERAL") then
      "LITERAL"
    else;Fi[	@1iI"_      "LITERAL"
    else
      if (version == "LITERAL") then
        "LITERAL"
      else;Fi[	@1iI"i        "LITERAL"
      else
        if (version == "LITERAL") then
          "LITERAL"
        else;Fi[	@Í#@ri[[	@Ô!iRI"£      var.run
      if var.success? then
        Logger.message("STRING")
      else
        raise(Errors::Archive::PipelineError, ("STRING" + var.error_messages));Fi[	@
iDI"Æ        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages));Fi[	@
iDI"Æ        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages));Fi[	@ä"@Í#i[[	@Ô!iQI"q      (var << "STRING")
      var.run
      if var.success? then
        Logger.message("STRING")
      else;Fi[	@
iCI"{        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else;Fi[	@
iCI"{        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else;Fi[	@7(@—i[[	@µi)I"Ú      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns self, to support chaining of calls.
      #
      # Also see related method +add_previous_sibling+.;Fi[	@µi5I"Ó      # +node_or_tags+ can be a Nokogiri::XML::Node, a Nokogiri::XML::DocumentFragment, or a string containing markup.
      #
      # Returns self, to support chaining of calls.
      #
      # Also see related method +add_next_sibling+.;Fi[	@µiwI"Â      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #
      # Returns self, to support chaining of calls.
      #
      # Also see related method +replace+.;Fi[	@‘)@é%i[[	@√i‘I"°      puts("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?
      @show_summary_heading = true
      ignore_interrupts { var.unlink };Fi[	@√iÈI"Ç      puts("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?
      @show_summary_heading = true
    end;Fi[	@√i˛I"Ç      opoo("STRING")
      puts("STRING")
      ohai(var, var.backtrace) if ARGV.debug?
      @show_summary_heading = true
    end;Fi[	I"u# +node_or_tags+ can be a Nokogiri::XML::Node, a Nokogiri::XML::DocumentFragment, or a string containing markup.;F@—i[[	@µi3I"˚      ####
      # Insert +node_or_tags+ after this node (as a sibling).
      # +node_or_tags+ can be a Nokogiri::XML::Node, a Nokogiri::XML::DocumentFragment, or a string containing markup.
      #
      # Returns self, to support chaining of calls.;Fi[	@µi?I"⁄      ####
      # Set the inner html for this Node to +node_or_tags+
      # +node_or_tags+ can be a Nokogiri::XML::Node, a Nokogiri::XML::DocumentFragment, or a string containing markup.
      #
      # Returns self.;Fi[	@µiKI"N      ####
      # Set the inner html for this Node +node_or_tags+
      # +node_or_tags+ can be a Nokogiri::XML::Node, a Nokogiri::XML::DocumentFragment, or a string containing markup.
      #
      # Returns the reparented node (if +node_or_tags+ is a Node), or NodeSet (if +node_or_tags+ is a DocumentFragment, NodeSet, or string).;Fi[	I"rescue NotImplementedError;F@ÿi[[	@Oi*I"î            begin
              File.lchown(var.uid, var.gid, var)
            rescue NotImplementedError
              # do nothing
            end;Fi[	@Oi4I"ô            begin
              File.lchmod(var.mode.&("LITERAL"), var)
            rescue NotImplementedError
              # do nothing
            end;Fi[	@Oi>I"å            begin
              File.lchmod(var.mode, var)
            rescue NotImplementedError
              # do nothing
            end;Fi[	@äI"var = var.value;Fi[[	@	iI"ümodule Compass::SassExtensions::Functions::InlineImage
  def function(var, var)
    var = var.value
    var = File.join(Compass.configuration.images_path, var);Fi[	@w$i4I"Ñ    end
    
    def function(var, var)
      var = var.value
      return Sass::Script::String.new("STRING") if absolute_path?(var);Fi[	@w$i{I"û    end
    
    def function(var, var)
      var = var.value
      if var =~ /^#{Regexp.escape(Compass.configuration.http_generated_images_path)}STRING/ then;Fi[	I"Pvar = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil);FI"3var = var.last.is_a?(Hash) ? (var.pop) : (nil);Fi[[	@∏iGI"Ë      # Nokogiri::XML::Node#xpath
      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil)
        var = NodeSet.new(document);Fi[	@∏iXI"ˇ      # For more information see Nokogiri::XML::Node#css
      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil)
        var = NodeSet.new(document);Fi[	@∏ilI"      # For more information see Nokogiri::XML::Node#xpath
      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil)
        var = NodeSet.new(document);Fi[	I"MercurialDownloadStrategy;F@Íi[[	@$i I"ä      GitDownloadStrategy
    when "LITERAL" then
      MercurialDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy;Fi[	@$iI"ç      FossilDownloadStrategy
    when "LITERAL" then
      MercurialDownloadStrategy
    when "LITERAL" then
      SubversionDownloadStrategy;Fi[	@$i%I"ã      GitDownloadStrategy
    when "LITERAL" then
      MercurialDownloadStrategy
    when "LITERAL" then
      NoUnzipCurlDownloadStrategy;Fi[	I"GitDownloadStrategy;F@Íi[[	@$i¸I"Ä      BazaarDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy;Fi[	@$i˛I"É      GitDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then
      MercurialDownloadStrategy;Fi[	@$i#I"É      CVSDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then
      MercurialDownloadStrategy;Fi[	I"if @spec and @ref then;F@%i[[	@$i=I"x    var = Dir.getwd
    Dir.chdir(@clone) do
      if @spec and @ref then
        ohai("STRING")
        case @spec;Fi[	@$i¢I"√    var = Dir.getwd
    Dir.chdir(@clone) do
      if @spec and @ref then
        ohai("STRING")
        safe_system(hgpath, "STRING", "STRING", "STRING", "STRING", @ref, "STRING", "STRING", var);Fi[	@$i‰I"ö  def function
    safe_system(fossilpath, "STRING", @clone)
    if @spec and @ref then
      ohai("STRING")
      safe_system(fossilpath, "STRING", @ref);Fi[	@V+I" var = NodeSet.new(document);Fi[[	@∏iHI"‡      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil)
        var = NodeSet.new(document)
        var.each do |var|;Fi[	@∏iYI"‹      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil)
        var = NodeSet.new(document)
        each do |var|;Fi[	@∏imI"      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil)
        var = NodeSet.new(document)
        each { |var| var = (var + var.xpath(*(var + [var, var].compact))) };Fi[	I"
chdir;F@Íi[[	@$i^I"æ    when "LITERAL", "LITERAL", "LITERAL", "LITERAL" then
      safe_system("STRING", "STRING", @tarball_path)
      chdir
    when "LITERAL" then
      raise("STRING") unless which("STRING");Fi[	@$ibI"∂      raise("STRING") unless which("STRING")
      safe_system("STRING")
      chdir
    when "LITERAL" then
      safe_system("STRING", "STRING", @tarball_path, File.basename(@url));Fi[	@$ieI"≤    when "LITERAL" then
      safe_system("STRING", "STRING", @tarball_path, File.basename(@url))
      chdir
    when "LITERAL" then
      raise("STRING") unless which("STRING");Fi[	I"document.decorate(var);F@ài[[	@∏iMI"™          var = (var + send((var =~ "LITERAL" ? ("LITERAL") : ("LITERAL")), *(var + [var, var]).compact))
        end
        document.decorate(var)
        var
      end;Fi[	@∏icI"          var = (var + var.xpath(*(var + [var, var].compact)))
        end
        document.decorate(var)
        var
      end;Fi[	@∏ipI"§        var = NodeSet.new(document)
        each { |var| var = (var + var.xpath(*(var + [var, var].compact))) }
        document.decorate(var)
        var
      end;Fi[	I"var.choice("LITERAL") do;F@ii[[	@ºijI"Ç      end
      if has_debugger? then
        var.choice("LITERAL") do
          puts("STRING")
          var.restart { debugger };Fi[	@ºipI"|      end
      if can_use_readline? then
        var.choice("LITERAL") do
          puts("STRING")
          var.restart do;Fi[	@ºi|I"o        end
      end
      var.choice("LITERAL") do
        puts("STRING")
        interactive_shell(var);Fi[	I"+if @compilers.include?("LITERAL") then;F@˚i[[	@iiI"Ä    ENV.send(case ENV.compiler
    when "LITERAL" then
      if @compilers.include?("LITERAL") then
        "LITERAL"
      else;Fi[	@irI"q        "LITERAL"
      else
        if @compilers.include?("LITERAL") then
          "LITERAL"
        else;Fi[	@i|I"q        "LITERAL"
      else
        if @compilers.include?("LITERAL") then
          "LITERAL"
        else;Fi[	I"HOMEBREW_REPOSITORY.cd do;F@¬(i[[	@ëi1I"s  
  def function
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING");Fi[	@ëiCI"á  def function
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING");Fi[	@ëiWI"á  def function
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new
      var.in_repo_expect("STRING");Fi[	I"require("omniauth");FI"module OmniAuth;Fi[[	@##i I"Jrequire("omniauth")
module OmniAuth
  class Builder < ::Rack::Builder;Fi [	@æi I"5require("omniauth")
module OmniAuth
  class Form;Fi [	I"9projects//omni_proj/omniauth/strategies/developer.rb;Ti I"<require("omniauth")
module OmniAuth
  module Strategies;Fi [	@äI"var = self;Fi[[	@≤ixI"¬      # @raise [Haml::Error] if none of \{#compile}, \{#render}, and
      #   \{#render_with_options} are overridden
      def function(var, var)
        var = self
        var.instance_eval do;Fi[	@-iI"c  #
  # Returns an array
  def function(var, var)
    var = self
    if var.has_key?(var) then;Fi[	@M
iI"°      
      # Creates a new named scope with the given name
      def function(var, var)
        var = self
        owner_class.def_dataset_method(var) do |var|;Fi[	@äI"var = prep_opts(var, var);Fi[[	@\iI"n  end
  
  def function(var, var)
    var = prep_opts(var, var)
    var = visible_shareable_ids(var, var);Fi[	@\iI"g  end
  
  def function(var, var)
    var = prep_opts(var, var)
    visible_ids_from_sql(var, var);Fi[	@\iI"t  
  # @return [Array<Integer>]
  def function(var, var)
    var = prep_opts(var, var)
    var["LITERAL"] = var;Fi[	I"-dump_formula_report("LITERAL", "STRING");F@Õ+i[[	@!ivI"©class Report < Hash
  def function
    dump_formula_report("LITERAL", "STRING")
    dump_formula_report("LITERAL", "STRING")
    dump_formula_report("LITERAL", "STRING");Fi[	@!iwI"¬  def function
    dump_formula_report("LITERAL", "STRING")
    dump_formula_report("LITERAL", "STRING")
    dump_formula_report("LITERAL", "STRING")
    dump_formula_report("LITERAL", "STRING");Fi[	@!ixI"π    dump_formula_report("LITERAL", "STRING")
    dump_formula_report("LITERAL", "STRING")
    dump_formula_report("LITERAL", "STRING")
    dump_formula_report("LITERAL", "STRING")
  end;Fi[	@*I"%#     include OmniAuth::Strategy;Fi[[	@¡i'I"í      # @example Using a yield to configure the default options.
      #
      #   class MyStrategy
      #     include OmniAuth::Strategy
      #;Fi[	@¡i1I"™      # @example Using a hash to configure the default options.
      #
      #   class MyStrategy
      #     include OmniAuth::Strategy
      #     configure foo: 'bar';Fi[	@¡iCI"g      # @example
      #
      #   class MyStrategy
      #     include OmniAuth::Strategy
      #;Fi[	@áI"+var = eval(IO.read(var), binding, var);Fi[[	@Úi I"wvar = "STRING"
var = eval(IO.read(var), binding, var)
var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL");Fi [	I"Sprojects//state_machine_proj/state_machine/integrations/mongo_mapper/locale.rb;Ti I"wvar = "STRING"
var = eval(IO.read(var), binding, var)
var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL");Fi [	I"Nprojects//state_machine_proj/state_machine/integrations/mongoid/locale.rb;Ti I"wvar = "STRING"
var = eval(IO.read(var), binding, var)
var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL");Fi [	I"var.merge!(var.report);FI""assert(var.expectations_met?);Fi[[	@ëi;I"Ä      var.pull!
      var = Report.new
      var.merge!(var.report)
      assert(var.expectations_met?)
      assert(var.empty?);Fi[	@ëiMI"ö      var.pull!
      var = Report.new
      var.merge!(var.report)
      assert(var.expectations_met?)
      assert(var.select_formula("LITERAL").empty?);Fi[	@ëiaI"Ø      var.pull!
      var = Report.new
      var.merge!(var.report)
      assert(var.expectations_met?)
      assert_equal(["STRING", "STRING"], var.select_formula("LITERAL"));Fi[	I"var.pull!;FI"var = Report.new;Fi[[	@ëi9I"ì      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING")
      var.pull!
      var = Report.new
      var.merge!(var.report);Fi[	@ëiKI"ò      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var)
      var.pull!
      var = Report.new
      var.merge!(var.report);Fi[	@ëi_I"ò      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var)
      var.pull!
      var = Report.new
      var.merge!(var.report);Fi[	@ı+@Í+i[[	@ëi:I"ä      var.in_repo_expect("STRING")
      var.pull!
      var = Report.new
      var.merge!(var.report)
      assert(var.expectations_met?);Fi[	@ëiLI"è      var.in_repo_expect("STRING", var)
      var.pull!
      var = Report.new
      var.merge!(var.report)
      assert(var.expectations_met?);Fi[	@ëi`I"è      var.in_repo_expect("STRING", var)
      var.pull!
      var = Report.new
      var.merge!(var.report)
      assert(var.expectations_met?);Fi[	I"¢Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }));F@,i[[	I"7projects//rescue_proj/resque/server/test_helper.rb;TiI"      
      def function
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }));Fi[	@	,iI"æ      def function
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }));Fi[	@	,iI"µ        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
        Resque::Failure.create("LITERAL" => (Exception.new), "LITERAL" => (Resque::Worker.new("LITERAL")), "LITERAL" => "STRING", "LITERAL" => ({ "STRING" => "STRING" }))
      end;Fi[	I"if var.symlink? then;F@Ôi[[	@Oi'I"Ø        File.utime(var.atime, var.mtime, var) if var.symlink?.!
        begin
          if var.symlink? then
            begin
              File.lchown(var.uid, var.gid, var);Fi[	@Oi1I"ê          end
        rescue Errno::EPERM
          if var.symlink? then
            begin
              File.lchmod(var.mode.&("LITERAL"), var);Fi[	@Oi;I"y          end
        else
          if var.symlink? then
            begin
              File.lchmod(var.mode, var);Fi[	@äI"(@html << "STRING");Fi[[	@æiI"Q    end
    
    def function(var, var)
      (@html << "STRING")
      self;Fi[	@æi I"Q    end
    
    def function(var, var)
      (@html << "STRING")
      self;Fi[	@æiAI"Q    end
    
    def function(var, var)
      (@html << "STRING")
      self;Fi[	I"$n = "LITERAL";FI"$d = "LITERAL";Fi[[	I")projects//homebrew_proj/cmd/prune.rb;TiI"f  # certain filesystem actions.
  def function
    $n = "LITERAL"
    $d = "LITERAL"
    var = [];Fi[	@RiwI",  end
end
$n = "LITERAL"
$d = "LITERAL";Fi[	@ifI"©    begin
      (raise("STRING") if linked_keg_record.directory?
      $n = "LITERAL"
      $d = "LITERAL"
      var = ["STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	I"case var.length;F@Íi[[	@ÚidI"d  
  def function(var, var, var)
    case var.length
    when "LITERAL" then
      # do nothing;Fi[	@$iuI"m  def function
    var = Dir["STRING"]
    case var.length
    when "LITERAL" then
      raise("STRING");Fi[	@{ivI"p    def function(var)
      var = "STRING"
      case var.length
      when "LITERAL" then
        "STRING";Fi[	@áI"var[var].each do |var|;Fi[[	@9iI"·      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      FileUtils.mkdir_p("STRING")
      var = "STRING"
      var[var].each do |var|
        var = var["LITERAL"];Fi[	@Ti:I"∑      var = []
      if (var["LITERAL"] or var["LITERAL"]) then
        var = "STRING"
        var[var].each do |var|
          if var["LITERAL"] and (var["LITERAL"] == "STRING") then;Fi[	@TiOI"´      var = []
      if var["LITERAL"] and (var["LITERAL"].to_i > "LITERAL") then
        var = "STRING"
        var[var].each do |var|
          var = var["LITERAL"].to_s;Fi[	I"var = nil;F@ri[[	@ñiÛI"~      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then;Fi[	@ñi˝I"~      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then;Fi[	@ñiI"~      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then;Fi[	I"var.caveats;FI"var.finish);Fi[[	@©iWI"ê      (var = FormulaInstaller.new(var)
      var.install
      var.caveats
      var.finish)
    rescue FormulaInstallationAlreadyAttemptedError;Fi[	@+i>I"Å      var.unlink if var
      var.install
      var.caveats
      var.finish)
    rescue FormulaInstallationAlreadyAttemptedError;Fi[	@√itI"]      var.unlink if var
      var.install
      var.caveats
      var.finish)
    ensure;Fi[	I".if ((var >> "LITERAL") == "LITERAL") then;FI"%if check_next_sequence(var) then;Fi[[	@iI"Ø  def self.sequence_length(var)
    var = var.get_byte["LITERAL"]
    if ((var >> "LITERAL") == "LITERAL") then
      if check_next_sequence(var) then
        return "LITERAL";Fi[	@i$I"ñ      end
    else
      if ((var >> "LITERAL") == "LITERAL") then
        if check_next_sequence(var) then
          if check_next_sequence(var) then;Fi[	@i/I"†        end
      else
        if ((var >> "LITERAL") == "LITERAL") then
          if check_next_sequence(var) then
            if check_next_sequence(var) then;Fi[	I"exec_browser("STRING");F@ri[[	@+iI"ê  def function
    if ARGV.include?("STRING") then
      exec_browser("STRING")
    else
      exec_browser("STRING") if ARGV.include?("STRING");Fi[	@ciI"~  def function
    if ARGV.include?("STRING") then
      exec_browser("STRING")
    else
      if ARGV.include?("STRING") then;Fi[	@ciI"v    else
      if ARGV.include?("STRING") then
        exec_browser("STRING")
      else
        var = ARGV.first;Fi[	@¨@\,i[[	@+iI"Ä  # Create a formula from a tarball URL
  def function
    if ARGV.include?("STRING") then
      exec_browser("STRING")
    else;Fi[	@ciI"`  
  def function
    if ARGV.include?("STRING") then
      exec_browser("STRING")
    else;Fi[	@ciI"z      exec_browser("STRING")
    else
      if ARGV.include?("STRING") then
        exec_browser("STRING")
      else;Fi[	@S,@S,i[[	@i%I"©    else
      if ((var >> "LITERAL") == "LITERAL") then
        if check_next_sequence(var) then
          if check_next_sequence(var) then
            return "LITERAL";Fi[	@i0I"√      else
        if ((var >> "LITERAL") == "LITERAL") then
          if check_next_sequence(var) then
            if check_next_sequence(var) then
              if check_next_sequence(var) then;Fi[	@i1I"Ÿ        if ((var >> "LITERAL") == "LITERAL") then
          if check_next_sequence(var) then
            if check_next_sequence(var) then
              if check_next_sequence(var) then
                return "LITERAL";Fi[	@á@ëi[[	@Ÿ
iI"j    end
    if (var.length <= var) and var.! then
      var = "STRING"
      var = "LITERAL"
    else;Fi[	@{i4I"Ñ    def function
      return if @to_merge.empty?
      var = "STRING"
      var = "LITERAL"
      @to_merge.each do |var, var, var|;Fi[	@hi*I"|    def self.destroy(var, var, var)
      var = var.to_s
      var = "STRING"
      var = "LITERAL"
      if var.empty? then;Fi[	@ã@ri[[	@ØioI"ô    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var)
    else
      super(absolutify_local_url(var));Fi[	@ØixI"ô    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var)
    else
      super(absolutify_local_url(var));Fi[	@Øi|I"ô    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var)
    else
      super(absolutify_local_url(var));Fi[	@S,@©&i[[	@iI"ö    var = var.get_byte["LITERAL"]
    if ((var >> "LITERAL") == "LITERAL") then
      if check_next_sequence(var) then
        return "LITERAL"
      else;Fi[	@i&I"Ø      if ((var >> "LITERAL") == "LITERAL") then
        if check_next_sequence(var) then
          if check_next_sequence(var) then
            return "LITERAL"
          else;Fi[	@i2I"∫          if check_next_sequence(var) then
            if check_next_sequence(var) then
              if check_next_sequence(var) then
                return "LITERAL"
              else;Fi[	I"require("blacklist");F@»i[[	@+iI"Krequire("formula")
require("blacklist")
module Homebrew
  extend(self);Fi[	@©iI"irequire("formula_installer")
require("hardware")
require("blacklist")
module Homebrew
  extend(self);Fi[	@ciI"Krequire("formula")
require("blacklist")
module Homebrew
  extend(self);Fi[	I"&@additional_options ||= Array.new;F@{i[[	@
i-I"∑        @skip_tables ||= Array.new
        @only_tables ||= Array.new
        @additional_options ||= Array.new
        instance_eval(&var) if block_given?
        @name ||= "LITERAL";Fi[	@
i.I"À        @skip_tables ||= Array.new
        @only_tables ||= Array.new
        @additional_options ||= Array.new
        instance_eval(&var) if block_given?
        @pg_dump_utility ||= utility("LITERAL");Fi[	@ıi(I"†      def function(var, var)
        super(var)
        @additional_options ||= Array.new
        instance_eval(&var) if block_given?
        @name ||= "STRING";Fi[	@©&@ri[[	@iI"ï    if ((var >> "LITERAL") == "LITERAL") then
      if check_next_sequence(var) then
        return "LITERAL"
      else
        var.pos -= "LITERAL";Fi[	@i'I"†        if check_next_sequence(var) then
          if check_next_sequence(var) then
            return "LITERAL"
          else
            var.pos -= "LITERAL";Fi[	@i3I"¥            if check_next_sequence(var) then
              if check_next_sequence(var) then
                return "LITERAL"
              else
                var.pos -= "LITERAL";Fi[	I"G# Returns an Array of string pids of all the other workers on this;FI"<# machine. Useful when pruning dead workers on startup.;Fi[[	@«i¸I"†    end
    
    # Returns an Array of string pids of all the other workers on this
    # machine. Useful when pruning dead workers on startup.
    def function;Fi[	@«iI"√    # Find Resque worker pids on Windows.
    #
    # Returns an Array of string pids of all the other workers on this
    # machine. Useful when pruning dead workers on startup.
    def function;Fi[	@«i I"Ã    # Find worker pids - platform independent
    #
    # Returns an Array of string pids of all the other workers on this
    # machine. Useful when pruning dead workers on startup.
    def function(var);Fi[	I"&["STRING", "STRING"].map do |var|;F@1#i[[	@ÚiàI"ß      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING";Fi[	@ÚiíI"ß      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING";Fi[	@
i\I"¿      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING".gsub("STRING", "STRING");Fi[	@Ô)@—i[[	@ i"I"Ë  # from performing one of the above operations to determine the queue.
  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`
  #
  # Returns true if the job was queued, nil if the job was rejected by a;Fi[	@ iHI"Ê  # from performing one of the above operations to determine the queue.
  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`
  #
  # If no args are given, this method will dequeue *all* jobs matching;Fi[	@ irI"ﬁ  # Validates if the given klass could be a valid Resque job
  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`
  #
  # If given klass is nil this method will raise a `Resque::NoClassError`;Fi[	I"#when "LITERAL", "LITERAL" then;F@˚i[[	@.i3I"µ      end
      var = case read("LITERAL", (var + "LITERAL")).unpack("STRING")["LITERAL"]
      when "LITERAL", "LITERAL" then
        "LITERAL"
      when "LITERAL", "LITERAL" then;Fi[	@.i5I"í      when "LITERAL", "LITERAL" then
        "LITERAL"
      when "LITERAL", "LITERAL" then
        "LITERAL"
      when "LITERAL", "LITERAL" then;Fi[	@.i7I"}      when "LITERAL", "LITERAL" then
        "LITERAL"
      when "LITERAL", "LITERAL" then
        "LITERAL"
      else;Fi[	@ÓI"var = @attrs["STRING"];Fi[[	@ºiI"e    
    # @return [Integer]
    def function
      var = @attrs["STRING"]
      var.to_i if var;Fi[	@ºiI"e    
    # @return [Integer]
    def function
      var = @attrs["STRING"]
      var.to_i if var;Fi[	@ºiI"k    
    # @return [Time]
    def function
      var = @attrs["STRING"]
      Time.at(var.to_i) if var;Fi[	I"# raised.;F@—i[[	@˘
iI"¡    # Validates that the given hash only includes at *most* one of a set of
    # exclusive keys.  If more than one key is found, an ArgumentError will be
    # raised.
    # 
    # == Examples;Fi[	@i.I"»    # *Note* that multiple events on the same state machine / attribute cannot
    # be run in parallel.  If this is attempted, an ArgumentError will be
    # raised.
    # 
    # == Halting callbacks;Fi[	@Yi@I"Ω    # Determines the current state of the given object as configured by this
    # state machine.  If no state is found, then an ArgumentError will be
    # raised.
    # 
    # == Examples;Fi[	I"(require("state_machine/assertions");F@’i[[	@˛
iI"çrequire("state_machine/matcher")
require("state_machine/eval_helpers")
require("state_machine/assertions")
module StateMachine
  class Branch;Fi[	@2i I"]require("state_machine/assertions")
module StateMachine
  class MachineCollection < Hash;Fi [	@√i I"Srequire("state_machine/assertions")
module StateMachine
  class NodeCollection;Fi [	I"*problem("STRING") if var =~ "LITERAL";F@Ê,i[[	@˝i}I"≈      next if var =~ "LITERAL"
      next unless var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL";Fi[	@˝i~I"“      next unless var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL";Fi[	@˝iI"∑      problem("STRING") if var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL"
      problem("STRING") if var =~ "LITERAL"
    end;Fi[	@ÔI"end.join("STRING");Fi[[	@
iÉI"ö        skip_tables.map do |var|
          var = (dump_all? or var["STRING"]) ? (var) : ("STRING")
          "STRING"
        end.join("STRING")
      end;Fi[	@¨i]I"õ        var.namespace_scopes.map do |var|
          var = var.prefix.nil? ? ("STRING") : ("STRING")
          "STRING"
        end.join("STRING")
      end;Fi[	@tiI"t            end
          end.map do |var|
            "STRING"
          end.join("STRING")
          "STRING";Fi[	I"problem("STRING");F@ri[[	@˝iÜI"°      next if var.nil?
      if var.version.to_s.empty? then
        problem("STRING")
      else
        var = var.version unless var.version.detected_from_url?;Fi[	@˝iöI"¥      problem("STRING") if (var.hash_type == "LITERAL")
      if var.empty? then
        problem("STRING")
      else
        problem("STRING") unless (var.hexdigest.length == var);Fi[	@˝i™I"â        problem("STRING") unless var.url =~ "LITERAL"
      when "LITERAL" then
        problem("STRING")
      else
        # do nothing;Fi[	I"N# * <tt>:from</tt> - One or more states being transitioned from.  If none;FI"E#   are specified, then this will be the object's current state.;Fi[[	@`i|I"    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are;Fi[	@iI"    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are;Fi[	@i3I"    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are;Fi[	@Ó@Òi[[	@diBI")  end
  
  def function
    []
  end;Fi[	@diFI")  end
  
  def function
    []
  end;Fi[	@"iI"É        
        # The list of ancestor names that cause this integration to matched.
        def function
          []
        end;Fi[	I"N# * <tt>:to</tt> - One or more states being transitioned to.  If none are;FI"6#   specified, then this will match any to state.;Fi[[	@`i~I"`    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless;Fi[	@iI"c    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none;Fi[	@i5I"c    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none;Fi[	I"var.install;F@H,i[[	@©iVI"k    begin
      (var = FormulaInstaller.new(var)
      var.install
      var.caveats
      var.finish);Fi[	@+i=I"f      oh1("STRING")
      var.unlink if var
      var.install
      var.caveats
      var.finish);Fi[	@√isI"f      oh1("STRING")
      var.unlink if var
      var.install
      var.caveats
      var.finish);Fi[	I"8# data to the specified path based on the 'trigger';F@Ói[[	@
i5I"ö      ##
      # Performs the mysqldump command and outputs the
      # data to the specified path based on the 'trigger'
      def function
        super;Fi[	@
i5I"ó      ##
      # Performs the pgdump command and outputs the
      # data to the specified path based on the 'trigger'
      def function
        super;Fi[	@¯i$I"õ      ##
      # Performs the riak-admin command and outputs the
      # data to the specified path based on the 'trigger'
      def function
        super;Fi[	I"@object = var;FI"@machine = var;Fi[[	@oiI"ö    def function(var, var, var)
      assert_valid_keys(var, "LITERAL", "LITERAL")
      @object = var
      @machine = var
      @target = var["LITERAL"];Fi[	@vi!I"      var = { "LITERAL" => (false), "LITERAL" => (var.states.match!(var).name) }.merge(var)
      assert_valid_keys(var, "LITERAL", "LITERAL", "LITERAL", "LITERAL")
      @object = var
      @machine = var
      @from_name = var.states.fetch(var["LITERAL"]).name;Fi[	@›iRI"ë    # Creates a new, specific transition
    def function(var, var, var, var, var, var)
      @object = var
      @machine = var
      @args = [];Fi[	I"#require("state_machine/error");F@’i[[	@`i	I"úrequire("state_machine/assertions")
require("state_machine/matcher_helpers")
require("state_machine/error")
module StateMachine
  class InvalidEvent < Error;Fi[	I"?projects//state_machine_proj/state_machine/integrations.rb;Ti	I"ô  require("state_machine/integrations/#{File.basename(path)}")
end
require("state_machine/error")
module StateMachine
  class IntegrationNotFound < Error;Fi[	@›iI"Érequire("state_machine/transition_collection")
require("state_machine/error")
module StateMachine
  class InvalidTransition < Error;Fi[	@ñ)@-i[[	@`i{I"    # object.  If no transitions can be made, then this will return nil.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.;Fi[	@iI"ˇ    # Gets the list of events that can be fired on the given object.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.;Fi[	@i2I"    # Gets the list of transitions that can be run on the given object.
    # 
    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.;Fi[	@-@-i[[	@`i}I"7    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.;Fi[	@iI"7    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.;Fi[	@i4I"7    # Valid requirement options:
    # * <tt>:from</tt> - One or more states being transitioned from.  If none
    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.;Fi[	@Ò%@—i[[	@iI"÷    #   are specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # 
    # == Examples;Fi[	@i:I"÷    #   are specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # 
    # == Examples;Fi[	@5iôI"”    #   specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    # 
    # For example,;Fi[	I"0["STRING", "STRING", "STRING"].map do |var|;F@1#i[[	@
ifI"±      # to perform the database dumping process
      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING";Fi[	@
idI"˙      # both the host and the socket are specified, the socket will take priority over the host
      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING".gsub("STRING", "STRING");Fi[	@ıifI"±      # to perform the database dumping process
      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING";Fi[	@Ó@K(i[[	@ÊiäI"[  end
  
  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING");Fi[	@ÊiöI"[  end
  
  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING");Fi[	@Êi™I"[  end
  
  def function
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING");Fi[	@ÓI"2FileUtils.touch((HOMEBREW_PREFIX / "STRING"));Fi[[	@i-I"{  end
  
  def function
    FileUtils.touch((HOMEBREW_PREFIX / "STRING"))
    assert_raise(RuntimeError) { @keg.link };Fi[	@i2I"g  end
  
  def function
    FileUtils.touch((HOMEBREW_PREFIX / "STRING"))
    var = OpenStruct.new;Fi[	@i9I"g  end
  
  def function
    FileUtils.touch((HOMEBREW_PREFIX / "STRING"))
    var = OpenStruct.new;Fi[	@>&@&*i[[	@6iI"†  def function
    var = TestLLVMFailure.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"));Fi[	@6i+I"©  def function
    var = TestNoCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL").!);Fi[	@6iDI"Æ  def function
    var = TestMoreMixedCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"));Fi[	I")ENV.prepend_path("STRING", "STRING");F@}-i[[	@ÇiII"±    var.each do |var|
      var = var.opt_prefix
      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", "STRING");Fi[	@ÇiJI"∆      var = var.opt_prefix
      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", "STRING");Fi[	@ÇiKI"—      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", "STRING")
      ENV.prepend_path("STRING", var);Fi[	@Ò$@»i[[	@∏iI"Erequire("formula")
require("keg")
module Homebrew
  extend(self);Fi[	@&,i I"2require("keg")
module Homebrew
  extend(self);Fi [	@öiI"\require("extend/ENV")
require("hardware")
require("keg")
module Homebrew
  extend(self);Fi[	@{I"!#       other_states :idling;Fi[[	@YiI"    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end;Fi[	@Yi*I"    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end;Fi[	@YiEI"    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end;Fi[	@ÓI" (@set << X11Dependency.new);Fi[[	I"7projects//homebrew_proj/test/test_comparableset.rb;TiI"\  end
  
  def function
    (@set << X11Dependency.new)
    (@set << X11Dependency.new);Fi[	@ö-iI"f  end
  
  def function
    (@set << X11Dependency.new)
    (@set << X11Dependency.new("STRING"));Fi[	@ö-i#I"Z  end
  
  def function
    (@set << X11Dependency.new)
    (@set << Requirement.new);Fi[	@ÓI"(@env << "LITERAL");Fi[[	I";projects//homebrew_proj/test/test_build_environment.rb;TiI"J  end
  
  def function
    (@env << "LITERAL")
    assert(@env.std?);Fi[	@§-iI"P  end
  
  def function
    (@env << "LITERAL")
    assert(@env.userpaths?);Fi[	@§-iI"Y  end
  
  def function
    (@env << "LITERAL")
    (@env << Proc.new { "LITERAL" });Fi[	I"(#         transition all => :active;F@◊i[[	@iMI"±    #     state_machine :alarm_state, :namespace => 'alarm', :initial => :on do
    #       event :enable do
    #         transition all => :active
    #       end
    #       ;Fi[	@iI"µ    #     state_machine :alarm_state, :namespace => 'alarm', :initial => :active do
    #       event :enable do
    #         transition all => :active
    #       end
    #       ;Fi[	@5iÃI"∂    #     state_machine :alarm_state, :initial => :active, :namespace => 'alarm' do
    #       event :turn_on do
    #         transition all => :active
    #       end
    #       ;Fi[	I"#       event :disable do;F@Ôi[[	@iPI"t    #       end
    #       
    #       event :disable do
    #         transition all => :off
    #       end;Fi[	@i}I"t    #       end
    #       
    #       event :disable do
    #         transition all => :off
    #       end;Fi[	@5iîI"ö    #     
    #     state_machine :alarm_state, :initial => :active do
    #       event :disable do
    #         transition all => :off
    #       end;Fi[	I"1#       -----BEGIN PGP PUBLIC KEY BLOCK-----;FI"/#       Version: GnuPG v1.4.12 (GNU/Linux);Fi[[	@‘i{I"§      #
      #     enc.keys['joe@example.com'] = <<-EOS
      #       -----BEGIN PGP PUBLIC KEY BLOCK-----
      #       Version: GnuPG v1.4.12 (GNU/Linux)
      #;Fi[	@‘iáI"•      #
      #     enc.keys['mary@example.com'] = <<-EOS
      #       -----BEGIN PGP PUBLIC KEY BLOCK-----
      #       Version: GnuPG v1.4.12 (GNU/Linux)
      #;Fi[	@‘iI"¬      #     encryptor.keys = {}
      #     encryptor.keys['joe@example.com'] = <<-EOS
      #       -----BEGIN PGP PUBLIC KEY BLOCK-----
      #       Version: GnuPG v1.4.12 (GNU/Linux)
      #;Fi[	@æ-@—i[[	@‘i|I"Î      #     enc.keys['joe@example.com'] = <<-EOS
      #       -----BEGIN PGP PUBLIC KEY BLOCK-----
      #       Version: GnuPG v1.4.12 (GNU/Linux)
      #
      #       mQMqBEd5F8MRCACfArHCJFR6nkmxNiW+UE4PAW3bQla9JWFqCwu4VqLkPI/lHb5p;Fi[	@‘iàI"Ï      #     enc.keys['mary@example.com'] = <<-EOS
      #       -----BEGIN PGP PUBLIC KEY BLOCK-----
      #       Version: GnuPG v1.4.12 (GNU/Linux)
      #
      #       2SlVmAGHOCJhShx1vfNGVYNxHff8Fzy2O89BxD/6in9kGvD0+mhXSDxsJn2oNK50;Fi[	@‘iI"Ò      #     encryptor.keys['joe@example.com'] = <<-EOS
      #       -----BEGIN PGP PUBLIC KEY BLOCK-----
      #       Version: GnuPG v1.4.12 (GNU/Linux)
      #
      #       mI0EUBR6CwEEAMVSlFtAXO4jXYnVFAWy6chyaMw+gXOFKlWojNXOOKmE3SujdLKh;Fi[	@ÓI"require("version");Fi[[	@RiòI"K  end
  
  def function
    require("version")
    Version.parse(self);Fi[	@iZI"R  end
  
  def function
    require("version")
    Version.new(basename.to_s);Fi[	@1i
I"∏  # This can be compared to numerics, strings, or symbols
  # using the standard Ruby Comparable methods.
  def function
    require("version")
    MacOSVersion.new(MACOS_VERSION.to_s);Fi[	I"D#   vehicle.state                                 # => "idling";FI"A#   vehicle.alarm_state                           # => "off";Fi[[	@i[I"÷    #   
    #   vehicle.fire_events(:ignite, :disable_alarm)  # => true
    #   vehicle.state                                 # => "idling"
    #   vehicle.alarm_state                           # => "off"
    #   ;Fi[	@i`I"	    #   # If any event fails, the entire event chain fails
    #   vehicle.fire_events(:ignite, :enable_alarm)   # => false
    #   vehicle.state                                 # => "idling"
    #   vehicle.alarm_state                           # => "off"
    #   ;Fi[	@ieI":    #   # Exception raised on invalid event
    #   vehicle.fire_events(:park, :invalid)          # => StateMachine::InvalidEvent: :invalid is an unknown event
    #   vehicle.state                                 # => "idling"
    #   vehicle.alarm_state                           # => "off"
    def function(var);Fi[	I"#     </li>;FI"&#     <li class='nav' role='nav'>;Fi[[	@~iüI"û    #     <li class='nav' role='nav'>
    #       <a href='#'>Home</a>
    #     </li>
    #     <li class='nav' role='nav'>
    #       <a href='#'>About</a>;Fi[	@~i¢I"°    #     <li class='nav' role='nav'>
    #       <a href='#'>About</a>
    #     </li>
    #     <li class='nav' role='nav'>
    #       <a href='#'>Contact</a>;Fi[	@~i•I"ü    #     <li class='nav' role='nav'>
    #       <a href='#'>Contact</a>
    #     </li>
    #     <li class='nav' role='nav'>
    #       <a href='#'>FAQ</a>;Fi[	@÷I"module DataMapper;Fi[[	@%iI"Zmodule StateMachine
  module Integrations
    module DataMapper
      module Observer;Fi[	@ù
iI"_module StateMachine
  module Integrations
    module DataMapper
      version("STRING") do;Fi[	@ÃiI"Xmodule StateMachine
  module Integrations
    module DataMapper
      include(Base);Fi[	I"2# Adds a validation error to the given object;FI"%def function(var, var, var, var);Fi[[	@A
iI"í      end
      
      # Adds a validation error to the given object 
      def function(var, var, var, var)
        if supports_validations? then;Fi[	@ÃiI"ë      end
      
      # Adds a validation error to the given object
      def function(var, var, var, var)
        if supports_validations? then;Fi[	@M
iI"≥      end
      
      # Adds a validation error to the given object
      def function(var, var, var, var)
        var.errors.add(self.attribute(var), generate_message(var, var));Fi[	I"L# Describes the current validation errors on the given object.  If none;FI"F# are specific, then the default error is interpeted as a "halt".;Fi[[	@A
i%I"æ      end
      
      # Describes the current validation errors on the given object.  If none
      # are specific, then the default error is interpeted as a "halt".
      def function(var);Fi[	@ÃiI"æ      end
      
      # Describes the current validation errors on the given object.  If none
      # are specific, then the default error is interpeted as a "halt".
      def function(var);Fi[	@M
i"I"æ      end
      
      # Describes the current validation errors on the given object.  If none
      # are specific, then the default error is interpeted as a "halt".
      def function(var);Fi[	I"class << self;FI"###;Fi[[	@iJI"      end
      
      class << self
        ###
        # Parse HTML.  +string_or_io+ may be a String, or any object that;Fi[	@}iI"Çmodule Nokogiri
  module XML
    class << self
      ###
      # Create a new Nokogiri::XML::RelaxNG document from +string_or_io+.;Fi[	@åiI"Åmodule Nokogiri
  module XML
    class << self
      ###
      # Create a new Nokogiri::XML::Schema object using a +string_or_io+;Fi[	@ .@õi[[	@A
i&I"      
      # Describes the current validation errors on the given object.  If none
      # are specific, then the default error is interpeted as a "halt".
      def function(var)
        var.errors.empty? ? ("STRING") : ((var.errors.full_messages * "STRING"));Fi[	@ÃiI"÷      
      # Describes the current validation errors on the given object.  If none
      # are specific, then the default error is interpeted as a "halt".
      def function(var)
        if var.errors.empty? then;Fi[	@M
i#I"      
      # Describes the current validation errors on the given object.  If none
      # are specific, then the default error is interpeted as a "halt".
      def function(var)
        var.errors.empty? ? ("STRING") : ((var.errors.full_messages * "STRING"));Fi[	I"L# Resets any errors previously added when invalidating the given object;F@õi[[	@A
i+I"®      end
      
      # Resets any errors previously added when invalidating the given object
      def function(var)
        var.errors.clear if supports_validations?;Fi[	@Ãi*I"®      end
      
      # Resets any errors previously added when invalidating the given object
      def function(var)
        var.errors.clear if supports_validations?;Fi[	@M
i(I"è      end
      
      # Resets any errors previously added when invalidating the given object
      def function(var)
        var.errors.clear;Fi[	I"A# Adds hooks into validation for automatically firing events;F@Ói[[	@A
iïI"y      end
      
      # Adds hooks into validation for automatically firing events
      def function
        super;Fi[	@ÃiiI"y      end
      
      # Adds hooks into validation for automatically firing events
      def function
        super;Fi[	@M
i\I"¥      end
      
      # Adds hooks into validation for automatically firing events
      def function
        define_helper("LITERAL", "STRING", "STRING", ("LITERAL" + "LITERAL"));Fi[	I"module Haml;FI"module Helpers;Fi[[	@4i I"Bmodule Haml
  module Helpers
    @@action_view_defined = true;Fi [	@Fi I"4module Haml
  module Helpers
    module XssMods;Fi [	@~i I"7module Haml
  module Helpers
    class ErrorReturn;Fi [	I"version("LITERAL") do;FI"3process("LITERAL" => (["LITERAL", "LITERAL"]));Fi[[	@riI"o  end
  
  version("LITERAL") do
    process("LITERAL" => (["LITERAL", "LITERAL"]))
    process("LITERAL");Fi[	@riI"o  end
  
  version("LITERAL") do
    process("LITERAL" => (["LITERAL", "LITERAL"]))
    process("LITERAL");Fi[	@riI"o  end
  
  version("LITERAL") do
    process("LITERAL" => (["LITERAL", "LITERAL"]))
    process("LITERAL");Fi[	I"self.perform(var);F@ri[[	@Ïi5I"Ÿ        self.parent_author_signature = self.sign_with_key(var.encryption_key)
        Postzord::Dispatcher.build(var, self).post
        self.perform(var)
      else
        if self.parent_author_signature_valid? then;Fi[	@Ïi8I"ç      else
        if self.parent_author_signature_valid? then
          self.perform(var)
        else
          Rails.logger.info("STRING");Fi[	@¯ iYI"É    else
      if self.target_author_signature_valid? then
        self.perform(var)
      else
        Rails.logger.info("STRING");Fi[	I"/if (var.nil? or var.match("LITERAL")) then;F@ãi[[	@ØinI"Ü  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var)
    else;Fi[	@ØiwI"Ü  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var)
    else;Fi[	@Øi{I"Ü  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var)
    else;Fi[	I"	var);F@≤'i[[	@‘iûI"ñ            run("STRING")
          end
          var)
        rescue => var
          raise(Errors::Encryptor::GPG::HomedirError.wrap(var, "STRING"));Fi[	@‘iMI"–          var = var.match("LITERAL").to_a["LITERAL"]
          raise("STRING") unless var
          var)
        rescue => var
          Logger.warn(Errors::Encryptor::GPG::KeyImportError.wrap(var, "STRING"));Fi[	@iÉI"≤          write_cache!(var)
          var.render("STRING")
          var)
        rescue => var
          raise(Errors::Storage::Dropbox::AuthenticationError.wrap(var, "STRING"));Fi[	I"*return image_url if (var == "STRING");F@J.i[[	@ØimI"Ä  
  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var);Fi[	@ØivI"Ä  
  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var);Fi[	@ØiI"Ä  
  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then
      super(var);Fi[	@ﬂ+I"Avar["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL");Fi[[	@ÚiI"var = "STRING"
var = eval(IO.read(var), binding, var)
var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL")
unless (::ActiveRecord::VERSION::MAJOR == "LITERAL") and ((::ActiveRecord::VERSION::MINOR < "LITERAL") or (::ActiveRecord::VERSION::TINY < "LITERAL")) then;Fi[	@‰+iI"{var = "STRING"
var = eval(IO.read(var), binding, var)
var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL")
var;Fi[	@Á+iI"{var = "STRING"
var = eval(IO.read(var), binding, var)
var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL")
var;Fi[	@ÓI"'if params["LITERAL"].present? then;Fi[[	@4
i/I"ã  end
  
  def function
    if params["LITERAL"].present? then
      @invite ||= InvitationCode.find_by_token(params["LITERAL"]["LITERAL"]);Fi[	@˘iI"¨  
  ##### These methods need to go away once we pass publisher object into the partial ######
  def function
    if params["LITERAL"].present? then
      params["LITERAL"];Fi[	@˘iI"[  end
  
  def function
    if params["LITERAL"].present? then
      params["LITERAL"];Fi[	I"Logger.warn("STRING");F@ÿi[[	@‘iÊI"g          "STRING"
        else
          Logger.warn("STRING")
          # do nothing
        end;Fi[	@‘iI"}      def function
        if user_recipients.empty? then
          Logger.warn("STRING")
          # do nothing
        else;Fi[	@‘i"I"Ç              import_key(var, var)
            else
              Logger.warn("STRING")
              # do nothing
            end;Fi[	@ÿ@ri[[	@‘iI"∑        if user_recipients.empty? then
          Logger.warn("STRING")
          # do nothing
        else
          ("STRING" + user_recipients.map { |var| "STRING" }.join("STRING"));Fi[	I"8projects//devise_proj/devise/rails/warden_compat.rb;Ti'I"~      if var.message =~ "LITERAL" then
        Rails.logger.debug("STRING")
        # do nothing
      else
        raise;Fi[	@)iI"◊      var = OEmbed::Providers.get(self.url, "LITERAL" => "LITERAL", "LITERAL" => "LITERAL", "LITERAL" => "LITERAL", "LITERAL" => "LITERAL")
    rescue => var
      # do nothing
    else
      (self.data = var.fields;Fi[	@ÿ@i[[	@Ñ.i"I"o      else
        Rails.logger.warn("STRING")
        # do nothing
      end)
    rescue NameError => var;Fi[	@i~I"9      end
    else
      # do nothing
    end)
  end;Fi[	@i™I"›            catch("LITERAL") do
              Nokogiri::HTML::SAX::Parser.new(JumpSAXHandler.new("LITERAL".to_s)).parse(var)
              # do nothing
            end)
          rescue Nokogiri::SyntaxError, RuntimeError;Fi[	@CI"Rowner_class.set_callback("LITERAL", "LITERAL", "STRING", "LITERAL" => (true));Fi[[	@F
ihI"µ        end
        def function
          if (action_hook == "LITERAL") then
            owner_class.set_callback("LITERAL", "LITERAL", "STRING", "LITERAL" => (true))
          end;Fi[	@¶
iPI"µ        end
        def function
          if (action_hook == "LITERAL") then
            owner_class.set_callback("LITERAL", "LITERAL", "STRING", "LITERAL" => (true))
          end;Fi[	@±
iI"µ        end
        def function
          if (action_hook == "LITERAL") then
            owner_class.set_callback("LITERAL", "LITERAL", "STRING", "LITERAL" => (true))
          end;Fi[	I"# @note Validation;F@Ói[[	@ÒiuI"  end
  
  # @note Validation
  def function
    (errors["LITERAL"] << "STRING") if (self.identifier == self.sender.email);Fi[	@ÒizI"  end
  
  # @note Validation
  def function
    (errors["LITERAL"] << "STRING") if self.sender_id.!=(self.aspect.user_id);Fi[	@ÒiI"Y  end
  
  # @note Validation
  def function
    return false unless self.identifier;Fi[	@CI"Nowner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true));Fi[[	@Âi<I"Ë      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else;Fi[	@Úi5I"Ë      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else;Fi[	@˜i9I"Ë      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else;Fi[	@à@W
i[[	@‡i¶I"œ          var.merge!("LITERAL" => (File.expand_path(@sendmail))) if @sendmail
          var.merge!("LITERAL" => (@sendmail_args)) if @sendmail_args
          var
        when "STRING" then
          var = {};Fi[	@‡i´I"Ì          var.merge!("LITERAL" => (File.expand_path(@exim))) if @exim
          var.merge!("LITERAL" => (@exim_args)) if @exim_args
          var
        when "STRING" then
          @mail_folder ||= File.join(Config.root_path, "STRING");Fi[	@*itI"í          var.gsub!(@config["STRING"]["STRING"], var.toc_content)
        end
        var
      when "STRING" then
        Maruku.new(var).to_html;Fi[	I" # @return [Invitation] self;F@Ói[[	@Òi>I"f  # invitation.
  #
  # @return [Invitation] self
  def function
    if email_like_identifer then;Fi[	@ÒiJI"ï  # converts a personal invitation to an admin invite
  # used in account deletion
  # @return [Invitation] self
  def function
    self.admin = true;Fi[	@ÒiSI"I  end
  
  # @return [Invitation] self
  def function
    self.send!;Fi[	I"B# Only runs validations on the action if using <tt>:save</tt>;F@Ói[[	@ÂiI"ã      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function
        (action == "LITERAL");Fi[	@ÚiI"ã      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function
        (action == "LITERAL");Fi[	@˜iI"ã      protected
      
      # Only runs validations on the action if using <tt>:save</tt>
      def function
        (action == "LITERAL");Fi[	@ÓI"Q@aspect = current_user.aspects.where("LITERAL" => (params["LITERAL"])).first;Fi[[	@ñ!i*I"w  end
  
  def function
    @aspect = current_user.aspects.where("LITERAL" => (params["LITERAL"])).first
    begin;Fi[	@ñ!iZI"¢  end
  
  def function
    @aspect = current_user.aspects.where("LITERAL" => (params["LITERAL"])).first
    if @aspect.update_attributes!(params["LITERAL"]) then;Fi[	@ñ!idI"é  end
  
  def function
    @aspect = current_user.aspects.where("LITERAL" => (params["LITERAL"])).first
    if @aspect.contacts_visible? then;Fi[	@ÓI"if params["LITERAL"] then;Fi[[	I"Bprojects//diaspora_proj/controllers/application_controller.rb;TiI"`  end
  
  def function
    if params["LITERAL"] then
      Time.at(params["LITERAL"].to_i);Fi[	@9i{I"{  end
  
  def function
    if params["LITERAL"] then
      Webfinger.in_background(params["LITERAL"], "LITERAL" => (true));Fi[	@i,I"{  end
  
  def function
    if params["LITERAL"] then
      @person = Person.local_by_account_identifier(params["LITERAL"]);Fi[	I"+class Generator < Federated::Generator;FI"def self.federated_class;Fi[[	I".projects//diaspora_proj/models/comment.rb;Ti[I"a  end
  
  class Generator < Federated::Generator
    def self.federated_class
      Comment;Fi[	@iI"wclass Like < Federated::Relayable
  class Generator < Federated::Generator
    def self.federated_class
      Like;Fi[	@iI"Ñclass Participation < Federated::Relayable
  class Generator < Federated::Generator
    def self.federated_class
      Participation;Fi[	@ÓI"if user_signed_in? then;Fi[[	@“.iII"b  end
  
  def function
    if user_signed_in? then
      I18n.locale = current_user.language;Fi[	@ıiGI"à  private
  
  def function
    if user_signed_in? then
      @post = current_user.find_visible_shareable_by_id(Post, params["LITERAL"]);Fi[	I";projects//diaspora_proj/controllers/home_controller.rb;TiI"{class HomeController < ApplicationController
  def function
    if user_signed_in? then
      redirect_to(stream_path);Fi[	@Ó@D"i[[	@9iI"l      end
      
      def function
        view_directory("LITERAL")
        view_directory("LITERAL");Fi[	@9i4I"Å      
      # Override copy_views to just copy mailer and shared.
      def function
        view_directory("LITERAL")
      end;Fi[	@9iVI"V      hide!
      
      def function
        view_directory("LITERAL")
      end;Fi[	I"H# Uses around callbacks to run state events if using the :save hook;F@Ói[[	@Âi:I"ò      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then;Fi[	@Úi3I"ò      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then;Fi[	@˜i7I"ò      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function
        if (action_hook == "LITERAL") then;Fi[	@ì@ìi[[	@´i I"VBackup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup;Fi [	@Æi I"VBackup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup;Fi [	@&
i I"VBackup::Dependency.load("STRING")
Backup::Dependency.load("STRING")
module Backup;Fi [	@Ú@Ó i[[	@i1I"å    end
    if (@person.nil? or @person.closed_account?) then
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
      return
    end;Fi[	@iFI"ù    if (var.nil? or var.owner_id.nil?) then
      Rails.logger.error("STRING")
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
      return
    end;Fi[	@iSI"Ä  def function
    if params["LITERAL"].nil? then
      render("LITERAL" => (true), "LITERAL" => "LITERAL")
      return
    end;Fi[	@£.@ri[[	@Âi=I"Æ      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else
          super;Fi[	@Úi6I"Æ      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else
          super;Fi[	@˜i:I"Æ      def function
        if (action_hook == "LITERAL") then
          owner_class.set_callback("LITERAL", "LITERAL", self, "LITERAL" => (true))
        else
          super;Fi[	I":# Runs state events around the machine's :save action;F@õi[[	@ÂiCI"∞      end
      
      # Runs state events around the machine's :save action
      def function(var)
        var.class.state_machines.transitions(var, action).perform { yield };Fi[	@Úi<I"∞      end
      
      # Runs state events around the machine's :save action
      def function(var)
        var.class.state_machines.transitions(var, action).perform { yield };Fi[	@˜i@I"∞      end
      
      # Runs state events around the machine's :save action
      def function(var)
        var.class.state_machines.transitions(var, action).perform { yield };Fi[	@Ó@p(i[[	@iZI"6    end
    
    def function
      yield
    end;Fi[	@0iSI"å    # The hook which is called inside devise.
    # So your ORM can include devise compatibility stuff.
    def function
      yield
    end;Fi[	@G	iI"@      end
      
      def function
        yield
      end;Fi[	@3&@—i[[	@%iI"Å        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial;Fi[	@%iSI"Å        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial;Fi[	@%iwI"Å        # 
        #   class Vehicle
        #     include DataMapper::Resource
        #     
        #     property :id, Serial;Fi[	@ÓI")Compass::Util.compass_warn("STRING");Fi[[	@u'i
I"h      extend(self)
      
      def function
        Compass::Util.compass_warn("STRING")
      end;Fi[	@u'iI"j      end
      
      def function
        Compass::Util.compass_warn("STRING")
        ::Rails.root;Fi[	@u'iI"i      end
      
      def function
        Compass::Util.compass_warn("STRING")
        ::Rails.env;Fi[	@t)I"##     property :state, :String;Fi[[	@%iI"í        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     ;Fi[	@%iUI"í        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     ;Fi[	@%iyI"í        #     include DataMapper::Resource
        #     
        #     property :id, Serial
        #     property :state, :String
        #     ;Fi[	@9/@—i[[	@%iI"ö        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do;Fi[	@%iVI"ö        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do;Fi[	@%izI"ö        #     
        #     property :id, Serial
        #     property :state, :String
        #     
        #     state_machine :initial => :parked do;Fi[	@Ó@[-i[[	@
ieI"Á      # Builds the MySQL connectivity options syntax to connect the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?;Fi[	@
icI"!      # that's the option PostgreSQL takes for socket connections as well. In case
      # both the host and the socket are specified, the socket will take priority over the host
      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?;Fi[	@ıieI"Á      # Builds the Redis connectivity options syntax to connect the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?;Fi[	@Ó@±,i[[	@ÚiáI"€      # Builds the MongoDB credentials syntax to authenticate the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?;Fi[	@ÚiëI"ﬂ      # Builds the MongoDB connectivity options syntax to connect the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?;Fi[	@
i[I"Ÿ      # Builds the credentials MySQL syntax to authenticate the user
      # to perform the database dumping process
      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?;Fi[	@é-@¢i[[	@YiI"    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end;Fi[	@Yi+I"    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end;Fi[	@YiFI"    #   class Vehicle
    #     state_machine :initial => :parked do
    #       other_states :idling
    #     end
    #   end;Fi[	I"3var.docstring = ["STRING", "STRING", "STRING"];FI" var.parameters = ["STRING"];Fi[[	@èi{I"H          return if inherited_machine
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING", "LITERAL")))
          var.docstring = ["STRING", "STRING", "STRING"]
          var.parameters = ["STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING", "LITERAL")));Fi[	@èi~I"D          var.parameters = ["STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING", "LITERAL")))
          var.docstring = ["STRING", "STRING", "STRING"]
          var.parameters = ["STRING"]
          unless (integration or instance_attributes.include?(machine.attribute.to_sym)) then;Fi[	@èi∂I"R            var.parameters = [["STRING", "STRING"]]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var.qualified_name)))
            var.docstring = ["STRING", "STRING", "STRING"]
            var.parameters = ["STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")));Fi[	@$I"Qregister((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")));Fi[[	@èiÖI"a            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var;Fi[	@èiêI"a            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var;Fi[	@èiöI"E          var.parameters = ["STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
          var.docstring = ["STRING", "STRING"];Fi[	@l/I"=var.docstring = ["STRING", "STRING", "STRING", "STRING"];Fi[[	@èiñI"ı            var.parameters = ["STRING"]
          end
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
          var.docstring = ["STRING", "STRING", "STRING", "STRING"]
          var.parameters = ["STRING"];Fi[	@èi¶I"\          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
          var.docstring = ["STRING", "STRING", "STRING", "STRING"]
          var.parameters = ["STRING", "STRING"];Fi[	@èi∏I"(            var.docstring = ["STRING", "STRING", "STRING"]
            var.parameters = ["STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING", "STRING", "STRING"]
            var.parameters = ["STRING"];Fi[	I"eregister((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))));FI"[var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"];Fi[[	@èiùI"É          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
          var.docstring = ["STRING", "STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]];Fi[	@èi†I"ê          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]];Fi[	@èi£I"ê          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]];Fi[	@/I",var.parameters = [["STRING", "STRING"]];Fi[[	@èiûI"ó          var.docstring = ["STRING", "STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))));Fi[	@èi°I"ö          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))));Fi[	@èi§I"Ü          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")));Fi[	I"$require("twitter/action/tweet");F@(i[[	@ïi I"Crequire("twitter/action/tweet")
module Twitter
  module Action;Fi [	@ûi I"Crequire("twitter/action/tweet")
module Twitter
  module Action;Fi [	@i I"Crequire("twitter/action/tweet")
module Twitter
  module Action;Fi [	I"var = I18n.t("STRING");FI"Irespond_with(@photo, "LITERAL" => (photos_path), "LITERAL" => (var));Fi[[	@#iπI"Ø      yield
    rescue TypeError
      var = I18n.t("STRING")
      respond_with(@photo, "LITERAL" => (photos_path), "LITERAL" => (var))
    rescue CarrierWave::IntegrityError;Fi[	@#iºI"¯      respond_with(@photo, "LITERAL" => (photos_path), "LITERAL" => (var))
    rescue CarrierWave::IntegrityError
      var = I18n.t("STRING")
      respond_with(@photo, "LITERAL" => (photos_path), "LITERAL" => (var))
    rescue RuntimeError => var;Fi[	@#iøI"‚      respond_with(@photo, "LITERAL" => (photos_path), "LITERAL" => (var))
    rescue RuntimeError => var
      var = I18n.t("STRING")
      respond_with(@photo, "LITERAL" => (photos_path), "LITERAL" => (var))
      raise(var);Fi[	I"ILastThreeCommentsDecorator.new(PostPresenter.new(var, current_user));FI"
end));Fi[[	@9igI"∑      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end;Fi[	I">projects//diaspora_proj/controllers/streams_controller.rb;TiAI"∑      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end;Fi[	@†!i)I"∑      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end))
      end;Fi[	@ÍI"4Sass::Script::Number.new("LITERAL", ["STRING"]);Fi[[	@ñiÕI"¶      var.value.map! do |var|
        case var.value
        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"])
        when "LITERAL" then;Fi[	@ñiœI"«        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"])
        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"])
        when "LITERAL" then;Fi[	@ñi—I"∏        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"])
        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"])
        else;Fi[	@rI"(var = @url.sub("LITERAL", "STRING");Fi[[	@$iôI"ì        safe_system(hgpath, "STRING")
      end
    else
      var = @url.sub("LITERAL", "STRING")
      safe_system(hgpath, "STRING", var, @clone);Fi[	@$iæI"æ      puts("STRING")
      Dir.chdir(@clone) { safe_system(bzrpath, "STRING") }
    else
      var = @url.sub("LITERAL", "STRING")
      safe_system(bzrpath, "STRING", "STRING", var, @clone);Fi[	@$i‹I"∂      puts("STRING")
      safe_system(fossilpath, "STRING", "STRING", @clone)
    else
      var = @url.sub("LITERAL", "STRING")
      safe_system(fossilpath, "STRING", var, @clone);Fi[	I";render("LITERAL" => (@stream.stream_posts.map do |var|;F@§/i[[	@9ifI"       var.all { respond_with(@person, "LITERAL" => ({ "LITERAL" => "LITERAL" })) }
      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end));Fi[	@™/i@I"”      var.mobile { render("STRING") }
      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end));Fi[	@†!i(I"«    respond_with do |var|
      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user))
        end));Fi[	@ÍI"+raise("STRING") unless which("STRING");Fi[[	@$i_I"°      safe_system("STRING", "STRING", @tarball_path)
      chdir
    when "LITERAL" then
      raise("STRING") unless which("STRING")
      safe_system("STRING");Fi[	@$ifI"      safe_system("STRING", "STRING", @tarball_path, File.basename(@url))
      chdir
    when "LITERAL" then
      raise("STRING") unless which("STRING")
      quiet_safe_system("STRING", "STRING", { "LITERAL" => "STRING" }, @tarball_path);Fi[	@$iiI"¸      raise("STRING") unless which("STRING")
      quiet_safe_system("STRING", "STRING", { "LITERAL" => "STRING" }, @tarball_path)
    when "LITERAL" then
      raise("STRING") unless which("STRING")
      safe_system("STRING", "STRING", @tarball_path);Fi[	@(I"class Entity;Fi[[	I"5projects//twitter_proj/twitter/entity/hashtag.rb;TiI"`require("twitter/entity")
module Twitter
  class Entity
    class Hashtag < Twitter::Entity;Fi[	I"1projects//twitter_proj/twitter/entity/url.rb;TiI"\require("twitter/entity")
module Twitter
  class Entity
    class Url < Twitter::Entity;Fi[	I":projects//twitter_proj/twitter/entity/user_mention.rb;TiI"drequire("twitter/entity")
module Twitter
  class Entity
    class UserMention < Twitter::Entity;Fi[	I"%var.any("LITERAL", "LITERAL") do;F@Úi[[	@ıi.I"∫      respond_to do |var|
        var.mobile { redirect_to("LITERAL") }
        var.any("LITERAL", "LITERAL") do
          render("LITERAL" => (true), "LITERAL" => "LITERAL")
        end;Fi[	@
i!I"¨    if var and var.destroy then
      respond_to do |var|
        var.any("LITERAL", "LITERAL") do
          render("LITERAL" => (true), "LITERAL" => "LITERAL")
        end;Fi[	@
i'I"ï    else
      respond_to do |var|
        var.any("LITERAL", "LITERAL") do
          render("LITERAL" => (true), "LITERAL" => "LITERAL")
        end;Fi[	@r@¨i[[	@iI"∫    if ARGV.include?("STRING") then
      Formula.each { |var| puts("STRING") }
    else
      if ARGV.include?("STRING") then
        raise(FormulaUnspecifiedError) if ARGV.named.empty?;Fi[	@µi
I"ó  if ARGV.include?("STRING") then
    Formula
  else
    if ARGV.include?("STRING") then
      var = Homebrew.outdated_brews.collect { |var| var.name };Fi[	@ciI"é    if ARGV.include?("STRING") then
      exec_browser("STRING")
    else
      if ARGV.include?("STRING") then
        exec_browser("STRING");Fi[	@Í@h+i[[	@$i˚I"~    when "LITERAL" then
      BazaarDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then;Fi[	@$i˝I"{    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then;Fi[	@$i"I"{    when "LITERAL" then
      CVSDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then;Fi[	@Í@_+i[[	@$iˇI"Å    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then
      MercurialDownloadStrategy
    when "LITERAL" then;Fi[	@$iI"Ñ    when "LITERAL" then
      FossilDownloadStrategy
    when "LITERAL" then
      MercurialDownloadStrategy
    when "LITERAL" then;Fi[	@$i$I"Å    when "LITERAL" then
      GitDownloadStrategy
    when "LITERAL" then
      MercurialDownloadStrategy
    when "LITERAL" then;Fi[	@˙"@-i[[	@3i I"Lrequire("twitter/creatable")
require("twitter/identity")
module Twitter;Fi [	@6i I"Lrequire("twitter/creatable")
require("twitter/identity")
module Twitter;Fi [	@>i I"Lrequire("twitter/creatable")
require("twitter/identity")
module Twitter;Fi [	@LI"Evar.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") };Fi[[	@¯iI"‹    @like = Like.find_by_id_and_author_id!(params["LITERAL"], current_user.person.id)
    current_user.retract(@like)
    respond_to do |var|
      var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
    end;Fi[	@#iRI"¶    if var then
      current_user.retract(var)
      respond_to do |var|
        var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
        var.html do;Fi[	@†!iI"™      end
    else
      respond_to do |var|
        var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
        var.html { redirect_to(tag_path("STRING")) };Fi[	@LI"Cvar.js { render("LITERAL" => (true), "LITERAL" => "LITERAL") };Fi[[	@ıi&I"     if (current_user.owns?(@comment) or current_user.owns?(@comment.parent)) then
      current_user.retract(@comment)
      respond_to do |var|
        var.js { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
        var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") };Fi[	@ii/I"ü      flash["LITERAL"] = I18n.t("STRING")
    end
    respond_to do |var|
      var.js { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
      var.any do;Fi[	@CiUI"¿      end
    end
    respond_to do |var|
      var.js { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
      var.all { redirect_to((var ? (new_user_session_path) : (edit_user_path))) };Fi[	@@êi[[	@ViI"∞      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var);Fi[	@?iI"∞      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var);Fi[	@?iI"∞      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var);Fi[	@r@Ì i[[	I"6projects//diaspora_proj/models/jobs/http_multi.rb;TiI"¡        if (var < MAX_RETRIES) then
          Resque.enqueue(Jobs::HttpMulti, var, var, var.failed_people, var, (var + "LITERAL"))
        else
          Rails.logger.info("STRING")
        end;Fi[	@Ïi9I"ì        if self.parent_author_signature_valid? then
          self.perform(var)
        else
          Rails.logger.info("STRING")
          return;Fi[	@¯ iZI"â      if self.target_author_signature_valid? then
        self.perform(var)
      else
        Rails.logger.info("STRING")
        return;Fi[	@rI"if var.empty? then;Fi[[	I"Bprojects//diaspora_proj/controllers/invitations_controller.rb;Ti4I"ß    if var.empty? then
      flash["LITERAL"] = t("STRING")
    else
      if var.empty? then
        flash["LITERAL"] = t("STRING", "LITERAL" => (var.join("STRING")));Fi[	@.0i7I"»      if var.empty? then
        flash["LITERAL"] = t("STRING", "LITERAL" => (var.join("STRING")))
      else
        if var.empty? then
          flash["LITERAL"] = (t("STRING") + var.join("STRING"));Fi[	@{iòI"~        @dont_indent_next_line = var
        return if var
      else
        if var.empty? then
          var = "STRING";Fi[	@M@¡/i[[	@9ieI"
    respond_to do |var|
      var.all { respond_with(@person, "LITERAL" => ({ "LITERAL" => "LITERAL" })) }
      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user));Fi[	@™/i?I"È      var.html { render("STRING") }
      var.mobile { render("STRING") }
      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user));Fi[	@†!i'I"3    @stream = Stream::Tag.new(current_user, params["LITERAL"], "LITERAL" => (max_time), "LITERAL" => (params["LITERAL"]))
    respond_with do |var|
      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|
          LastThreeCommentsDecorator.new(PostPresenter.new(var, current_user));Fi[	I"respond_with do |var|;F@Mi[[	@ïiI"Ì      @aspect = var.aspect
      flash.now["LITERAL"] = I18n.t("STRING")
      respond_with do |var|
        var.json do
          render("LITERAL" => ({ "LITERAL" => (@person_id), "LITERAL" => (@contact.aspects.map { |var| var.id }) }));Fi[	@ıi=I"Ò    raise(ActiveRecord::RecordNotFound.new) unless @post
    @comments = @post.comments.for_a_stream
    respond_with do |var|
      var.json do
        render("LITERAL" => (CommentPresenter.as_collection(@comments)), "LITERAL" => "LITERAL");Fi[	@†!i&I"    gon.tagFollowings = tags if user_signed_in?
    @stream = Stream::Tag.new(current_user, params["LITERAL"], "LITERAL" => (max_time), "LITERAL" => (params["LITERAL"]))
    respond_with do |var|
      var.json do
        render("LITERAL" => (@stream.stream_posts.map do |var|;Fi[	@rI",flash.now["LITERAL"] = I18n.t("STRING");Fi[[	@ïiI"©        var.html { redirect_to("LITERAL") }
      end
    else
      flash.now["LITERAL"] = I18n.t("STRING")
      var = var ? (var.errors.full_messages) : (t("STRING"));Fi[	@ïi-I"      flash.now["LITERAL"] = I18n.t("STRING")
      respond_with(AspectMembership.where("LITERAL" => (@contact.id), "LITERAL" => (@aspect.id)).first)
    else
      flash.now["LITERAL"] = I18n.t("STRING")
      render("LITERAL" => (true), "LITERAL" => "LITERAL");Fi[	@#irI"™          var.js { render("LITERAL" => (var), "LITERAL" => "LITERAL") }
        end
      else
        flash.now["LITERAL"] = I18n.t("STRING")
        respond_to do |var|;Fi[	@rI"&if list_of_color_stops?(var) then;Fi[[	@ñiÙI"Å        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var);Fi[	@ñi˛I"Å        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var);Fi[	@ñiI"Å        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var);Fi[	@@i[[	@≈iAI"$      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>]
      # @param query [String] The search query to run against people search.;Fi[	@≈iRI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>]
      # @overload contributees(options={});Fi[	@≈iiI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>]
      # @overload contributors(options={});Fi[	@I"2# @return [Twitter::List] The specified list.;Fi[[	@Vi¢I"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The specified list.
      # @overload list_subscribe(list, options={});Fi[	@ViŸI"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The specified list.
      # @overload list_unsubscribe(list, options={});Fi[	@Vi≥I"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The specified list.
      # @overload list(list, options={});Fi[	@±+I"module Test;Fi[[	I"9projects//omni_proj/omniauth/test/strategy_macros.rb;Ti I"<module OmniAuth
  module Test
    module StrategyMacros;Fi [	@Ë
iI"grequire("rack")
require("omniauth/test")
module OmniAuth
  module Test
    module StrategyTestCase;Fi[	I")projects//omni_proj/omniauth/test.rb;Ti I"Dmodule OmniAuth
  module Test
    autoload("LITERAL", "STRING");Fi [	I"#:nodoc:;F@Ói[[	@ôi[I"•        
        # Generate a token checking if one does not already exist in the database.
        #:nodoc:
        def function
          generate_token("LITERAL");Fi[	@øiI"¢    
    # We need to setup the environment variables and the response in the controller.
    #:nodoc:
    def function
      @request.env["STRING"] = @controller;Fi[	@øiI"§    
    # Quick access to Warden::Proxy.
    #:nodoc:
    def function
      @warden ||= (var = Warden::Manager.new(nil) { |var| var.merge!(Devise.warden_config) };Fi[	@˚@ï"i[[	@.i(I"®      var = case read("LITERAL", var).unpack("STRING")
      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL";Fi[	@.i*I"É        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL";Fi[	@.i,I"É        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL"
      when ["LITERAL", "LITERAL"] then
        "LITERAL";Fi[	@Ï@ß(i[[	I"0projects//diaspora_proj/mailers/notifier.rb;TiI"v  
  def self.admin(var, var, var)
    var = []
    var.each do |var|
      var = single_admin(var, var, var.dup);Fi[	@RiI"M  
  def function(var)
    var = []
    var.each do |var|
      case var;Fi[	@Ri<I"z  # Creates symlinks to sources in this folder.
  def function(var)
    var = []
    var.each do |var|
      case var;Fi[	@Í@_&i[[	@yi"I"Ì      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.;Fi[	@yi5I"Ì      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.;Fi[	@yiOI"Ì      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.;Fi[	@r@>i[[	@˚i8I"®          (var << "STRING") unless var.directory?
          (var << "STRING")
        else
          (var << "STRING")
          (var << "STRING") unless var.directory?;Fi[	@˚iJI"Ü            (var << "STRING")
            (var << "STRING")
          else
            (var << "STRING")
            (var << "STRING");Fi[	@˚iNI"ö            (var << "STRING")
          end
        else
          (var << "STRING")
          f.plist_startup ? ((var << "STRING")) : ((var << "STRING"));Fi[	@r@¯!i[[	@iMI"Ç            if var = var.files.get(var) then
              var.destroy
            else
              (var << var)
            end;Fi[	@ÒiI"∏          if var.is_a?(String) then
            (var << [var, File.join((css_path or (css_dir or "STRING")), File.basename(var))])
          else
            (var << var)
          end;Fi[	@iGI"™          if ["LITERAL", "LITERAL"].include?(var) then
            var = (var["LITERAL", (var - "LITERAL")] + [var])
          else
            (var << var)
          end;Fi[	@r@Ói[[	@i6I"á    if method_defined?("LITERAL") then
      alias "LITERAL" "LITERAL"
    else
      def function
        Script::String.new("STRING");Fi[	@xi¶I"v        "STRING"
      end
    else
      def function
        "STRING".encode(Encoding.find(options["LITERAL"]));Fi[	@{i#I"é    if (RUBY_VERSION < "STRING") then
      attr_reader :function
    else
      def function
        var = Encoding.find(@options["LITERAL"]);Fi[	@ö@‡ i[[	@i!I"¨      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @path ||= "STRING"
        @access_type ||= "LITERAL";Fi[	@iI"µ      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @path ||= "STRING"
        instance_eval(&var) if block_given?;Fi[	@	iI"µ      # Creates a new instance of the storage object
      def function(var, var, var)
        super(var, var)
        @path ||= "STRING"
        instance_eval(&var) if block_given?;Fi[	@r@õi[[	@Ki’I"\        # do nothing
      end
    else
      def function(var)
        unless var then;Fi[	@xi–I"}    if (RUBY_VERSION < "STRING") then
      attr_writer("LITERAL")
    else
      def function(var)
        return unless var;Fi[	@>ijI"y        var.gsub("LITERAL", "STRING")
      end
    else
      def function(var)
        if var.valid_encoding? then;Fi[	@õI"it("STRING") do;Fi[[	@l0iI"ã      end
      
      def function(var)
        it("STRING") do
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var));Fi[	@l0iI"ã      end
      
      def function(var)
        it("STRING") do
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var));Fi[	@l0iI"ã      end
      
      def function(var)
        it("STRING") do
          expect((last_request.env["STRING"] or {})["STRING"]).to(eq(var));Fi[	@õ@U+i[[	@∏iFI"      # For more information see Nokogiri::XML::Node#css and
      # Nokogiri::XML::Node#xpath
      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil);Fi[	@∏iWI"„      #
      # For more information see Nokogiri::XML::Node#css
      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil);Fi[	@∏ikI"Â      #
      # For more information see Nokogiri::XML::Node#xpath
      def function(var)
        var = [Hash, String, Symbol].include?(var.last.class).! ? (var.pop) : (nil)
        var = var.last.is_a?(Hash) ? (var.pop) : (nil);Fi[	I"O# Generate a token checking if one does not already exist in the database.;F@Ói[[	@1
i‘I"†        end
        
        # Generate a token checking if one does not already exist in the database.
        def function
          generate_token("LITERAL");Fi[	@—
i`I"†        end
        
        # Generate a token checking if one does not already exist in the database.
        def function
          generate_token("LITERAL");Fi[	@‘
i2I"†        end
        
        # Generate a token checking if one does not already exist in the database.
        def function
          generate_token("LITERAL");Fi[	@HI"connection do |var|;Fi[[	@Ùi9I"¿      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package)
        connection do |var|
          create_remote_path(var, var);Fi[	@´i/I"∑      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package)
        connection do |var|
          var.exec!("STRING");Fi[	@Æi-I"¿      # Transfers the archived file to the specified remote server
      def function
        var = remote_path_for(@package)
        connection do |var|
          create_remote_path(var, var);Fi[	@õI"var = destination(var);Fi[[	@ñiÄI"    #
    # Returns nothing.
    def function(var)
      var = destination(var)
      FileUtils.mkdir_p(File.dirname(var));Fi[	@ôi—I"    #
    # Returns nothing.
    def function(var)
      var = destination(var)
      FileUtils.mkdir_p(File.dirname(var));Fi[	@i6I"π    #
    # Returns false if the file was not modified since last time (no-op).
    def function(var)
      var = destination(var)
      return false if File.exist?(var) and modified?.!;Fi[	@ˇI"if var = var.pop then;Fi[[	@)i'I"¥      #     Twitter.favorites('sferik')
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var = var.pop then
          merge_user!(var.options, var);Fi[	@éi"I"Ë      #     Twitter.suggestions("art-design")
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var = var.pop then
          object_from_response(Twitter::Suggestion, "LITERAL", "STRING", var.options);Fi[	@≈iI"ø      #     Twitter.user(7505382)  # Same as above
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var = var.pop then
          merge_user!(var.options, var);Fi[	@ˇ@…i[[	@ViII"Î      #     Twitter.list_timeline(7505382, 8863586)
      def function(var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then;Fi[	@ViI"Ë      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then;Fi[	@ViI"Õ      
      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then;Fi[	@ˇ@ i[[	@≈i±I"      #     Twitter.profile_banner(7505382)  # Same as above
      def function(var)
        var = Twitter::API::Arguments.new(var)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then
          merge_user!(var.options, (var.pop or screen_name));Fi[	@?iI"˚      # @return [Array<Twitter::User>]
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then
          merge_user!(var.options, (var.pop or screen_name));Fi[	@?i`I"      # @return [Twitter::Cursor]
      def function(var, var, var, var, var, var)
        var = Twitter::API::Arguments.new(var)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then
          merge_user!(var.options, (var.pop or screen_name));Fi[	@ˇI"&merge_user!(var.options, var.pop);Fi[[	@Vi'I"´      
      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_user!(var.options, var.pop)
        merge_list!(var.options, var.pop);Fi[	@≈ißI"	      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        merge_user!(var.options, var.pop)
        cursor_from_response("LITERAL", nil, "LITERAL", "STRING", var.options, "LITERAL");Fi[	@?i,I"—      # @return [Array]
      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_user!(var.options, var.pop)
        objects_from_response(var, var, var, var.options);Fi[	@5@Ïi[[	@i`I"‡      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |var, var| (var << ("STRING" + "STRING")) };Fi[	@˜i)I"”      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |var, var| (var << "STRING") };Fi[	@´i>I"‡      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |var, var| (var << ("STRING" + "STRING")) };Fi[	I"#         end;F@◊i[[	@ÍiXI"ï      #         pending_notifications.each do |n|
      #           devise_mailer.send(n, self).deliver
      #         end
      #       end
      #;Fi[	@~i£I"z    #         haml_tag :td do
    #           haml_concat "more_data"
    #         end
    #       end
    #     end;Fi[	@"iAI"|        #             # Override here...
        #           end
        #         end
        #       end
        #     end;Fi[	@õI"4if (var["LITERAL", "LITERAL"] == "STRING") then;Fi[[	@µi§I"{    end
    
    def function(var)
      if (var["LITERAL", "LITERAL"] == "STRING") then
        ["LITERAL", "STRING", nil];Fi[	@µi¨I"|    end
    
    def function(var)
      if (var["LITERAL", "LITERAL"] == "STRING") then
        ["LITERAL", "STRING", true];Fi[	@µi¥I"}    end
    
    def function(var)
      if (var["LITERAL", "LITERAL"] == "STRING") then
        ["LITERAL", "STRING", false];Fi[	I"^# @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;F@Íi[[	@0i¬I"ë      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Relationship]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      # @param options [Hash] A customizable set of options.
      # @option options [Boolean] :device Enable/disable device notifications from the target user.;Fi[	@ëiFI"Ø      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@≈i≥I"∏      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Boolean] true if the authenticating user is blocking the target user, otherwise false.
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      # @param options [Hash] A customizable set of options.
      # @example Check whether the authenticating user is blocking @sferik;Fi[	@õI"@f = var;Fi[[	@˚iI"G  attr_reader("LITERAL")
  
  def function(var)
    @f = var
  end;Fi[	@˝iJI"π  BUILD_TIME_DEPS = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
  
  def function(var)
    @f = var
    @problems = [];Fi[	@iWI"ñ  NAMES = { "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => "STRING" }
  
  def function(var)
    @f = var
    @old_compiler = ENV.compiler;Fi[	@õI"if is_haml? then;Fi[[	@©iI"¨    alias_method("LITERAL", "LITERAL")
    
    def function(var)
      if is_haml? then
        if Haml::Util.rails_xss_safe? and var.is_a?(ActiveSupport::SafeBuffer) then;Fi[	@FiêI"ä    
    module TextHelper
      def function(var)
        if is_haml? then
          haml_buffer.buffer.concat(haml_xss_html_escape(var));Fi[	@FiúI"â      alias_method("LITERAL", "LITERAL")
      
      def function(var)
        if is_haml? then
          haml_buffer.buffer.concat(var);Fi[	@õ@\.i[[	@ØilI"z  end
  
  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then;Fi[	@ØiuI"z  end
  
  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then;Fi[	@Øi~I"z  end
  
  def function(var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match("LITERAL")) then;Fi[	@õI"!Rails.logger.debug("STRING");Fi[[	@Ïi%I"ç  end
  
  def function(var)
    Rails.logger.debug("STRING")
    if (self.parent_author_signature.nil?.! or self.parent.author.remote?) then;Fi[	@ı i/I"i  end
  
  def function(var)
    Rails.logger.debug("STRING")
    self.target.destroy if self.target;Fi[	@¯ iHI"ù  end
  
  def function(var)
    Rails.logger.debug("STRING")
    if var = Reshare.where("LITERAL" => (var.person.id), "LITERAL" => (target_guid)).first then;Fi[	@RI"/def function(var, var, var, var, var, var);Fi[[	@?i^I"¡      # @param args [Array]
      # @param method_name [Symbol]
      # @return [Twitter::Cursor]
      def function(var, var, var, var, var, var)
        var = Twitter::API::Arguments.new(var);Fi[	@?imI"∑      # @param options [Hash]
      # @param method_name [Symbol]
      # @return [Twitter::Cursor]
      def function(var, var, var, var, var, var)
        merge_default_cursor!(var);Fi[	@•iI"Æ    # @param method_name [String, Symbol]
    # @param method_options [Hash]
    # @return [Twitter::Cursor]
    def function(var, var, var, var, var, var)
      @attrs = var;Fi[	@RI"'# @overload friend_ids(options={});Fi[[	@0iI">      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following;Fi[	@0iˇI">      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following;Fi[	@0iI">      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following;Fi[	@[1I"[#   Returns an array of numeric IDs for every user the authenticated user is following;Fi[[	@0iI"      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #;Fi[	@0i I"      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #;Fi[	@0iI"      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #;Fi[	@õI"verify_map(var, "STRING");Fi[[	@?i#I"á  #       background-image: inline-sprite($icon-sprites);
  #      }
  def function(var)
    verify_map(var, "STRING")
    var.generate;Fi[	@?iTI"¿  # Returns the name of a sprite map
  # The name is derived from the folder than contains the sprites.
  def function(var)
    verify_map(var, "STRING")
    Sass::Script::String.new(var.name);Fi[	@?i|I"q  
  # Returns a url to the sprite image.
  def function(var)
    verify_map(var, "STRING")
    var.generate;Fi[	@@±+i[[	I"4projects//devise_proj/devise/omniauth/config.rb;Ti I"Kmodule Devise
  module OmniAuth
    class StrategyNotFound < NameError;Fi [	I"9projects//devise_proj/devise/omniauth/url_helpers.rb;Ti I":module Devise
  module OmniAuth
    module UrlHelpers;Fi [	I"-projects//devise_proj/devise/omniauth.rb;TiI"l  var.action("LITERAL").call(var)
end
module Devise
  module OmniAuth
    autoload("LITERAL", "STRING");Fi[	@d1@—i[[	@0iI"Ó      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.;Fi[	@0iI"Ó      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.;Fi[	@0i I"Ó      # @return [Twitter::Cursor]
      # @overload friend_ids(options={})
      #   Returns an array of numeric IDs for every user the authenticated user is following
      #
      #   @param options [Hash] A customizable set of options.;Fi[	I">#   @example Return the authenticated user's friends' IDs;FI"#     Twitter.friend_ids;Fi[[	@0iI"       #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={});Fi[	@0iI"ô      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={});Fi[	@0i&I"ô      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={});Fi[	I"=(@version["LITERAL"] << "STRING") if @version["LITERAL"];F@ì1i[[	I".projects//compass_proj/compass/version.rb;TiI"      @version["LITERAL"] = @version["LITERAL"]
      @version["LITERAL"] = "STRING"
      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if @version["LITERAL"];Fi[	@ñ1iI"       @version["LITERAL"] = "STRING"
      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if @version["LITERAL"];Fi[	@ñ1iI"I      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if @version["LITERAL"]
      (@version["LITERAL"] << "STRING") if ENV["STRING"].! and var = revision;Fi[	@ä1I"-# @overload friend_ids(user, options={});Fi[[	@0iI"      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following;Fi[	@0iI"p      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following;Fi[	@0i'I"p      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following;Fi[	@ù1I"W#   Returns an array of numeric IDs for every user the specified user is following;Fi[[	@0iI"Ó      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #;Fi[	@0i	I"Ó      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #;Fi[	@0i(I"Ó      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #;Fi[	I"var.destroy;F@ri[[	@iLI"§            Logger.message(("STRING" + "STRING"))
            if var = var.files.get(var) then
              var.destroy
            else
              (var << var);Fi[	@&
iíI"–                if var then
                  MUTEX.synchronize { Logger.message("STRING") }
                  var.destroy
                else
                  MUTEX.synchronize { Logger.message("STRING") };Fi[	I"6projects//diaspora_proj/models/user/connecting.rb;Ti)I"í    var = var.posts.all
    if (var.mutual?.! or var["LITERAL"]) then
      var.destroy
    else
      var.update_attributes("LITERAL" => (false));Fi[	I".Sass::Script::String.new(clean_path(var));F@ri[[	@w$i%I"Ç      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var);Fi[	@w$i>I"Ç      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var);Fi[	@w$iqI"â      var = "STRING" if var
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else
        clean_url(var);Fi[	I"if var.to_bool then;F@π1i[[	@w$i$I"z      end
      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else;Fi[	@w$i=I"z      end
      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else;Fi[	@w$ipI"|      end
      var = "STRING" if var
      if var.to_bool then
        Sass::Script::String.new(clean_path(var))
      else;Fi[	@¶1@—i[[	@0iI"      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[	@0i
I"      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[	@0i)I"      #     Twitter.friend_ids
      # @overload friend_ids(user, options={})
      #   Returns an array of numeric IDs for every user the specified user is following
      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[	I"#var = convert_sprite_name(var);FI"verify_map(var);Fi[[	@?iDI"  #     background: url('/images/icons.png?12345678') 0 -24px no-repeat;
  def function(var, var, var, var)
    var = convert_sprite_name(var)
    verify_map(var)
    raise(Sass::SyntaxError, "STRING") unless var.is_a?(Sass::Script::String);Fi[	@?ikI"ë  # Returns boolean if sprite has a parent
  def function(var, var)
    var = convert_sprite_name(var)
    verify_map(var)
    verify_sprite(var);Fi[	@?iuI"ö  # Returns boolean if sprite has the selector
  def function(var, var, var)
    var = convert_sprite_name(var)
    verify_map(var)
    verify_sprite(var);Fi[	I"var = (var.value + var);F@?,i[[	@ñiˆI"{      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end;Fi[	@ñi I"{      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end;Fi[	@ñiI"{      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end;Fi[	@O0@›1i[[	@ñiıI"|        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil;Fi[	@ñiˇI"|        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil;Fi[	@ñiI"|        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil;Fi[	I"var.unshift(var);F@?,i[[	@ñiÚI"y    def function(var, var, var)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else;Fi[	@ñi¸I"}      var = nil if var and var.to_bool.!
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else;Fi[	@ñiI"t    def function(var, var)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else;Fi[	I"if color_stop?(var) then;F@Ó1i[[	@ñiÒI"s    
    def function(var, var, var)
      if color_stop?(var) then
        var.unshift(var)
        var = nil;Fi[	@ñi˚I"|      end
      var = nil if var and var.to_bool.!
      if color_stop?(var) then
        var.unshift(var)
        var = nil;Fi[	@ñiI"n    
    def function(var, var)
      if color_stop?(var) then
        var.unshift(var)
        var = nil;Fi[	@õI"@set_attributes ||= {};Fi[[	@∂iXI"Ë      # The block will be passed the root-relative url of the asset.
      # When called without a block, returns the block that was previously set.
      def function(var)
        @set_attributes ||= {}
        if block_given? then;Fi[	@Çi}I"r        end
        
        def function(var)
          @set_attributes ||= {}
          send("STRING", nil);Fi[	@ÇiI"s        end
        
        def function(var)
          @set_attributes ||= {}
          @set_attributes[var];Fi[	@îI"module Cloud;Fi[[	@&
iI"uBackup::Dependency.load("STRING")
module Backup
  module Syncer
    module Cloud
      class Base < Syncer::Base;Fi[	@iI"Qmodule Backup
  module Syncer
    module Cloud
      class CloudFiles < Base;Fi[	@iI"Imodule Backup
  module Syncer
    module Cloud
      class S3 < Base;Fi[	@Ó(@®i[[	@0iœI"‰      # Returns detailed information about the relationship between two users
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@0iÈI"’      # Test for the existence of friendship between two users
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@0iI"!      # Returns a cursored collection of user objects for every user the specified user is following (otherwise known as their "friends").
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/friendships/show
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@õ@ëi[[	@<iI"∞  
  # Returns a new list after removing any non-true values
  def function(var)
    var = "LITERAL"
    if (var.size == "LITERAL") and var.first.is_a?(Sass::Script::List) then;Fi[	@.iåI"K  end
  
  def function(var)
    var = "LITERAL"
    var.each do |var|;Fi[	I")projects//homebrew_proj/cmd/untap.rb;TiI"r  end
  
  def function(var)
    var = "LITERAL"
    var = (HOMEBREW_LIBRARY / "STRING").read.split rescue [];Fi[	I"$merge_user!(var, var, "STRING");FI"Fvar["LITERAL"] = var.delete("LITERAL") unless var["LITERAL"].nil?;Fi[[	@0i‹I"˛      #   Twitter.friendship(7505382, 14100886)    # Same as above
      def function(var, var, var)
        merge_user!(var, var, "STRING")
        var["LITERAL"] = var.delete("LITERAL") unless var["LITERAL"].nil?
        merge_user!(var, var, "STRING");Fi[	@0iﬁI"1        merge_user!(var, var, "STRING")
        var["LITERAL"] = var.delete("LITERAL") unless var["LITERAL"].nil?
        merge_user!(var, var, "STRING")
        var["LITERAL"] = var.delete("LITERAL") unless var["LITERAL"].nil?
        object_from_response(Twitter::Relationship, "LITERAL", "STRING", var);Fi[	@ViVI"±      # @return [Hash]
      def function(var, var)
        merge_user!(var, var, "STRING")
        var["LITERAL"] = var.delete("LITERAL") unless var["LITERAL"].nil?
        var;Fi[	@õ@&i[[	I"<projects//diaspora_proj/helpers/aspect_global_helper.rb;TiI"\  end
  
  def function(var)
    var = {}
    var.each { |var| var[var.to_s] = var.id };Fi[	@;i‚I"Y    end
    
    def function(var)
      var = {}
      var = StringScanner.new(var);Fi[	@iI"D  end
  
  def function(var)
    var = {}
    var.each do |var|;Fi[	@õ@ß(i[[	@ÇiI"Ä      
      module ClassMethods
        def function(var)
          var.each do |var|
            var = ("LITERAL" + "LITERAL");Fi[	@ÇiI"        # value when not set. The inherited reader calls to a raw reader that acts like a normal attribute
        # reader but prefixes the attribute name with "raw_".
        def function(var)
          var.each do |var|
            var = ("LITERAL" + "LITERAL");Fi[	I":projects//compass_proj/compass/configuration/paths.rb;TiI"õ  TRAILING_SEPARATOR = /.(\/|#{Regexp.escape(File::SEPARATOR)}STRING/
  
  def function(var)
    var.each do |var|
      alias_method("STRING".to_sym, var);Fi[	@4I"when Hash then;Fi[[	@µiZI"    # Strings contained in x must be valid UTF-8.
    def function(var)
      case var
      when Hash then
        objenc(var);Fi[	@µieI"W    
    def function(var)
      case var
      when Hash then
        objenc(var);Fi[	@ßi0I"¢        var = Nokogiri::XML::Element.new(var, self, &var)
        var.each do |var|
          case var
          when Hash then
            var.each do |var, var|;Fi[	@ÄI"# @param options [Hash];Fi[[	@?i3I"ñ      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param options [Hash]
      # @return [Array];Fi[	@?iPI"ó      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param options [Hash]
      # @return [Object];Fi[	@?ijI"¢      # @param klass [Class]
      # @param request_method [Symbol]
      # @param path [String]
      # @param options [Hash]
      # @param method_name [Symbol];Fi[	@4I"when Integer then;Fi[[	@ViBI"}      # @return [Hash]
      def function(var, var)
        case var
        when Integer then
          var["LITERAL"] = var;Fi[	@?iäI"¶      # @return [Hash]
      def function(var, var, var)
        case var
        when Integer then
          var[[var, "STRING"].compact.join("STRING").to_sym] = var;Fi[	@?i®I"É        var, var = [], []
        var.flatten.each do |var|
          case var
          when Integer then
            (var << var);Fi[	I"9Sass::Script::List.new(var.value.dup, var.separator);F@ri[[	@ÖiI"¿    def function(var)
      var = if var.is_a?(Sass::Script::List) then
        Sass::Script::List.new(var.value.dup, var.separator)
      else
        Sass::Script::List.new([var], "LITERAL");Fi[	@ñi∫I"∫      var = var
      var = if var.is_a?(Sass::Script::List) then
        Sass::Script::List.new(var.value.dup, var.separator)
      else
        Sass::Script::List.new([var], "LITERAL");Fi[	@<i2I"∞  def function(var)
    if var.is_a?(Sass::Script::List) then
      Sass::Script::List.new(var.value.dup, var.separator)
    else
      Sass::Script::List.new([var], "LITERAL");Fi[	@∂@i[[	@ViI"®      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var);Fi[	@?iI"®      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var);Fi[	@?iI"®      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array<Twitter::User>]
      def function(var, var, var);Fi[	@ê@ƒ*i[[	@îiÿI"g      end
      
      def function(var, var, var)
        var = [var.first, var.last]
        var;Fi[	@îi›I"g      end
      
      def function(var, var, var)
        var = [var.first, var.last]
        var;Fi[	@îi‚I"g      end
      
      def function(var, var, var)
        var = [var.first, var.last]
        var;Fi[	I"# @return [Array];F@äi[[	@ëi∏I"p      
      # @param count [Integer]
      # @return [Array]
      def function(var, var)
        var = {};Fi[	@?i=I"û      # @param klass [Class]
      # @param array [Array]
      # @return [Array]
      def function(var, var)
        var.map { |var| var.fetch_or_new(var) };Fi[	@•i&I"«    # @param collection [Array]
    # @param cursor [Integer]
    # @return [Array]
    def function(var, var)
      var = @client.send(@method_name.to_sym, @method_options.merge("LITERAL" => (var)));Fi[	@s2@ˆ-i[[	@?i*I"¶      # @param path [String]
      # @param args [Array]
      # @return [Array]
      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var);Fi[	@?i5I"≠      # @param path [String]
      # @param options [Hash]
      # @return [Array]
      def function(var, var, var, var)
        var = send(var.to_sym, var, var)["LITERAL"];Fi[	@?iFI"¶      # @param path [String]
      # @param args [Array]
      # @return [Array]
      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var);Fi[	I""if var.delete("LITERAL") then;FI"Fvar = TemplateContext.ctx("LITERAL" => (var), "LITERAL" => (var));Fi[[	@*iPI"◊          else
            var = File.new(var).read
            if var.delete("LITERAL") then
              var = TemplateContext.ctx("LITERAL" => (var), "LITERAL" => (var))
              var = process_erb(var, var);Fi[	@*i_I"˚        var = targetize(install_location_for_stylesheet(var, var))
        var = File.new(var).read
        if var.delete("LITERAL") then
          var = TemplateContext.ctx("LITERAL" => (var), "LITERAL" => (var))
          var = process_erb(var, var);Fi[	@*iáI"          var = install_location_for_html(var, var) if respond_to?("LITERAL")
          var = File.read(templatize(var))
          if var.delete("LITERAL") then
            var = TemplateContext.ctx("LITERAL" => (var), "LITERAL" => (var))
            var = process_erb(var, var);Fi[	@€(I"<# @note This method can only return up to 3,200 Tweets.;Fi[[	@ëi(I"Û      # Returns the 20 most recent Tweets posted by the specified user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes;Fi[	@ëi@I"ı      # Returns the 20 most recent retweets posted by the specified user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes;Fi[	@ëiXI"˙      # Returns the 20 most recent retweets posted by the authenticating user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes;Fi[	@ê@ÿi[[	@’i"I"L    end
    
    def function(var, var, var)
      # do nothing
    end;Fi[	@wiI"y  module XML
    class ProcessingInstruction < Node
      def function(var, var, var)
        # do nothing
      end;Fi[	@ÄiI"Ö        ###
        # Called when an XML declaration is parsed
        def function(var, var, var)
          # do nothing
        end;Fi[	I"# debug options: -vhP;F@Ói[[	@•iI"Æ        ##
        # Performs the RSync::Local operation
        # debug options: -vhP
        def function
          Logger.message(("STRING" + @directories.join("STRING")));Fi[	@iI"        ##
        # Performs the RSync::Pull operation
        # debug options: -vhP
        def function
          begin;Fi[	@i-I"~        ##
        # Performs the RSync:Push operation
        # debug options: -vhP
        def function
          begin;Fi[	I"2var.on("STRING", "STRING", "STRING") do |var|;FI"require("pathname");Fi[[	@™iI"¡      ::Compass.configuration.require(library)
    end
    var.on("STRING", "STRING", "STRING") do |var|
      require("pathname")
      ::Compass.configuration.load(Pathname.new(var).realpath);Fi[	@™iI"’      ::Compass.configuration.load(Pathname.new(var).realpath)
    end
    var.on("STRING", "STRING", "STRING") do |var|
      require("pathname")
      ::Compass.configuration.discover(Pathname.new(var).realpath);Fi[	@™iI"‡      ::Compass.configuration.discover(Pathname.new(var).realpath)
    end
    var.on("STRING", "STRING", "STRING") do |var|
      require("pathname")
      ::Compass.configuration.add_import_path(Pathname.new(var).realpath);Fi[	@é2@®i[[	@ëi)I"⁄      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ëiAI"⁄      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ëiYI"⁄      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@‘&I"´# @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[[	@ëiLI"Ö      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @example Return the 20 most recent retweets posted by @sferik;Fi[	@ëicI"ï      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @example Return the 20 most recent retweets posted by the authenticating user;Fi[	@ëiäI"√      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @option options [Boolean, String, Integer] :include_entities The tweet entities node will be disincluded when set to false.;Fi[	@p(@ri[[	@1
iïI"ô      def function
        if (confirmed?.! or pending_reconfirmation?) then
          yield
        else
          self.errors.add("LITERAL", "LITERAL");Fi[	@ iI"~      def function
        if access_locked? then
          yield
        else
          self.errors.add("LITERAL", "LITERAL");Fi[	@	i'I"Ddef function
  if ARGV.verbose? then
    yield
  else
    begin;Fi[	@”@Íi[[	@≈i0I"Ñ      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param options [Hash] A customizable set of options.
      # @option options [Boolean, String, Integer] :skip_status Do not include user's Tweets when set to true, 't' or 1.;Fi[	@≈iQI"s      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param options [Hash] A customizable set of options.
      # @option options [String] :name Full name associated with the profile. Maximum of 20 characters.;Fi[	@≈isI"`      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::User] The authenticated user.
      # @param options [Hash] A customizable set of options.
      # @option options [String] :profile_background_color Profile background color.;Fi[	I"# Initializes a new object;F@—i[[	@Ci6I"P    end
    
    # Initializes a new object
    #
    # @param attrs [Hash];Fi[	@ iI"P    end
    
    # Initializes a new object
    #
    # @param attrs [Hash];Fi[	@fiI"wmodule Twitter
  class Relationship < Twitter::Base
    # Initializes a new object
    #
    # @param attrs [Hash];Fi[	@eI"J# Creates (if they don't exist yet) all the directories on the remote;Fi[[	@ÙiRI"≤      end
      
      ##
      # Creates (if they don't exist yet) all the directories on the remote
      # server in order to upload the backup file. Net::FTP does not support;Fi[	@iSI"ò      end
      
      ##
      # Creates (if they don't exist yet) all the directories on the remote
      # server in order to upload the backup file.;Fi[	@ÆiFI"≥      end
      
      ##
      # Creates (if they don't exist yet) all the directories on the remote
      # server in order to upload the backup file. Net::SFTP does not support;Fi[	@eI";# When set to true, the user will be notified by email;Fi[[	@èiI"Æ      alias "LITERAL" "LITERAL"
      
      ##
      # When set to true, the user will be notified by email
      # when a backup process ends without raising any exceptions;Fi[	@èiI"      # When set to true, the user will be notified by email
      # when a backup process ends without raising any exceptions
      ##
      # When set to true, the user will be notified by email
      # when a backup process is successful, but has warnings;Fi[	@èiI"      # When set to true, the user will be notified by email
      # when a backup process is successful, but has warnings
      ##
      # When set to true, the user will be notified by email
      # when a backup process raises an exception before finishing;Fi[	I"&# Returns an array of photo sizes;F@—i[[	@Qi
I"ß    attr_reader("LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL")
    
    # Returns an array of photo sizes
    #
    # @return [Array<Twitter::Size>];Fi[	@9iI"±      attr_reader("LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL")
      
      # Returns an array of photo sizes
      #
      # @return [Array<Twitter::Size>];Fi[	@ciI"Ömodule Twitter
  class ProfileBanner < Twitter::Base
    # Returns an array of photo sizes
    #
    # @return [Array<Twitter::Size>];Fi[	@∞(@Ói[[	@QiI"™    # Returns an array of photo sizes
    #
    # @return [Array<Twitter::Size>]
    def function
      @photo_sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|;Fi[	@9iI"Æ      # Returns an array of photo sizes
      #
      # @return [Array<Twitter::Size>]
      def function
        @sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|;Fi[	@ci
I"§    # Returns an array of photo sizes
    #
    # @return [Array<Twitter::Size>]
    def function
      @sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|;Fi[	@NI"module Commands;Fi[[	I"4projects//compass_proj/compass/commands/base.rb;Ti I"4module Compass
  module Commands
    class Base;Fi [	I"Aprojects//compass_proj/compass/commands/installer_command.rb;TiI"_require("compass/installers")
module Compass
  module Commands
    module InstallerCommand;Fi[	@+
i	I"årequire("compass/commands/base")
require("compass/commands/installer_command")
module Compass
  module Commands
    class ProjectBase < Base;Fi[	I"/var[var] = Twitter::Size.fetch_or_new(var);F@ài[[	@QiI"¢    def function
      @photo_sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|
        var[var] = Twitter::Size.fetch_or_new(var)
        var
      end;Fi[	@9iI"¶      def function
        @sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|
          var[var] = Twitter::Size.fetch_or_new(var)
          var
        end;Fi[	@ciI"ú    def function
      @sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|
        var[var] = Twitter::Size.fetch_or_new(var)
        var
      end;Fi[	@eI"4# Name of the database that needs to get dumped;Fi[[	@ÚiI"r      attr_accessor("LITERAL")
      
      ##
      # Name of the database that needs to get dumped
      ##;Fi[	@
iI"      attr_deprecate("LITERAL", "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => (lambda { |var, var| var.mysqldump_utility = var }))
      
      ##
      # Name of the database that needs to get dumped
      # To dump all databases, set this to `:all` or leave blank.;Fi[	@
iI"‹      attr_deprecate("LITERAL", "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => (lambda { |var, var| var.pg_dump_utility = var }))
      
      ##
      # Name of the database that needs to get dumped
      ##;Fi[	I"return false unless var;F@}i[[	@1i≤I"ê  def function(var)
    if Hardware.is_32_bit? and (MacOS.version == "LITERAL") then
      return false unless var
      raise("STRING")
    end;Fi[	@1i∂I"{    end
    unless (HOMEBREW_PREFIX.to_s == "STRING") then
      return false unless var
      raise("STRING")
    end;Fi[	@1i∫I"{    end
    unless (HOMEBREW_CELLAR.to_s == "STRING") then
      return false unless var
      raise("STRING")
    end;Fi[	I"require("twitter/entity");F@(i[[	@÷/i I"<require("twitter/entity")
module Twitter
  class Entity;Fi [	@Ÿ/i I"<require("twitter/entity")
module Twitter
  class Entity;Fi [	@‹/i I"<require("twitter/entity")
module Twitter
  class Entity;Fi [	I"var.first;F@ri[[	@qiI"†      var = @commands.keys.select { |var| var.to_s.=~(var) }
      if (var.size == "LITERAL") then
        var.first
      else
        if var =~ "LITERAL" then;Fi[	@ÇizI"í      else
        if ((var = var.rack.children).size == "LITERAL") and var.first.directory? then
          var.first
        else
          raise;Fi[	@îi^I"ß      def function(var, var, var)
        var = if var["LITERAL"].nil? then
          var.first
        else
          Node.new("LITERAL", [var.first, var["LITERAL"]]);Fi[	@e@¯)i[[	@~iI"õ      include(Backup::Configuration::Helpers)
      
      ##
      # Yields to the block the compressor command and filename extension.
      def function;Fi[	@äiI"|      end
      
      ##
      # Yields to the block the compressor command and filename extension.
      def function;Fi[	@çi#I"|      end
      
      ##
      # Yields to the block the compressor command and filename extension.
      def function;Fi[	@ˆ-@*i[[	@ñiI"¥    # dir  - The String path between the source and the file.
    # name - The String filename of the file.
    def function(var, var, var, var)
      @site = var
      @base = var;Fi[	@ôi%I"Ñ    #
    # Returns the new Post.
    def function(var, var, var, var)
      @site = var
      @base = File.join(var, var, "STRING");Fi[	@iI"≤    # dir  - The String path between <source> and the file.
    # name - The String filename of the file.
    def function(var, var, var, var)
      @site = var
      @base = var;Fi[	@îI"module Encryptor;Fi[[	@$i I"4module Backup
  module Encryptor
    class Base;Fi [	@‘i I":module Backup
  module Encryptor
    class GPG < Base;Fi [	@ûi I">module Backup
  module Encryptor
    class OpenSSL < Base;Fi [	@ˆ-@ˇi[[	@ViI"ê      end
      
      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_list!(var.options, var.pop);Fi[	@?i+I"≥      # @param args [Array]
      # @return [Array]
      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_user!(var.options, var.pop);Fi[	@?iGI"≥      # @param args [Array]
      # @return [Array]
      def function(var, var, var, var)
        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|;Fi[	@î@Øi[[	@ëi I"<module Backup
  module Configuration
    module Helpers;Fi [	I"8projects//backup_proj/backup/configuration/store.rb;TiI"Yrequire("ostruct")
module Backup
  module Configuration
    class Store < OpenStruct;Fi[	I"2projects//backup_proj/backup/configuration.rb;TiI"}  require(File.expand_path("../configuration/#{file}", "(string)"))
end
module Backup
  module Configuration
    extend(self);Fi[	@r'I"module StandAlone;Fi[[	@iI"dmodule Compass
  module AppIntegration
    module StandAlone
      module ConfigurationDefaults;Fi[	@iI"z  end
  
  module AppIntegration
    module StandAlone
      class Installer < Compass::Installers::ManifestInstaller;Fi[	@z'i	I"Xend
module Compass
  module AppIntegration
    module StandAlone
      extend(self);Fi[	@$@c/i[[	@èiàI"            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            var.parameters = ["STRING"]
          end;Fi[	@èiìI"            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            var.parameters = ["STRING"]
          end;Fi[	@I"each_value do |var|;Fi[[	@2iI"Î      assert_valid_keys(var, "LITERAL", "LITERAL", "LITERAL")
      var = { "LITERAL" => (true), "LITERAL" => (true) }.merge(var)
      if var["LITERAL"] then
        each_value do |var|
          unless var.dynamic_initial_state? then;Fi[	@2i'I"ë      end
      var = yield if block_given?
      if var["LITERAL"] then
        each_value do |var|
          if var.dynamic_initial_state? then;Fi[	@I")if @user.update_attributes(var) then;Fi[[	@Ci3I"°            end
          else
            if var["LITERAL"] then
              if @user.update_attributes(var) then
                I18n.locale = @user.language;Fi[	@CiDI"º                end
              else
                if var["LITERAL"] then
                  if @user.update_attributes(var) then
                    flash["LITERAL"] = I18n.t("STRING");Fi[	@0/@ài[[	@Öi,I"±              Sass::Script::Number.new(("LITERAL" - var.value), ["STRING"])
            else
              Compass::Util.compass_warn("STRING")
              var
            end;Fi[	@Öi0I"r            end
          else
            Compass::Util.compass_warn("STRING")
            var
          end;Fi[	@0/I"	next;Fi[[	@#iI"ã      var.each do |var|
        unless ASPECTS.include?(var) then
          Compass::Util.compass_warn("STRING")
          next
        end;Fi[	@#i1I"ã      var.each do |var|
        unless ASPECTS.include?(var) then
          Compass::Util.compass_warn("STRING")
          next
        end;Fi[	@‚I"class Base;Fi[[	@iI">module Compass
  module Installers
    class Base
    end;Fi[	@*iI"Mmodule Compass
  module Installers
    class Base
      include(Actions);Fi[	I"W(var, Compass.configuration.project_path = Compass.configuration.project_path, nil;FI"%Compass.configuration.serialize);Fi[[	@i!I"¡        def function
          begin
            (var, Compass.configuration.project_path = Compass.configuration.project_path, nil
            Compass.configuration.serialize)
          ensure;Fi[	@iI"∑      def function
        begin
          (var, Compass.configuration.project_path = Compass.configuration.project_path, nil
          Compass.configuration.serialize)
        ensure;Fi[	@î3@Ói[[	@i"I"·          begin
            (var, Compass.configuration.project_path = Compass.configuration.project_path, nil
            Compass.configuration.serialize)
          ensure
            Compass.configuration.project_path = var;Fi[	@iI"◊        begin
          (var, Compass.configuration.project_path = Compass.configuration.project_path, nil
          Compass.configuration.serialize)
        ensure
          Compass.configuration.project_path = var;Fi[	@@ri[[	@}'iI"õ      def function(var)
        if @project_types[var].nil? then
          raise(Compass::Error, "STRING")
        else
          eval(@project_types[var]);Fi[	@qiI"ú        else
          if (var.size > "LITERAL") then
            raise(Compass::Error, "STRING")
          else
            raise(Compass::Error, "STRING");Fi[	@ˆ-I""if supports_validations? then;Fi[[	@A
iI"¨      
      # Adds a validation error to the given object 
      def function(var, var, var, var)
        if supports_validations? then
          var = self.attribute(var);Fi[	@ÃiI"—      
      # Adds a validation error to the given object
      def function(var, var, var, var)
        if supports_validations? then
          var.errors.add(self.attribute(var), generate_message(var, var));Fi[	I"F#   # => Adds support for moz and webkit to the image() function.;F@—i[[	@#iI"Ê    #
    #   Compass::BrowserSupport.add_support("image", "moz", "webkit")
    #   # => Adds support for moz and webkit to the image() function.
    #
    # This function can be called one or more times in a compass configuration;Fi[	@#i)I"„    #
    #   Compass::BrowserSupport.remove_support("image", "o", "ms")
    #   # => Adds support for moz and webkit to the image() function.
    #
    # This function can be called one or more times in a compass configuration;Fi[	I"&unless ASPECTS.include?(var) then;F@0/i[[	@#iI"ö    def function(var, var)
      var.each do |var|
        unless ASPECTS.include?(var) then
          Compass::Util.compass_warn("STRING")
          next;Fi[	@#i0I"ö    def function(var, var)
      var.each do |var|
        unless ASPECTS.include?(var) then
          Compass::Util.compass_warn("STRING")
          next;Fi[	I"add_project_configuration;FI"1Compass.add_configuration(options, "STRING");Fi[[	@3iI"ï      
      def function
        add_project_configuration
        Compass.add_configuration(options, "STRING")
        Compass.discover_extensions!;Fi[	@+
iI"∂      
      def function
        add_project_configuration
        Compass.add_configuration(options, "STRING")
        Compass.discover_extensions! unless skip_extension_discovery?;Fi[	@ˆ-I"/var = var.exist? ? ("STRING") : ("STRING");Fi[[	@$iŸI"v  end
  
  def function(var, var, var, var)
    var = var.exist? ? ("STRING") : ("STRING")
    var = [@@svn, var];Fi[	@$iˆI"Œend
class UnsafeSubversionDownloadStrategy < SubversionDownloadStrategy
  def function(var, var, var, var)
    var = var.exist? ? ("STRING") : ("STRING")
    var = [@@svn, var, "STRING", "STRING", "STRING"];Fi[	@ˆ-I"var = super;Fi[[	@±
iI"        # Forces the change in state to be recognized regardless of whether the
        # state value actually changed
        def function(var, var, var, var)
          var = super
          if ((var == "LITERAL") or ((var == "LITERAL") and var)) and var.send("STRING").! then;Fi[	@M
iI"—      # Forces the change in state to be recognized regardless of whether the
      # state value actually changed
      def function(var, var, var, var)
        var = super
        var = self.attribute.to_sym;Fi[	@ˆ-@i[[	@ñiMI"á    end
    
    def function(var, var, var, var)
      assert_type(var, "LITERAL")
      var = var.value[(var.value - "LITERAL")].stop;Fi[	@?ióI"é  #
  #     background-position: 3px -36px;
  def function(var, var, var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL");Fi[	@ˆ-I"8var ||= self.options if self.respond_to?("LITERAL");Fi[[	@iI"    
    # copy/process a template in the compass template directory to the project directory.
    def function(var, var, var, var)
      var ||= self.options if self.respond_to?("LITERAL")
      var ? (var = File.new(var, "STRING").read) : (var = File.new(var).read);Fi[	@i$I"µ    
    # Write a file given the file contents as a string
    def function(var, var, var, var)
      var ||= self.options if self.respond_to?("LITERAL")
      var = var["LITERAL"];Fi[	@ë@ri[[	@Ÿ
iI"    if (var.length <= var) and var.! then
      var = "STRING"
      var = "LITERAL"
    else
      var = ("STRING" + Rails.application.routes.url_helpers.short_post_url(var, "LITERAL" => (AppConfig.pod_uri.scheme), "LITERAL" => (AppConfig.pod_uri.authority)));Fi[	@{i>I"Ω          var = ("STRING" + var) if var.!=("LITERAL") and @options["LITERAL"].!
          (var << "STRING")
          var = "LITERAL"
        else
          raise(SyntaxError.new("STRING"));Fi[	@ˆ-@ái[[	@ñi I"Z    end
    
    def function(var, var, var, var)
      var = "STRING"
      svg(var);Fi[	@>iI"Ú    #   and the rest of the string.
    #   `["Foo (Bar (Baz bang) bop)", " (Bang (bop bip))"]` in the example above.
    def function(var, var, var, var)
      var = "STRING"
      var = StringScanner.new(var) unless var.is_a?(StringScanner);Fi[	@ëI" Dir["STRING"].each do |var|;Fi[[	@≥iDI"k  
  def function
    var = "LITERAL"
    Dir["STRING"].each do |var|
      if File.exist?(var).! then;Fi[	@o*iI"¿      raise(ArgumentError, "STRING") unless File.directory?(var)
      FileUtils.mkdir_p("STRING")
      var = "LITERAL"
      Dir["STRING"].each do |var|
        next unless File.exists?(var);Fi[	@ëI",while var = Resque::Failure.all(var) do;Fi[[	@’i7I"ß    
    def self.requeue_queue(var)
      var = "LITERAL"
      while var = Resque::Failure.all(var) do
        Resque::Failure.requeue(var) if (var["STRING"] == var);Fi[	@’i?I"é    
    def self.remove_queue(var)
      var = "LITERAL"
      while var = Resque::Failure.all(var) do
        if (var["STRING"] == var) then;Fi[	I"var = (var + "LITERAL");F@ri[[	@≥i8I"ö        if $?.success? then
          (var << var.basename.to_s)
          var = (var + "LITERAL")
        else
          var = var.realpath if var.exist?;Fi[	@µiI"≠            if var then
              var[var] = ("STRING" << var)
              var = (var + "LITERAL")
            else
              var = (var + ucharenc(var, var, var));Fi[	@˘3@˘3i[[	@µiI"Á          when "STRING", "STRING", "STRING", "STRING" then
            var[var] = var[var]
            var = (var + "LITERAL")
            var = (var + "LITERAL")
          when "STRING", "STRING", "STRING", "STRING", "STRING" then;Fi[	@µiÙI"–          when "STRING", "STRING", "STRING", "STRING", "STRING" then
            var[var] = Unesc[var[var]]
            var = (var + "LITERAL")
            var = (var + "LITERAL")
          when "STRING" then;Fi[	@˘3I"next_line;Fi[[	@;i$I"◊        if (var.strip["LITERAL"] == "STRING") and (var.message == Error.message("LITERAL")) then
          ((var << "STRING") << @next_line.text)
          var = (var + "LITERAL")
          next_line
          retry;Fi[	@;i?I"ù        if var.eos? then
          ((var << "STRING") << @next_line.text)
          var = (var + "LITERAL")
          next_line
          var.scan("LITERAL");Fi[	I"2var["LITERAL"] = output_style if output_style;FI"#var["LITERAL"] = line_comments;Fi[[	@ÒiI"æ        end
        var = { "LITERAL" => (var) }
        var["LITERAL"] = output_style if output_style
        var["LITERAL"] = line_comments
        var["LITERAL"] = cache unless cache.nil?;Fi[	@Òi6I"ø      def function
        var = { "LITERAL" => (sass_load_paths) }
        var["LITERAL"] = output_style if output_style
        var["LITERAL"] = line_comments
        var["LITERAL"] = cache;Fi[	@7 @"i[[	@çiI"E  
  def function(var)
    @name = var
    super("STRING")
  end;Fi[	@çiI"E  
  def function(var)
    @name = var
    super("STRING")
  end;Fi[	@ 2I"if block_given? then;Fi[[	@∂iYI"œ      # When called without a block, returns the block that was previously set.
      def function(var)
        @set_attributes ||= {}
        if block_given? then
          @set_attributes["LITERAL"] = true;Fi[	@∂itI"©      #     asset_cache_buster :none
      def function(var, var)
        @set_attributes ||= {}
        if block_given? then
          @set_attributes["LITERAL"] = true;Fi[	@4I"&@set_attributes["LITERAL"] = true;Fi[[	@∂iZI"õ      def function(var)
        @set_attributes ||= {}
        if block_given? then
          @set_attributes["LITERAL"] = true
          @asset_host = var;Fi[	@∂iuI"®      def function(var, var)
        @set_attributes ||= {}
        if block_given? then
          @set_attributes["LITERAL"] = true
          @asset_cache_buster = var;Fi[	@l@ri[[	@∂i¨I"À        var = ((var.keys - ATTRIBUTES) - ARRAY_ATTRIBUTES)
        if (var.size == "LITERAL") then
          raise(Error, "STRING")
        else
          raise(Error, "STRING") if (var.size > "LITERAL");Fi[	@µiI"Ü          end
        when (var == "STRING"), (var < Spc) then
          raise(Error, "STRING")
        else
          (var[var] = var;Fi[	@xI"require("tempfile");Fi[[	I"$projects//backup_proj/backup.rb;TiI"Qrequire("rubygems")
require("fileutils")
require("tempfile")
require("yaml");Fi[	@‡i I"=require("fileutils")
require("tempfile")
module Whenever;Fi [	I"inherited_reader(*var);FI"inherited_writer(*var);Fi[[	@Çi!I"u        
        def function(var)
          inherited_reader(*var)
          inherited_writer(*var)
        end;Fi[	@ÇiPI"Ä        
        def function(var)
          inherited_reader(*var)
          inherited_writer(*var)
          var.each do |var|;Fi[	@x@i[[	@i I"?require("fileutils")
require("rubygems")
require("sequel");Fi [	@i I"?require("fileutils")
require("rubygems")
require("sequel");Fi [	@p(I"Cpush_silent("STRING", "LITERAL") unless @node.value["LITERAL"];Fi[[	@{ijI"œ        @node.value["LITERAL"] = @dont_indent_next_line
        @node.value["LITERAL"] = @dont_tab_up_next_text
        yield
        push_silent("STRING", "LITERAL") unless @node.value["LITERAL"]
      else;Fi[	@{i_I"•      flush_merged_text
      push_silent("STRING")
      yield
      push_silent("STRING", "LITERAL") unless @node.value["LITERAL"]
      (@precompiled << "STRING");Fi[	@@xi[[	@24i I"Arequire("rubygems")
require("fileutils")
require("tempfile");Fi [	@
iI"f  Dir[var].each { |var| require(f) }
end
require("rubygems")
require("fileutils")
require("time");Fi[	I"7var = "STRING" unless (var["LITERAL"] == "STRING");F@ái[[	@w$ibI"|      end
      if var then
        var = "STRING" unless (var["LITERAL"] == "STRING")
        var = "STRING"
      end;Fi[	@w$ièI"|      end
      if var then
        var = "STRING" unless (var["LITERAL"] == "STRING")
        var = "STRING"
      end;Fi[	I"require("sass");F@]i[[	@iI"Cbegin
  require("sass")
rescue LoadError
  require("rubygems");Fi[	@i
I"\  require("rubygems")
  begin
    require("sass")
  rescue LoadError
    puts("STRING");Fi[	I"&# concatenated, ready for the CLI;F@Ói[[	@•i*I"€        ##
        # Returns all the specified Rsync::Local options,
        # concatenated, ready for the CLI
        def function
          ([archive_option, mirror_option] + additional_options).compact.join("STRING");Fi[	@iBI"        ##
        # Returns all the specified Rsync::[Push/Pull] options,
        # concatenated, ready for the CLI
        def function
          ([archive_option, mirror_option, compress_option, port_option, password_option] + additional_options).compact.join("STRING");Fi[	I"Mvar.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true };F@g4i[[	@™iI"      self.options["LITERAL"] = true
    end
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true };Fi[	@™iI"<    end
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = false };Fi[	@g4I"Nvar.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = false };Fi[[	@™i I"o    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = false }
    var.on_tail("STRING", "STRING", "STRING", "STRING") do;Fi[	@≠i!I"Ã      self.options["LITERAL"] = var
    end
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = false }
  end;Fi[	I";var.on_tail("STRING", "STRING", "STRING", "STRING") do;F@ç%i[[	@™i"I"    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = true }
    var.on("STRING", "LITERAL", "STRING") { self.options["LITERAL"] = false }
    var.on_tail("STRING", "STRING", "STRING", "STRING") do
      puts(var)
      exit;Fi[	@1iPI"Ω          @options["LITERAL"] = true if RbConfig::CONFIG["STRING"] =~ "LITERAL"
        end
        var.on_tail("STRING", "STRING", "STRING", "STRING") do
          puts(var)
          exit;Fi[	I"$stderr.puts("STRING");F@a!i[[	@1iI"¨          $stderr.print("STRING") unless (var.class == RuntimeError)
          $stderr.puts("STRING")
          $stderr.puts("STRING")
          exit("LITERAL")
        end;Fi[	@1ióI"∏        var = var.message["LITERAL", "LITERAL"]
        raise(var) if (@options["LITERAL"] or (var.nil? or var.empty?))
        $stderr.puts("STRING")
        exit("LITERAL")
      end;Fi[	I"H# Once pre-configured defaults and RSync specific defaults are set,;F@¥&i[[	@•iI"Ê        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and RSync specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var);Fi[	@i"I"Ê        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and RSync specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var);Fi[	I"2require("compass/exec/global_options_parser");FI"3require("compass/exec/project_options_parser");Fi[[	@i I"vrequire("compass/exec/global_options_parser")
require("compass/exec/project_options_parser")
module Compass::Exec;Fi [	@i I"vrequire("compass/exec/global_options_parser")
require("compass/exec/project_options_parser")
module Compass::Exec;Fi [	@ã4I"module Compass::Exec;Fi[[	@iI"Ürequire("compass/exec/global_options_parser")
require("compass/exec/project_options_parser")
module Compass::Exec
  class SubCommandUI;Fi[	@iI"Çrequire("compass/exec/global_options_parser")
require("compass/exec/project_options_parser")
module Compass::Exec
  class SwitchUI;Fi[	I"(raise(var) if var.is_a?(SystemExit);FI"Qif (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then;Fi[[	@iI"⁄        return perform!
      rescue Exception => var
        raise(var) if var.is_a?(SystemExit)
        if (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then
          $stderr.puts(var.message);Fi[	@iI"”        perform!
      rescue Exception => var
        raise(var) if var.is_a?(SystemExit)
        if (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then
          $stderr.puts(var.message);Fi[	@ö4I"$stderr.puts(var.message);Fi[[	@iI"œ      rescue Exception => var
        raise(var) if var.is_a?(SystemExit)
        if (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then
          $stderr.puts(var.message)
        else;Fi[	@iI"œ      rescue Exception => var
        raise(var) if var.is_a?(SystemExit)
        if (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then
          $stderr.puts(var.message)
        else;Fi[	@°4@ri[[	@iI"¯        raise(var) if var.is_a?(SystemExit)
        if (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then
          $stderr.puts(var.message)
        else
          ::Compass::Exec::Helpers.report_error(var, (@options or {}));Fi[	@iI"        raise(var) if var.is_a?(SystemExit)
        if (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then
          $stderr.puts(var.message)
        else
          ::Compass::Exec::Helpers.report_error(var, @options);Fi[	I"1# are set via a super() call to RSync::Base,;F@T&i[[	@•iI"Õ        # Pre-configured defaults specified in
        # Configuration::Syncer::RSync::Local
        # are set via a super() call to RSync::Base,
        # which in turn will invoke Syncer::Base.
        #;Fi[	@iI"Õ        # Configuration::Syncer::RSync::Push or
        # Configuration::Syncer::RSync::Pull
        # are set via a super() call to RSync::Base,
        # which in turn will invoke Syncer::Base.
        #;Fi[	I"Avar.on("STRING", "STRING", "LITERAL", "STRING", "STRING") do;FI"(self.options["LITERAL"] = "LITERAL";Fi[[	@i<I"¥      var.separator("STRING")
      var.separator("STRING")
      var.on("STRING", "STRING", "LITERAL", "STRING", "STRING") do
        self.options["LITERAL"] = "LITERAL"
      end;Fi[	@i?I"Æ        self.options["LITERAL"] = "LITERAL"
      end
      var.on("STRING", "STRING", "LITERAL", "STRING", "STRING") do
        self.options["LITERAL"] = "LITERAL"
      end;Fi[	I"var = File.new(var).read;F@Ñ2i[[	@*iOI"“            copy(var, var, nil, var)
          else
            var = File.new(var).read
            if var.delete("LITERAL") then
              var = TemplateContext.ctx("LITERAL" => (var), "LITERAL" => (var));Fi[	@*i^I"Û        var = templatize(var)
        var = targetize(install_location_for_stylesheet(var, var))
        var = File.new(var).read
        if var.delete("LITERAL") then
          var = TemplateContext.ctx("LITERAL" => (var), "LITERAL" => (var));Fi[	I"=# This is the provider that Fog uses for the Cloud Files;F@Ói[[	@i?I"|        
        ##
        # This is the provider that Fog uses for the Cloud Files
        def function
          "STRING";Fi[	@i6I"|        
        ##
        # This is the provider that Fog uses for the Cloud Files
        def function
          "STRING";Fi[	I"S# Paths can use unix style "/" and will be corrected for the current platform.;F@õi[[	@*iîI"      
      # returns an absolute path given a path relative to the current installation target.
      # Paths can use unix style "/" and will be corrected for the current platform.
      def function(var)
        strip_trailing_separator(File.join(target_path, separate(var)));Fi[	@*iöI"      
      # returns an absolute path given a path relative to the current template.
      # Paths can use unix style "/" and will be corrected for the current platform.
      def function(var)
        strip_trailing_separator(File.join(template_path, separate(var)));Fi[	I"D# otherwise it will create it first and fetch use that instead.;F@Ói[[	@i9I"T        # Creates a new @repository_object (container).
        # Fetches it from Cloud Files if it already exists,
        # otherwise it will create it first and fetch use that instead.
        def function
          @repository_object ||= (connection.directories.get(container) or connection.directories.create("LITERAL" => (container)));Fi[	@i0I"Y        # Creates a new @repository_object (bucket).
        # Fetches it from S3 if it already exists,
        # otherwise it will create it first and fetch use that instead.
        def function
          @repository_object ||= (connection.directories.get(bucket) or connection.directories.create("LITERAL" => (bucket), "LITERAL" => (region)));Fi[	I"%($stderr.write(color("LITERAL"));FI"$$stdout.write(color("LITERAL"));Fi[[	@i!I"|    def function
      begin
        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL"))
        yield);Fi[	@i,I"|    def function
      begin
        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL"))
        yield);Fi[	@⁄4@Ìi[[	@i"I"}      begin
        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL"))
        yield)
      ensure;Fi[	@i-I"}      begin
        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL"))
        yield)
      ensure;Fi[	I"H# Once pre-configured defaults and Cloud specific defaults are set,;F@¥&i[[	@i&I"Ê        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and Cloud specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var);Fi[	@iI"Ê        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and Cloud specific defaults are set,
        # the block from the user's configuration file is evaluated.
        def function(var);Fi[	@I"Evar = Sass::Script::Color.new(["LITERAL", "LITERAL", "LITERAL"]);Fi[[	@i(I"¥  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var = Sass::Script::Color.new(["LITERAL", "LITERAL", "LITERAL"])
    mix(var, var, var);Fi[	@i/I"¥  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var = Sass::Script::Color.new(["LITERAL", "LITERAL", "LITERAL"])
    mix(var, var, var);Fi[	@I"<raise(Sass::SyntaxError, "STRING") unless var.unitless?;Fi[[	@#i&I"  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    raise(Sass::SyntaxError, "STRING") unless var.unitless?
    var = Math.log(var.value, var.value) rescue (Math.log(var.value) / Math.log(var.value));Fi[	@#i<I"˜  def function(var, var)
    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    raise(Sass::SyntaxError, "STRING") unless var.unitless?
    Sass::Script::Number.new((var.value ** var.value), var.numerator_units, var.denominator_units);Fi[	I"9# returns the opposite position of a side or corner.;F@õi[[	@ÖiI"¸      Sass::Script::Bool.new((var.is_a?(Sass::Script::List) and var.value.all? { |var| is_position(var).to_bool }))
    end
    # returns the opposite position of a side or corner.
    def function(var)
      var = if var.is_a?(Sass::Script::List) then;Fi[	@Öi@I"ë    end
  else
    # returns the opposite position of a side or corner.
    def function(var)
      var = var.value.split("LITERAL").map do |var|;Fi[	I"0var = if var.is_a?(Sass::Script::List) then;F@Z2i[[	@ÖiI"»    # returns the opposite position of a side or corner.
    def function(var)
      var = if var.is_a?(Sass::Script::List) then
        Sass::Script::List.new(var.value.dup, var.separator)
      else;Fi[	@ñiπI"ü    def function(var)
      var = var
      var = if var.is_a?(Sass::Script::List) then
        Sass::Script::List.new(var.value.dup, var.separator)
      else;Fi[	I"1# are set via a super() call to Cloud::Base,;F@T&i[[	@i#I"“        # Pre-configured defaults specified in
        # Configuration::Syncer::Cloud::CloudFiles
        # are set via a super() call to Cloud::Base,
        # which in turn will invoke Syncer::Base.
        #;Fi[	@iI"         # Pre-configured defaults specified in
        # Configuration::Syncer::Cloud::S3
        # are set via a super() call to Cloud::Base,
        # which in turn will invoke Syncer::Base.
        #;Fi[	I",if var.is_a?(Sass::Script::String) then;FI"var = (var + "STRING");Fi[[	@w$iiI"Å      end
      if var.to_bool then
        if var.is_a?(Sass::Script::String) then
          var = (var + "STRING")
        else;Fi[	@w$iñI"Å      end
      if var.to_bool then
        if var.is_a?(Sass::Script::String) then
          var = (var + "STRING")
        else;Fi[	I"#(var << color_stops.to_s(var));F@>i[[	@ñi`I"»      ((var << position.to_s(var)) << "STRING") if position
      ((var << shape_and_size.to_s(var)) << "STRING") if shape_and_size
      (var << color_stops.to_s(var))
      (var << "STRING")
    end;Fi[	@ñiãI"ß      var = "STRING"
      ((var << position_or_angle.to_s(var)) << "STRING") if position_or_angle
      (var << color_stops.to_s(var))
      (var << "STRING")
    end;Fi[	@4I"when String then;Fi[[	@µiÇI"Y    
    def function(var)
      case var
      when String then
        strenc(var);Fi[	@ i#I"v  #      or `Redis::Namespace`.
  def function(var)
    case var
    when String then
      if var["STRING"] then;Fi[	@4I""when Sass::Script::Color then;Fi[[	@ñi¶I"¢      var = var = nil
      var = Sass::Script::Parser.parse(var.value, "LITERAL", "LITERAL")
      case var
      when Sass::Script::Color then
        var = var;Fi[	@?i√I"É  
  def function(var)
    case var
    when Sass::Script::Color then
      Sass::Script::String.new(reversed_color_names[var.rgb]);Fi[	@Ø/@Íi[[	@ñiŒI"¬        case var.value
        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"])
        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"]);Fi[	@ñi–I"Â          Sass::Script::Number.new("LITERAL", ["STRING"])
        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"])
        when "LITERAL" then
          Sass::Script::Number.new("LITERAL", ["STRING"]);Fi[	I"ColorStop.new(var);F@ri[[	@ñi‡I"ô        else
          if Sass::Script::Color.===(var) then
            ColorStop.new(var)
          else
            if Sass::Script::List.===(var) then;Fi[	@ñiÊI"…            else
              if Sass::Script::String.===(var) and (var.value == "STRING") then
                ColorStop.new(var)
              else
                raise(Sass::SyntaxError, "STRING");Fi[	I"5# Amazon Simple Storage Service (S3) Credentials;F@ei[[	@	iI"{      
      ##
      # Amazon Simple Storage Service (S3) Credentials
      ##
      # Amazon S3 bucket name and path;Fi[	@iI"Å        
        ##
        # Amazon Simple Storage Service (S3) Credentials
        ##
        # The S3 bucket to store files to;Fi[	I"=# Returns Rsync syntax for defining a port to connect to;F@Ói[[	@ixI"w      
      ##
      # Returns Rsync syntax for defining a port to connect to
      def function
        "STRING";Fi[	@iNI"|        
        ##
        # Returns Rsync syntax for defining a port to connect to
        def function
          "STRING";Fi[	I"/# (temporary file containing the password);F@Ói[[	@ikI"≤      ##
      # Removes the previously created @password_file
      # (temporary file containing the password)
      def function
        @password_file.delete if @password_file;Fi[	@ifI"º        ##
        # Removes the previously created @password_file
        # (temporary file containing the password)
        def function
          @password_file.delete if @password_file;Fi[	I"4# Removes the previously created @password_file;F@I5i[[	@ijI"â      
      ##
      # Removes the previously created @password_file
      # (temporary file containing the password)
      def function;Fi[	@ieI"ì        
        ##
        # Removes the previously created @password_file
        # (temporary file containing the password)
        def function;Fi[	I"*def function(var, var, var, var, var);FI",var = [var, var].compact.join("STRING");Fi[[	@Äi2I"F        # +uri+ is the associated namespace URI
        # +ns+ is a hash of namespace prefix:urls associated with the element
        def function(var, var, var, var, var)
          var = [var, var].compact.join("STRING")
          var = (var.map { |var, var| [["STRING", var].compact.join("STRING"), var] } + var.map do |var|;Fi[	@ªi0I"”      end
      
      def function(var, var, var, var, var)
        var = [var, var].compact.join("STRING")
        var = (var.map { |var, var| [["STRING", var].compact.join("STRING"), var] } + var.map do |var|;Fi[	I"var.value.first;F@ri[[	@<i#I"à    assert_type(var, "LITERAL")
    if (var.value == "STRING") then
      var.value.first
    else
      if (var.value == "STRING") then;Fi[	@<i]I"É    else
      if defined? Sass::Script::List and var.is_a?(Sass::Script::List) then
        var.value.first
      else
        var;Fi[	@m1I"var.generate;Fi[[	@?i$I"u  #      }
  def function(var)
    verify_map(var, "STRING")
    var.generate
    inline_image(sprite_path(var));Fi[	@?i}I"•  # Returns a url to the sprite image.
  def function(var)
    verify_map(var, "STRING")
    var.generate
    generated_image_url(Sass::Script::String.new("STRING"));Fi[	@”1@m1i[[	@?i]I"√  # Returns the path to the original image file for the sprite with the given name
  def function(var, var)
    var = convert_sprite_name(var)
    verify_map(var, "STRING")
    verify_sprite(var);Fi[	@?iöI"∏    assert_type(var, "LITERAL")
    assert_type(var, "LITERAL")
    var = convert_sprite_name(var)
    verify_map(var, "STRING")
    unless var and var.is_a?(Sass::Script::String) then;Fi[	I"=# the rsync utility can read the password from this file;F@Ói[[	@i`I"Ω      ##
      # Writes the provided password to a temporary file so that
      # the rsync utility can read the password from this file
      def function
        unless password.nil? then;Fi[	@i[I"»        ##
        # Writes the provided password to a temporary file so that
        # the rsync utility can read the password from this file
        def function
          unless @password.nil? then;Fi[	@‘1I"verify_sprite(var);Fi[[	@?ilI"ß  def function(var, var)
    var = convert_sprite_name(var)
    verify_map(var)
    verify_sprite(var)
    Sass::Script::Bool.new(var.image_for(var.value).parent.nil?);Fi[	@?ivI"ü  def function(var, var, var)
    var = convert_sprite_name(var)
    verify_map(var)
    verify_sprite(var)
    unless VALID_SELECTORS.include?(var.value) then;Fi[	I"?# Writes the provided password to a temporary file so that;F@s5i[[	@i_I"¢      
      ##
      # Writes the provided password to a temporary file so that
      # the rsync utility can read the password from this file
      def function;Fi[	@iZI"¨        
        ##
        # Writes the provided password to a temporary file so that
        # the rsync utility can read the password from this file
        def function;Fi[	@¬1@5i[[	@w$ihI"ß        Compass.configuration.asset_host.call(var)
      end
      if var.to_bool then
        if var.is_a?(Sass::Script::String) then
          var = (var + "STRING");Fi[	@w$iïI"ß        Compass.configuration.asset_host.call(var)
      end
      if var.to_bool then
        if var.is_a?(Sass::Script::String) then
          var = (var + "STRING");Fi[	I"XNet::SSH.start(ip, username, "LITERAL" => (password), "LITERAL" => (port)) do |var|;FI"yield(var);Fi[[	@i3I"≈      # Establishes a connection to the remote server
      def function
        Net::SSH.start(ip, username, "LITERAL" => (password), "LITERAL" => (port)) do |var|
          yield(var)
        end;Fi[	@´i'I"Õ      # Net::SCP will use this connection to transfer backups
      def function
        Net::SSH.start(ip, username, "LITERAL" => (password), "LITERAL" => (port)) do |var|
          yield(var)
        end;Fi[	I"%# Server IP Address and SSH port;F@ei[[	@iI"y      # Server credentials
      ##
      # Server IP Address and SSH port
      ##
      # Path to store backups to;Fi[	@iI"Ä        # Server credentials
        ##
        # Server IP Address and SSH port
        ##
        # The SSH port to connect to;Fi[	I"var = $1;F@ri[[	@w$iQI"—      var = var.value
      if var =~ /^#{Regexp.escape(Compass.configuration.http_images_path)}STRING/ then
        var = $1
      else
        return Sass::Script::String.new("STRING") if absolute_path?(var);Fi[	@w$i~I"€      var = var.value
      if var =~ /^#{Regexp.escape(Compass.configuration.http_generated_images_path)}STRING/ then
        var = $1
      else
        return Sass::Script::String.new("STRING") if absolute_path?(var);Fi[	@5@ri[[	@w$ijI"£      if var.to_bool then
        if var.is_a?(Sass::Script::String) then
          var = (var + "STRING")
        else
          var = cache_busted_path(var, var);Fi[	@w$ióI"£      if var.to_bool then
        if var.is_a?(Sass::Script::String) then
          var = (var + "STRING")
        else
          var = cache_busted_path(var, var);Fi[	@√$@√$i[[	@Ri.I"ô      var = (self + File.basename(var))
    end
    var = var.to_s
    var = var.to_s
    raise("STRING") unless (File.symlink?(var) or File.exist?(var));Fi[	@RiPI"e      var = (self + File.basename(var))
    end
    var = var.to_s
    var = var.to_s
    mkpath;Fi[	I",# Establishes a connection to Amazon S3;F@Ói[[	@i"I"œ      
      ##
      # Establishes a connection to Amazon S3
      def function
        @connection ||= Fog::Storage.new("LITERAL" => (provider), "LITERAL" => (storage_token), "LITERAL" => (storage_secret));Fi[	@	i&I"È      
      ##
      # Establishes a connection to Amazon S3
      def function
        @connection ||= Fog::Storage.new("LITERAL" => (provider), "LITERAL" => (access_key_id), "LITERAL" => (secret_access_key), "LITERAL" => (region));Fi[	I"&var.split("STRING").each do |var|;F@¯!i[[	@Ùi\I"Ö      def function(var, var)
        var = Array.new
        var.split("STRING").each do |var|
          (var << var)
          begin;Fi[	@ÆiPI"Ö      def function(var, var)
        var = Array.new
        var.split("STRING").each do |var|
          (var << var)
          begin;Fi[	I"require("forwardable");F@Ni[[	@¬i I"Brequire("forwardable")
module Compass
  module SassExtensions;Fi [	@»i I"Brequire("forwardable")
module Compass
  module SassExtensions;Fi [	I"-# Calculates the overal image dimensions;FI"@# collects image sizes and input parameters for each sprite;Fi[[	@≈i%I"û        end
        
        # Calculates the overal image dimensions
        # collects image sizes and input parameters for each sprite
        def function;Fi[	@-iI"-        # Changing this string will invalidate all previously generated sprite images.
        # We should do so only when the packing algorithm changes
        # Calculates the overal image dimensions
        # collects image sizes and input parameters for each sprite
        # Calculates the height;Fi[	I"next if (var == "LITERAL");FI"%var = @images[(var - "LITERAL")];Fi[[	@≈iÄI"’              var.position.value
            end
            next if (var == "LITERAL")
            var = @images[(var - "LITERAL")]
            var.left = ((var.left + var.width) + [var.spacing, var.spacing].max);Fi[	@≈içI"⁄              var.position.value
            end).to_i
            next if (var == "LITERAL")
            var = @images[(var - "LITERAL")]
            var.top = ((var.top + var.height) + [var.spacing, var.spacing].max);Fi[	I""var, var = path_and_name(var);F@ài[[	@’iEI"]    
    def self.sprite_name(var)
      var, var = path_and_name(var)
      var
    end;Fi[	@’iJI"V    
    def self.path(var)
      var, var = path_and_name(var)
      var
    end;Fi[	I"var = Array.new;F@∑5i[[	@Ùi[I"¢      # already exists, so we have rescue it
      def function(var, var)
        var = Array.new
        var.split("STRING").each do |var|
          (var << var);Fi[	@ÆiOI"¢      # already exists, so we have rescue it
      def function(var, var)
        var = Array.new
        var.split("STRING").each do |var|
          (var << var);Fi[	I"rescue ArgumentError;F@ÿi[[	@µi
I"ë            begin
              var.force_encoding(document.encoding)
            rescue ArgumentError
              # do nothing
            end;Fi[	@«iI"|        trap("STRING", "STRING")
        trap("STRING", "STRING"))
      rescue ArgumentError
        # do nothing
      end;Fi[	I"+# already exists, so we have rescue it;F@äi[[	@ÙiYI"¯      # '/') and loop through that to create the directories one by one.
      # Net::FTP raises an exception when the directory it's trying to create
      # already exists, so we have rescue it
      def function(var, var)
        var = Array.new;Fi[	@ÆiMI"˘      # '/') and loop through that to create the directories one by one.
      # Net::SFTP raises an exception when the directory it's trying to create
      # already exists, so we have rescue it
      def function(var, var)
        var = Array.new;Fi[	I"K# directories. Instead, we split the parts up in to an array (for each;FI"G# '/') and loop through that to create the directories one by one.;Fi[[	@ÙiVI"t      # server in order to upload the backup file. Net::FTP does not support
      # paths to directories that don't yet exist when creating new
      # directories. Instead, we split the parts up in to an array (for each
      # '/') and loop through that to create the directories one by one.
      # Net::FTP raises an exception when the directory it's trying to create;Fi[	@ÆiJI"v      # server in order to upload the backup file. Net::SFTP does not support
      # paths to directories that don't yet exist when creating new
      # directories. Instead, we split the parts up in to an array (for each
      # '/') and loop through that to create the directories one by one.
      # Net::SFTP raises an exception when the directory it's trying to create;Fi[	I"B# paths to directories that don't yet exist when creating new;F@Ò5i[[	@ÙiUI"r      # Creates (if they don't exist yet) all the directories on the remote
      # server in order to upload the backup file. Net::FTP does not support
      # paths to directories that don't yet exist when creating new
      # directories. Instead, we split the parts up in to an array (for each
      # '/') and loop through that to create the directories one by one.;Fi[	@ÆiII"s      # Creates (if they don't exist yet) all the directories on the remote
      # server in order to upload the backup file. Net::SFTP does not support
      # paths to directories that don't yet exist when creating new
      # directories. Instead, we split the parts up in to an array (for each
      # '/') and loop through that to create the directories one by one.;Fi[	I"!create_remote_path(var, var);F@È#i[[	@Ùi;I"‘        var = remote_path_for(@package)
        connection do |var|
          create_remote_path(var, var)
          files_to_transfer_for(@package) do |var, var|
            Logger.message(("STRING" + "STRING"));Fi[	@Æi/I"‘        var = remote_path_for(@package)
        connection do |var|
          create_remote_path(var, var)
          files_to_transfer_for(@package) do |var, var|
            Logger.message(("STRING" + "STRING"));Fi[	@ÿ0@ 6i[[	@Ùi:I"µ      def function
        var = remote_path_for(@package)
        connection do |var|
          create_remote_path(var, var)
          files_to_transfer_for(@package) do |var, var|;Fi[	@Æi.I"µ      def function
        var = remote_path_for(@package)
        connection do |var|
          create_remote_path(var, var)
          files_to_transfer_for(@package) do |var, var|;Fi[	I"var = var.extract_options!;F@_i[[	@ÂiFI"ñ      #
      def function(var, var)
        var = var.extract_options!
        var = Devise::Mapping.find_scope!(var)
        var = (var.last or var);Fi[	@Âi∏I"∏      # parameters as the sign_in method.
      def function(var, var)
        var = var.extract_options!
        var = Devise::Mapping.find_scope!(var)
        var = (var.last or var);Fi[	@_I"var = (var.last or var);Fi[[	@ÂiGI"π      def function(var, var)
        var = var.extract_options!
        var = Devise::Mapping.find_scope!(var)
        var = (var.last or var)
        expire_session_data_after_sign_in!;Fi[	@ÂiπI"≠      def function(var, var)
        var = var.extract_options!
        var = Devise::Mapping.find_scope!(var)
        var = (var.last or var)
        sign_in(var, var, var);Fi[	I"-#   sign_out :user     # sign_out(scope);FI"0#   sign_out @user     # sign_out(resource);Fi[[	@Âi[I"Ç      # Examples:
      #
      #   sign_out :user     # sign_out(scope)
      #   sign_out @user     # sign_out(resource)
      #;Fi[	@øi1I"}    # Examples:
    #
    #   sign_out :user     # sign_out(scope)
    #   sign_out @user     # sign_out(resource)
    #;Fi[	@6@—i[[	@Âi\I"à      #
      #   sign_out :user     # sign_out(scope)
      #   sign_out @user     # sign_out(resource)
      #
      def function(var);Fi[	@øi2I"~    #
    #   sign_out :user     # sign_out(scope)
    #   sign_out @user     # sign_out(resource)
    #
    def function(var);Fi[	@ÿ0@ƒ i[[	@ÙiII"ª      def function(var)
        var = remote_path_for(var)
        connection do |var|
          transferred_files_for(var) do |var, var|
            Logger.message(("STRING" + "STRING"));Fi[	@Æi=I"ª      def function(var)
        var = remote_path_for(var)
        connection do |var|
          transferred_files_for(var) do |var, var|
            Logger.message(("STRING" + "STRING"));Fi[	@S'@Ói[[	@i1I"¥      
      ##
      # Establishes a connection to the remote server
      def function
        Net::SSH.start(ip, username, "LITERAL" => (password), "LITERAL" => (port)) do |var|;Fi[	@Æi#I"µ      
      ##
      # Establishes a connection to the remote server
      def function
        Net::SFTP.start(ip, username, "LITERAL" => (password), "LITERAL" => (port)) do |var|;Fi[	I"(var = var ? ("STRING") : ("STRING");F@ái[[	@%iI"·          ["LITERAL", "LITERAL"].each do |var|
            var.each do |var|
              var = var ? ("STRING") : ("STRING")
              var = "STRING"
              class_eval("STRING", "STRING", ("LITERAL" + "LITERAL"));Fi[	@/2iI"m  
  def function(var, var)
    var = var ? ("STRING") : ("STRING")
    var = "STRING"
    var.html_safe;Fi[	I"#   en:;FI"#     devise:;Fi[[	@∂iGI"j      # based on the current mapping:
      #
      #   en:
      #     devise:
      #       mailer:;Fi[	@∂iOI"Ñ      # If one does not exist, it fallbacks to ActionMailer default:
      #
      #   en:
      #     devise:
      #       mailer:;Fi[	@=6I"#       mailer:;Fi[[	@∂iHI"o      #
      #   en:
      #     devise:
      #       mailer:
      #         confirmation_instructions:;Fi[	@∂iPI"o      #
      #   en:
      #     devise:
      #       mailer:
      #         confirmation_instructions:;Fi[	@D6I")#         confirmation_instructions:;Fi[[	@∂iII"à      #   en:
      #     devise:
      #       mailer:
      #         confirmation_instructions:
      #           user_subject: '...';Fi[	@∂iQI"É      #   en:
      #     devise:
      #       mailer:
      #         confirmation_instructions:
      #           subject: '...';Fi[	I"!if var = var["LITERAL"] then;FI"(var = (var == true) ? (var) : (var);Fi[[	@: i-I"»    (ALL << var)
    var.assert_valid_keys("LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL")
    if var = var["LITERAL"] then
      var = (var == true) ? (var) : (var)
      STRATEGIES[var] = var;Fi[	@: i1I"ã      STRATEGIES[var] = var
    end
    if var = var["LITERAL"] then
      var = (var == true) ? (var) : (var)
      CONTROLLERS[var] = var;Fi[	I"Mtransferred_files_for(var) { |var, var| (var << ("STRING" + "STRING")) };FI"'Logger.message(var.join("STRING"));Fi[[	@ibI"”        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |var, var| (var << ("STRING" + "STRING")) }
        Logger.message(var.join("STRING"))
        connection.file_delete(var);Fi[	@´i@I"¿        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |var, var| (var << ("STRING" + "STRING")) }
        Logger.message(var.join("STRING"))
        var = [];Fi[	@5@ÿ0i[[	@ÙiHI"Õ      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        connection do |var|
          transferred_files_for(var) do |var, var|;Fi[	@Æi<I"Õ      # and a warning will be logged, containing the error message.
      def function(var)
        var = remote_path_for(var)
        connection do |var|
          transferred_files_for(var) do |var, var|;Fi[	I"var = var[var];FI"+next unless var.respond_to?("LITERAL");Fi[[	I"1projects//devise_proj/devise/param_filter.rb;TiI"∂      var = stringify_params(var.dup)
      @case_insensitive_keys.each do |var|
        var = var[var]
        next unless var.respond_to?("LITERAL")
        var[var] = var.downcase;Fi[	@l6iI"ó      end
      @strip_whitespace_keys.each do |var|
        var = var[var]
        next unless var.respond_to?("LITERAL")
        var[var] = var.strip;Fi[	I"loop do;FI"var = eat("STRING", var);Fi[[	@µiDI"∏      var[var] = var
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      loop do
        var = eat("STRING", var)
        var, var, var = pairparse(var);Fi[	@µi_I"∞      (var << var)
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      loop do
        var = eat("STRING", var)
        var, var = valparse(var);Fi[	I"4#   confirmation_period_valid?   # returns true;F@—i[[	@1
ixI"Ô      #
      #   # allow_unconfirmed_access_for = 1.day and confirmation_sent_at = today
      #   confirmation_period_valid?   # returns true
      #
      #   # allow_unconfirmed_access_for = 5.days and confirmation_sent_at = 4.days.ago;Fi[	@1
i{I"ı      #
      #   # allow_unconfirmed_access_for = 5.days and confirmation_sent_at = 4.days.ago
      #   confirmation_period_valid?   # returns true
      #
      #   # allow_unconfirmed_access_for = 5.days and confirmation_sent_at = 5.days.ago;Fi[	I"*self.errors.add("LITERAL", "LITERAL");F@ôi[[	@1
ióI"m          yield
        else
          self.errors.add("LITERAL", "LITERAL")
          false
        end;Fi[	@ i|I"m          yield
        else
          self.errors.add("LITERAL", "LITERAL")
          false
        end;Fi[	I"*# Options must contain the user email;F@õi[[	@1
i¿I"L        # confirmation instructions to it. If not, try searching for a user by unconfirmed_email
        # field. If no user is found, returns a new user with an email not found error.
        # Options must contain the user email
        def function(var)
          var = find_by_unconfirmed_email_with_errors(var) if reconfirmable;Fi[	@ iÖI"
        # unlock instructions to it. If not user is found, returns a new user
        # with an email not found error.
        # Options must contain the user email
        def function(var)
          var = find_or_initialize_with_errors(unlock_keys, var, "LITERAL");Fi[	@H@È#i[[	@˛i0I"ﬁ      # Transfers the archived file to the specified Cloud Files container
      def function
        var = remote_path_for(@package)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING");Fi[	@iRI"◊      # Transfers the archived file to the specified Dropbox folder
      def function
        var = remote_path_for(@package)
        files_to_transfer_for(@package) do |var, var|
          Logger.message("STRING");Fi[	I"var.delete("LITERAL");F@î6i[[	@ÌiJI"ã      #
      def function(var, var)
        var.delete("LITERAL")
        var.delete("LITERAL")
        var = update_attributes(var, *var);Fi[	@˛
i)I"        @state_requirements = [{ "LITERAL" => (build_matcher(var, "LITERAL", "LITERAL")), "LITERAL" => (build_matcher(var, "LITERAL", "LITERAL")) }]
      else
        var.delete("LITERAL")
        var.delete("LITERAL")
        @state_requirements = var.collect do |var, var|;Fi[	I"clean_up_passwords;F@ài[[	@Ìi9I"Q          false
        end
        clean_up_passwords
        var
      end;Fi[	@ÌiMI"~        var.delete("LITERAL")
        var = update_attributes(var, *var)
        clean_up_passwords
        var
      end;Fi[	@öI"Gself.data["STRING"] = "STRING" unless self.data.has_key?("STRING");Fi[[	@ñi'I"†    # Returns nothing.
    def function(var, var)
      super(var, var)
      self.data["STRING"] = "STRING" unless self.data.has_key?("STRING")
      self.data;Fi[	@ôiEI"†    # Returns nothing.
    def function(var, var)
      super(var, var)
      self.data["STRING"] = "STRING" unless self.data.has_key?("STRING")
      self.data;Fi[	@ÏI"?var.find_formula { |var| (var << var.basename.join(var)) };Fi[[	@≥iI"≥    raise("STRING") if var.directory?
    abort unless system("STRING")
    var = []
    var.find_formula { |var| (var << var.basename.join(var)) }
    var = link_tap_formula(var);Fi[	@≥iNI"À    var = "LITERAL"
    HOMEBREW_REPOSITORY.join("STRING").children.each do |var|
      var = []
      var.find_formula { |var| (var << var.basename.join(var)) }
      var = (var + link_tap_formula(var));Fi[	@Ï@Z6i[[	@iaI"«      def function(var)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |var, var| (var << ("STRING" + "STRING")) }
        Logger.message(var.join("STRING"));Fi[	@´i?I"«      def function(var)
        var = remote_path_for(var)
        var = []
        transferred_files_for(var) { |var, var| (var << ("STRING" + "STRING")) }
        Logger.message(var.join("STRING"));Fi[	@˚@¬,i[[	@.i4I"Ω      var = case read("LITERAL", (var + "LITERAL")).unpack("STRING")["LITERAL"]
      when "LITERAL", "LITERAL" then
        "LITERAL"
      when "LITERAL", "LITERAL" then
        "LITERAL";Fi[	@.i6I"        "LITERAL"
      when "LITERAL", "LITERAL" then
        "LITERAL"
      when "LITERAL", "LITERAL" then
        "LITERAL";Fi[	@Ï@Ôi[[	@»iAI"g        def function
          fast_fit
          var = []
          begin
            var = false;Fi[	@0iI"}      var = (var + "STRING")
      var = "LITERAL"
      var = []
      begin
        (var = ((var or "LITERAL") + "LITERAL");Fi[	I"self.password = var;FI"%self.password_confirmation = var;Fi[[	@—
iI"√      # the passwords are valid and the record was saved, false otherwise.
      def function(var, var)
        self.password = var
        self.password_confirmation = var
        if valid? then;Fi[	@.i∫I"•    self["LITERAL"] = "STRING"
    var = SecureRandom.hex("LITERAL")
    self.password = var
    self.password_confirmation = var
    self.save("LITERAL" => (false));Fi[	I"6#   reset_password_period_valid?   # returns true;F@—i[[	@—
i)I"Á      #
      #   # reset_password_within = 1.day and reset_password_sent_at = today
      #   reset_password_period_valid?   # returns true
      #
      #   # reset_password_within = 5.days and reset_password_sent_at = 4.days.ago;Fi[	@—
i,I"Ì      #
      #   # reset_password_within = 5.days and reset_password_sent_at = 4.days.ago
      #   reset_password_period_valid?   # returns true
      #
      #   # reset_password_within = 5.days and reset_password_sent_at = 5.days.ago;Fi[	I"*# Example: '-i -t -X/tmp/traffic.log';F@ei[[	@‡i^I"      # Note that this will override the defaults set by the Mail gem (currently: '-i -t')
      # So, if set here, be sure to set all the arguments you require.
      # Example: '-i -t -X/tmp/traffic.log'
      ##
      # When using the `:exim` `delivery_method` option,;Fi[	@‡igI".      # Note that this will override the defaults set by the Mail gem (currently: '-i -t')
      # So, if set here, be sure to set all the arguments you require.
      # Example: '-i -t -X/tmp/traffic.log'
      ##
      # Folder where mail will be kept when using the `:file` `delivery_method` option.;Fi[	I"&var = [nil, "LITERAL", "LITERAL"];FI"]var.add_module("LITERAL", "LITERAL" => "LITERAL", "LITERAL" => ({ "LITERAL" => (var) }));Fi[[	I",projects//devise_proj/devise/modules.rb;TiI"WDevise.with_options("LITERAL" => (true)) do |var|
  var.with_options("LITERAL" => (true)) do |var|
    var = [nil, "LITERAL", "LITERAL"]
    var.add_module("LITERAL", "LITERAL" => "LITERAL", "LITERAL" => ({ "LITERAL" => (var) }))
    var.add_module("LITERAL", "LITERAL" => "LITERAL", "LITERAL" => ({ "LITERAL" => (var) }), "LITERAL" => (true));Fi[	@‹6iI":  end
  var.add_module("LITERAL", "LITERAL" => "LITERAL", "LITERAL" => "LITERAL")
  var = [nil, "LITERAL", "LITERAL"]
  var.add_module("LITERAL", "LITERAL" => "LITERAL", "LITERAL" => ({ "LITERAL" => (var) }))
  var.add_module("LITERAL", "LITERAL" => "LITERAL", "LITERAL" => ({ "LITERAL" => ((var << "LITERAL")) }));Fi[	I"E# So, if set here, be sure to set all the arguments you require.;F@—6i[[	@‡i]I"      # Optional arguments to pass to `sendmail`
      # Note that this will override the defaults set by the Mail gem (currently: '-i -t')
      # So, if set here, be sure to set all the arguments you require.
      # Example: '-i -t -X/tmp/traffic.log'
      ##;Fi[	@‡ifI"      # Optional arguments to pass to `exim`
      # Note that this will override the defaults set by the Mail gem (currently: '-i -t')
      # So, if set here, be sure to set all the arguments you require.
      # Example: '-i -t -X/tmp/traffic.log'
      ##;Fi[	@`!@ri[[	@`i=I"a      end) then
        var, var = var
        warn("STRING")
      else
        add_actions;Fi[	@}iLI"¥        if var = var.detect { |var, var| var.attribute.!=(var.attribute) } then
          var, var = var
          warn("STRING")
        else
          add_predicate if var.empty?;Fi[	I"Y# Note that this will override the defaults set by the Mail gem (currently: '-i -t');F@·6i[[	@‡i\I"      ##
      # Optional arguments to pass to `sendmail`
      # Note that this will override the defaults set by the Mail gem (currently: '-i -t')
      # So, if set here, be sure to set all the arguments you require.
      # Example: '-i -t -X/tmp/traffic.log';Fi[	@‡ieI"      ##
      # Optional arguments to pass to `exim`
      # Note that this will override the defaults set by the Mail gem (currently: '-i -t')
      # So, if set here, be sure to set all the arguments you require.
      # Example: '-i -t -X/tmp/traffic.log';Fi[	I"#      end;F@—i[[	@ihI"æ    #      namespace :publisher do
    #        devise_for :account
    #      end
    #
    #    Will use publisher/sessions controller instead of devise/sessions controller. You can revert;Fi[	@i¶I"u    #      def update
    #         # do something different here
    #      end
    #
    #      def deactivate;Fi[	I"#     devise_for :users;F@–i[[	@iâI"O    #
    #   scope "/my" do
    #     devise_for :users
    #   end
    #;Fi[	@iêI"S    #
    #   scope ":locale" do
    #     devise_for :users
    #   end
    #;Fi[	I"Gvar["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?;F@7i[[	@i∑I"      @devise_finalized = false
      var = var.extract_options!
      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?
      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?
      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?;Fi[	@i∏I"J      var = var.extract_options!
      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?
      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?
      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?
      var["LITERAL"] = (@scope["LITERAL"] or {}).merge((var["LITERAL"] or {}));Fi[	I"Mvar["LITERAL"] = (@scope["LITERAL"] or {}).merge((var["LITERAL"] or {}));F@
7i[[	@i∫I"~      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?
      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?
      var["LITERAL"] = (@scope["LITERAL"] or {}).merge((var["LITERAL"] or {}))
      var["LITERAL"] = (@scope["LITERAL"] or {}).merge((var["LITERAL"] or {}))
      var["LITERAL"] = (@scope["LITERAL"] or {}).merge((var["LITERAL"] or {}));Fi[	@iªI"f      var["LITERAL"] ||= @scope["LITERAL"] if @scope["LITERAL"].present?
      var["LITERAL"] = (@scope["LITERAL"] or {}).merge((var["LITERAL"] or {}))
      var["LITERAL"] = (@scope["LITERAL"] or {}).merge((var["LITERAL"] or {}))
      var["LITERAL"] = (@scope["LITERAL"] or {}).merge((var["LITERAL"] or {}))
      var["LITERAL"] = (@scope["LITERAL"] or {});Fi[	@Ö3@ri[[	@&i5I"s      else
        if var.file?.! then
          next
        else
          if (var.extname == "STRING") then;Fi[	@:iI"â      if var.dry_run and var.overwrite then
        print("STRING") { var.link(var) }
        next
      else
        if var.dry_run then;Fi[	I"-#     root :to => "admin/dashboard#show";F@–i[[	@iËI"Ç    #
    #   authenticate :user, lambda {|u| u.role == "admin"} do
    #     root :to => "admin/dashboard#show"
    #   end
    #;Fi[	@iˇI"É    #
    #   authenticated :user, lambda {|u| u.role == "admin"} do
    #     root :to => "admin/dashboard#show"
    #   end
    #;Fi[	@}@ri[[	@iwI"∫    def function(var)
      if ::OmniAuth.config.path_prefix and ::OmniAuth.config.path_prefix.!=(var) then
        raise("STRING")
      else
        ::OmniAuth.config.path_prefix = var;Fi[	@1iÔI"Ñ            raise("STRING")
          when ::Haml::Error then
            raise("STRING")
          else
            raise("STRING");Fi[	I"&require("devise/strategies/base");F@i[[	@Îi I"Hrequire("devise/strategies/base")
module Devise
  module Strategies;Fi [	@‡i I"Hrequire("devise/strategies/base")
module Devise
  module Strategies;Fi [	I"2#   * If all authentication keys are present;;F@—i[[	@Îi>I"À      #   * Validating if the model allows params authentication;
      #   * If any of the authorization headers were sent;
      #   * If all authentication keys are present;
      #
      def function;Fi[	@ÎiII"÷      #   * If the request hits the sessions controller through POST;
      #   * If the params[scope] returns a hash with credentials;
      #   * If all authentication keys are present;
      #
      def function;Fi[	I"1require("devise/strategies/authenticatable");F@i[[	@Ä i I"Srequire("devise/strategies/authenticatable")
module Devise
  module Strategies;Fi [	@∞i I"Srequire("devise/strategies/authenticatable")
module Devise
  module Strategies;Fi [	@‘I"arrenc(var);Fi[[	@µi]I"b      when Hash then
        objenc(var)
      when Array then
        arrenc(var)
      else;Fi[	@µihI"n      when Hash then
        objenc(var)
      when Array then
        arrenc(var)
      when String then;Fi[	@?2I"objenc(var);Fi[[	@µi[I"h    def function(var)
      case var
      when Hash then
        objenc(var)
      when Array then;Fi[	@µifI"h    def function(var)
      case var
      when Hash then
        objenc(var)
      when Array then;Fi[	@M@ái[[	@0iNI"ù        end
      when "STRING" then
        var = var["STRING"]
        var = "STRING"
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?;Fi[	@0iRI"“        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?
      when "STRING" then
        var = var["STRING"]
        var = "STRING"
        var["STRING"]["STRING"].each { |var| (var << "STRING") };Fi[	@M@ii[[	@„iI"v    def self.build(var, var)
      var = var["STRING"]
      var = var["STRING"]
      puts("STRING")
      begin;Fi[	@„i!I"ë      require("directory_watcher")
      var = var["STRING"]
      var = var["STRING"]
      puts("STRING")
      var = DirectoryWatcher.new(var);Fi[	I"var, var = case var;F@Íi[[	@›iHI"{      #
      def function(var)
        var, var = case var
        when "LITERAL" then
          ["STRING", success_color];Fi[	@‡iÇI"x      #
      def function(var)
        var, var = case var
        when "LITERAL" then
          ["STRING", false];Fi[	I"require("set");F@fi[[	@Â	i I"6require("set")
module Jekyll
  module Convertible;Fi [	@bi I".require("set")
module Jekyll
  class Site;Fi [	I"require("securerandom");F@i[[	@: i
I"prequire("orm_adapter")
require("set")
require("securerandom")
module Devise
  autoload("LITERAL", "STRING");Fi[	@±$i I">require("securerandom")
module Devise
  module Generators;Fi [	@¨$I"9class DeviseGenerator < Rails::Generators::NamedBase;Fi[[	@èiI"åmodule Devise
  module Generators
    class DeviseGenerator < Rails::Generators::NamedBase
      include(Rails::Generators::ResourceHelpers);Fi[	@<iI"≤require("generators/devise/orm_helpers")
module Mongoid
  module Generators
    class DeviseGenerator < Rails::Generators::NamedBase
      include(Devise::Generators::OrmHelpers);Fi[	I"K# One of :yellow, :red, :green, :purple, or :random. (default: yellow);F@ei[[	@›i!I"ƒ      ##
      # The background color of a success message.
      # One of :yellow, :red, :green, :purple, or :random. (default: yellow)
      ##
      # The background color of a warning message.;Fi[	@›i$I"√      ##
      # The background color of a warning message.
      # One of :yellow, :red, :green, :purple, or :random. (default: yellow)
      ##
      # The background color of an error message.;Fi[	I"@segment = t("STRING");F@W
i[[	I"=projects//diaspora_proj/controllers/admins_controller.rb;Ti3I"Å    when "STRING" then
      var = "LITERAL".week
      @segment = t("STRING")
    when "STRING" then
      var = "LITERAL".weeks;Fi[	@y7i6I"Ç    when "STRING" then
      var = "LITERAL".weeks
      @segment = t("STRING")
    when "STRING" then
      var = "LITERAL".month;Fi[	@F0@Li[[	@#inI"‚    if var then
      if current_user.update_post(var, params["LITERAL"]) then
        flash.now["LITERAL"] = I18n.t("STRING")
        respond_to do |var|
          var.js { render("LITERAL" => (var), "LITERAL" => "LITERAL") };Fi[	@#isI"ó        end
      else
        flash.now["LITERAL"] = I18n.t("STRING")
        respond_to do |var|
          var.html { redirect_to(["LITERAL", var]) };Fi[	@ÍI""((var + "STRING") + "STRING");Fi[[	@ÜiII"∞        var = "STRING" unless var =~ "LITERAL"
        case var.value["LITERAL"]
        when "LITERAL" then
          ((var + "STRING") + "STRING")
        when "LITERAL" then;Fi[	@ÜiKI"£        when "LITERAL" then
          ((var + "STRING") + "STRING")
        when "LITERAL" then
          ((var + "STRING") + "STRING")
        when "LITERAL" then;Fi[	@=0I"-var.html { redirect_to("LITERAL", var) };Fi[[	I"=projects//diaspora_proj/controllers/blocks_controller.rb;TiI"¬      var = { "LITERAL" => (t("STRING")) }
    end
    respond_with do |var|
      var.html { redirect_to("LITERAL", var) }
      var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") };Fi[	@é7iI"¬      var = { "LITERAL" => (t("STRING")) }
    end
    respond_with do |var|
      var.html { redirect_to("LITERAL", var) }
      var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") };Fi[	@ÍI"iif var.value["LITERAL"].is_a?(Nokogiri::CSS::Node) and (var.value["LITERAL"].type == "LITERAL") then;Fi[[	@ÜiI"Â        when "LITERAL" then
          "STRING"
        when "LITERAL" then
          if var.value["LITERAL"].is_a?(Nokogiri::CSS::Node) and (var.value["LITERAL"].type == "LITERAL") then
            an_plus_b(var.value["LITERAL"]);Fi[	@ÜiI"Ó            "STRING"
          end
        when "LITERAL" then
          if var.value["LITERAL"].is_a?(Nokogiri::CSS::Node) and (var.value["LITERAL"].type == "LITERAL") then
            an_plus_b(var.value["LITERAL"], "LITERAL" => (true));Fi[	@ÍI"var.bottle;Fi[[	@ÙiI"á      var = SnowLeopardBottleSpecTestBall.new
      assert_equal(case MacOS.cat
      when "LITERAL" then
        var.bottle
      else;Fi[	@Ùi!I"Ä      var = LionBottleSpecTestBall.new
      assert_equal(case MacOS.cat
      when "LITERAL" then
        var.bottle
      else;Fi[	@LI"Bvar.js { render("LITERAL" => (var), "LITERAL" => "LITERAL") };Fi[[	@ïiI"ˆ      flash.now["LITERAL"] = I18n.t("STRING")
      var = var ? (var.errors.full_messages) : (t("STRING"))
      respond_to do |var|
        var.js { render("LITERAL" => (var), "LITERAL" => "LITERAL") }
        var.html { redirect_to("LITERAL") };Fi[	@#ioI"ﬁ      if current_user.update_post(var, params["LITERAL"]) then
        flash.now["LITERAL"] = I18n.t("STRING")
        respond_to do |var|
          var.js { render("LITERAL" => (var), "LITERAL" => "LITERAL") }
        end;Fi[	@ÍI"8@compressed_filename = (@patch_filename + "STRING");Fi[[	@iSI"π    detect_compression!
    case @compression
    when "LITERAL" then
      @compressed_filename = (@patch_filename + "STRING")
      FileUtils.mv(@patch_filename, @compressed_filename);Fi[	@iVI"ˇ      @compressed_filename = (@patch_filename + "STRING")
      FileUtils.mv(@patch_filename, @compressed_filename)
    when "LITERAL" then
      @compressed_filename = (@patch_filename + "STRING")
      FileUtils.mv(@patch_filename, @compressed_filename);Fi[	@ÍI"(self / "STRING");Fi[[	@iKI"s  def function(var)
    var = case var
    when "LITERAL" then
      (self / "STRING")
    when "LITERAL" then;Fi[	@iMI"m    when "LITERAL" then
      (self / "STRING")
    when "LITERAL" then
      (self / "STRING")
    else;Fi[	@L@ﬂ/i[[	@
i I"Á    var = current_user.tag_followings.find_by_tag_id(params["STRING"])
    if var and var.destroy then
      respond_to do |var|
        var.any("LITERAL", "LITERAL") do
          render("LITERAL" => (true), "LITERAL" => "LITERAL");Fi[	@
i&I"ì      end
    else
      respond_to do |var|
        var.any("LITERAL", "LITERAL") do
          render("LITERAL" => (true), "LITERAL" => "LITERAL");Fi[	@LI"-var.html { redirect_to(post_path(var)) };Fi[[	@i7I"  def function
    var = Post.visible_from_author(@post.author, current_user).newer(@post)
    respond_to do |var|
      var.html { redirect_to(post_path(var)) }
      var.json { render("LITERAL" => (PostPresenter.new(var, current_user))) };Fi[	@i?I"  def function
    var = Post.visible_from_author(@post.author, current_user).older(@post)
    respond_to do |var|
      var.html { redirect_to(post_path(var)) }
      var.json { render("LITERAL" => (PostPresenter.new(var, current_user))) };Fi[	@LI"(var.html { redirect_to("LITERAL") };Fi[[	@õ!i5I"˜      current_user.participate!(@status_message)
      flash["LITERAL"] = successful_mention_message if coming_from_profile_page?
      respond_to do |var|
        var.html { redirect_to("LITERAL") }
        var.mobile { redirect_to(stream_path) };Fi[	@õ!i=I"à      end
    else
      respond_to do |var|
        var.html { redirect_to("LITERAL") }
        var.mobile { redirect_to(stream_path) };Fi[	I"if @contact then;FI",@aspects_with_person = @contact.aspects;Fi[[	@#iI"⁄      @is_contact = (@person.!=(current_user.person) and @contact)
      @aspects_with_person = []
      if @contact then
        @aspects_with_person = @contact.aspects
        @contacts_of_contact = @contact.contacts;Fi[	@õ!iI"Ÿ      @contact = current_user.contact_for(@person)
      @aspects_with_person = []
      if @contact then
        @aspects_with_person = @contact.aspects
        @aspect_ids = @aspects_with_person.map { |var| var.id };Fi[	I"var.html do;F@0i[[	@ñ!iI"œ      respond_to do |var|
        var.js { render("LITERAL" => (I18n.t("STRING")), "LITERAL" => "LITERAL") }
        var.html do
          flash["LITERAL"] = I18n.t("STRING")
          redirect_to("LITERAL");Fi[	@#iTI"Í      respond_to do |var|
        var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
        var.html do
          flash["LITERAL"] = I18n.t("STRING")
          if StatusMessage.find_by_guid(var.status_message_guid) then;Fi[	@ÍI"BazaarDownloadStrategy;Fi[[	@$i˘I"~    when "LITERAL" then
      CVSDownloadStrategy
    when "LITERAL" then
      BazaarDownloadStrategy
    when "LITERAL" then;Fi[	@$iI"|  def self.detect_from_symbol(var)
    case var
    when "LITERAL" then
      BazaarDownloadStrategy
    when "LITERAL" then;Fi[	@0I"redirect_to("LITERAL");Fi[[	@ñ!iI"¡        var.js { render("LITERAL" => (I18n.t("STRING")), "LITERAL" => "LITERAL") }
        var.html do
          flash["LITERAL"] = I18n.t("STRING")
          redirect_to("LITERAL")
        end;Fi[	@.0iFI"í  def function
    unless AppConfig.settings.invitations.open? then
      flash["LITERAL"] = I18n.t("STRING")
      redirect_to("LITERAL")
    end;Fi[	I")var = { "LITERAL" => (t("STRING")) };F@ri[[	@é7iI"õ    if var.save then
      disconnect_if_contact(var.person)
      var = { "LITERAL" => (t("STRING")) }
    else
      var = { "LITERAL" => (t("STRING")) };Fi[	@é7iI"¨  def function
    if current_user.blocks.find(params["LITERAL"]).delete then
      var = { "LITERAL" => (t("STRING")) }
    else
      var = { "LITERAL" => (t("STRING")) };Fi[	@ã7@0i[[	@é7iI"ü    end
    respond_with do |var|
      var.html { redirect_to("LITERAL", var) }
      var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
    end;Fi[	@é7iI"ü    end
    respond_with do |var|
      var.html { redirect_to("LITERAL", var) }
      var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") }
    end;Fi[	@ÍI"CVSDownloadStrategy;Fi[[	@$i˜I"{  def self.detect_from_url(var)
    case var
    when "LITERAL" then
      CVSDownloadStrategy
    when "LITERAL" then;Fi[	@$i I"|    when "LITERAL" then
      CurlDownloadStrategy
    when "LITERAL" then
      CVSDownloadStrategy
    when "LITERAL" then;Fi[	I" render("LITERAL" => (true));F@ri[[	@3iMI"ü    end
    if (session["LITERAL"] == true) and request.format.html? then
      render("LITERAL" => (true))
    else
      if render("LITERAL" => (false)) then;Fi[	@9i~I"»    if params["LITERAL"] then
      Webfinger.in_background(params["LITERAL"], "LITERAL" => (true))
      render("LITERAL" => (true))
    else
      render("LITERAL" => (true), "LITERAL" => "LITERAL");Fi[	I"#flash["LITERAL"] = t("STRING");F@ri[[	@.0i3I"j    end
    if var.empty? then
      flash["LITERAL"] = t("STRING")
    else
      if var.empty? then;Fi[	@CibI"¥    else
      if params["LITERAL"].present? and params["LITERAL"]["LITERAL"].present? then
        flash["LITERAL"] = t("STRING")
      else
        flash["LITERAL"] = t("STRING");Fi[	@+0@3i[[	@RiI"q      case var
      when Array then
        if var.empty? then
          opoo("STRING")
          return [];Fi[	@RiI"è        var.each { |var| (var << install_p(var)) }
      when Hash then
        if var.empty? then
          opoo("STRING")
          return [];Fi[	@Ì @ri[[	@4
iI"ï      @user.seed_aspects
      sign_in_and_redirect("LITERAL", @user)
      Rails.logger.info("STRING")
    else
      @user.errors.delete("LITERAL");Fi[	@ı i>I"ò    else
      if (self.target.nil? or self.target.author.!=(self.person)) then
        Rails.logger.info("STRING")
      else
        self.perform(var);Fi[	@ÍI"Xif @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then;Fi[[	@inI"”        @compilers.include?("LITERAL") ? ("LITERAL") : (ENV.compiler)
      end
    when "LITERAL" then
      if @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then
        "LITERAL";Fi[	@ixI"ô        end
      end
    when "LITERAL" then
      if @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then
        "LITERAL";Fi[	@ 'I"@user = current_user;Fi[[	@CiI"i  
  def function
    @aspect = "LITERAL"
    @user = current_user
    @email_prefs = Hash.new(true);Fi[	@CixI"b  
  def function
    @aspect = "LITERAL"
    @user = current_user
    @person = @user.person;Fi[	I"1@contact = current_user.contact_for(@person);FI"@aspects_with_person = [];Fi[[	@9iUI"Ó      if current_user then
        @block = current_user.blocks.where("LITERAL" => (@person.id)).first
        @contact = current_user.contact_for(@person)
        @aspects_with_person = []
        if @contact and params["LITERAL"].! then;Fi[	@õ!iI"‰    if params["LITERAL"] and @person = Person.where("LITERAL" => (params["LITERAL"])).first then
      @aspect = "LITERAL"
      @contact = current_user.contact_for(@person)
      @aspects_with_person = []
      if @contact then;Fi[	@$8@ 7i[[	@#iI"‹      @contact = current_user.contact_for(@person)
      @is_contact = (@person.!=(current_user.person) and @contact)
      @aspects_with_person = []
      if @contact then
        @aspects_with_person = @contact.aspects;Fi[	@õ!iI"≥      @aspect = "LITERAL"
      @contact = current_user.contact_for(@person)
      @aspects_with_person = []
      if @contact then
        @aspects_with_person = @contact.aspects;Fi[	@À7I"7@aspect_ids = @aspects_with_person.map(&"LITERAL");Fi[[	@9iXI"         @aspects_with_person = []
        if @contact and params["LITERAL"].! then
          @aspects_with_person = @contact.aspects
          @aspect_ids = @aspects_with_person.map(&"LITERAL")
          @contacts_of_contact_count = @contact.contacts.count;Fi[	@9iãI"<      @contacts_of_contact = @contact.contacts.paginate("LITERAL" => (params["LITERAL"]), "LITERAL" => ((params["LITERAL"] or "LITERAL")))
      @hashes = hashes_for_people(@contacts_of_contact, @aspects)
      @aspects_with_person = @contact.aspects
      @aspect_ids = @aspects_with_person.map(&"LITERAL")
    else;Fi[	@Í@{i[[	@‡iwI"j  def function
    case @reason
    when "LITERAL" then
      "STRING".undent
    when "LITERAL" then;Fi[	@‡iyI"i    when "LITERAL" then
      "STRING".undent
    when "LITERAL" then
      "STRING".undent
    else;Fi[	@ÍI"@Parallel.each(all_file_names, { "LITERAL" => (var) }, &var);Fi[[	@&
iLI"“            when FalseClass then
              all_file_names.each(&var)
            when "LITERAL" then
              Parallel.each(all_file_names, { "LITERAL" => (var) }, &var)
            when "LITERAL" then;Fi[	@&
iNI"‰            when "LITERAL" then
              Parallel.each(all_file_names, { "LITERAL" => (var) }, &var)
            when "LITERAL" then
              Parallel.each(all_file_names, { "LITERAL" => (var) }, &var)
            else;Fi[	I"5@person = Person.find_by_guid(params["LITERAL"]);FI"if @person then;Fi[[	@9iÖI"ç  
  def function
    @person = Person.find_by_guid(params["LITERAL"])
    if @person then
      @contact = current_user.contact_for(@person);Fi[	@#iI"ì  def function
    @post_type = "LITERAL"
    @person = Person.find_by_guid(params["LITERAL"])
    if @person then
      @profile = @person.profile;Fi[	I"C@photo = current_user.build_post("LITERAL", params["LITERAL"]);FI"if @photo.save then;Fi[[	@#i+I"¡    rescuing_photo_errors do
      if remotipart_submitted? then
        @photo = current_user.build_post("LITERAL", params["LITERAL"])
        if @photo.save then
          respond_to do |var|;Fi[	@#iùI"‚    end
    params["LITERAL"]["LITERAL"] = file_handler(params)
    @photo = current_user.build_post("LITERAL", params["LITERAL"])
    if @photo.save then
      var = current_user.aspects_from_ids(params["LITERAL"]["LITERAL"]);Fi[	I"Lvar = current_user.photos.where("LITERAL" => (params["LITERAL"])).first;F@„"i[[	@#iOI"ç  
  def function
    var = current_user.photos.where("LITERAL" => (params["LITERAL"])).first
    if var then
      current_user.retract(var);Fi[	@#ikI"¨  
  def function
    var = current_user.photos.where("LITERAL" => (params["LITERAL"])).first
    if var then
      if current_user.update_post(var, params["LITERAL"]) then;Fi[	@ÍI"["STRING", true];Fi[[	@‡iÖI"ä        when "LITERAL" then
          ["STRING", false]
        when "LITERAL" then
          ["STRING", true]
        when "LITERAL" then;Fi[	@‡iáI"        when "LITERAL" then
          ["STRING", true]
        when "LITERAL" then
          ["STRING", true]
        else;Fi[	I"## Campfire account's subdomain;F@ei[[	@ÌiI"É      # Campfire api authentication token
      ##
      # Campfire account's subdomain
      ##
      # Campfire account's room id;Fi[	@Ìi[I"ë        # Campfire api authentication api_token
        ##
        # Campfire account's subdomain
        ##
        # Campfire account's room id;Fi[	@n.I"params["LITERAL"];Fi[[	@˘iI"≤  ##### These methods need to go away once we pass publisher object into the partial ######
  def function
    if params["LITERAL"].present? then
      params["LITERAL"]
    else;Fi[	@˘iI"^  
  def function
    if params["LITERAL"].present? then
      params["LITERAL"]
    else;Fi[	@√7I",var.mobile { redirect_to(stream_path) };Fi[[	@õ!i6I"⁄      flash["LITERAL"] = successful_mention_message if coming_from_profile_page?
      respond_to do |var|
        var.html { redirect_to("LITERAL") }
        var.mobile { redirect_to(stream_path) }
        var.json do;Fi[	@õ!i>I"«    else
      respond_to do |var|
        var.html { redirect_to("LITERAL") }
        var.mobile { redirect_to(stream_path) }
        var.json { render("LITERAL" => (true), "LITERAL" => "LITERAL") };Fi[	@x3@0i[[	@Ci-I"µ        else
          if (var["LITERAL"] or var["LITERAL"]) then
            if @user.update_attributes(var) then
              flash["LITERAL"] = I18n.t("STRING")
            else;Fi[	@CiEI"ø              else
                if var["LITERAL"] then
                  if @user.update_attributes(var) then
                    flash["LITERAL"] = I18n.t("STRING")
                  else;Fi[	I"0(var << [javascript_include_tag("STRING")]);FI"((var << [javascript_tag("STRING")]);Fi[[	@li?I"∏    if AppConfig.privacy.jquery_cdn? then
      var = Jquery::Rails::JQUERY_VERSION
      (var << [javascript_include_tag("STRING")])
      (var << [javascript_tag("STRING")])
    else;Fi[	@liDI"Ã      (var << [javascript_include_tag("STRING")])
    end
    (var << [javascript_include_tag("STRING")])
    (var << [javascript_tag("STRING")])
    (var << [javascript_tag("STRING")]) if Rails.env.test?;Fi[	@j8@ri[[	@˘iI"ê  def function
    if params["LITERAL"].present? then
      params["LITERAL"]
    else
      defined? @stream ? (@stream.publisher.text) : (nil);Fi[	@˘iI"ì  def function
    if params["LITERAL"].present? then
      params["LITERAL"]
    else
      defined? @stream ? (@stream.publisher.prefill) : (nil);Fi[	I"@orig_err.send("LITERAL");F@ri[[	@òi;I"≤        return unless @orig_err
        @orig_err_class ||= if @orig_err.is_a?(Errors::Error) then
          @orig_err.send("LITERAL")
        else
          @orig_err.class.to_s;Fi[	@òiEI"µ        return @orig_err_msg unless @orig_err_msg.nil?
        var = if @orig_err.is_a?(Errors::Error) then
          @orig_err.send("LITERAL")
        else
          @orig_err.to_s;Fi[	I"2var = process_newlines(var) if var["LITERAL"];F@ài[[	@üiI"Õ    if var.respond_to?("LITERAL") then
      var = truncate(var.formatted_message("LITERAL" => (true)), "LITERAL" => (var["LITERAL"]))
      var = process_newlines(var) if var["LITERAL"]
      var
    else;Fi[	@üiI"ú    var["LITERAL"] ||= "LITERAL"
    var = truncate(var.text, "LITERAL" => (var["LITERAL"]))
    var = process_newlines(var) if var["LITERAL"]
    var
  end;Fi[	I"var = ["STRING"];FI"-(var << "STRING") if @options["LITERAL"];Fi[[	@‡i=I"≤    def function
      return @current_crontab if @current_crontab
      var = ["STRING"]
      (var << "STRING") if @options["LITERAL"]
      var = `#{var.join("STRING")}STRING`;Fi[	@‡iGI"Ä      (var << var)
      var.fsync
      var = ["STRING"]
      (var << "STRING") if @options["LITERAL"]
      (var << var.path);Fi[	@î&@{i[[	@i	I"Ü  when "LITERAL", "STRING", "LITERAL" then
    "STRING".undent
  when "STRING", "STRING" then
    "STRING".undent
  when "STRING" then;Fi[	@iI"u  when "STRING" then
    "STRING".undent
  when "STRING", "STRING" then
    "STRING".undent
  when "STRING" then;Fi[	@ﬂ&I"<var = unescape_interpolation(var["LITERAL"].strip, var);Fi[[	@;iOI"∞      when "STRING" then
        var = true
        if (var["LITERAL"] == "STRING") then
          var = unescape_interpolation(var["LITERAL"].strip, var)
          var = false;Fi[	@;iWI"Ã          var = true
          var = (var["LITERAL"] == "STRING")
          if (var["LITERAL"] == "STRING") then
            var = unescape_interpolation(var["LITERAL"].strip, var)
            var = false;Fi[	@ﬂ&I"3var = ["STRING", "STRING", "STRING", "STRING"];Fi[[	@îiÍI"       # reduce 42 omitted
      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          var = ["STRING", "STRING", "STRING", "STRING"]
          var = Node.new("LITERAL", var);Fi[	@îiÓI"           var = Node.new("LITERAL", var)
        else
          if (var["LITERAL"] == "STRING") then
            var = ["STRING", "STRING", "STRING", "STRING"]
            var = Node.new("LITERAL", var);Fi[	I"#@headers["LITERAL"] = "STRING";FI"b@headers["LITERAL"] = truncate(@comment.comment_email_subject, "LITERAL" => (TRUNCATION_LEN));Fi[[	I"Kprojects//diaspora_proj/mailers/notification_mailers/also_commented.rb;TiI"      @comment = Comment.find_by_id(var)
      if mail? then
        @headers["LITERAL"] = "STRING"
        @headers["LITERAL"] = truncate(@comment.comment_email_subject, "LITERAL" => (TRUNCATION_LEN))
        @headers["LITERAL"] = "STRING";Fi[	I"Lprojects//diaspora_proj/mailers/notification_mailers/comment_on_post.rb;TiI"Ê    def function(var)
      @comment = Comment.find(var)
      @headers["LITERAL"] = "STRING"
      @headers["LITERAL"] = truncate(@comment.comment_email_subject, "LITERAL" => (TRUNCATION_LEN))
      @headers["LITERAL"] = "STRING";Fi[	@∑8@∂8i[[	@∫8iI"—      if mail? then
        @headers["LITERAL"] = "STRING"
        @headers["LITERAL"] = truncate(@comment.comment_email_subject, "LITERAL" => (TRUNCATION_LEN))
        @headers["LITERAL"] = "STRING"
      end;Fi[	@Ω8iI"ÿ      @comment = Comment.find(var)
      @headers["LITERAL"] = "STRING"
      @headers["LITERAL"] = truncate(@comment.comment_email_subject, "LITERAL" => (TRUNCATION_LEN))
      @headers["LITERAL"] = "STRING"
    end;Fi[	I"var.text;FI"var.html;Fi[[	@á0i I"U    var.merge!(var)
    mail(var) do |var|
      var.text
      var.html
    end;Fi[	@á0iYI"x    with_recipient_locale do
      mail(@notification.headers) do |var|
        var.text
        var.html
      end;Fi[	I"var = false;F@ri[[	@{iÇI"Ñ        var = var["LITERAL"] ? (nil) : (var["LITERAL"])
        var = {}
        var = false
      else
        var = var["LITERAL"];Fi[	@;iYI"¿          if (var["LITERAL"] == "STRING") then
            var = unescape_interpolation(var["LITERAL"].strip, var)
            var = false
          else
            var = var["LITERAL"].strip;Fi[	@…&@Œ8i[[	@;i`I"û          if contains_interpolation?(var) then
            var = unescape_interpolation(var, var)
            var = true
            var = false
          end;Fi[	@;igI"î        if contains_interpolation?(var) then
          var = unescape_interpolation(var, var)
          var = true
          var = false
        end;Fi[	I"if var =~ "LITERAL" then;F@ài[[	@úi"I"o  def function(var)
    var.gsub("LITERAL") do |var|
      if var =~ "LITERAL" then
        var
      else;Fi[	@µidI"{        xpath(*(var.map do |var|
          var = var.to_s
          if var =~ "LITERAL" then
            var
          else;Fi[	I"raise("STRING") unless var;F@S.i[[	@‘iLI"ì          var.delete
          var = var.match("LITERAL").to_a["LITERAL"]
          raise("STRING") unless var
          var)
        rescue => var;Fi[	@} iI"      def function
        @mapping ||= (var = Devise.mappings[scope]
        raise("STRING") unless var
        var)
      end;Fi[	I"case self.service;F@W
i[[	@ÒiZI"k  # @return [String]
  def function
    case self.service
    when "STRING" then
      self.identifier;Fi[	@ÒifI"k  # @return [String]
  def function
    case self.service
    when "STRING" then
      self.identifier;Fi[	I"self.identifier;F@W
i[[	@Òi\I"u    case self.service
    when "STRING" then
      self.identifier
    when "STRING" then
      I18n.t("STRING");Fi[	@ÒihI"j    case self.service
    when "STRING" then
      self.identifier
    when "STRING" then
      false;Fi[	@ÿI"(end, var.bottle.checksum.hexdigest);Fi[[	@niaI"ç      "STRING"
    else
      # do nothing
    end, var.bottle.checksum.hexdigest)
    assert_match("LITERAL", var.stable.checksum.hexdigest);Fi[	@ni¯I"]      "STRING"
    else
      # do nothing
    end, var.bottle.checksum.hexdigest)
  end;Fi[	@ÿI",rescue CannotInstallFormulaError => var;Fi[[	@©iZI"ù      var.finish)
    rescue FormulaInstallationAlreadyAttemptedError
      # do nothing
    rescue CannotInstallFormulaError => var
      ofail(var.message);Fi[	@+iAI"ï      var.finish)
    rescue FormulaInstallationAlreadyAttemptedError
      # do nothing
    rescue CannotInstallFormulaError => var
      ofail(var);Fi[	I"def self.perform(var);FI"var = Invitation.find(var);Fi[[	@ài
I"ì      @queue = "LITERAL"
      
      def self.perform(var)
        var = Invitation.find(var)
        I18n.with_locale(var.language) { var.send! };Fi[	I"=projects//diaspora_proj/models/jobs/resend_invitation.rb;Ti	I"l    @queue = "LITERAL"
    
    def self.perform(var)
      var = Invitation.find(var)
      var.resend;Fi[	I"var.path);F@≤'i[[	@‘iªI"y          var.close
          check_gpg_config(var.path)
          var.path)
        rescue => var
          cleanup;Fi[	@‘i¯I"µ          var.write(passphrase.to_s)
          var.close
          var.path)
        rescue => var
          Logger.warn(Errors::Encryptor::GPG::PassphraseError.wrap(var, "STRING"));Fi[	I"(@tempdirs << var);FI"'var = Tempfile.open("STRING", var);Fi[[	@‘i∂I"Â          (return false unless gpg_config
          var = Dir.mktmpdir("STRING", Config.tmp_path)
          (@tempdirs << var)
          var = Tempfile.open("STRING", var)
          var.write(gpg_config.gsub("LITERAL", "STRING"));Fi[	@‘iÙI"ÿ          (return false if passphrase.to_s.empty?
          var = Dir.mktmpdir("STRING", Config.tmp_path)
          (@tempdirs << var)
          var = Tempfile.open("STRING", var)
          var.write(passphrase.to_s);Fi[	I""# NOTE API V1 to be extracted;F@Ói[[	@ai*I"å  before_validation("LITERAL")
  
  # NOTE API V1 to be extracted
  def function
    diaspora_handle.downcase! unless diaspora_handle.blank?;Fi[	@Èi;I"√  after_create { queue_processing_job if self.author.local? }
  
  # NOTE API V1 to be extracted
  def function
    if self.status_message_guid and self.status_message.text_and_photos_blank? then;Fi[	I"%scope("LITERAL", lambda do |var|;FI"Ejoins("LITERAL").where("LITERAL" => ({ "LITERAL" => (var.id) }));Fi[[	@di&I"}  end)
  
  scope("LITERAL", lambda do |var|
    joins("LITERAL").where("LITERAL" => ({ "LITERAL" => (var.id) }))
  end);Fi[	@Ë"i"I"£  after_create("LITERAL", "LITERAL" => "LITERAL")
  
  scope("LITERAL", lambda do |var|
    joins("LITERAL").where("LITERAL" => ({ "LITERAL" => (var.id) }))
  end);Fi[	I"&def self.diaspora_initialize(var);FI" var = self.new(var.to_hash);Fi[[	@ÈiMI"q  end
  
  def self.diaspora_initialize(var)
    var = self.new(var.to_hash)
    var.author = var["LITERAL"];Fi[	@dimI"q  end
  
  def self.diaspora_initialize(var)
    var = self.new(var.to_hash)
    var.author = var["LITERAL"];Fi[	@-9I" var.author = var["LITERAL"];Fi[[	@ÈiNI"ò  
  def self.diaspora_initialize(var)
    var = self.new(var.to_hash)
    var.author = var["LITERAL"]
    var.public = var["LITERAL"] if var["LITERAL"];Fi[	@dinI"ò  
  def self.diaspora_initialize(var)
    var = self.new(var.to_hash)
    var.author = var["LITERAL"]
    var.public = var["LITERAL"] if var["LITERAL"];Fi[	@49I"2var.public = var["LITERAL"] if var["LITERAL"];Fi[[	@ÈiOI"»  def self.diaspora_initialize(var)
    var = self.new(var.to_hash)
    var.author = var["LITERAL"]
    var.public = var["LITERAL"] if var["LITERAL"]
    var.pending = var["LITERAL"] if var["LITERAL"];Fi[	@dioI"»  def self.diaspora_initialize(var)
    var = self.new(var.to_hash)
    var.author = var["LITERAL"]
    var.public = var["LITERAL"] if var["LITERAL"]
    var.pending = var["LITERAL"] if var["LITERAL"];Fi[	@;9I"3var.pending = var["LITERAL"] if var["LITERAL"];Fi[[	@ÈiPI"Ÿ    var = self.new(var.to_hash)
    var.author = var["LITERAL"]
    var.public = var["LITERAL"] if var["LITERAL"]
    var.pending = var["LITERAL"] if var["LITERAL"]
    var.diaspora_handle = var.author.diaspora_handle;Fi[	@dipI"Ÿ    var = self.new(var.to_hash)
    var.author = var["LITERAL"]
    var.public = var["LITERAL"] if var["LITERAL"]
    var.pending = var["LITERAL"] if var["LITERAL"]
    var.diaspora_handle = var.author.diaspora_handle;Fi[	@B9I"5var.diaspora_handle = var.author.diaspora_handle;Fi[[	@ÈiQI"Ì    var.author = var["LITERAL"]
    var.public = var["LITERAL"] if var["LITERAL"]
    var.pending = var["LITERAL"] if var["LITERAL"]
    var.diaspora_handle = var.author.diaspora_handle
    var.random_string = SecureRandom.hex("LITERAL");Fi[	@diqI"¡    var.author = var["LITERAL"]
    var.public = var["LITERAL"] if var["LITERAL"]
    var.pending = var["LITERAL"] if var["LITERAL"]
    var.diaspora_handle = var.author.diaspora_handle
    var;Fi[	@%9@i[[	@di'I"y  
  scope("LITERAL", lambda do |var|
    joins("LITERAL").where("LITERAL" => ({ "LITERAL" => (var.id) }))
  end)
  ;Fi[	@Ë"i#I"y  
  scope("LITERAL", lambda do |var|
    joins("LITERAL").where("LITERAL" => ({ "LITERAL" => (var.id) }))
  end)
  ;Fi[	I"self["LITERAL"];F@ri[[	@Øi[I"†  def function(var)
    var = if (var == "LITERAL") and self["LITERAL"] then
      self["LITERAL"]
    else
      if (var == "LITERAL") and self["LITERAL"] then;Fi[	@Øi^I"}    else
      if (var == "LITERAL") and self["LITERAL"] then
        self["LITERAL"]
      else
        self["LITERAL"];Fi[	I"2var = Dir.mktmpdir("STRING", Config.tmp_path);F@9i[[	@‘iµI"π        begin
          (return false unless gpg_config
          var = Dir.mktmpdir("STRING", Config.tmp_path)
          (@tempdirs << var)
          var = Tempfile.open("STRING", var);Fi[	@‘iÛI"¡        begin
          (return false if passphrase.to_s.empty?
          var = Dir.mktmpdir("STRING", Config.tmp_path)
          (@tempdirs << var)
          var = Tempfile.open("STRING", var);Fi[	I"(var << "STRING") if var;F@ài[[	@©ivI"î          end
          var = (form_tag_without_haml(var, var, *var, &var) + "STRING")
          (var << "STRING") if var
          var
        else;Fi[	@©iÖI"|        end
        var = form_for_without_haml(var, *var, &var)
        (var << "STRING") if var
        var
      end;Fi[	I"if self.target.nil? then;F@Ì i[[	@Ïi.I"l  
  def function(var, var)
    if self.target.nil? then
      Rails.logger.info("STRING")
      return;Fi[	@¯ iTI"l  
  def function(var, var)
    if self.target.nil? then
      Rails.logger.info("STRING")
      return;Fi[	I"?# Once the packaging procedure is complete, it will return;FI"=# so that any clean-up may be performed after the yield.;Fi[[	@‘iTI"Y      # It sets up the needed options to pass to the gpg command,
      # then yields the command to use as part of the packaging procedure.
      # Once the packaging procedure is complete, it will return
      # so that any clean-up may be performed after the yield.
      # Cleanup is also ensured, as temporary files may hold sensitive data.;Fi[	@ûi(I"#      # It sets up the needed options to pass to the openssl command,
      # then yields the command to use as part of the packaging procedure.
      # Once the packaging procedure is complete, it will return
      # so that any clean-up may be performed after the yield.
      def function;Fi[	I"%# @note Used for XML marshalling;F@«i[[	@Œi&I"T  end
  
  # @note Used for XML marshalling
  # @return [String]
  def function;Fi[	@Œi0I"T  end
  
  # @note Used for XML marshalling
  # @return [String]
  def function;Fi[	@ª+I"var = self.statement;Fi[[	@`iI"Ω        def function
          if owner.is_a?(StateMachine::Machine) then
            var = self
            var = self.statement
            var = extract_node_names(var.parameters(false));Fi[	@eiI"Ω        def function
          if owner.is_a?(StateMachine::Machine) then
            var = self
            var = self.statement
            var = extract_node_names(var.parameters(false));Fi[	@ƒ+I"var["LITERAL"] = var;Fi[[	@\iI"ì  # @return [Array<Integer>]
  def function(var, var)
    var = prep_opts(var, var)
    var["LITERAL"] = var
    var["LITERAL"] ||= self.aspect_ids;Fi[	@\i$I"ù  def function(var, var)
    var = var.table_name
    var = prep_opts(var, var)
    var["LITERAL"] = var
    var = construct_shareable_from_others_query(var);Fi[	@à9@W
i[[	@;iI"õ          break if var
          var, var, var = parse_old_attributes(var)
          var["LITERAL"] = var
        when "STRING" then
          break if var;Fi[	@;iI"≠          break if var
          var, var, var = parse_new_attributes(var)
          var["LITERAL"] = var
        when "STRING" then
          break unless (var == "STRING");Fi[	I"(var["LITERAL"] = var.url("LITERAL");F@ï9i[[	@.iEI"Ê    if var = var.delete("LITERAL") then
      var.update_attributes("LITERAL" => (false)) if var.pending
      var["LITERAL"] = var.url("LITERAL")
      var["LITERAL"] = var.url("LITERAL")
      var["LITERAL"] = var.url("LITERAL");Fi[	@.iFI"∆      var.update_attributes("LITERAL" => (false)) if var.pending
      var["LITERAL"] = var.url("LITERAL")
      var["LITERAL"] = var.url("LITERAL")
      var["LITERAL"] = var.url("LITERAL")
    end;Fi[	I"9self.aspects.create("LITERAL" => (I18n.t("STRING")));F@ú9i[[	@.ipI"º  
  def function
    self.aspects.create("LITERAL" => (I18n.t("STRING")))
    self.aspects.create("LITERAL" => (I18n.t("STRING")))
    self.aspects.create("LITERAL" => (I18n.t("STRING")));Fi[	@.iqI"¯  def function
    self.aspects.create("LITERAL" => (I18n.t("STRING")))
    self.aspects.create("LITERAL" => (I18n.t("STRING")))
    self.aspects.create("LITERAL" => (I18n.t("STRING")))
    var = self.aspects.create("LITERAL" => (I18n.t("STRING")));Fi[	I"I# then yields the command to use as part of the packaging procedure.;F@r9i[[	@‘iSI"Q      # This is called as part of the procedure run by the Packager.
      # It sets up the needed options to pass to the gpg command,
      # then yields the command to use as part of the packaging procedure.
      # Once the packaging procedure is complete, it will return
      # so that any clean-up may be performed after the yield.;Fi[	@ûi'I"U      # This is called as part of the procedure run by the Packager.
      # It sets up the needed options to pass to the openssl command,
      # then yields the command to use as part of the packaging procedure.
      # Once the packaging procedure is complete, it will return
      # so that any clean-up may be performed after the yield.;Fi[	@¢I"#   }.new);Fi[[	@µiÑI"û      #     def regex node_set, regex
      #       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ }
      #     end
      #   }.new)
      #;Fi[	@µiÆI"û      #     def regex node_set, regex
      #       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ }
      #     end
      #   }.new)
      #;Fi[	@-.I"class Compiler;Fi[[	@{iI"Hrequire("cgi")
module Haml
  class Compiler
    include(Haml::Util);Fi[	@Åi
I"}require("haml/helpers/action_view_extensions")
require("haml/helpers/xss_mods")
module Haml
  class Compiler
    def function;Fi[	@-.I"module Filters;Fi[[	@≤iI"Brequire("tilt")
module Haml
  module Filters
    extend(self);Fi[	@™i I"[module Haml
  module Filters
    class SassRailsTemplate < ::Sass::Rails::SassTemplate;Fi [	I"<# @return [Boolean] Whether or not the format is HTML5.;F@Ói[[	@xi7I"Ç    end
    
    # @return [Boolean] Whether or not the format is HTML5.
    def function
      (@options["LITERAL"] == "LITERAL");Fi[	@xi≠I"z    end
    
    # @return [Boolean] Whether or not the format is HTML5.
    def function
      (format == "LITERAL");Fi[	I"I# @param tab_change [Fixnum] The number of tabs by which to increase;FI"/#   or decrease the document's indentation;Fi[[	@xibI"˝    #
    # @param text [String] The text to append
    # @param tab_change [Fixnum] The number of tabs by which to increase
    #   or decrease the document's indentation
    # @param dont_tab_up [Boolean] If true, don't indent the first line of `text`;Fi[	@xipI"√    # Modifies the indentation of the document.
    #
    # @param tab_change [Fixnum] The number of tabs by which to increase
    #   or decrease the document's indentation
    def function(var);Fi[	I"&if (RUBY_VERSION < "STRING") then;F@õi[[	@Ki—I"h    private
    
    if (RUBY_VERSION < "STRING") then
      def function(var)
        # do nothing;Fi[	@>iúI"j    end
    
    if (RUBY_VERSION < "STRING") then
      def function(var)
        return var.inspect;Fi[	I"1if (@node.value["LITERAL"] == "STRING") then;F@Ôi[[	@{iÓI"Ö      else
        if @options.xhtml? then
          if (@node.value["LITERAL"] == "STRING") then
            "STRING"
          else;Fi[	@{iÒI"Ñ            "STRING"
          else
            if (@node.value["LITERAL"] == "STRING") then
              "STRING"
            else;Fi[	I" case @node.value["LITERAL"];F@W
i[[	@{iÙI"ã              "STRING"
            else
              case @node.value["LITERAL"]
              when "STRING" then
                "STRING";Fi[	@{iI"å        else
          if @options.html4? then
            case @node.value["LITERAL"]
            when "STRING" then
              "STRING";Fi[	I"var, var = var;F@`!i[[	@`i<I"ã        var.!=(@machine) and var.events[qualified_name, "LITERAL"]
      end) then
        var, var = var
        warn("STRING")
      else;Fi[	@}iKI"ö        end
        if var = var.detect { |var, var| var.attribute.!=(var.attribute) } then
          var, var = var
          warn("STRING")
        else;Fi[	I"F# See {file:REFERENCE.md#options the Haml options documentation}.;F@—i[[	@KiI"é    
    # The Haml::Options instance.
    # See {file:REFERENCE.md#options the Haml options documentation}.
    #
    # @return Haml::Options;Fi[	@xi‹I"¬    # it can be `Kernel#eval`ed to get the same result back.
    #
    # See {file:REFERENCE.md#options the Haml options documentation}.
    #
    # @return [{Symbol => Object}] The options hash;Fi[	I"5# Note that Haml modifies the evaluation context;FI"K# (either the scope object or the `self` object of the scope binding).;Fi[[	@KiAI"    # otherwise, Haml just uses its `#instance_eval` context.
    #
    # Note that Haml modifies the evaluation context
    # (either the scope object or the `self` object of the scope binding).
    # It extends {Haml::Helpers}, and various instance variables are set;Fi[	@Ki¬I"-    #     obj.render(:foo => "Hello!") #=> NameError: undefined local variable or method `foo'
    #
    # Note that Haml modifies the evaluation context
    # (either the scope object or the `self` object of the scope binding).
    # It extends {Haml::Helpers}, and various instance variables are set;Fi[	@Ú9I"I# It extends {Haml::Helpers}, and various instance variables are set;Fi[[	@KiBI"Ò    #
    # Note that Haml modifies the evaluation context
    # (either the scope object or the `self` object of the scope binding).
    # It extends {Haml::Helpers}, and various instance variables are set
    # (all prefixed with `haml_`).;Fi[	@Ki√I"Ò    #
    # Note that Haml modifies the evaluation context
    # (either the scope object or the `self` object of the scope binding).
    # It extends {Haml::Helpers}, and various instance variables are set
    # (all prefixed with `haml_`).;Fi[	@˘9I"## (all prefixed with `haml_`).;Fi[[	@KiCI"˛    # Note that Haml modifies the evaluation context
    # (either the scope object or the `self` object of the scope binding).
    # It extends {Haml::Helpers}, and various instance variables are set
    # (all prefixed with `haml_`).
    # For example:;Fi[	@KiƒI"Ò    # Note that Haml modifies the evaluation context
    # (either the scope object or the `self` object of the scope binding).
    # It extends {Haml::Helpers}, and various instance variables are set
    # (all prefixed with `haml_`).
    #;Fi[	I"4if (var.is_a?(Binding) or var.is_a?(Proc)) then;FI"var = eval("STRING", var);Fi[[	@KikI"‘      end
      var = Haml::Buffer.new(var, @options.for_buffer)
      if (var.is_a?(Binding) or var.is_a?(Proc)) then
        var = eval("STRING", var)
        var = var.instance_eval { binding } if block_given?;Fi[	@KióI"∂    # @return [Proc] The proc that will run the template
    def function(var, var)
      if (var.is_a?(Binding) or var.is_a?(Proc)) then
        var = eval("STRING", var)
      else;Fi[	I" rescue ::SyntaxError => var;FI"$raise(SyntaxError, var.message);Fi[[	@KiyI"≈      begin
        eval(@compiler.precompiled_with_return_value, var, @options["LITERAL"], @options["LITERAL"])
      rescue ::SyntaxError => var
        raise(SyntaxError, var.message)
      end);Fi[	@KiüI"‹      begin
        eval((("STRING" + compiler.precompiled_with_ambles(var)) + "STRING"), var, @options["LITERAL"], @options["LITERAL"])
      rescue ::SyntaxError => var
        raise(SyntaxError, var.message)
      end;Fi[	I"1# However, due to an unfortunate Ruby quirk,;FI"F# the local variables which can be assigned must be pre-declared.;Fi[[	@KiÑI"    #
    # The first argument of the returned proc is a hash of local variable names to values.
    # However, due to an unfortunate Ruby quirk,
    # the local variables which can be assigned must be pre-declared.
    # This is done with the `local_names` argument.;Fi[	@Ki≥I"    #
    # The first argument of the defined method is a hash of local variable names to values.
    # However, due to an unfortunate Ruby quirk,
    # the local variables which can be assigned must be pre-declared.
    # This is done with the `local_names` argument.;Fi[	@:I"4# This is done with the `local_names` argument.;Fi[[	@KiÖI"    # The first argument of the returned proc is a hash of local variable names to values.
    # However, due to an unfortunate Ruby quirk,
    # the local variables which can be assigned must be pre-declared.
    # This is done with the `local_names` argument.
    # For example:;Fi[	@Ki¥I"    # The first argument of the defined method is a hash of local variable names to values.
    # However, due to an unfortunate Ruby quirk,
    # the local variables which can be assigned must be pre-declared.
    # This is done with the `local_names` argument.
    # For example:;Fi[	@:@Fi[[	@KiÜI"√    # However, due to an unfortunate Ruby quirk,
    # the local variables which can be assigned must be pre-declared.
    # This is done with the `local_names` argument.
    # For example:
    #;Fi[	@KiµI"√    # However, due to an unfortunate Ruby quirk,
    # the local variables which can be assigned must be pre-declared.
    # This is done with the `local_names` argument.
    # For example:
    #;Fi[	I"=# @param args [Array<String>] The command-line arguments;F@õi[[	@1iI"ä  module Exec
    class Generic
      # @param args [Array<String>] The command-line arguments
      def function(var)
        @args = var;Fi[	@1iùI"Ç    
    class Haml < Generic
      # @param args [Array<String>] The command-line arguments
      def function(var)
        super;Fi[	I"!# @param opts [OptionParser];F@õi[[	@1iGI"®      # so they can add their own options.
      #
      # @param opts [OptionParser]
      def function(var)
        var.on("STRING", "STRING", "LITERAL", "STRING") do;Fi[	@1ißI"É      # Tells optparse how to parse the arguments.
      #
      # @param opts [OptionParser]
      def function(var)
        super;Fi[	I"E# @param text [String] The source text for the filter to process;FI"+# @return [String] The filtered result;Fi[[	@≤iNI"˝      # If \{#compile} is overridden, however, \{#render} doesn't need to be.
      #
      # @param text [String] The source text for the filter to process
      # @return [String] The filtered result
      # @raise [Haml::Error] if it's not overridden;Fi[	@≤iYI"“      #
      # @see #render
      # @param text [String] The source text for the filter to process
      # @return [String] The filtered result
      # @raise [Haml::Error] if it or \{#render} isn't overridden;Fi[	I")if contains_interpolation?(var) then;FI"+var = unescape_interpolation(var, var);Fi[[	@;i^I"ì          end
        else
          if contains_interpolation?(var) then
            var = unescape_interpolation(var, var)
            var = true;Fi[	@;ieI"â        end
      else
        if contains_interpolation?(var) then
          var = unescape_interpolation(var, var)
          var = true;Fi[	I"*#     sub   1024R/92C8DFD8 2012-07-20;F@—i[[	@‘iæI"ﬂ      #     Key fingerprint = FFEA D1DB 201F B214 873E  7399 4A83 569F 4E5E 8D8A
      #     uid                  John Smith <john.smith@example.com>
      #     sub   1024R/92C8DFD8 2012-07-20
      #
      # [Long Key ID];Fi[	@‘i”I"º      #     pub   1024R/4E5E8D8A 2012-07-20
      #     uid                  John Smith <john.smith@example.com>
      #     sub   1024R/92C8DFD8 2012-07-20
      #
      # [Email Address];Fi[	I"$# @see Base#render_with_options;F@äi[[	@≤ióI"î      include(Base)
      
      # @see Base#render_with_options
      def function(var, var)
        var = var["LITERAL"] ? ("STRING") : ("STRING");Fi[	@≤ißI"î      include(Base)
      
      # @see Base#render_with_options
      def function(var, var)
        var = var["LITERAL"] ? ("STRING") : ("STRING");Fi[	I"3var = var["LITERAL"] ? ("STRING") : ("STRING");FI"H(var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING");Fi[[	@≤iôI"‹      # @see Base#render_with_options
      def function(var, var)
        var = var["LITERAL"] ? ("STRING") : ("STRING")
        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING")
        var = "STRING";Fi[	@≤i©I"‹      # @see Base#render_with_options
      def function(var, var)
        var = var["LITERAL"] ? ("STRING") : ("STRING")
        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING")
        var = "STRING";Fi[	@Y:@ái[[	@≤iöI"‚      def function(var, var)
        var = var["LITERAL"] ? ("STRING") : ("STRING")
        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING")
        var = "STRING"
        (var << "STRING") if var["LITERAL"];Fi[	@≤i™I"‚      def function(var, var)
        var = var["LITERAL"] ? ("STRING") : ("STRING")
        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING")
        var = "STRING"
        (var << "STRING") if var["LITERAL"];Fi[	@	.@õi[[	@≤iI"o    
    module Erb
      class << self
        def function(var)
          super.sub("LITERAL", "STRING");Fi[	@äiPI"j  end
  
  class << self
    def function(var)
      var.nil? ? (@bottle_sha1) : (@bottle_sha1 = var);Ti[	I"C#     uid                  John Smith <john.smith@example.com>;F@J:i[[	@‘iΩI"ı      #     pub   1024R/4E5E8D8A 2012-07-20
      #     Key fingerprint = FFEA D1DB 201F B214 873E  7399 4A83 569F 4E5E 8D8A
      #     uid                  John Smith <john.smith@example.com>
      #     sub   1024R/92C8DFD8 2012-07-20
      #;Fi[	@‘i“I"–      #     $ gpg -k john.smith@example.com
      #     pub   1024R/4E5E8D8A 2012-07-20
      #     uid                  John Smith <john.smith@example.com>
      #     sub   1024R/92C8DFD8 2012-07-20
      #;Fi[	I"module ActionView;F@..i[[	I"?projects//haml_proj/haml/helpers/rails_323_textarea_fix.rb;Ti I"@module ActionView
  module Helpers
    module FormTagHelper;Fi [	@FihI"J  end
end
module ActionView
  module Helpers
    module CaptureHelper;Fi[	I"module CaptureHelper;F@äi[[	@©i-I"{  
  module Helpers
    module CaptureHelper
      def function(var, var)
        if Haml::Helpers.block_is_haml?(var) then;Fi[	@FijI"ñmodule ActionView
  module Helpers
    module CaptureHelper
      def function(var, var)
        var = with_output_buffer_without_haml_xss(*var, &var);Fi[	@Y@Ói[[	@›i1I"ø    private
    
    # @return [Hash]
    def function
      { "LITERAL" => (@consumer_key), "LITERAL" => (@consumer_secret), "LITERAL" => (@oauth_token), "LITERAL" => (@oauth_token_secret) };Fi[	@›i6I"j    end
    
    # @return [Hash]
    def function
      Hash[Twitter::Configurable.keys.map do |var|;Fi[	I"3# Input is escaped, output is always HTML safe;F@äi[[	@Fi+I"∫      end
      
      # Input is escaped, output is always HTML safe
      def function(var, var)
        Haml::Util.html_safe(precede_without_haml_xss(haml_xss_html_escape(var), &var));Fi[	@Fi0I"∫      end
      
      # Input is escaped, output is always HTML safe
      def function(var, var)
        Haml::Util.html_safe(succeed_without_haml_xss(haml_xss_html_escape(var), &var));Fi[	@ 5I"strenc(var);Fi[[	@µijI"q      when Array then
        arrenc(var)
      when String then
        strenc(var)
      when Numeric then;Fi[	@µiÉI"_    def function(var)
      case var
      when String then
        strenc(var)
      else;Fi[	I"*#   Escapes newlines within a string.;F@—i[[	@~iXI"Œ    #
    # @overload find_and_preserve(input, tags = haml_buffer.options[:preserve])
    #   Escapes newlines within a string.
    #
    #   @param input [String] The string within which to escape newlines;Fi[	@~imI"¢    #
    # @overload perserve(input)
    #   Escapes newlines within a string.
    #
    #   @param input [String] The string within which to escape all newlines;Fi[	I"6#   Escapes newlines within a block of Haml code.;F@—i[[	@~i\I"    #   @param input [String] The string within which to escape newlines
    # @overload find_and_preserve(tags = haml_buffer.options[:preserve])
    #   Escapes newlines within a block of Haml code.
    #
    #   @yield The block within which to escape newlines;Fi[	@~iqI"⁄    #   @param input [String] The string within which to escape all newlines
    # @overload perserve
    #   Escapes newlines within a block of Haml code.
    #
    #   @yield The block within which to escape newlines;Fi[	@Y@êi[[	@?iI"⁄      # @param hash [Hash]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.
      # @return [Hash]
      def function(var, var, var)
        merge_user!(var.dup, var, var);Fi[	@?iàI"ƒ      # @param hash [Hash]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.
      # @return [Hash]
      def function(var, var, var)
        case var;Fi[	@‚-@—i[[	@~iîI"w    #       <h3>Description</h3>
    #       <p>A book about all the stuff.</p>
    #     </li>
    #
    # While:;Fi[	@~i®I"    #     <li class='nav' role='nav'>
    #       <a href='#'>FAQ</a>
    #     </li>
    #
    #  `[[class", "nav"], [role", "nav"]]` could have been used instead of `{class: "nav", role: "nav"}` (or any enumerable collection where each pair of items responds to #to_s);Fi[	I"#     EOS;F@—i[[	@‘iÑI"é      #       =8gNa
      #       -----END PGP PUBLIC KEY BLOCK-----
      #     EOS
      #
      #     enc.keys['mary@example.com'] = <<-EOS;Fi[	@‘i"I"ë      #       =KEW+
      #       -----END PGP PUBLIC KEY BLOCK-----
      #     EOS
      #
      #     encryptor.recipients = 'joe@example.com';Fi[	I"/#       -----END PGP PUBLIC KEY BLOCK-----;F@∞:i[[	@‘iÉI"ü      #       SkQEHOxhMiFjAN9q4LuirSOu65uR1bnTmF+Z92++qMIuEkH4/LnN
      #       =8gNa
      #       -----END PGP PUBLIC KEY BLOCK-----
      #     EOS
      #;Fi[	@‘i!I"£      #       mLekS3xntUhhgHKc4lhf4IVBqG4cFmwSZ0tZEJJUSESb3TqkkdnNLjE=
      #       =KEW+
      #       -----END PGP PUBLIC KEY BLOCK-----
      #     EOS
      #;Fi[	I"#     %strong baz;F@—i[[	@~i€I"V    #     %p bar
    #     - tab_down
    #     %strong baz
    #
    # Produces:;Fi[	@~i˙I"_    #     - with_tabs(2) do
    #       %p bar
    #     %strong baz
    #
    # Produces:;Fi[	I"#     <strong>baz</strong>;F@—i[[	@~i·I"¢    #     <h1>foo</h1>
    #       <p>bar</p>
    #     <strong>baz</strong>
    #
    # @param i [Fixnum] The number of tabs by which to increase the indentation;Fi[	@~i I"_    #     <h1>foo</h1>
    #         <p>bar</p>
    #     <strong>baz</strong>
    #
    #;Fi[	I""# with no whitespace between.;F@Fi[[	@~i,I"~    
    # Prepends a string to the beginning of a Haml block,
    # with no whitespace between.
    # For example:
    #;Fi[	@~i=I"w    
    # Appends a string to the end of a Haml block,
    # with no whitespace between.
    # For example:
    #;Fi[	I"haml_concat(var);FI"tab_up;Fi[[	@~i◊I"Ñ        var = var.to_s
        if var.include?("STRING") then
          haml_concat(var)
          tab_up
          haml_concat(var);Fi[	@~iËI"T        return var
      end
      haml_concat(var)
      tab_up
      var.call;Fi[	I"tab_down;FI"haml_concat("STRING");Fi[[	@~i⁄I"p          tab_up
          haml_concat(var)
          tab_down
          haml_concat("STRING")
        else;Fi[	@~iÎI"U      tab_up
      var.call
      tab_down
      haml_concat("STRING")
      var;Fi[	I"2# @param text [String] The string to sanitize;FI",# @return [String] The sanitized string;Fi[[	@~i˙I"™    # this won't escape text declared as "safe".
    #
    # @param text [String] The string to sanitize
    # @return [String] The sanitized string
    def function(var);Fi[	@~i	I"™    # that is already part of an escaped entity.
    #
    # @param text [String] The string to sanitize
    # @return [String] The sanitized string
    def function(var);Fi[	@‰:@õi[[	@~i˚I"é    #
    # @param text [String] The string to sanitize
    # @return [String] The sanitized string
    def function(var)
      var = "STRING";Fi[	@~i
I"é    #
    # @param text [String] The string to sanitize
    # @return [String] The sanitized string
    def function(var)
      var = "STRING";Fi[	I"-var = if (RUBY_VERSION >= "STRING") then;FI"HRegexp.new(var.force_encoding(var.encoding), Regexp::FIXEDENCODING);Fi[[	@~i˛I"∞    def function(var)
      var = "STRING"
      var = if (RUBY_VERSION >= "STRING") then
        Regexp.new(var.force_encoding(var.encoding), Regexp::FIXEDENCODING)
      else;Fi[	@~iI"∞    def function(var)
      var = "STRING"
      var = if (RUBY_VERSION >= "STRING") then
        Regexp.new(var.force_encoding(var.encoding), Regexp::FIXEDENCODING)
      else;Fi[	@Ú:@ri[[	@~iˇI"≤      var = "STRING"
      var = if (RUBY_VERSION >= "STRING") then
        Regexp.new(var.force_encoding(var.encoding), Regexp::FIXEDENCODING)
      else
        Regexp.new(var);Fi[	@~iI"≤      var = "STRING"
      var = if (RUBY_VERSION >= "STRING") then
        Regexp.new(var.force_encoding(var.encoding), Regexp::FIXEDENCODING)
      else
        Regexp.new(var);Fi[	I"require("strscan");F@-.i[[	@;i I"2require("strscan")
module Haml
  class Parser;Fi [	@>iI"Trequire("set")
require("stringio")
require("strscan")
module Haml
  module Util;Fi[	I"case var["LITERAL"];F@W
i[[	@;i˛I"t      var = {}
      while var do
        case var["LITERAL"]
        when "STRING" then
          break if var;Fi[	@µiÇI"©    # it is the lexeme.
    def function(var)
      case var["LITERAL"]
      when "STRING" then
        ["STRING", var["LITERAL", "LITERAL"], var["LITERAL", "LITERAL"]];Fi[	I"var = block_keyword(var);FI"!check_push_script_stack(var);Fi[[	@;iI"      var = handle_ruby_multiline(var)
      var = @options["LITERAL"] if var.nil?
      var = block_keyword(var)
      check_push_script_stack(var)
      ParseNode.new("LITERAL", @index, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var));Fi[	@;i!I"≠      end
      var = handle_ruby_multiline(var)
      var = block_keyword(var)
      check_push_script_stack(var)
      if ["STRING", "STRING", "STRING"].include?(var) then;Fi[	@®8@Œ8i[[	@;iPI"£        var = true
        if (var["LITERAL"] == "STRING") then
          var = unescape_interpolation(var["LITERAL"].strip, var)
          var = false
        end;Fi[	@;iXI"∆          var = (var["LITERAL"] == "STRING")
          if (var["LITERAL"] == "STRING") then
            var = unescape_interpolation(var["LITERAL"].strip, var)
            var = false
          else;Fi[	@C:@…&i[[	@;i_I"ù        else
          if contains_interpolation?(var) then
            var = unescape_interpolation(var, var)
            var = true
            var = false;Fi[	@;ifI"ì      else
        if contains_interpolation?(var) then
          var = unescape_interpolation(var, var)
          var = true
          var = false;Fi[	I"!var = StringScanner.new(var);FI",while var.skip_until(HIGH_BIT_RANGE) do;Fi[[	@i
I"Å  
  def self.valid?(var)
    var = StringScanner.new(var)
    while var.skip_until(HIGH_BIT_RANGE) do
      var.pos -= "LITERAL";Fi[	@iI"í  
  def self.clean!(var)
    var = StringScanner.new(var)
    while var.skip_until(HIGH_BIT_RANGE) do
      var = var.pos = (var.pos - "LITERAL");Fi[	I"+((var << "STRING") << @next_line.text);F@˘3i[[	@;i#I"Á      rescue SyntaxError => var
        if (var.strip["LITERAL"] == "STRING") and (var.message == Error.message("LITERAL")) then
          ((var << "STRING") << @next_line.text)
          var = (var + "LITERAL")
          next_line;Fi[	@;i>I"õ        var.scan("LITERAL")
        if var.eos? then
          ((var << "STRING") << @next_line.text)
          var = (var + "LITERAL")
          next_line;Fi[	I"var[var] = var;FI"Lreturn [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING");Fi[[	@µiBI"„      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      var, var, var = pairparse(var)
      var[var] = var
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      loop do;Fi[	@µiGI"∏        var = eat("STRING", var)
        var, var, var = pairparse(var)
        var[var] = var
        return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      end;Fi[	I" break if (var == "LITERAL");FI"next if var.strip.empty?;Fi[[	@;iîI"º      var.text.slice!("LITERAL")
      while var = raw_next_line.first do
        break if (var == "LITERAL")
        next if var.strip.empty?
        break unless is_multiline?(var.strip);Fi[	@;ißI"õ      begin
        var = raw_next_line.first
        break if (var == "LITERAL")
        next if var.strip.empty?
        ((var << "STRING") << var.strip);Fi[	@R$@—i[[	@¡iAI"ë      # @param value [Object] The value your object defaults to. Nil if not provided.
      #
      # @example
      #
      #   class MyStrategy;Fi[	@¡i>I"û    # evaluates to true when you would like to skip info.
    #
    # @example
    #
    #   use MyStrategy, :skip_info => lambda{|uid| User.find_by_uid(uid)};Fi[	I"var = Set.new;FI"3var.each { |var| (var << var.patch_filename) };Fi[[	@i3I"µ    var = Patches.new(var)
    assert_equal("LITERAL", var.patches.length)
    var = Set.new
    var.each { |var| (var << var.patch_filename) }
    assert_equal("LITERAL", var.size);Fi[	@i<I"µ    var = Patches.new(var)
    assert_equal("LITERAL", var.patches.length)
    var = Set.new
    var.each { |var| (var << var.patch_filename) }
    assert_equal("LITERAL", var.size);Fi[	I"E# @yield [msg] A block in which an encoding error can be raised.;FI"2#   Only yields if there is an encoding error;Fi[[	@>icI"      #
      # @param str [String] The string of which to check the encoding
      # @yield [msg] A block in which an encoding error can be raised.
      #   Only yields if there is an encoding error
      # @yieldparam msg [String] The error message to be raised;Fi[	@>iãI"      #
      # @param str [String] The Haml template of which to check the encoding
      # @yield [msg] A block in which an encoding error can be raised.
      #   Only yields if there is an encoding error
      # @yieldparam msg [String] The error message to be raised;Fi[	@O;I"># @yieldparam msg [String] The error message to be raised;Fi[[	@>idI"T      # @param str [String] The string of which to check the encoding
      # @yield [msg] A block in which an encoding error can be raised.
      #   Only yields if there is an encoding error
      # @yieldparam msg [String] The error message to be raised
      # @return [String] `str`, potentially with encoding gotchas like BOMs removed;Fi[	@>iåI"E      # @param str [String] The Haml template of which to check the encoding
      # @yield [msg] A block in which an encoding error can be raised.
      #   Only yields if there is an encoding error
      # @yieldparam msg [String] The error message to be raised
      # @return [String] The original string encoded properly;Fi[	I"%if var.include?("STRING").! then;F@ái[[	@>i#I"±    # @return [String] The name of the indentation (e.g. `"12 spaces"`, `"1 tab"`)
    def function(var)
      if var.include?("STRING").! then
        var = "STRING"
      else;Fi[	@>i&I"u        var = "STRING"
      else
        if var.include?("STRING").! then
          var = "STRING"
        else;Fi[	@{@ri[[	@iI"R    "STRING"
  when "STRING" then
    "STRING".undent
  else
    # do nothing;Fi[	@‡izI"_      "STRING".undent
    when "LITERAL" then
      "STRING".undent
    else
      @reason;Fi[	I"#       =8gNa;F@∑:i[[	@‘iÇI"´      #       [...]
      #       SkQEHOxhMiFjAN9q4LuirSOu65uR1bnTmF+Z92++qMIuEkH4/LnN
      #       =8gNa
      #       -----END PGP PUBLIC KEY BLOCK-----
      #     EOS;Fi[	@‘iéI"£      #       [...]
      #       AN9q4LSkQEHOxhMiFjuirSOu65u++qMIuEkH4/LnNR1bnTmF+Z92
      #       =8gNa
      #       -----END PGP PUBLIC KEY BLOCK-----
      #;Fi[	@{@Íi[[	@iI"d    "STRING".undent
  when "STRING" then
    "STRING".undent
  when "LITERAL" then
    "STRING";Fi[	@‡ixI"q    case @reason
    when "LITERAL" then
      "STRING".undent
    when "LITERAL" then
      "STRING".undent;Fi[	@{@î&i[[	@iI"Ö  case var.downcase
  when "LITERAL", "STRING", "LITERAL" then
    "STRING".undent
  when "STRING", "STRING" then
    "STRING".undent;Fi[	@iI"t    "STRING".undent
  when "STRING" then
    "STRING".undent
  when "STRING", "STRING" then
    "STRING".undent;Fi[	I"require("hardware");F@Ò$i[[	@ÇiI"u    raise($!) if $!
    trap("STRING", STD_TRAP)
    require("hardware")
    require("keg")
    system("STRING");Fi[	@öiI"Mrequire("extend/ENV")
require("hardware")
require("keg")
module Homebrew;Fi[	@};@»i[[	@ái I"7require("hardware")
module Homebrew
  extend(self);Fi [	@˙iI"Krequire("superenv")
require("hardware")
module Homebrew
  extend(self);Fi[	@]@}i[[	@≈iI"Gbegin
  require("airbrake")
rescue LoadError
  raise("STRING")
end;Fi[	@ iI"Obegin
  require("hoptoad_notifier")
rescue LoadError
  raise("STRING")
end;Fi[	@Ò$@«i[[	@Ci I"6require("keg")
require("formula")
module Homebrew;Fi [	@JiI"Ä    begin
      (var = nil
      require("keg")
      require("formula")
      @kegs ||= downcased_unique_named.collect do |var|;Fi[	I"Marshal.dump(var, var);FI"var.close;Fi[[	@Çi$I"}    else
      var.continuation = nil if ARGV.debug?
      Marshal.dump(var, var)
      var.close
      exit!("LITERAL");Fi[	@√i∞I"ˆ          exec("STRING", "STRING", "STRING", "STRING", Pathname.new("STRING").dirname, "STRING", "STRING", f.path, *var.options_only))
        rescue Exception => var
          Marshal.dump(var, var)
          var.close
          exit!("LITERAL");Fi[	@]@`!i[[	@~1iI"d  (require("omniauth")
  require("omniauth/version"))
rescue LoadError
  warn("STRING")
  raise;Fi[	@	iAI"[  (require("rubygems")
  require("mocha/setup"))
rescue LoadError
  warn("STRING")
end;Fi[	@]@ÿi[[	@≤iI"m  (require("haml/filters/maruku")
  require("haml/filters/textile"))
rescue LoadError
  # do nothing
end;Fi[	@ei>I"_      begin
        require("psych")
      rescue LoadError
        # do nothing
      end;Fi[	I"# specified by the user;F@Ói[[	@
inI"ß      ##
      # Builds a MySQL compatible string for the additional options
      # specified by the user
      def function
        additional_options.join("STRING");Fi[	@
ilI"¨      ##
      # Builds a PostgreSQL compatible string for the additional options
      # specified by the user
      def function
        additional_options.join("STRING");Fi[	@/*I"0assert_equal(case MacOS.clang_build_version;Fi[[	@6iI"Ø    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    var.select_compiler
    assert_equal(case MacOS.clang_build_version
    when "LITERAL" then;Fi[	@6iWI"ã      var.fails_with?("LITERAL").!
    end)
    var.select_compiler
    assert_equal(case MacOS.clang_build_version
    when "LITERAL" then;Fi[	@%@ii[[	@Çi]I"l    end
    if ARGV.flag?("STRING") then
      ohai("STRING")
      puts("STRING")
      puts("STRING");Fi[	@¨iI"\      puts("STRING")
    else
      ohai("STRING")
      puts("STRING")
      IRB.start;Fi[	@
I"var = Pipeline.new;Fi[[	@
i7I"å      # data to the specified path based on the 'trigger'
      def function
        super
        var = Pipeline.new
        var = "STRING";Fi[	@
i7I"å      # data to the specified path based on the 'trigger'
      def function
        super
        var = Pipeline.new
        var = "STRING";Fi[	I"@only_tables ||= Array.new;F@ñ,i[[	@
i,I"Æ        super(var)
        @skip_tables ||= Array.new
        @only_tables ||= Array.new
        @additional_options ||= Array.new
        instance_eval(&var) if block_given?;Fi[	@
i-I"Æ        super(var)
        @skip_tables ||= Array.new
        @only_tables ||= Array.new
        @additional_options ||= Array.new
        instance_eval(&var) if block_given?;Fi[	I"if f.plist_startup then;F@>i[[	@˚i4I"≤      var = (var / var)
      if var.file?.! and var.symlink?.! then
        if f.plist_startup then
          (var << "STRING")
          (var << "STRING") unless var.directory?;Fi[	@˚iFI"°        if Kernel.system("STRING") then
          (var << "STRING")
          if f.plist_startup then
            (var << "STRING")
            (var << "STRING");Fi[	I",(var << "STRING") unless var.directory?;F@>i[[	@˚i6I"ñ        if f.plist_startup then
          (var << "STRING")
          (var << "STRING") unless var.directory?
          (var << "STRING")
        else;Fi[	@˚i:I"Ç        else
          (var << "STRING")
          (var << "STRING") unless var.directory?
          (var << "STRING")
        end;Fi[	@Ø%@ri[[	@&,iI"≤    var.sort.reverse_each { |var| var.rmdir_if_possible }
    if ($n == "LITERAL") and ($d == "LITERAL") then
      puts("STRING") if ARGV.verbose?
    else
      print("STRING");Fi[	@iøI"≤  def function(var, var, var)
    if var.exist? and (var.realpath == var.realpath) then
      puts("STRING") if ARGV.verbose?
    else
      if var.dry_run and var.overwrite then;Fi[	I"*var.unlink unless @f.skip_clean?(var);F@ri[[	@&i8I"™        else
          if (var.extname == "STRING") then
            var.unlink unless @f.skip_clean?(var)
          else
            if (var == (@f.lib + "STRING")) then;Fi[	@&i;I"¿          else
            if (var == (@f.lib + "STRING")) then
              var.unlink unless @f.skip_clean?(var)
            else
              clean_file_permissions(var) if var.symlink?.!;Fi[	I"puts(hardware);F@ii[[	@áikI"±    puts("STRING") if HOMEBREW_PREFIX.to_s.!=("STRING")
    puts("STRING") if HOMEBREW_CELLAR.to_s.!=("STRING")
    puts(hardware)
    puts("STRING")
    puts("STRING") if xcode;Fi[	@áiÖI"l    puts("STRING")
    puts("STRING")
    puts(hardware)
    puts("STRING")
    puts("STRING") if xcode;Fi[	I"puts("STRING") if xcode;FI"puts("STRING") if clt;Fi[[	@áimI"ë    puts(hardware)
    puts("STRING")
    puts("STRING") if xcode
    puts("STRING") if clt
    puts("STRING") if RUBY_VERSION.to_f.!=("LITERAL");Fi[	@áiáI"}    puts(hardware)
    puts("STRING")
    puts("STRING") if xcode
    puts("STRING") if clt
    puts("STRING") if gcc_40;Fi[	I"puts("STRING") if gcc_40;FI"puts("STRING") if gcc_42;Fi[[	@áiqI"≤    puts("STRING") if RUBY_VERSION.to_f.!=("LITERAL")
    unless MacOS.compilers_standard? then
      puts("STRING") if gcc_40
      puts("STRING") if gcc_42
      puts("STRING");Fi[	@áiâI"Ç    puts("STRING") if xcode
    puts("STRING") if clt
    puts("STRING") if gcc_40
    puts("STRING") if gcc_42
    puts("STRING");Fi[	@˜;@ii[[	@áirI"ë    unless MacOS.compilers_standard? then
      puts("STRING") if gcc_40
      puts("STRING") if gcc_42
      puts("STRING")
      puts("STRING");Fi[	@áiäI"~    puts("STRING") if clt
    puts("STRING") if gcc_40
    puts("STRING") if gcc_42
    puts("STRING")
    puts("STRING");Fi[	I"'puts("STRING") if macports_or_fink;F@ii[[	@áivI"a      puts("STRING")
    end
    puts("STRING") if macports_or_fink
    puts("STRING")
  end;Fi[	@áiçI"w    puts("STRING")
    puts("STRING")
    puts("STRING") if macports_or_fink
    puts("STRING")
    puts("STRING");Fi[	I"@skip_tables ||= Array.new;F@≈;i[[	@
i+I"ü      def function(var, var)
        super(var)
        @skip_tables ||= Array.new
        @only_tables ||= Array.new
        @additional_options ||= Array.new;Fi[	@
i,I"ü      def function(var, var)
        super(var)
        @skip_tables ||= Array.new
        @only_tables ||= Array.new
        @additional_options ||= Array.new;Fi[	I"D# Tables to dump, tables that aren't specified won't get dumped;F@ei[[	@
i"I"∞      # Tables to skip while dumping the database
      ##
      # Tables to dump, tables that aren't specified won't get dumped
      ##
      # Additional "mysqldump" options;Fi[	@
i!I"Æ      # Tables to skip while dumping the database
      ##
      # Tables to dump, tables that aren't specified won't get dumped
      ##
      # Additional "pg_dump" options;Fi[	@«@ç,i[[	@+i I"<require("formula")
require("blacklist")
module Homebrew;Fi [	@ci I"<require("formula")
require("blacklist")
module Homebrew;Fi [	@«@ˆi[[	@RiI"Vrequire("testing_env")
require("formula")
require("test/testball")
require("keg");Fi[	@IiI"Zrequire("testing_env")
require("formula")
require("test/testball")
require("version");Fi[	I"0# Tables to skip while dumping the database;F@ei[[	@
i I"¶      # Connectivity options
      ##
      # Tables to skip while dumping the database
      ##
      # Tables to dump, tables that aren't specified won't get dumped;Fi[	@
iI"¶      # Connectivity options
      ##
      # Tables to skip while dumping the database
      ##
      # Tables to dump, tables that aren't specified won't get dumped;Fi[	I"/# additional options specified by the user;F@Ói[[	@Úi†I"™      ##
      # Builds a MongoDB compatible string for the
      # additional options specified by the user
      def function
        @additional_options.join("STRING");Fi[	@ıinI"®      ##
      # Builds a Redis compatible string for the
      # additional options specified by the user
      def function
        @additional_options.join("STRING");Fi[	@Ô@î&i[[	@¶iI"y    @elem = case var
    when "STRING", "STRING" then
      "STRING"
    when "STRING", "STRING" then
      "STRING";Fi[	@ÜidI"ñ          case var.value.first
          when "STRING", "STRING" then
            "STRING"
          when "STRING", "STRING" then
            "STRING";Fi[	@1#I"&"STRING".gsub("STRING", "STRING");Fi[[	@
i]I"≥      def function
        ["STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING".gsub("STRING", "STRING")
        end.compact.join("STRING");Fi[	@
ieI"Ω      def function
        ["STRING", "STRING", "STRING"].map do |var|
          next if send(var).to_s.empty?
          "STRING".gsub("STRING", "STRING")
        end.compact.join("STRING");Fi[	@Ó@Ÿ4i[[	@i$I"ï        $stdout.write(color("LITERAL"))
        yield)
      ensure
        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL")));Fi[	@i/I"ï        $stdout.write(color("LITERAL"))
        yield)
      ensure
        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL")));Fi[	I"if ARGV.dry_run? then;F@ii[[	@’i(I"£          if var.can_cleanup? then
            if Keg.new(var).linked?.! then
              if ARGV.dry_run? then
                puts("STRING")
              else;Fi[	@’iEI"Î        var = bottle_file_outdated?(var, var)
        if ((var and (var.version > var)) or ((ARGV.switch?("STRING") and (var and var.installed?.!)) or var)) then
          if ARGV.dry_run? then
            puts("STRING")
          else;Fi[	@ã@<i[[	@
i*I"∞      # Creates a new instance of the MySQL adapter object
      def function(var, var)
        super(var)
        @skip_tables ||= Array.new
        @only_tables ||= Array.new;Fi[	@
i+I"™      # so it doesn't prompt and hang in the process
      def function(var, var)
        super(var)
        @skip_tables ||= Array.new
        @only_tables ||= Array.new;Fi[	@3@ei[[	@ÚiI"}      
      ##
      # Name of the database that needs to get dumped
      ##
      # Credentials for the specified database;Fi[	@
iI"}      
      ##
      # Name of the database that needs to get dumped
      ##
      # Credentials for the specified database;Fi[	@„"I"synchronize do;Fi[[	I"0projects//rescue_proj/resque/multi_queue.rb;Ti!I"{    # pop, a ThreadError is raised.
    def function(var)
      if var then
        synchronize do
          var = nil;Fi[	I"*projects//rescue_proj/resque/queue.rb;TiDI"ã    # pop, a ThreadError is raised.
    def function(var)
      if var then
        synchronize do
          var = @redis.lpop(@redis_name);Fi[	@„"@\i[[	@©inI"•        if is_haml? then
          var = (block_given? and block_is_haml?(var))
          if var then
            var = var
            var = haml_bind_proc do |var|;Fi[	@©iÄI"‘      def function(var, var, var)
        var = (block_given? and (is_haml? and block_is_haml?(var)))
        if var then
          var = var
          var = proc { |var| with_tabs("LITERAL") { var.call(*var) } };Fi[	I"*var = if ARGV.include?("STRING") then;FI"ARGV.next;Fi[[	@≥i
I"o  def function
    var = Pathname.getwd
    var = if ARGV.include?("STRING") then
      ARGV.next
    else;Fi[	@≥iI"å      var.version.to_s.empty? ? (raise("STRING")) : (var.version)
    end
    var = if ARGV.include?("STRING") then
      ARGV.next
    else;Fi[	@i<@ri[[	@≥iI"ù    var = Pathname.getwd
    var = if ARGV.include?("STRING") then
      ARGV.next
    else
      var.version.to_s.empty? ? (raise("STRING")) : (var.version);Fi[	@≥iI"z    end
    var = if ARGV.include?("STRING") then
      ARGV.next
    else
      var.basename.to_s =~ /(.*?)-?#{var}/;Fi[	I"Lif Process.uid.zero? and File.stat(HOMEBREW_BREW_FILE).uid.zero?.! then;F@}i[[	@©iI"{      end
    end
    if Process.uid.zero? and File.stat(HOMEBREW_BREW_FILE).uid.zero?.! then
      raise("STRING")
    end;Fi[	@:iI"¨  def function
    raise(KegUnspecifiedError) if ARGV.named.empty?
    if Process.uid.zero? and File.stat(HOMEBREW_BREW_FILE).uid.zero?.! then
      raise("STRING")
    end;Fi[	@„"@R4i[[	@w$iaI"         File.join(Compass.configuration.project_path, Compass.configuration.images_dir, var)
      end
      if var then
        var = "STRING" unless (var["LITERAL"] == "STRING")
        var = "STRING";Fi[	@w$iéI"‘        File.join(Compass.configuration.project_path, Compass.configuration.generated_images_dir, var)
      end
      if var then
        var = "STRING" unless (var["LITERAL"] == "STRING")
        var = "STRING";Fi[	@I,I"4rescue FormulaInstallationAlreadyAttemptedError;Fi[[	@©iXI"|      var.install
      var.caveats
      var.finish)
    rescue FormulaInstallationAlreadyAttemptedError
      # do nothing;Fi[	@+i?I"|      var.install
      var.caveats
      var.finish)
    rescue FormulaInstallationAlreadyAttemptedError
      # do nothing;Fi[	@É<@ÿi[[	@©iYI"ñ      var.caveats
      var.finish)
    rescue FormulaInstallationAlreadyAttemptedError
      # do nothing
    rescue CannotInstallFormulaError => var;Fi[	@+i@I"ñ      var.caveats
      var.finish)
    rescue FormulaInstallationAlreadyAttemptedError
      # do nothing
    rescue CannotInstallFormulaError => var;Fi[	I"class String;F@Ói[[	@¨iI"D  end
end
class String
  def function
    Formula.factory(self);Fi[	@·
i I"Zclass String
  def function
    gsub(/^.{#{slice("LITERAL").length}STRING/, "STRING");Fi [	I"var = OpenStruct.new;FI"var.overwrite = true;Fi[[	@i4I"û  def function
    FileUtils.touch((HOMEBREW_PREFIX / "STRING"))
    var = OpenStruct.new
    var.overwrite = true
    assert_equal("LITERAL", @keg.link(var));Fi[	@i;I"â  def function
    FileUtils.touch((HOMEBREW_PREFIX / "STRING"))
    var = OpenStruct.new
    var.overwrite = true
    var.dry_run = true;Fi[	I"&print("STRING") { var.link(var) };F@Ö3i[[	@:iI"|      end
      if var.dry_run and var.overwrite then
        print("STRING") { var.link(var) }
        next
      else;Fi[	@:iI"r      else
        if var.dry_run then
          print("STRING") { var.link(var) }
          next
        end;Fi[	@äI"<assert_valid_keys(var, "LITERAL", "LITERAL", "LITERAL");Fi[[	@`iÇI"    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.
    def function(var, var)
      assert_valid_keys(var, "LITERAL", "LITERAL", "LITERAL")
      unless var = var.include?("LITERAL") then;Fi[	@2iI"3    # * <tt>:to</tt> - A hash to write the initialized state to instead of
    #   writing to the object.  Default is to write directly to the object.
    def function(var, var)
      assert_valid_keys(var, "LITERAL", "LITERAL", "LITERAL")
      var = { "LITERAL" => (true), "LITERAL" => (true) }.merge(var);Fi[	@äI"label_field(var, var);Fi[[	@æi%I"i    end
    
    def function(var, var)
      label_field(var, var)
      input_field("STRING", var);Fi[	@æi+I"i    end
    
    def function(var, var)
      label_field(var, var)
      input_field("STRING", var);Fi[	@äI"check_encoding(var);Fi[[	@Éi-I"ƒ        # The encoding beings used for this document.
        # Create a new Parser with +doc+ and +encoding+
        def function(var, var)
          check_encoding(var)
          @encoding = var;Fi[	@ÉiAI"}        ###
        # Parse given +io+
        def function(var, var)
          check_encoding(var)
          @encoding = var;Fi[	I"when nil then;F@Ôi[[	@ciI"y        var = ARGV.first
        var = case var
        when nil then
          "STRING"
        when "LITERAL" then;Fi[	@µirI"[      when false then
        "STRING"
      when nil then
        "STRING"
      else;Fi[	@ª<I"var.fails_with?("LITERAL");Fi[[	@6i/I"è    assert(var.fails_with?("LITERAL").!)
    assert(case MacOS.gcc_42_build_version
    when nil then
      var.fails_with?("LITERAL")
    else;Fi[	@6iRI"ç    assert(var.fails_with?("LITERAL"))
    assert(case MacOS.gcc_42_build_version
    when nil then
      var.fails_with?("LITERAL")
    else;Fi[	@äI"$raise(ArgumentError) unless var;Fi[[	@ iI"º        ###
        # Parse html stored in +data+ using +encoding+
        def function(var, var)
          raise(ArgumentError) unless var
          return unless (var.length > "LITERAL");Fi[	@ iI"≥        ###
        # Parse a file with +filename+
        def function(var, var)
          raise(ArgumentError) unless var
          raise(Errno::ENOENT) unless File.exists?(var);Fi[	I"require("open-uri");F@xi[[	@oiI"crequire("rss/1.0")
require("rss/2.0")
require("open-uri")
require("fileutils")
require("yaml");Fi[	@0iI"Urequire("rubygems")
require("open-uri")
require("fileutils")
require("nokogiri");Fi[	I"ENV["STRING"] = "STRING";F@◊<i[[	@—iI"Ω      ENV["STRING"] = ENV["STRING"].split("STRING").insert("LITERAL", "STRING").join("STRING")
    end
    ENV["STRING"] = "STRING"
    ENV["STRING"] = "STRING"
    ENV["STRING"] = "STRING";Fi[	@—iI"≥    end
    ENV["STRING"] = "STRING"
    ENV["STRING"] = "STRING"
    ENV["STRING"] = "STRING"
    puts("STRING".undent_________________________________________________________72);Fi[	I"+when /^#{HOMEBREW_LIBRARY}STRING/ then;F@Ôi[[	@≥i`I"Ü  def function
    case self.to_s
    when /^#{HOMEBREW_LIBRARY}STRING/ then
      "STRING"
    when /^#{HOMEBREW_LIBRARY}STRING/ then;Fi[	@≥ibI"|    when /^#{HOMEBREW_LIBRARY}STRING/ then
      "STRING"
    when /^#{HOMEBREW_LIBRARY}STRING/ then
      "STRING"
    else;Fi[	I"ofail("STRING");F@Ö3i[[	@öiI"t    ARGV.formulae.each do |var|
      unless var.installed? then
        ofail("STRING")
        next
      end;Fi[	@öiI"a      end
      unless var.test_defined? then
        ofail("STRING")
        next
      end;Fi[	I"var.unlink;FI"var.uninstall;Fi[[	@CiI"Ü        ARGV.kegs.each do |var|
          puts("STRING")
          var.unlink
          var.uninstall
          rm_opt_link(var.fname);Fi[	@Ri#I"f    yield
    var = Keg.new(var.prefix)
    var.unlink
    var.uninstall
    assert(var.exist?.!);Fi[	@äI"<var = var.sub("LITERAL", (("STRING" + var) + "STRING"));Fi[[	@*iI"'              var = add_code_tags(Pygments.highlight(var, "LITERAL" => (var), "LITERAL" => ({ "LITERAL" => "STRING" })), var)
            end
            def function(var, var)
              var = var.sub("LITERAL", (("STRING" + var) + "STRING"))
              var = var.sub("LITERAL", "STRING");Fi[	@9!i6I"é    end
    
    def function(var, var)
      var = var.sub("LITERAL", (("STRING" + var) + "STRING"))
      var = var.sub("LITERAL", "STRING");Fi[	@äI"%return if var.options["LITERAL"];Fi[[	@≤iÃI"É      
      # @see Base#compile
      def function(var, var)
        return if var.options["LITERAL"]
        var.instance_eval do;Fi[	@≤i˙I"Ñ      end
      
      def function(var, var)
        return if var.options["LITERAL"]
        var.send("LITERAL", precompiled(var));Fi[	@ä@X:i[[	@≤iòI"Ã      
      # @see Base#render_with_options
      def function(var, var)
        var = var["LITERAL"] ? ("STRING") : ("STRING")
        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING");Fi[	@≤i®I"Ã      
      # @see Base#render_with_options
      def function(var, var)
        var = var["LITERAL"] ? ("STRING") : ("STRING")
        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING");Fi[	@äI"'find_or_create_participation!(var);Fi[[	I":projects//diaspora_proj/models/user/social_actions.rb;TiI"}  end
  
  def function(var, var)
    find_or_create_participation!(var)
    Like::Generator.new(self, var).create!(var);Fi[	@=iI"Å  end
  
  def function(var, var)
    find_or_create_participation!(var)
    var = build_post("LITERAL", "LITERAL" => (var.guid));Fi[	@ä@à9i[[	@i@I"˜      # @example Return the direct message with the id 1825786345
      #   Twitter.direct_message(1825786345)
      def function(var, var)
        var["LITERAL"] = var
        object_from_response(Twitter::DirectMessage, "LITERAL", "STRING", var);Fi[	@†iI"¡      # @example Return the top 10 trending topics for San Francisco
      #   Twitter.trends(2487956)
      def function(var, var)
        var["LITERAL"] = var
        var = get("STRING", var);Fi[	I" fetch(var, []).map do |var|;F@4i[[	@!i}I"d  
  def function(var)
    fetch(var, []).map do |var|
      case var
      when "LITERAL" then;Fi[	@!iãI"d  
  def function(var)
    fetch(var, []).map do |var|
      case var
      when "LITERAL" then;Fi[	@ä@k9i[[	@Ïi-I"e  end
  
  def function(var, var)
    if self.target.nil? then
      Rails.logger.info("STRING");Fi[	@¯ iSI"e  end
  
  def function(var, var)
    if self.target.nil? then
      Rails.logger.info("STRING");Fi[	I"oh1("STRING");FI"var.unlink if var;Fi[[	@+i;I"©      var.show_header = false
      var.install_bottle = (install_bottle?(var) and var.used_options.empty?)
      oh1("STRING")
      var.unlink if var
      var.install;Fi[	@√iqI"}      var.ignore_deps = true
      var.show_header = false
      oh1("STRING")
      var.unlink if var
      var.install;Fi[	I"6var = Keg.new(var.linked_keg.realpath) rescue nil;FI")var = FormulaInstaller.new(var, var);Fi[[	@+i7I"•        Tab.dummy_tab(var)
      end
      var = Keg.new(var.linked_keg.realpath) rescue nil
      var = FormulaInstaller.new(var, var)
      var.show_header = false;Fi[	@√imI"´    begin
      (var = Tab.for_formula(var)
      var = Keg.new(var.linked_keg.realpath) rescue nil
      var = FormulaInstaller.new(var, var)
      var.ignore_deps = true;Fi[	@$=@-i[[	@+i<I"ù      var.install_bottle = (install_bottle?(var) and var.used_options.empty?)
      oh1("STRING")
      var.unlink if var
      var.install
      var.caveats;Fi[	@√irI"r      var.show_header = false
      oh1("STRING")
      var.unlink if var
      var.install
      var.caveats;Fi[	@äI" var["LITERAL"] ||= "STRING";Fi[[	@liI"V  end
  
  def function(var, var)
    var["LITERAL"] ||= "STRING"
    if var then;Fi[	I"5projects//diaspora_proj/helpers/people_helper.rb;Ti"I"ù  end
  
  def function(var, var)
    var["LITERAL"] ||= "STRING"
    if defined? user_signed_in? and (user_signed_in? and (current_user.person == var)) then;Fi[	@äI"#var = var.map { |var| var.id };Fi[[	@9i™I"Z  private
  
  def function(var, var)
    var = var.map { |var| var.id }
    var = {};Fi[	@.i˜I"m  end
  
  def function(var, var)
    var = var.map { |var| var.id }
    var.each { |var| (var << var) };Fi[	@8@˚i[[	@ioI"ò      end
    when "LITERAL" then
      if @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then
        "LITERAL"
      else;Fi[	@iyI"ò      end
    when "LITERAL" then
      if @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then
        "LITERAL"
      else;Fi[	@ä@Ì i[[	@ØiOI"ö  end
  
  def function(var, var)
    Rails.logger.info("STRING")
    var.profile.update_attributes(self.attributes.merge("LITERAL" => (self.tag_string)));Fi[	@ŒiMI"ı  # @note A [Contact] may already exist if the [Request]'s recipient is sharing with the sender
  # @return [Request]
  def function(var, var)
    Rails.logger.info("STRING")
    var = var.contacts.find_or_initialize_by_person_id(self.sender.id);Fi[	@ä@I1i[[	@giI"É  end
  
  def function(var, var)
    Rails.logger.debug("STRING")
    post_to_facebook("STRING", create_post_params(var).to_param);Fi[	@oiI"j  end
  
  def function(var, var)
    Rails.logger.debug("STRING")
    var = public_message(var, var);Fi[	I"&var = (HOMEBREW_CACHE / "STRING");FI"(assert_equal("STRING", var.extname);Fi[[	@˚iáI"Ñ  
  def function
    var = (HOMEBREW_CACHE / "STRING")
    assert_equal("STRING", var.extname)
    assert_equal("STRING", var.stem);Fi[	@˚iãI"«    assert_equal("STRING", var.stem)
    assert_version_equal("STRING", var.version)
    var = (HOMEBREW_CACHE / "STRING")
    assert_equal("STRING", var.extname)
    assert_equal("STRING", var.stem);Fi[	I"!(var << var) if var.exist?.!;F@¯!i[[	@$iﬂI"|      (var << "STRING")
    end
    (var << var) if var.exist?.!
    (var << var)
    ((var << "STRING") << var) if var;Fi[	@$i˘I"ª    var = var.exist? ? ("STRING") : ("STRING")
    var = [@@svn, var, "STRING", "STRING", "STRING"]
    (var << var) if var.exist?.!
    (var << var)
    ((var << "STRING") << var) if var;Fi[	I"&((var << "STRING") << var) if var;F@d9i[[	@$i·I"ê    (var << var) if var.exist?.!
    (var << var)
    ((var << "STRING") << var) if var
    (var << "STRING") if var
    quiet_safe_system(*var);Fi[	@$i˚I"ê    (var << var) if var.exist?.!
    (var << var)
    ((var << "STRING") << var) if var
    (var << "STRING") if var
    quiet_safe_system(*var);Fi[	@äI"var = lambda do |var|;Fi[[	@iÎI"º    #   end
    #
    def function(var, var)
      var = lambda do |var|
        var.env["STRING"].authenticate!("LITERAL" => (var)) and (var.nil? or var.call(var.env["STRING"].user(var)));Fi[	@iI"”    #   root :to => 'landing#show'
    #
    def function(var, var)
      var = lambda do |var|
        var.env["STRING"].authenticate?("LITERAL" => (var)) and (var.nil? or var.call(var.env["STRING"].user(var)));Fi[	I"Dir.chdir(@clone) do;F@q+i[[	@$i<I"t  def function
    var = Dir.getwd
    Dir.chdir(@clone) do
      if @spec and @ref then
        ohai("STRING");Fi[	@$i°I"t  def function
    var = Dir.getwd
    Dir.chdir(@clone) do
      if @spec and @ref then
        ohai("STRING");Fi[	I"var = Dir.getwd;F@w=i[[	@$i;I"`  
  def function
    var = Dir.getwd
    Dir.chdir(@clone) do
      if @spec and @ref then;Fi[	@$i†I"`  
  def function
    var = Dir.getwd
    Dir.chdir(@clone) do
      if @spec and @ref then;Fi[	@äI"var = to_adapter.get(var);Fi[[	@Íi{I"í        end
        
        def function(var, var)
          var = to_adapter.get(var)
          var if var and (var.authenticatable_salt == var);Fi[	@ôiSI"⁄        
        # Recreate the user based on the stored cookie
        def function(var, var)
          var = to_adapter.get(var)
          if var and ((var.rememberable_value == var) and var.remember_expired?.!) then;Fi[	I"nostdout do;FI"assert_nothing_raised do;Fi[[	@˚iBI"Ö    require("cmd/prune")
    require("cleaner")
    nostdout do
      assert_nothing_raised do
        var = TestBallWithRealPath.new;Fi[	@˚inI"x  
  def function
    nostdout do
      assert_nothing_raised do
        assert(Pathname.getwd.rmdir_if_possible.!);Fi[	I"Find.prune;F@Íi[[	@i‹I"¥        case yield(var.relative_path_from(var))
        when "LITERAL", nil then
          Find.prune
        when "LITERAL" then
          next if (File.basename(var) == "STRING");Fi[	@iÍI"Ω          case yield(var.relative_path_from(var))
          when "LITERAL" then
            Find.prune
          when "LITERAL" then
            var.mkpath unless resolve_any_conflicts(var);Fi[	@Ù7@Íi[[	@$i¯I"x    case var
    when "LITERAL" then
      CVSDownloadStrategy
    when "LITERAL" then
      BazaarDownloadStrategy;Fi[	@$i!I"~      CurlDownloadStrategy
    when "LITERAL" then
      CVSDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy;Fi[	@Ÿ7@Íi[[	@$i˙I"Ä      CVSDownloadStrategy
    when "LITERAL" then
      BazaarDownloadStrategy
    when "LITERAL" then
      GitDownloadStrategy;Fi[	@$iI"y    case var
    when "LITERAL" then
      BazaarDownloadStrategy
    when "LITERAL" then
      CurlDownloadStrategy;Fi[	@ä@_i[[	@{1iI"~      end
      
      def function(var, var)
        var = Devise::Mapping.find_scope!(var)
        send("STRING", *var);Fi[	@{1iI"~      end
      
      def function(var, var)
        var = Devise::Mapping.find_scope!(var)
        send("STRING", *var);Fi[	@ä@6i[[	@ÂiEI"«      #   sign_in @user, :bypass => true            # sign_in(resource, options)
      #
      def function(var, var)
        var = var.extract_options!
        var = Devise::Mapping.find_scope!(var);Fi[	@Âi∑I"È      # then to the url specified by after_sign_in_path_for. It accepts the same
      # parameters as the sign_in method.
      def function(var, var)
        var = var.extract_options!
        var = Devise::Mapping.find_scope!(var);Fi[	@ä@”1i[[	@?i\I"Ø  
  # Returns the path to the original image file for the sprite with the given name
  def function(var, var)
    var = convert_sprite_name(var)
    verify_map(var, "STRING");Fi[	@?ijI"}  
  # Returns boolean if sprite has a parent
  def function(var, var)
    var = convert_sprite_name(var)
    verify_map(var);Fi[	I"## Request a 32-bit only build.;FI"L# This is needed for some use-cases though we prefer to build Universal;Fi[[	@JiI"ò  end
  
  # Request a 32-bit only build.
  # This is needed for some use-cases though we prefer to build Universal
  # when a 32-bit version is needed.;Fi[	@‡i»I"ò  end
  
  # Request a 32-bit only build.
  # This is needed for some use-cases though we prefer to build Universal
  # when a 32-bit version is needed.;Fi[	@∫=I"'# when a 32-bit version is needed.;Fi[[	@Ji{I"°  
  # Request a 32-bit only build.
  # This is needed for some use-cases though we prefer to build Universal
  # when a 32-bit version is needed.
  def function;Fi[	@‡i…I"°  
  # Request a 32-bit only build.
  # This is needed for some use-cases though we prefer to build Universal
  # when a 32-bit version is needed.
  def function;Fi[	@¡=@Ói[[	@Ji|I"µ  # Request a 32-bit only build.
  # This is needed for some use-cases though we prefer to build Universal
  # when a 32-bit version is needed.
  def function
    include?("STRING");Fi[	@‡i I"ª  # Request a 32-bit only build.
  # This is needed for some use-cases though we prefer to build Universal
  # when a 32-bit version is needed.
  def function
    @args.include?("STRING");Fi[	@äI"&var, var = *grad_point(var).value;Fi[[	@ñieI"ä    end
    
    def function(var, var)
      var, var = *grad_point(var).value
      var, var = *grad_point(opposite_position(var)).value;Fi[	@ñimI"ê    end
    
    def function(var, var)
      var, var = *grad_point(var).value
      var = grad_end_position(var, Sass::Script::Bool.new(true));Fi[	I"raise("STRING".undent);F@ri[[	@Ri I"ê      if $?.success?.! then
        if self.exist? then
          raise("STRING".undent)
        else
          if dirname.writable_real?.! then;Fi[	@RiI"å        else
          if dirname.writable_real?.! then
            raise("STRING".undent)
          else
            raise("STRING".undent);Fi[	I"+var = [var] unless var.kind_of?(Array);F@ß(i[[	@·
i;I"∫  # Removes variable assignments completely.
  def function(var)
    var = [var] unless var.kind_of?(Array)
    var.each do |var|
      var = gsub!(Regexp.new("STRING"), "STRING", false);Fi[	@iI"¬    var = "LITERAL"
    normalize_patches(var).each do |var, var|
      var = [var] unless var.kind_of?(Array)
      var.each do |var|
        (@patches << Patch.new(var, ("STRING" % var), var));Fi[	@äI"'var ||= configuration.project_path;Fi[[	@ëi}I"≈      
      # Returns a full path to the relative path to the project directory
      def function(var, var)
        var ||= configuration.project_path
        File.join(var, *var.split("STRING"));Fi[	@ëi}I"ö      end
      
      def function(var, var)
        var ||= configuration.project_path
        if (var[("LITERAL"..(var.size - "LITERAL"))] == var) then;Fi[	I"-if (f.prefix + "STRING").directory? then;F@3i[[	@√iI"m  
  def function
    if (f.prefix + "STRING").directory? then
      opoo("STRING")
      puts("STRING");Fi[	@√i!I"m  
  def function
    if (f.prefix + "STRING").directory? then
      opoo("STRING")
      puts("STRING");Fi[	I";var = (HOMEBREW_PREFIX + var.relative_path_from(self));FI"*var.extend(ObserverPathnameExtension);Fi[[	@i0I"Ú      var.find do |var|
        next if (var == self)
        var = (HOMEBREW_PREFIX + var.relative_path_from(self))
        var.extend(ObserverPathnameExtension)
        next if (var.symlink?.! or (var.exist?.! or var.!=(var.resolved_path)));Fi[	@i÷I"±    var.find do |var|
      next if (var == var)
      var = (HOMEBREW_PREFIX + var.relative_path_from(self))
      var.extend(ObserverPathnameExtension)
      if var.file? then;Fi[	@äI"if var.nil? then;Fi[[	@RiJI"c  end
  
  def function(var, var)
    if var.nil? then
      var = (self + File.basename(var));Fi[	@‡iïI"J  end
  
  def function(var, var)
    if var.nil? then
      case var;Fi[	I"%link_dir("STRING", var) do |var|;FI"case var.to_s;Fi[[	@ipI"Ø      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) do |var|
        case var.to_s
        when "STRING" then;Fi[	@i{I"r        end
      end
      link_dir("STRING", var) do |var|
        case var.to_s
        when "STRING" then;Fi[	@>@W
i[[	@iqI"ó      link_dir("STRING", var) { "LITERAL" }
      link_dir("STRING", var) do |var|
        case var.to_s
        when "STRING" then
          "LITERAL";Fi[	@i|I"z      end
      link_dir("STRING", var) do |var|
        case var.to_s
        when "STRING" then
          "LITERAL";Fi[	I"Asystem(MacOS.locate("STRING"), "STRING", var, "STRING", var);F@ri[[	@iI"          var.each do |var|
            if (var.dylib? or var.mach_o_bundle?) and (var.parent + var).exist? then
              system(MacOS.locate("STRING"), "STRING", var, "STRING", var)
            else
              if var.mach_o_executable? and (lib / var).exist? then;Fi[	@iI"Ò            else
              if var.mach_o_executable? and (lib / var).exist? then
                system(MacOS.locate("STRING"), "STRING", var, "STRING", var)
              else
                var = find_dylib(Pathname.new(var).basename);Fi[	@äI"return if var.nil?;Fi[[	@ëiI"w      end
      
      def function(var, var)
        return if var.nil?
        var = configuration_for(var, var);Fi[	@1iéI"ê      private
      
      def function(var, var)
        return if var.nil?
        var = "STRING" if @options["LITERAL"] and (var == "STRING");Fi[	I"case MacOS.version;F@Íi[[	@0iI"T  
  def function
    case MacOS.version
    when "LITERAL" then
      "STRING";Fi[	@êiI"    else
      if (prefix.to_s == "STRING") then
        case MacOS.version
        when "LITERAL" then
          "STRING";Fi[	@ä@ëi[[	I"7projects//diaspora_proj/helpers/contacts_helper.rb;TiI"u  end
  
  def function(var, var)
    var = "LITERAL"
    var = { "LITERAL" => "STRING", "LITERAL" => "STRING" };Fi[	@oiI"É  end
  
  def function(var, var)
    var = "LITERAL"
    URI.extract(var.text("LITERAL" => (true)), ["STRING", "STRING"]) do |var|;Fi[	@ä@i[[	@+
iKI"í      private
      
      def function(var, var)
        if var["LITERAL"] then
          File.basename(strip_trailing_separator(var["LITERAL"]));Fi[	@+
iSI"}      end
      
      def function(var, var)
        if var["LITERAL"] then
          if absolute_path?(var["LITERAL"]) then;Fi[	@®7I"8FileUtils.mv(@patch_filename, @compressed_filename);Fi[[	@iTI"π    case @compression
    when "LITERAL" then
      @compressed_filename = (@patch_filename + "STRING")
      FileUtils.mv(@patch_filename, @compressed_filename)
    when "LITERAL" then;Fi[	@iWI"Œ      FileUtils.mv(@patch_filename, @compressed_filename)
    when "LITERAL" then
      @compressed_filename = (@patch_filename + "STRING")
      FileUtils.mv(@patch_filename, @compressed_filename)
    else;Fi[	I"case @language;F@Íi[[	@[iI"z  
  def function
    case @language
    when "LITERAL" then
      ["STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	@[i.I"P  
  def function
    case @language
    when "LITERAL" then
      "STRING";Fi[	@ä@‹5i[[	@ÙiZI"Ÿ      # Net::FTP raises an exception when the directory it's trying to create
      # already exists, so we have rescue it
      def function(var, var)
        var = Array.new
        var.split("STRING").each do |var|;Fi[	@ÆiNI"⁄      # Net::SFTP raises an exception when the directory it's trying to create
      # already exists, so we have rescue it
      def function(var, var)
        var = Array.new
        var.split("STRING").each do |var|;Fi[	@ä@„"i[[	@iPI"z      attr_reader("LITERAL")
      
      def function(var, var)
        if var then
          @welcome_message = var;Fi[	@Ri%I"U  end
  
  def function(var, var)
    if var then
      var = File.basename(var);Fi[	@ı@};i[[	I"-projects//homebrew_proj/test/test_ENV.rb;Ti I"]require("testing_env")
require("hardware")
class EnvironmentTests < Test::Unit::TestCase;Fi [	I"2projects//homebrew_proj/test/test_hardware.rb;Ti I"Zrequire("testing_env")
require("hardware")
class HardwareTests < Test::Unit::TestCase;Fi [	@ä@ß(i[[	@#iI"“    # file in order to add support for new, simple browser functions without
    # waiting for a new compass release.
    def function(var, var)
      var.each do |var|
        unless ASPECTS.include?(var) then;Fi[	@#i.I"Ë    # file in order to remove support for simple functions that no longer need to
    # a prefix without waiting for a new compass release.
    def function(var, var)
      var.each do |var|
        unless ASPECTS.include?(var) then;Fi[	I"!# Used only within the specs;F@Ói[[	@ëiI"l        
        ##
        # Used only within the specs
        def function
          defaults.reset!;Fi[	@\3iI"_      
      ##
      # Used only within the specs
      def function
        @table.clear;Fi[	@G$@Ói[[	@%i
I"•      extend(ActiveSupport::Concern)
      
      module ClassMethods
        def function
          defined? @scoped_views ? (@scoped_views) : (Devise.scoped_views);Fi[	@9iI"      end
      
      module ClassMethods
        def function
          Rails::Generators.hide_namespace(self.namespace);Fi[	@ıI"require("extend/string");Fi[[	@ûi I"_require("testing_env")
require("extend/string")
class InreplaceTest < Test::Unit::TestCase;Fi [	I"0projects//homebrew_proj/test/test_string.rb;Ti I"\require("testing_env")
require("extend/string")
class StringTest < Test::Unit::TestCase;Fi [	I"ARGV.unshift("STRING");F@m>i[[	@ç$i"I"g  
  def function
    ARGV.unshift("STRING")
    ARGV.unshift("STRING")
    ARGV.unshift("STRING");Fi[	@ç$i#I"Ç  def function
    ARGV.unshift("STRING")
    ARGV.unshift("STRING")
    ARGV.unshift("STRING")
    assert(ARGV.switch?("STRING"));Fi[	I"#assert(ARGV.switch?("STRING"));F@t>i[[	@ç$i%I"û    ARGV.unshift("STRING")
    ARGV.unshift("STRING")
    assert(ARGV.switch?("STRING"))
    assert(ARGV.switch?("STRING"))
    assert(ARGV.switch?("STRING"));Fi[	@ç$i&I"®    ARGV.unshift("STRING")
    assert(ARGV.switch?("STRING"))
    assert(ARGV.switch?("STRING"))
    assert(ARGV.switch?("STRING"))
    assert(ARGV.switch?("STRING").!);Fi[	I"IBackup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"));F@
i[[	@äi I"¬      # Yields to the block the compressor command and filename extension.
      def function
        Backup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"))
        super
      end;Fi[	@çi&I"¬      # Yields to the block the compressor command and filename extension.
      def function
        Backup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"))
        super
      end;Fi[	@ˆ@Ò$i[[	@RiI"Årequire("testing_env")
require("formula")
require("test/testball")
require("keg")
class TestScriptFileFormula < ScriptFileFormula;Fi[	@iI"Wrequire("testing_env")
require("test/testball")
require("keg")
require("stringio");Fi[	I"@best ||= false;FI"@fast ||= false;Fi[[	@äiI"ä      def function(var)
        load_defaults!
        @best ||= false
        @fast ||= false
        instance_eval(&var) if block_given?;Fi[	@çiI"|      def function(var)
        load_defaults!
        @best ||= false
        @fast ||= false
        @processors ||= false;Fi[	@ö7@ri[[	@ÙiI"q      assert_equal(case MacOS.cat
      when "LITERAL" then
        var.bottle
      else
        var.stable;Fi[	@Ùi"I"q      assert_equal(case MacOS.cat
      when "LITERAL" then
        var.bottle
      else
        var.stable;Fi[	I"var.stable;FI"end, var.active_spec);Fi[[	@ÙiI"x        var.bottle
      else
        var.stable
      end, var.active_spec)
      var = LionBottleSpecTestBall.new;Fi[	@Ùi$I"{        var.bottle
      else
        var.stable
      end, var.active_spec)
      var = AllCatsBottleSpecTestBall.new;Fi[	I")@stable = SoftwareSpec.new("STRING");F@"i[[	@˚iI"±    var = (HOMEBREW_CACHE.parent + "STRING")
    Kernel.system("STRING", "STRING", "STRING", var, ABS__FILE__)
    @stable = SoftwareSpec.new("STRING")
    super("STRING")
  end;Fi[	@%i5I"{class TestCompilerFailures < Formula
  def function(var)
    @stable = SoftwareSpec.new("STRING")
    super("STRING")
  end;Fi[	I"6# faster (-1) rather than better when set to true;F@ei[[	@äiI"∑      ##
      # Tells Backup::Compressor::Lzma to compress
      # faster (-1) rather than better when set to true
      ##
      # Creates a new instance of Backup::Compressor::Lzma;Fi[	@çiI"¡      ##
      # Tells Backup::Compressor::Lzma to compress
      # faster (-1) rather than better when set to true
      ##
      # Tells Backup::Compressor::Pbzip2 how many processors to use.;Fi[	I"
# end;F@Ói[[	@˚i=I"y  #   ARGV.named.each{|f| n+=1 if f == 'foo'}
  #   assert_equal 1, n
  # end
  def function
    require("cmd/info");Fi[	@Ii˚I".  #   assert_version_detected '7.0.4', 'http://www.haskell.org/ghc/dist/7.0.4/ghc-7.0.4-x86_64-apple-darwin.tar.bz2'
  #   assert_version_detected '7.0.4', 'http://www.haskell.org/ghc/dist/7.0.4/ghc-7.0.4-i386-apple-darwin.tar.bz2'
  # end
  def function
    assert_version_detected("STRING", "STRING");Fi[	I"6# better (-9) rather than faster when set to true;F@ei[[	@äiI"Ø      ##
      # Tells Backup::Compressor::Lzma to compress
      # better (-9) rather than faster when set to true
      ##
      # Tells Backup::Compressor::Lzma to compress;Fi[	@çiI"Ø      ##
      # Tells Backup::Compressor::Lzma to compress
      # better (-9) rather than faster when set to true
      ##
      # Tells Backup::Compressor::Lzma to compress;Fi[	@CI"/var.stable.instance_eval { md5("STRING") };Fi[[	@˛iI"s  
  def function
    var = TestBall.new
    var.stable.instance_eval { md5("STRING") }
    good_checksum(var);Fi[	@˛iI"r  
  def function
    var = TestBall.new
    var.stable.instance_eval { md5("STRING") }
    bad_checksum(var);Fi[	@CI"0var.stable.instance_eval { sha1("STRING") };Fi[[	@˛iI"t  
  def function
    var = TestBall.new
    var.stable.instance_eval { sha1("STRING") }
    good_checksum(var);Fi[	@˛i#I"s  
  def function
    var = TestBall.new
    var.stable.instance_eval { sha1("STRING") }
    bad_checksum(var);Fi[	@CI"2var.stable.instance_eval { sha256("STRING") };Fi[[	@˛i)I"v  
  def function
    var = TestBall.new
    var.stable.instance_eval { sha256("STRING") }
    good_checksum(var);Fi[	@˛i/I"u  
  def function
    var = TestBall.new
    var.stable.instance_eval { sha256("STRING") }
    bad_checksum(var);Fi[	@»'@•>i[[	@äiI"¥      # better (-9) rather than faster when set to true
      ##
      # Tells Backup::Compressor::Lzma to compress
      # faster (-1) rather than better when set to true
      ##;Fi[	@çiI"¥      # better (-9) rather than faster when set to true
      ##
      # Tells Backup::Compressor::Lzma to compress
      # faster (-1) rather than better when set to true
      ##;Fi[	@[*@Ci[[	@˚iOI"Í    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") }
    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") };Fi[	@˚iRI"Í    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") }
    var.active_spec.instance_eval { @version = Version.new("STRING") }
    var = TestBall.new
    var.instance_eval { @version = Version.new("STRING") };Fi[	I"var.brew { var.install };F@€>i[[	@˚iWI"≥    var.active_spec.instance_eval { @version = Version.new("STRING") }
    nostdout do
      var.brew { var.install }
      var.brew { var.install }
      var.brew { var.install };Fi[	@˚iXI"y    nostdout do
      var.brew { var.install }
      var.brew { var.install }
      var.brew { var.install }
    end;Fi[	I"assert(var.installed?);F@‚>i[[	@˚i[I"|      var.brew { var.install }
    end
    assert(var.installed?)
    assert(var.installed?)
    assert(var.installed?);Fi[	@˚i\I"á    end
    assert(var.installed?)
    assert(var.installed?)
    assert(var.installed?)
    nostdout { Homebrew.cleanup_formula(var) };Fi[	@»'@≥>i[[	@äiI"É      
      ##
      # Tells Backup::Compressor::Lzma to compress
      # better (-9) rather than faster when set to true
      ##;Fi[	@çiI"É      
      ##
      # Tells Backup::Compressor::Lzma to compress
      # better (-9) rather than faster when set to true
      ##;Fi[	@[=I"%assert_equal("STRING", var.stem);Fi[[	@˚iàI"±  def function
    var = (HOMEBREW_CACHE / "STRING")
    assert_equal("STRING", var.extname)
    assert_equal("STRING", var.stem)
    assert_version_equal("STRING", var.version);Fi[	@˚iåI"“    assert_version_equal("STRING", var.version)
    var = (HOMEBREW_CACHE / "STRING")
    assert_equal("STRING", var.extname)
    assert_equal("STRING", var.stem)
    assert_version_equal("STRING", var.version);Fi[	@Ô>@ª%i[[	@˚iâI"»    var = (HOMEBREW_CACHE / "STRING")
    assert_equal("STRING", var.extname)
    assert_equal("STRING", var.stem)
    assert_version_equal("STRING", var.version)
    var = (HOMEBREW_CACHE / "STRING");Fi[	@˚içI"®    var = (HOMEBREW_CACHE / "STRING")
    assert_equal("STRING", var.extname)
    assert_equal("STRING", var.stem)
    assert_version_equal("STRING", var.version)
  end;Fi[	@ª%@`%i[[	@ni}I"Ÿ    var = SpecTestBall.new
    assert_equal(var.devel, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_equal("STRING", var.url)
    assert_equal(CurlDownloadStrategy, var.download_strategy);Fi[	@niÉI"◊    var = SpecTestBall.new
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy);Fi[	I"@level ||= false;F@{i[[	@ÅiI"ã      def function(var)
        load_defaults!
        @level ||= false
        instance_eval(&var) if block_given?
        @cmd = "STRING";Fi[	@áiI"ã      def function(var)
        load_defaults!
        @level ||= false
        instance_eval(&var) if block_given?
        @cmd = "STRING";Fi[	I"cp("STRING", lib);F@	?i[[	I"1projects//homebrew_proj/test/test_cleaner.rb;TiI"u      lib.mkpath
      cp("STRING", bin)
      cp("STRING", lib)
      cp("STRING", lib)
      cp("STRING", lib);Fi[	@?iI"l      cp("STRING", bin)
      cp("STRING", lib)
      cp("STRING", lib)
      cp("STRING", lib)
    end;Fi[	I"<assert_equal("LITERAL", (var.lib / "STRING").stat.mode);F@?i[[	@?iI"    assert_nothing_raised { Cleaner.new(var) }
    assert_equal("LITERAL", (var.bin / "STRING").stat.mode)
    assert_equal("LITERAL", (var.lib / "STRING").stat.mode)
    assert_equal("LITERAL", (var.lib / "STRING").stat.mode)
    assert_equal("LITERAL", (var.lib / "STRING").stat.mode);Fi[	@?iI"ı    assert_equal("LITERAL", (var.bin / "STRING").stat.mode)
    assert_equal("LITERAL", (var.lib / "STRING").stat.mode)
    assert_equal("LITERAL", (var.lib / "STRING").stat.mode)
    assert_equal("LITERAL", (var.lib / "STRING").stat.mode)
  end;Fi[	@ó-I"(assert_equal(@set.count, "LITERAL");Fi[[	@ö-iI"î  def function
    (@set << X11Dependency.new)
    (@set << X11Dependency.new)
    assert_equal(@set.count, "LITERAL")
    (@set << Requirement.new);Fi[	@ö-iI"ª  def function
    (@set << X11Dependency.new("STRING"))
    (@set << X11Dependency.new)
    assert_equal(@set.count, "LITERAL")
    assert_equal(@set.to_a, [X11Dependency.new("STRING")]);Fi[	@&*@/*i[[	@6iI"¿    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    var.select_compiler
    assert_equal(case MacOS.clang_build_version;Fi[	@6ieI"«    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL").!)
    var.select_compiler
    assert_equal(MacOS.default_compiler, ENV.compiler);Fi[	@&*@&*i[[	@6i,I"≈    var = TestNoCompilerFailures.new
    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL").!)
    assert(case MacOS.gcc_42_build_version;Fi[	@6idI"¥    var = CompilerSelector.new(var)
    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL").!)
    var.select_compiler;Fi[	@Ì@à>i[[	@äiI"ô      # Creates a new instance of Backup::Compressor::Lzma
      def function(var)
        load_defaults!
        @best ||= false
        @fast ||= false;Fi[	@çiI"õ      # Creates a new instance of Backup::Compressor::Pbzip2
      def function(var)
        load_defaults!
        @best ||= false
        @fast ||= false;Fi[	@Ì@?i[[	@ÅiI"Ø      # Creates a new instance of Backup::Compressor::Bzip2
      def function(var)
        load_defaults!
        @level ||= false
        instance_eval(&var) if block_given?;Fi[	@áiI"Æ      # Creates a new instance of Backup::Compressor::Gzip
      def function(var)
        load_defaults!
        @level ||= false
        instance_eval(&var) if block_given?;Fi[	I"%# for the compressor being used.;F@—i[[	@ÅiI"Ÿ      # Note that setting the level to either extreme may or may not
      # give the desired result. Be sure to check the documentation
      # for the compressor being used.
      #
      # The default `level` is 9.;Fi[	@áiI"Ÿ      # Note that setting the level to either extreme may or may not
      # give the desired result. Be sure to check the documentation
      # for the compressor being used.
      #
      # The default `level` is 6.;Fi[	I"B# give the desired result. Be sure to check the documentation;F@7?i[[	@ÅiI"Ï      # Values should be a single digit from 1 to 9.
      # Note that setting the level to either extreme may or may not
      # give the desired result. Be sure to check the documentation
      # for the compressor being used.
      #;Fi[	@áiI"Ï      # Values should be a single digit from 1 to 9.
      # Note that setting the level to either extreme may or may not
      # give the desired result. Be sure to check the documentation
      # for the compressor being used.
      #;Fi[	@±;@Íi[[	@6iI"ò    assert(var.fails_with?("LITERAL").!)
    var.select_compiler
    assert_equal(case MacOS.clang_build_version
    when "LITERAL" then
      "LITERAL";Fi[	@6iXI"}    end)
    var.select_compiler
    assert_equal(case MacOS.clang_build_version
    when "LITERAL" then
      "LITERAL";Fi[	I"+assert(case MacOS.gcc_42_build_version;F@ª<i[[	@6i.I"Ø    assert(var.fails_with?("LITERAL").!)
    assert(var.fails_with?("LITERAL").!)
    assert(case MacOS.gcc_42_build_version
    when nil then
      var.fails_with?("LITERAL");Fi[	@6iQI"´    assert(var.fails_with?("LITERAL"))
    assert(var.fails_with?("LITERAL"))
    assert(case MacOS.gcc_42_build_version
    when nil then
      var.fails_with?("LITERAL");Fi[	@¬<@ri[[	@6i0I"â    assert(case MacOS.gcc_42_build_version
    when nil then
      var.fails_with?("LITERAL")
    else
      var.fails_with?("LITERAL").!;Fi[	@6iSI"â    assert(case MacOS.gcc_42_build_version
    when nil then
      var.fails_with?("LITERAL")
    else
      var.fails_with?("LITERAL").!;Fi[	I"!var.fails_with?("LITERAL").!;F@i[[	@6i2I"r      var.fails_with?("LITERAL")
    else
      var.fails_with?("LITERAL").!
    end)
    var.select_compiler;Fi[	@6iUI"r      var.fails_with?("LITERAL")
    else
      var.fails_with?("LITERAL").!
    end)
    var.select_compiler;Fi[	I".(@deps << Dependency.new(var["LITERAL"]));F@_?i[[	@0$iI"Í  def function
    var = { "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => "STRING" }
    (@deps << Dependency.new(var["LITERAL"]))
    (@deps << Dependency.new(var["LITERAL"]))
    (@deps << Dependency.new(var["LITERAL"]));Fi[	@0$iI"%    var = { "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => "STRING" }
    (@deps << Dependency.new(var["LITERAL"]))
    (@deps << Dependency.new(var["LITERAL"]))
    (@deps << Dependency.new(var["LITERAL"]))
    @deps.each_with_index { |var, var| assert_equal(var[var], var.name) };Fi[	I"#var = Dependency.new("STRING");F@f?i[[	@3$i-I"  
  def function
    var = Dependency.new("STRING")
    var = Dependency.new("STRING")
    var = Dependency.new("STRING");Fi[	@3$i.I"í  def function
    var = Dependency.new("STRING")
    var = Dependency.new("STRING")
    var = Dependency.new("STRING")
    assert_equal(var, var);Fi[	I""var = DependencyCollector.new;FI"2var.each { |var, var| var.add(var => (var)) };Fi[[	@6$iTI"«class ExternalDepsTests < Test::Unit::TestCase
  def function(var)
    var = DependencyCollector.new
    var.each { |var, var| var.add(var => (var)) }
    assert((var.requirements.size == "LITERAL"));Fi[	@6$i[I"õ  
  def function(var)
    var = DependencyCollector.new
    var.each { |var, var| var.add(var => (var)) }
    assert((var.requirements.size == "LITERAL"));Fi[	@n?I"1assert((var.requirements.size == "LITERAL"));Fi[[	@6$iUI"–  def function(var)
    var = DependencyCollector.new
    var.each { |var, var| var.add(var => (var)) }
    assert((var.requirements.size == "LITERAL"))
    var.requirements { |var| assert(var.satisfied?.!) };Fi[	@6$i\I"Œ  def function(var)
    var = DependencyCollector.new
    var.each { |var, var| var.add(var => (var)) }
    assert((var.requirements.size == "LITERAL"))
    var.requirements { |var| assert(var.satisfied?) };Fi[	I"C# Note that setting the level to either extreme may or may not;F@>?i[[	@ÅiI"Ï      #
      # Values should be a single digit from 1 to 9.
      # Note that setting the level to either extreme may or may not
      # give the desired result. Be sure to check the documentation
      # for the compressor being used.;Fi[	@áiI"Ï      #
      # Values should be a single digit from 1 to 9.
      # Note that setting the level to either extreme may or may not
      # give the desired result. Be sure to check the documentation
      # for the compressor being used.;Fi[	I"3# Values should be a single digit from 1 to 9.;F@|?i[[	@ÅiI"ˆ      # Specify the level of compression to use.
      #
      # Values should be a single digit from 1 to 9.
      # Note that setting the level to either extreme may or may not
      # give the desired result. Be sure to check the documentation;Fi[	@áiI"ˆ      # Specify the level of compression to use.
      #
      # Values should be a single digit from 1 to 9.
      # Note that setting the level to either extreme may or may not
      # give the desired result. Be sure to check the documentation;Fi[	I">assert_equal(CurlDownloadStrategy, var.download_strategy);FI"=assert_instance_of(CurlDownloadStrategy, var.downloader);Fi[[	@ni=I"	    assert_version_equal("STRING", var.version)
    assert_equal(var.stable, var.active_spec)
    assert_equal(CurlDownloadStrategy, var.download_strategy)
    assert_instance_of(CurlDownloadStrategy, var.downloader)
    assert_instance_of(SoftwareSpec, var.stable);Fi[	@niI"Ë    assert_version_equal("STRING", var.version)
    assert_equal("STRING", var.url)
    assert_equal(CurlDownloadStrategy, var.download_strategy)
    assert_instance_of(CurlDownloadStrategy, var.downloader)
    ARGV.delete("STRING");Fi[	I"/# Specify the level of compression to use.;F@—i[[	@ÅiI"}      
      ##
      # Specify the level of compression to use.
      #
      # Values should be a single digit from 1 to 9.;Fi[	@áiI"}      
      ##
      # Specify the level of compression to use.
      #
      # Values should be a single digit from 1 to 9.;Fi[	I"assert_nil(var.bottle.md5);FI""assert_nil(var.bottle.sha256);Fi[[	@nigI"ü    assert_nil(var.stable.md5)
    assert_nil(var.stable.sha256)
    assert_nil(var.bottle.md5)
    assert_nil(var.bottle.sha256)
    assert_nil(var.devel.md5);Fi[	@ni†I"€      assert(var.bottle.checksum.empty?.!)
      assert_equal("STRING", var.bottle.sha1.hexdigest)
      assert_nil(var.bottle.md5)
      assert_nil(var.bottle.sha256)
      assert(var.bottle.version.detected_from_url?);Fi[	I"ARGV.push("STRING");FI"var = SpecTestBall.new;Fi[[	@nizI"v  
  def function
    ARGV.push("STRING")
    var = SpecTestBall.new
    assert_equal(var.devel, var.active_spec);Fi[	@niÄI"u  
  def function
    ARGV.push("STRING")
    var = SpecTestBall.new
    assert_equal(var.head, var.active_spec);Fi[	@i@a!i[[	@„i.I"}      unless var["STRING"] then
        trap("STRING") do
          puts("STRING")
          exit("LITERAL")
        end;Fi[	@‡i*I"          puts(Whenever.cron(@options))
          puts("STRING")
          puts("STRING")
          exit("LITERAL")
        end;Fi[	@iI"+var.backtrace.each { |var| puts(var) };Fi[[	@Â	iFI"ø        self.content = Liquid::Template.parse(self.content).render!(var, var)
      rescue => var
        puts("STRING")
        var.backtrace.each { |var| puts(var) }
        abort("STRING");Fi[	@Â	iSI"«          self.output = Liquid::Template.parse(var.content).render!(var, var)
        rescue => var
          puts("STRING")
          var.backtrace.each { |var| puts(var) }
          abort("STRING");Fi[	@i@w=i[[	@$iI"Ü    end
    if @clone.exist? then
      puts("STRING")
      Dir.chdir(@clone) do
        safe_system(@@git, "STRING", "STRING", @url);Fi[	@$iîI"Ç    ohai("STRING")
    if @clone.exist? then
      puts("STRING")
      Dir.chdir(@clone) do
        safe_system(hgpath, "STRING");Fi[	@i@Ó;i[[	@áilI"ì    puts("STRING") if HOMEBREW_CELLAR.to_s.!=("STRING")
    puts(hardware)
    puts("STRING")
    puts("STRING") if xcode
    puts("STRING") if clt;Fi[	@áiÜI"s    puts("STRING")
    puts(hardware)
    puts("STRING")
    puts("STRING") if xcode
    puts("STRING") if clt;Fi[	@i@Ó i[[	@1i›I"ü          (var = ::Haml::Engine.new(var, @options["LITERAL"])
          if @options["LITERAL"] then
            puts("STRING")
            return
          end;Fi[	@√iˆI"b        puts("STRING")
        puts("STRING")
        puts("STRING")
        return
      end;Fi[	@i@Ö3i[[	@:iI"c      if var.linked? then
        opoo("STRING")
        puts("STRING")
        next
      end;Fi[	@Êi-I"j          puts("STRING")
          puts("STRING")
          puts("STRING")
          next
        end;Fi[	@W
@Òi[[	@3i5I"~      def function(var)
        case Resque.redis.type(var)
        when "STRING" then
          []
        when "STRING" then;Fi[	@3iGI"É      def function(var, var)
        case Resque.redis.type(var)
        when "STRING" then
          []
        when "STRING" then;Fi[	I"+var = IncompleteStableSpecTestBall.new;F@∫)i[[	@niøI"y  
  def function
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable);Fi[	@niŒI"y  
  def function
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable);Fi[	@W
I"arrparse(var);Fi[[	@µi#I"m      when "STRING" then
        objparse(var)
      when "STRING" then
        arrparse(var)
      else;Fi[	@µi2I"Ç      when "STRING" then
        objparse(var)
      when "STRING" then
        arrparse(var)
      when "LITERAL", "LITERAL" then;Fi[	@W
I"objparse(var);Fi[[	@µi!I"{      var, var, var = var["LITERAL"]
      case var
      when "STRING" then
        objparse(var)
      when "STRING" then;Fi[	@µi0I"{      var, var, var = var["LITERAL"]
      case var
      when "STRING" then
        objparse(var)
      when "STRING" then;Fi[	I"var.dry_run = true;FI",assert_equal("LITERAL", @keg.link(var));Fi[[	@iI"Ö  def function
    var = OpenStruct.new
    var.dry_run = true
    assert_equal("LITERAL", @keg.link(var))
    assert(@keg.linked?.!);Fi[	@i=I"è    var = OpenStruct.new
    var.overwrite = true
    var.dry_run = true
    assert_equal("LITERAL", @keg.link(var))
    assert(@keg.linked?.!);Fi[	@Í?I"assert(@keg.linked?.!);Fi[[	@i I"ß    var = OpenStruct.new
    var.dry_run = true
    assert_equal("LITERAL", @keg.link(var))
    assert(@keg.linked?.!)
    ["STRING", "STRING", "STRING"].each do |var|;Fi[	@i>I"°    var.overwrite = true
    var.dry_run = true
    assert_equal("LITERAL", @keg.link(var))
    assert(@keg.linked?.!)
    assert_equal("STRING", $stdout.string);Fi[	@l-@ó<i[[	@i3I"z  
  def function
    FileUtils.touch((HOMEBREW_PREFIX / "STRING"))
    var = OpenStruct.new
    var.overwrite = true;Fi[	@i:I"z  
  def function
    FileUtils.touch((HOMEBREW_PREFIX / "STRING"))
    var = OpenStruct.new
    var.overwrite = true;Fi[	@W
I"break if var;Fi[[	@;iˇI"î      while var do
        case var["LITERAL"]
        when "STRING" then
          break if var
          var, var, var = parse_old_attributes(var);Fi[	@;iI"∏          var, var, var = parse_old_attributes(var)
          var["LITERAL"] = var
        when "STRING" then
          break if var
          var, var, var = parse_new_attributes(var);Fi[	@W
@8i[[	@Òi[I"m  def function
    case self.service
    when "STRING" then
      self.identifier
    when "STRING" then;Fi[	@ÒigI"m  def function
    case self.service
    when "STRING" then
      self.identifier
    when "STRING" then;Fi[	@W
@&i[[	@‡i¢I"ä        when "STRING" then
          { "LITERAL" => (@address), "LITERAL" => (@port), "LITERAL" => (@domain), "LITERAL" => (@user_name), "LITERAL" => (@password), "LITERAL" => (@authentication), "LITERAL" => (@enable_starttls_auto), "LITERAL" => (@openssl_verify_mode) }
        when "STRING" then
          var = {}
          var.merge!("LITERAL" => (File.expand_path(@sendmail))) if @sendmail;Fi[	@‡ißI"«          var.merge!("LITERAL" => (@sendmail_args)) if @sendmail_args
          var
        when "STRING" then
          var = {}
          var.merge!("LITERAL" => (File.expand_path(@exim))) if @exim;Fi[	@W
@…&i[[	@;iII"°      var = ((var == "STRING") or (var.!=("STRING") and @options["LITERAL"]))
      case var
      when "STRING" then
        var = true
      when "STRING" then;Fi[	@;iMI"ä      when "STRING" then
        var = var = true
      when "STRING" then
        var = true
        if (var["LITERAL"] == "STRING") then;Fi[	@JI"assert(var.x86_64?);Fi[[	@Êi)I"à    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!);Fi[	@ÊiaI"à    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!);Fi[	I"if overwrite?(var) then;FI"&File.open(var, "STRING") do |var|;Fi[[	@liTI"‚        var = File.join(var, "STRING")
        FileUtils.mkdir_p(var)
        if overwrite?(var) then
          File.open(var, "STRING") do |var|
            var.write(Backup::Template.new("LITERAL" => (var)).result("STRING"));Fi[	@linI"Œ        var = File.join(var, "STRING")
        FileUtils.mkdir_p(var)
        if overwrite?(var) then
          File.open(var, "STRING") do |var|
            var.write(Backup::Template.new.result("STRING"));Fi[	I"#var = File.join(var, "STRING");F@&@i[[	@liPI"T        var = options.merge("LITERAL" => (options["LITERAL"].gsub("LITERAL", "STRING")), "LITERAL" => (options["LITERAL"] ? (File.expand_path(options["LITERAL"])) : (nil)))
        var = (var["LITERAL"] or Config.root_path)
        var = File.join(var, "STRING")
        var = File.join(var, "STRING")
        var = File.join(var, "STRING");Fi[	@liQI"∆        var = (var["LITERAL"] or Config.root_path)
        var = File.join(var, "STRING")
        var = File.join(var, "STRING")
        var = File.join(var, "STRING")
        FileUtils.mkdir_p(var);Fi[	@&@I"FileUtils.mkdir_p(var);Fi[[	@liRI"≥        var = File.join(var, "STRING")
        var = File.join(var, "STRING")
        var = File.join(var, "STRING")
        FileUtils.mkdir_p(var)
        if overwrite?(var) then;Fi[	@lilI"å          Config.root_path
        end
        var = File.join(var, "STRING")
        FileUtils.mkdir_p(var)
        if overwrite?(var) then;Fi[	@ß(@µ3i[[	@#iI"¥    # waiting for a new compass release.
    def function(var, var)
      var.each do |var|
        unless ASPECTS.include?(var) then
          Compass::Util.compass_warn("STRING");Fi[	@#i/I"≈    # a prefix without waiting for a new compass release.
    def function(var, var)
      var.each do |var|
        unless ASPECTS.include?(var) then
          Compass::Util.compass_warn("STRING");Fi[	@î(@ô4i[[	@iI"¬      begin
        return perform!
      rescue Exception => var
        raise(var) if var.is_a?(SystemExit)
        if (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then;Fi[	@iI"ª      begin
        perform!
      rescue Exception => var
        raise(var) if var.is_a?(SystemExit)
        if (var.is_a?(::Compass::Error) or var.is_a?(OptionParser::ParseError)) then;Fi[	@@/*i[[	@6i3I"É    else
      var.fails_with?("LITERAL").!
    end)
    var.select_compiler
    assert_equal(MacOS.default_compiler, ENV.compiler);Fi[	@6iVI"|    else
      var.fails_with?("LITERAL").!
    end)
    var.select_compiler
    assert_equal(case MacOS.clang_build_version;Fi[	@ÄI"!assert(var.text_executable?);Fi[[	@ÊiìI"ó    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs);Fi[	@Êi£I"ó    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs);Fi[	@@]i[[	@*i I"∞          @config["STRING"]["STRING"].each do |var|
            @redcarpet_extensions[var.to_sym] = true
          end)
        rescue LoadError
          STDERR.puts("STRING");Fi[	@*iGI"—            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"]
            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"]
          end)
        rescue LoadError
          STDERR.puts("STRING");Fi[	@ó;I"exit!("LITERAL");Fi[[	@Çi%I"|      var.continuation = nil if ARGV.debug?
      Marshal.dump(var, var)
      var.close
      exit!("LITERAL")
    end;Fi[	@√i±I"{        rescue Exception => var
          Marshal.dump(var, var)
          var.close
          exit!("LITERAL")
        end;Fi[	@V(I"*assert_no_match("LITERAL", file(var));Fi[[	@ÊiwI"ª    assert(var.text_executable?.!)
    assert(var.mach_o_bundle?.!)
    assert((var.arch == "LITERAL"))
    assert_no_match("LITERAL", file(var))
    assert_no_match("LITERAL", file(var));Fi[	@Êi∂I"ñ    assert(var.text_executable?.!)
    assert_equal([], var.archs)
    assert((var.arch == "LITERAL"))
    assert_no_match("LITERAL", file(var))
  end;Fi[	@ÔI"#post_retweet(var, var.options);Fi[[	@ßiëI"≈        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin
            post_retweet(var, var.options)
          rescue Twitter::Error::Forbidden => var;Fi[	@ßi¨I"≈        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin
            post_retweet(var, var.options)
          rescue Twitter::Error::Forbidden => var;Fi[	@ I"assert(var.i386?);Fi[[	@ÊiI"|  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?)
    assert(var.x86_64?.!);Fi[	@ÊiRI"|  def function
    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?)
    assert(var.x86_64?.!);Fi[	@h@@i[[	@ÊiI"à    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@ÊiSI"à    var = Pathname.new("STRING")
    assert(var.universal?.!)
    assert(var.i386?)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!);Fi[	@@@‚i[[	@Êi*I"Ä    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÊibI"Ä    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!);Fi[	@ÔI"eobject_from_response(Twitter::Tweet, "LITERAL", "STRING", var.options.merge("LITERAL" => (var)));Fi[[	@)iYI"        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin
            object_from_response(Twitter::Tweet, "LITERAL", "STRING", var.options.merge("LITERAL" => (var)))
          rescue Twitter::Error::Forbidden => var;Fi[	@)i{I"        var = Twitter::API::Arguments.new(var)
        var.flatten.threaded_map do |var|
          begin
            object_from_response(Twitter::Tweet, "LITERAL", "STRING", var.options.merge("LITERAL" => (var)))
          rescue Twitter::Error::Forbidden => var;Fi[	@ÔI"(var = nil;Fi[[	@JiI"F  
  def function
    begin
      (var = nil
      require("keg");Fi[	@RiI"R  
  def function
    begin
      (var = nil
      unless writable_real? then;Fi[	@Ô@Ÿ4i[[	@i I"w    
    def function
      begin
        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL"));Fi[	@i+I"w    
    def function
      begin
        ($stderr.write(color("LITERAL"))
        $stdout.write(color("LITERAL"));Fi[	@F@@U(i[[	@ÊiîI"¢    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs)
    assert((var.arch == "LITERAL"));Fi[	@Êi§I"¢    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs)
    assert((var.arch == "LITERAL"));Fi[	@Ô@Y4i[[	@i I"-begin
  require("sass")
rescue LoadError;Fi [	@i	I"Zrescue LoadError
  require("rubygems")
  begin
    require("sass")
  rescue LoadError;Fi[	@(@F;i[[	@i2I"     var = { "LITERAL" => "STRING", "LITERAL" => "STRING" }
    var = Patches.new(var)
    assert_equal("LITERAL", var.patches.length)
    var = Set.new
    var.each { |var| (var << var.patch_filename) };Fi[	@i;I"Ê    var = { "LITERAL" => (["STRING", "STRING"]), "LITERAL" => (["STRING", "STRING"]) }
    var = Patches.new(var)
    assert_equal("LITERAL", var.patches.length)
    var = Set.new
    var.each { |var| (var << var.patch_filename) };Fi[	@Ô@ì3i[[	@i I"π        
        def function
          begin
            (var, Compass.configuration.project_path = Compass.configuration.project_path, nil
            Compass.configuration.serialize);Fi[	@iI"Ø      
      def function
        begin
          (var, Compass.configuration.project_path = Compass.configuration.project_path, nil
          Compass.configuration.serialize);Fi[	@ÔI"(write_password_file!;Fi[[	@iI"ã        # debug options: -vhP
        def function
          begin
            (write_password_file!
            @directories.each do |var|;Fi[	@i/I"©        # debug options: -vhP
        def function
          begin
            (write_password_file!
            Logger.message(("STRING" + @directories.join("STRING")));Fi[	I"-var = Patches.new("LITERAL" => "STRING");F@(i[[	@i"I"è  
  def function
    var = Patches.new("LITERAL" => "STRING")
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"];Fi[	@i)I"è  
  def function
    var = Patches.new("LITERAL" => "STRING")
    assert_equal("LITERAL", var.patches.length)
    var = var.patches["LITERAL"];Fi[	I"var = Patches.new(var);F@(i[[	@i1I"¶  def function
    var = { "LITERAL" => "STRING", "LITERAL" => "STRING" }
    var = Patches.new(var)
    assert_equal("LITERAL", var.patches.length)
    var = Set.new;Fi[	@i:I"¬  def function
    var = { "LITERAL" => (["STRING", "STRING"]), "LITERAL" => (["STRING", "STRING"]) }
    var = Patches.new(var)
    assert_equal("LITERAL", var.patches.length)
    var = Set.new;Fi[	@Ô@Œ8i[[	@»iBI"r          fast_fit
          var = []
          begin
            var = false
            catch("LITERAL") do;Fi[	@ºi_I"R  puts("STRING")
  puts("STRING")
  begin
    var = false
    choose do |var|;Fi[	@'@€$i[[	@*i3I"g  
  def function
    with_temp_folder do
      FileUtils.mkdir_p("STRING")
      system("STRING");Fi[	@*iPI"g  
  def function
    with_temp_folder do
      FileUtils.mkdir_p("STRING")
      system("STRING");Fi[	I"$TARGET_FOLDER.install("STRING");F@’i[[	@*iI"¬  def function
    with_temp_folder do
      TARGET_FOLDER.install("STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING");Fi[	@*i6I"“      FileUtils.mkdir_p("STRING")
      system("STRING")
      TARGET_FOLDER.install("STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING");Fi[	@Ô@Ôi[[	@ºiQI"\def function
  if can_use_readline? then
    begin
    begin
      (require("rubygems");Fi[	@Úi	I"var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL")
unless (::ActiveRecord::VERSION::MAJOR == "LITERAL") and ((::ActiveRecord::VERSION::MINOR < "LITERAL") or (::ActiveRecord::VERSION::TINY < "LITERAL")) then
  begin
  begin
    require("i18n/version");Fi[	@—I"s# @param size [String, Symbol] The size of the image. Must be one of: 'mini', 'normal', 'bigger' or 'original';Fi[[	@,'iGI"≈    
    # Return the URL to the user's profile image
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mini', 'normal', 'bigger' or 'original'
    # @return [String];Fi[	@,'iOI"Ã    
    # Return the secure URL to the user's profile image
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mini', 'normal', 'bigger' or 'original'
    # @return [String];Fi[	@€$@ëi[[	I"2projects//jekyll_proj/jekyll/migrators/csv.rb;TiI"Ñ  module CSV
    def self.process(var)
      FileUtils.mkdir_p("STRING")
      var = "LITERAL"
      FasterCSV.foreach(var) do |var|;Fi[	@o*iI"¥    def self.process(var)
      raise(ArgumentError, "STRING") unless File.directory?(var)
      FileUtils.mkdir_p("STRING")
      var = "LITERAL"
      Dir["STRING"].each do |var|;Fi[	@€$I"system("STRING");Fi[[	@*i4I"Ö  def function
    with_temp_folder do
      FileUtils.mkdir_p("STRING")
      system("STRING")
      TARGET_FOLDER.install("STRING");Fi[	@*iQI"ë  def function
    with_temp_folder do
      FileUtils.mkdir_p("STRING")
      system("STRING")
      TARGET_FOLDER.install("STRING" => "STRING");Fi[	@€$@⁄$i[[	@iI"›    
    def self.process(var, var, var, var)
      FileUtils.mkdir_p("STRING")
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      var[SQL].each do |var|;Fi[	@TiI"√        end
      end
      FileUtils.mkdir_p("STRING")
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING")
      var = var["LITERAL"];Fi[	@—I"é# @param size [String, Symbol] The size of the image. Must be one of: 'mobile', 'mobile_retina', 'web', 'web_retina', 'ipad', or 'ipad_retina';Fi[[	@,'i/I"Ï    
    # Return the URL to the user's profile banner image
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mobile', 'mobile_retina', 'web', 'web_retina', 'ipad', or 'ipad_retina'
    # @return [String];Fi[	@,'i9I"Û    
    # Return the secure URL to the user's profile banner image
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mobile', 'mobile_retina', 'web', 'web_retina', 'ipad', or 'ipad_retina'
    # @return [String];Fi[	@z(@z(i[[	@*i@I"Î      TARGET_FOLDER.install("STRING" => "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
    end;Fi[	@*iJI"Û      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
      assert((TARGET_FOLDER + "STRING").exist?.!, "STRING")
    end;Fi[	I"0ENV.expects("LITERAL").yields.returns(true);FI"ENV.expects("LITERAL");Fi[[	@9$i0I"Ä  
  def function
    ENV.expects("LITERAL").yields.returns(true)
    ENV.expects("LITERAL")
    var = Class.new(Requirement) do;Fi[	@9$i:I"Ä  
  def function
    ENV.expects("LITERAL").yields.returns(true)
    ENV.expects("LITERAL")
    var = Class.new(Requirement) do;Fi[	@˛@I"$var = Class.new(Requirement) do;Fi[[	@9$i1I"í  def function
    ENV.expects("LITERAL").yields.returns(true)
    ENV.expects("LITERAL")
    var = Class.new(Requirement) do
      env("LITERAL");Fi[	@9$i;I"æ  def function
    ENV.expects("LITERAL").yields.returns(true)
    ENV.expects("LITERAL")
    var = Class.new(Requirement) do
      satisfy("LITERAL" => (true), "LITERAL" => (true)) { true };Fi[	@—I"?# @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids;Fi[[	@≈iúI"∫      
      # Returns an array of numeric user ids the authenticating user is blocking
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids
      # @rate_limited Yes;Fi[	@≈i≠I"≥      
      # Returns true if the authenticating user is blocking a target user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids
      # @rate_limited Yes;Fi[	@—I"D# @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed;Fi[[	@ßiÃI"ë      
      # Returns oEmbed for a Tweet
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed
      # @rate_limited Yes;Fi[	@ßi‚I"ë      
      # Returns oEmbeds for Tweets
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed
      # @rate_limited Yes;Fi[	@—I"J# @see https://dev.twitter.com/docs/api/1.1/post/statuses/retweet/:id;Fi[[	@ßiÄI"µ      
      # Retweets the specified Tweets as the authenticating user
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/retweet/:id
      # @rate_limited Yes;Fi[	@ßiöI"Î      
      # Retweets the specified Tweets as the authenticating user and raises an error if one has already been retweeted
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/retweet/:id
      # @rate_limited Yes;Fi[	@—I"F# @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id;Fi[[	@ßi6I"à      
      # Returns a Tweet
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id
      # @rate_limited Yes;Fi[	@ßiFI"á      
      # Returns Tweets
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id
      # @rate_limited Yes;Fi[	@ÛI"&var.in_repo_expect("STRING", var);Fi[[	@ëiII"™      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var)
      var.pull!;Fi[	@ëi]I"™      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var)
      var.pull!;Fi[	I"var = fixture("STRING");F@ß+i[[	@ëiBI"l  
  def function
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new;Fi[	@ëiVI"l  
  def function
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new;Fi[	@(A@Ù+i[[	@ëiJI"û      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var)
      var.pull!
      var = Report.new;Fi[	@ëi^I"û      var.in_repo_expect("STRING")
      var.in_repo_expect("STRING", "STRING")
      var.in_repo_expect("STRING", var)
      var.pull!
      var = Report.new;Fi[	I"1assert(var.select_formula("LITERAL").empty?);F@<Ai[[	@ëiOI"Ÿ      var.merge!(var.report)
      assert(var.expectations_met?)
      assert(var.select_formula("LITERAL").empty?)
      assert(var.select_formula("LITERAL").empty?)
      assert(var.select_formula("LITERAL").empty?);Fi[	@ëiPI"ƒ      assert(var.expectations_met?)
      assert(var.select_formula("LITERAL").empty?)
      assert(var.select_formula("LITERAL").empty?)
      assert(var.select_formula("LITERAL").empty?)
    end;Fi[	@A(@ri[[	@D(iI"ˆ    if `sw_vers -productVersion` =~ "LITERAL" and ($1.to_i >= "LITERAL") then
      assert_equal("LITERAL", var.length)
      assert(var.include?("LITERAL"))
    else
      if `sw_vers -productVersion` =~ "LITERAL" and ($1.to_i == "LITERAL") then;Fi[	@D(iI"≤        assert_equal("LITERAL", var.length)
        assert(var.include?("LITERAL"))
        assert(var.include?("LITERAL"))
      else
        assert_equal("LITERAL", var.length);Fi[	@—I"J# @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id;Fi[[	@ßiI"µ      
      # Returns up to 100 of the first retweets of a given tweet
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id
      # @rate_limited Yes;Fi[	@ßi"I"©      
      # Show up to 100 users who retweeted the Tweet
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id
      # @rate_limited Yes;Fi[	I" var = Version.new("STRING");FI"assert(var.devel?);Fi[[	@Ii6I"É    var = Version.new("STRING")
    assert(var.alpha?)
    var = Version.new("STRING")
    assert(var.devel?)
    assert(var.beta?);Fi[	@Ii9I"|    assert(var.devel?)
    assert(var.beta?)
    var = Version.new("STRING")
    assert(var.devel?)
    assert(var.rc?);Fi[	@—I"K# @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline;Fi[[	@ëilI"4      
      # Returns the 20 most recent Tweets, including retweets if they exist, posted by the authenticating user and the users they follow
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.;Fi[	@ëi~I"      
      # Returns the 20 most recent retweets posted by users the authenticating user follow.
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.;Fi[	I"8# name parameter required for some Formula::factory;F@õi[[	@%iI"érequire("formula")
class TestBall < Formula
  # name parameter required for some Formula::factory
  def function(var)
    @homepage = "STRING";Fi[	@%i*I"u  md5("STRING")
  
  # name parameter required for some Formula::factory
  def function(var)
    super("STRING");Fi[	I"devel do;F@‰(i[[	@%imI"a  head("STRING", "LITERAL" => "STRING")
  
  devel do
    url("STRING")
    mirror("STRING");Fi[	@%iÅI"K  version("STRING")
  
  devel do
    url("STRING")
    sha1("STRING");Fi[	@—I"N#   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id;Fi[[	@iI"      # @overload saved_search(*ids)
      #   Retrieve the data for saved searches owned by the authenticating user
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;Fi[	@i!I"       # @overload saved_search(*ids, options)
      #   Retrieve the data for saved searches owned by the authenticating user
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;Fi[	I"bottle do;F@äi[[	@%isI"Y  end
  
  bottle do
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL");Fi[	@%i˝I"d  sha1("STRING")
  
  bottle do
    sha1("STRING" => "LITERAL")
    sha1("STRING" => "LITERAL");Fi[	@—I"H# @see https://dev.twitter.com/docs/api/1.1/post/friendships/create;Fi[[	@0iwI"ÿ      
      # Allows the authenticating user to follow the specified users, unless they are already followed
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/create
      # @rate_limited No;Fi[	@0iãI"∂      
      # Allows the authenticating user to follow the specified users
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/create
      # @rate_limited No;Fi[	@—I"F# @see https://dev.twitter.com/docs/api/1.1/post/favorites/create;Fi[[	@)iII"±      
      # Favorites the specified Tweets as the authenticating user
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/create
      # @rate_limited No;Fi[	@)ijI"Á      
      # Favorites the specified Tweets as the authenticating user and raises an error if one has already been favorited
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/create
      # @rate_limited No;Fi[	I"/require("multi_json/adapters/json_common");F@âi[[	@êiI"yrequire("json") unless defined? ::JSON
require("multi_json/adapters/json_common")
module MultiJson
  module Adapters;Fi[	@ìiI"~require("json/pure") unless defined? ::JSON
require("multi_json/adapters/json_common")
module MultiJson
  module Adapters;Fi[	I"def self.load(var, var);FI"1var = var.read if var.respond_to?("LITERAL");Fi[[	@∞iI"¡      ParseError = ::MultiJson::OkJson::Error
      
      def self.load(var, var)
        var = var.read if var.respond_to?("LITERAL")
        var = var.dataUsingEncoding(NSUTF8StringEncoding);Fi[	@IiI"¿      ParseError = ::MultiJson::OkJson::Error
      
      def self.load(var, var)
        var = var.read if var.respond_to?("LITERAL")
        var = ::MultiJson::OkJson.decode("STRING").first;Fi[	@—I"K#   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show;Fi[[	@iWI"È      # @overload direct_messages(*ids)
      #   Returns direct messages
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;Fi[	@i^I"Ú      # @overload direct_messages(*ids, options)
      #   Returns direct messages
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;Fi[	I"7raise(Error, "STRING") if (var.length < "LITERAL");FI"#var, var, var = var["LITERAL"];Fi[[	@µiI"∂    # except that it does not accept atomic values.
    def function(var)
      raise(Error, "STRING") if (var.length < "LITERAL")
      var, var, var = var["LITERAL"]
      case var;Fi[	@µi-I"∫    # Returns the parsed value and any trailing tokens.
    def function(var)
      raise(Error, "STRING") if (var.length < "LITERAL")
      var, var, var = var["LITERAL"]
      case var;Fi[	@†A@4i[[	@µiI"õ    def function(var)
      raise(Error, "STRING") if (var.length < "LITERAL")
      var, var, var = var["LITERAL"]
      case var
      when "STRING" then;Fi[	@µi.I"õ    def function(var)
      raise(Error, "STRING") if (var.length < "LITERAL")
      var, var, var = var["LITERAL"]
      case var
      when "STRING" then;Fi[	@‚?@W
i[[	@µi"I"q      case var
      when "STRING" then
        objparse(var)
      when "STRING" then
        arrparse(var);Fi[	@µi1I"q      case var
      when "STRING" then
        objparse(var)
      when "STRING" then
        arrparse(var);Fi[	@r6I"var, var = valparse(var);Fi[[	@µiQI"¡      (var, var, var), var = var["LITERAL"], var["LITERAL"]
      raise(Error, "STRING") if var.!=("LITERAL")
      var = eat("STRING", var)
      var, var = valparse(var)
      [var, var, var];Fi[	@µi`I"≤      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      loop do
        var = eat("STRING", var)
        var, var = valparse(var)
        (var << var);Fi[	@1;@q6i[[	@µiCI"∂      var, var, var = pairparse(var)
      var[var] = var
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      loop do
        var = eat("STRING", var);Fi[	@µi^I"Æ      var, var = valparse(var)
      (var << var)
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      loop do
        var = eat("STRING", var);Fi[	I"#var, var, var = pairparse(var);F@0;i[[	@µiAI"‰      var = {}
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      var, var, var = pairparse(var)
      var[var] = var
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING");Fi[	@µiFI"º      loop do
        var = eat("STRING", var)
        var, var, var = pairparse(var)
        var[var] = var
        return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING");Fi[	@≥A@¯!i[[	@µi\I"‹      var = []
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      var, var = valparse(var)
      (var << var)
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING");Fi[	@µiaI"¥      loop do
        var = eat("STRING", var)
        var, var = valparse(var)
        (var << var)
        return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING");Fi[	@—I"Q# Once the callbacks are run, they cannot be run again until this transition;Fi[[	@›i`I"œ    # callbacks that are configured to match the event, from state, and to
    # state will be invoked.
    # 
    # Once the callbacks are run, they cannot be run again until this transition
    # is reset.;Fi[	@›i~I"œ    # callbacks that are configured to match the event, from state, and to
    # state will be invoked.
    # 
    # Once the callbacks are run, they cannot be run again until this transition
    # is reset.;Fi[	@—I""#   vehicle.state = 'invalid';Fi[[	@Yi6I"Â    #   vehicle.state = 'idling'
    #   states.match(vehicle)         # => #<StateMachine::State name=:idling value="idling" initial=true>
    #   
    #   vehicle.state = 'invalid'
    #   states.match(vehicle)         # => nil;Fi[	@YiNI"E    #   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">
    #   states.match!(vehicle)        # => #<StateMachine::State name=:parked value="parked" initial=true>
    #   
    #   vehicle.state = 'invalid'
    #   states.match!(vehicle)        # => ArgumentError: "invalid" is not a known state value;Fi[	@—I"Q#   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">;Fi[[	@Yi0I"˚    #   
    #   states = Vehicle.state_machine.states
    #   
    #   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">
    #   states.match(vehicle)         # => #<StateMachine::State name=:parked value="parked" initial=true>;Fi[	@YiKI"˚    #   
    #   states = Vehicle.state_machine.states
    #   
    #   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">
    #   states.match!(vehicle)        # => #<StateMachine::State name=:parked value="parked" initial=true>;Fi[	I"8var[(var + "LITERAL")] = Utagx.|(var.&(Umaskx)).chr;F@˚i[[	@µi#I"·      when (var <= Uchar2max) then
        var[(var + "LITERAL")] = Utag2.|((var >> "LITERAL").&("LITERAL")).chr
        var[(var + "LITERAL")] = Utagx.|(var.&(Umaskx)).chr
        "LITERAL"
      when (var <= Uchar3max) then;Fi[	@µi(I"Ò        var[(var + "LITERAL")] = Utag3.|((var >> "LITERAL").&("LITERAL")).chr
        var[(var + "LITERAL")] = Utagx.|((var >> "LITERAL").&(Umaskx)).chr
        var[(var + "LITERAL")] = Utagx.|(var.&(Umaskx)).chr
        "LITERAL"
      else;Fi[	I"Gvar[(var + "LITERAL")] = Utagx.|((var >> "LITERAL").&(Umaskx)).chr;F@‚Ai[[	@µi'I"	      when (var <= Uchar3max) then
        var[(var + "LITERAL")] = Utag3.|((var >> "LITERAL").&("LITERAL")).chr
        var[(var + "LITERAL")] = Utagx.|((var >> "LITERAL").&(Umaskx)).chr
        var[(var + "LITERAL")] = Utagx.|(var.&(Umaskx)).chr
        "LITERAL";Fi[	@µi-I"3        (var[(var + "LITERAL")] = Utag4.|((var >> "LITERAL").&("LITERAL")).chr
        var[(var + "LITERAL")] = Utagx.|((var >> "LITERAL").&(Umaskx)).chr
        var[(var + "LITERAL")] = Utagx.|((var >> "LITERAL").&(Umaskx)).chr
        var[(var + "LITERAL")] = Utagx.|(var.&(Umaskx)).chr
        "LITERAL");Fi[	I"8when (("STRING" <= var) and (var <= "STRING")) then;FI"+((var.ord - "STRING".ord) + "LITERAL");Fi[[	@µiGI"˝      when (("STRING" <= var) and (var <= "STRING")) then
        (var.ord - "STRING".ord)
      when (("STRING" <= var) and (var <= "STRING")) then
        ((var.ord - "STRING".ord) + "LITERAL")
      when (("STRING" <= var) and (var <= "STRING")) then;Fi[	@µiII"‹      when (("STRING" <= var) and (var <= "STRING")) then
        ((var.ord - "STRING".ord) + "LITERAL")
      when (("STRING" <= var) and (var <= "STRING")) then
        ((var.ord - "STRING".ord) + "LITERAL")
      else;Fi[	@@7@‘i[[	@µi\I"f      case var
      when Hash then
        objenc(var)
      when Array then
        arrenc(var);Fi[	@µigI"f      case var
      when Hash then
        objenc(var)
      when Array then
        arrenc(var);Fi[	@—I"B#   path.to_states  # => [:parked, :idling, :first_gear, ...];Fi[[	@oi,I"x    # 
    # For example,
    # 
    #   path.to_states  # => [:parked, :idling, :first_gear, ...]
    def function;Fi[	@oi;I"x    # 
    # For example,
    # 
    #   path.to_states  # => [:parked, :idling, :first_gear, ...]
    def function;Fi[	@—I"O# The above will look up the "parked" key in a hash indexed by each node's;Fi[[	@√iÉI"ö    # 
    #   collection['parked', :value]
    # 
    # The above will look up the "parked" key in a hash indexed by each node's
    # +value+ attribute.;Fi[	@√iëI"ö    # 
    #   collection['parked', :value]
    # 
    # The above will look up the "parked" key in a hash indexed by each node's
    # +value+ attribute.;Fi[	@—I"%#   collection['parked', :value];Fi[[	@√iÅI"û    # key in the first index configured for the collection.  A custom index can
    # be specified like so:
    # 
    #   collection['parked', :value]
    # ;Fi[	@√ièI"û    # key in the first index configured for the collection.  A custom index can
    # be specified like so:
    # 
    #   collection['parked', :value]
    # ;Fi[	@—I"2#   states = StateMachine::NodeCollection.new;Fi[[	@√iiI"‘    # Calls the block once for each element in self, passing that element as a
    # parameter.
    # 
    #   states = StateMachine::NodeCollection.new
    #   states << StateMachine::State.new(machine, :parked);Fi[	@√ixI"°    
    # Gets the node at the given index.
    # 
    #   states = StateMachine::NodeCollection.new
    #   states << StateMachine::State.new(machine, :parked);Fi[	@—I"#       event :turn_off do;Fi[[	@5iƒI"Ñ    #         transition all => :on
    #       end
    #       
    #       event :turn_off do
    #         transition all => :off;Fi[	@5iŒI"à    #         transition all => :active
    #       end
    #       
    #       event :turn_off do
    #         transition all => :off;Fi[	@—I"#     observe Vehicle;Fi[[	@%i$I"à        #   class VehicleObserver
        #     include DataMapper::Observer
        #     
        #     observe Vehicle
        #     ;Fi[	@%iaI"à        #   class VehicleObserver
        #     include DataMapper::Observer
        #     
        #     observe Vehicle
        #     ;Fi[	@—I"x#   vehicle = Vehicle.new                         # => #<Vehicle:0xb7c02850 @state="parked", @alarm_state="active">;Fi[[	@iUI"ﬁ    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                         # => #<Vehicle:0xb7c02850 @state="parked", @alarm_state="active">
    #   vehicle.state                                 # => "parked";Fi[	@iÇI"⁄    #     end
    #   end
    #   
    #   vehicle = Vehicle.new                         # => #<Vehicle:0xb7c02850 @state="parked", @alarm_state="active">
    #   vehicle.fire_events(:ignite, :disable_alarm)  # => true;Fi[	@—@¥-i[[	@iOI"á    #         transition all => :active
    #       end
    #       
    #       event :disable do
    #         transition all => :off;Fi[	@i|I"á    #         transition all => :active
    #       end
    #       
    #       event :disable do
    #         transition all => :off;Fi[	I"case @config["STRING"];F@W
i[[	@*iI"p    def function
      return if @setup
      case @config["STRING"]
      when "STRING" then
        begin;Fi[	@*i`I"•    def function(var)
      setup
      case @config["STRING"]
      when "STRING" then
        @redcarpet_extensions["LITERAL"] = @redcarpet_extensions["LITERAL"].!;Fi[	@—@–i[[	@iI"ë    #         transition :idling => :parked
    #       end
    #       
    #       event :ignite do
    #         transition :parked => :idling;Fi[	@iCI"ë    #         transition :idling => :parked
    #       end
    #       
    #       event :ignite do
    #         transition :parked => :idling;Fi[	I"=MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"];F@BBi[[	@*iDI".            MaRuKu::Globals["LITERAL"] = "STRING"
            MaRuKu::Globals["LITERAL"] = true
            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"]
            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"]
            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"];Fi[	@*iEI"            MaRuKu::Globals["LITERAL"] = true
            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"]
            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"]
            MaRuKu::Globals["LITERAL"] = @config["STRING"]["STRING"]
          end);Fi[	@—I"M# See StateMachine::Machine#transition for a description of the possible;Fi[[	@`idI"ö    #    end
    #  end
    # 
    # See StateMachine::Machine#transition for a description of the possible
    # configurations for defining transitions.;Fi[	@`i)I"ú    #     end
    #   end
    # 
    # See StateMachine::Machine#transition for a description of the possible
    # configurations for defining transitions.;Fi[	@—I"*#   person = Person.new('John Smith');Fi[[	@i!I"X    #     end
    #   end
    # 
    #   person = Person.new('John Smith')
    #   ;Fi[	@i1I"X    # 
    # For example,
    # 
    #   person = Person.new('John Smith')
    #   ;Fi[	@—I"N#   branch = StateMachine::Branch.new(:parked => :idling, :on => :ignite);Fi[[	@˛
i?I"{    # 
    # == Examples
    # 
    #   branch = StateMachine::Branch.new(:parked => :idling, :on => :ignite)
    #   ;Fi[	@˛
ihI"{    # 
    # == Examples
    # 
    #   branch = StateMachine::Branch.new(:parked => :idling, :on => :ignite)
    #   ;Fi[	@ﬁ)@ˆ-i[[	@ñiI"Œ    # base - The String path to the source.
    # dir  - The String path between the source and the file.
    # name - The String filename of the file.
    def function(var, var, var, var)
      @site = var;Fi[	@iI"Œ    # base - The String path to the <source>.
    # dir  - The String path between <source> and the file.
    # name - The String filename of the file.
    def function(var, var, var, var)
      @site = var;Fi[	@—I"K# Returns true if the job was queued, nil if the job was rejected by a;Fi[[	@ i#I"π  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`
  #
  # Returns true if the job was queued, nil if the job was rejected by a
  # before_enqueue hook.;Fi[	@ i0I"Æ  #
  # `queue` should be the String name of the queue you're targeting.
  #
  # Returns true if the job was queued, nil if the job was rejected by a
  # before_enqueue hook.;Fi[	@—I"L# If either of those conditions are met, it will use the value obtained;Fi[[	@ iI"—  #   a) has a @queue ivar set
  #   b) responds to `queue`
  #
  # If either of those conditions are met, it will use the value obtained
  # from performing one of the above operations to determine the queue.;Fi[	@ iDI"—  #   a) has a @queue ivar set
  #   b) responds to `queue`
  #
  # If either of those conditions are met, it will use the value obtained
  # from performing one of the above operations to determine the queue.;Fi[	@—I"!#   a) has a @queue ivar set;Fi[[	@ iI"≠  # It assumes the class you're passing it is a real Ruby class (not
  # a string or reference) which either:
  #
  #   a) has a @queue ivar set
  #   b) responds to `queue`;Fi[	@ iAI"≠  # It assumes the class you're passing it is a real Ruby class (not
  # a string or reference) which either:
  #
  #   a) has a @queue ivar set
  #   b) responds to `queue`;Fi[	I")# Returns the HTML formatted String.;F@õi[[	@{iI"õ    # input - The Textile String to convert.
    #
    # Returns the HTML formatted String.
    def function(var)
      var = @context.registers["LITERAL"];Fi[	@{iI"ú    # input - The Markdown String to convert.
    #
    # Returns the HTML formatted String.
    def function(var)
      var = @context.registers["LITERAL"];Fi[	I"!# date - The Time to format.;F@—i[[	@{i)I"ã    # Format a date in long format e.g. "27 January 2011".
    #
    # date - The Time to format.
    #
    # Returns the formatted String.;Fi[	@{i2I"d    # Format a date for use in XML.
    #
    # date - The Time to format.
    #
    # Examples;Fi[	@—@ß,i[[	@«iI"∑    
    # Find Resque worker pids on Windows.
    #
    # Returns an Array of string pids of all the other workers on this
    # machine. Useful when pruning dead workers on startup.;Fi[	@«iI"ª    
    # Find worker pids - platform independent
    #
    # Returns an Array of string pids of all the other workers on this
    # machine. Useful when pruning dead workers on startup.;Fi[	@—I"E# Can optionally accept a second int parameter. The stat is then;Fi[[	I")projects//rescue_proj/resque/stat.rb;TiI"´    
    # For a string stat name, increments the stat by one.
    #
    # Can optionally accept a second int parameter. The stat is then
    # incremented by that amount.;Fi[	@êBi#I"´    
    # For a string stat name, decrements the stat by one.
    #
    # Can optionally accept a second int parameter. The stat is then
    # decremented by that amount.;Fi[	I"$# input - The String to escape.;F@—i[[	@{iAI"o    # appropriate HTML entity replacements.
    #
    # input - The String to escape.
    #
    # Examples;Fi[	@{iPI"l    # with appropriate %XX replacements.
    #
    # input - The String to escape.
    #
    # Examples;Fi[	I""# Returns the escaped String.;F@õi[[	@{iHI"Ü    #   # => "foo &quot;bar&quot; &lt;baz&gt;"
    #
    # Returns the escaped String.
    def function(var)
      CGI.escapeHTML(var);Fi[	@{iWI"z    #   # => "foo%2Cbar%3Bbaz%3F"
    #
    # Returns the escaped String.
    def function(var)
      CGI.escape(var);Fi[	@—I"P# Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking;Fi[[	@\<iI"    # Pop an item off one of the queues.  This method will block until an item
    # is available. This method returns a tuple of the queue object and job.
    #
    # Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking
    # pop, a ThreadError is raised.;Fi[	@_<i@I"‘    # Pop an item off the queue.  This method will block until an item is
    # available.
    #
    # Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking
    # pop, a ThreadError is raised.;Fi[	@*I"@dir = var;Fi[[	@ñiI"p    def function(var, var, var, var)
      @site = var
      @base = var
      @dir = var
      @name = var;Fi[	@iI"p    def function(var, var, var, var)
      @site = var
      @base = var
      @dir = var
      @name = var;Fi[	@@•i[[	@ﬁ@iI""        var = (var + "LITERAL")
        var = (((var["LITERAL"].split("STRING")["LITERAL"] + "STRING") + var["LITERAL"]) + (var["LITERAL"] =~ "LITERAL" ? ("STRING") : ("STRING")))
        File.open("STRING", "STRING") do |var|
          var.puts("STRING")
          var.puts(var["LITERAL"]);Fi[	@2i-I"æ          var.each do |var|
            FileUtils.mkdir_p(var["LITERAL"])
            File.open("STRING", "STRING") do |var|
              var.puts("STRING")
              var.puts("STRING");Fi[	@—I"A# A hash of namespace bindings may be appended. For example:;Fi[[	@µiuI"{      #
      #   node.xpath('.//title')
      #
      # A hash of namespace bindings may be appended. For example:
      #;Fi[	@µi†I"§      #   node.css('body h1.bold')
      #   node.css('div + p.green', 'div#one')
      #
      # A hash of namespace bindings may be appended. For example:
      #;Fi[	@—I"#   <root xmlns:foo="bar">;Fi[[	@ßi`I"Ç      #
      # For example, given this document:
      #
      #   <root xmlns:foo="bar">
      #     <bar xmlns:hello="world" />;Fi[	@ßikI"Ω      # WARNING: this method will clobber duplicate names in the keys.
      # For example, given this document:
      #
      #   <root xmlns:foo="bar">
      #     <bar xmlns:foo="baz" />;Fi[	@—I"9#   node.serialize(:encoding => 'UTF-8') do |config|;Fi[[	@i:I"{      #
      # or
      #
      #   node.serialize(:encoding => 'UTF-8') do |config|
      #     config.format.as_xml;Fi[	@µi°I"{      #
      # or
      #
      #   node.serialize(:encoding => 'UTF-8') do |config|
      #     config.format.as_xml;Fi[	@—I"	# or;Fi[[	@i8I"t      #
      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML)
      #
      # or
      #;Fi[	@µiüI"t      #
      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML)
      #
      # or
      #;Fi[	@—I"K#  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML);Fi[[	@i6I"ë      #
      # These two statements are equivalent:
      #
      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML)
      #;Fi[	@µiùI"ë      #
      # These two statements are equivalent:
      #
      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML)
      #;Fi[	@—I"+# These two statements are equivalent:;Fi[[	@i4I"™      # Serialize Node using +options+.  Save options can also be set using a
      # block. See SaveOptions.
      #
      # These two statements are equivalent:
      #;Fi[	@µiõI"™      # Serialize Node using +options+.  Save options can also be set using a
      # block. See SaveOptions.
      #
      # These two statements are equivalent:
      #;Fi[	@—I"# Returns nothing;Fi[[	@bi.I"P    
    # Reset Site details.
    #
    # Returns nothing
    def function;Fi[	@ i—I"Ä  #
  #   Resque.push('archive', 'class' => 'Archive', 'args' => [ 35, 'tar' ])
  #
  # Returns nothing
  def function(var, var);Fi[	I"fvar = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |var, var|;F@pi[[	@2i I"û        var = var.strip.downcase.gsub("LITERAL", "STRING").gsub("LITERAL", "STRING").gsub("LITERAL", "STRING").gsub("LITERAL", "STRING").gsub("LITERAL", "STRING").gsub("LITERAL", "STRING")
        var = ((var.strftime("STRING") + var) + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml;Fi[	@i,I"          var = var["STRING"].!
          var = ("STRING" % [var.year, var.month, var.day, var])
          var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var) }.delete_if do |var, var|
            (var.nil? or (var == "STRING"))
          end.to_yaml;Fi[	@—@úBi[[	@{iGI"í    #   xml_escape('foo "bar" <baz>')
    #   # => "foo &quot;bar&quot; &lt;baz&gt;"
    #
    # Returns the escaped String.
    def function(var);Fi[	@{iVI"Ç    #   cgi_escape('foo,bar;baz?')
    #   # => "foo%2Cbar%3Bbaz%3F"
    #
    # Returns the escaped String.
    def function(var);Fi[	@pI"end.to_yaml);Fi[[	@iI"1        File.open("STRING", "STRING") do |var|
          var.puts({ "STRING" => "STRING", "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"]), "STRING" => (var["LITERAL"]) }.delete_if do |var, var|
            (var.nil? or (var == "STRING"))
          end.to_yaml)
          var.puts("STRING");Fi[	@iI"        File.open("STRING", "STRING") do |var|
          var.puts({ "STRING" => "STRING", "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"]) }.delete_if do |var, var|
            (var.nil? or (var == "STRING"))
          end.to_yaml)
          var.puts("STRING");Fi[	@—@ïBi[[	@{i@I"£    # XML escape a string for use. Replaces any special characters with
    # appropriate HTML entity replacements.
    #
    # input - The String to escape.
    #;Fi[	@{iOI"§    # CGI escape a string for use in a URL. Replaces any special characters
    # with appropriate %XX replacements.
    #
    # input - The String to escape.
    #;Fi[	@—@ÄBi[[	@{i(I"q    
    # Format a date in long format e.g. "27 January 2011".
    #
    # date - The Time to format.
    #;Fi[	@{i1I"Z    
    # Format a date for use in XML.
    #
    # date - The Time to format.
    #;Fi[	@Ü*@€$i[[	@iI"Â    SQL = "STRING"
    
    def self.process(var, var, var, var)
      FileUtils.mkdir_p("STRING")
      var = Sequel.postgres("LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING");Fi[	@iI"”    SQL = "STRING"
    
    def self.process(var, var, var, var)
      FileUtils.mkdir_p("STRING")
      var = Sequel.mysql(var, "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => (var), "LITERAL" => "STRING");Fi[	@—@yBi[[	@{iI"|    #
    # input - The Textile String to convert.
    #
    # Returns the HTML formatted String.
    def function(var);Fi[	@{iI"}    #
    # input - The Markdown String to convert.
    #
    # Returns the HTML formatted String.
    def function(var);Fi[	@ıB@•i[[	@iI"6          var.puts({ "STRING" => "STRING", "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"]), "STRING" => (var["LITERAL"]) }.delete_if do |var, var|
            (var.nil? or (var == "STRING"))
          end.to_yaml)
          var.puts("STRING")
          var.puts(var["LITERAL"].delete("STRING"));Fi[	@iI"          var.puts({ "STRING" => "STRING", "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"]) }.delete_if do |var, var|
            (var.nil? or (var == "STRING"))
          end.to_yaml)
          var.puts("STRING")
          var.puts(var["LITERAL"].delete("STRING"));Fi[	@—I"# Defaults to `false`.;Fi[[	@xitI"”    # were present. Use with caution as this may cause whitespace-related
    # formatting errors.
    #
    # Defaults to `false`.
    # Whether or not attribute hashes and Ruby scripts designated by `=` or `~`;Fi[	@xiyI"œ    # should be evaluated. If this is `true`, said scripts are rendered as empty
    # strings.
    #
    # Defaults to `false`.
    # If set to `true`, Haml makes no attempt to properly indent or format the;Fi[	I"B(var << ("STRING" + var["STRING"])) unless var["STRING"].nil?;F@W
i[[	@0i;I"¿        var = (var["STRING"] or var["STRING"])
        var = "STRING"
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?
      when "STRING" then
        var = var["STRING"];Fi[	@0iPI"≠        var = var["STRING"]
        var = "STRING"
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?
      when "STRING" then
        var = var["STRING"];Fi[	@Ø8@)i[[	@îiÎI"Ω      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          var = ["STRING", "STRING", "STRING", "STRING"]
          var = Node.new("LITERAL", var)
        else;Fi[	@îiÔI"∞        else
          if (var["LITERAL"] == "STRING") then
            var = ["STRING", "STRING", "STRING", "STRING"]
            var = Node.new("LITERAL", var)
          else;Fi[	I"0var = clean_entities(var) if var["LITERAL"];FI"var = var["LITERAL"].to_s;Fi[[	@Ti*I"œ      var = ("STRING" % [var.year, var.month, var.day, var])
      var = var["LITERAL"].to_s
      var = clean_entities(var) if var["LITERAL"]
      var = var["LITERAL"].to_s
      var = var.index("LITERAL");Fi[	@TiSI"¯          var = var["LITERAL"].to_s
          var.force_encoding("STRING") if var.respond_to?("LITERAL")
          var = clean_entities(var) if var["LITERAL"]
          var = var["LITERAL"].to_s
          var = clean_entities(var) if var["LITERAL"];Fi[	@/C@.Ci[[	@Ti)I"◊      var = (var["LITERAL"] or Time.now)
      var = ("STRING" % [var.year, var.month, var.day, var])
      var = var["LITERAL"].to_s
      var = clean_entities(var) if var["LITERAL"]
      var = var["LITERAL"].to_s;Fi[	@TiTI"ø          var.force_encoding("STRING") if var.respond_to?("LITERAL")
          var = clean_entities(var) if var["LITERAL"]
          var = var["LITERAL"].to_s
          var = clean_entities(var) if var["LITERAL"]
          (var << { "STRING" => (var["LITERAL"].to_i), "STRING" => (var), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var["LITERAL"].to_s), "STRING" => (var) });Fi[	I"<if var["LITERAL"] and (var["LITERAL"] == "STRING") then;F@i[[	@Ti<I"–        var = "STRING"
        var[var].each do |var|
          if var["LITERAL"] and (var["LITERAL"] == "STRING") then
            if var["LITERAL"] then
              (var << clean_entities(var["LITERAL"]));Fi[	@TiCI"ø            end
          else
            if var["LITERAL"] and (var["LITERAL"] == "STRING") then
              if var["LITERAL"] then
                (var << clean_entities(var["LITERAL"]));Fi[	I",(var << clean_entities(var["LITERAL"]));F@ri[[	@Ti>I"—          if var["LITERAL"] and (var["LITERAL"] == "STRING") then
            if var["LITERAL"] then
              (var << clean_entities(var["LITERAL"]))
            else
              (var << var["LITERAL"]);Fi[	@TiEI"€            if var["LITERAL"] and (var["LITERAL"] == "STRING") then
              if var["LITERAL"] then
                (var << clean_entities(var["LITERAL"]))
              else
                (var << var["LITERAL"]);Fi[	@—@„:i[[	@~i˘I"ﬂ    # (as is the default for Rails 3.0+, and optional for version 2.3.5+),
    # this won't escape text declared as "safe".
    #
    # @param text [String] The string to sanitize
    # @return [String] The sanitized string;Fi[	@~iI"›    # Escapes HTML entities in `text`, but without escaping an ampersand
    # that is already part of an escaped entity.
    #
    # @param text [String] The string to sanitize
    # @return [String] The sanitized string;Fi[	I"require("time");F@¡i[[	@œi I"3require("time")
module Resque
  module Failure;Fi [	@«i I"1require("time")
module Resque
  class Worker;Fi [	@™B@7 i[[	@ñiI"c      @site = var
      @base = var
      @dir = var
      @name = var
      self.process(var);Fi[	@iI"S      @site = var
      @base = var
      @dir = var
      @name = var
    end;Fi[	I"return @url if @url;FI"var = if permalink then;Fi[[	@ñiLI"}    # Returns the String url.
    def function
      return @url if @url
      var = if permalink then
        permalink;Fi[	@ôiÖI"}    # Returns the String URL.
    def function
      return @url if @url
      var = if permalink then
        permalink;Fi[	@^CI"permalink;Fi[[	@ñiMI"j    def function
      return @url if @url
      var = if permalink then
        permalink
      else;Fi[	@ôiÜI"j    def function
      return @url if @url
      var = if permalink then
        permalink
      else;Fi[	@eC@ri[[	@ñiNI"∫      return @url if @url
      var = if permalink then
        permalink
      else
        { "STRING" => (self.basename), "STRING" => (self.output_ext) }.inject(template) do |var, var|;Fi[	@ôiáI"€      return @url if @url
      var = if permalink then
        permalink
      else
        { "STRING" => (date.strftime("STRING")), "STRING" => (date.strftime("STRING")), "STRING" => (date.strftime("STRING")), "STRING" => (CGI.escape(slug)), "STRING" => (date.strftime("STRING").to_i.to_s), "STRING" => (date.strftime("STRING").to_i.to_s), "STRING" => (categories.map { |var| URI.escape(var) }.join("STRING")), "STRING" => (self.output_ext) }.inject(template) do |var, var|;Fi[	I"P@url = var.split("STRING").reject { |var| var =~ "LITERAL" }.join("STRING");FI"1@url = (@url + "STRING") if var =~ "LITERAL";Fi[[	@ñiTI"ø        end.gsub("LITERAL", "STRING")
      end
      @url = var.split("STRING").reject { |var| var =~ "LITERAL" }.join("STRING")
      @url = (@url + "STRING") if var =~ "LITERAL"
      @url;Fi[	@ôiçI"ø        end.gsub("LITERAL", "STRING")
      end
      @url = var.split("STRING").reject { |var| var =~ "LITERAL" }.join("STRING")
      @url = (@url + "STRING") if var =~ "LITERAL"
      @url;Fi[	@—I"#     click;Fi[[	@~i?I"j    # with no whitespace between.
    # For example:
    #
    #     click
    #     = succeed '.' do;Fi[	@~iEI"T    #
    # Produces:
    #
    #     click
    #     <a href='thing'>here</a>.;Fi[	@—I"#     <h1>foo</h1>;Fi[[	@~iﬁI"N    #
    # Produces:
    #
    #     <h1>foo</h1>
    #       <p>bar</p>;Fi[	@~i˝I"P    #
    # Produces:
    #
    #     <h1>foo</h1>
    #         <p>bar</p>;Fi[	@·0I")FileUtils.mkdir_p(File.dirname(var));Fi[[	@ñiÅI"¥    # Returns nothing.
    def function(var)
      var = destination(var)
      FileUtils.mkdir_p(File.dirname(var))
      File.open(var, "STRING") { |var| var.write(self.output) };Fi[	@ôi“I"¥    # Returns nothing.
    def function(var)
      var = destination(var)
      FileUtils.mkdir_p(File.dirname(var))
      File.open(var, "STRING") { |var| var.write(self.output) };Fi[	I"$stdout.print("STRING");FI"$stdout.flush;Fi[[	@ôi£I"©          (puts("STRING")
          var = Classifier::LSI.new("LITERAL" => (false))
          $stdout.print("STRING")
          $stdout.flush
          var.each do |var|;Fi[	@ôi¶I"è          $stdout.flush
          var.each do |var|
            $stdout.print("STRING")
            $stdout.flush
            var.add_item(var);Fi[	@—I"#     %h1 foo;Fi[[	@~i÷I"f    # to the lines of the template.
    # For example:
    #
    #     %h1 foo
    #     - tab_up;Fi[	@~iˆI"y    # but only for the duration of the block.
    # For example:
    #
    #     %h1 foo
    #     - with_tabs(2) do;Fi[	@—I"9#   @yield The block within which to escape newlines;Fi[[	@~i]I"›    # @overload find_and_preserve(tags = haml_buffer.options[:preserve])
    #   Escapes newlines within a block of Haml code.
    #
    #   @yield The block within which to escape newlines
    def function(var, var, var);Fi[	@~irI"®    # @overload perserve
    #   Escapes newlines within a block of Haml code.
    #
    #   @yield The block within which to escape newlines
    def function(var, var);Fi[	@—@3:i[[	@1iFI"¢      # This is meant to be overridden by subclasses
      # so they can add their own options.
      #
      # @param opts [OptionParser]
      def function(var);Fi[	@1i¶I"|      
      # Tells optparse how to parse the arguments.
      #
      # @param opts [OptionParser]
      def function(var);Fi[	@—I"3# This is meant to be overridden by subclasses;Fi[[	@1iCI"¿      # Tells optparse how to parse the arguments
      # available for all executables.
      #
      # This is meant to be overridden by subclasses
      # so they can add their own options.;Fi[	@1i]I"Ÿ      # In particular, sets `@options[:input]` and `@options[:output]`
      # to appropriate IO streams.
      #
      # This is meant to be overridden by subclasses
      # so they can run their respective programs.;Fi[	@
.I"## Parse a file with +filename+;Fi[[	@ iI"k        end
        
        ###
        # Parse a file with +filename+
        def function(var, var);Fi[	@ÉiII"f        end
        
        ###
        # Parse a file with +filename+
        def function(var);Fi[	@
.I"2# Add +node_or_tags+ as a child of this Node.;Fi[[	@µiÓI"≈      end
      
      ###
      # Add +node_or_tags+ as a child of this Node.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.;Fi[	@µiˇI"≈      end
      
      ###
      # Add +node_or_tags+ as a child of this Node.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.;Fi[	@
.I",# Called at the beginning of an element;Fi[[	@ÄiI"        end
        
        ###
        # Called at the beginning of an element
        # * +name+ is the name of the tag;Fi[	@Äi+I"z        end
        
        ###
        # Called at the beginning of an element
        # +name+ is the element name;Fi[	@
.I"&# Called at the end of an element;Fi[[	@Äi$I"p        end
        
        ###
        # Called at the end of an element
        # +name+ is the tag name;Fi[	@Äi:I"v        end
        
        ###
        # Called at the end of an element
        # +name+ is the element's name;Fi[	@—I"#     # This doesn't;Fi[[	@KiåI"‡    #     Haml::Engine.new("%p= foo").render_proc(Object.new, :foo).call :foo => "Hello!"
    #       #=> "<p>Hello!</p>"
    #
    #     # This doesn't
    #     Haml::Engine.new("%p= foo").render_proc.call :foo => "Hello!";Fi[	@KiºI"π    #     Haml::Engine.new("%p= foo").def_method(obj, :render, :foo)
    #     obj.render(:foo => "Hello!") #=> "<p>Hello!</p>"
    #
    #     # This doesn't
    #     obj = Object.new;Fi[	@ª*I"xvar.value["LITERAL"] = Node.new("LITERAL", [Node.new("LITERAL", ["STRING"]), Node.new("LITERAL", ["STRING", var])]);Fi[[	@i4I"B            var = var.value["LITERAL"].value.first.gsub("LITERAL", "STRING")
            var = var.value["LITERAL"].value.first
            var.value["LITERAL"].value = ["STRING"]
            var.value["LITERAL"] = Node.new("LITERAL", [Node.new("LITERAL", ["STRING"]), Node.new("LITERAL", ["STRING", var])])
          else;Fi[	@i9I"?            if ("STRING" == var.value["LITERAL"].value.first) then
              var = var.value["LITERAL"].value.first
              var.value["LITERAL"].value = ["STRING"]
              var.value["LITERAL"] = Node.new("LITERAL", [Node.new("LITERAL", ["STRING"]), Node.new("LITERAL", ["STRING", var])])
            end;Fi[	I"var = var.flatten;F@ài[[	@îi5I"]      
      def function(var, var, var)
        var = var.flatten
        var
      end;Fi[	@îi:I"]      
      def function(var, var, var)
        var = var.flatten
        var
      end;Fi[	@—I"#     # This works;Fi[[	@KiàI"Ω    # This is done with the `local_names` argument.
    # For example:
    #
    #     # This works
    #     Haml::Engine.new("%p= foo").render_proc(Object.new, :foo).call :foo => "Hello!";Fi[	@Ki∑I"~    # This is done with the `local_names` argument.
    # For example:
    #
    #     # This works
    #     obj = Object.new;Fi[	@—I"Z# @param scope [Binding, Proc, Object] The context in which the template is evaluated;Fi[[	@Ki]I"    # but if you're relying on local variables defined in the context of `scope`,
    # they won't work.
    #
    # @param scope [Binding, Proc, Object] The context in which the template is evaluated
    # @param locals [{Symbol => Object}] Local variables that will be made available;Fi[	@KiíI"    #
    # The proc doesn't take a block; any yields in the template will fail.
    #
    # @param scope [Binding, Proc, Object] The context in which the template is evaluated
    # @param local_names [Array<Symbol>] The names of the locals that can be passed to the proc;Fi[	@—@Ò9i[[	@Ki@I"ˇ    # Haml uses it as the second argument to `Kernel#eval`;
    # otherwise, Haml just uses its `#instance_eval` context.
    #
    # Note that Haml modifies the evaluation context
    # (either the scope object or the `self` object of the scope binding).;Fi[	@Ki¡I"#    #     Haml::Engine.new("%p= foo").def_method(obj, :render)
    #     obj.render(:foo => "Hello!") #=> NameError: undefined local variable or method `foo'
    #
    # Note that Haml modifies the evaluation context
    # (either the scope object or the `self` object of the scope binding).;Fi[	I"Nvar = Node.new("LITERAL", [[var.first, var.last].compact.join("STRING")]);F@ài[[	@îiñI"ê      
      def function(var, var, var)
        var = Node.new("LITERAL", [[var.first, var.last].compact.join("STRING")])
        var
      end;Fi[	@îi∂I"ê      
      def function(var, var, var)
        var = Node.new("LITERAL", [[var.first, var.last].compact.join("STRING")])
        var
      end;Fi[	I"Kvar = Node.new("LITERAL", ([var["LITERAL"]] + (var["LITERAL"] or [])));F@ài[[	@îißI"†      # reduce 27 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", ([var["LITERAL"]] + (var["LITERAL"] or [])))
        var
      end;Fi[	@îi¨I"ç      
      def function(var, var, var)
        var = Node.new("LITERAL", ([var["LITERAL"]] + (var["LITERAL"] or [])))
        var
      end;Fi[	@—@ÿi[[	@Òi4I"m  
  # Determine if we want to skip emailing the recipient.
  #
  # @return [Boolean]
  # @return [void];Fi[	@>iKI"±    # as is the default for Rails 3.0+, and optional for version 2.3.5+.
    # Overridden in haml/template.rb if this is the case.
    #
    # @return [Boolean]
    def function;Fi[	@—I"@#   * Validating if the model allows params authentication;;Fi[[	@Îi;I"“      
      # Check if this is strategy is valid for http authentication by:
      #
      #   * Validating if the model allows params authentication;
      #   * If any of the authorization headers were sent;;Fi[	@ÎiEI"ﬂ      
      # Check if this is strategy is valid for params authentication by:
      #
      #   * Validating if the model allows params authentication;
      #   * If the request hits the sessions controller through POST;;Fi[	I"var.unshift("STRING");F@)i[[	@îiI"§      def function(var, var, var)
        if (var["LITERAL"] == "STRING") then
          var.unshift("STRING")
          var = Node.new("LITERAL", var)
        else;Fi[	@îiI"∞          if (var["LITERAL"] == "STRING") then
            var["LITERAL"] = "STRING"
            var.unshift("STRING")
            var = Node.new("LITERAL", var)
          else;Fi[	I"&var = [var.first, var["LITERAL"]];F@ài[[	@îiFI"m      
      def function(var, var, var)
        var = [var.first, var["LITERAL"]]
        var
      end;Fi[	@îiKI"m      
      def function(var, var, var)
        var = [var.first, var["LITERAL"]]
        var
      end;Fi[	@—@<6i[[	@∂iFI"£      # Setup a subject doing an I18n lookup. At first, it attemps to set a subject
      # based on the current mapping:
      #
      #   en:
      #     devise:;Fi[	@∂iNI"{      #
      # If one does not exist, it fallbacks to ActionMailer default:
      #
      #   en:
      #     devise:;Fi[	@—@6i[[	@ÂiZI"Ç      #
      # Examples:
      #
      #   sign_out :user     # sign_out(scope)
      #   sign_out @user     # sign_out(resource);Fi[	@øi0I"}    #
    # Examples:
    #
    #   sign_out :user     # sign_out(scope)
    #   sign_out @user     # sign_out(resource);Fi[	I"8return self.send(var, var) if self.respond_to?(var);FI"case var.value.first;Fi[[	@Üi
I"Ÿ      def function(var)
        var = :"visit_function_#{var.value.first.gsub("LITERAL", "STRING")}"
        return self.send(var, var) if self.respond_to?(var)
        case var.value.first
        when "LITERAL" then;Fi[	@ÜiaI"„        else
          var = :"visit_pseudo_class_#{var.value.first.gsub("LITERAL", "STRING")}"
          return self.send(var, var) if self.respond_to?(var)
          case var.value.first
          when "STRING", "STRING" then;Fi[	@Ñ7@Íi[[	@ÜiJI"©        case var.value["LITERAL"]
        when "LITERAL" then
          ((var + "STRING") + "STRING")
        when "LITERAL" then
          ((var + "STRING") + "STRING");Fi[	@ÜiLI"ö          ((var + "STRING") + "STRING")
        when "LITERAL" then
          ((var + "STRING") + "STRING")
        when "LITERAL" then
          "STRING";Fi[	@I"class ElementDescription;Fi[[	@úiI"Imodule Nokogiri
  module HTML
    class ElementDescription
      ###;Fi[	@ÿiI"ˆmodule Nokogiri
  module HTML
    class ElementDescription
      Desc = Struct.new("STRING", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL");Fi[	@—I"O# This function can be called one or more times in a compass configuration;Fi[[	@#iI"-    #   Compass::BrowserSupport.add_support("image", "moz", "webkit")
    #   # => Adds support for moz and webkit to the image() function.
    #
    # This function can be called one or more times in a compass configuration
    # file in order to add support for new, simple browser functions without;Fi[	@#i*I"/    #   Compass::BrowserSupport.remove_support("image", "o", "ms")
    #   # => Adds support for moz and webkit to the image() function.
    #
    # This function can be called one or more times in a compass configuration
    # file in order to remove support for simple functions that no longer need to;Fi[	I"	####;FI";# Create a Nokogiri::XML::DocumentFragment from +tags+;Fi[[	@iDI"u      end
      
      ####
      # Create a Nokogiri::XML::DocumentFragment from +tags+
      def function(var);Fi[	@¨iPI"Ä      
      class << self
        ####
        # Create a Nokogiri::XML::DocumentFragment from +tags+
        def function(var);Fi[	I"L# Serialize Node using +options+.  Save options can also be set using a;FI"# block. See SaveOptions.;Fi[[	@i2I"á      
      ####
      # Serialize Node using +options+.  Save options can also be set using a
      # block. See SaveOptions.
      #;Fi[	@µiôI"Ü      
      ###
      # Serialize Node using +options+.  Save options can also be set using a
      # block. See SaveOptions.
      #;Fi[	@QD@—i[[	@i3I"≠      ####
      # Serialize Node using +options+.  Save options can also be set using a
      # block. See SaveOptions.
      #
      # These two statements are equivalent:;Fi[	@µiöI"¨      ###
      # Serialize Node using +options+.  Save options can also be set using a
      # block. See SaveOptions.
      #
      # These two statements are equivalent:;Fi[	@⁄B@—i[[	@i5I"©      # block. See SaveOptions.
      #
      # These two statements are equivalent:
      #
      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML);Fi[	@µiúI"©      # block. See SaveOptions.
      #
      # These two statements are equivalent:
      #
      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML);Fi[	@”B@—i[[	@i7I"î      # These two statements are equivalent:
      #
      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML)
      #
      # or;Fi[	@µiûI"î      # These two statements are equivalent:
      #
      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML)
      #
      # or;Fi[	@ÃB@—i[[	@i9I"¢      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML)
      #
      # or
      #
      #   node.serialize(:encoding => 'UTF-8') do |config|;Fi[	@µi†I"¢      #  node.serialize(:encoding => 'UTF-8', :save_with => FORMAT | AS_XML)
      #
      # or
      #
      #   node.serialize(:encoding => 'UTF-8') do |config|;Fi[	@≈BI"#     config.format.as_xml;Fi[[	@i;I"|      # or
      #
      #   node.serialize(:encoding => 'UTF-8') do |config|
      #     config.format.as_xml
      #   end;Fi[	@µi¢I"|      # or
      #
      #   node.serialize(:encoding => 'UTF-8') do |config|
      #     config.format.as_xml
      #   end;Fi[	@pD@–i[[	@i<I"~      #
      #   node.serialize(:encoding => 'UTF-8') do |config|
      #     config.format.as_xml
      #   end
      #;Fi[	@µi£I"~      #
      #   node.serialize(:encoding => 'UTF-8') do |config|
      #     config.format.as_xml
      #   end
      #;Fi[	@ID@õi[[	@iEI"ô      
      ####
      # Create a Nokogiri::XML::DocumentFragment from +tags+
      def function(var)
        DocumentFragment.new(self, var, self.root);Fi[	@¨iQI"§      class << self
        ####
        # Create a Nokogiri::XML::DocumentFragment from +tags+
        def function(var)
          self.new(XML::Document.new, var);Fi[	@—@É4i[[	@•iI"        # are set via a super() call to RSync::Base,
        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and RSync specific defaults are set,
        # the block from the user's configuration file is evaluated.;Fi[	@i!I"        # are set via a super() call to RSync::Base,
        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and RSync specific defaults are set,
        # the block from the user's configuration file is evaluated.;Fi[	@—@Á4i[[	@i%I"        # are set via a super() call to Cloud::Base,
        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and Cloud specific defaults are set,
        # the block from the user's configuration file is evaluated.;Fi[	@iI"        # are set via a super() call to Cloud::Base,
        # which in turn will invoke Syncer::Base.
        #
        # Once pre-configured defaults and Cloud specific defaults are set,
        # the block from the user's configuration file is evaluated.;Fi[	I";(var = var.match("LITERAL") and return var["LITERAL"]);FI"catch("LITERAL") do;Fi[[	@iìI"2          var = var.match("LITERAL") and return Nokogiri.XML(var["LITERAL"]).encoding
          if Nokogiri.jruby? then
            (var = var.match("LITERAL") and return var["LITERAL"])
            catch("LITERAL") do
              Nokogiri::HTML::SAX::Parser.new(JumpSAXHandler.new("LITERAL")).parse(var);Fi[	@ißI"*          begin
            ((var = var.match("LITERAL") and return Nokogiri.XML(var["LITERAL"]).encoding)
            (var = var.match("LITERAL") and return var["LITERAL"])
            catch("LITERAL") do
              Nokogiri::HTML::SAX::Parser.new(JumpSAXHandler.new("LITERAL".to_s)).parse(var);Fi[	I",var["LITERAL"] = var["LITERAL"].flatten;F@óDi[[	@ÿi+I"√        begin
          (var = Desc.new(*var)
          var["LITERAL"] = var["LITERAL"].flatten
          var["LITERAL"] = var["LITERAL"].flatten
          var["LITERAL"] = var["LITERAL"].flatten;Fi[	@ÿi,I"Ë          (var = Desc.new(*var)
          var["LITERAL"] = var["LITERAL"].flatten
          var["LITERAL"] = var["LITERAL"].flatten
          var["LITERAL"] = var["LITERAL"].flatten
          var["LITERAL"] = var["LITERAL"].flatten);Fi[	@#I"class PushParser;Fi[[	@&iI"lmodule Nokogiri
  module HTML
    module SAX
      class PushParser
        def function(var, var, var);Fi[	@âiI"hmodule Nokogiri
  module XML
    module SAX
      class PushParser
        attr_accessor("LITERAL");Fi[	@…<I"2raise(Errno::ENOENT) unless File.exists?(var);Fi[[	@ iI"ﬁ        # Parse a file with +filename+
        def function(var, var)
          raise(ArgumentError) unless var
          raise(Errno::ENOENT) unless File.exists?(var)
          raise(Errno::EISDIR) if File.directory?(var);Fi[	@ÉiLI"Ÿ        # Parse a file with +filename+
        def function(var)
          raise(ArgumentError) unless var
          raise(Errno::ENOENT) unless File.exists?(var)
          raise(Errno::EISDIR) if File.directory?(var);Fi[	@•DI"1raise(Errno::EISDIR) if File.directory?(var);Fi[[	@ iI"‰        def function(var, var)
          raise(ArgumentError) unless var
          raise(Errno::ENOENT) unless File.exists?(var)
          raise(Errno::EISDIR) if File.directory?(var)
          var = ParserContext.file(var, var);Fi[	@ÉiMI"⁄        def function(var)
          raise(ArgumentError) unless var
          raise(Errno::ENOENT) unless File.exists?(var)
          raise(Errno::EISDIR) if File.directory?(var)
          var = ParserContext.file(var);Fi[	I"def self.new(var, var);FI"Gif ["LITERAL", "LITERAL"].all? { |var| var.respond_to?(var) } then;Fi[[	@#i	I"     module SAX
      class ParserContext < Nokogiri::XML::SAX::ParserContext
        def self.new(var, var)
          if ["LITERAL", "LITERAL"].all? { |var| var.respond_to?(var) } then
            super;Fi[	@Üi	I"¿    module SAX
      class ParserContext
        def self.new(var, var)
          if ["LITERAL", "LITERAL"].all? { |var| var.respond_to?(var) } then
            io(var, Parser::ENCODINGS[var]);Fi[	I"@document = var;FI"@encoding = var;Fi[[	@&i
I"¨      class PushParser
        def function(var, var, var)
          @document = var
          @encoding = var
          @sax_parser = HTML::SAX::Parser.new(var, @encoding);Fi[	@âiI"∫        # an optional +file_name+ and +encoding+
        def function(var, var, var)
          @document = var
          @encoding = var
          @sax_parser = XML::SAX::Parser.new(var);Fi[	@DI"class Node;Fi[[	@eiI"Hmodule Nokogiri
  module XML
    class Node
      class SaveOptions;Fi[	@µiI"|require("nokogiri/xml/node/save_options")
module Nokogiri
  module XML
    class Node
      include(Nokogiri::XML::PP::Node);Fi[	@D@	.i[[	@}iI"=module Nokogiri
  module XML
    class << self
      ###;Fi[	@åiI"=module Nokogiri
  module XML
    class << self
      ###;Fi[	@—I"# @api private;Fi[[	@‘iHI"b      #     notify_by Mail
      #   end
      #
      # @api private
      def function(var);Fi[	@‘iXI"«      # Cleanup is also ensured, as temporary files may hold sensitive data.
      # If no options can be built, the packaging process will be aborted.
      #
      # @api private
      def function;Fi[	@DI"module PP;Fi[[	@ªiI"Jmodule Nokogiri
  module XML
    module PP
      module CharacterData;Fi[	@tiI"Amodule Nokogiri
  module XML
    module PP
      module Node;Fi[	@DI"class XPath;Fi[[	@ïiI"\module Nokogiri
  module XML
    class XPath
      class SyntaxError < XML::SyntaxError;Fi[	@òiI"{require("nokogiri/xml/xpath/syntax_error")
module Nokogiri
  module XML
    class XPath
      attr_accessor("LITERAL");Fi[	@—I"6#   Backup::Model.new(:my_backup, 'My Backup') do;Fi[[	@‘i‹I"      # here is the same as that used in {#keys}. Also, all spaces will be stripped
      # from the identifier when used, so the following would be valid.
      #
      #   Backup::Model.new(:my_backup, 'My Backup') do
      #     encrypt_with GPG do |enc|;Fi[	@‘iI"ú      #     enc.passphrase = 'secret phrase'
      #   end
      #
      #   Backup::Model.new(:my_backup, 'My Backup') do
      #     # other directives...;Fi[	I"(# For example, given this document:;F@—i[[	@ßi_I"~      # return them as a hash.
      #
      # For example, given this document:
      #
      #   <root xmlns:foo="bar">;Fi[	@ßijI"°      #
      # WARNING: this method will clobber duplicate names in the keys.
      # For example, given this document:
      #
      #   <root xmlns:foo="bar">;Fi[	I"#   </root>;F@—i[[	@ßicI"É      #   <root xmlns:foo="bar">
      #     <bar xmlns:hello="world" />
      #   </root>
      #
      # This method will return:;Fi[	@ßinI"¶      #   <root xmlns:foo="bar">
      #     <bar xmlns:foo="baz" />
      #   </root>
      #
      # The hash returned will look like this: { 'xmlns:foo' => 'bar' };Fi[	@—@É?i[[	@ÅiI"ª      ##
      # Specify the level of compression to use.
      #
      # Values should be a single digit from 1 to 9.
      # Note that setting the level to either extreme may or may not;Fi[	@áiI"ª      ##
      # Specify the level of compression to use.
      #
      # Values should be a single digit from 1 to 9.
      # Note that setting the level to either extreme may or may not;Fi[	@ÿ"I"+var.each { |var| add_child_node(var) };Fi[[	@µi˜I"ó      def function(var)
        var = coerce(var)
        if var.is_a?(XML::NodeSet) then
          var.each { |var| add_child_node(var) }
        else;Fi[	@µiSI"ó        var = coerce(var)
        children.unlink
        if var.is_a?(XML::NodeSet) then
          var.each { |var| add_child_node(var) }
        else;Fi[	I"# Entity node type;FI"# PI node type;Fi[[	@µi=I"ƒ      # CDATA node type, see Nokogiri::XML::Node#cdata?
      # Entity reference node type
      # Entity node type
      # PI node type
      # Comment node type, see Nokogiri::XML::Node#comment?;Fi[	@zi;I"Ç      # CDATA node type
      # Entity Reference node type
      # Entity node type
      # PI node type
      # Comment node type;Fi[	@∑B@—i[[	@µivI"π      #   node.xpath('.//title')
      #
      # A hash of namespace bindings may be appended. For example:
      #
      #   node.xpath('.//foo:name', {'foo' => 'http://example.org/'});Fi[	@µi°I"ƒ      #   node.css('div + p.green', 'div#one')
      #
      # A hash of namespace bindings may be appended. For example:
      #
      #   node.css('bike|tire', {'bike' => 'http://schwinn.com/'});Fi[	I"$#     def regex node_set, regex;FI"N#       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ };Fi[[	@µiÇI"À      #
      #   node.xpath('.//title[regex(., "\w+")]', Class.new {
      #     def regex node_set, regex
      #       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ }
      #     end;Fi[	@µi¨I"¬      #
      #   node.css('title:regex("\w+")', Class.new {
      #     def regex node_set, regex
      #       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ }
      #     end;Fi[	@E@¢i[[	@µiÉI"‘      #   node.xpath('.//title[regex(., "\w+")]', Class.new {
      #     def regex node_set, regex
      #       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ }
      #     end
      #   }.new);Fi[	@µi≠I"À      #   node.css('title:regex("\w+")', Class.new {
      #     def regex node_set, regex
      #       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ }
      #     end
      #   }.new);Fi[	@™9@—i[[	@µiÖI"ê      #       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ }
      #     end
      #   }.new)
      #
      def function(var);Fi[	@µiØI"æ      #       node_set.find_all { |node| node['some_attribute'] =~ /#{regex}/ }
      #     end
      #   }.new)
      #
      # Note that the CSS query string is case-sensitive with regards;Fi[	@πC@∞i[[	@µiÔI"√      
      ###
      # Add +node_or_tags+ as a child of this Node.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #;Fi[	@µi I"√      
      ###
      # Add +node_or_tags+ as a child of this Node.
      # +node_or_tags+ can be a Nokogiri::XML::Node, a ::DocumentFragment, a ::NodeSet, or a string containing markup.
      #;Fi[	@..@{:i[[	@©i,I"T  end
  
  module Helpers
    module CaptureHelper
      def function(var, var);Fi[	@FiiI"aend
module ActionView
  module Helpers
    module CaptureHelper
      def function(var, var);Fi[	@>I"@f.plist_startup ? ((var << "STRING")) : ((var << "STRING"));Fi[[	@˚i=I"§          (var << "STRING")
        end
        (var << "STRING")
        f.plist_startup ? ((var << "STRING")) : ((var << "STRING"))
        if f.plist_manual then;Fi[	@˚iOI"à          end
        else
          (var << "STRING")
          f.plist_startup ? ((var << "STRING")) : ((var << "STRING"))
        end;Fi[	@ E@ri[[	@µi¯I"ù        var = coerce(var)
        if var.is_a?(XML::NodeSet) then
          var.each { |var| add_child_node(var) }
        else
          add_child_node(var);Fi[	@µiTI"õ        children.unlink
        if var.is_a?(XML::NodeSet) then
          var.each { |var| add_child_node(var) }
        else
          add_child_node(var);Fi[	@>@”;i[[	@˚i5I"∂      if var.file?.! and var.symlink?.! then
        if f.plist_startup then
          (var << "STRING")
          (var << "STRING") unless var.directory?
          (var << "STRING");Fi[	@˚i9I"í          (var << "STRING")
        else
          (var << "STRING")
          (var << "STRING") unless var.directory?
          (var << "STRING");Fi[	@>@ài[[	@≤iüI"z        (var << "STRING")
        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        var
      end;Fi[	@≤iØI"z        (var << "STRING")
        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        var
      end;Fi[	I"if text? then;FI"6var = Nokogiri::XML::Node.new("STRING", document);Fi[[	@µicI"∫      # Also see related method +swap+.
      def function(var)
        if text? then
          var = Nokogiri::XML::Node.new("STRING", document)
          add_previous_sibling_node(var);Fi[	@µi5I"≤        var = coerce(var)
        if var.is_a?(XML::NodeSet) then
          if text? then
            var = Nokogiri::XML::Node.new("STRING", document)
            send(var, var);Fi[	@4%@õi[[	@µi«I"æ      #   doc.to_xml(:indent => 5, :encoding => 'UTF-8')
      #
      # See Node#write_to for a list of +options+
      def function(var)
        var["LITERAL"] ||= SaveOptions::DEFAULT_XML;Fi[	@µi“I"ø      #   doc.to_xhtml(:indent => 5, :encoding => 'UTF-8')
      #
      # See Node#write_to for a list of +options+
      def function(var)
        to_format(SaveOptions::DEFAULT_XHTML, var);Fi[	@4%@äi[[	@µi˚I"≈      # Write Node as HTML to +io+ with +options+
      #
      # See Node#write_to for a list of +options+
      def function(var, var)
        write_format_to(SaveOptions::DEFAULT_HTML, var, var);Fi[	@µiI"«      # Write Node as XHTML to +io+ with +options+
      #
      # See Node#write_to for a list of +options+
      def function(var, var)
        write_format_to(SaveOptions::DEFAULT_XHTML, var, var);Fi[	@>@Â%i[[	@≤iùI"¢        var = "STRING"
        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        (var << "STRING") if var["LITERAL"]
        (var << "STRING");Fi[	@≤i≠I"¢        var = "STRING"
        (var << "STRING") if var["LITERAL"]
        (var << "STRING")
        (var << "STRING") if var["LITERAL"]
        (var << "STRING");Fi[	@¯!@1;i[[	@µi]I"€      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      var, var = valparse(var)
      (var << var)
      return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      loop do;Fi[	@µibI"∞        var = eat("STRING", var)
        var, var = valparse(var)
        (var << var)
        return [var, var["LITERAL"]] if (var["LITERAL"]["LITERAL"] == "STRING")
      end;Fi[	@¯!@i=i[[	@$i‡I"|    end
    (var << var) if var.exist?.!
    (var << var)
    ((var << "STRING") << var) if var
    (var << "STRING") if var;Fi[	@$i˙I"©    var = [@@svn, var, "STRING", "STRING", "STRING"]
    (var << var) if var.exist?.!
    (var << var)
    ((var << "STRING") << var) if var
    (var << "STRING") if var;Fi[	I"# :nodoc:;F@Ói[[	@ªiI"S        end
        
        # :nodoc:
        def function
          "STRING";Fi[	@ti	I"~    module PP
      module Node
        # :nodoc:
        def function
          var = inspect_attributes.reject do |var|;Fi[	I"0var.group("LITERAL", "STRING", "STRING") do;FI"var.breakable;Fi[[	@tiI"        def function(var)
          var = self.class.name.split("STRING").last
          var.group("LITERAL", "STRING", "STRING") do
            var.breakable
            var = inspect_attributes.map { |var| [var, send(var)] if respond_to?(var) }.compact.find_all do |var|;Fi[	@ti#I"             var.seplist(var) do |var|
              if ["LITERAL", "LITERAL"].include?(var.first) then
                var.group("LITERAL", "STRING", "STRING") do
                  var.breakable
                  var.seplist(var.last) { |var| var.pp(var) };Fi[	@X5I"cvar = (var.map { |var, var| [["STRING", var].compact.join("STRING"), var] } + var.map do |var|;Fi[[	@Äi3I"b        # +ns+ is a hash of namespace prefix:urls associated with the element
        def function(var, var, var, var, var)
          var = [var, var].compact.join("STRING")
          var = (var.map { |var, var| [["STRING", var].compact.join("STRING"), var] } + var.map do |var|
            [[var.prefix, var.localname].compact.join("STRING"), var.value];Fi[	@ªi1I"      
      def function(var, var, var, var, var)
        var = [var, var].compact.join("STRING")
        var = (var.map { |var, var| [["STRING", var].compact.join("STRING"), var] } + var.map do |var|
          [[var.prefix, var.localname].compact.join("STRING"), var.value];Fi[	@ÉEI"D[[var.prefix, var.localname].compact.join("STRING"), var.value];Fi[[	@Äi4I"#        def function(var, var, var, var, var)
          var = [var, var].compact.join("STRING")
          var = (var.map { |var, var| [["STRING", var].compact.join("STRING"), var] } + var.map do |var|
            [[var.prefix, var.localname].compact.join("STRING"), var.value]
          end);Fi[	@ªi2I"!      def function(var, var, var, var, var)
        var = [var, var].compact.join("STRING")
        var = (var.map { |var, var| [["STRING", var].compact.join("STRING"), var] } + var.map do |var|
          [[var.prefix, var.localname].compact.join("STRING"), var.value]
        end.flatten);Fi[	@¥<@ºDi[[	@Éi.I"®        # Create a new Parser with +doc+ and +encoding+
        def function(var, var)
          check_encoding(var)
          @encoding = var
          @document = var;Fi[	@ÉiBI"ß        # Parse given +io+
        def function(var, var)
          check_encoding(var)
          @encoding = var
          var = ParserContext.io(var, ENCODINGS[var]);Fi[	I"require("hashie/mash");F@±+i[[	I".projects//omni_proj/omniauth/auth_hash.rb;Ti I"Krequire("hashie/mash")
module OmniAuth
  class AuthHash < Hashie::Mash;Fi [	@¡iI"frequire("omniauth")
require("hashie/mash")
module OmniAuth
  class NoSessionError < StandardError;Fi[	@¯!@Ôi[[	@Ùi]I"í        var = Array.new
        var.split("STRING").each do |var|
          (var << var)
          begin
            var.mkdir(var.join("STRING"));Fi[	@ÆiQI"ì        var = Array.new
        var.split("STRING").each do |var|
          (var << var)
          begin
            var.mkdir!(var.join("STRING"));Fi[	I")return "STRING" unless env["STRING"];F@Ôi[[	I"5projects//omni_proj/omniauth/failure_endpoint.rb;Ti!I"\    
    def function
      return "STRING" unless env["STRING"]
      "STRING"
    end;Fi[	@®Ei&I"\    
    def function
      return "STRING" unless env["STRING"]
      "STRING"
    end;Fi[	@≠<I"input_field("STRING", var);Fi[[	@æi&I"l    
    def function(var, var)
      label_field(var, var)
      input_field("STRING", var)
      self;Fi[	@æi,I"l    
    def function(var, var)
      label_field(var, var)
      input_field("STRING", var)
      self;Fi[	@¯!@ri[[	@di8I"∞          end
          if block_given? and var = yield(var, var) then
            (var << var)
          else
            (var << serialize_property(var, var)) unless var.nil?;Fi[	@ÚiQI"å    var.children.sort.each do |var|
      if var.directory? then
        (var << var)
      else
        if block_given? and yield(var) then;Fi[	@÷+@—i[[	@¡i(I"s      #
      #   class MyStrategy
      #     include OmniAuth::Strategy
      #
      #     configure do |c|;Fi[	@¡iDI"u      #
      #   class MyStrategy
      #     include OmniAuth::Strategy
      #
      #     option :foo, 'bar';Fi[	@Ê$I"log("LITERAL", "STRING");Fi[[	@¡i•I"µ    # Performs the steps necessary to run the request phase of a strategy.
    def function
      setup_phase
      log("LITERAL", "STRING")
      session["STRING"] = request.params;Fi[	@¡iæI"Ω    # Performs the steps necessary to run the callback phase of a strategy.
    def function
      setup_phase
      log("LITERAL", "STRING")
      @env["STRING"] = session.delete("STRING");Fi[	I".@env["STRING"] = session.delete("STRING");FI"8@env["STRING"] = nil if (env["STRING"] == "STRING");Fi[[	@¡i¿I"“      setup_phase
      log("LITERAL", "STRING")
      @env["STRING"] = session.delete("STRING")
      @env["STRING"] = nil if (env["STRING"] == "STRING")
      @env["STRING"] = (session.delete("STRING") or {});Fi[	@¡iI"÷        @env["STRING"] = var
        @env["STRING"] = (session.delete("STRING") or {})
        @env["STRING"] = session.delete("STRING")
        @env["STRING"] = nil if (env["STRING"] == "STRING")
        call_app!;Fi[	@æ;@ái[[	@
i8I"r      def function
        super
        var = Pipeline.new
        var = "STRING"
        (var << mysqldump);Fi[	@
i8I"o      def function
        super
        var = Pipeline.new
        var = "STRING"
        (var << pgdump);Fi[	I")require("resque/failure/thoughtbot");F@¡i[[	@≈i
I"^  raise("STRING")
end
require("resque/failure/thoughtbot")
module Resque
  module Failure;Fi[	@ i
I"^  raise("STRING")
end
require("resque/failure/thoughtbot")
module Resque
  module Failure;Fi[	I"@queue = var;FI"@payload = var;Fi[[	@Î
iI"i        @exception = var
        @worker = var
        @queue = var
        @payload = var
      end;Fi[	@hiI"è    # This job's associated payload object.
    def function(var, var)
      @queue = var
      @payload = var
      @failure_hooks_ran = false;Fi[	I"rescue Object => var;FI"!Resque.logger.info("STRING");Fi[[	@«i£I"É        var.perform
        run_hook("LITERAL", var))
      rescue Object => var
        Resque.logger.info("STRING")
        begin;Fi[	@«ißI"z        begin
          var.fail(var)
        rescue Object => var
          Resque.logger.info("STRING")
        end;Fi[	I"require("redis");FI"require("redis-namespace");Fi[[	@\<i I"Brequire("redis")
require("redis-namespace")
require("thread");Fi [	@_<i I"Brequire("redis")
require("redis-namespace")
require("thread");Fi [	@ÌEI"require("thread");Fi[[	@\<iI"Urequire("redis")
require("redis-namespace")
require("thread")
require("mutex_m");Fi[	@_<iI"Urequire("redis")
require("redis-namespace")
require("thread")
require("mutex_m");Fi[	@ÙEI"require("mutex_m");Fi[[	@\<iI"crequire("redis")
require("redis-namespace")
require("thread")
require("mutex_m")
module Resque;Fi[	@_<iI"crequire("redis")
require("redis-namespace")
require("thread")
require("mutex_m")
module Resque;Fi[	@˚E@¡i[[	@\<iI"erequire("redis-namespace")
require("thread")
require("mutex_m")
module Resque
  class MultiQueue;Fi[	@_<iI"xrequire("redis-namespace")
require("thread")
require("mutex_m")
module Resque
  class QueueDestroyed < RuntimeError;Fi[	@£BI"$# pop, a ThreadError is raised.;Fi[[	@\<iI"‹    # is available. This method returns a tuple of the queue object and job.
    #
    # Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking
    # pop, a ThreadError is raised.
    def function(var);Fi[	@_<iAI"†    # available.
    #
    # Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking
    # pop, a ThreadError is raised.
    def function(var);Fi[	@F@õi[[	@\<iI"°    #
    # Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking
    # pop, a ThreadError is raised.
    def function(var)
      if var then;Fi[	@_<iBI"°    #
    # Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking
    # pop, a ThreadError is raised.
    def function(var)
      if var then;Fi[	@Y<I"until var do;Fi[[	@\<i0I"¥        var = @queues.map { |var| var.redis_name }
        if var.any? then
          synchronize do
            until var do
              var = @redis.blpop(*(var + ["LITERAL"]));Fi[	@_<iKI"{        end
      else
        synchronize do
          until var do
            var = @redis.blpop(@redis_name, "LITERAL");Fi[	I"/var = @queues.map { |var| var.redis_name };FI"if var.any? then;Fi[[	@\<i.I"{        end
      else
        var = @queues.map { |var| var.redis_name }
        if var.any? then
          synchronize do;Fi[	@\<iCI"®    # the timeout expires.
    def function(var)
      var = @queues.map { |var| var.redis_name }
      if var.any? then
        var, var = @redis.blpop(*(var + [var]));Fi[	@á@!Ci[[	@0i:I"Ω      when "STRING" then
        var = (var["STRING"] or var["STRING"])
        var = "STRING"
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?
      when "STRING" then;Fi[	@0iOI"™      when "STRING" then
        var = var["STRING"]
        var = "STRING"
        (var << ("STRING" + var["STRING"])) unless var["STRING"].nil?
      when "STRING" then;Fi[	I"'alias_method("LITERAL", "LITERAL");F@Ói[[	@3iI"≈    helpers do
      include(Rack::Utils)
      alias_method("LITERAL", "LITERAL")
      def function
        url_path(request.path_info.sub("STRING", "STRING").split("STRING")["LITERAL"].downcase);Fi[	@3i$I"ü        [path_prefix, var].join("STRING").squeeze("STRING")
      end
      alias_method("LITERAL", "LITERAL")
      def function
        request.env["STRING"];Fi[	I" case Resque.redis.type(var);F@W
i[[	@3i4I"r      end
      def function(var)
        case Resque.redis.type(var)
        when "STRING" then
          [];Fi[	@3iFI"w      end
      def function(var, var)
        case Resque.redis.type(var)
        when "STRING" then
          [];Fi[	@Ò@ri[[	@ iÛI"ê    var = queue(var).slice(var, var)
    if var.nil? then
      []
    else
      var.respond_to?("LITERAL") ? ((var.to_ary or [var])) : ([var]);Fi[	@oiwI"∫    def function
      @transitions ||= if empty? then
        []
      else
        machine.events.transitions_for(object, "LITERAL" => (to_name), "LITERAL" => (@guard)).select do |var|;Fi[	@Ò@W
i[[	@3i6I"á        case Resque.redis.type(var)
        when "STRING" then
          []
        when "STRING" then
          Resque.redis.llen(var);Fi[	@3iHI"°        case Resque.redis.type(var)
        when "STRING" then
          []
        when "STRING" then
          Resque.redis.lrange(var, var, (var + "LITERAL"));Fi[	I"namespace("LITERAL") do;F@Z"i[[	@a"i I"Bnamespace("LITERAL") do
  desc("STRING")
  task("LITERAL") do;Fi [	@d"iI"É  _cset("LITERAL") { "STRING" }
  _cset("LITERAL") { "STRING" }
  namespace("LITERAL") do
    desc("STRING")
    task("LITERAL") do;Fi[	@á@Ò:i[[	@~i˝I"—    # @return [String] The sanitized string
    def function(var)
      var = "STRING"
      var = if (RUBY_VERSION >= "STRING") then
        Regexp.new(var.force_encoding(var.encoding), Regexp::FIXEDENCODING);Fi[	@~iI"—    # @return [String] The sanitized string
    def function(var)
      var = "STRING"
      var = if (RUBY_VERSION >= "STRING") then
        Regexp.new(var.force_encoding(var.encoding), Regexp::FIXEDENCODING);Fi[	@áI"Ovar = (var + "STRING") unless ["STRING", "STRING"].include?(var.port.to_s);Fi[[	@aiΩI"ª    begin
      (var = URI.parse(@attributes["STRING"])
      var = "STRING"
      var = (var + "STRING") unless ["STRING", "STRING"].include?(var.port.to_s)
      var = (var + "STRING"));Fi[	@ai#I"©  def function(var)
    var = URI.parse(var)
    var = "STRING"
    var = (var + "STRING") unless ["STRING", "STRING"].include?(var.port.to_s)
    var = (var + "STRING");Fi[	@á@¬1i[[	@w$i#I"{        end
      end
      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var));Fi[	@w$i<I"ò        Compass.configuration.http_fonts_path
      end
      var = "STRING"
      if var.to_bool then
        Sass::Script::String.new(clean_path(var));Fi[	@áI"0raise(Compass::FilesystemConflict.new(var));Fi[[	@iI"∫      unless File.exists?(var) and File.directory?(var) then
        if File.exists?(var) then
          var = "STRING"
          raise(Compass::FilesystemConflict.new(var))
        else;Fi[	@i1I"•            log_action("LITERAL", basename(var), var)
          else
            var = "STRING"
            raise(Compass::FilesystemConflict.new(var))
          end;Fi[	@á@Ôi[[	@{iFI"{      (var.map do |var|
        var = "STRING"
        var = "STRING"
        "STRING"
      end.join("STRING") + "STRING");Fi[	@T
iI"odef function(var)
  var = File.dirname(serializer.serialized_path(object))
  var = "STRING"
  "STRING"
end;Fi[	I"var.worker = self;FI"working_on(var);Fi[[	@«ioI"≠          if var = reserve(var) then
            Resque.logger.info("STRING")
            var.worker = self
            working_on(var)
            if (@child = fork(var) do;Fi[	@«iîI"o          return
        end
        var.worker = self
        working_on(var)
        perform(var, &var));Fi[	I"procline("STRING");FI"reconnect;Fi[[	@«isI"®            if (@child = fork(var) do
              unregister_signal_handlers
              procline("STRING")
              reconnect
              perform(var, &var);Fi[	@«i{I"©              var.fail(DirtyExit.new($?.to_s)) if $?.signaled?
            else
              procline("STRING")
              reconnect
              perform(var, &var);Fi[	@tFI"perform(var, &var);Fi[[	@«itI"ò              unregister_signal_handlers
              procline("STRING")
              reconnect
              perform(var, &var)
            end) then;Fi[	@«i|I"            else
              procline("STRING")
              reconnect
              perform(var, &var)
            end;Fi[	I""Resque.logger.debug("STRING");FI"#Process.kill("STRING", @child);Fi[[	@«i;I"Œ            Resque.logger.debug("STRING")
          else
            Resque.logger.debug("STRING")
            Process.kill("STRING", @child)
            (term_timeout.to_f * "LITERAL").round.times do |var|;Fi[	@«iAI"¥              return if Process.waitpid(@child, Process::WNOHANG)
            end
            Resque.logger.debug("STRING")
            Process.kill("STRING", @child)
          end;Fi[	I"(Stat << "STRING");F@äFi[[	@«iÆI"w    # Tell Redis we've processed a job.
    def function
      (Stat << "STRING")
      (Stat << "STRING")
    end;Fi[	@«iπI"u    # Tells Redis we've failed a job.
    def function
      (Stat << "STRING")
      (Stat << "STRING")
    end;Fi[	@á@Â%i[[	@≤iõI"ﬂ        var = var["LITERAL"] ? ("STRING") : ("STRING")
        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING")
        var = "STRING"
        (var << "STRING") if var["LITERAL"]
        (var << "STRING");Fi[	@≤i´I"ﬂ        var = var["LITERAL"] ? ("STRING") : ("STRING")
        (var["LITERAL"] == "LITERAL") ? (var = "STRING") : (var = "STRING")
        var = "STRING"
        (var << "STRING") if var["LITERAL"]
        (var << "STRING");Fi[	@®,@Ói[[	@«i˝I"¡    
    # Returns an Array of string pids of all the other workers on this
    # machine. Useful when pruning dead workers on startup.
    def function
      if RUBY_PLATFORM =~ "LITERAL" then;Fi[	@«i	I"Ï    #
    # Returns an Array of string pids of all the other workers on this
    # machine. Useful when pruning dead workers on startup.
    def function
      `tasklist  /FI \"IMAGENAME eq ruby.exe\" /FO list`.split($/).select do |var|;Fi[	@á@>i[[	@Ai'I"¨      link_to("STRING", post_comments_path(var, "LITERAL" => "STRING"), "LITERAL" => "STRING")
    else
      var = "STRING"
      (var << "STRING")
      (var << "STRING");Fi[	@√igI"}class Formula
  def function
    var = "STRING"
    (var << "STRING")
    if (lib.directory? or include.directory?) then;Fi[	@-@@@i[[	@liSI"∏        var = File.join(var, "STRING")
        var = File.join(var, "STRING")
        FileUtils.mkdir_p(var)
        if overwrite?(var) then
          File.open(var, "STRING") do |var|;Fi[	@limI"ù        end
        var = File.join(var, "STRING")
        FileUtils.mkdir_p(var)
        if overwrite?(var) then
          File.open(var, "STRING") do |var|;Fi[	I"G# It assumes the class you're passing it is a real Ruby class (not;FI"+# a string or reference) which either:;Fi[[	@ iI"∑  #
  # This method can be used to conveniently add a job to a queue.
  # It assumes the class you're passing it is a real Ruby class (not
  # a string or reference) which either:
  #;Fi[	@ i?I"ª  
  # This method can be used to conveniently remove a job from a queue.
  # It assumes the class you're passing it is a real Ruby class (not
  # a string or reference) which either:
  #;Fi[	@™F@—i[[	@ iI"“  # This method can be used to conveniently add a job to a queue.
  # It assumes the class you're passing it is a real Ruby class (not
  # a string or reference) which either:
  #
  #   a) has a @queue ivar set;Fi[	@ i@I"◊  # This method can be used to conveniently remove a job from a queue.
  # It assumes the class you're passing it is a real Ruby class (not
  # a string or reference) which either:
  #
  #   a) has a @queue ivar set;Fi[	@rBI"#   b) responds to `queue`;Fi[[	@ iI"q  # a string or reference) which either:
  #
  #   a) has a @queue ivar set
  #   b) responds to `queue`
  #;Fi[	@ iBI"q  # a string or reference) which either:
  #
  #   a) has a @queue ivar set
  #   b) responds to `queue`
  #;Fi[	@∑F@—i[[	@ iI"ç  #
  #   a) has a @queue ivar set
  #   b) responds to `queue`
  #
  # If either of those conditions are met, it will use the value obtained;Fi[	@ iCI"ç  #
  #   a) has a @queue ivar set
  #   b) responds to `queue`
  #
  # If either of those conditions are met, it will use the value obtained;Fi[	@kBI"J# from performing one of the above operations to determine the queue.;Fi[[	@ iI"∂  #   b) responds to `queue`
  #
  # If either of those conditions are met, it will use the value obtained
  # from performing one of the above operations to determine the queue.
  #;Fi[	@ iEI"∂  #   b) responds to `queue`
  #
  # If either of those conditions are met, it will use the value obtained
  # from performing one of the above operations to determine the queue.
  #;Fi[	@ƒF@—i[[	@ i I"È  #
  # If either of those conditions are met, it will use the value obtained
  # from performing one of the above operations to determine the queue.
  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`;Fi[	@ iFI"È  #
  # If either of those conditions are met, it will use the value obtained
  # from performing one of the above operations to determine the queue.
  #
  # If no queue can be inferred this method will raise a `Resque::NoQueueError`;Fi[	@ÓI".return unless @output.has_key?("LITERAL");Fi[[	@ÁiI"ü      protected
      
      def function
        return unless @output.has_key?("LITERAL")
        @output["LITERAL"].nil? ? ("STRING") : (@output["LITERAL"]);Fi[	@Ái!I"ô      end
      
      def function
        return unless @output.has_key?("LITERAL")
        @output["LITERAL"].nil? ? ("STRING") : (@output["LITERAL"]);Fi[	@dBI"# before_enqueue hook.;Fi[[	@ i$I"π  # If no queue can be inferred this method will raise a `Resque::NoQueueError`
  #
  # Returns true if the job was queued, nil if the job was rejected by a
  # before_enqueue hook.
  #;Fi[	@ i1I"Æ  # `queue` should be the String name of the queue you're targeting.
  #
  # Returns true if the job was queued, nil if the job was rejected by a
  # before_enqueue hook.
  #;Fi[	@ÿF@—i[[	@ i%I"°  #
  # Returns true if the job was queued, nil if the job was rejected by a
  # before_enqueue hook.
  #
  # This method is considered part of the `stable` API.;Fi[	@ i2I"°  #
  # Returns true if the job was queued, nil if the job was rejected by a
  # before_enqueue hook.
  #
  # This method is considered part of the `stable` API.;Fi[	@§%@äi[[	@ i'I"û  # before_enqueue hook.
  #
  # This method is considered part of the `stable` API.
  def function(var, var)
    enqueue_to(queue_from_class(var), var, *var);Fi[	@ iXI"Î  #   Resque.dequeue(GitHub::Jobs::UpdateNetworkGraph, 'repo:135325')
  #
  # This method is considered part of the `stable` API.
  def function(var, var)
    var = Plugin.before_dequeue_hooks(var).collect { |var| var.send(var, *var) };Fi[	@ÓI"var = @attrs["LITERAL"];Fi[[	@Ãi#I"f    
    # @return [Integer]
    def function
      var = @attrs["LITERAL"]
      var.to_i if var;Fi[	@Ãi^I"f    
    # @return [Integer]
    def function
      var = @attrs["LITERAL"]
      var.to_i if var;Fi[	@ÓI"A@sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|;Fi[[	@9iI"ª      #
      # @return [Array<Twitter::Size>]
      def function
        @sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|
          var[var] = Twitter::Size.fetch_or_new(var);Fi[	@ciI"±    #
    # @return [Array<Twitter::Size>]
    def function
      @sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|
        var[var] = Twitter::Size.fetch_or_new(var);Fi[	@ÓI"/if owner.is_a?(StateMachine::Machine) then;Fi[[	@`iI"ë        handles(method_call("LITERAL"))
        
        def function
          if owner.is_a?(StateMachine::Machine) then
            var = self;Fi[	@eiI"ë        handles(method_call("LITERAL"))
        
        def function
          if owner.is_a?(StateMachine::Machine) then
            var = self;Fi[	@ÓI"var = self.name;Fi[[	@A
iãI"•      
      # Skips defining reader/writer methods since this is done automatically
      def function
        var = self.name
        if supports_validations? then;Fi[	@M
iNI"¿      
      # Skips defining reader/writer methods since this is done automatically
      def function
        var = self.name
        owner_class.validates_each(attribute) do |var, var, var|;Fi[	@Ó@äFi[[	@«i≠I"t    
    # Tell Redis we've processed a job.
    def function
      (Stat << "STRING")
      (Stat << "STRING");Fi[	@«i∏I"r    
    # Tells Redis we've failed a job.
    def function
      (Stat << "STRING")
      (Stat << "STRING");Fi[	I"R# configured for this branch.  In addition to matching the event, from state,;FI"L# and to state, this will also check whether the configured :if/:unless;Fi[[	@˛
i:I"    
    # Determines whether the given object / query matches the requirements
    # configured for this branch.  In addition to matching the event, from state,
    # and to state, this will also check whether the configured :if/:unless
    # conditions pass on the given object.;Fi[	@˛
iUI"    
    # Attempts to match the given object / query against the set of requirements
    # configured for this branch.  In addition to matching the event, from state,
    # and to state, this will also check whether the configured :if/:unless
    # conditions pass on the given object.;Fi[	@GI"+# conditions pass on the given object.;Fi[[	@˛
i;I"    # Determines whether the given object / query matches the requirements
    # configured for this branch.  In addition to matching the event, from state,
    # and to state, this will also check whether the configured :if/:unless
    # conditions pass on the given object.
    # ;Fi[	@˛
iVI"     # Attempts to match the given object / query against the set of requirements
    # configured for this branch.  In addition to matching the event, from state,
    # and to state, this will also check whether the configured :if/:unless
    # conditions pass on the given object.
    # ;Fi[	@G@—i[[	@˛
i<I"·    # configured for this branch.  In addition to matching the event, from state,
    # and to state, this will also check whether the configured :if/:unless
    # conditions pass on the given object.
    # 
    # == Examples;Fi[	@˛
iWI"    # configured for this branch.  In addition to matching the event, from state,
    # and to state, this will also check whether the configured :if/:unless
    # conditions pass on the given object.
    # 
    # If a match is found, then the event/state requirements that the query;Fi[	@WB@—i[[	@˛
i@I"Ñ    # == Examples
    # 
    #   branch = StateMachine::Branch.new(:parked => :idling, :on => :ignite)
    #   
    #   # Successful;Fi[	@˛
iiI"…    # == Examples
    # 
    #   branch = StateMachine::Branch.new(:parked => :idling, :on => :ignite)
    #   
    #   branch.match(object, :on => :ignite)  # => {:to => ..., :from => ..., :on => ...};Fi[	@Ó@ÂEi[[	@«i%I"°    # Schedule this worker for shutdown. Will finish processing the
    # current job.
    def function
      Resque.logger.info("STRING")
      @shutdown = true;Fi[	@«i^I"Ø    # Stop processing jobs after the current one has completed (if we're
    # currently running one).
    def function
      Resque.logger.info("STRING")
      @paused = true;Fi[	@ÓI"footer;Fi[[	@æiNI";    end
    
    def function
      footer
      @html;Fi[	@æiSI"V    end
    
    def function
      footer
      Rack::Response.new(@html).finish;Fi[	I"# transition.;F@—i[[	@˛
i}I"∂    # 
    # Each edge will be labeled with the name of the event that would cause the
    # transition.
    # 
    # The collection of edges generated on the graph will be returned.;Fi[	@%iEI"        # Creates a callback that will be invoked *after* a transition is
        # performed so long as the given configuration options match the
        # transition.
        # 
        # See +before_transition+ for a description of the possible configurations;Fi[	@Ó@•Ei[[	@®Ei I"\    end
    
    def function
      return "STRING" unless env["STRING"]
      "STRING";Fi[	@®Ei%I"\    end
    
    def function
      return "STRING" unless env["STRING"]
      "STRING";Fi[	@PB@—i[[	@i"I"ô    #   end
    # 
    #   person = Person.new('John Smith')
    #   
    #   evaluate_method(person, :name)                            # => "John Smith";Fi[	@i2I"æ    # For example,
    # 
    #   person = Person.new('John Smith')
    #   
    #   evaluate_method(person, lambda {|person| person.name}, 21)                              # => "John Smith";Fi[	@ÓI"(return nil if (length == "LITERAL");Fi[[	@∏iI"≤      # Removes the last element from set and returns it, or +nil+ if
      # the set is empty
      def function
        return nil if (length == "LITERAL")
        delete(last);Fi[	@∏iI"¡      # Returns the first element of the NodeSet and removes it.  Returns
      # +nil+ if the set is empty.
      def function
        return nil if (length == "LITERAL")
        delete(first);Fi[	@0-@7 i[[	@`i4I"¿    def function(var, var, var)
      assert_valid_keys(var, "LITERAL")
      @machine = var
      @name = var
      @qualified_name = var.namespace ? (:"#{var}STRING#{var.namespace}") : (var);Fi[	@}i=I"Ù    def function(var, var, var)
      assert_valid_keys(var, "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL")
      @machine = var
      @name = var
      @qualified_name = var and var.namespace ? (:"#{var.namespace}STRING#{var}") : (var);Fi[	I"# example:;F@—i[[	@`i\I"«    # Since this transition is being defined within an event context, you do
    # *not* need to specify the <tt>:on</tt> option for the transition.  For
    # example:
    # 
    #  state_machine do;Fi[	@`i!I"…    # Since this transition is being defined within a state context, you do
    # *not* need to specify the <tt>:from</tt> option for the transition.  For
    # example:
    # 
    #   state_machine do;Fi[	@IBI"/# configurations for defining transitions.;Fi[[	@`ieI"£    #  end
    # 
    # See StateMachine::Machine#transition for a description of the possible
    # configurations for defining transitions.
    def function(var);Fi[	@`i*I"§    #   end
    # 
    # See StateMachine::Machine#transition for a description of the possible
    # configurations for defining transitions.
    def function(var);Fi[	@\G@õi[[	@`ifI"À    # 
    # See StateMachine::Machine#transition for a description of the possible
    # configurations for defining transitions.
    def function(var)
      raise(ArgumentError, "STRING") if var.empty?;Fi[	@`i+I"Ï    # 
    # See StateMachine::Machine#transition for a description of the possible
    # configurations for defining transitions.
    def function(var)
      assert_valid_keys(var, "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL");Fi[	@ÓI"("STRING" + "STRING");Fi[[	@Úi`I"Ñ      ##
      # Builds the full mongodump string based on all attributes
      def function
        ("STRING" + "STRING")
      end;Fi[	@
iNI"Ñ      ##
      # Builds the full mysqldump string based on all attributes
      def function
        ("STRING" + "STRING")
      end;Fi[	@ÓI"&var = self.site.posts.index(self);Fi[[	@ôi„I"à    end
    
    def function
      var = self.site.posts.index(self)
      if var and (var < (self.site.posts.length - "LITERAL")) then;Fi[	@ôiÏI"î    end
    
    def function
      var = self.site.posts.index(self)
      var and (var > "LITERAL") ? (self.site.posts[(var - "LITERAL")]) : (nil);Fi[	@-I"M# * <tt>:on</tt> - One or more events that fire the transition.  If none;Fi[[	@iI"L    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.;Fi[	@i6I"L    #   are specified, then this will be the object's current state.
    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.;Fi[	@Ó@]Ci[[	@ñiKI"q    #
    # Returns the String url.
    def function
      return @url if @url
      var = if permalink then;Fi[	@ôiÑI"q    #
    # Returns the String URL.
    def function
      return @url if @url
      var = if permalink then;Fi[	@%I"*#   conditionals defined for each one;Fi[[	@oiI"Ô    # Configuration options:
    # * <tt>:target</tt> - The target state to end the path on
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one
    def function(var, var, var);Fi[	@viI"    # * <tt>:to</tt> - The target end state
    # * <tt>:deep</tt> - Whether to enable deep searches for the target state.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one
    def function(var, var, var);Fi[	@Ó@/Ai[[	@ëiAI"V  end
  
  def function
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do;Fi[	@ëiUI"V  end
  
  def function
    var = fixture("STRING")
    HOMEBREW_REPOSITORY.cd do;Fi[	I"2#   node's label that gets drawn on the graph;F@äi[[	@`i∫I"Ù    # Configuration options:
    # * <tt>:human_name</tt> - Whether to use the event's human name for the
    #   node's label that gets drawn on the graph
    def function(var, var)
      var = machine.states.by_priority.map { |var| var.name };Fi[	@}i—I"r    # Configuration options:
    # * <tt>:human_name</tt> - Whether to use the state's human name for the
    #   node's label that gets drawn on the graph
    def function(var, var)
      var = var.add_node((name ? (name.to_s) : ("STRING")), "LITERAL" => (description(var)), "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => (final? ? ("STRING") : ("STRING")));Fi[	@wGI"7#   are specified, then this will match any event.;Fi[[	@iI"Q    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless;Fi[	@i7I"Q    # * <tt>:to</tt> - One or more states being transitioned to.  If none are
    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless;Fi[	@òG@%i[[	@iI"@    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.;Fi[	@i8I"@    #   specified, then this will match any to state.
    # * <tt>:on</tt> - One or more events that fire the transition.  If none
    #   are specified, then this will match any event.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one.  Default is true.;Fi[	@Ó@˝@i[[	@9$i/I"g  end
  
  def function
    ENV.expects("LITERAL").yields.returns(true)
    ENV.expects("LITERAL");Fi[	@9$i9I"g  end
  
  def function
    ENV.expects("LITERAL").yields.returns(true)
    ENV.expects("LITERAL");Fi[	@Ó@Æ@i[[	@i!I"y  end
  
  def function
    var = Patches.new("LITERAL" => "STRING")
    assert_equal("LITERAL", var.patches.length);Fi[	@i(I"y  end
  
  def function
    var = Patches.new("LITERAL" => "STRING")
    assert_equal("LITERAL", var.patches.length);Fi[	@ÓI"@keg.link;Fi[[	@iI"S  end
  
  def function
    @keg.link
    assert_equal("LITERAL", @keg.unlink);Fi[	@i(I"a  end
  
  def function
    @keg.link
    assert_raise(RuntimeError, "STRING") { @keg.link };Fi[	@Ó@‘?i[[	@niæI"d  end
  
  def function
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head);Fi[	@niÕI"d  end
  
  def function
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head);Fi[	@{@{i[[	@iI"í    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :park do
    #         transition :idling => :parked;Fi[	@i?I"í    # 
    #   class Vehicle
    #     state_machine :initial => :parked do
    #       event :park do
    #         transition :idling => :parked;Fi[	@Ó@°?i[[	@niyI"O  end
  
  def function
    ARGV.push("STRING")
    var = SpecTestBall.new;Fi[	@niI"O  end
  
  def function
    ARGV.push("STRING")
    var = SpecTestBall.new;Fi[	@ÓI""@d.add("LITERAL" => "STRING");Fi[[	@6$i5I"a  end
  
  def function
    @d.add("LITERAL" => "STRING")
    @d.add("LITERAL" => "LITERAL");Fi[	@6$iAI"Ñ  end
  
  def function
    @d.add("LITERAL" => "STRING")
    assert_equal("STRING", @d.find_requirement(X11Dependency).min_version);Fi[	@ÓI"@d.add("LITERAL");Fi[[	@6$i+I"i  end
  
  def function
    @d.add("LITERAL")
    assert_not_nil(@d.find_requirement(X11Dependency));Fi[	@6$i<I"l  end
  
  def function
    @d.add("LITERAL")
    assert_empty(@d.find_requirement(X11Dependency).tags);Fi[	@ÓI"((@deps << Dependency.new("STRING"));Fi[[	@0$iI"w  end
  
  def function
    (@deps << Dependency.new("STRING"))
    (@deps << Dependency.new("STRING", "LITERAL"));Fi[	@0$iI"l  end
  
  def function
    (@deps << Dependency.new("STRING"))
    (@deps << Dependency.new("STRING"));Fi[	@ÓI"var = mock("STRING");Fi[[	@§-i&I"a  end
  
  def function
    var = mock("STRING")
    (@env << Proc.new { var.some_message });Fi[	@§-i-I"g  end
  
  def function
    var = mock("STRING")
    (@env << Proc.new { |var| var.some_message });Fi[	@Ó@7>i[[	@[iI"G  end
  
  def function
    case @language
    when "LITERAL" then;Fi[	@[i-I"G  end
  
  def function
    case @language
    when "LITERAL" then;Fi[	I".#   events = Vehicle.state_machine.events;F@—i[[	@iJI"∞    #   end
    #   
    #   events = Vehicle.state_machine.events
    #   
    #   vehicle = Vehicle.new                             # => #<Vehicle:0xb7c464b0 @state="parked">;Fi[	@ikI"æ    #   
    #   vehicle = Vehicle.new                       # => #<Vehicle id: nil, state: "parked">
    #   events = Vehicle.state_machine.events
    #   
    #   vehicle.state_event = nil;Fi[	@ÓI"#return unless f.lib.directory?;Fi[[	@√i)I"  end
  
  def function
    return unless f.lib.directory?
    var = f.lib.children.select { |var| var.to_s =~ "LITERAL" };Fi[	@√i8I"≥  end
  
  def function
    return unless f.lib.directory?
    var = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	I"#       event :enable do;F@´-i[[	@iLI"Ø    #     
    #     state_machine :alarm_state, :namespace => 'alarm', :initial => :on do
    #       event :enable do
    #         transition all => :active
    #       end;Fi[	@i~I"≥    #     
    #     state_machine :alarm_state, :namespace => 'alarm', :initial => :active do
    #       event :enable do
    #         transition all => :active
    #       end;Fi[	@Ó@Í=i[[	@√iI"^  end
  
  def function
    if (f.prefix + "STRING").directory? then
      opoo("STRING");Fi[	@√i I"^  end
  
  def function
    if (f.prefix + "STRING").directory? then
      opoo("STRING");Fi[	@ÓI")raise("STRING") unless self.symlink?;Fi[[	@RiI"ä  end
  
  def function
    raise("STRING") unless self.symlink?
    system("STRING", "STRING", self.to_s, (self.dirname + "STRING").to_s);Fi[	@Ri"I"î  end
  
  def function
    raise("STRING") unless self.symlink?
    system("STRING", "STRING", "STRING", self.to_s, (self.dirname + "STRING").to_s);Fi[	@Ó@å=i[[	@˚imI"K  end
  
  def function
    nostdout do
      assert_nothing_raised do;Fi[	@niI"d  include(VersionAssertions)
  
  def function
    nostdout do
      TestBall.new.brew do |var|;Fi[	@Ó@~=i[[	@$i:I"I  end
  
  def function
    var = Dir.getwd
    Dir.chdir(@clone) do;Fi[	@$iüI"I  end
  
  def function
    var = Dir.getwd
    Dir.chdir(@clone) do;Fi[	@Ÿ-@—i[[	@i\I"    #   vehicle.fire_events(:ignite, :disable_alarm)  # => true
    #   vehicle.state                                 # => "idling"
    #   vehicle.alarm_state                           # => "off"
    #   
    #   # If any event fails, the entire event chain fails;Fi[	@iaI"˙    #   vehicle.fire_events(:ignite, :enable_alarm)   # => false
    #   vehicle.state                                 # => "idling"
    #   vehicle.alarm_state                           # => "off"
    #   
    #   # Exception raised on invalid event;Fi[	@÷I"module ActiveRecord;Fi[[	@F
iI"amodule StateMachine
  module Integrations
    module ActiveRecord
      version("STRING") do;Fi[	@ÂiI"àrequire("state_machine/integrations/active_model")
module StateMachine
  module Integrations
    module ActiveRecord
      include(Base);Fi[	@÷I"module Mongoid;Fi[[	@±
iI"\module StateMachine
  module Integrations
    module Mongoid
      version("STRING") do;Fi[	@˜iI"Umodule StateMachine
  module Integrations
    module Mongoid
      include(Base);Fi[	@Ó@ß+i[[	@i.I"ä  private
  
  def function
    HOMEBREW_REPOSITORY.cd do
      `git rev-list --abbrev-commit HEAD -- Library/Formula/#{name}STRING`.split;Fi[	@ëi0I"V  end
  
  def function
    HOMEBREW_REPOSITORY.cd do
      var = UpdaterMock.new;Fi[	@Ó@ /i[[	@i	I"í  extend(self)
  
  def function
    raise("STRING") unless which("STRING")
    raise("STRING") unless (HOMEBREW_REPOSITORY / "STRING").directory?;Fi[	@$iI"Z  end
  
  def function
    raise("STRING") unless which("STRING")
    ohai("STRING");Fi[	@÷I"module MongoMapper;Fi[[	@¶
iI"`module StateMachine
  module Integrations
    module MongoMapper
      version("STRING") do;Fi[	@ÚiI"árequire("state_machine/integrations/active_model")
module StateMachine
  module Integrations
    module MongoMapper
      include(Base);Fi[	@÷I"module Sequel;Fi[[	@ÕiI"[module StateMachine
  module Integrations
    module Sequel
      version("STRING") do;Fi[	@M
iI"Tmodule StateMachine
  module Integrations
    module Sequel
      include(Base);Fi[	@ÓI"4raise(KegUnspecifiedError) if ARGV.named.empty?;Fi[[	@:i	I"†  extend(self)
  
  def function
    raise(KegUnspecifiedError) if ARGV.named.empty?
    if Process.uid.zero? and File.stat(HOMEBREW_BREW_FILE).uid.zero?.! then;Fi[	I"*projects//homebrew_proj/cmd/unlink.rb;TiI"u  extend(self)
  
  def function
    raise(KegUnspecifiedError) if ARGV.named.empty?
    ARGV.kegs.each do |var|;Fi[	@ÓI"2if instance_variable_defined?("LITERAL") then;Fi[[	@ái!I"[  end
  
  def function
    if instance_variable_defined?("LITERAL") then
      @xcode;Fi[	@ái-I"Y  end
  
  def function
    if instance_variable_defined?("LITERAL") then
      @clt;Fi[	@ÓI"if superenv? then;Fi[[	@˙i
I"~  extend(self)
  
  def function
    if superenv? then
      ENV.deps = ARGV.formulae.map(&"LITERAL") unless ARGV.named.empty?;Fi[	@—i
I"P  extend(self)
  
  def function
    if superenv? then
      ENV.x11 = true;Fi[	@Ó@„@i[[	@’iPI"7  end
  
  def function
    system("STRING")
  end;Fi[	@%i/I"7  end
  
  def function
    system("STRING")
  end;Fi[	@ÓI"manipulate! do |var|;Fi[[	@ri(I"E  end
  
  def function
    manipulate! do |var|
      var.strip;Fi[	@uiI"Z  process("LITERAL")
  
  def function
    manipulate! do |var|
      var.auto_orient;Fi[	I"G# Initializes class-level extensions and defaults for this machine;F@Ói[[	@A
iqI"      end
      
      # Initializes class-level extensions and defaults for this machine
      def function
        super;Fi[	@Ãi1I"Ä      protected
      
      # Initializes class-level extensions and defaults for this machine
      def function
        super;Fi[	I"a(callbacks["LITERAL"] << Callback.new("LITERAL") { |var, var| notify("LITERAL", var, var) });F@rHi[[	@A
iÑI"k      def function
        if supports_observers? then
          (callbacks["LITERAL"] << Callback.new("LITERAL") { |var, var| notify("LITERAL", var, var) })
          (callbacks["LITERAL"] << Callback.new("LITERAL") { |var, var| notify("LITERAL", var, var) })
          (callbacks["LITERAL"] << Callback.new("LITERAL") { |var, var| notify("LITERAL", var, var) });Fi[	@A
iÖI"d        if supports_observers? then
          (callbacks["LITERAL"] << Callback.new("LITERAL") { |var, var| notify("LITERAL", var, var) })
          (callbacks["LITERAL"] << Callback.new("LITERAL") { |var, var| notify("LITERAL", var, var) })
          (callbacks["LITERAL"] << Callback.new("LITERAL") { |var, var| notify("LITERAL", var, var) })
        end;Fi[	@Ó@ª+i[[	I".projects//diaspora_proj/models/reshare.rb;TiHI"K  end
  
  def function
    var = self
    while var.is_a?(Reshare) do;Fi[	@;iÜI"n      
      # @private
      def function
        var = self
        @tabs ||= compiler.instance_eval do;Fi[	I"=owner_class.validates_each(attribute) do |var, var, var|;FI"'var = var.class.state_machine(var);Fi[[	@A
iéI"        var = self.name
        if supports_validations? then
          owner_class.validates_each(attribute) do |var, var, var|
            var = var.class.state_machine(var)
            var.invalidate(var, "LITERAL", "LITERAL") unless var.states.match(var);Fi[	@M
iPI"È      def function
        var = self.name
        owner_class.validates_each(attribute) do |var, var, var|
          var = var.class.state_machine(var)
          var.invalidate(var, "LITERAL", "LITERAL") unless var.states.match(var);Fi[	@Ó@È8i[[	@ÒiYI"X  
  # @return [String]
  def function
    case self.service
    when "STRING" then;Fi[	@ÒieI"X  
  # @return [String]
  def function
    case self.service
    when "STRING" then;Fi[	@ÓI"self.person.lock_access!;Fi[[	I"6projects//diaspora_proj/models/account_deleter.rb;TiUI"X  end
  
  def function
    self.person.lock_access!
    self.person.clear_profile!;Fi[	@.iØI"O  end
  
  def function
    self.person.lock_access!
    self.lock_access!;Fi[	@e.@ài[[	@‰+iI"{var = "STRING"
var = eval(IO.read(var), binding, var)
var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL")
var;Fi[	@Á+iI"{var = "STRING"
var = eval(IO.read(var), binding, var)
var["LITERAL"]["LITERAL"] = var["LITERAL"].delete("LITERAL")
var;Fi[	@ÓI".find_current_user_post(params["LITERAL"]);Fi[[	@iII"j  end
  
  def function
    find_current_user_post(params["LITERAL"])
    current_user.retract(@post);Fi[	@iSI"p  end
  
  def function
    find_current_user_post(params["LITERAL"])
    @post.favorite = @post.favorite.!;Fi[	@Ó@U8i[[	@#iNI"x  end
  
  def function
    var = current_user.photos.where("LITERAL" => (params["LITERAL"])).first
    if var then;Fi[	@#ijI"x  end
  
  def function
    var = current_user.photos.where("LITERAL" => (params["LITERAL"])).first
    if var then;Fi[	I"7require("state_machine/integrations/active_model");F@’i[[	@Âi I"arequire("state_machine/integrations/active_model")
module StateMachine
  module Integrations;Fi [	@Úi I"arequire("state_machine/integrations/active_model")
module StateMachine
  module Integrations;Fi [	@Ó@E8i[[	@9iÑI"e  end
  
  def function
    @person = Person.find_by_guid(params["LITERAL"])
    if @person then;Fi[	@9iìI"~  end
  
  def function
    @person = Person.find_by_guid(params["LITERAL"])
    if (@person == current_user.person) then;Fi[	@ÓI"8@person = Person.find_from_guid_or_username(params);Fi[[	@9iII"≤  
  # renders the persons user profile page
  def function
    @person = Person.find_from_guid_or_username(params)
    authenticate_user! if remote_profile_with_no_user_session?;Fi[	@9izI"ü  end
  
  def function
    @person = Person.find_from_guid_or_username(params)
    var = Post.visible_from_author(@person, current_user).order("STRING").first;Fi[	@Ó@Ì i[[	@@i1I"_  end
  
  def function
    Rails.logger.info("STRING")
    flash["LITERAL"] = t("STRING");Fi[	I".projects//diaspora_proj/models/mention.rb;TiI"õ  after_destroy("LITERAL")
  
  def function
    Rails.logger.info("STRING")
    Notification.notify(person.owner, self, post.author) unless person.remote?;Fi[	@Ó@Li[[	I"?projects//diaspora_proj/controllers/contacts_controller.rb;TiI"l  before_filter("LITERAL")
  
  def function
    respond_to do |var|
      var.html { set_up_contacts };Fi[	@
i.I"~  end
  
  def function
    respond_to do |var|
      var.json { render("LITERAL" => (tags.to_json), "LITERAL" => "LITERAL") };Fi[	@ÓI"if model_exists? then;Fi[[	@<iI"è      end
      
      def function
        if model_exists? then
          inject_into_file(model_path, migration_data, "LITERAL" => "STRING");Fi[	@<iI"è      end
      
      def function
        if model_exists? then
          inject_into_file(model_path, model_contents, "LITERAL" => "STRING");Fi[	@Ó@}i[[	@*i+I"t      
      # The install method override this to install
      def function
        raise("STRING")
      end;Fi[	@¶i§I":  private
  
  def function
    raise("STRING")
  end;Fi[	@ÓI"Cgenerate_reset_password_token! if should_generate_reset_token?;Fi[[	@—
iI"ﬁ      
      # Resets reset password token and send reset password instructions by email
      def function
        generate_reset_password_token! if should_generate_reset_token?
        send_devise_notification("LITERAL");Fi[	@.i£I"ã  end
  
  def function
    generate_reset_password_token! if should_generate_reset_token?
    Resque.enqueue(Jobs::ResetPassword, self.id);Fi[	@ÓI" pending_any_confirmation do;Fi[[	@1
iI"       # is already confirmed, add an error to email field. If the user is invalid
      # add errors
      def function
        pending_any_confirmation do
          if confirmation_period_expired? then;Fi[	@1
iBI"€      
      # Resend confirmation token. This method does not need to generate a new token.
      def function
        pending_any_confirmation do
          self.confirmation_token = nil if confirmation_period_expired?;Fi[	@ÓI"%self.file_size = File.size(path);Fi[[	@iEI"~      end
      
      def function
        self.file_size = File.size(path)
        css.each_selector do |var, var, var|;Fi[	@ikI"e      end
      
      def function
        self.file_size = File.size(path)
        visit_tree!;Fi[	@ÓI"*@images.each_with_index do |var, var|;Fi[[	@≈i~I"õ        end
        
        def function
          @images.each_with_index do |var, var|
            var.top = if (var.position.unit_str == "STRING") then;Fi[	@≈iÜI"ù        end
        
        def function
          @images.each_with_index do |var, var|
            var.left = (if (var.position.unit_str == "STRING") then;Fi[	@Ó@º3i[[	@3iI"ì      include(Compass::Installers)
      
      def function
        add_project_configuration
        Compass.add_configuration(options, "STRING");Fi[	@+
iI"Ä      protected
      
      def function
        add_project_configuration
        Compass.add_configuration(options, "STRING");Fi[	I"L# Generates the fully-qualifed column name for this machine's attribute;F@Ói[[	@ÂiTI"ñ      end
      
      # Generates the fully-qualifed column name for this machine's attribute
      def function
        var = owner_class.connection;Fi[	@M
içI"√      end
      
      # Generates the fully-qualifed column name for this machine's attribute
      def function
        ::Sequel::SQL::QualifiedIdentifier.new(owner_class.table_name, attribute);Fi[	@ÓI"#directory(targetize("STRING"));Fi[[	@iI"¢    module StandAlone
      class Installer < Compass::Installers::ManifestInstaller
        def function
          directory(targetize("STRING"))
          super;Fi[	@iI"á      end
      
      def function
        directory(targetize("STRING"))
        directory(targetize(Compass.configuration.sass_dir));Fi[	@ÓI",@password_file.delete if @password_file;Fi[[	@ilI"∆      # Removes the previously created @password_file
      # (temporary file containing the password)
      def function
        @password_file.delete if @password_file
        @password_file = nil;Fi[	@igI"–        # Removes the previously created @password_file
        # (temporary file containing the password)
        def function
          @password_file.delete if @password_file
          @password_file = nil;Fi[	@Ó@é5i[[	@i2I"¬      ##
      # Establishes a connection to the remote server
      def function
        Net::SSH.start(ip, username, "LITERAL" => (password), "LITERAL" => (port)) do |var|
          yield(var);Fi[	@´i&I"Ì      # and yields the Net::SSH connection.
      # Net::SCP will use this connection to transfer backups
      def function
        Net::SSH.start(ip, username, "LITERAL" => (password), "LITERAL" => (port)) do |var|
          yield(var);Fi[	@b)I"'#     include DataMapper::Observer;Fi[[	@%i"I"}        #   end
        #   
        #   class VehicleObserver
        #     include DataMapper::Observer
        #     ;Fi[	@%i_I"}        #   end
        #   
        #   class VehicleObserver
        #     include DataMapper::Observer
        #     ;Fi[	@I@—i[[	@%i#I"Ü        #   
        #   class VehicleObserver
        #     include DataMapper::Observer
        #     
        #     observe Vehicle;Fi[	@%i`I"Ü        #   
        #   class VehicleObserver
        #     include DataMapper::Observer
        #     
        #     observe Vehicle;Fi[	@!B@—i[[	@%i%I"Ñ        #     include DataMapper::Observer
        #     
        #     observe Vehicle
        #     
        #     before :save do;Fi[	@%ibI"ù        #     include DataMapper::Observer
        #     
        #     observe Vehicle
        #     
        #     around_transition do |transition, block|;Fi[	I"#       # log message;F@¢i[[	@%i(I"p        #     
        #     before :save do
        #       # log message
        #     end
        #     ;Fi[	@%i7I"Æ        #     # Target all state machines without requirements
        #     before_transition do |transition|
        #       # log message
        #     end
        #   end;Fi[	I"#       # put on seatbelt;F@¢i[[	@%i-I"≤        #     # Target all state machines
        #     before_transition :parked => :idling, :on => :ignite do
        #       # put on seatbelt
        #     end
        #     ;Fi[	@%i2I"¨        #     # Target a specific state machine
        #     before_transition :state, any => :idling do
        #       # put on seatbelt
        #     end
        #     ;Fi[	@Q)I"# for defining callbacks.;Fi[[	@%iGI"¥        # transition.
        # 
        # See +before_transition+ for a description of the possible configurations
        # for defining callbacks.
        def function(var, var);Fi[	@%ikI"Æ        #   end
        # 
        # See +before_transition+ for a description of the possible configurations
        # for defining callbacks.
        def function(var, var);Fi[	@6I@äi[[	@%iHI"◊        # 
        # See +before_transition+ for a description of the possible configurations
        # for defining callbacks.
        def function(var, var)
          add_transition_callback("LITERAL", *var, &var);Fi[	@%ilI"◊        # 
        # See +before_transition+ for a description of the possible configurations
        # for defining callbacks.
        def function(var, var)
          add_transition_callback("LITERAL", *var, &var);Fi[	I"#       # log failure;F@¢i[[	@%iÄI"ó        #   class VehicleObserver
        #     after_transition_failure do |transition|
        #       # log failure
        #     end
        #     ;Fi[	@%iÑI"á        #     
        #     after_transition_failure :on => :ignite do
        #       # log failure
        #     end
        #   end;Fi[	I"7# Pluralizes the name using the built-in inflector;F@õi[[	@ÃiMI"ï      end
      
      # Pluralizes the name using the built-in inflector
      def function(var)
        ::DataMapper::Inflector.pluralize(var.to_s);Fi[	@M
i-I"}      end
      
      # Pluralizes the name using the built-in inflector
      def function(var)
        load_inflector;Fi[	I"9# Uses internal save hooks if using the :save action;F@Ói[[	@ÃiqI"î      end
      
      # Uses internal save hooks if using the :save action
      def function
        (action == "LITERAL") ? ("LITERAL") : (super);Fi[	@M
iaI"î      end
      
      # Uses internal save hooks if using the :save action
      def function
        (action == "LITERAL") ? ("LITERAL") : (super);Fi[	I"G# Runs a new database transaction, rolling back any changes if the;FI"0# yielded block fails (i.e. returns false).;Fi[[	@Ãi}I"£      end
      
      # Runs a new database transaction, rolling back any changes if the
      # yielded block fails (i.e. returns false).
      def function(var);Fi[	@M
iíI"£      end
      
      # Runs a new database transaction, rolling back any changes if the
      # yielded block fails (i.e. returns false).
      def function(var);Fi[	@YI@õi[[	@Ãi~I"€      
      # Runs a new database transaction, rolling back any changes if the
      # yielded block fails (i.e. returns false).
      def function(var)
        var.class.transaction { |var| var.rollback unless yield };Fi[	@M
iìI"Ê      
      # Runs a new database transaction, rolling back any changes if the
      # yielded block fails (i.e. returns false).
      def function(var)
        var.db.transaction { raise(::Sequel::Error::Rollback) unless yield };Fi[	I"I# Creates a new callback in the callback chain, always ensuring that;FI"J# it's configured to bind to the object as this is the convention for;Fi[[	@ÃiÉI"À      end
      
      # Creates a new callback in the callback chain, always ensuring that
      # it's configured to bind to the object as this is the convention for
      # DataMapper/Extlib callbacks;Fi[	@M
iòI"¿      end
      
      # Creates a new callback in the callback chain, always ensuring that
      # it's configured to bind to the object as this is the convention for
      # Sequel callbacks;Fi[	I".# Defines a new scope with the given name;F@äi[[	@ÚiMI"•      end
      
      # Defines a new scope with the given name
      def function(var, var)
        lambda { |var, var| var.query.merge(var.query(var.call(var))) };Fi[	@˜iQI"ù      end
      
      # Defines a new scope with the given name
      def function(var, var)
        lambda { |var, var| var.criteria.where(var.call(var)) };Fi[	I"L# Forces the change in state to be recognized regardless of whether the;FI"## state value actually changed;Fi[[	@±
iI"’          ::Mongoid::VERSION =~ "LITERAL"
        end
        # Forces the change in state to be recognized regardless of whether the
        # state value actually changed
        def function(var, var, var, var);Fi[	@M
iI"™      end
      
      # Forces the change in state to be recognized regardless of whether the
      # state value actually changed
      def function(var, var, var, var);Fi[	@vI@ˆ-i[[	@±
iI"¡        end
        # Forces the change in state to be recognized regardless of whether the
        # state value actually changed
        def function(var, var, var, var)
          var = super;Fi[	@M
iI"¥      
      # Forces the change in state to be recognized regardless of whether the
      # state value actually changed
      def function(var, var, var, var)
        var = super;Fi[	I"#         ...;F@◊i[[	@5i=I"j    #     state_machine do
    #       event :park do
    #         ...
    #       end
    #     end;Fi[	@5i)I"â    #       @state = 'idling'
    #       initialize_state_machines(:static => :force) do
    #         ...
    #       end
    #     end;Fi[	@ÓI"@package = @model.package;Fi[[	I"-projects//backup_proj/backup/splitter.rb;Ti#I"È    # final @package.basename, plus a '-' separator. This `prefix` will then
    # be suffixed using 'aa', 'ab', and so on... for each file.
    def function
      @package = @model.package
      Logger.message(("STRING" + "STRING"));Fi[	@¿
i!I"y      ##
      # Performs the backup transfer
      def function
        @package = @model.package
        transfer!;Fi[	I"N# state machine based on the *name* of the machine.  Any existing methods;FI"# will not be overwritten.;Fi[[	@5iaI"    # 
    # The following class methods will be automatically generated by the
    # state machine based on the *name* of the machine.  Any existing methods
    # will not be overwritten.
    # * <tt>human_state_name(state)</tt> - Gets the humanized value for the;Fi[	@5i{I"    # 
    # The following instance methods will be automatically generated by the
    # state machine based on the *name* of the machine.  Any existing methods
    # will not be overwritten.
    # * <tt>state</tt> - Gets the current value for the attribute;Fi[	@†@–i[[	@5imI"ú    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling;Fi[	@5iûI"ú    # 
    #   class Vehicle
    #     state_machine :state, :initial => :parked do
    #       event :ignite do
    #         transition :parked => :idling;Fi[	@ÓI"return unless @orig_err;Fi[[	@òi8I"Ü      end
      
      def function
        return unless @orig_err
        @orig_err_class ||= if @orig_err.is_a?(Errors::Error) then;Fi[	@òiAI"      end
      
      def function
        return unless @orig_err
        return @orig_err_msg unless @orig_err_msg.nil?;Fi[	I"™#                                             #     [#<StateMachine::Transition attribute=:state event=:park from="idling" from_name=:idling to="parked" to_name=:parked>,;FI"¨#                                             #      #<StateMachine::Transition attribute=:state event=:ignite from="parked" from_name=:parked to="idling" to_name=:idling>];Fi[[	@5iƒI"¸    #   # Getting current path availability
    #   vehicle.state_paths                       # => [
    #                                             #     [#<StateMachine::Transition attribute=:state event=:park from="idling" from_name=:idling to="parked" to_name=:parked>,
    #                                             #      #<StateMachine::Transition attribute=:state event=:ignite from="parked" from_name=:parked to="idling" to_name=:idling>]
    #                                             #   ];Fi[	@5i»I"    #                                             #   ]
    #   vehicle.state_paths(:guard => false)      # => 
    #                                             #     [#<StateMachine::Transition attribute=:state event=:park from="idling" from_name=:idling to="parked" to_name=:parked>,
    #                                             #      #<StateMachine::Transition attribute=:state event=:ignite from="parked" from_name=:parked to="idling" to_name=:idling>]
    #                                             #   ];Fi[	@®II"8#                                             #   ];Fi[[	@5i≈I"    #   vehicle.state_paths                       # => [
    #                                             #     [#<StateMachine::Transition attribute=:state event=:park from="idling" from_name=:idling to="parked" to_name=:parked>,
    #                                             #      #<StateMachine::Transition attribute=:state event=:ignite from="parked" from_name=:parked to="idling" to_name=:idling>]
    #                                             #   ]
    #   vehicle.state_paths(:guard => false)      # => ;Fi[	@5i…I"ÿ    #   vehicle.state_paths(:guard => false)      # => 
    #                                             #     [#<StateMachine::Transition attribute=:state event=:park from="idling" from_name=:idling to="parked" to_name=:parked>,
    #                                             #      #<StateMachine::Transition attribute=:state event=:ignite from="parked" from_name=:parked to="idling" to_name=:idling>]
    #                                             #   ]
    #   ;Fi[	@Ó@Œ8i[[	@iBI"{      ##
      # Attempt to load a cached session
      def function
        var = false
        if cache_exists? then;Fi[	@CiI"E  end
  
  def function
    var = false
    @user = current_user;Fi[	@ÓI"-var = "STRING".gsub("LITERAL", "STRING");Fi[[	@Úi∞I"´      # and ensure no 'write operations' are performed during the
      # dump process
      def function
        var = "STRING".gsub("LITERAL", "STRING")
        run(var);Fi[	@Úi∑I"É      ##
      # Unlocks the (locked) database
      def function
        var = "STRING".gsub("LITERAL", "STRING")
        run(var);Fi[	@?)@˘i[[	@5i¸I"k    #     end
    #     
    #     def initialize(attributes = {})
    #       ...
    #       super();Fi[	@5iI"}    #     end
    #     
    #     def initialize(attributes = {})
    #       ...
    #       initialize_state_machines;Fi[	@B@Ôi[[	@5i≈I"u    #       end
    #       
    #       event :turn_off do
    #         transition all => :off
    #       end;Fi[	@5iœI"u    #       end
    #       
    #       event :turn_off do
    #         transition all => :off
    #       end;Fi[	I"3#   matcher.matches?(:parked)       # => false;FI"2#   matcher.matches?(:first_gear)   # => true;Fi[[	@÷iI"     # 
    #   matcher = StateMachine::AllMatcher.instance - [:parked, :idling]
    #   matcher.matches?(:parked)       # => false
    #   matcher.matches?(:first_gear)   # => true
    def function(var);Fi[	@÷iOI"œ    # 
    #   matcher = StateMachine::BlacklistMatcher.new([:parked, :idling])
    #   matcher.matches?(:parked)       # => false
    #   matcher.matches?(:first_gear)   # => true
    def function(var, var);Fi[	I"# for this matcher.;F@—i[[	@÷i7I"ü  class WhitelistMatcher < Matcher
    # Checks whether the given value exists within the whitelist configured
    # for this matcher.
    # 
    # == Examples;Fi[	@÷iJI"†  class BlacklistMatcher < Matcher
    # Checks whether the given value exists outside the blacklist configured
    # for this matcher.
    # 
    # == Examples;Fi[	I"3# A human-readable description of this matcher;F@Ói[[	@÷iBI"ü    end
    
    # A human-readable description of this matcher
    def function
      (values.length == "LITERAL") ? (values.first.inspect) : (values.inspect);Fi[	@÷i[I"d    end
    
    # A human-readable description of this matcher
    def function
      "STRING";Fi[	@BI"<#   states << StateMachine::State.new(machine, :parked);Fi[[	@√ijI"¡    # parameter.
    # 
    #   states = StateMachine::NodeCollection.new
    #   states << StateMachine::State.new(machine, :parked)
    #   states << StateMachine::State.new(machine, :idling);Fi[	@√iyI"ÿ    # Gets the node at the given index.
    # 
    #   states = StateMachine::NodeCollection.new
    #   states << StateMachine::State.new(machine, :parked)
    #   states << StateMachine::State.new(machine, :idling);Fi[	@ÂII"<#   states << StateMachine::State.new(machine, :idling);Fi[[	@√ikI"Ê    # 
    #   states = StateMachine::NodeCollection.new
    #   states << StateMachine::State.new(machine, :parked)
    #   states << StateMachine::State.new(machine, :idling)
    #   states.each {|state| puts state.name, ' -- '};Fi[	@√izI"π    # 
    #   states = StateMachine::NodeCollection.new
    #   states << StateMachine::State.new(machine, :parked)
    #   states << StateMachine::State.new(machine, :idling)
    #   ;Fi[	I"Q# Gets the node indexed by the given key.  By default, this will look up the;FI"P# key in the first index configured for the collection.  A custom index can;Fi[[	@√i~I"…    end
    
    # Gets the node indexed by the given key.  By default, this will look up the
    # key in the first index configured for the collection.  A custom index can
    # be specified like so:;Fi[	@√iåI"…    end
    
    # Gets the node indexed by the given key.  By default, this will look up the
    # key in the first index configured for the collection.  A custom index can
    # be specified like so:;Fi[	@ÙII"# be specified like so:;Fi[[	@√iI"»    
    # Gets the node indexed by the given key.  By default, this will look up the
    # key in the first index configured for the collection.  A custom index can
    # be specified like so:
    # ;Fi[	@√içI"»    
    # Gets the node indexed by the given key.  By default, this will look up the
    # key in the first index configured for the collection.  A custom index can
    # be specified like so:
    # ;Fi[	@˚I@—i[[	@√iÄI"Ë    # Gets the node indexed by the given key.  By default, this will look up the
    # key in the first index configured for the collection.  A custom index can
    # be specified like so:
    # 
    #   collection['parked', :value];Fi[	@√iéI"Ë    # Gets the node indexed by the given key.  By default, this will look up the
    # key in the first index configured for the collection.  A custom index can
    # be specified like so:
    # 
    #   collection['parked', :value];Fi[	@B@—i[[	@√iÇI"ù    # be specified like so:
    # 
    #   collection['parked', :value]
    # 
    # The above will look up the "parked" key in a hash indexed by each node's;Fi[	@√iêI"ù    # be specified like so:
    # 
    #   collection['parked', :value]
    # 
    # The above will look up the "parked" key in a hash indexed by each node's;Fi[	@BI"# +value+ attribute.;Fi[[	@√iÑI"ö    #   collection['parked', :value]
    # 
    # The above will look up the "parked" key in a hash indexed by each node's
    # +value+ attribute.
    # ;Fi[	@√iíI"ö    #   collection['parked', :value]
    # 
    # The above will look up the "parked" key in a hash indexed by each node's
    # +value+ attribute.
    # ;Fi[	@J@—i[[	@√iÖI"≤    # 
    # The above will look up the "parked" key in a hash indexed by each node's
    # +value+ attribute.
    # 
    # If the key cannot be found, then nil will be returned.;Fi[	@√iìI"ƒ    # 
    # The above will look up the "parked" key in a hash indexed by each node's
    # +value+ attribute.
    # 
    # If the key cannot be found, then an IndexError exception will be raised:;Fi[	I"9# Gets the value for the given attribute on the node;F@äi[[	@√i•I"y    end
    
    # Gets the value for the given attribute on the node
    def function(var, var)
      var.send(var);Fi[	@YilI"ú    private
    
    # Gets the value for the given attribute on the node
    def function(var, var)
      (var == "LITERAL") ? (var.value(false)) : (super);Fi[	I"# versions of the index;F@äi[[	@√i≥I"£    
    # Removes the given key from an index, including the string and symbol
    # versions of the index
    def function(var, var)
      index(var).delete(var);Fi[	@√iªI"£    
    # Updates the node for the given index, including the string and symbol
    # versions of the index
    def function(var, var)
      var = self.index(var);Fi[	@ÑG@êi[[	@oiI"    # * <tt>:target</tt> - The target state to end the path on
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one
    def function(var, var, var)
      assert_valid_keys(var, "LITERAL", "LITERAL");Fi[	@viI">    # * <tt>:deep</tt> - Whether to enable deep searches for the target state.
    # * <tt>:guard</tt> - Whether to guard transitions with the if/unless
    #   conditionals defined for each one
    def function(var, var, var)
      var = { "LITERAL" => (false), "LITERAL" => (var.states.match!(var).name) }.merge(var);Fi[	@˛A@Ói[[	@oi-I"ì    # For example,
    # 
    #   path.to_states  # => [:parked, :idling, :first_gear, ...]
    def function
      map { |var| var.from_name }.uniq;Fi[	@oi<I"ë    # For example,
    # 
    #   path.to_states  # => [:parked, :idling, :first_gear, ...]
    def function
      map { |var| var.to_name }.uniq;Fi[	I"# this collection.;F@—i[[	@vi+I"Ü    
    # Lists all of the states that can be transitioned from through the paths in
    # this collection.
    # 
    # For example,;Fi[	@vi5I"Ñ    
    # Lists all of the states that can be transitioned to through the paths in
    # this collection.
    # 
    # For example,;Fi[	@Ó@{>i[[	@äiI"¡      ##
      # Yields to the block the compressor command and filename extension.
      def function
        Backup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"))
        super;Fi[	@çi%I"¡      ##
      # Yields to the block the compressor command and filename extension.
      def function
        Backup::Logger.warn(((("STRING" + "STRING") + "STRING") + "STRING"))
        super;Fi[	@)@—i[[	@Yi/I"ú    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   
    #   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">;Fi[	@YiJI"ú    #   end
    #   
    #   states = Vehicle.state_machine.states
    #   
    #   vehicle = Vehicle.new         # => #<Vehicle:0xb7c464b0 @state="parked">;Fi[	@ )I"c#   transition = StateMachine::Transition.new(Vehicle.new, machine, :ignite, :parked, :idling);Fi[[	@›i§I";    # == Example
    # 
    #   machine = StateMachine.new(Vehicle)
    #   transition = StateMachine::Transition.new(Vehicle.new, machine, :ignite, :parked, :idling)
    #   transition.attributes   # => {:object => #<Vehicle:0xb7d60ea4>, :attribute => :state, :event => :ignite, :from => 'parked', :to => 'idling'};Fi[	@›iôI"¸    # == Example
    # 
    #   machine = StateMachine.new(Vehicle)
    #   transition = StateMachine::Transition.new(Vehicle.new, machine, :ignite, :parked, :idling)
    #   transition.context    # => {:on => :ignite, :from => :parked, :to => :idling};Fi[	I"K# callbacks that are configured to match the event, from state, and to;FI"# state will be invoked.;Fi[[	@›i^I"∫    
    # Runs the machine's +before+ callbacks for this transition.  Only
    # callbacks that are configured to match the event, from state, and to
    # state will be invoked.
    # ;Fi[	@›i|I"π    
    # Runs the machine's +after+ callbacks for this transition.  Only
    # callbacks that are configured to match the event, from state, and to
    # state will be invoked.
    # ;Fi[	@PJ@—i[[	@›i_I"    # Runs the machine's +before+ callbacks for this transition.  Only
    # callbacks that are configured to match the event, from state, and to
    # state will be invoked.
    # 
    # Once the callbacks are run, they cannot be run again until this transition;Fi[	@›i}I"    # Runs the machine's +after+ callbacks for this transition.  Only
    # callbacks that are configured to match the event, from state, and to
    # state will be invoked.
    # 
    # Once the callbacks are run, they cannot be run again until this transition;Fi[	@ÕAI"# is reset.;Fi[[	@›iaI"§    # state will be invoked.
    # 
    # Once the callbacks are run, they cannot be run again until this transition
    # is reset.
    def function(var, var, var);Fi[	@›iI"ã    # state will be invoked.
    # 
    # Once the callbacks are run, they cannot be run again until this transition
    # is reset.
    # ;Fi[	@Ó@Ìi[[	@$iI"m      include(Backup::Configuration::Helpers)
      
      def function
        load_defaults!
      end;Fi[	@óiI"~      ##
      # Flag for mirroring the files/directories
      def function
        load_defaults!
        @path ||= "STRING";Fi[	@˘F@ª+i[[	@`iI"ä        
        def function
          if owner.is_a?(StateMachine::Machine) then
            var = self
            var = self.statement;Fi[	@eiI"ä        
        def function
          if owner.is_a?(StateMachine::Machine) then
            var = self
            var = self.statement;Fi[	@Å9I"4var = extract_node_names(var.parameters(false));Fi[[	@`iI"∆          if owner.is_a?(StateMachine::Machine) then
            var = self
            var = self.statement
            var = extract_node_names(var.parameters(false))
            var.each do |var|;Fi[	@eiI"∆          if owner.is_a?(StateMachine::Machine) then
            var = self
            var = self.statement
            var = extract_node_names(var.parameters(false))
            var.each do |var|;Fi[	@pJ@ß(i[[	@`iI"Ë            var = self
            var = self.statement
            var = extract_node_names(var.parameters(false))
            var.each do |var|
              owner.event(var) { var.parse_block(var.last.last, "LITERAL" => (self)) };Fi[	@eiI"Ë            var = self
            var = self.statement
            var = extract_node_names(var.parameters(false))
            var.each do |var|
              owner.state(var) { var.parse_block(var.last.last, "LITERAL" => (self)) };Fi[	I"\register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING", "LITERAL")));F@b/i[[	@èiI"˚        def function
          return if inherited_machine
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING", "LITERAL")))
          var.docstring = ["STRING", "STRING", "STRING"]
          var.parameters = ["STRING"];Fi[	@èi}I"          var.docstring = ["STRING", "STRING", "STRING"]
          var.parameters = ["STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING", "LITERAL")))
          var.docstring = ["STRING", "STRING", "STRING"]
          var.parameters = ["STRING"];Fi[	@ÓI"	log!;Fi[[	@~iI"ç      ##
      # Yields to the block the compressor command and filename extension.
      def function
        log!
        yield(@cmd, @ext);Fi[	@ûi*I"¡      # Once the packaging procedure is complete, it will return
      # so that any clean-up may be performed after the yield.
      def function
        log!
        yield("STRING", "STRING");Fi[	I".namespace.attributes["LITERAL"][var] = {};FI"Lregister((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)));Fi[[	@èiÇI"M          unless (integration or instance_attributes.include?(machine.attribute.to_sym)) then
            var = machine.attribute
            namespace.attributes["LITERAL"][var] = {}
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var;Fi[	@èiçI"f          if ((integration and (integration.defaults["LITERAL"] and options.include?("LITERAL").!)) or options["LITERAL"]) then
            var = "STRING"
            namespace.attributes["LITERAL"][var] = {}
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var;Fi[	@åJ@$i[[	@èiÉI"             var = machine.attribute
            namespace.attributes["LITERAL"][var] = {}
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"];Fi[	@èiéI"            var = "STRING"
            namespace.attributes["LITERAL"][var] = {}
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var)))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"];Fi[	@rI""job_type("LITERAL", "STRING");Fi[[	I".projects//whenever_proj/whenever/setup.rb;TiI"Ñ  job_type("LITERAL", "STRING")
  job_type("LITERAL", "STRING")
else
  job_type("LITERAL", "STRING")
  job_type("LITERAL", "STRING");Fi[	@úJiI"fif Whenever.rails3? then
  job_type("LITERAL", "STRING")
else
  job_type("LITERAL", "STRING")
end;Fi[	@@CCi[[	@Ti=I"         var[var].each do |var|
          if var["LITERAL"] and (var["LITERAL"] == "STRING") then
            if var["LITERAL"] then
              (var << clean_entities(var["LITERAL"]))
            else;Fi[	@TiDI"¬          else
            if var["LITERAL"] and (var["LITERAL"] == "STRING") then
              if var["LITERAL"] then
                (var << clean_entities(var["LITERAL"]))
              else;Fi[	@r@R,i[[	@i#I"à        var.pos -= "LITERAL"
      end
    else
      if ((var >> "LITERAL") == "LITERAL") then
        if check_next_sequence(var) then;Fi[	@i.I"í          var.pos -= "LITERAL"
        end
      else
        if ((var >> "LITERAL") == "LITERAL") then
          if check_next_sequence(var) then;Fi[	@l/@$i[[	@èiÜI">            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"];Fi[	@èiëI">            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            namespace.attributes["LITERAL"][var]["LITERAL"] = var
            var.docstring = ["STRING", "STRING"];Fi[	@l/@$i[[	@èiõI"ä          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
          var.docstring = ["STRING", "STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))));Fi[	@èiƒI"∫              next
            end
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING"]
          end;Fi[	@r@ÂEi[[	@«i´I"]        end
        failed!
      else
        Resque.logger.info("STRING")
      ensure;Fi[	@«iœI"m          sleep(var)
          retry
        else
          Resque.logger.info("STRING")
          raise;Fi[	@l/I"Qvar.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING"];Fi[[	@èiØI"Y          machine.events.each do |var|
            next if inherited_machine and inherited_machine.events[var.name]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
            var.parameters = [["STRING", "STRING"]];Fi[	@èi≤I"r            var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
            var.parameters = [["STRING", "STRING"]]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
            var.parameters = [["STRING", "STRING"]];Fi[	@u/@c/i[[	@èióI"8          end
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
          var.docstring = ["STRING", "STRING", "STRING", "STRING"]
          var.parameters = ["STRING"]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))));Fi[	@èiπI"˚            var.parameters = ["STRING"]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING", "STRING", "STRING"]
            var.parameters = ["STRING"]
          end;Fi[	@r@Y<i[[	@_<i5I"€      if (var == "LITERAL") then
        synchronize { decode(@redis.lindex(@redis_name, var)) }
      else
        synchronize do
          Array(@redis.lrange(@redis_name, var, ((var + var) - "LITERAL"))).map do |var|;Fi[	@_<iJI"_          decode(var)
        end
      else
        synchronize do
          until var do;Fi[	@rI"Mif env["STRING"] and env["STRING"].match(/#{request_path}STRING/).! then;Fi[[	@¡i≤I"          if request.params["STRING"] then
            env["STRING"]["STRING"] = request.params["STRING"]
          else
            if env["STRING"] and env["STRING"].match(/#{request_path}STRING/).! then
              env["STRING"]["STRING"] = env["STRING"];Fi[	@¡iÚI"Ò      if request.params["STRING"] then
        @env["STRING"]["STRING"] = request.params["STRING"]
      else
        if env["STRING"] and env["STRING"].match(/#{request_path}STRING/).! then
          @env["STRING"]["STRING"] = env["STRING"];Fi[	@à/@l/i[[	@èi•I"ó          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
          var.docstring = ["STRING", "STRING", "STRING", "STRING"];Fi[	@èi±I"ó            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
            var.parameters = [["STRING", "STRING"]]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	@à/@~/i[[	@èiüI"…          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	@èi¢I"…          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
          var.parameters = [["STRING", "STRING"]]
          register((var = ::YARD::CodeObjects::MethodObject.new(namespace, machine.attribute("LITERAL"))))
          var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING", "STRING"];Fi[	@øJ@à/i[[	@èi∞I"ã            next if inherited_machine and inherited_machine.events[var.name]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
            var.parameters = [["STRING", "STRING"]]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")));Fi[	@èi≥I"|            var.parameters = [["STRING", "STRING"]]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, "STRING")))
            var.docstring = ["STRING", "STRING", "STRING", "STRING", "STRING", "STRING"]
            var.parameters = [["STRING", "STRING"]]
            register((var = ::YARD::CodeObjects::MethodObject.new(namespace, var.qualified_name)));Fi[	@rI"var.to_s;Fi[[	@&iGI"∞      if var.is_a?(Symbol) then
        I18n.t(:"#{scope}STRING#{var}", "LITERAL" => (scope), "LITERAL" => "STRING", "LITERAL" => ([var]))
      else
        var.to_s
      end;Fi[	@¶i≥I"N    when "LITERAL" then
      "LITERAL"
    else
      var.to_s
    end));Fi[	I",puts("STRING") if var.delete("LITERAL");FI" Rake::Task["STRING"].invoke;Fi[[	@a"iI"µ    var["LITERAL"] = (ENV["STRING"] == "STRING") if ENV["STRING"]
    if defined? Rails then
      puts("STRING") if var.delete("LITERAL")
      Rake::Task["STRING"].invoke
    else;Fi[	@a"iI"é    else
      if defined? Merb then
        puts("STRING") if var.delete("LITERAL")
        Rake::Task["STRING"].invoke
        $0 = "STRING";Fi[	@r@X?i[[	@6i1I"l    when nil then
      var.fails_with?("LITERAL")
    else
      var.fails_with?("LITERAL").!
    end);Fi[	@6iTI"l    when nil then
      var.fails_with?("LITERAL")
    else
      var.fails_with?("LITERAL").!
    end);Fi[	@r@ñ>i[[	@ÙiI"k      when "LITERAL" then
        var.bottle
      else
        var.stable
      end, var.active_spec);Fi[	@Ùi#I"k      when "LITERAL" then
        var.bottle
      else
        var.stable
      end, var.active_spec);Fi[	@r@+i[[	@1iI"w    if (version == "LITERAL") then
      "LITERAL"
    else
      if (version == "LITERAL") then
        "LITERAL";Fi[	@1iI"|      if (version == "LITERAL") then
        "LITERAL"
      else
        if (version == "LITERAL") then
          "LITERAL";Fi[	@r@û+i[[	@iqI"π      if @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then
        "LITERAL"
      else
        if @compilers.include?("LITERAL") then
          "LITERAL";Fi[	@i{I"π      if @compilers.include?("LITERAL") and (MacOS.clang_build_version >= "LITERAL") then
        "LITERAL"
      else
        if @compilers.include?("LITERAL") then
          "LITERAL";Fi[	@rI"if var.dry_run then;Fi[[	@:iI"â        print("STRING") { var.link(var) }
        next
      else
        if var.dry_run then
          print("STRING") { var.link(var) };Fi[	@iƒI"n        puts(var) if var.exist?
        return
      else
        if var.dry_run then
          puts(var);Fi[	@(I"class Geo;Fi[[	@≤iI"Urequire("twitter/geo")
module Twitter
  class Geo
    class Point < Twitter::Geo;Fi[	I"2projects//twitter_proj/twitter/geo/polygon.rb;TiI"Wrequire("twitter/geo")
module Twitter
  class Geo
    class Polygon < Twitter::Geo;Fi[	@(I"module Response;Fi[[	@KiI"}require("faraday")
require("multi_json")
module Twitter
  module Response
    class ParseJson < Faraday::Response::Middleware;Fi[	I";projects//twitter_proj/twitter/response/raise_error.rb;TiI"©require("twitter/error/unauthorized")
require("twitter/error/unprocessable_entity")
module Twitter
  module Response
    class RaiseError < Faraday::Response::Middleware;Fi[	@r@êi[[	@iI"÷          puts
        end
      else
        raise(FormulaUnspecifiedError) if ARGV.named.empty?
        var = ARGV.formulae.map { |var| ARGV.one? ? (var.deps) : (var.recursive_deps) }.intersection.map(&"LITERAL");Fi[	@µiI"ﬂ      var = Homebrew.outdated_brews.collect { |var| var.name }
      Formula.select { |var| (var.installed? or var.include?(var.name)) }
    else
      raise(FormulaUnspecifiedError) if ARGV.named.empty?
      ARGV.formulae;Fi[	@rI"if var.directory? then;Fi[[	@ÚiAI"»          ((var.extname == "STRING") or (var.extname == "STRING")) and var.symlink?.!
        end
      else
        if var.directory? then
          var.symlink? ? (puts("STRING")) : (print_dir(var));Fi[	@i‰I"ò          make_relative_symlink(var, var, var)
        end
      else
        if var.directory? then
          next if var.directory? and var.symlink?.!;Fi[	@r@B:i[[	@;i]I"£            var = var["LITERAL"].strip
          end
        else
          if contains_interpolation?(var) then
            var = unescape_interpolation(var, var);Fi[	@;idI"Ç          end
        end
      else
        if contains_interpolation?(var) then
          var = unescape_interpolation(var, var);Fi[	@r@ª+i[[	@Ri*I"}      var = (self + var)
      var = Pathname.new(var)
    else
      var = self
      var = (self + File.basename(var));Fi[	@µi8I"å            var = Nokogiri::XML::Node.new("STRING", document)
            send(var, var)
          else
            var = self
          end;Fi[	@r@ﬂ&i[[	@îiÌI"ÿ          var = ["STRING", "STRING", "STRING", "STRING"]
          var = Node.new("LITERAL", var)
        else
          if (var["LITERAL"] == "STRING") then
            var = ["STRING", "STRING", "STRING", "STRING"];Fi[	@îiI"™          var.unshift("STRING")
          var = Node.new("LITERAL", var)
        else
          if (var["LITERAL"] == "STRING") then
            var["LITERAL"] = "STRING";Fi[	@rI"if var.is_a?(Reshare) then;Fi[[	@,"i	I"›    if var.is_a?(Photo) then
      I18n.t("STRING", "LITERAL" => "LITERAL", "LITERAL" => (var.status_message_author_name))
    else
      if var.is_a?(Reshare) then
        I18n.t("STRING", "LITERAL" => (var.author_name));Fi[	@¢iI"Û        if (var.is_a?(Comment) or var.is_a?(Like)) then
          var = var.concatenate_or_create(var, var.parent, var, var)
        else
          if var.is_a?(Reshare) then
            var = var.concatenate_or_create(var, var.root, var, var);Fi[	@˙"I""require("twitter/exceptable");Fi[[	@ÃiI"rrequire("forwardable")
require("twitter/creatable")
require("twitter/exceptable")
require("twitter/identity");Fi[	@,'iI"lrequire("twitter/basic_user")
require("twitter/creatable")
require("twitter/exceptable")
module Twitter;Fi[	@r@j8i[[	@.0iI"í      var = User.find_by_invitation_token(params["LITERAL"])
      var.ugly_accept_invitation_code if var
    else
      params["LITERAL"]
    end;Fi[	@õ!iJI"•    if (params["LITERAL"]["LITERAL"] or (params["LITERAL"]["LITERAL"].first == "STRING")) then
      current_user.aspect_ids
    else
      params["LITERAL"]
    end;Fi[	@r@8i[[	@.0i#I"ú    if @invitation_code.present? then
      render("STRING", "LITERAL" => (false))
    else
      flash["LITERAL"] = t("STRING")
      redirect_to(root_url);Fi[	@CicI"µ      if params["LITERAL"].present? and params["LITERAL"]["LITERAL"].present? then
        flash["LITERAL"] = t("STRING")
      else
        flash["LITERAL"] = t("STRING")
      end;Fi[	@r@Á7i[[	@é7iI"é      disconnect_if_contact(var.person)
      var = { "LITERAL" => (t("STRING")) }
    else
      var = { "LITERAL" => (t("STRING")) }
    end;Fi[	@é7iI"•    if current_user.blocks.find(params["LITERAL"]).delete then
      var = { "LITERAL" => (t("STRING")) }
    else
      var = { "LITERAL" => (t("STRING")) }
    end;Fi[	@r@œ.i[[	@∆Hi&I"ö    when "STRING" then
      current_user.contacts
    else
      if params["LITERAL"] then
        @aspect = current_user.aspects.find(params["LITERAL"]);Fi[	@3iGI"˝    if params["LITERAL"] then
      @contact_ids = current_user.contacts.find(params["LITERAL"]).id
    else
      if params["LITERAL"] then
        @contact_ids = current_user.aspects.find(params["LITERAL"]).contacts.map { |var| var.id }.join("STRING");Fi[	@\I"require("twitter/cursor");Fi[[	@0iI"require("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/cursor")
require("twitter/error/forbidden");Fi[	@ºiI"írequire("twitter/action_factory")
require("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/cursor")
require("twitter/tweet");Fi[	@\I"+require("twitter/core_ext/enumerable");Fi[[	@ViI"~require("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/core_ext/enumerable")
require("twitter/cursor");Fi[	@≈iI"árequire("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/core_ext/enumerable")
require("twitter/error/not_found");Fi[	@\I"require("twitter/place");Fi[[	@yi I"Irequire("twitter/api/utils")
require("twitter/place")
module Twitter;Fi [	@†i I"Srequire("twitter/api/utils")
require("twitter/place")
require("twitter/trend");Fi [	@r@`!i[[	@ÃiôI"ï      if entities? then
        Array(@attrs["LITERAL"][var.to_sym]).map { |var| var.fetch_or_new(var) }
      else
        warn("STRING")
        [];Fi[	@‡iPI"d        var.close!
        exit("LITERAL")
      else
        warn("STRING")
        var.close!;Fi[	@r@Ä6i[[	@1
iñI"ñ        if (confirmed?.! or pending_reconfirmation?) then
          yield
        else
          self.errors.add("LITERAL", "LITERAL")
          false;Fi[	@ i{I"{        if access_locked? then
          yield
        else
          self.errors.add("LITERAL", "LITERAL")
          false;Fi[	@rI"return false;Fi[[	@›.iEI"∆      if self.post.comments.where("LITERAL" => (var.person.id)).!=([]) and self.author_id.!=(var.person.id) then
        return Notifications::AlsoCommented
      else
        return false
      end;Fi[	@.iI"c      if self.like_for(var) then
        return true
      else
        return false
      end;Fi[	@rI"var.position.value;Fi[[	@≈i}I"Õ            var.top = if (var.position.unit_str == "STRING") then
              ((@height - var.height) * (var.position.value / "LITERAL"))
            else
              var.position.value
            end;Fi[	@≈iäI"”            var.left = (if (var.position.unit_str == "STRING") then
              ((@width - var.width) * (var.position.value / "LITERAL"))
            else
              var.position.value
            end).to_i;Fi[	@@«i[[	@Mi,I"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [String]
      # @example Return {https://twitter.com/privacy Twitter's Privacy Policy};Fi[	@Mi9I"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [String]
      # @example Return {https://twitter.com/tos Twitter's Terms of Service};Fi[	@rI"Ereturn Sass::Script::String.new("STRING") if absolute_path?(var);Fi[[	@w$iRI"≈      if var =~ /^#{Regexp.escape(Compass.configuration.http_images_path)}STRING/ then
        var = $1
      else
        return Sass::Script::String.new("STRING") if absolute_path?(var)
      end;Fi[	@w$iI"œ      if var =~ /^#{Regexp.escape(Compass.configuration.http_generated_images_path)}STRING/ then
        var = $1
      else
        return Sass::Script::String.new("STRING") if absolute_path?(var)
      end;Fi[	@r@|4i[[	I"3projects//compass_proj/compass/exec/helpers.rb;TiI"ó      if var["LITERAL"] then
        var.backtrace["LITERAL"].each { |var| $stderr.puts("STRING") }
      else
        $stderr.puts("STRING")
      end;Fi[	@w$iŸI"r    if File.readable?(var) then
      File.mtime(var).to_i.to_s
    else
      $stderr.puts("STRING")
    end;Fi[	@rI"if var.is_a?(String) then;Fi[[	@ëi)I"Ë              if var.is_a?(Hash) then
                Compass::Configuration::Data.new(var, var)
              else
                if var.is_a?(String) then
                  Compass::Configuration::FileData.new_from_file(var, var);Fi[	@w$i¿I"x    if var.nil? then
      return var
    else
      if var.is_a?(String) then
        var = { "LITERAL" => (var) };Fi[	@I"<# @return [Array<Twitter::Tweet>] The favorited Tweets.;Fi[[	@)iMI"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The favorited Tweets.
      # @overload favorite(*ids);Fi[	@)ioI"[      # @authentication Requires user context
      # @raise [Twitter::Error::AlreadyFavorited] Error raised when tweet has already been favorited.
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The favorited Tweets.
      # @overload favorite(*ids);Fi[	@r@˘3i[[	@ñiàI"Ó              var[var].stop = var[(var - "LITERAL")].stop.plus(var[var].stop.minus(var[(var - "LITERAL")].stop).div(Sass::Script::Number.new(var)))
              break
            else
              var = (var + "LITERAL")
            end;Fi[	@’iCI"á        if (var["STRING"] == var) then
          Resque::Failure.remove(var)
        else
          var = (var + "LITERAL")
        end;Fi[	@I":# @return [Array<Twitter::User>] The requested users.;Fi[[	@0iGI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The requested users.
      # @overload friendships(*users);Fi[	@≈iÛI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The requested users.
      # @overload users(*users);Fi[	@I"9# @return [Array<Twitter::User>] The followed users.;Fi[[	@0i{I"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The followed users.
      # @overload follow(*users);Fi[	@0ièI"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::User>] The followed users.
      # @overload follow!(*users);Fi[	@I"&# @return [Twitter::Relationship];Fi[[	@0i¿I"7      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Relationship]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[	@0i“I"O      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Relationship]
      # @param source [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the source user.;Fi[	@r@0/i[[	@Öi+I"˚            if (var.numerator_units == ["STRING"]) and (var.denominator_units == []) then
              Sass::Script::Number.new(("LITERAL" - var.value), ["STRING"])
            else
              Compass::Util.compass_warn("STRING")
              var;Fi[	@Öi/I"v              var
            end
          else
            Compass::Util.compass_warn("STRING")
            var;Fi[	@r@%i[[	@#i~I"™    if request.params["LITERAL"].is_a?(String).! then
      var["LITERAL"]
    else
      var = var["LITERAL"]
      var = if (request.content_type.to_s == "STRING") then;Fi[	@{iÉI"n        var = {}
        var = false
      else
        var = var["LITERAL"]
        var = var["LITERAL"];Fi[	@I"0# @return [Twitter::List] The created list.;Fi[[	@ViÇI"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The created list.
      # @overload list_update(list, options={});Fi[	@Vi°I"      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::List] The created list.
      # @param name [String] The name for the list.;Fi[	@I"4# @return [Twitter::Tweet] The requested Tweet.;Fi[[	@ßi:I"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The requested Tweet.
      # @param id [Integer] A Tweet ID.;Fi[	@ºiQI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The requested Tweet.
      # @param id [Integer] A Tweet ID.;Fi[	@I"<# @return [Array<Twitter::Tweet>] The requested Tweets.;Fi[[	@ßiJI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The requested Tweets.
      # @overload statuses(*ids);Fi[	@ºidI"      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The requested Tweets.
      # @overload statuses_activity(*ids);Fi[	@I"2# @return [Twitter::Tweet] The created Tweet.;Fi[[	@ßitI"8      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The created Tweet.
      # @param status [String] The text of your status update, up to 140 characters.;Fi[	@ßi∫I"8      # @rate_limited No
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The created Tweet.
      # @param status [String] The text of your status update, up to 140 characters.;Fi[	@I"Y# @return [Array<Twitter::Tweet>] The original tweets with retweet details embedded.;Fi[[	@ßiÑI"+      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The original tweets with retweet details embedded.
      # @overload retweet(*ids);Fi[	@ßiüI"x      # @authentication Requires user context
      # @raise [Twitter::Error::AlreadyRetweeted] Error raised when tweet has already been retweeted.
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The original tweets with retweet details embedded.
      # @overload retweet!(*ids);Fi[	@rI"if var.is_a?(Hash) then;Fi[[	I"-projects//backup_proj/backup/template.rb;TiI"õ      if var.is_a?(Binding) then
        @binding = var
      else
        if var.is_a?(Hash) then
          @binding = Backup::Binder.new(var).get_binding;Fi[	@ëi&I"˙              var ||= var.to_s if var.is_a?(Pathname)
              Compass::Configuration::FileData.new_from_string(var.read, var, var)
            else
              if var.is_a?(Hash) then
                Compass::Configuration::Data.new(var, var);Fi[	@rI"3MUTEX.synchronize { Logger.message("STRING") };Fi[[	@&
iàI"˚              if var and (var.etag == var.md5) then
                MUTEX.synchronize { Logger.message("STRING") }
              else
                MUTEX.synchronize { Logger.message("STRING") }
                File.open(var.path, "STRING") do |var|;Fi[	@&
iìI"»                  MUTEX.synchronize { Logger.message("STRING") }
                  var.destroy
                else
                  MUTEX.synchronize { Logger.message("STRING") }
                end;Fi[	@ÍI"À# @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[[	@0i_I"      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      # @example Return an array of numeric IDs for every user who has a pending request to follow the authenticating user;Fi[	@0inI"       # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      # @example Return an array of numeric IDs for every protected user for whom the authenticating user has a pending follow request;Fi[	@r@w.i[[	@‘iÂI"        if var and File.exist?(var) then
          "STRING"
        else
          Logger.warn("STRING")
          # do nothing;Fi[	@‘i!I"ä            if var then
              import_key(var, var)
            else
              Logger.warn("STRING")
              # do nothing;Fi[	@r@ù(i[[	@>ioI"∫          if var.encoding.name =~ "LITERAL" then
            return var.gsub(Regexp.new("STRING".encode(var.encoding.name)), "STRING")
          else
            return var
          end;Fi[	@Õi\I"∫            if (@at.is_a?(Time) or (@at.is_a?(Numeric) and (@at > "LITERAL"))) then
              raise(ArgumentError, "STRING")
            else
              return var
            end;Fi[	@õI"@attrs = var;Fi[[	@Ci:I"m    # @param attrs [Hash]
    # @return [Twitter::Base]
    def function(var)
      @attrs = var
    end;Fi[	@ºiI"]    
    # @return [Twitter::RateLimit]
    def function(var)
      @attrs = var
    end;Fi[	@õI"@attrs.update(var);Fi[[	@CiQI"v    # @param attrs [Hash]
    # @return [Twitter::Base]
    def function(var)
      @attrs.update(var)
      self;Fi[	@ºi-I"{    # @param attrs [Hash]
    # @return [Twitter::RateLimit]
    def function(var)
      @attrs.update(var)
      self;Fi[	@HI"Q# @option options [Integer] :page Specifies the page of results to retrieve.;Fi[[	@iI"+      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Integer] :page Specifies the page of results to retrieve.
      # @example Return the 20 most recent direct messages sent to the authenticating user;Fi[	@i,I"+      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Integer] :page Specifies the page of results to retrieve.
      # @example Return the 20 most recent direct messages sent by the authenticating user;Fi[	@õI"case var.type;Fi[[	@§iI"†        
        # Extracts the value from the node as either a string or symbol
        def function(var)
          case var.type
          when "LITERAL" then;Fi[	@8iI"Ø        
        # Extracts the statement requirement from the given node
        def function(var)
          case var.type
          when "LITERAL", "LITERAL", "LITERAL" then;Fi[	I"'# @param id [Integer] A Tweet IDs.;F@Íi[[	@i<I"Q      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::DirectMessage] The requested messages.
      # @param id [Integer] A Tweet IDs.
      # @param options [Hash] A customizable set of options.
      # @example Return the direct message with the id 1825786345;Fi[	@i5I"u      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::SavedSearch] The saved searches.
      # @param id [Integer] A Tweet IDs.
      # @param options [Hash] A customizable set of options.
      # @example Retrieve the data for a saved search owned by the authenticating user with the ID 16129012;Fi[	@õI"super.each do |var|;Fi[[	@A
i∑I"œ      
      # Configures new states with the built-in humanize scheme
      def function(var)
        super.each do |var|
          var.human_name = lambda { |var, var| translate(var, "LITERAL", var.name) };Fi[	@A
iæI"Œ      
      # Configures new event with the built-in humanize scheme
      def function(var)
        super.each do |var|
          var.human_name = lambda { |var, var| translate(var, "LITERAL", var.name) };Fi[	@õI"@var = [true, false].include?(var.last) ? (var.pop) : (true);Fi[[	@iáI"U    #   
    #   vehicle.fire_events!(:ignite, :disable_alarm) # => StateMachine::InvalidTranstion: Cannot run events in parallel: ignite, disable_alarm
    def function(var)
      var = [true, false].include?(var.last) ? (var.pop) : (true)
      (fire_events(*(var + [var])) or raise(StateMachine::InvalidParallelTransition.new(self, var)));Fi[	@›iΩI"J    #   transition.perform(Time.now)        # => Passes in additional arguments and runs the +save+ action
    #   transition.perform(Time.now, false) # => Passes in additional arguments and only sets the state attribute
    def function(var)
      var = [true, false].include?(var.last) ? (var.pop) : (true)
      self.args = var;Fi[	@ˇI"y#   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.;Fi[[	@)iI"ñ      #   Returns the 20 most recent favorite Tweets for the authenticating user
      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@)i!I"ß      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;Fi[	@õI"@app = var;Fi[[	I"7projects//omni_proj/omniauth/test/phony_session.rb;TiI"Pclass OmniAuth::Test::PhonySession
  def function(var)
    @app = var
  end;Fi[	@«iI"\    end
    
    def function(var)
      @app = var
      @template = ERB.new(TEMPLATE);Fi[	@õI"/var = self.class.name.split("STRING").last;Fi[[	@ªi	I"ª    module PP
      module CharacterData
        def function(var)
          var = self.class.name.split("STRING").last
          var.group("LITERAL", "STRING", "STRING") { var.pp(text) };Fi[	@tiI"ô        end
        
        def function(var)
          var = self.class.name.split("STRING").last
          var.group("LITERAL", "STRING", "STRING") do;Fi[	@ˇI"\#   @option options [Boolean] :follow (false) Enable notifications for the target user.;Fi[[	@0i~I"z      # @overload follow(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean] :follow (false) Enable notifications for the target user.
      def function(var);Fi[	@0ióI"{      # @overload follow!(*users, options)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean] :follow (false) Enable notifications for the target user.
      def function(var);Fi[	@õI"each do |var|;Fi[[	@∏iöI"π      ###
      # Append the class attribute +name+ to all Node objects in the NodeSet.
      def function(var)
        each do |var|
          var = var["STRING"].to_s.split("LITERAL");Fi[	@∏i¶I"ü      # If +name+ is nil, remove the class attribute from all Nodes in the
      # NodeSet.
      def function(var)
        each do |var|
          if var then;Fi[	@ˇI"#   @option options [String] :mode ('public') Whether your list is public or private. Values can be 'public' or 'private'.;Fi[[	@ViÜI"ú      # @overload list_update(list, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [String] :mode ('public') Whether your list is public or private. Values can be 'public' or 'private'.
      #   @option options [String] :description The description to give the list.;Fi[	@VièI"Œ      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [String] :mode ('public') Whether your list is public or private. Values can be 'public' or 'private'.
      #   @option options [String] :description The description to give the list.;Fi[	@õI"#var = document.root.namespaces;Fi[[	@µi¡I"Á      ###
      # Search this node's immediate children using CSS selector +selector+
      def function(var)
        var = document.root.namespaces
        xpath(CSS.xpath_for(var, "LITERAL" => "STRING", "LITERAL" => (var)).first);Fi[	@∏ivI"Ò      ###
      # Search this NodeSet's nodes' immediate children using CSS selector +selector+
      def function(var)
        var = document.root.namespaces
        xpath(CSS.xpath_for(var, "LITERAL" => "STRING", "LITERAL" => (var)).first);Fi[	@ˇI"œ#     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[[	@ºi8I"Ñ      #   Returns users following followers of the specified user
      #
      #   @param options [Hash] A customizable set of options.
      #     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #     @return [Twitter::Cursor];Fi[	@ºiBI"§      #
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #     @return [Twitter::Cursor];Fi[	@õ@◊"i[[	@µiıI"á      #
      # Also see related method +<<+.
      def function(var)
        var = coerce(var)
        if var.is_a?(XML::NodeSet) then;Fi[	@µiPI"      #
      # Also see related method +inner_html=+
      def function(var)
        var = coerce(var)
        children.unlink;Fi[	@ #I"y#   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;Fi[[	@iPI"      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      #   @option options [Integer] :page Specifies the page of results to retrieve.;Fi[	@ëi2I"      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      #   @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ #I"L#   @option options [Integer] :count The number of results to retrieve.;Fi[[	@Vi7I"ˆ      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      #   @option options [Integer] :count The number of results to retrieve.
      #   @example Show tweet timeline for members of the authenticated user's "presidents" list;Fi[	@ViAI"Á      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      #   @option options [Integer] :count The number of results to retrieve.
      #   @example Show tweet timeline for members of @sferik's "presidents" list;Fi[	I" #   Returns direct messages;F@—i[[	@iVI"¬      #     Twitter.direct_messages
      # @overload direct_messages(*ids)
      #   Returns direct messages
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show;Fi[	@i]I"◊      #     Twitter.direct_messages(1825786345)
      # @overload direct_messages(*ids, options)
      #   Returns direct messages
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show;Fi[	@òA@#i[[	@iXI"      #   Returns direct messages
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Return the direct message with the id 1825786345;Fi[	@i_I"       #   Returns direct messages
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/direct_messages/show
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.;Fi[	@õI"!self.ext = File.extname(var);Fi[[	@Ô	i(I"b    #
    # Returns nothing.
    def function(var)
      self.ext = File.extname(var)
    end;Fi[	@ñi^I"ú    #
    # Returns nothing.
    def function(var)
      self.ext = File.extname(var)
      self.basename = var[("LITERAL"..(-self.ext.length - "LITERAL"))];Fi[	@#I"<#   @example Favorite the Tweet with the ID 25938088801;Fi[[	@)iPI"      # @return [Array<Twitter::Tweet>] The favorited Tweets.
      # @overload favorite(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Favorite the Tweet with the ID 25938088801
      #     Twitter.favorite(25938088801);Fi[	@)irI"      # @return [Array<Twitter::Tweet>] The favorited Tweets.
      # @overload favorite(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Favorite the Tweet with the ID 25938088801
      #     Twitter.favorite(25938088801);Fi[	@#I";#   @example Retweet the Tweet with the ID 28561922516;Fi[[	@ßiáI"+      # @return [Array<Twitter::Tweet>] The original tweets with retweet details embedded.
      # @overload retweet(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Retweet the Tweet with the ID 28561922516
      #     Twitter.retweet(28561922516);Fi[	@ßi¢I"-      # @return [Array<Twitter::Tweet>] The original tweets with retweet details embedded.
      # @overload retweet!(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Retweet the Tweet with the ID 28561922516
      #     Twitter.retweet!(28561922516);Fi[	@ˇ@+0i[[	@icI"–      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var.empty? then
          direct_messages_received(var.options);Fi[	@i&I"¯      #   @param options [Hash] A customizable set of options.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var.empty? then
          objects_from_response(Twitter::SavedSearch, "LITERAL", "STRING", var.options);Fi[	@õI"(var = @context.registers["LITERAL"];Fi[[	@{iI"©    #
    # Returns the HTML formatted String.
    def function(var)
      var = @context.registers["LITERAL"]
      var = var.getConverterImpl(Jekyll::TextileConverter);Fi[	@{iI"™    #
    # Returns the HTML formatted String.
    def function(var)
      var = @context.registers["LITERAL"]
      var = var.getConverterImpl(Jekyll::MarkdownConverter);Fi[	@õI"
setup;Fi[[	@*i^I"P    end
    
    def function(var)
      setup
      case @config["STRING"];Fi[	@_i"I"t    end
    
    def function(var)
      setup
      return RedCloth.new(var).to_html if @config["STRING"].nil?;Fi[	@ˇI"Gvar.options["LITERAL"] = true if var.options.delete("LITERAL").!.!;Fi[[	@0iÅI"      #   @option options [Boolean] :follow (false) Enable notifications for the target user.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.options["LITERAL"] = true if var.options.delete("LITERAL").!.!
        var = Thread.new { friend_ids.ids };Fi[	@0iöI"      #   @option options [Boolean] :follow (false) Enable notifications for the target user.
      def function(var)
        var = Twitter::API::Arguments.new(var)
        var.options["LITERAL"] = true if var.options.delete("LITERAL").!.!
        var.flatten.threaded_map do |var|;Fi[	@õI"Ovar = (("STRING" + @config["STRING"].gsub("STRING", "STRING")) + "STRING");Fi[[	@*iUI"•    end
    
    def function(var)
      var = (("STRING" + @config["STRING"].gsub("STRING", "STRING")) + "STRING")
      var.=~(Regexp.new(var, Regexp::IGNORECASE));Fi[	@_iI"•    end
    
    def function(var)
      var = (("STRING" + @config["STRING"].gsub("STRING", "STRING")) + "STRING")
      var.=~(Regexp.new(var, Regexp::IGNORECASE));Fi[	@õI"var = var.to_s.downcase;Fi[[	@¶iI"b  include(Comparable)
  
  def function(var)
    var = var.to_s.downcase
    @elem = case var;Fi[	@3i+I"°        "STRING" if [current_section, current_subtab].include?(var)
      end
      def function(var)
        var = var.to_s.downcase
        var = url_path(var);Fi[	@õ@ƒi[[	@µiºI"}    end
    
    def function(var)
      var = "LITERAL".match(var)
      if var and (var.begin("LITERAL") == "LITERAL") then;Fi[	@µiÕI"n    end
    
    def function(var)
      var = "LITERAL".match(var)
      raise(Error, "STRING") if var.!;Fi[	@õ@r6i[[	@µi=I"Æ    # Parses an "object" in the sense of RFC 4627.
    # Returns the parsed value and any trailing tokens.
    def function(var)
      var = eat("STRING", var)
      var = {};Fi[	@µiXI"≠    # Parses an "array" in the sense of RFC 4627.
    # Returns the parsed value and any trailing tokens.
    def function(var)
      var = eat("STRING", var)
      var = [];Fi[	@õ@üAi[[	@µiI"ÿ    # Note: this is almost the same as valparse,
    # except that it does not accept atomic values.
    def function(var)
      raise(Error, "STRING") if (var.length < "LITERAL")
      var, var, var = var["LITERAL"];Fi[	@µi,I"‹    # Parses a "value" in the sense of RFC 4627.
    # Returns the parsed value and any trailing tokens.
    def function(var)
      raise(Error, "STRING") if (var.length < "LITERAL")
      var, var, var = var["LITERAL"];Fi[	I"'require("twitter/error/forbidden");F@(i[[	@,i I"Drequire("twitter/error/forbidden")
module Twitter
  class Error;Fi [	@/i I"Drequire("twitter/error/forbidden")
module Twitter
  class Error;Fi [	@õ@m?i[[	@6$iSI"öend
class ExternalDepsTests < Test::Unit::TestCase
  def function(var)
    var = DependencyCollector.new
    var.each { |var, var| var.add(var => (var)) };Fi[	@6$iZI"u  end
  
  def function(var)
    var = DependencyCollector.new
    var.each { |var, var| var.add(var => (var)) };Fi[	@\L@s#i[[	@)iI"û      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @example Return the 20 most recent favorite Tweets for the authenticating user;Fi[	@)i"I"Ë      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      #   @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      #   @example Return the 20 most recent favorite Tweets for @sferik;Fi[	@õ@=i[[	@!i|I"P  end
  
  def function(var)
    fetch(var, []).map do |var|
      case var;Fi[	@!iäI"P  end
  
  def function(var)
    fetch(var, []).map do |var|
      case var;Fi[	@õI"if var.likes.loaded? then;Fi[[	@.i
I"ñ  # Check whether the user has liked a post.
  # @param [Post] post
  def function(var)
    if var.likes.loaded? then
      if self.like_for(var) then;Fi[	@.iI"•  # @param [Post] post
  # @return [Like]
  def function(var)
    if var.likes.loaded? then
      return var.likes.detect { |var| (var.author_id == self.person.id) };Fi[	@Í0I""merge_user!(var.options, var);Fi[[	@)i(I"ò      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var = var.pop then
          merge_user!(var.options, var)
        end;Fi[	@≈i I"‹      def function(var)
        var = Twitter::API::Arguments.new(var)
        if var = var.pop then
          merge_user!(var.options, var)
          object_from_response(Twitter::User, "LITERAL", "STRING", var.options);Fi[	@ÇA@ûi[[	@)iJI"ÿ      # Favorites the specified Tweets as the authenticating user
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/create
      # @rate_limited No
      # @authentication Requires user context;Fi[	@)ikI"      # Favorites the specified Tweets as the authenticating user and raises an error if one has already been favorited
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/favorites/create
      # @rate_limited No
      # @authentication Requires user context;Fi[	@∏KI"# @overload favorite(*ids);Fi[[	@)iNI"@      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The favorited Tweets.
      # @overload favorite(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;Fi[	@)ipI"x      # @raise [Twitter::Error::AlreadyFavorited] Error raised when tweet has already been favorited.
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The favorited Tweets.
      # @overload favorite(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;Fi[	@3M@#i[[	@)iOI"P      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The favorited Tweets.
      # @overload favorite(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Favorite the Tweet with the ID 25938088801;Fi[	@)iqI"P      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>] The favorited Tweets.
      # @overload favorite(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Favorite the Tweet with the ID 25938088801;Fi[	@ΩLI"(#     Twitter.favorite(25938088801);Fi[[	@)iQI"˝      # @overload favorite(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Favorite the Tweet with the ID 25938088801
      #     Twitter.favorite(25938088801)
      # @overload favorite(*ids, options);Fi[	@)isI"˝      # @overload favorite(*ids)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Favorite the Tweet with the ID 25938088801
      #     Twitter.favorite(25938088801)
      # @overload favorite(*ids, options);Fi[	@@MI"(# @overload favorite(*ids, options);Fi[[	@)iRI"'      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Favorite the Tweet with the ID 25938088801
      #     Twitter.favorite(25938088801)
      # @overload favorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;Fi[	@)itI"'      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Favorite the Tweet with the ID 25938088801
      #     Twitter.favorite(25938088801)
      # @overload favorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.;Fi[	@GM@#i[[	@)iSI"      #   @example Favorite the Tweet with the ID 25938088801
      #     Twitter.favorite(25938088801)
      # @overload favorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.;Fi[	@)iuI"      #   @example Favorite the Tweet with the ID 25938088801
      #     Twitter.favorite(25938088801)
      # @overload favorite(*ids, options)
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.;Fi[	@õI";var = find_or_initialize_with_error_by("LITERAL", var);Fi[[	@1
iŒI"ˇ        # If the user is already confirmed, create an error for the user
        # Options must have the confirmation_token
        def function(var)
          var = find_or_initialize_with_error_by("LITERAL", var)
          var.confirm! if var.persisted?;Fi[	@ iêI"˘        # If the user is not locked, creates an error for the user
        # Options must have the unlock_token
        def function(var)
          var = find_or_initialize_with_error_by("LITERAL", var)
          var.unlock_access! if var.persisted?;Fi[	@{@I",rescue Twitter::Error::Forbidden => var;Fi[[	@)iZI",        var.flatten.threaded_map do |var|
          begin
            object_from_response(Twitter::Tweet, "LITERAL", "STRING", var.options.merge("LITERAL" => (var)))
          rescue Twitter::Error::Forbidden => var
            raise unless (var.message == Twitter::Error::AlreadyFavorited::MESSAGE);Fi[	@)i|I""        var.flatten.threaded_map do |var|
          begin
            object_from_response(Twitter::Tweet, "LITERAL", "STRING", var.options.merge("LITERAL" => (var)))
          rescue Twitter::Error::Forbidden => var
            handle_forbidden_error(Twitter::Error::AlreadyFavorited, var);Fi[	@nK@Mi[[	@0iI"örequire("twitter/api/arguments")
require("twitter/api/utils")
require("twitter/cursor")
require("twitter/error/forbidden")
require("twitter/relationship");Fi[	@ViI"£require("twitter/api/utils")
require("twitter/core_ext/enumerable")
require("twitter/cursor")
require("twitter/error/forbidden")
require("twitter/error/not_found");Fi[	@õI"7var = lambda { |var| var.to_s.singularize.to_sym };Fi[[	I",projects//devise_proj/devise/mapping.rb;TiqI"Å    end
    
    def function(var)
      var = lambda { |var| var.to_s.singularize.to_sym }
      if var.has_key?("LITERAL") then;Fi[	@kMi~I"Ç    end
    
    def function(var)
      var = lambda { |var| var.to_s.singularize.to_sym }
      if (var["LITERAL"] == true) then;Fi[	@R@Íi[[	@0i^I"«      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@0imI"«      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Cursor]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.;Fi[	@õI"var = var.dup;Fi[[	@;iI"N    end
    
    def function(var)
      var = var.dup
      var = @index;Fi[	@;i.I"^    end
    
    def function(var)
      var = var.dup
      var = StringScanner.new(var);Fi[	@õI"var.visit(self);Fi[[	I"Oprojects//compass_proj/compass/sass_extensions/monkey_patches/traversal.rb;Ti	I"∏    class Node
      unless method_defined?("LITERAL") then
        def function(var)
          var.visit(self)
          var.down(self) if children.any? and var.respond_to?("LITERAL");Fi[	@µiåI"ê      ###
      # Accept a visitor.  This method calls "visit" on +visitor+ with self.
      def function(var)
        var.visit(self)
      end;Fi[	@õI"*if var.is_a?(Sass::Script::List) then;Fi[[	@<i0I"˘  # This can be used to unpack a space separated list that got turned
  # into a string by sass before it was passed to a mixin.
  def function(var)
    if var.is_a?(Sass::Script::List) then
      Sass::Script::List.new(var.value.dup, var.separator);Fi[	@<i:I"  # If the argument is a list, it will return a new list that is space delimited
  # Otherwise it returns a new, single element, space-delimited list.
  def function(var)
    if var.is_a?(Sass::Script::List) then
      Sass::Script::List.new(var.value.dup, "LITERAL");Fi[	@à#@ì#i[[	@0iI"Ü      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi[	@0i#I"Ü      #
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi[	@õ@5i[[	@Öi7I"¥      (var.value.size == "LITERAL") ? (var.value.first) : (var)
    end
    def function(var)
      if var.is_a?(Sass::Script::String) then
        var = var.value =~ "LITERAL".!.!;Fi[	@<iXI"∫  
  # returns the first value of a space delimited list.
  def function(var)
    if var.is_a?(Sass::Script::String) then
      Sass::Script::String.new(var.value.split("LITERAL").first);Fi[	@õ@i[[	@i6I"¬  # returns an IE hex string for a color with an alpha channel
  # suitable for passing to IE filters.
  def function(var)
    assert_type(var, "LITERAL")
    var = (var.alpha * "LITERAL").round;Fi[	@ñi"I"j    end
    
    def function(var)
      assert_type(var, "LITERAL")
      var = normalize_stops(var);Fi[	@õI"/var, var = var.split("LITERAL", "LITERAL");Fi[[	@iYI"o    end
    
    def function(var)
      var, var = var.split("LITERAL", "LITERAL")
      var ||= "STRING";Fi[	@ibI"n    end
    
    def function(var)
      var, var = var.split("LITERAL", "LITERAL")
      var = self[var];Fi[	@õI"self.args = var;Fi[[	@iI"Z    attr_accessor("LITERAL")
    
    def function(var)
      self.args = var
    end;Fi[	@iI"{    attr_accessor("LITERAL", "LITERAL", "LITERAL")
    
    def function(var)
      self.args = var
      self.options = {};Fi[	@¨@ì#i[[	@0iI"Ä      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi[	@0i-I"Ä      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.;Fi[	@¨I"◊#   @option options [Boolean, String, Integer] :filter_to_owned_lists When set to true, t or 1, will return just lists the authenticating user owns, and the user represented by user_id or screen_name is a member of.;Fi[[	@ViwI"e      # @overload memberships(options={})
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :filter_to_owned_lists When set to true, t or 1, will return just lists the authenticating user owns, and the user represented by user_id or screen_name is a member of.
      #   @example List the lists the authenticated user has been added to;Fi[	@Vi~I"ì      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :filter_to_owned_lists When set to true, t or 1, will return just lists the authenticating user owns, and the user represented by user_id or screen_name is a member of.
      #   @example List the lists that @sferik has been added to;Fi[	@õ@74i[[	@Çi I"u        end
        
        def function(var)
          inherited_reader(*var)
          inherited_writer(*var);Fi[	@ÇiOI"u        end
        
        def function(var)
          inherited_reader(*var)
          inherited_writer(*var);Fi[	@ZI"G#   @example Return extended information for @sferik and @pengwynn;Fi[[	@0iJI"|      # @return [Array<Twitter::User>] The requested users.
      # @overload friendships(*users)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @example Return extended information for @sferik and @pengwynn
      #     Twitter.friendships('sferik', 'pengwynn');Fi[	@≈iˆI"©      # @return [Array<Twitter::User>] The requested users.
      # @overload users(*users)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @example Return extended information for @sferik and @pengwynn
      #     Twitter.users('sferik', 'pengwynn')  # Retrieve users with a POST request using screen_names;Fi[	@ZI" #   @example Follow @sferik;Fi[[	@0i~I">      # @return [Array<Twitter::User>] The followed users.
      # @overload follow(*users)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @example Follow @sferik
      #     Twitter.follow('sferik');Fi[	@0iíI"@      # @return [Array<Twitter::User>] The followed users.
      # @overload follow!(*users)
      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @example Follow @sferik
      #     Twitter.follow!('sferik');Fi[	@{A@ûi[[	@0ixI"ˇ      # Allows the authenticating user to follow the specified users, unless they are already followed
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/create
      # @rate_limited No
      # @authentication Requires user context;Fi[	@0iåI"›      # Allows the authenticating user to follow the specified users
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/friendships/create
      # @rate_limited No
      # @authentication Requires user context;Fi[	@rL@õi[[	@0iI"      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean] :follow (false) Enable notifications for the target user.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@0iòI"      #   @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen names, or objects.
      #   @param options [Hash] A customizable set of options.
      #   @option options [Boolean] :follow (false) Enable notifications for the target user.
      def function(var)
        var = Twitter::API::Arguments.new(var);Fi[	@”K@õi[[	@fiI"~    #
    # @param attrs [Hash]
    # @return [Twitter::Relationship]
    def function(var)
      @attrs = var["LITERAL"];Fi[	@fiI"ö    #
    # @param attrs [Hash]
    # @return [Twitter::Relationship]
    def function(var)
      @attrs.update(var["LITERAL"]) unless var["LITERAL"].nil?;Fi[	@õI"var.each do |var, var|;Fi[[	@∂iDI"à      end
      
      def function(var)
        var.each do |var, var|
          self.send("STRING", var) if self.respond_to?("STRING");Fi[	@õi
I"ü      ###
      # Register namespaces in +namespaces+
      def function(var)
        var.each do |var, var|
          var = var.to_s.gsub("LITERAL", "STRING");Fi[	I"u# @param source [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the source user.;FI"u# @param target [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the target user.;Fi[[	@0i‘I"ª      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Relationship]
      # @param source [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the source user.
      # @param target [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the target user.
      # @param options [Hash] A customizable set of options.;Fi[	@0iÓI"›      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Boolean] true if user_a follows user_b, otherwise false.
      # @param source [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the source user.
      # @param target [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the target user.
      # @param options [Hash] A customizable set of options.;Fi[	@ÁM@Íi[[	@0i’I"ô      # @return [Twitter::Relationship]
      # @param source [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the source user.
      # @param target [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the target user.
      # @param options [Hash] A customizable set of options.
      # @example Return the relationship between @sferik and @pengwynn;Fi[	@0iÔI"Æ      # @return [Boolean] true if user_a follows user_b, otherwise false.
      # @param source [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the source user.
      # @param target [Integer, String, Twitter::User] The Twitter user ID, screen name, or object of the target user.
      # @param options [Hash] A customizable set of options.
      # @example Return true if @sferik follows @pengwynn;Fi[	@õI"#var = /^#{Regexp.escape(var)}/;Fi[[	@qiI"Ñ    end
    
    def function(var)
      var = /^#{Regexp.escape(var)}/
      var = @commands.keys.select { |var| var.to_s.=~(var) };Fi[	@qi$I"~    end
    
    def function(var)
      var = /^#{Regexp.escape(var)}/
      @commands.keys.detect { |var| var.to_s.=~(var) };Fi[	@õ@%i[[	@xi∏I"À    # The second object, if present, is used as a prefix,
    # just like you can do with `dom_id()` and `dom_class()` in Rails
    def function(var)
      var = var["LITERAL"]
      var = var["LITERAL"];Fi[	@µiﬁI"Ã    # The rules are different than for Ruby, so we cannot use eval.
    # Unquote will raise an error if q contains control characters.
    def function(var)
      var = var["LITERAL"]
      var = var.dup;Fi[	@∆%@â1i[[	@0iI"û      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids;Fi[	@0i%I"û      #   @option options [Integer] :cursor (-1) Breaks the results into pages. This is recommended for users who are following many users. Provide a value of -1 to begin paging. Provide values as returned in the response body's next_cursor and previous_cursor attributes to page back and forth in the list.
      #   @option options [Boolean, String, Integer] :skip_status Do not include contributee's Tweets when set to true, 't' or 1.
      #   @option options [Boolean, String, Integer] :include_user_entities The user entities node will be disincluded when set to false.
      #   @example Return the authenticated user's friends' IDs
      #     Twitter.friend_ids;Fi[	@õI"var ||= {};Fi[[	@aiI""  end
  
  def function(var)
    var ||= {}
    var = { "LITERAL" => (self.id), "LITERAL" => (self.guid), "LITERAL" => (self.name), "LITERAL" => (self.profile.image_url("LITERAL")), "LITERAL" => (self.diaspora_handle), "LITERAL" => (Rails.application.routes.url_helpers.person_path(self)) };Fi[	@˛
ißI"Ú    # given query.  If a match is found, then a hash containing the
    # event/state requirements that passed will be returned; otherwise, nil.
    def function(var)
      var ||= {}
      if match_event(var) and var = match_states(var) then;Fi[	@õ@S7i[[	@›iGI"§      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var, var = case var
        when "LITERAL" then;Fi[	@‡iÅI"§      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(var)
        var, var = case var
        when "LITERAL" then;Fi[	@˛I"i#   @param user_to_remove [Integer, String] The user id or screen name of the list member to remove.;Fi[[	@ViYI"u      # @return [Twitter::List] The list.
      # @overload list_remove_member(list, user_to_remove, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_remove [Integer, String] The user id or screen name of the list member to remove.
      #   @param options [Hash] A customizable set of options.;Fi[	@VibI"≥      # @overload list_remove_member(user, list, user_to_remove, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_remove [Integer, String] The user id or screen name of the list member to remove.
      #   @param options [Hash] A customizable set of options.;Fi[	@˛I"i#   @param user_to_check [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.;Fi[[	@ViæI"ß      # @return [Boolean] true if user is a subscriber of the specified list, otherwise false.
      # @overload list_subscriber?(list, user_to_check, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@Vi«I"∞      # @overload list_subscriber?(user, list, user_to_check, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.;Fi[	@˛I"^#   @param user_to_check [Integer, String] The user ID or screen name of the list member.;Fi[[	@ViI"î      # @return [Boolean] true if user is a member of the specified list, otherwise false.
      # @overload list_member?(list, user_to_check, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String] The user ID or screen name of the list member.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViI"°      # @overload list_member?(user, list, user_to_check, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String] The user ID or screen name of the list member.
      #   @param options [Hash] A customizable set of options.;Fi[	@˛I"\#   @param user_to_add [Integer, String] The user id or screen name to add to the list.;Fi[[	@ViNI"b      # @return [Twitter::List] The list.
      # @overload list_add_member(list, user_to_add, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_add [Integer, String] The user id or screen name to add to the list.
      #   @param options [Hash] A customizable set of options.;Fi[	@ViVI"†      # @overload list_add_member(user, list, user_to_add, options={})
      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_add [Integer, String] The user id or screen name to add to the list.
      #   @param options [Hash] A customizable set of options.;Fi[	@õ@„"i[[	@\<i I"≤    # Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking
    # pop, a ThreadError is raised.
    def function(var)
      if var then
        synchronize do;Fi[	@_<iCI"≤    # Pass +true+ for a non-blocking pop.  If nothing is read on a non-blocking
    # pop, a ThreadError is raised.
    def function(var)
      if var then
        synchronize do;Fi[	@N@ˇi[[	@ViZI"¢      # @overload list_remove_member(list, user_to_remove, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_remove [Integer, String] The user id or screen name of the list member to remove.
      #   @param options [Hash] A customizable set of options.
      #   @example Remove @BarackObama from the authenticated user's "presidents" list;Fi[	@VicI"Æ      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_remove [Integer, String] The user id or screen name of the list member to remove.
      #   @param options [Hash] A customizable set of options.
      #   @example Remove @BarackObama from @sferik's "presidents" list;Fi[	@N@ˇi[[	@ViøI"Ø      # @overload list_subscriber?(list, user_to_check, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Check if @BarackObama is a subscriber of the authenticated user's "presidents" list;Fi[	@Vi»I"æ      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #   @example Check if @BarackObama is a subscriber of @sferik's "presidents" list;Fi[	@"N@ˇi[[	@ViI"ú      # @overload list_member?(list, user_to_check, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String] The user ID or screen name of the list member.
      #   @param options [Hash] A customizable set of options.
      #   @example Check if @BarackObama is a member of the authenticated user's "presidents" list;Fi[	@ViI"Ø      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_check [Integer, String] The user ID or screen name of the list member.
      #   @param options [Hash] A customizable set of options.
      #   @example Check if @BarackObama is a member of @sferik's "presidents" list;Fi[	@)N@ˇi[[	@ViOI"ä      # @overload list_add_member(list, user_to_add, options={})
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_add [Integer, String] The user id or screen name to add to the list.
      #   @param options [Hash] A customizable set of options.
      #   @example Add @BarackObama to the authenticated user's "presidents" list;Fi[	@ViWI"ú      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param user_to_add [Integer, String] The user id or screen name to add to the list.
      #   @param options [Hash] A customizable set of options.
      #   @example Add @BarackObama to @sferik's "presidents" list;Fi[	@ÄLI"P#   @option options [String] :description The description to give the list.;Fi[[	@ViáI"ˆ      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [String] :mode ('public') Whether your list is public or private. Values can be 'public' or 'private'.
      #   @option options [String] :description The description to give the list.
      #   @example Update the authenticated user's "presidents" list to have the description "Presidents of the United States of America";Fi[	@ViêI"Î      #   @param list [Integer, String, Twitter::List] A Twitter list ID, slug, or object.
      #   @param options [Hash] A customizable set of options.
      #   @option options [String] :mode ('public') Whether your list is public or private. Values can be 'public' or 'private'.
      #   @option options [String] :description The description to give the list.
      #   @example Update the @sferik's "presidents" list to have the description "Presidents of the United States of America";Fi[	@õI"@model = var;Fi[[	@@iI"∑      # Creates a new instance of the MongoDB database object
      # * Called using super(model) from subclasses *
      def function(var)
        @model = var
        load_defaults!;Fi[	@èi!I"}      ##
      # Called with super(model) from subclasses
      def function(var)
        @model = var
        load_defaults!;Fi[	@UN@Ìi[[	@@iI"É      # * Called using super(model) from subclasses *
      def function(var)
        @model = var
        load_defaults!
      end;Fi[	@èi"I"¢      # Called with super(model) from subclasses
      def function(var)
        @model = var
        load_defaults!
        @on_success = true if on_success.nil?;Fi[	@ê@ªDi[[	@&i	I"}    module SAX
      class PushParser
        def function(var, var, var)
          @document = var
          @encoding = var;Fi[	@âiI"‘        # Create a new PushParser with +doc+ as the SAX Document, providing
        # an optional +file_name+ and +encoding+
        def function(var, var, var)
          @document = var
          @encoding = var;Fi[	@êI"return self unless var;Fi[[	@iI"j      end
      
      def function(var, var, var)
        return self unless var
        if var then;Fi[	@¨iI"≤      #  subtree created, e.g., namespaces will be resolved relative
      #  to +ctx+.
      def function(var, var, var)
        return self unless var
        var = if var then;Fi[	@∂@s2i[[	@?i)I"û      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array]
      def function(var, var, var, var);Fi[	@?iEI"û      # @param request_method [Symbol]
      # @param path [String]
      # @param args [Array]
      # @return [Array]
      def function(var, var, var, var);Fi[	@1@…i[[	@ViI"‡        begin
          (var = Twitter::API::Arguments.new(var)
          merge_user!(var.options, var.pop)
          merge_list!(var.options, var.pop)
          unless (var.options["LITERAL"] or var.options["LITERAL"]) then;Fi[	@Vi(I"Î      def function(var, var, var)
        var = Twitter::API::Arguments.new(var)
        merge_user!(var.options, var.pop)
        merge_list!(var.options, var.pop)
        unless (var.options["LITERAL"] or var.options["LITERAL"]) then;Fi[	@ÜI"^# @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.;Fi[[	@?i}I"›      # Take a user and merge it into the hash with the correct key
      #
      # @param hash [Hash]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.
      # @return [Hash];Fi[	@?iÜI"›      # Take a user and merge it into the hash with the correct key
      #
      # @param hash [Hash]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.
      # @return [Hash];Fi[	@ÜI"ì# @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.;Fi[[	@?iôI"$      # Take a multiple users and merge them into the hash with the correct keys
      #
      # @param hash [Hash]
      # @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.
      # @return [Hash];Fi[	@?i¢I"$      # Take a multiple users and merge them into the hash with the correct keys
      #
      # @param hash [Hash]
      # @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.
      # @return [Hash];Fi[	@ÜI"# @param key [Symbol];Fi[[	@ïiI"é    # Return a hash that includes everything but the given keys.
    #
    # @param hash [Hash]
    # @param key [Symbol]
    # @return [Hash];Fi[	@ïi I"}    # Replaces the hash without the given keys.
    #
    # @param hash [Hash]
    # @param key [Symbol]
    # @return [Hash];Fi[	@ê@Di[[	@îiEI"m      end
      
      def function(var, var, var)
        var = [var.first, var["LITERAL"]]
        var;Fi[	@îiJI"m      end
      
      def function(var, var, var)
        var = [var.first, var["LITERAL"]]
        var;Fi[	I"&# @return [Array<Twitter::Place>];FI"T# @example Return the locations that Twitter has trending topic information for;Fi[[	@†i*I"F      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @return [Array<Twitter::Place>]
      # @example Return the locations that Twitter has trending topic information for
      #   Twitter.trends_available;Fi[	@†i<I"ö      # @option options [Float] :lat If provided with a :long option the available trend locations will be sorted by distance, nearest to furthest, to the co-ordinate pair. The valid ranges for latitude are -90.0 to +90.0 (North is positive) inclusive.
      # @option options [Float] :long If provided with a :lat option the available trend locations will be sorted by distance, nearest to furthest, to the co-ordinate pair. The valid ranges for longitude are -180.0 to +180.0 (East is positive) inclusive.
      # @return [Array<Twitter::Place>]
      # @example Return the locations that Twitter has trending topic information for
      #   Twitter.trends_closest;Fi[	I"«# @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.;FI"Ï# @option options [String] :"attribute:street_address" This option searches for places which have this given street address. There are other well-known and application-specific attributes available. Custom attributes are also permitted.;Fi[[	@yi=I"°      # @option options [String] :accuracy ('0m') A hint on the "region" in which to search. If a number, then this is a radius in meters, but it can also take a string that is suffixed with ft to specify feet. If coming from a device, in practice, this value is whatever accuracy the device has measuring its location (whether it be coming from a GPS, WiFi triangulation, etc.).
      # @option options [Integer] :max_results A hint as to the number of results to return. This does not guarantee that the number of results returned will equal max_results, but instead informs how many "nearby" results to return. Ideally, only pass in the number of places you intend to display to the user here.
      # @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.
      # @option options [String] :"attribute:street_address" This option searches for places which have this given street address. There are other well-known and application-specific attributes available. Custom attributes are also permitted.
      # @return [Array<Twitter::Place>];Fi[	@yiSI"V      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :name The name a place is known as.
      # @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.
      # @option options [String] :"attribute:street_address" This option searches for places which have this given street address. There are other well-known and application-specific attributes available. Custom attributes are also permitted.
      # @return [Array<Twitter::Place>];Fi[	@üN@ñNi[[	@yi>I"r      # @option options [Integer] :max_results A hint as to the number of results to return. This does not guarantee that the number of results returned will equal max_results, but instead informs how many "nearby" results to return. Ideally, only pass in the number of places you intend to display to the user here.
      # @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.
      # @option options [String] :"attribute:street_address" This option searches for places which have this given street address. There are other well-known and application-specific attributes available. Custom attributes are also permitted.
      # @return [Array<Twitter::Place>]
      # @example Return an array of places near the IP address 74.125.19.104;Fi[	@yiTI"n      # @option options [String] :name The name a place is known as.
      # @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.
      # @option options [String] :"attribute:street_address" This option searches for places which have this given street address. There are other well-known and application-specific attributes available. Custom attributes are also permitted.
      # @return [Array<Twitter::Place>]
      # @example Return an array of places similar to Twitter HQ;Fi[	I"C# @option options [String] :name The name a place is known as.;F@ûNi[[	@yiRI"      # @option options [Float] :lat The latitude to search around. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude to search around. The valid range for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :name The name a place is known as.
      # @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.
      # @option options [String] :"attribute:street_address" This option searches for places which have this given street address. There are other well-known and application-specific attributes available. Custom attributes are also permitted.;Fi[	@yieI"2      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @param options [Hash] A customizable set of options.
      # @option options [String] :name The name a place is known as.
      # @option options [String] :contained_within This is the place_id which you would like to restrict the search results to. Setting this value means only places within the given place_id will be found.
      # @option options [String] :token The token found in the response from {Twitter::API::PlacesAndGeo#places_similar}.;Fi[	@s2@êi[[	@yiwI"€      # @param path [String]
      # @param params [Hash]
      # @return [Array]
      def function(var, var, var)
        objects_from_array(Twitter::Place, send(var.to_sym, var, var)["LITERAL"]["LITERAL"]["LITERAL"]);Fi[	@ëi´I"û      # @param collection [Array]
      # @param max_id [Integer, NilClass]
      # @return [Array]
      def function(var, var, var)
        var = yield(var);Fi[	@ê@˛Ci[[	@îi¶I"ù      
      # reduce 27 omitted
      def function(var, var, var)
        var = Node.new("LITERAL", ([var["LITERAL"]] + (var["LITERAL"] or [])))
        var;Fi[	@îi´I"ç      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", ([var["LITERAL"]] + (var["LITERAL"] or [])))
        var;Fi[	@ê@˜Ci[[	@îiïI"ê      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [[var.first, var.last].compact.join("STRING")])
        var;Fi[	@îiµI"ê      end
      
      def function(var, var, var)
        var = Node.new("LITERAL", [[var.first, var.last].compact.join("STRING")])
        var;Fi[	I"N#   Retrieve the data for saved searches owned by the authenticating user;F@—i[[	@iI"Ô      #     Twitter.saved_searches
      # @overload saved_search(*ids)
      #   Retrieve the data for saved searches owned by the authenticating user
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id;Fi[	@i I"       #     Twitter.saved_search(16129012)
      # @overload saved_search(*ids, options)
      #   Retrieve the data for saved searches owned by the authenticating user
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id;Fi[	@mA@#i[[	@iI"`      #   Retrieve the data for saved searches owned by the authenticating user
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @example Retrieve the data for a saved search owned by the authenticating user with the ID 16129012;Fi[	@i"I"1      #   Retrieve the data for saved searches owned by the authenticating user
      #
      #   @see https://dev.twitter.com/docs/api/1.1/get/saved_searches/show/:id
      #   @param ids [Array<Integer>, Set<Integer>] An array of Tweet IDs.
      #   @param options [Hash] A customizable set of options.;Fi[	@ê@‹Ci[[	@îi4I"]      end
      
      def function(var, var, var)
        var = var.flatten
        var;Fi[	@îi9I"]      end
      
      def function(var, var, var)
        var = var.flatten
        var;Fi[	@!I"7# @example Update the authenticating user's status;Fi[[	@ßi}I"=      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.
      # @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @example Update the authenticating user's status
      #   Twitter.update("I'm tweeting with @gem!");Fi[	@ßiƒI"i      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.
      # @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @example Update the authenticating user's status
      #   Twitter.update_with_media("I'm tweeting with @gem!", File.new('my_awesome_pic.jpeg'));Fi[	@êI"var["LITERAL"] = true;Fi[[	@ÃiÜI"Ω      # it's configured to bind to the object as this is the convention for
      # DataMapper/Extlib callbacks
      def function(var, var, var)
        var["LITERAL"] = true
        super;Fi[	@M
iõI"Ì      # it's configured to bind to the object as this is the convention for
      # Sequel callbacks
      def function(var, var, var)
        var["LITERAL"] = true
        var["LITERAL"] = @terminator ||= lambda { |var| (var == false) };Fi[	@e@ƒ4i[[	@i>I"z        end
        
        ##
        # This is the provider that Fog uses for the Cloud Files
        def function;Fi[	@i5I"z        end
        
        ##
        # This is the provider that Fog uses for the Cloud Files
        def function;Fi[	@e@;5i[[	@	iI"s      attr_accessor("LITERAL")
      
      ##
      # Amazon Simple Storage Service (S3) Credentials
      ##;Fi[	@iI"}        attr_accessor("LITERAL")
        
        ##
        # Amazon Simple Storage Service (S3) Credentials
        ##;Fi[	@e@B5i[[	@iwI"p      end
      
      ##
      # Returns Rsync syntax for defining a port to connect to
      def function;Fi[	@iMI"z        end
        
        ##
        # Returns Rsync syntax for defining a port to connect to
        def function;Fi[	@∫2I"}# @option options [Boolean, String, Integer] :include_entities The tweet entities node will be disincluded when set to false.;Fi[[	@ëizI"A      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :include_rts Specifies that the timeline should include native retweets in addition to regular tweets. Note: If you're using the trim_user parameter in conjunction with include_rts, the retweets will no longer contain a full user object.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @option options [Boolean, String, Integer] :include_entities The tweet entities node will be disincluded when set to false.
      # @example Return the 20 most recent Tweets, including retweets if they exist, posted by the authenticating user and the users they follow;Fi[	@ëiãI"≤      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @option options [Boolean, String, Integer] :include_entities The tweet entities node will be disincluded when set to false.
      # @example Return the 20 most recent retweets posted by users followed by the authenticating user;Fi[	@XAI"N# @note This method can only return up to 800 Tweets, including retweets.;Fi[[	@ëimI"G      # Returns the 20 most recent Tweets, including retweets if they exist, posted by the authenticating user and the users they follow
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes;Fi[	@ëiI"      # Returns the 20 most recent retweets posted by users the authenticating user follow.
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes;Fi[	@ˇN@®i[[	@ëinI"Ï      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ëiÄI"Ï      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@IA@®i[[	@ßiI"‹      # Returns up to 100 of the first retweets of a given tweet
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ßi#I"–      # Show up to 100 users who retweeted the Tweet
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/retweets/:id
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	I"A# @param id [Integer] The numerical ID of the desired Tweet.;F@Íi[[	@ßiI"â      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param id [Integer] The numerical ID of the desired Tweet.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.;Fi[	@ßi(I"y      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array]
      # @param id [Integer] The numerical ID of the desired Tweet.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.;Fi[	@Ô"@4i[[	@ºiI"ç      # @return [Array] An array of actions
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @example Return activity about me;Fi[	@ºi(I"è      # @return [Array] An array of actions
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @example Return activity by friends;Fi[	@Ô"@!i[[	@ßiI"      # @param id [Integer] The numerical ID of the desired Tweet.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @example Return up to 100 of the first retweets of the Tweet with the ID 28561922516;Fi[	@ßi*I"      # @param id [Integer] The numerical ID of the desired Tweet.
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean] :ids_only ('false') Only return user ids instead of full user objects.;Fi[	@!A@®i[[	@ßi7I"Ø      # Returns a Tweet
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ßiGI"Æ      # Returns Tweets
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/show/:id
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ÌKI"&# @param id [Integer] A Tweet ID.;Fi[[	@ßi;I"1      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The requested Tweet.
      # @param id [Integer] A Tweet ID.
      # @param options [Hash] A customizable set of options.;Fi[	@ºiRI"1      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The requested Tweet.
      # @param id [Integer] A Tweet ID.
      # @param options [Hash] A customizable set of options.;Fi[	@+O@Íi[[	@ßi<I"æ      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The requested Tweet.
      # @param id [Integer] A Tweet ID.
      # @param options [Hash] A customizable set of options.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ºiSI"R      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The requested Tweet.
      # @param id [Integer] A Tweet ID.
      # @param options [Hash] A customizable set of options.
      # @example Return activity summary for the Tweet with the ID 25938088801;Fi[	@˚KI"S# @param status [String] The text of your status update, up to 140 characters.;Fi[[	@ßiuI"\      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The created Tweet.
      # @param status [String] The text of your status update, up to 140 characters.
      # @param options [Hash] A customizable set of options.;Fi[	@ßiªI"t      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Twitter::Tweet] The created Tweet.
      # @param status [String] The text of your status update, up to 140 characters.
      # @param media [File, Hash] A File object with your picture (PNG, JPEG or GIF);Fi[	I"˛# @option options [Float] :lat The latitude of the location this tweet refers to. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.;FI"7# @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.;Fi[[	@ßiyI"Ñ      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :in_reply_to_status_id The ID of an existing status that the update is in reply to.
      # @option options [Float] :lat The latitude of the location this tweet refers to. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.;Fi[	@ßi¿I"É      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :in_reply_to_status_id The ID of an existing Tweet that the update is in reply to.
      # @option options [Float] :lat The latitude of the location this tweet refers to. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.;Fi[	@@OI"à# @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.;Fi[[	@ßizI"Œ      # @option options [Integer] :in_reply_to_status_id The ID of an existing status that the update is in reply to.
      # @option options [Float] :lat The latitude of the location this tweet refers to. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.
      # @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.;Fi[	@ßi¡I"Õ      # @option options [Integer] :in_reply_to_status_id The ID of an existing Tweet that the update is in reply to.
      # @option options [Float] :lat The latitude of the location this tweet refers to. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.
      # @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.;Fi[	@GOI"Ä# @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.;Fi[[	@ßi{I"      # @option options [Float] :lat The latitude of the location this tweet refers to. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.
      # @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@ßi¬I"      # @option options [Float] :lat The latitude of the location this tweet refers to. This option will be ignored unless it is inside the range -90.0 to +90.0 (North is positive) inclusive. It will also be ignored if there isn't a corresponding :long option.
      # @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.
      # @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;Fi[	@NO@!i[[	@ßi|I"G      # @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.
      # @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @example Update the authenticating user's status;Fi[	@ßi√I"G      # @option options [Float] :long The longitude of the location this tweet refers to. The valid ranges for longitude is -180.0 to +180.0 (East is positive) inclusive. This option will be ignored if outside that range, if it is not a number, if geo_enabled is disabled, or if there not a corresponding :lat option.
      # @option options [String] :place_id A place in the world. These IDs can be retrieved from {Twitter::API::PlacesAndGeo#reverse_geocode}.
      # @option options [String] :display_coordinates Whether or not to put a pin on the exact coordinates a tweet has been sent from.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @example Update the authenticating user's status;Fi[	@A@®i[[	@ßiÅI"‹      # Retweets the specified Tweets as the authenticating user
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/retweet/:id
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ßiõI"      # Retweets the specified Tweets as the authenticating user and raises an error if one has already been retweeted
      #
      # @see https://dev.twitter.com/docs/api/1.1/post/statuses/retweet/:id
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@a@@[Mi[[	@ßiíI"Í        var.flatten.threaded_map do |var|
          begin
            post_retweet(var, var.options)
          rescue Twitter::Error::Forbidden => var
            raise unless (var.message == Twitter::Error::AlreadyRetweeted::MESSAGE);Fi[	@ßi≠I"‡        var.flatten.threaded_map do |var|
          begin
            post_retweet(var, var.options)
          rescue Twitter::Error::Forbidden => var
            handle_forbidden_error(Twitter::Error::AlreadyRetweeted, var);Fi[	@A@®i[[	@ßiÕI"∏      # Returns oEmbed for a Tweet
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@ßi„I"∏      # Returns oEmbeds for Tweets
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/oembed
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@e@P5i[[	@iiI"Ä      end
      
      ##
      # Removes the previously created @password_file
      # (temporary file containing the password);Fi[	@idI"ä        end
        
        ##
        # Removes the previously created @password_file
        # (temporary file containing the password);Fi[	I"*# @return [Array] An array of actions;F@Íi[[	@ºiI"x      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array] An array of actions
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.;Fi[	@ºi&I"y      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid./
      # @return [Array] An array of actions
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 100.;Fi[	@éLI"$#     @return [Twitter::Cursor];Fi[[	@ºi9I"~      #
      #   @param options [Hash] A customizable set of options.
      #     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #     @return [Twitter::Cursor]
      #   @example Return users follow followers of @sferik;Fi[	@ºiCI"ÿ      #   @param user [Integer, String, Twitter::User] A Twitter user ID, screen name, or object.
      #   @param options [Hash] A customizable set of options.
      #     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #     @return [Twitter::Cursor]
      #   @example Return users follow followers of @sferik;Fi[	@zOI":#   @example Return users follow followers of @sferik;Fi[[	@ºi:I"°      #   @param options [Hash] A customizable set of options.
      #     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #     @return [Twitter::Cursor]
      #   @example Return users follow followers of @sferik
      #     Twitter.following_followers_of;Fi[	@ºiDI"´      #   @param options [Hash] A customizable set of options.
      #     @option options [Integer] :cursor (-1) Breaks the results into pages. Provide values as returned in the response objects's next_cursor and previous_cursor attributes to page back and forth in the list.
      #     @return [Twitter::Cursor]
      #   @example Return users follow followers of @sferik
      #     Twitter.following_followers_of('sferik');Fi[	@e@Å5i[[	@i^I"ô      end
      
      ##
      # Writes the provided password to a temporary file so that
      # the rsync utility can read the password from this file;Fi[	@iYI"£        end
        
        ##
        # Writes the provided password to a temporary file so that
        # the rsync utility can read the password from this file;Fi[	@A@®i[[	@≈iùI"·      # Returns an array of numeric user ids the authenticating user is blocking
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@≈iÆI"⁄      # Returns true if the authenticating user is blocking a target user
      #
      # @see https://dev.twitter.com/docs/api/1.1/get/blocks/ids
      # @rate_limited Yes
      # @authentication Requires user context;Fi[	@e@ñ5i[[	@iI"a      ##
      # Server credentials
      ##
      # Server IP Address and SSH port
      ##;Fi[	@iI"k        ##
        # Server credentials
        ##
        # Server IP Address and SSH port
        ##;Fi[	I"&# @param collection_name [Symbol];F@=i[[	@?iXI"|      end
      
      # @param collection_name [Symbol]
      # @param klass [Class]
      # @param request_method [Symbol];Fi[	@?igI"|      end
      
      # @param collection_name [Symbol]
      # @param klass [Class]
      # @param request_method [Symbol];Fi[	I""# @param method_name [Symbol];F@Ri[[	@?i]I"Ø      # @param path [String]
      # @param args [Array]
      # @param method_name [Symbol]
      # @return [Twitter::Cursor]
      def function(var, var, var, var, var, var);Fi[	@?ilI"±      # @param path [String]
      # @param options [Hash]
      # @param method_name [Symbol]
      # @return [Twitter::Cursor]
      def function(var, var, var, var, var, var);Fi[	I"B# Take a user and merge it into the hash with the correct key;F@—i[[	@?i{I"|      end
      
      # Take a user and merge it into the hash with the correct key
      #
      # @param hash [Hash];Fi[	@?iÑI"|      end
      
      # Take a user and merge it into the hash with the correct key
      #
      # @param hash [Hash];Fi[	@{N@Yi[[	@?i~I"ª      #
      # @param hash [Hash]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.
      # @return [Hash]
      def function(var, var, var);Fi[	@?iáI"ª      #
      # @param hash [Hash]
      # @param user [Integer, String, Twitter::User] A Twitter user ID, screen_name, or object.
      # @return [Hash]
      def function(var, var, var);Fi[	I"O# Take a multiple users and merge them into the hash with the correct keys;F@—i[[	@?ióI"Ñ      end
      
      # Take a multiple users and merge them into the hash with the correct keys
      #
      # @param hash [Hash];Fi[	@?i†I"Ñ      end
      
      # Take a multiple users and merge them into the hash with the correct keys
      #
      # @param hash [Hash];Fi[	@ÇN@Yi[[	@?iöI"      #
      # @param hash [Hash]
      # @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.
      # @return [Hash]
      def function(var, var);Fi[	@?i£I"      #
      # @param hash [Hash]
      # @param users [Array<Integer, String, Twitter::User>, Set<Integer, String, Twitter::User>] An array of Twitter user IDs, screen_names, or objects.
      # @return [Hash]
      def function(var, var);Fi[	I"4require("twitter/error/identity_map_key_error");F@(i[[	@Ci I"Prequire("twitter/error/identity_map_key_error")
module Twitter
  class Base;Fi [	@ iI"|require("twitter/base")
require("twitter/error/identity_map_key_error")
module Twitter
  class Identity < Twitter::Base;Fi[	I"def self.fetch(var);FI"return unless identity_map;Fi[[	@CiI"    end
    
    def self.fetch(var)
      return unless identity_map
      if var = identity_map.fetch(Marshal.dump(var)) then;Fi[	@ i	I"Émodule Twitter
  class Identity < Twitter::Base
    def self.fetch(var)
      return unless identity_map
      var = var["LITERAL"];Fi[	I"def self.store(var);FI"#return var unless identity_map;Fi[[	@Ci$I"Ä    end
    
    def self.store(var)
      return var unless identity_map
      identity_map.store(Marshal.dump(var.attrs), var);Fi[	@ iI"t    end
    
    def self.store(var)
      return var unless identity_map
      identity_map.store(var.id, var);Fi[	@e@∞5i[[	@i!I"_      end
      
      ##
      # Establishes a connection to Amazon S3
      def function;Fi[	@	i%I"_      end
      
      ##
      # Establishes a connection to Amazon S3
      def function;Fi[	@û@”Ki[[	@fi
I"    # Initializes a new object
    #
    # @param attrs [Hash]
    # @return [Twitter::Relationship]
    def function(var);Fi[	@fiI"â    # Update the attributes of a Relationship
    #
    # @param attrs [Hash]
    # @return [Twitter::Relationship]
    def function(var);Fi[	@ûI"# @return [Twitter::Base];Fi[[	@Ci8I"w    # Initializes a new object
    #
    # @param attrs [Hash]
    # @return [Twitter::Base]
    def function(var);Fi[	@CiOI"|    # Update the attributes of an object
    #
    # @param attrs [Hash]
    # @return [Twitter::Base]
    def function(var);Fi[	@ÂO@õi[[	@Ci9I"k    #
    # @param attrs [Hash]
    # @return [Twitter::Base]
    def function(var)
      @attrs = var;Fi[	@CiPI"q    #
    # @param attrs [Hash]
    # @return [Twitter::Base]
    def function(var)
      @attrs.update(var);Fi[	I"## @param other [Twitter::Base];F@ÿi[[	@CiYI"z    
    # @param attr [Symbol]
    # @param other [Twitter::Base]
    # @return [Boolean]
    def function(var, var);Fi[	@Ci_I"b    end
    
    # @param other [Twitter::Base]
    # @return [Boolean]
    def function(var);Fi[	@eI"!# Campfire account's room id;Fi[[	@ÌiI"{      ##
      # Campfire account's subdomain
      ##
      # Campfire account's room id
      def function(var, var);Fi[	@Ìi\I"q        ##
        # Campfire account's subdomain
        ##
        # Campfire account's room id
        ##;Fi[	I"require("faraday");FI"require("multi_json");Fi[[	I"-projects//twitter_proj/twitter/client.rb;Ti I"Trequire("faraday")
require("multi_json")
require("twitter/api/direct_messages");Fi [	@Ki I"<require("faraday")
require("multi_json")
module Twitter;Fi [	@e@c8i[[	@ÌiI"n      ##
      # Campfire api authentication token
      ##
      # Campfire account's subdomain
      ##;Fi[	@ÌiZI"|        ##
        # Campfire api authentication api_token
        ##
        # Campfire account's subdomain
        ##;Fi[	@eI"C# This is called as part of the procedure run by the Packager.;Fi[[	@‘iPI"†      end
      
      ##
      # This is called as part of the procedure run by the Packager.
      # It sets up the needed options to pass to the gpg command,;Fi[	@ûi$I"§      end
      
      ##
      # This is called as part of the procedure run by the Packager.
      # It sets up the needed options to pass to the openssl command,;Fi[	@e@<i[[	@
i!I"í      ##
      # Tables to skip while dumping the database
      ##
      # Tables to dump, tables that aren't specified won't get dumped
      ##;Fi[	@
i I"í      ##
      # Tables to skip while dumping the database
      ##
      # Tables to dump, tables that aren't specified won't get dumped
      ##;Fi[	@e@%<i[[	@
iI"n      ##
      # Connectivity options
      ##
      # Tables to skip while dumping the database
      ##;Fi[	@
iI"n      ##
      # Connectivity options
      ##
      # Tables to skip while dumping the database
      ##;Fi[	I"# @return [Time];F@Ói[[	I"0projects//twitter_proj/twitter/creatable.rb;Ti
I"ù    # Time when the object was created on Twitter
    #
    # @return [Time]
    def function
      @created_at ||= Time.parse(@attrs["LITERAL"]) if created?;Fi[	@ºiI"T    end
    
    # @return [Time]
    def function
      var = @attrs["STRING"];Fi[	@eI"<# Creates a new instance of the MongoDB database object;Fi[[	@@iI"±      include(Backup::Configuration::Helpers)
      
      ##
      # Creates a new instance of the MongoDB database object
      # * Called using super(model) from subclasses *;Fi[	@Úi.I"≠      ##
      # 'lock' dump meaning wrapping mongodump with fsync & lock
      ##
      # Creates a new instance of the MongoDB database object
      def function(var, var);Fi[	@e@X>i[[	@ëiI"^        end
        
        ##
        # Used only within the specs
        def function;Fi[	@\3iI"T      end
      
      ##
      # Used only within the specs
      def function;Fi[	@e@í?i[[	@ÅiI"Ÿ      attr_deprecate("LITERAL", "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => (lambda { |var, var| var.level = "LITERAL" if var }))
      
      ##
      # Specify the level of compression to use.
      #;Fi[	@áiI"Ÿ      attr_deprecate("LITERAL", "LITERAL" => "STRING", "LITERAL" => "STRING", "LITERAL" => (lambda { |var, var| var.level = "LITERAL" if var }))
      
      ##
      # Specify the level of compression to use.
      #;Fi[	I"A# Return a hash that includes everything but the given keys.;F@—i[[	@ïi	I"w    private
    
    # Return a hash that includes everything but the given keys.
    #
    # @param klass [Class];Fi[	@ïiI"q    end
    
    # Return a hash that includes everything but the given keys.
    #
    # @param hash [Hash];Fi[	@âN@Yi[[	@ïiI"m    #
    # @param hash [Hash]
    # @param key [Symbol]
    # @return [Hash]
    def function(var, var);Fi[	@ïi!I"m    #
    # @param hash [Hash]
    # @param key [Symbol]
    # @return [Hash]
    def function(var, var);Fi[	I"require("twitter/geo");F@(i[[	@≤i I"6require("twitter/geo")
module Twitter
  class Geo;Fi [	@Ki I"6require("twitter/geo")
module Twitter
  class Geo;Fi [	@îI"module CLI;Fi[[	@†(i I"2module Backup
  module CLI
    module Helpers;Fi [	@li I"8module Backup
  module CLI
    class Utility < Thor;Fi [	@ÚF@
3i[[	@9iI"¡      # @return [Array<Twitter::Size>]
      def function
        @sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|
          var[var] = Twitter::Size.fetch_or_new(var)
          var;Fi[	@ciI"∑    # @return [Array<Twitter::Size>]
    def function
      @sizes ||= Array(@attrs["LITERAL"]).inject({}) do |var, var|
        var[var] = Twitter::Size.fetch_or_new(var)
        var;Fi[	I"# @return [Twitter::Geo];F@Ói[[	@πiI"ú    alias "LITERAL" "LITERAL"
    
    # @return [Twitter::Geo]
    def function
      @bounding_box ||= Twitter::GeoFactory.fetch_or_new(@attrs["LITERAL"]);Fi[	@ÃiBI"}    end
    
    # @return [Twitter::Geo]
    def function
      @geo ||= Twitter::GeoFactory.fetch_or_new(@attrs["LITERAL"]);Fi[	I"## @return [Twitter::RateLimit];F@õi[[	@ºiI"s    alias "LITERAL" "LITERAL"
    
    # @return [Twitter::RateLimit]
    def function(var)
      @attrs = var;Fi[	@ºi,I"v    #
    # @param attrs [Hash]
    # @return [Twitter::RateLimit]
    def function(var)
      @attrs.update(var);Fi[	I"# @return [Twitter::Place];F@Ói[[	@ki
I"˝    attr_reader("LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL", "LITERAL")
    
    # @return [Twitter::Place]
    def function
      @trend_location ||= Twitter::Place.fetch_or_new(Array(@attrs["LITERAL"]).first);Fi[	@ÃiXI"|    end
    
    # @return [Twitter::Place]
    def function
      @place ||= Twitter::Place.fetch_or_new(@attrs["LITERAL"]);Fi[	I"## @param other [Twitter::Size];F@ÿi[[	@niI"x    alias "LITERAL" "LITERAL"
    
    # @param other [Twitter::Size]
    # @return [Boolean]
    def function(var);Fi[	@niI"h    protected
    
    # @param other [Twitter::Size]
    # @return [Boolean]
    def function(var);Fi[	I""require("twitter/basic_user");F@(i[[	I"2projects//twitter_proj/twitter/source_user.rb;Ti I"Yrequire("twitter/basic_user")
module Twitter
  class SourceUser < Twitter::BasicUser;Fi [	I"2projects//twitter_proj/twitter/target_user.rb;Ti I"Yrequire("twitter/basic_user")
module Twitter
  class TargetUser < Twitter::BasicUser;Fi [	I"# @return [Twitter::Tweet];F@Ói[[	@ÃiqI"ƒ    # If this Tweet is a retweet, the original Tweet is available here.
    #
    # @return [Twitter::Tweet]
    def function
      @retweeted_status ||= self.class.fetch_or_new(@attrs["LITERAL"]);Fi[	@,'i\I"¨    alias "LITERAL" "LITERAL"
    
    # @return [Twitter::Tweet]
    def function
      @status ||= fetch_or_new_without_self(Twitter::Tweet, @attrs, "LITERAL", "LITERAL");Fi[	@@@«i[[	@,'i0I"˝    # Return the URL to the user's profile banner image
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mobile', 'mobile_retina', 'web', 'web_retina', 'ipad', or 'ipad_retina'
    # @return [String]
    def function(var);Fi[	@,'i:I"    # Return the secure URL to the user's profile banner image
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mobile', 'mobile_retina', 'web', 'web_retina', 'ipad', or 'ipad_retina'
    # @return [String]
    def function(var);Fi[	@’@@«i[[	@,'iHI"÷    # Return the URL to the user's profile image
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mini', 'normal', 'bigger' or 'original'
    # @return [String]
    def function(var);Fi[	@,'iPI"›    # Return the secure URL to the user's profile image
    #
    # @param size [String, Symbol] The size of the image. Must be one of: 'mini', 'normal', 'bigger' or 'original'
    # @return [String]
    def function(var);Fi[	I""_cset("LITERAL") { "STRING" };F@ñPi[[	@d"iI"π  _cset("LITERAL") { fetch("LITERAL") }
  _cset("LITERAL") { fetch("LITERAL", "STRING") }
  _cset("LITERAL") { "STRING" }
  _cset("LITERAL") { "STRING" }
  _cset("LITERAL") { "STRING" };Fi[	@d"iI"´  _cset("LITERAL") { fetch("LITERAL", "STRING") }
  _cset("LITERAL") { "STRING" }
  _cset("LITERAL") { "STRING" }
  _cset("LITERAL") { "STRING" }
  namespace("LITERAL") do;Fi[	I"module Whenever;FI"module Output;Fi[[	@ÕiI"Frequire("chronic")
module Whenever
  module Output
    class Cron;Fi[	@Ái I":module Whenever
  module Output
    class Redirection;Fi [	I"var.close!;F@a!i[[	@‡iNI"        var = "STRING" if @options["LITERAL"]
        puts("STRING")
        var.close!
        exit("LITERAL")
      else;Fi[	@‡iRI"[      else
        warn("STRING")
        var.close!
        exit("LITERAL")
      end;Fi[	@a!@ri[[	@‡iOI"h        puts("STRING")
        var.close!
        exit("LITERAL")
      else
        warn("STRING");Fi[	@‡iZI"      if read_crontab.=~(Regexp.new("STRING")) and read_crontab.=~(Regexp.new("STRING")).nil? then
        warn("STRING")
        exit("LITERAL")
      else
        if read_crontab.=~(Regexp.new("STRING")).nil? and read_crontab.=~(Regexp.new("STRING")) then;Fi[	I"?var["LITERAL"] = @at.is_a?(Time) ? (@at.min) : ("LITERAL");FI"@var["LITERAL"] = @at.is_a?(Time) ? (@at.hour) : ("LITERAL");Fi[[	@ÕixI"G        when ("LITERAL".month.."LITERAL".months) then
          var = ((((@time / "LITERAL") / "LITERAL") / "LITERAL") / "LITERAL").round
          var["LITERAL"] = @at.is_a?(Time) ? (@at.min) : ("LITERAL")
          var["LITERAL"] = @at.is_a?(Time) ? (@at.hour) : ("LITERAL")
          var["LITERAL"] = if @at.is_a?(Time) then;Fi[	@ÕiÖI"        var = @time.to_s
        var = Array.new("LITERAL", "STRING")
        var["LITERAL"] = @at.is_a?(Time) ? (@at.min) : ("LITERAL")
        var["LITERAL"] = @at.is_a?(Time) ? (@at.hour) : ("LITERAL")
        return ((var << "STRING") * "STRING") if var.downcase.index("STRING");Fi[	@ôJ@ri[[	@úJiI"~if Whenever.bundler? then
  job_type("LITERAL", "STRING")
  job_type("LITERAL", "STRING")
else
  job_type("LITERAL", "STRING");Fi[	@úJiI"fend
if Whenever.rails3? then
  job_type("LITERAL", "STRING")
else
  job_type("LITERAL", "STRING");Fi[	@ôJ@ôJi[[	@úJi
I"|job_type("LITERAL", "STRING")
if Whenever.bundler? then
  job_type("LITERAL", "STRING")
  job_type("LITERAL", "STRING")
else;Fi[	@úJiI"m  job_type("LITERAL", "STRING")
else
  job_type("LITERAL", "STRING")
  job_type("LITERAL", "STRING")
end;Fi
