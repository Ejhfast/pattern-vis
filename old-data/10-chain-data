[{:loci)O:projects[I"activemodel_proj:ETI"activerecord_proj;TI"backup_proj;TI"bundler_proj;TI"cancan_proj;TI"capistrano_proj;TI"compass_proj;TI"cucumber_prok;TI"devise_proj;TI"diaspora_proj;TI"haml_proj;TI"homebrew_proj;TI"jekyll_proj;TI"mongoid_proj;TI"nokogiri_proj;TI"omni_proj;TI"paperclip_proj;TI"redmine_proj;TI"rescue_proj;TI"rspec_proj;TI"sinatra_proj;TI"SiriProxy_proj;TI"state_machine_proj;TI"twitter_proj;TI"whenever_proj;T[õ[I"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi[[	I"2data//redmine_proj/plugins/rfpdf/lib/tcpdf.rb;Tia
I"    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/ib
I"    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/ic
I"    newobj
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/id
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/ie
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/if
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/ig
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/ih
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/ii
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/ij
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/ik
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/il
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/im
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/in
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    newobj;Fi[	@/io
I"    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    newobj
    out("STRING");Fi[I"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");FI"empty_directory("STRING");Fi[[	I"Mdata//redmine_proj/generators/redmine_plugin/redmine_plugin_generator.rb;TiI"à  end
  
  def function(arglist)
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@ZiI"†  
  def function(arglist)
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@ZiI"ª  def function(arglist)
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@ZiI"¡    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@ZiI"¡    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING");Fi[	@ZiI"ƒ    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    template("STRING", "STRING");Fi[	@ZiI"«    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    empty_directory("STRING")
    template("STRING", "STRING")
    template("STRING", "STRING");Fi[I"# `:warning`;FI"D# : The backup completed successfully, but warnings were logged;FI"C# : Notification will be sent, including a copy of the current;FI"6# : backup log, if `on_warning` was set to `true`;FI"#;FI"# `:failure`;FI"%# : The backup operation failed.;FI"H# : Notification will be sent, including the Exception which caused;FI"F# : the failure, the Exception's backtrace, a copy of the current;FI"K# : backup log and other information if `on_failure` was set to `true`;Fi[[	I"2data//backup_proj/backup/notifier/campfire.rb;Ti$I"ö      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var);Fi[	I"1data//backup_proj/backup/notifier/hipchat.rb;Ti<I"ö      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var);Fi[	I".data//backup_proj/backup/notifier/mail.rb;Ti{I"ö      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var);Fi[	I"/data//backup_proj/backup/notifier/prowl.rb;Ti"I"ö      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var);Fi[	I"2data//backup_proj/backup/notifier/pushover.rb;Ti,I"ö      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var);Fi[	I"1data//backup_proj/backup/notifier/twitter.rb;Ti I"ö      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var);Fi[@j@k@lI"#;FI"# `:failure`;F@o@p@q@rI"#;Fi[[	@ui%I"Ñ      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var;Fi[	@xi=I"â      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var, var = case var;Fi[	@{i|I"â      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var, var = case var;Fi[	@~i#I"Ñ      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var;Fi[	@|i-I"Ñ      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var;Fi[	@i!I"Ñ      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var;Fi[I"# `:success`;FI"+# : The backup completed successfully.;FI"D# : Notification will be sent if `on_success` was set to `true`;FI"#;FI"# `:warning`;F@j@k@lI"#;FI"# `:failure`;Fi[[	@ui I"a      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@xi8I"a      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@{iwI"a      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@~iI"a      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@|i(I"a      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[	@iI"a      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused;Fi[I"##;FI"7# Notify the user of the backup operation results.;FI"/# `status` indicates one of the following:;FI"#;FI"# `:success`;F@î@ïI"#;FI"# `:warning`;F@ji[[	@uiI"˙      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@xi4I"˙      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@{isI"˙      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@~iI"˙      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@|i$I"˙      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[	@iI"˙      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`
      # : The backup completed successfully.
      # : Notification will be sent if `on_success` was set to `true`
      #
      # `:warning`
      # : The backup completed successfully, but warnings were logged
      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`;Fi[I"$def function(arglist, var, var);FI"super(var);FI"(instance_eval(&var) if block_given?;FI"end;FI" ;FI"private;FI" ;FI"##;F@©@™i
[
[	@uiI"i      # Campfire account's subdomain
      ##
      # Campfire account's room id
      def function(arglist, var, var)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@{ilI"≈      # Folder where mail will be kept when using the `:file` `delivery_method` option.
      # Default location is '$HOME/Backup/emails'
      # Example: '/tmp/test-mails'
      def function(arglist, var, var)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@~iI"x      ##
      # API-Key
      # Create a Prowl account and request an API key on prowlapp.com.
      def function(arglist, var, var)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@|iI"d      # The message title
      ##
      # The priority of the notification
      def function(arglist, var, var)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[	@iI"d      # Twitter consumer key credentials
      ##
      # OAuth credentials
      def function(arglist, var, var)
        super(var)
        instance_eval(&var) if block_given?
      end
      
      private
      
      ##
      # Notify the user of the backup operation results.
      # `status` indicates one of the following:
      #
      # `:success`;Fi[I"def function(arglist, var);FI"@var = var;FI"	self;FI"end;FI" ;FI"def function(arglist, var);FI"@var = var;FI"	self;FI"end;FI" ;Fi
[
[	I"Pdata//paperclip_proj/paperclip/matchers/validate_attachment_size_matcher.rb;TiI"/          @var = var
        end
        
        def function(arglist, var)
          @var = var
          self
        end
        
        def function(arglist, var)
          @var = var
          self
        end
        
        def function(arglist, var)
          @var, @var = var.first, var.last;Fi[	I"-data//rspec_proj/spec/matchers/change.rb;Ti7I"˘        "STRING"
      end
      
      def function(arglist, var)
        @var = var
        self
      end
      
      def function(arglist, var)
        @var = var
        self
      end
      
      def function(arglist, var)
        @var = var;Fi[	@‡i<I"ı        self
      end
      
      def function(arglist, var)
        @var = var
        self
      end
      
      def function(arglist, var)
        @var = var
        self
      end
      
      def function(arglist, var)
        @var = var;Fi[	@‡iAI"ı        self
      end
      
      def function(arglist, var)
        @var = var
        self
      end
      
      def function(arglist, var)
        @var = var
        self
      end
      
      def function(arglist, var)
        @var = var;Fi[	@‡iFI"Ó        self
      end
      
      def function(arglist, var)
        @var = var
        self
      end
      
      def function(arglist, var)
        @var = var
        self
      end
      
      def function(arglist)
        "STRING";Fi[I"when "STRING" then;FI""STRING";FI"when "STRING" then;FI""STRING";FI"when "STRING" then;FI""STRING";FI"when "STRING" then;FI""STRING";FI"when "STRING" then;FI""STRING";Fi
[
[	I"Fdata//compass_proj/compass/sass_extensions/functions/constants.rb;TiI"∞      var.value.map! do |arglist, var|
        if var.is_a?(Sass::Script::String) then
          var = case var.value
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else
            (Compass::Util.compass_warn("STRING");Fi[	@ıiDI"h    def function(arglist, var)
      var = var.value.split(/ +/).map do |arglist, var|
        case var
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        else
          var;Fi[	I"%data//haml_proj/haml/compiler.rb;Ti˜I"ö              "STRING"
            else
              case @var.value[:type]
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              else
                "STRING";Fi[	I"6data//nokogiri_proj/nokogiri/css/xpath_visitor.rb;TigI"|            "STRING"
          when "STRING", "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING";Fi[	@˝iiI"x            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else
            (var.value.first + "STRING");Fi[I"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";FI"@var["STRING"] = "STRING";Fi
[
[	I"7data//redmine_proj/plugins/rfpdf/lib/rfpdf/fpdf.rb;Ti*I"ê    @var = 0
    @var = []
    @var = {}
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@i+I"°    @var = []
    @var = {}
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@i,I"±    @var = {}
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING";Fi[	@i-I"¡    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    if (var == "STRING") then;Fi[	@i.I"≤    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    @var["STRING"] = "STRING"
    if (var == "STRING") then
      @var = 1;Fi[I"def function(arglist);FI"var = "STRING";FI")var.extend(StringInreplaceExtension);FI"-var.change_make_var!("STRING", "STRING");FI" assert_equal("STRING", var);FI"end;FI" ;FI"def function(arglist);FI"var = "STRING";FI")var.extend(StringInreplaceExtension);Fi
[
[	I"/data//homebrew_proj/test/test_inreplace.rb;TiI"•require("testing_env")
require("extend/string")
class InreplaceTest < Test::Unit::TestCase
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@%iI"s    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@%iI"s    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@%iI"s    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var);Fi[	@%i$I"i    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.remove_make_var!("STRING")
    assert_equal("STRING", var);Fi[I"newobj;FI"out("STRING");FI"$out(("STRING" + var["STRING"]));FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi	[	[	I"9data//redmine_proj/plugins/rfpdf/lib/fpdf/chinese.rb;TiUI"#    end
    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	I":data//redmine_proj/plugins/rfpdf/lib/fpdf/japanese.rb;TiVI"     out((var + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	I"8data//redmine_proj/plugins/rfpdf/lib/fpdf/korean.rb;Ti@I"#    end
    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/iùI"     out((var + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[I"var = -1;FI"var = var;FI"var = 0;FI"if (var == 1) then;FI"@var = @var;FI"!var = ((@var - @var) - @var);FI"/var = (((var - (2 * @var)) * 1000) / @var);FI"end;FI"var = (var + 1);FI"	next;Fi	[	[	@<iΩI"≤      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      var = var if (var.! or (var.chr == "STRING"));Fi[	@?i∏I"é      if (var == 10) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      if (var < 128) then;Fi[	@Bi¨I"≤      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      var = var if (var.! or (var.chr == "STRING"));Fi[	@iöI"£      if (var == "STRING"[0]) then
        self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      if (var == "STRING"[0]) then;Fi[I"var = @var["STRING"];FI"!var = ((@var - @var) - @var);FI"/var = (((var - (2 * @var)) * 1000) / @var);FI"'var = var.gsub("STRING", "STRING");FI"var = var.length;FI"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;FI"var = 1;Fi	[	[	@<i≠I"p  end
  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@?i®I"p  end
  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@BiúI"p  end
  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@iãI"?  end
  
  def function(arglist, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var];Fi[I"var = "STRING";F@@@I"end;FI" ;FI"def function(arglist);FI"var = "STRING";F@"I"-var.change_make_var!("STRING", "STRING");Fi	[	[	@%i	I"îrequire("extend/string")
class InreplaceTest < Test::Unit::TestCase
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@%iI"Y  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@%iI"Y  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[	@%iI"Y  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end;Fi[@G@H@I@J@K@L@M@N@OI"	else;Fi	[	[	@<i€I"∑          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + (var ? (1) : (2)))
      end;Fi[	@?i„I"¡          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + var)
        var = var if (var >= 128);Fi[	@Bi I"∑          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + (var ? (1) : (2)))
      end;Fi[	@iªI"É          self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
          var = (var + 1)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
      else
        var = (var + 1)
      end;Fi[@\@]@^I"var = var.length;FI"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;FI"var = 1;FI"while (var < var) do;Fi	[	[	@<iÆI"Ç  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128);Fi[	@?i©I"z  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var;Fi[	@BiùI"Ç  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128);Fi[	@iåI"\  
  def function(arglist, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var]
      if (var == "STRING"[0]) then;Fi[@@@I"end;FI" ;FI"def function(arglist);FI"var = "STRING";F@"@tI" assert_equal("STRING", var);Fi	[	[	@%i
I"~class InreplaceTest < Test::Unit::TestCase
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  ;Fi[	@%iI"V  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  ;Fi[	@%iI"V  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  ;Fi[	@%iI"V  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  
  def function(arglist)
    var = "STRING"
    var.extend(StringInreplaceExtension)
    var.change_make_var!("STRING", "STRING")
    assert_equal("STRING", var)
  end
  ;Fi[I"var = (var + 1);FI"var = -1;FI"var = var;FI"var = 0;F@JI"@var = @var;F@L@MI"end;FI"var = (var + 1);Fi	[	[	@<iºI"ñ      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end;Fi[	@?i∑I"Ñ      var = var
      if (var == 10) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end;Fi[	@Bi´I"ñ      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end;Fi[	@iôI"ï      var = var[var]
      if (var == "STRING"[0]) then
        self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end;Fi[I"# `:failure`;F@o@p@q@rI"#;FI"def function(arglist, var);FI"var = case var;FI"when :success then;FI""STRING";Fi	[	[	@ui)I"9      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING";Fi[	@~i'I"9      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING";Fi[	@|i1I"9      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :failure then
          "STRING";Fi[	@i%I"9      # : Notification will be sent, including a copy of the current
      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING";Fi[I"$out(("STRING" + var["STRING"]));FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi	[	[	@<iWI"˛    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@?iXI"˛    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@BiBI"˛    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@/iüI"˝    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
  ;Fi[@#@$@%@&@'@(@)@*@+I"end;Fi	[	[	@<iXI"Ï    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@?iYI"Ï    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@BiCI"Ï    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@/i†I"Ô    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
  
  #;Fi[@#I"newobj;FI"out("STRING");F@2I"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi	[	[	@<iTI"      end
    end
    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@?iUI"    end
    out((var + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@Bi?I".      var = (var + "STRING")
    end
    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[	@/iúI"    end
    out((var + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING");Fi[@#I"Cout(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]));FI"$out(("STRING" + var["STRING"]));FI"3out((("STRING" + (@var + 1).to_s) + "STRING"));FI"out("STRING");FI"out("STRING");FI"newobj;FI"out("STRING");FI"out("STRING");FI"$out(("STRING" + var["STRING"]));Fi	[	[	@<i:I"  end
  
  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((((("STRING" + textstring("STRING")) + "STRING") + textstring(var["STRING"]["STRING"])) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[	@?iCI"‰  end
  
  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[	@Bi)I"‰  end
  
  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[	@/iäI"¯  def function(arglist, var)
    newobj
    out("STRING")
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[I"var = var;FI"var = 0;F@JI"@var = @var;F@L@MI"end;FI"var = (var + 1);FI"	next;FI"end;Fi	[	[	@<iæI"«        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      var = var if (var.! or (var.chr == "STRING"))
      var = (var + (var ? (var[var.chr]) : (1000) or 0));Fi[	@?iπI"û        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      if (var < 128) then
        var = (var + (var[var.chr] or 0));Fi[	@Bi≠I"«        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      var = var if (var.! or (var.chr == "STRING"))
      var = (var + (var ? (var[var.chr]) : (1000) or 0));Fi[	@iõI"í        self.Cell(var, var, var[var, (var - var)], 0, 2, "STRING", 0, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next
      end
      if (var == "STRING"[0]) then
        var = var;Fi[@#@I"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");FI"out("STRING");Fi	[	[	@<iVI"!    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end;Fi[	@?iWI"    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end;Fi[	@BiAI"!    out((("STRING" + var) + "STRING"))
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end;Fi[	@/iûI"    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end;Fi[I""STRING";F@ÎI""STRING";F@ÌI""STRING";F@ÔI""STRING";F@ÒI""STRING";FI"	else;Fi	[	[	@ıiI"†        if var.is_a?(Sass::Script::String) then
          var = case var.value
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else
            (Compass::Util.compass_warn("STRING")
            var.value);Fi[	@ıiEI"U      var = var.value.split(/ +/).map do |arglist, var|
        case var
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        when "STRING" then
          "STRING"
        else
          var
        end;Fi[	@˙i¯I"ï            else
              case @var.value[:type]
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              when "STRING" then
                "STRING"
              else
                "STRING"
              end;Fi[	@˝ijI"q          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          when "STRING" then
            "STRING"
          else
            (var.value.first + "STRING")
          end;Fi[I"*#   class Person < ActiveRecord::Base;FI"#     has_many :pets;TI"#   end;TI"#;TI"#   person.pets;TI"#   # => [;TI"A#   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;TI";#   #       #<Pet id: 2, name: "Spook", person_id: 1>,;TI">#   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;TI"#   #    ];Ti	[	[	I"Kdata//activerecord_proj/active_record/associations/collection_proxy.rb;Ti%I"‡      #
      # *First:* Specify a subset of fields to be selected from the result set.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.select(:name);Ti[	@Ni^I"F      # rules as <tt>ActiveRecord::Base.find</tt>. Returns <tt>ActiveRecord::RecordNotFound</tt>
      # error if the object can not be found.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.find(1) # => #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>;Ti[	@Ni|I"+      # If the collection is empty, the first form returns +nil+, and the second
      # form returns an empty array.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.first # => #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>;Ti[	@NiïI"(      # If the collection is empty, the first form returns +nil+, and the second
      # form returns an empty array.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.last # => #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[I"assert_not_nil(var.head);FI"assert_nil(var.stable);FI"assert_nil(var.bottle);FI"assert_nil(var.devel);FI",assert_equal(var.head, var.active_spec);FI"0assert_version_equal("STRING", var.version);FI""assert_nil(var.head.checksum);FI"$assert_equal("STRING", var.url);FI"=assert_equal(GitDownloadStrategy, var.download_strategy);FI"<assert_instance_of(GitDownloadStrategy, var.downloader);Fi[[	I"-data//homebrew_proj/test/test_formula.rb;Ti±I"˛  
  def function(arglist)
    var = HeadOnlySpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[	@ci¿I"  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[	@ciœI"  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end;Fi[I"@var = var;FI"@var = (var + var);FI"	else;FI"@var = var if (var == 2);FI"end;FI"end;FI"end;FI" ;FI".def function(arglist, var, var, var, var);FI")if (@var["STRING"] == "STRING") then;Fi[[	@<iúI"(      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var)
    else;Fi[	@?ióI"*      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then
      SJISWrite(var, var, var, var)
    else;Fi[	@BiãI"(      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var)
    else;Fi[I"
begin;FI"!var = (var + var.iconv(var));FI""rescue Iconv::IllegalSequence;FI"var = (var + $!.success);FI"6var = ("STRING" + $!.failed[1, $!.failed.length]);FI"
retry;FI"rescue;FI"var = (var + $!.success);FI"end;FI"var = var;Fi[[	I"/data//redmine_proj/redmine/codeset_util.rb;TiI"ó        else
          var = Iconv.new("STRING", "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end;Fi[	@ài>I"í        else
          var = Iconv.new("STRING", var)
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end;Fi[	@ài|I"í        else
          var = Iconv.new(var, "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end;Fi[@B@C@D@EI" #   person.pets.size # => 3;TI"#   person.pets;TI"#   # => [;TI"A#   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;TI";#   #       #<Pet id: 2, name: "Spook", person_id: 1>,;TI">#   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[[	@NièI"ﬁ      # This will _always_ remove the records ignoring the +:dependent+
      # option.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@NiI"	      # You can pass +Fixnum+ or +String+ values, it finds the records
      # responding to the +id+ and executes delete on them.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@NiI"      # This method will _always_ remove record from the database ignoring
      # the +:dependent+ option. Returns an array with the removed records.
      #
      #   class Person < ActiveRecord::Base
      #     has_many :pets
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[I"#   # => [;F@H@I@JI"#   #    ];TI"#;TI"#   person.pets.delete_all;TI"#   # => [;TI"A#   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;TI";#   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[[	@Ni>I",      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@Ni_I",      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[	@NixI",      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ];Ti[I".def function(arglist, var, var, var, var);FI"var = @var["STRING"];F@\@]@^I"var = var.length;FI"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;Fi[[	@<i¨I"7    end
  end
  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@?ißI"7    end
  end
  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@BiõI"7    end
  end
  
  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[I"var = @var;FI"var = @var;FI"var = @var["STRING"];FI"/var = ((@var - @var) - @var) if (var == 0);FI"/var = (((var - (2 * @var)) * 1000) / @var);FI"'var = var.gsub("STRING", "STRING");FI"var = var.length;FI"Bvar = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING");FI"var = 0;FI"if var then;Fi[[	@<iYI"¢  end
  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING";Fi[	@?iNI"¢  end
  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING";Fi[	@BiHI"¢  end
  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING";Fi[@ª@Ω@æ@ø@¿I"var = var.length;F@¬I"var = 0;FI"if var then;FI"if (var == 1) then;Fi[[	@<iZI"≥  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING";Fi[	@?iOI"≥  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING";Fi[	@BiII"≥  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING";Fi[@ªI" @var.each do |arglist, var|;FI"newobj;FI"'out((("STRING" + var) + "STRING"));FI"out("STRING");FI"end;FI"*@var.each_pair do |arglist, var, var|;FI"newobj;FI"@var[var]["STRING"] = @var;FI"5var = (FPDF_FONTPATH + var) if defined("STRING");Fi[[	@<iI"m  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!;Fi[	@?i˘I"m  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!;Fi[	@BiﬂI"m  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!;Fi[I"#   person.pets;TI"#   # => [;T@H@I@JI"#   #    ];TI"#;T@†I"#   # => [;T@¢i[[	@Ni=I")      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@Ni^I")      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[	@NiwI")      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[@H@I@JI"#   #    ];TI"#;T@†I"#   # => [;T@¢@£I">#   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;Ti[[	@Ni?I",      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@Ni`I",      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[	@NiyI",      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #;Ti[@I@JI"#   #    ];TI"#;T@†I"#   # => [;T@¢@£@˙I"#   #    ];Ti[[	@Ni@I",      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.size # => 0;Ti[	@NiaI"&      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1, 2, 3);Ti[	@NizI"&      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   Pet.find(1, 2, 3);Ti[I" #   person.pets.size # => 3;TI"#   person.pets;TI"#   # => [;TI"A#   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,;TI";#   #       #<Pet id: 2, name: "Spook", person_id: 1>,;TI">#   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>;TI"#   #    ];TI"#;T@†I"#   # => [;Ti[[	@Ni<I",      #     has_many :pets # dependent: :nullify option by default
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@Ni]I"      #     has_many :pets, dependent: :destroy
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[	@NivI"      #     has_many :pets, dependent: :delete_all
      #   end
      #
      #   person.pets.size # => 3
      #   person.pets
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,
      #   #       #<Pet id: 3, name: "Choo-Choo", person_id: 1>
      #   #    ]
      #
      #   person.pets.delete_all
      #   # => [
      #   #       #<Pet id: 1, name: "Fancy-Fancy", person_id: 1>,
      #   #       #<Pet id: 2, name: "Spook", person_id: 1>,;Ti[I"unless var.empty? then;FI"opoo("STRING");FI"puts("STRING");FI"puts("STRING");FI"puts(var);FI"@var = true;FI"end;FI"end;FI" ;FI"def function(arglist);Fi[[	I"-data//homebrew_proj/formula_installer.rb;Ti=I"@      next if var.directory?
      var.include?(var.extname).!
    end
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    return unless f.bin.directory?
    var = f.bin.children.select do |arglist, var|;Fi[	@,iKI"a    var = f.bin.children.select do |arglist, var|
      (var.directory? or var.executable?.!)
    end
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    return unless f.sbin.directory?
    var = f.sbin.children.select do |arglist, var|;Fi[	@,iYI"2    var = f.sbin.children.select do |arglist, var|
      (var.directory? or var.executable?.!)
    end
    unless var.empty? then
      opoo("STRING")
      puts("STRING")
      puts("STRING")
      puts(var)
      @var = true
    end
  end
  
  def function(arglist)
    check_jars
    check_non_libraries;Fi[I"var = 0;F@[I"var = var.length;FI"var = 0;FI"while (var < var) do;FI"?var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);FI"if (var < 128) then;FI"/var = (var + var[var.chr]) if var[var.chr];FI"var = (var + 1);FI"	else;Fi[[	@<i?I"h  end
  
  def function(arglist, var)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000)
        var = (var + 2);Fi[	@?i/I"  end
  
  def function(arglist, var)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        if (var >= 161) and (var <= 223) then
          var = (var + 500);Fi[	@Bi.I"h  end
  
  def function(arglist, var)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000)
        var = (var + 2);Fi[@3I"if var then;F@–I"var = "STRING";FI"var = "STRING";FI"var = "STRING";FI"	else;FI"var = "STRING";FI"8var = "STRING" unless var.to_s.index("STRING").nil?;FI"@var = (var + "STRING") unless var.to_s.index("STRING").nil?;Fi[[	@<iaI"Á    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end;Fi[	@?iVI"Á    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end;Fi[	@BiPI"Á    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end;Fi[I" @var.each do |arglist, var|;FI"newobj;F@€I"out("STRING");FI"end;F@ﬁI"newobj;F@‡@·I"var = filesize(var);Fi[[	@<iÒI"  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var));Fi[	@?i˙I"  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var));Fi[	@Bi‡I"  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var));Fi[I"	else;FI"$out(("STRING" + var["STRING"]));FI"out("STRING");FI"out("STRING");FI".out((("STRING" + (@var + 1)) + "STRING"));FI".out((("STRING" + (@var + 2)) + "STRING"));FI"if var["STRING"] then;FI"!if var["STRING"].nil?.! then;FI"9out((("STRING" + (var + var["STRING"])) + "STRING"));FI"	else;Fi[[	@<iI"        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end;Fi[	@?iI"        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end;Fi[	@BiI"        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end;Fi[@2I"when :warning then;FI""STRING";FI"when :failure then;FI""STRING";FI"	else;FI"# do nothing;FI"end;FI"var = ("STRING" % var);FI"send_message(var);Fi[[	@ui2I";      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      ;Fi[	@~i0I";      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      ;Fi[	@i.I";      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      ;Fi[@2@vI""STRING";FI"	else;FI"# do nothing;FI"end;F@{@|I"end;FI" ;Fi[[	@ui4I"O        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      ##
      # Creates a new Campfire::Interface object and passes in the;Fi[	@~i2I"t        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      def function(arglist, var)
        var = Prowler.new(:application => (application), :api_key => (api_key));Fi[	@i0I"R        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      def function(arglist, var)
        ::Twitter.configure do |arglist, var|;Fi[@bI"DCell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);FI";var = (var[var].chr == "STRING") ? ((var + 1)) : (var);FI"end;FI"var = -1;FI"var = var;FI"var = 0;FI"if (var == 1) then;FI"@var = @var;FI"!var = ((@var - @var) - @var);Fi[[	@<i◊I"          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@?iﬂI"Û          end
          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[	@Bi∆I"          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end;Fi[@ÈI"var.print("STRING");FI"when "STRING" then;FI"var.print("STRING");FI"when "STRING" then;FI"var.print("STRING");FI"when "STRING" then;FI"var.print("STRING");FI"when "STRING" then;FI"var.print("STRING");Fi[[	I";data//homebrew_proj/vendor/multi_json/vendor/okjson.rb;TiÖI"æ      var = var.class.method_defined?(:encoding)
      while (var < var.length) do
        case var[var]
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@ØiáI"§        case var[var]
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING");Fi[	@ØiâI"ö          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        when "STRING" then
          var.print("STRING")
        else
          (var = var[var];Fi[@bI"if (var == 0) then;FI"@var = var;F@kI"	else;F@mI"end;FI"end;FI"end;FI" ;Fi[[	@<iöI"P    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then;Fi[	@?iïI"P    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then;Fi[	@BiâI"P    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then;Fi[@bI"<Cell(var, var, var[var, (var - var)], var, 2, var, var);FI";var = (var[var].chr == "STRING") ? ((var + 1)) : (var);FI"end;FI"var = -1;FI"var = var;FI"var = 0;FI"var = (var + 1);FI"$var = var if var and (var == 2);FI"	else;Fi[[	@<iâI"ı        if ((var == -1) or (var == var)) then
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)))
      end;Fi[	@?iÉI"Ò        if ((var == -1) or (var == var)) then
          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + var)
        var = var if (var >= 128);Fi[	@Bi}I"ı        if ((var == -1) or (var == var)) then
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)))
      end;Fi[I"var = case var;F@øI""STRING";F@tI""STRING";F@vI""STRING";FI"	else;FI"# do nothing;FI"end;Fi[[	@ui0I"      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var);Fi[	@~i.I"      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var);Fi[	@i,I"      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var);Fi[I"if @var.compressor then;FI"9@var.compressor.compress_with do |arglist, var, var|;FI"(var << var);FI"(var << var);FI"end;FI"end;FI"(var << "STRING");FI"var.run;FI"if var.success? then;FI"Logger.message("STRING");Fi[[	I"(data//backup_proj/backup/archive.rb;TiII"≠      var = "STRING"
      var = Pipeline.new
      (var << ("STRING" + "STRING"))
      if @var.compressor then
        @var.compressor.compress_with do |arglist, var, var|
          (var << var)
          (var << var)
        end
      end
      (var << "STRING")
      var.run
      if var.success? then
        Logger.message("STRING")
      else
        raise(Errors::Archive::PipelineError, ("STRING" + var.error_messages));Fi[	I"/data//backup_proj/backup/database/mysql.rb;Ti;I"¿        var = Pipeline.new
        var = "STRING"
        (var << mysqldump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist, var, var|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages));Fi[	I"4data//backup_proj/backup/database/postgresql.rb;Ti;I"Ω        var = Pipeline.new
        var = "STRING"
        (var << pgdump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist, var, var|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages));Fi[@ÊI"(var << var);FI"(var << var);FI"end;FI"end;FI"(var << "STRING");FI"var.run;F@Ì@ÓI"	else;Fi[[	@ÒiJI"¢      var = Pipeline.new
      (var << ("STRING" + "STRING"))
      if @var.compressor then
        @var.compressor.compress_with do |arglist, var, var|
          (var << var)
          (var << var)
        end
      end
      (var << "STRING")
      var.run
      if var.success? then
        Logger.message("STRING")
      else
        raise(Errors::Archive::PipelineError, ("STRING" + var.error_messages))
      end;Fi[	@Ùi<I"±        var = "STRING"
        (var << mysqldump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist, var, var|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages))
        end;Fi[	@˜i<I"Æ        var = "STRING"
        (var << pgdump)
        if @var.compressor then
          @var.compressor.compress_with do |arglist, var, var|
            (var << var)
            (var << var)
          end
        end
        (var << "STRING")
        var.run
        if var.success? then
          Logger.message("STRING")
        else
          raise(Errors::Database::PipelineError, ("STRING" + var.error_messages))
        end;Fi[@bI"var = var["STRING"];FI"out(("STRING" + var));FI"(if (var["STRING"] == "STRING") then;FI"out("STRING");FI";out("STRING") if var.!=("STRING") and var.!=("STRING");FI"	else;F@cI"out("STRING");FI"out("STRING");Fi[[	@<iI"Ï      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"));Fi[	@?iI"Ï      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"));Fi[	@Bi˚I"Ï      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"));Fi[@bI"var = "STRING";F@J@KI"@var = var.to_s.index("STRING") ? ((var + "STRING")) : (var);FI"end;FI"end;FI"var = -1;FI"var = 0;FI"var = 0;Fi[[	@<igI"y        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[	@?i\I"y        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[	@BiVI"y        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1;Fi[I")def function(arglist, var, var, var);FI"%var = Node.new(:COMBINATOR, var);FI"var;FI"end;FI" ;FI")def function(arglist, var, var, var);FI"%var = Node.new(:COMBINATOR, var);FI"var;FI"end;FI" ;Fi[[	I"/data//nokogiri_proj/nokogiri/css/parser.rb;Ti(I"p      
      # reduce 49 omitted
      # reduce 50 omitted
      def function(arglist, var, var, var)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:COMBINATOR, var);Fi[	@4i-I"R        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:COMBINATOR, var);Fi[	@4i2I"2        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:COMBINATOR, var)
        var
      end
      
      # reduce 55 omitted
      # reduce 56 omitted;Fi[@(I"Avar = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);FI"var;FI"end;FI" ;FI")def function(arglist, var, var, var);FI"Avar = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);FI"var;FI"end;FI" ;Fi[[	@4iƒI"¶        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);Fi[	@4i…I"¶        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten);Fi[	@4iŒI"Ö        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist, var, var, var)
        var = Node.new(:FUNCTION, [var.first.strip, var[1]].flatten)
        var
      end
      
      def function(arglist, var, var, var)
        var = [var.first, var.last];Fi[@o@p@q@rI"#;F@ΩI"var = case var;F@øI""STRING";F@ti[[	@ui*I"      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then;Fi[	@~i(I"      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then;Fi[	@i&I"      # : backup log, if `on_warning` was set to `true`
      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then;Fi[@p@q@rI"#;F@ΩI"var = case var;F@øI""STRING";F@tI""STRING";Fi[[	@ui+I"Í      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING";Fi[	@~i)I"Í      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING";Fi[	@i'I"Í      #
      # `:failure`
      # : The backup operation failed.
      # : Notification will be sent, including the Exception which caused
      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING";Fi[@øI""STRING";F@tI""STRING";F@vI""STRING";FI"	else;FI"# do nothing;FI"end;F@{i[[	@ui1I"<      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end;Fi[	@~i/I"<      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end;Fi[	@i-I"<      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end;Fi[@tI""STRING";F@vI""STRING";FI"	else;FI"# do nothing;FI"end;F@{@|I"end;Fi[[	@ui3I"#        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      ##;Fi[	@~i1I";        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      def function(arglist, var);Fi[	@i/I";        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var)
        send_message(var)
      end
      
      def function(arglist, var);Fi[I"@var ||= "STRING";FI"try("STRING", var);FI"	self;FI"end;FI" ;FI"$def function(arglist, var, var);FI"@var ||= "STRING";FI"try("STRING", var);FI"	self;FI"end;Fi[[	I"Idata//capistrano_proj/capistrano/recipes/deploy/remote_dependency.rb;TiI"(      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var);Fi[	@ãiI"(      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var);Fi[	@ãi I"-      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var, var);Fi[I"if (var == 0) then;FI"@var = var;F@kI"	else;F@mI"end;FI"end;FI"end;FI" ;F@ri[[	@<iõI"6    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var);Fi[	@?iñI"8    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then
      SJISWrite(var, var, var, var);Fi[	@BiäI"6    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var)
    if (@var["STRING"] == "STRING") then
      MBWrite(var, var, var, var);Fi[I"var = (var - var);FI"-var = (var == 0) ? (10) : ((var / 20.0));FI"var = ((var + var) - var);FI",var = (scale_divisions or (var / 10.0));FI":var = (var < 1) ? (1) : (var.round) if scale_integers;FI"var = [];FI"5var = ((var % var) == 0) ? (var) : ((var + var));FI"7var.step(var, var) { |arglist, var| (var << var) };FI"return var;FI"end;Fi[[	I"(data//redmine_proj/SVG/Graph/Bar.rb;TiI"      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      
      def function(arglist, var);Fi[	I"2data//redmine_proj/SVG/Graph/BarHorizontal.rb;TiI"Î      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      
      def function(arglist);Fi[	I")data//redmine_proj/SVG/Graph/Line.rb;TiTI"ˇ      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      
      def function(arglist, var, var, var, var);Fi[I"if var["STRING"] then;F@i@jI"	else;FI"out("STRING");FI"end;FI"end;FI"end;FI"out("STRING");FI"out("STRING");Fi[[	@<iI"           out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj;Fi[	@?i!I"           out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj;Fi[	@BiI"           out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj;Fi[@I"#if (action_hook == :save) then;FI"Gowner_class.set_callback(:save, :around, self, :prepend => (true));FI"	else;FI"
super;FI"end;FI"end;FI" ;FI":# Runs state events around the machine's :save action;FI"def function(arglist, var);Fi[[	I"Idata//state_machine_proj/state_machine/integrations/active_record.rb;Ti;I"·      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end;Fi[	I"Hdata//state_machine_proj/state_machine/integrations/mongo_mapper.rb;Ti4I"·      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end;Fi[	I"Cdata//state_machine_proj/state_machine/integrations/mongoid.rb;Ti8I"·      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end;Fi[I"var = var.length;F@¬I"var = 0;FI"if var then;F@–I"var = "STRING";FI"var = "STRING";FI"var = "STRING";FI"	else;FI"var = "STRING";Fi[[	@<i_I"˜    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?;Fi[	@?iTI"˜    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?;Fi[	@BiNI"˜    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?;Fi[I"if color_stop?(var) then;FI"var.unshift(var);FI"var = nil;FI"	else;FI"&if list_of_color_stops?(var) then;FI"var = (var.value + var);FI"var = nil;FI"end;FI"end;FI"'var = nil if var and var.to_bool.!;Fi[[	I"Mdata//compass_proj/compass/sass_extensions/functions/gradient_support.rb;TiÛI"a    end
    
    def function(arglist, var, var, var)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      if color_stop?(var) then
        var.unshift(var);Fi[	@ˆi˝I"í        end
      end
      var = nil if var and var.to_bool.!
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      if (var.size == 1) and list_of_color_stops?(var.first) then
        var = var.first.value;Fi[	@ˆiI"©    end
    
    def function(arglist, var, var)
      if color_stop?(var) then
        var.unshift(var)
        var = nil
      else
        if list_of_color_stops?(var) then
          var = (var.value + var)
          var = nil
        end
      end
      var = nil if var and var.to_bool.!
      var = var if (var.size == 1) and var = list_of_color_stops?(var.first)
      LinearGradient.new(var, send(:color_stops, *var));Fi[I"*@var.each_pair do |arglist, var, var|;FI"newobj;F@‡I"out("STRING");FI"(if (var["STRING"] == "STRING") then;FI"putType0(var);FI"	else;FI"var = var["STRING"];F@
@i[[	@<iI"£      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING");Fi[	@?iI"£      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING");Fi[	@BiıI"£      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING");Fi[I"@var = @var;FI"	else;F@∂I"@var = var;F@kI"	else;F@mI"end;FI"end;FI"end;Fi[[	@<iôI"m    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var);Fi[	@?iîI"m    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var);Fi[	@BiàI"m    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  
  def function(arglist, var, var, var, var);Fi[I"var = var["STRING"];FI"var = "STRING";FI"L32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) };FI"out((var + "STRING"));FI"out("STRING");FI"newobj;FI"var = ("STRING" + var);FI"3var["STRING"].each_pair do |arglist, var, var|;FI"8var = (var + ((("STRING" + var) + "STRING") + var));FI"end;Fi[[	@<i$I"ˇ        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then;Fi[	@?i-I"ˇ        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then;Fi[	@BiI"ˇ        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then;Fi[@@
@I"out("STRING");F@I"	else;F@cI"out("STRING");FI"out("STRING");F@fi[[	@<iI"¯      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then;Fi[	@?iI"¯      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then;Fi[	@Bi¸I"¯      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then;Fi[@I"if var then;FI"Ñvar = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"));FI"end;FI"out((var + "STRING"));FI"out("STRING");FI"end;FI"end;FI"end;FI"end;Fi[[	@<i.I"’          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist, var);Fi[	@?i7I"’          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist, var);Fi[	@BiI"’          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end
    end
  end
  
  def function(arglist, var);Fi[I"'var = var.gsub("STRING", "STRING");FI"var = var.length;F@¬I"var = 0;FI"if var then;F@–I"var = "STRING";FI"var = "STRING";FI"var = "STRING";FI"	else;Fi[[	@<i^I"Ã    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?;Fi[	@?iSI"Ã    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?;Fi[	@BiMI"Ã    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?;Fi[I",assert_equal(var.head, var.active_spec);FI"0assert_version_equal("STRING", var.version);F@]@^@_@`I"3assert_instance_of(HeadSoftwareSpec, var.head);FI"end;FI" ;FI"def function(arglist);Fi[[	@ciµI"    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head);Fi[	@ciƒI"    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head);Fi[	@ci”I"    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = ExplicitStrategySpecTestBall.new
    assert_not_nil(var.stable);Fi[I"if var then;F@–I"var = "STRING";FI"var = "STRING";FI"var = "STRING";FI"	else;FI"var = "STRING";F@J@K@i[[	@<ibI"»    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end;Fi[	@?iWI"»    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end;Fi[	@BiQI"»    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end;Fi[@X@Y@Z@[@\@]@^@_@`@\i[[	@ci≤I"˛  def function(arglist)
    var = HeadOnlySpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  ;Fi[	@ci¡I"  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  ;Fi[	@ci–I"  def function(arglist)
    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  ;Fi[@Y@Z@[@\@]@^@_@`@\I"end;Fi[[	@ci≥I"˛    var = HeadOnlySpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist);Fi[	@ci¬I"    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist);Fi[	@ci—I"    var = IncompleteStableSpecTestBall.new
    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist);Fi[@Z@[@\@]@^@_@`@\I"end;FI" ;Fi[[	@ci¥I"    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new;Fi[	@ci√I"    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = IncompleteStableSpecTestBall.new;Fi[	@ci“I"    assert_not_nil(var.head)
    assert_nil(var.stable)
    assert_nil(var.bottle)
    assert_nil(var.devel)
    assert_equal(var.head, var.active_spec)
    assert_version_equal("STRING", var.version)
    assert_nil(var.head.checksum)
    assert_equal("STRING", var.url)
    assert_equal(GitDownloadStrategy, var.download_strategy)
    assert_instance_of(GitDownloadStrategy, var.downloader)
    assert_instance_of(HeadSoftwareSpec, var.head)
  end
  
  def function(arglist)
    var = ExplicitStrategySpecTestBall.new;Fi[@I"var = max_value;FI"var = min_value;FI"var = (var - var);F@¢@£@§@•I"var = [];F@ßi[[	@≠iI"Â        @var[:fields]
      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var;Fi[	@∞iI"‹      
      protected
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var;Fi[	@≥iQI"Ú        @var = var if (var > @var)
      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var;Fi[I"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");FI"0assert_version_detected("STRING", "STRING");Fi[[	I".data//homebrew_proj/test/test_versions.rb;Ti˝I"“  #   assert_version_detected '7.0.4', 'http://www.haskell.org/ghc/dist/7.0.4/ghc-7.0.4-i386-apple-darwin.tar.bz2'
  # end
  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING");Fi[	@©i˛I"e  # end
  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end;Fi[	@©iˇI"a  def function(arglist)
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
    assert_version_detected("STRING", "STRING")
  end
end;Fi[I"$(var.nil? or (var == "STRING"));FI"end.to_yaml;FI"4File.open("STRING", "STRING") do |arglist, var|;FI"var.puts(var);FI"var.puts("STRING");FI"var.puts(var);FI"end;FI"end;FI"end;FI"end;Fi[[	I"1data//jekyll_proj/jekyll/migrators/joomla.rb;TiI"Î        var = var[:content]
        var = ("STRING" % [var.year, var.month, var.day, var])
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:id]), "STRING" => (var[:alias]), "STRING" => (var) }.delete_if do |arglist, var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist, var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	I"3data//jekyll_proj/jekyll/migrators/mephisto.rb;Ti I"œ        var = var[:body]
        var = ([var.year, var.month, var.day, var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:entry_id]) }.delete_if do |arglist, var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist, var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	I"6data//jekyll_proj/jekyll/migrators/textpattern.rb;TiI"Ÿ        var = var[:Body]
        var = ([var.strftime("STRING"), var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:Keywords].split("STRING")) }.delete_if do |arglist, var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist, var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[I"end.to_yaml;F@≤I"var.puts(var);F@¥I"var.puts(var);FI"end;FI"end;FI"end;FI"end;FI"end;Fi[[	@ºiI"œ        var = ("STRING" % [var.year, var.month, var.day, var])
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:id]), "STRING" => (var[:alias]), "STRING" => (var) }.delete_if do |arglist, var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist, var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	@øi!I"∂        var = ([var.year, var.month, var.day, var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:entry_id]) }.delete_if do |arglist, var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist, var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[	@¬iI"¿        var = ([var.strftime("STRING"), var].join("STRING") + "STRING")
        var = { "STRING" => "STRING", "STRING" => (var.to_s), "STRING" => (var[:Keywords].split("STRING")) }.delete_if do |arglist, var, var|
          (var.nil? or (var == "STRING"))
        end.to_yaml
        File.open("STRING", "STRING") do |arglist, var|
          var.puts(var)
          var.puts("STRING")
          var.puts(var)
        end
      end
    end
  end
end;Fi[I"!if var["STRING"].nil?.! then;F@jI"	else;F@∏I"end;FI"end;FI"end;FI"out("STRING");FI"out("STRING");FI"'if var["STRING"].!=("STRING") then;Fi[[	@<iI"–          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"];Fi[	@?i"I"–          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"];Fi[	@BiI"–          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"];Fi[I"# @since 2.1.0;FI"def function(arglist);FI"
begin;FI"((Threaded.begin_execution("STRING");FI"yield);FI"ensure;FI"&Threaded.exit_execution("STRING");FI"end;FI"end;FI" ;Fi[[	I"9data//mongoid_proj/lib/mongoid/threaded/lifecycle.rb;Ti6I"V      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in binding mode?
      #;Fi[	@ÒiUI"W      #
      # @return [ Object ] The return value of the block.
      #
      # @since 2.1.0
      def function(arglist)
        begin
          (Threaded.begin_execution("STRING")
          yield)
        ensure
          Threaded.exit_execution("STRING")
        end
      end
      
      # Is the current thread in building mode?
      #;Fi[	@ÒiÆI"}        #
        # @return [ Object ] The return value of the block.
        #
        # @since 2.1.0
        def function(arglist)
          begin
            (Threaded.begin_execution("STRING")
            yield)
          ensure
            Threaded.exit_execution("STRING")
          end
        end
        
        # Is the current thread in loading revision mode?
        #;Fi[I"-# @return [ Strategy ] The new strategy.;FI"$def function(arglist, var, var);FI"@var, @var = var, var;FI"@var = var.send(var.name);FI"end;FI" ;FI"L# Execute the cascading deletion for the relation if it already exists.;FI"I# This should be optimized in the future potentially not to load all;FI"# objects from the db.;FI"#;Fi[[	I"Adata//mongoid_proj/lib/mongoid/relations/cascading/delete.rb;TiI"T        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist, var, var)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading delete.
        #   strategy.cascade;Fi[	I"Bdata//mongoid_proj/lib/mongoid/relations/cascading/destroy.rb;TiI"U        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist, var, var)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading destroy.
        #   strategy.cascade;Fi[	I"Cdata//mongoid_proj/lib/mongoid/relations/cascading/restrict.rb;TiI"T        # @param [ Document ] document The document to cascade from.
        # @param [ Metadata ] metadata The relation's metadata.
        #
        # @return [ Strategy ] The new strategy.
        def function(arglist, var, var)
          @var, @var = var, var
          @var = var.send(var.name)
        end
        
        # Execute the cascading deletion for the relation if it already exists.
        # This should be optimized in the future potentially not to load all
        # objects from the db.
        #
        # @example Perform the cascading delete.
        #   strategy.cascade;Fi[@I"'var = (HOMEBREW_PREFIX / "STRING");FI"var.write("STRING");FI"assert(var.universal?.!);FI"assert(var.i386?.!);FI"assert(var.x86_64?.!);FI"assert(var.ppc7400?.!);FI"assert(var.ppc64?.!);FI"assert(var.dylib?.!);FI"%assert(var.mach_o_executable?.!);Fi[[	I"*data//homebrew_proj/test/test_mach.rb;TiäI"â    (HOMEBREW_PREFIX / "STRING").unlink
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs);Fi[	@iöI"ê    assert_match(/text executable/, file(var))
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?)
    assert_equal([], var.archs);Fi[	@i™I"í    assert_match(/text executable/, file(var))
  end
  
  def function(arglist)
    var = (HOMEBREW_PREFIX / "STRING")
    var.write("STRING")
    assert(var.universal?.!)
    assert(var.i386?.!)
    assert(var.x86_64?.!)
    assert(var.ppc7400?.!)
    assert(var.ppc64?.!)
    assert(var.dylib?.!)
    assert(var.mach_o_executable?.!)
    assert(var.text_executable?.!)
    assert_equal([], var.archs);Fi[@0@€I"out("STRING");FI"end;F@ﬁI"newobj;F@‡@·@YI"Error("STRING") if var.!;Fi[[	@<iÚI"©  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING");Fi[	@?i˚I"©  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING");Fi[	@Bi·I"©  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var)
      Error("STRING") if var.!
      out(("STRING" + var))
      out("STRING") if (var[-2] == "STRING");Fi[@0@‡I"out("STRING");F@ I"putType0(var);FI"	else;F@@
@I"out("STRING");Fi[[	@<iI"û      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else;Fi[	@?iI"û      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else;Fi[	@BiˆI"û      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else;Fi[@0I"var = var["STRING"];FI"var = "STRING";F@@I"out("STRING");FI"newobj;F@!@"@#i[[	@<i#I"ˇ        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"];Fi[	@?i,I"ˇ        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"];Fi[	@BiI"ˇ        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"];Fi[@0@!@"@#I"end;FI"var = var["STRING"];FI"if var then;F@:I"end;F@<i[[	@<i)I"Y          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end;Fi[	@?i2I"Y          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end;Fi[	@BiI"Y          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end;Fi[@I"var = @var;T@ŸI"newobj;F@€I"out("STRING");FI"end;F@ﬁI"newobj;F@‡i[[	@<iÔI"Q  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var);Fi[	@?i¯I"Q  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var);Fi[	@BiﬁI"Q  
  private
  
  def function(arglist)
    var = @var
    @var.each do |arglist, var|
      newobj
      out((("STRING" + var) + "STRING"))
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      var = (FPDF_FONTPATH + var) if defined("STRING")
      var = filesize(var);Fi[@[@æ@ø@¿I"var = var.length;F@¬I"var = 0;FI"if var then;F@–I"var = "STRING";Fi[[	@<i[I"«  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING";Fi[	@?iPI"«  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING";Fi[	@BiJI"«  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING";Fi[@o@@I"out("STRING");FI"newobj;F@!@"@#I"end;F@Di[[	@<i%I"z        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"));Fi[	@?i.I"z        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"));Fi[	@BiI"z        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"));Fi[I"=def function(arglist, var, var, var, var, var, var, var);FI"var = @var;FI"var = @var;F@Ω@æ@ø@¿I"var = var.length;F@¬I"var = 0;Fi[[	@<iXI"ì    end
  end
  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then;Fi[	@?iMI"ì    end
  end
  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then;Fi[	@BiGI"ì    end
  end
  
  def function(arglist, var, var, var, var, var, var, var)
    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then;Fi[@æ@ø@¿I"var = var.length;F@¬I"var = 0;FI"if var then;F@–I"var = "STRING";FI"var = "STRING";Fi[[	@<i\I"ó    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else;Fi[	@?iQI"ó    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else;Fi[	@BiKI"ó    var = @var
    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else;Fi[@ø@¿I"var = var.length;F@¬I"var = 0;FI"if var then;F@–I"var = "STRING";FI"var = "STRING";FI"var = "STRING";Fi[[	@<i]I"ü    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING";Fi[	@?iRI"ü    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING";Fi[	@BiLI"ü    var = @var
    var = @var["STRING"]
    var = ((@var - @var) - @var) if (var == 0)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING";Fi[@ø@¿@çI"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;FI"var = 1;F@êI"?var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[[	@<iØI"£  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then;Fi[	@?i™I"ë  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var
      if (var == 10) then;Fi[	@BiûI"£  def function(arglist, var, var, var, var)
    var = @var["STRING"]
    var = ((@var - @var) - @var)
    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then;Fi[@¬I"var = 0;FI"if var then;F@–I"var = "STRING";FI"var = "STRING";FI"var = "STRING";FI"	else;FI"var = "STRING";F@Ji[[	@<i`I"    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var);Fi[	@?iUI"    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var);Fi[	@BiOI"    var = (((var - (2 * @var)) * 1000) / @var)
    var = var.gsub("STRING", "STRING")
    var = var.length
    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var);Fi[@–I"var = "STRING";FI"var = "STRING";FI"var = "STRING";FI"	else;FI"var = "STRING";F@J@K@I"end;Fi[[	@<icI"¿    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1;Fi[	@?iXI"¿    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1;Fi[	@BiRI"¿    var = (var - 1) if (var > 0) and (var[(var - 1)] == "STRING")
    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1;Fi[@–I"@var = @var;FI"	else;F@∂I"@var = var;F@kI"	else;F@mI"end;FI"end;Fi[[	@<iòI"I    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  ;Fi[	@?iìI"I    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  ;Fi[	@BiáI"I    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end
  ;Fi[@J@K@I"end;FI"end;FI"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;FI"var = 1;Fi[[	@<iiI"•        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@?i^I"•        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[	@BiXI"•        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var]);Fi[@o@J@K@I"end;FI"end;FI"var = -1;FI"var = 0;FI"var = 0;FI"var = 0;Fi[[	@<ihI"{        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@?i]I"{        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[	@BiWI"{        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0
    var = 1
    while (var < var) do;Fi[@oI"var = "STRING";FI"	else;FI"var = "STRING";F@J@K@I"end;FI"end;FI"var = -1;Fi[[	@<ieI"ä    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0;Fi[	@?iZI"ä    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0;Fi[	@BiTI"ä    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0;Fi[I"<Cell(var, var, var[var, (var - var)], var, 2, var, var);FI"	else;F@≈@∆I"end;FI"var = -1;FI"var = var;FI"var = 0;FI"var = (var + 1);F@Ãi[[	@<iàI"      if (var > var) then
        if ((var == -1) or (var == var)) then
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)));Fi[	@?iÇI"È      if (var > var) then
        if ((var == -1) or (var == var)) then
          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + var);Fi[	@Bi|I"      if (var > var) then
        if ((var == -1) or (var == var)) then
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
        else
          Cell(var, var, var[var, (var - var)], var, 2, var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        var = (var + 1)
        var = var if var and (var == 2)
      else
        var = (var + (var ? (1) : (2)));Fi[@ˇI"if (var == 1) then;FI"@var = @var;FI"	else;F@∂I"@var = var;F@kI"	else;F@mI"end;Fi[[	@<ióI"P      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end;Fi[	@?iíI"P      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end;Fi[	@BiÜI"P      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end
  end;Fi[I"Fvar = (var + "STRING") if var and var.to_s.index("STRING").nil?.!;FI"<Cell(var, var, var[var, (var - var)], var, 2, var, var);FI"if (var == 1) then;FI"@var = @var;FI"	else;F@∂I"@var = var;F@kI"	else;F@mi[[	@<iñI"r        var = (var + (var ? (1) : (2)))
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end;Fi[	@?iëI"l        var = var if (var >= 128)
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end;Fi[	@BiÖI"r        var = (var + (var ? (1) : (2)))
      end
    end
    var = (var + "STRING") if var and var.to_s.index("STRING").nil?.!
    Cell(var, var, var[var, (var - var)], var, 2, var, var)
    if (var == 1) then
      @var = @var
    else
      if (var == 0) then
        @var = var
        @var = (var + var)
      else
        @var = var if (var == 2)
      end
    end;Fi[@o@ÒI"var = "STRING";FI"	else;FI"var = "STRING";F@J@K@I"end;FI"end;Fi[[	@<idI"ä    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0;Fi[	@?iYI"ä    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0;Fi[	@BiSI"ä    var = 0
    if var then
      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0;Fi[I"DCell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var);FI"var = (var + 1);FI"var = -1;FI"var = var;FI"var = 0;F@JI"@var = @var;F@L@MI"end;Fi[[	@<iªI"Õ      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next;Fi[	@?i∂I"ª      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = var
      if (var == 10) then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next;Fi[	@Bi™I"Õ      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      var = (var < 128)
      if (var.chr == "STRING") then
        Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        var = (var + 1)
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var)
        end
        var = (var + 1)
        next;Fi[@8I"	else;F@ì@îI"end;FI"var = -1;FI"var = var;FI"var = 0;F@ôI"@var = @var;Fi[[	@<i÷I"            next
          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@?iﬁI"¯            next
          end
          var = (var + var) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[	@Bi≈I"            next
          end
          var = (var + (var ? (1) : (2))) if (var == var)
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
        else
          Cell(var, var, var[var, (var - var)], 0, 2, "STRING", var, var)
          var = (var[var].chr == "STRING") ? ((var + 1)) : (var)
        end
        var = -1
        var = var
        var = 0
        if (var == 1) then
          @var = @var
          var = ((@var - @var) - @var)
          var = (((var - (2 * @var)) * 1000) / @var);Fi[@‡I"out("STRING");F@ I"putType0(var);FI"	else;F@@
@I"out("STRING");F@i[[	@<iI"¥    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]));Fi[	@?iI"¥    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]));Fi[	@Bi˜I"¥    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]));Fi[I"out(("STRING" + var));F@I"out("STRING");F@I"	else;F@cI"out("STRING");FI"out("STRING");F@f@gi[[	@<iI"˜        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then;Fi[	@?iI"˜        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then;Fi[	@Bi˝I"˜        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then;Fi[@À@Ã@Õ@f@g@h@i@jI"	else;F@∏i[[	@<iI"˛          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end;Fi[	@?iI"˛          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end;Fi[	@BiI"˛          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end;Fi[@oI"	else;FI"var = "STRING";F@J@K@I"end;FI"end;FI"var = -1;FI"var = 0;Fi[[	@<ifI"Ü      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0;Fi[	@?i[I"Ü      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0;Fi[	@BiUI"Ü      if (var == 1) then
        var = "STRING"
        var = "STRING"
        var = "STRING"
      else
        var = "STRING"
        var = "STRING" unless var.to_s.index("STRING").nil?
        var = (var + "STRING") unless var.to_s.index("STRING").nil?
        var = var.to_s.index("STRING") ? ((var + "STRING")) : (var)
      end
    end
    var = -1
    var = 0
    var = 0
    var = 0;Fi[@À@˛I"out("STRING");FI"out("STRING");FI"newobj;FI"out("STRING");FI"out("STRING");F@I"iout((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));FI"3out((("STRING" + (@var + 1).to_s) + "STRING"));Fi[[	@?iEI"!  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    var = "STRING"
    var["STRING"].keys.sort.each do |arglist, var|;Fi[	@Bi+I"  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    if (var["STRING"] == "STRING") then
      var = "STRING";Fi[	@/iåI"    out("STRING")
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    var = "STRING"
    var["STRING"].keys.sort.each do |arglist, var|;Fi[@#I"var = fopen(var, "STRING");FI"putstream(fread(var, var));FI"fclose(var);FI"out("STRING");FI"end;FI"*@var.each_pair do |arglist, var, var|;FI"newobj;FI"@var[var]["STRING"] = @var;FI"out("STRING");Fi[[	@<i I"œ      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var);Fi[	@?i	I"œ      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var);Fi[	@BiÔI"Õ      out("STRING") if (var[-2] == "STRING")
      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) if var["STRING"].nil?.!
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var);Fi[@#@ I"putType0(var);FI"	else;F@@
@I"out("STRING");F@I"	else;Fi[[	@<i	I"ƒ    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING");Fi[	@?iI"ƒ    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING");Fi[	@Bi¯I"ƒ    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING");Fi[@#@I"	else;F@cI"out("STRING");FI"out("STRING");F@f@g@h@ii[[	@<iI"*        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else;Fi[	@?iI"*        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else;Fi[	@BiˇI"*        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else;Fi[@#@$@f@g@h@i@jI"	else;F@∏I"end;Fi[[	@<iI"Ú          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end;Fi[	@?iI"Ú          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end;Fi[	@BiI"Ú          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end;Fi[@#@$@‹I"newobj;F@7@8@@I"out("STRING");FI"newobj;Fi[[	@<i I"Ω            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|;Fi[	@?i)I"Ω            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|;Fi[	@BiI"Ω            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|;Fi[@oI"
begin;F@}@~@@ÄI"
retry;FI"rescue;F@ÉI"end;Fi[[	@àiI"„          var = var.iconv(var)) rescue var = var.gsub(/[^\r\n\t\x20-\x7e]/, "STRING")
        else
          var = Iconv.new("STRING", "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end;Fi[	@ài=I"ﬁ          var = var.iconv(var)) rescue var = var.gsub(/[^\r\n\t\x20-\x7e]/, "STRING")
        else
          var = Iconv.new("STRING", var)
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end;Fi[	@ài{I"ﬁ          var = var.iconv(var)) rescue var = var.gsub(/[^\r\n\t\x20-\x7e]/, "STRING")
        else
          var = Iconv.new(var, "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end;Fi[@ΩI"@var ||= "STRING";F@ÄI"	self;FI"end;FI" ;F@Ñ@Ö@ÜI"	self;Fi[[	@ãiI"        @var = nil
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[	@ãiI"        self
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[	@ãiI"        self
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      
      def function(arglist, var, var)
        @var ||= "STRING"
        try("STRING", var)
        self
      end
      ;Fi[@#@$@%@&@'@(@)@*I"end;FI"end;Fi[[	@<iYI"·    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@?iZI"·    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[	@BiDI"·    out("STRING")
    out(("STRING" + var["STRING"]))
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
    out("STRING")
  end
end;Fi[@#@f@g@h@i@jI"	else;F@∏I"end;FI"end;Fi[[	@<iI"«        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING");Fi[	@?iI"«        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING");Fi[	@BiI"«        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING");Fi[@#@‹I"newobj;F@7@8@@I"out("STRING");FI"newobj;F@!i[[	@<i!I"Ì          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var));Fi[	@?i*I"Ì          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var));Fi[	@BiI"Ì          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var));Fi[@#@Í@!@"@#I"end;F@DI"if var then;F@:I"end;Fi[[	@<i(I"f          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING");Fi[	@?i1I"f          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING");Fi[	@BiI"f          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING");Fi[@—I"out("STRING");F@¸@˝@˛I"out("STRING");FI"out("STRING");FI"newobj;FI"out("STRING");FI"out("STRING");Fi[[	@<i9I"Î    end
  end
  
  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((((("STRING" + textstring("STRING")) + "STRING") + textstring(var["STRING"]["STRING"])) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));Fi[	@?iBI"π    end
  end
  
  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));Fi[	@Bi(I"π    end
  end
  
  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"));Fi[@—I"*return image_url if (var == "STRING");FI"4if (var.nil? or var.match(/^https?:\/\//)) then;FI"super(var);FI"	else;FI"%super(absolutify_local_url(var));FI"end;FI"end;FI" ;FI"def function(arglist, var);Fi[[	I"*data//diaspora_proj/models/profile.rb;TinI"X    end
  end
  
  def function(arglist, var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist, var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then;Fi[	@.iwI"X    end
  end
  
  def function(arglist, var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist, var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then;Fi[	@.i{I"|    end
  end
  
  def function(arglist, var)
    return image_url if (var == "STRING")
    if (var.nil? or var.match(/^https?:\/\//)) then
      super(var)
    else
      super(absolutify_local_url(var))
    end
  end
  
  def function(arglist, var)
    if ["STRING", "STRING"].all? { |arglist, var| var[var].present? } then
      var["STRING"] = "STRING" if var["STRING"].blank?;Fi[@—I"var = 0;F@[I"var = var.length;FI"var = 0;F@6@7@8@9I"var = (var + 1);Fi[[	@<i>I"X    end
  end
  
  def function(arglist, var)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000);Fi[	@?i.I"k    end
  end
  
  def function(arglist, var)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        if (var >= 161) and (var <= 223) then;Fi[	@Bi-I"X    end
  end
  
  def function(arglist, var)
    var = 0
    var = @var["STRING"]
    var = var.length
    var = 0
    while (var < var) do
      var = var[var].is_a?(String) ? (var[var].ord) : (var[var])
      if (var < 128) then
        var = (var + var[var.chr]) if var[var.chr]
        var = (var + 1)
      else
        var = (var + 1000);Fi[@î@ïI"fclose(var);FI"out("STRING");FI"end;F@ôI"newobj;F@õI"out("STRING");F@ i[[	@<iI"≠      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else;Fi[	@?i
I"≠      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else;Fi[	@BiI"´      out(("STRING" + var["STRING"]))
      out((("STRING" + var["STRING"]) + "STRING")) if var["STRING"].nil?.!
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else;Fi[@ïI"fclose(var);FI"out("STRING");FI"end;F@ôI"newobj;F@õI"out("STRING");F@ I"putType0(var);Fi[[	@<iI"£      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"];Fi[	@?iI"£      out((("STRING" + var["STRING"]) + "STRING")) unless var["STRING"].nil?
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"];Fi[	@BiÒI"°      out((("STRING" + var["STRING"]) + "STRING")) if var["STRING"].nil?.!
      out("STRING")
      var = fopen(var, "STRING")
      putstream(fread(var, var))
      fclose(var)
      out("STRING")
    end
    @var.each_pair do |arglist, var, var|
      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"];Fi[@ I"putType0(var);FI"	else;F@@
@I"out("STRING");F@I"	else;F@ci[[	@<i
I"≤      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING");Fi[	@?iI"≤      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING");Fi[	@Bi˘I"≤      newobj
      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING");Fi[@ I"out("STRING");F@I"	else;F@cI"out("STRING");FI"out("STRING");F@f@g@hi[[	@<iI"$      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"));Fi[	@?iI"$      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"));Fi[	@Bi˛I"$      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"));Fi[I"putType0(var);FI"	else;F@@
@I"out("STRING");F@I"	else;F@cI"out("STRING");Fi[[	@<iI"Ÿ      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"));Fi[	@?iI"Ÿ      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"));Fi[	@Bi˙I"Ÿ      @var[var]["STRING"] = @var
      out("STRING")
      if (var["STRING"] == "STRING") then
        putType0(var)
      else
        var = var["STRING"]
        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"));Fi[@I"	else;F@cI"out("STRING");FI"out("STRING");F@f@g@h@i@ji[[	@<iI"*        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING");Fi[	@?iI"*        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING");Fi[	@Bi I"*        out(("STRING" + var))
        if (var["STRING"] == "STRING") then
          out("STRING")
          out("STRING") if var.!=("STRING") and var.!=("STRING")
        else
          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING");Fi[@f@g@h@i@jI"	else;F@∏I"end;FI"end;FI"end;Fi[[	@<iI"–          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING");Fi[	@?iI"–          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING");Fi[	@BiI"–          out(("STRING" + var["STRING"]))
          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING");Fi[@g@h@i@jI"	else;F@∏I"end;FI"end;FI"end;FI"out("STRING");Fi[[	@<iI"—          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then;Fi[	@?i I"—          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then;Fi[	@BiI"—          out("STRING")
          out("STRING")
          out((("STRING" + (@var + 1)) + "STRING"))
          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then;Fi[@jI"	else;F@∏I"end;FI"end;FI"end;FI"out("STRING");FI"out("STRING");F@‹I"newobj;Fi[[	@<iI"µ          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING";Fi[	@?i#I"µ          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING";Fi[	@Bi	I"µ          out((("STRING" + (@var + 2)) + "STRING"))
          if var["STRING"] then
            if var["STRING"].nil?.! then
              out((("STRING" + (var + var["STRING"])) + "STRING"))
            else
              out("STRING")
            end
          end
        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING";Fi[@‹I"newobj;F@7@8@@I"out("STRING");FI"newobj;F@!@"i[[	@<i"I"Ì        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end;Fi[	@?i+I"Ì        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end;Fi[	@BiI"Ì        end
        out("STRING")
        out("STRING")
        if var["STRING"].!=("STRING") then
          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end;Fi[@@I"out("STRING");FI"newobj;F@!@"@#I"end;F@DI"if var then;Fi[[	@<i&I"]          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end;Fi[	@?i/I"]          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end;Fi[	@BiI"]          newobj
          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end;Fi[@I"out("STRING");FI"newobj;F@!@"@#I"end;F@DI"if var then;F@:i[[	@<i'I"l          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"));Fi[	@?i0I"l          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"));Fi[	@BiI"l          var = var["STRING"]
          var = "STRING"
          32.upto(255) { |arglist, var| var = (var + (var[var.chr] + "STRING")) }
          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"));Fi[@!@"@#I"end;F@DI"if var then;F@:I"end;F@<I"out("STRING");Fi[[	@<i*I"          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end;Fi[	@?i3I"          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end;Fi[	@BiI"          out((var + "STRING"))
          out("STRING")
          newobj
          var = ("STRING" + var)
          var["STRING"].each_pair do |arglist, var, var|
            var = (var + ((("STRING" + var) + "STRING") + var))
          end
          var = var["STRING"]
          if var then
            var = (var + (((("STRING" + ((var["STRING"] == "STRING") ? ("STRING") : ("STRING"))) + "STRING") + @var[var]["STRING"]) + "STRING"))
          end
          out((var + "STRING"))
          out("STRING")
        end
      end;Fi[@¸@˝@˛I"out("STRING");FI"out("STRING");FI"newobj;FI"out("STRING");FI"out("STRING");F@@äi[[	@?iDI"Ò  
  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    var = "STRING";Fi[	@Bi*I"  
  def function(arglist, var)
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    if (var["STRING"] == "STRING") then;Fi[	@/iãI"Ó    newobj
    out("STRING")
    out("STRING")
    out(((("STRING" + var["STRING"]) + "STRING") + var["STRING"]))
    out(("STRING" + var["STRING"]))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    out("STRING")
    out("STRING")
    newobj
    out("STRING")
    out("STRING")
    out(("STRING" + var["STRING"]))
    out((((("STRING" + var["STRING"]["STRING"]) + "STRING") + var["STRING"]["STRING"].to_s) + "STRING"))
    out((("STRING" + (@var + 1).to_s) + "STRING"))
    var = "STRING";Fi[@—I"var = case var;F@øI""STRING";F@tI""STRING";F@vI""STRING";FI"	else;FI"# do nothing;Fi[[	@ui/I"≠      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var);Fi[	@~i-I"≠      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var);Fi[	@i+I"≠      # : the failure, the Exception's backtrace, a copy of the current
      # : backup log and other information if `on_failure` was set to `true`
      #
      def function(arglist, var)
        var = case var
        when :success then
          "STRING"
        when :warning then
          "STRING"
        when :failure then
          "STRING"
        else
          # do nothing
        end
        var = ("STRING" % var);Fi[@}@~@@ÄI"
retry;FI"rescue;F@ÉI"end;FI"var = var;FI"end;Fi[[	@àiI"î          var = Iconv.new("STRING", "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var;Fi[	@ài?I"è          var = Iconv.new("STRING", var)
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var;Fi[	@ài}I"ç          var = Iconv.new(var, "STRING")
          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
    end;Fi[@~@@ÄI"
retry;FI"rescue;F@ÉI"end;FI"var = var;FI"end;FI"end;Fi[[	@àiI"n          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var
    end;Fi[	@ài@I"n          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
      var
    end;Fi[	@ài~I"j          var = "STRING"
          begin
            var = (var + var.iconv(var))
          rescue Iconv::IllegalSequence
            var = (var + $!.success)
            var = ("STRING" + $!.failed[1, $!.failed.length])
            retry
          rescue
            var = (var + $!.success)
          end
          var = var
        end
      end
    end
  end;Fi[I"var = max_value;FI"var = min_value;F@ì@¢@£@§@•I"var = [];F@ß@®i[[	@≠iI"Ÿ      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end;Fi[	@∞iI"ﬂ      protected
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end;Fi[	@≥iRI"Ÿ      end
      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end;Fi[I"var = min_value;F@ì@¢@£@§@•I"var = [];F@ß@®I"return var;Fi[[	@≠iI"÷      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      ;Fi[	@∞iI"÷      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      ;Fi[	@≥iSI"÷      
      def function(arglist)
        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      ;Fi[@¢@£@§@•I"var = [];F@ß@®I"return var;FI"end;FI" ;Fi[[	@≠iI"Ë        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      
      def function(arglist, var)
        (var / 2.0);Fi[	@∞iI"Â        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      
      def function(arglist)
        @var[:fields];Fi[	@≥iUI"        var = max_value
        var = min_value
        var = (var - var)
        var = (var == 0) ? (10) : ((var / 20.0))
        var = ((var + var) - var)
        var = (scale_divisions or (var / 10.0))
        var = (var < 1) ? (1) : (var.round) if scale_integers
        var = []
        var = ((var % var) == 0) ? (var) : ((var + var))
        var.step(var, var) { |arglist, var| (var << var) }
        return var
      end
      
      def function(arglist, var, var, var, var)
        var = { :x => 0, :y => 0 };Fi[I"H# Uses around callbacks to run state events if using the :save hook;FI"def function(arglist);FI"#if (action_hook == :save) then;F@«I"	else;FI"
super;FI"end;FI"end;FI" ;F@Õi[[	@—i:I"        define_helper(:instance, "STRING", "STRING", (468 + 1))
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield };Fi[	@‘i3I"Â        super
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield };Fi[	@◊i7I"Â        super
      end
      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield };Fi[@=	@«I"	else;FI"
super;FI"end;FI"end;FI" ;F@Õ@ŒI"Rvar.class.state_machines.transitions(var, action).perform { |arglist| yield };Fi[[	@—i<I"ﬁ      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      ;Fi[	@‘i5I"ﬁ      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      ;Fi[	@◊i9I"ﬁ      
      # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      ;Fi[@«I"	else;FI"
super;FI"end;FI"end;FI" ;F@Õ@Œ@P	I"end;Fi[[	@—i=I"       # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or;Fi[	@‘i6I"       # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or;Fi[	@◊i:I"       # Uses around callbacks to run state events if using the :save hook
      def function(arglist)
        if (action_hook == :save) then
          owner_class.set_callback(:save, :around, self, :prepend => (true))
        else
          super
        end
      end
      
      # Runs state events around the machine's :save action
      def function(arglist, var)
        var.class.state_machines.transitions(var, action).perform { |arglist| yield }
      end
      
      # Creates a scope for finding records *with* a particular state or;Fi[I"# @rate_limited Yes;FI",# @authentication Requires user context;FI"g# @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.;FI"&# @return [Array<Twitter::Tweet>];FI";# @param options [Hash] A customizable set of options.;FI"{# @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.;FI"|# @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.;FI"w# @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.;FI"¥# @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.;FI"S# @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.;Fi[[	I"0data//twitter_proj/twitter/api/timelines.rb;TiZI"C      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/user_timeline
      # @note This method can only return up to 3,200 Tweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @example Return the 20 most recent retweets posted by the authenticating user;Fi[	@s	ioI"       #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :include_rts Specifies that the timeline should include native retweets in addition to regular tweets. Note: If you're using the trim_user parameter in conjunction with include_rts, the retweets will no longer contain a full user object.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.;Fi[	@s	iÅI"É      #
      # @see https://dev.twitter.com/docs/api/1.1/get/statuses/home_timeline
      # @note This method can only return up to 800 Tweets, including retweets.
      # @rate_limited Yes
      # @authentication Requires user context
      # @raise [Twitter::Error::Unauthorized] Error raised when supplied user credentials are not valid.
      # @return [Array<Twitter::Tweet>]
      # @param options [Hash] A customizable set of options.
      # @option options [Integer] :since_id Returns results with an ID greater than (that is, more recent than) the specified ID.
      # @option options [Integer] :max_id Returns results with an ID less than (that is, older than) or equal to the specified ID.
      # @option options [Integer] :count Specifies the number of records to retrieve. Must be less than or equal to 200.
      # @option options [Boolean, String, Integer] :trim_user Each tweet returned in a timeline will include a user object with only the author's numerical ID when set to true, 't' or 1.
      # @option options [Boolean, String, Integer] :exclude_replies This parameter will prevent replies from appearing in the returned timeline. Using exclude_replies with the count parameter will mean you will receive up-to count tweets - this is because the count parameter retrieves that many tweets before filtering out retweets and replies.
      # @option options [Boolean, String, Integer] :contributor_details Specifies that the contributors element should be enhanced to include the screen_name of the contributor.
      # @option options [Boolean, String, Integer] :include_entities The tweet entities node will be disincluded when set to false.;Fi
